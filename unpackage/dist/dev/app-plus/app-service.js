if (typeof Promise !== "undefined" && !Promise.prototype.finally) {
  Promise.prototype.finally = function(callback) {
    const promise = this.constructor;
    return this.then(
      (value) => promise.resolve(callback()).then(() => value),
      (reason) => promise.resolve(callback()).then(() => {
        throw reason;
      })
    );
  };
}
;
if (typeof uni !== "undefined" && uni && uni.requireGlobal) {
  const global2 = uni.requireGlobal();
  ArrayBuffer = global2.ArrayBuffer;
  Int8Array = global2.Int8Array;
  Uint8Array = global2.Uint8Array;
  Uint8ClampedArray = global2.Uint8ClampedArray;
  Int16Array = global2.Int16Array;
  Uint16Array = global2.Uint16Array;
  Int32Array = global2.Int32Array;
  Uint32Array = global2.Uint32Array;
  Float32Array = global2.Float32Array;
  Float64Array = global2.Float64Array;
  BigInt64Array = global2.BigInt64Array;
  BigUint64Array = global2.BigUint64Array;
}
;
if (uni.restoreGlobal) {
  uni.restoreGlobal(Vue, weex, plus, setTimeout, clearTimeout, setInterval, clearInterval);
}
(function(vue) {
  "use strict";
  const ON_SHOW = "onShow";
  const ON_HIDE = "onHide";
  const ON_LAUNCH = "onLaunch";
  const ON_LOAD = "onLoad";
  const ON_READY = "onReady";
  const ON_UNLOAD = "onUnload";
  const ON_PAGE_SCROLL = "onPageScroll";
  const ON_REACH_BOTTOM = "onReachBottom";
  const ON_PULL_DOWN_REFRESH = "onPullDownRefresh";
  const ON_SHARE_TIMELINE = "onShareTimeline";
  const ON_SHARE_APP_MESSAGE = "onShareAppMessage";
  function formatAppLog(type, filename, ...args) {
    if (uni.__log__) {
      uni.__log__(type, filename, ...args);
    } else {
      console[type].apply(console, [...args, filename]);
    }
  }
  function resolveEasycom(component, easycom) {
    return typeof component === "string" ? easycom : component;
  }
  const createLifeCycleHook = (lifecycle, flag = 0) => (hook, target = vue.getCurrentInstance()) => {
    !vue.isInSSRComponentSetup && vue.injectHook(lifecycle, hook, target);
  };
  const onShow = /* @__PURE__ */ createLifeCycleHook(
    ON_SHOW,
    1 | 2
    /* HookFlags.PAGE */
  );
  const onHide = /* @__PURE__ */ createLifeCycleHook(
    ON_HIDE,
    1 | 2
    /* HookFlags.PAGE */
  );
  const onLaunch = /* @__PURE__ */ createLifeCycleHook(
    ON_LAUNCH,
    1
    /* HookFlags.APP */
  );
  const onLoad = /* @__PURE__ */ createLifeCycleHook(
    ON_LOAD,
    2
    /* HookFlags.PAGE */
  );
  const onReady = /* @__PURE__ */ createLifeCycleHook(
    ON_READY,
    2
    /* HookFlags.PAGE */
  );
  const onUnload = /* @__PURE__ */ createLifeCycleHook(
    ON_UNLOAD,
    2
    /* HookFlags.PAGE */
  );
  const onPageScroll = /* @__PURE__ */ createLifeCycleHook(
    ON_PAGE_SCROLL,
    2
    /* HookFlags.PAGE */
  );
  const onReachBottom = /* @__PURE__ */ createLifeCycleHook(
    ON_REACH_BOTTOM,
    2
    /* HookFlags.PAGE */
  );
  const onPullDownRefresh = /* @__PURE__ */ createLifeCycleHook(
    ON_PULL_DOWN_REFRESH,
    2
    /* HookFlags.PAGE */
  );
  const onShareTimeline = /* @__PURE__ */ createLifeCycleHook(
    ON_SHARE_TIMELINE,
    2
    /* HookFlags.PAGE */
  );
  const onShareAppMessage = /* @__PURE__ */ createLifeCycleHook(
    ON_SHARE_APP_MESSAGE,
    2
    /* HookFlags.PAGE */
  );
  const websiteUrl = vue.ref("http://localhost:8080");
  const devUrl = "http://localhost:8080";
  const cnURL = "https://api.fantuanpu.com";
  const usURL = "https://us-api.dogdogdoll.com";
  const image1Url = "https://images1.fantuanpu.com/";
  const SHARE_H5_BASE = "https://m.dogdogdoll.com";
  const USE_HASH_ROUTER = true;
  const dogdogdollVersion = "1.2.1";
  let global$1 = vue.reactive({
    isLogin: false,
    userInfo: {}
  });
  function getAppShellVersion() {
    var _a;
    const info = ((_a = uni.getAppBaseInfo) == null ? void 0 : _a.call(uni)) || {};
    return info.appVersion || dogdogdollVersion;
  }
  function wechatSignLogin() {
    {
      uni.showToast({
        title: "请在微信小程序中登录",
        icon: "none"
      });
      return;
    }
  }
  function bindWechat() {
    {
      uni.showToast({
        title: "请在微信小程序中绑定",
        icon: "none"
      });
      return Promise.reject("请在微信小程序中绑定");
    }
  }
  function getUserInfo() {
    const token = uni.getStorageSync("token");
    formatAppLog("log", "at common/config.js:190", "token:", token);
    if (!token) {
      formatAppLog("log", "at common/config.js:192", "没有token，无法获取用户信息");
      clearUserInfo();
      return;
    }
    formatAppLog("log", "at common/config.js:196", "请求接口");
    uni.request({
      url: `${websiteUrl.value}/with-state/mine`,
      method: "GET",
      header: {
        Authorization: token
      },
      success: (res) => {
        const data = res.data.data;
        if (data) {
          formatAppLog("log", "at common/config.js:206", "获取用户信息成功,进行存储", data);
          saveUserInfo(data);
        } else {
          formatAppLog("log", "at common/config.js:209", "无法获取，清理用户状态");
          clearUserInfo();
        }
      },
      fail: (err) => {
        handleRequestError(err, "获取用户信息失败");
      }
    });
  }
  function asyncGetUserInfo() {
    return new Promise((resolve, reject) => {
      const token = uni.getStorageSync("token");
      if (!token) {
        clearUserInfo();
        resolve(null);
        return;
      }
      uni.request({
        url: `${websiteUrl.value}/with-state/mine`,
        method: "GET",
        header: {
          Authorization: token
        },
        success: (res) => {
          const data = res.data.data;
          if (data) {
            formatAppLog("log", "at common/config.js:240", "返回：", data);
            saveUserInfo(data);
            resolve(data);
          } else {
            clearUserInfo();
            resolve(null);
          }
          return data;
        },
        fail: (err) => {
          handleRequestError(err, "获取用户信息失败");
          resolve(null);
        }
      });
    });
  }
  function getScene() {
    const platform2 = "app";
    formatAppLog("log", "at common/config.js:313", "platform:", platform2);
    {
      const systemInfo2 = uni.getSystemInfoSync();
      if (systemInfo2.osName === "ios") {
        return 2;
      }
      if (systemInfo2.osName === "android") {
        return 3;
      }
    }
    return 1;
  }
  async function initLoginState() {
    const token = uni.getStorageSync("token");
    const userInfo = uni.getStorageSync("userInfo");
    if (token && userInfo) {
      global$1.isLogin = true;
      global$1.userInfo = userInfo;
      return userInfo;
    }
    if (token && !userInfo) {
      const data = await asyncGetUserInfo();
      return data;
    }
    clearUserInfo();
    return null;
  }
  function saveUserInfo(data) {
    uni.setStorageSync("userInfo", data);
    uni.setStorageSync("token", data.last_token);
    global$1.userInfo = data;
    global$1.isLogin = true;
  }
  function clearUserInfo() {
    uni.removeStorageSync("userInfo");
    global$1.userInfo = {};
    global$1.isLogin = false;
  }
  function handleRequestError(error2, message = "请求失败") {
    formatAppLog("error", "at common/config.js:434", error2);
    uni.showToast({
      title: message,
      icon: "none"
    });
  }
  let _sysInfoCache = null;
  function getSystemInfoCached() {
    if (!_sysInfoCache) {
      try {
        _sysInfoCache = uni.getSystemInfoSync();
      } catch (e2) {
        _sysInfoCache = {};
      }
    }
    return _sysInfoCache;
  }
  function getStatusBarHeight() {
    const s2 = getSystemInfoCached();
    return Number(s2 == null ? void 0 : s2.statusBarHeight) || 0;
  }
  function getNavBarHeight() {
    return 44;
  }
  function getWindowTop() {
    return getStatusBarHeight();
  }
  function getSafeBottom() {
    const s2 = getSystemInfoCached();
    if ((s2 == null ? void 0 : s2.safeAreaInsets) && typeof s2.safeAreaInsets.bottom === "number") {
      return s2.safeAreaInsets.bottom;
    }
    const sa = s2 == null ? void 0 : s2.safeArea;
    const sh = s2 == null ? void 0 : s2.screenHeight;
    return sa && sh ? Math.max(0, sh - sa.bottom) : 0;
  }
  function toPx(n2) {
    return `${Math.max(0, Math.round(Number(n2) || 0))}px`;
  }
  function getHeaderPlaceholderHeight() {
    return getStatusBarHeight();
  }
  function getFooterPlaceholderHeight() {
    return getSafeBottom();
  }
  const _export_sfc = (sfc, props) => {
    const target = sfc.__vccOpts || sfc;
    for (const [key, val] of props) {
      target[key] = val;
    }
    return target;
  };
  const _sfc_main$21 = {
    __name: "view-logs",
    setup(__props, { expose: __expose }) {
      __expose();
      const getUVId = () => {
        let uvid = uni.getStorageSync("ft_uvid");
        if (!uvid) {
          uvid = Math.floor(Math.random() * 2147483647) + 1;
          uni.setStorageSync("ft_uvid", uvid);
        }
        return uvid;
      };
      const getCurrentPagePath = () => {
        const pages2 = getCurrentPages();
        if (pages2.length === 0)
          return "";
        return pages2[pages2.length - 1].route || "";
      };
      const sendViewLog = async () => {
        let userInfo = uni.getStorageSync("userInfo");
        const logData = {
          scene: getScene(),
          // 访问场景
          path: getCurrentPagePath(),
          // 当前页面路径
          uid: userInfo ? userInfo.id : 0,
          // 用户ID（如果登录）
          uvid: getUVId(),
          // 独立访客ID
          version: dogdogdollVersion
          // 版本号
        };
        formatAppLog("log", "at components/view-logs/view-logs.vue:40", "logData:", logData);
        if (!logData.path) {
          formatAppLog("warn", "at components/view-logs/view-logs.vue:44", "无法获取当前页面路径");
          return;
        }
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/log/view`,
            method: "POST",
            data: logData,
            header: {
              "content-type": "application/json"
            }
          });
        } catch (error2) {
          formatAppLog("error", "at components/view-logs/view-logs.vue:58", "日志发送错误:", error2);
        }
      };
      vue.onMounted(() => {
        sendViewLog();
      });
      const __returned__ = { getUVId, getCurrentPagePath, sendViewLog, onMounted: vue.onMounted, get getScene() {
        return getScene;
      }, get dogdogdollVersion() {
        return dogdogdollVersion;
      }, get global() {
        return global$1;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$20(_ctx, _cache, $props, $setup, $data, $options) {
    return null;
  }
  const __easycom_0$9 = /* @__PURE__ */ _export_sfc(_sfc_main$21, [["render", _sfc_render$20], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/view-logs/view-logs.vue"]]);
  const fontData = [
    {
      "font_class": "arrow-down",
      "unicode": ""
    },
    {
      "font_class": "arrow-left",
      "unicode": ""
    },
    {
      "font_class": "arrow-right",
      "unicode": ""
    },
    {
      "font_class": "arrow-up",
      "unicode": ""
    },
    {
      "font_class": "auth",
      "unicode": ""
    },
    {
      "font_class": "auth-filled",
      "unicode": ""
    },
    {
      "font_class": "back",
      "unicode": ""
    },
    {
      "font_class": "bars",
      "unicode": ""
    },
    {
      "font_class": "calendar",
      "unicode": ""
    },
    {
      "font_class": "calendar-filled",
      "unicode": ""
    },
    {
      "font_class": "camera",
      "unicode": ""
    },
    {
      "font_class": "camera-filled",
      "unicode": ""
    },
    {
      "font_class": "cart",
      "unicode": ""
    },
    {
      "font_class": "cart-filled",
      "unicode": ""
    },
    {
      "font_class": "chat",
      "unicode": ""
    },
    {
      "font_class": "chat-filled",
      "unicode": ""
    },
    {
      "font_class": "chatboxes",
      "unicode": ""
    },
    {
      "font_class": "chatboxes-filled",
      "unicode": ""
    },
    {
      "font_class": "chatbubble",
      "unicode": ""
    },
    {
      "font_class": "chatbubble-filled",
      "unicode": ""
    },
    {
      "font_class": "checkbox",
      "unicode": ""
    },
    {
      "font_class": "checkbox-filled",
      "unicode": ""
    },
    {
      "font_class": "checkmarkempty",
      "unicode": ""
    },
    {
      "font_class": "circle",
      "unicode": ""
    },
    {
      "font_class": "circle-filled",
      "unicode": ""
    },
    {
      "font_class": "clear",
      "unicode": ""
    },
    {
      "font_class": "close",
      "unicode": ""
    },
    {
      "font_class": "closeempty",
      "unicode": ""
    },
    {
      "font_class": "cloud-download",
      "unicode": ""
    },
    {
      "font_class": "cloud-download-filled",
      "unicode": ""
    },
    {
      "font_class": "cloud-upload",
      "unicode": ""
    },
    {
      "font_class": "cloud-upload-filled",
      "unicode": ""
    },
    {
      "font_class": "color",
      "unicode": ""
    },
    {
      "font_class": "color-filled",
      "unicode": ""
    },
    {
      "font_class": "compose",
      "unicode": ""
    },
    {
      "font_class": "contact",
      "unicode": ""
    },
    {
      "font_class": "contact-filled",
      "unicode": ""
    },
    {
      "font_class": "down",
      "unicode": ""
    },
    {
      "font_class": "bottom",
      "unicode": ""
    },
    {
      "font_class": "download",
      "unicode": ""
    },
    {
      "font_class": "download-filled",
      "unicode": ""
    },
    {
      "font_class": "email",
      "unicode": ""
    },
    {
      "font_class": "email-filled",
      "unicode": ""
    },
    {
      "font_class": "eye",
      "unicode": ""
    },
    {
      "font_class": "eye-filled",
      "unicode": ""
    },
    {
      "font_class": "eye-slash",
      "unicode": ""
    },
    {
      "font_class": "eye-slash-filled",
      "unicode": ""
    },
    {
      "font_class": "fire",
      "unicode": ""
    },
    {
      "font_class": "fire-filled",
      "unicode": ""
    },
    {
      "font_class": "flag",
      "unicode": ""
    },
    {
      "font_class": "flag-filled",
      "unicode": ""
    },
    {
      "font_class": "folder-add",
      "unicode": ""
    },
    {
      "font_class": "folder-add-filled",
      "unicode": ""
    },
    {
      "font_class": "font",
      "unicode": ""
    },
    {
      "font_class": "forward",
      "unicode": ""
    },
    {
      "font_class": "gear",
      "unicode": ""
    },
    {
      "font_class": "gear-filled",
      "unicode": ""
    },
    {
      "font_class": "gift",
      "unicode": ""
    },
    {
      "font_class": "gift-filled",
      "unicode": ""
    },
    {
      "font_class": "hand-down",
      "unicode": ""
    },
    {
      "font_class": "hand-down-filled",
      "unicode": ""
    },
    {
      "font_class": "hand-up",
      "unicode": ""
    },
    {
      "font_class": "hand-up-filled",
      "unicode": ""
    },
    {
      "font_class": "headphones",
      "unicode": ""
    },
    {
      "font_class": "heart",
      "unicode": ""
    },
    {
      "font_class": "heart-filled",
      "unicode": ""
    },
    {
      "font_class": "help",
      "unicode": ""
    },
    {
      "font_class": "help-filled",
      "unicode": ""
    },
    {
      "font_class": "home",
      "unicode": ""
    },
    {
      "font_class": "home-filled",
      "unicode": ""
    },
    {
      "font_class": "image",
      "unicode": ""
    },
    {
      "font_class": "image-filled",
      "unicode": ""
    },
    {
      "font_class": "images",
      "unicode": ""
    },
    {
      "font_class": "images-filled",
      "unicode": ""
    },
    {
      "font_class": "info",
      "unicode": ""
    },
    {
      "font_class": "info-filled",
      "unicode": ""
    },
    {
      "font_class": "left",
      "unicode": ""
    },
    {
      "font_class": "link",
      "unicode": ""
    },
    {
      "font_class": "list",
      "unicode": ""
    },
    {
      "font_class": "location",
      "unicode": ""
    },
    {
      "font_class": "location-filled",
      "unicode": ""
    },
    {
      "font_class": "locked",
      "unicode": ""
    },
    {
      "font_class": "locked-filled",
      "unicode": ""
    },
    {
      "font_class": "loop",
      "unicode": ""
    },
    {
      "font_class": "mail-open",
      "unicode": ""
    },
    {
      "font_class": "mail-open-filled",
      "unicode": ""
    },
    {
      "font_class": "map",
      "unicode": ""
    },
    {
      "font_class": "map-filled",
      "unicode": ""
    },
    {
      "font_class": "map-pin",
      "unicode": ""
    },
    {
      "font_class": "map-pin-ellipse",
      "unicode": ""
    },
    {
      "font_class": "medal",
      "unicode": ""
    },
    {
      "font_class": "medal-filled",
      "unicode": ""
    },
    {
      "font_class": "mic",
      "unicode": ""
    },
    {
      "font_class": "mic-filled",
      "unicode": ""
    },
    {
      "font_class": "micoff",
      "unicode": ""
    },
    {
      "font_class": "micoff-filled",
      "unicode": ""
    },
    {
      "font_class": "minus",
      "unicode": ""
    },
    {
      "font_class": "minus-filled",
      "unicode": ""
    },
    {
      "font_class": "more",
      "unicode": ""
    },
    {
      "font_class": "more-filled",
      "unicode": ""
    },
    {
      "font_class": "navigate",
      "unicode": ""
    },
    {
      "font_class": "navigate-filled",
      "unicode": ""
    },
    {
      "font_class": "notification",
      "unicode": ""
    },
    {
      "font_class": "notification-filled",
      "unicode": ""
    },
    {
      "font_class": "paperclip",
      "unicode": ""
    },
    {
      "font_class": "paperplane",
      "unicode": ""
    },
    {
      "font_class": "paperplane-filled",
      "unicode": ""
    },
    {
      "font_class": "person",
      "unicode": ""
    },
    {
      "font_class": "person-filled",
      "unicode": ""
    },
    {
      "font_class": "personadd",
      "unicode": ""
    },
    {
      "font_class": "personadd-filled",
      "unicode": ""
    },
    {
      "font_class": "personadd-filled-copy",
      "unicode": ""
    },
    {
      "font_class": "phone",
      "unicode": ""
    },
    {
      "font_class": "phone-filled",
      "unicode": ""
    },
    {
      "font_class": "plus",
      "unicode": ""
    },
    {
      "font_class": "plus-filled",
      "unicode": ""
    },
    {
      "font_class": "plusempty",
      "unicode": ""
    },
    {
      "font_class": "pulldown",
      "unicode": ""
    },
    {
      "font_class": "pyq",
      "unicode": ""
    },
    {
      "font_class": "qq",
      "unicode": ""
    },
    {
      "font_class": "redo",
      "unicode": ""
    },
    {
      "font_class": "redo-filled",
      "unicode": ""
    },
    {
      "font_class": "refresh",
      "unicode": ""
    },
    {
      "font_class": "refresh-filled",
      "unicode": ""
    },
    {
      "font_class": "refreshempty",
      "unicode": ""
    },
    {
      "font_class": "reload",
      "unicode": ""
    },
    {
      "font_class": "right",
      "unicode": ""
    },
    {
      "font_class": "scan",
      "unicode": ""
    },
    {
      "font_class": "search",
      "unicode": ""
    },
    {
      "font_class": "settings",
      "unicode": ""
    },
    {
      "font_class": "settings-filled",
      "unicode": ""
    },
    {
      "font_class": "shop",
      "unicode": ""
    },
    {
      "font_class": "shop-filled",
      "unicode": ""
    },
    {
      "font_class": "smallcircle",
      "unicode": ""
    },
    {
      "font_class": "smallcircle-filled",
      "unicode": ""
    },
    {
      "font_class": "sound",
      "unicode": ""
    },
    {
      "font_class": "sound-filled",
      "unicode": ""
    },
    {
      "font_class": "spinner-cycle",
      "unicode": ""
    },
    {
      "font_class": "staff",
      "unicode": ""
    },
    {
      "font_class": "staff-filled",
      "unicode": ""
    },
    {
      "font_class": "star",
      "unicode": ""
    },
    {
      "font_class": "star-filled",
      "unicode": ""
    },
    {
      "font_class": "starhalf",
      "unicode": ""
    },
    {
      "font_class": "trash",
      "unicode": ""
    },
    {
      "font_class": "trash-filled",
      "unicode": ""
    },
    {
      "font_class": "tune",
      "unicode": ""
    },
    {
      "font_class": "tune-filled",
      "unicode": ""
    },
    {
      "font_class": "undo",
      "unicode": ""
    },
    {
      "font_class": "undo-filled",
      "unicode": ""
    },
    {
      "font_class": "up",
      "unicode": ""
    },
    {
      "font_class": "top",
      "unicode": ""
    },
    {
      "font_class": "upload",
      "unicode": ""
    },
    {
      "font_class": "upload-filled",
      "unicode": ""
    },
    {
      "font_class": "videocam",
      "unicode": ""
    },
    {
      "font_class": "videocam-filled",
      "unicode": ""
    },
    {
      "font_class": "vip",
      "unicode": ""
    },
    {
      "font_class": "vip-filled",
      "unicode": ""
    },
    {
      "font_class": "wallet",
      "unicode": ""
    },
    {
      "font_class": "wallet-filled",
      "unicode": ""
    },
    {
      "font_class": "weibo",
      "unicode": ""
    },
    {
      "font_class": "weixin",
      "unicode": ""
    }
  ];
  const getVal$1 = (val) => {
    const reg = /^[0-9]*$/g;
    return typeof val === "number" || reg.test(val) ? val + "px" : val;
  };
  const _sfc_main$20 = {
    name: "UniIcons",
    emits: ["click"],
    props: {
      type: {
        type: String,
        default: ""
      },
      color: {
        type: String,
        default: "#333333"
      },
      size: {
        type: [Number, String],
        default: 16
      },
      customPrefix: {
        type: String,
        default: ""
      },
      fontFamily: {
        type: String,
        default: ""
      }
    },
    data() {
      return {
        icons: fontData
      };
    },
    computed: {
      unicode() {
        let code = this.icons.find((v2) => v2.font_class === this.type);
        if (code) {
          return code.unicode;
        }
        return "";
      },
      iconSize() {
        return getVal$1(this.size);
      },
      styleObj() {
        if (this.fontFamily !== "") {
          return `color: ${this.color}; font-size: ${this.iconSize}; font-family: ${this.fontFamily};`;
        }
        return `color: ${this.color}; font-size: ${this.iconSize};`;
      }
    },
    methods: {
      _onClick(e2) {
        this.$emit("click", e2);
      }
    }
  };
  function _sfc_render$1$(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "text",
      {
        style: vue.normalizeStyle($options.styleObj),
        class: vue.normalizeClass(["uni-icons", ["uniui-" + $props.type, $props.customPrefix, $props.customPrefix ? $props.type : ""]]),
        onClick: _cache[0] || (_cache[0] = (...args) => $options._onClick && $options._onClick(...args))
      },
      [
        vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
      ],
      6
      /* CLASS, STYLE */
    );
  }
  const __easycom_1$f = /* @__PURE__ */ _export_sfc(_sfc_main$20, [["render", _sfc_render$1$], ["__scopeId", "data-v-d31e1c47"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-icons/components/uni-icons/uni-icons.vue"]]);
  class MPAnimation {
    constructor(options, _this) {
      this.options = options;
      this.animation = uni.createAnimation({
        ...options
      });
      this.currentStepAnimates = {};
      this.next = 0;
      this.$ = _this;
    }
    _nvuePushAnimates(type, args) {
      let aniObj = this.currentStepAnimates[this.next];
      let styles = {};
      if (!aniObj) {
        styles = {
          styles: {},
          config: {}
        };
      } else {
        styles = aniObj;
      }
      if (animateTypes1.includes(type)) {
        if (!styles.styles.transform) {
          styles.styles.transform = "";
        }
        let unit = "";
        if (type === "rotate") {
          unit = "deg";
        }
        styles.styles.transform += `${type}(${args + unit}) `;
      } else {
        styles.styles[type] = `${args}`;
      }
      this.currentStepAnimates[this.next] = styles;
    }
    _animateRun(styles = {}, config2 = {}) {
      let ref = this.$.$refs["ani"].ref;
      if (!ref)
        return;
      return new Promise((resolve, reject) => {
        nvueAnimation.transition(ref, {
          styles,
          ...config2
        }, (res) => {
          resolve();
        });
      });
    }
    _nvueNextAnimate(animates, step = 0, fn) {
      let obj = animates[step];
      if (obj) {
        let {
          styles,
          config: config2
        } = obj;
        this._animateRun(styles, config2).then(() => {
          step += 1;
          this._nvueNextAnimate(animates, step, fn);
        });
      } else {
        this.currentStepAnimates = {};
        typeof fn === "function" && fn();
        this.isEnd = true;
      }
    }
    step(config2 = {}) {
      this.animation.step(config2);
      return this;
    }
    run(fn) {
      this.$.animationData = this.animation.export();
      this.$.timer = setTimeout(() => {
        typeof fn === "function" && fn();
      }, this.$.durationTime);
    }
  }
  const animateTypes1 = [
    "matrix",
    "matrix3d",
    "rotate",
    "rotate3d",
    "rotateX",
    "rotateY",
    "rotateZ",
    "scale",
    "scale3d",
    "scaleX",
    "scaleY",
    "scaleZ",
    "skew",
    "skewX",
    "skewY",
    "translate",
    "translate3d",
    "translateX",
    "translateY",
    "translateZ"
  ];
  const animateTypes2 = ["opacity", "backgroundColor"];
  const animateTypes3 = ["width", "height", "left", "right", "top", "bottom"];
  animateTypes1.concat(animateTypes2, animateTypes3).forEach((type) => {
    MPAnimation.prototype[type] = function(...args) {
      this.animation[type](...args);
      return this;
    };
  });
  function createAnimation(option, _this) {
    if (!_this)
      return;
    clearTimeout(_this.timer);
    return new MPAnimation(option, _this);
  }
  const _sfc_main$1$ = {
    name: "uniTransition",
    emits: ["click", "change"],
    props: {
      show: {
        type: Boolean,
        default: false
      },
      modeClass: {
        type: [Array, String],
        default() {
          return "fade";
        }
      },
      duration: {
        type: Number,
        default: 300
      },
      styles: {
        type: Object,
        default() {
          return {};
        }
      },
      customClass: {
        type: String,
        default: ""
      },
      onceRender: {
        type: Boolean,
        default: false
      }
    },
    data() {
      return {
        isShow: false,
        transform: "",
        opacity: 0,
        animationData: {},
        durationTime: 300,
        config: {}
      };
    },
    watch: {
      show: {
        handler(newVal) {
          if (newVal) {
            this.open();
          } else {
            if (this.isShow) {
              this.close();
            }
          }
        },
        immediate: true
      }
    },
    computed: {
      // 生成样式数据
      stylesObject() {
        let styles = {
          ...this.styles,
          "transition-duration": this.duration / 1e3 + "s"
        };
        let transform = "";
        for (let i2 in styles) {
          let line = this.toLine(i2);
          transform += line + ":" + styles[i2] + ";";
        }
        return transform;
      },
      // 初始化动画条件
      transformStyles() {
        return "transform:" + this.transform + ";opacity:" + this.opacity + ";" + this.stylesObject;
      }
    },
    created() {
      this.config = {
        duration: this.duration,
        timingFunction: "ease",
        transformOrigin: "50% 50%",
        delay: 0
      };
      this.durationTime = this.duration;
    },
    methods: {
      /**
       *  ref 触发 初始化动画
       */
      init(obj = {}) {
        if (obj.duration) {
          this.durationTime = obj.duration;
        }
        this.animation = createAnimation(Object.assign(this.config, obj), this);
      },
      /**
       * 点击组件触发回调
       */
      onClick() {
        this.$emit("click", {
          detail: this.isShow
        });
      },
      /**
       * ref 触发 动画分组
       * @param {Object} obj
       */
      step(obj, config2 = {}) {
        if (!this.animation)
          return this;
        Object.keys(obj).forEach((key) => {
          const value = obj[key];
          if (typeof this.animation[key] === "function") {
            Array.isArray(value) ? this.animation[key](...value) : this.animation[key](value);
          }
        });
        this.animation.step(config2);
        return this;
      },
      /**
       *  ref 触发 执行动画
       */
      run(fn) {
        if (!this.animation)
          return;
        this.animation.run(fn);
      },
      // 开始过度动画
      open() {
        clearTimeout(this.timer);
        this.isShow = true;
        this.transform = this.styleInit(false).transform || "";
        this.opacity = this.styleInit(false).opacity || 0;
        this.$nextTick(() => {
          this.timer = setTimeout(() => {
            this.animation = createAnimation(this.config, this);
            this.tranfromInit(false).step();
            this.animation.run(() => {
              this.transform = "";
              this.opacity = this.styleInit(false).opacity || 1;
              this.$emit("change", {
                detail: this.isShow
              });
            });
          }, 80);
        });
      },
      // 关闭过度动画
      close(type) {
        if (!this.animation)
          return;
        this.tranfromInit(true).step().run(() => {
          this.isShow = false;
          this.animationData = null;
          this.animation = null;
          let { opacity, transform } = this.styleInit(false);
          this.opacity = opacity || 1;
          this.transform = transform;
          this.$emit("change", {
            detail: this.isShow
          });
        });
      },
      // 处理动画开始前的默认样式
      styleInit(type) {
        let styles = { transform: "", opacity: 1 };
        const buildStyle = (type2, mode) => {
          const value = this.animationType(type2)[mode];
          if (mode.startsWith("fade")) {
            styles.opacity = value;
          } else {
            styles.transform += value + " ";
          }
        };
        if (typeof this.modeClass === "string") {
          buildStyle(type, this.modeClass);
        } else {
          this.modeClass.forEach((mode) => buildStyle(type, mode));
        }
        return styles;
      },
      // 处理内置组合动画
      tranfromInit(type) {
        let buildTranfrom = (type2, mode) => {
          let aniNum = null;
          if (mode === "fade") {
            aniNum = type2 ? 0 : 1;
          } else {
            aniNum = type2 ? "-100%" : "0";
            if (mode === "zoom-in") {
              aniNum = type2 ? 0.8 : 1;
            }
            if (mode === "zoom-out") {
              aniNum = type2 ? 1.2 : 1;
            }
            if (mode === "slide-right") {
              aniNum = type2 ? "100%" : "0";
            }
            if (mode === "slide-bottom") {
              aniNum = type2 ? "100%" : "0";
            }
          }
          this.animation[this.animationMode()[mode]](aniNum);
        };
        if (typeof this.modeClass === "string") {
          buildTranfrom(type, this.modeClass);
        } else {
          this.modeClass.forEach((mode) => {
            buildTranfrom(type, mode);
          });
        }
        return this.animation;
      },
      animationType(type) {
        return {
          fade: type ? 1 : 0,
          "slide-top": `translateY(${type ? "0" : "-100%"})`,
          "slide-right": `translateX(${type ? "0" : "100%"})`,
          "slide-bottom": `translateY(${type ? "0" : "100%"})`,
          "slide-left": `translateX(${type ? "0" : "-100%"})`,
          "zoom-in": `scaleX(${type ? 1 : 0.8}) scaleY(${type ? 1 : 0.8})`,
          "zoom-out": `scaleX(${type ? 1 : 1.2}) scaleY(${type ? 1 : 1.2})`
        };
      },
      // 内置动画类型与实际动画对应字典
      animationMode() {
        return {
          fade: "opacity",
          "slide-top": "translateY",
          "slide-right": "translateX",
          "slide-bottom": "translateY",
          "slide-left": "translateX",
          "zoom-in": "scale",
          "zoom-out": "scale"
        };
      },
      // 驼峰转中横线
      toLine(name) {
        return name.replace(/([A-Z])/g, "-$1").toLowerCase();
      }
    }
  };
  function _sfc_render$1_(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.withDirectives((vue.openBlock(), vue.createElementBlock("view", {
      ref: "ani",
      animation: $data.animationData,
      class: vue.normalizeClass($props.customClass),
      style: vue.normalizeStyle($options.transformStyles),
      onClick: _cache[0] || (_cache[0] = (...args) => $options.onClick && $options.onClick(...args))
    }, [
      vue.renderSlot(_ctx.$slots, "default")
    ], 14, ["animation"])), [
      [vue.vShow, $data.isShow]
    ]);
  }
  const __easycom_3$9 = /* @__PURE__ */ _export_sfc(_sfc_main$1$, [["render", _sfc_render$1_], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-transition/components/uni-transition/uni-transition.vue"]]);
  const _sfc_main$1_ = {
    __name: "common-modal",
    props: {
      visible: Boolean,
      top: {
        type: [String, Number],
        default: "30%"
      },
      width: {
        type: [String, Number],
        default: "80%"
      },
      height: {
        type: [String, Number],
        default: "auto"
      },
      closeable: {
        type: Boolean,
        default: true
      }
    },
    emits: ["update:visible"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const isApp = vue.ref(false);
      const props = __props;
      const modeClass = vue.ref(["fade", "zoom-in"]);
      const emit = __emit;
      const maskStyle = vue.computed(() => {
        return {
          backgroundColor: isApp.value ? "rgba(0, 0, 0, 0)" : "rgba(0, 0, 0, 0.5)"
        };
      });
      const containerStyle = vue.computed(() => ({
        top: formatValue(props.top),
        width: formatValue(props.width),
        height: formatValue(props.height)
      }));
      const moveHandle = () => false;
      function formatValue(val) {
        if (typeof val === "number")
          return `${val}px`;
        if (typeof val === "string" && /^\d+(\.\d+)?$/.test(val)) {
          return `${val}px`;
        }
        return val;
      }
      const closeModal = () => {
        if (props.closeable) {
          emit("update:visible", false);
        }
      };
      vue.onMounted(() => {
        const scene = getScene();
        isApp.value = scene === 2 || scene === 3;
      });
      const __returned__ = { isApp, props, modeClass, emit, maskStyle, containerStyle, moveHandle, formatValue, closeModal, computed: vue.computed, ref: vue.ref, onMounted: vue.onMounted, get getScene() {
        return getScene;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1Z(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_transition = resolveEasycom(vue.resolveDynamicComponent("uni-transition"), __easycom_3$9);
    return $props.visible ? (vue.openBlock(), vue.createElementBlock(
      "view",
      {
        key: 0,
        class: "modal-mask",
        onClick: vue.withModifiers($setup.closeModal, ["stop"]),
        onTouchmove: vue.withModifiers($setup.moveHandle, ["stop", "prevent"]),
        style: vue.normalizeStyle($setup.maskStyle)
      },
      [
        vue.createVNode(_component_uni_transition, {
          "mode-class": $setup.modeClass,
          show: $props.visible
        }, {
          default: vue.withCtx(() => [
            vue.createElementVNode(
              "view",
              {
                class: "modal-container",
                style: vue.normalizeStyle($setup.containerStyle),
                onClick: _cache[0] || (_cache[0] = vue.withModifiers(() => {
                }, ["stop"]))
              },
              [
                vue.createElementVNode("view", { class: "modal-content" }, [
                  vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
                ])
              ],
              4
              /* STYLE */
            )
          ]),
          _: 3
          /* FORWARDED */
        }, 8, ["mode-class", "show"])
      ],
      36
      /* STYLE, NEED_HYDRATION */
    )) : vue.createCommentVNode("v-if", true);
  }
  const __easycom_2$b = /* @__PURE__ */ _export_sfc(_sfc_main$1_, [["render", _sfc_render$1Z], ["__scopeId", "data-v-d7656854"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-modal/common-modal.vue"]]);
  const _imports_4$1 = "/static/new-icon/write.gif";
  const storageKey$1 = "privacyAgreementStatus";
  const _sfc_main$1Z = {
    __name: "privacy-permission-modal",
    setup(__props, { expose: __expose }) {
      __expose();
      const showModal = vue.ref(false);
      const hasAgreed = () => {
        let storageContent = uni.getStorageSync(storageKey$1);
        formatAppLog("log", "at components/privacy-permission-modal/privacy-permission-modal.vue:53", storageContent);
        return storageContent === "agreed";
      };
      const checkAndShow = () => {
        if (hasAgreed()) {
          formatAppLog("log", "at components/privacy-permission-modal/privacy-permission-modal.vue:60", "用户已经同意过隐私政策");
          return;
        }
        const scene = getScene();
        if (scene !== 2 && scene !== 3) {
          formatAppLog("log", "at components/privacy-permission-modal/privacy-permission-modal.vue:65", "并非安卓或iOS客户端：", scene);
          return;
        }
        setTimeout(() => {
          showModal.value = true;
        }, 1e3);
      };
      const goToPrivacy = () => {
        uni.navigateTo({
          url: "/pages/private/private"
        });
      };
      const goToPermissions = () => {
        uni.navigateTo({
          url: "/pages/permission/permission"
        });
      };
      const handleAgree = () => {
        uni.setStorageSync(storageKey$1, "agreed");
        showModal.value = false;
      };
      const handleDisagree = () => {
        uni.exitApp();
      };
      vue.onMounted(() => {
        checkAndShow();
      });
      const __returned__ = { showModal, storageKey: storageKey$1, hasAgreed, checkAndShow, goToPrivacy, goToPermissions, handleAgree, handleDisagree, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get getScene() {
        return getScene;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1Y(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createBlock(_component_common_modal, {
      visible: $setup.showModal,
      "onUpdate:visible": _cache[0] || (_cache[0] = ($event) => $setup.showModal = $event),
      top: "20vh",
      height: "auto",
      closeable: false
    }, {
      default: vue.withCtx(() => [
        vue.createElementVNode("view", { class: "privacy-container" }, [
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode("image", {
              src: _imports_4$1,
              class: "logo"
            }),
            vue.createElementVNode("text", { class: "title" }, "隐私与权限说明")
          ]),
          vue.createElementVNode("view", { class: "content" }, [
            vue.createElementVNode("text", { class: "desc" }, "欢迎使用我们的应用，为了提供更好的服务，我们需要获取相关权限并说明隐私政策："),
            vue.createElementVNode("view", {
              class: "policy-item",
              onClick: $setup.goToPrivacy
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "info",
                size: "18",
                color: "#80afff"
              }),
              vue.createElementVNode("text", { class: "policy-text" }, "《隐私政策》"),
              vue.createVNode(_component_uni_icons, {
                type: "arrowright",
                size: "16",
                color: "#999"
              })
            ]),
            vue.createElementVNode("view", {
              class: "policy-item",
              onClick: $setup.goToPermissions
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "locked",
                size: "18",
                color: "#80afff"
              }),
              vue.createElementVNode("text", { class: "policy-text" }, "《权限说明》"),
              vue.createVNode(_component_uni_icons, {
                type: "arrowright",
                size: "16",
                color: "#999"
              })
            ])
          ]),
          vue.createElementVNode("view", { class: "footer" }, [
            vue.createElementVNode("button", {
              class: "btn refuse",
              onClick: $setup.handleDisagree
            }, "拒绝并退出"),
            vue.createElementVNode("button", {
              class: "btn agree",
              onClick: $setup.handleAgree
            }, "同意并继续")
          ])
        ])
      ]),
      _: 1
      /* STABLE */
    }, 8, ["visible"]);
  }
  const __easycom_1$e = /* @__PURE__ */ _export_sfc(_sfc_main$1Z, [["render", _sfc_render$1Y], ["__scopeId", "data-v-c6b543e1"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/privacy-permission-modal/privacy-permission-modal.vue"]]);
  const _imports_0$j = "/static/loading.gif";
  const _sfc_main$1Y = {
    __name: "loading-toast",
    props: {
      show: {
        type: Boolean,
        default: false
      },
      text: {
        type: String,
        default: "To Connect The Love..."
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const __returned__ = { props, ref: vue.ref };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1X(_ctx, _cache, $props, $setup, $data, $options) {
    return $setup.props.show ? (vue.openBlock(), vue.createElementBlock("view", {
      key: 0,
      class: "loading-container"
    }, [
      vue.createElementVNode("view", { class: "loading-mask" }),
      vue.createElementVNode("view", { class: "loading-content" }, [
        vue.createElementVNode("image", {
          class: "loading-gif",
          src: _imports_0$j,
          mode: "aspectFit"
        }),
        $props.text ? (vue.openBlock(), vue.createElementBlock(
          "text",
          {
            key: 0,
            class: "loading-text"
          },
          vue.toDisplayString($props.text),
          1
          /* TEXT */
        )) : vue.createCommentVNode("v-if", true)
      ])
    ])) : vue.createCommentVNode("v-if", true);
  }
  const __easycom_3$8 = /* @__PURE__ */ _export_sfc(_sfc_main$1Y, [["render", _sfc_render$1X], ["__scopeId", "data-v-3ed615fd"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/loading-toast/loading-toast.vue"]]);
  const _sfc_main$1X = {
    __name: "server-selector",
    emits: ["server-change"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const servers = vue.reactive([
        {
          name: "中国",
          url: cnURL,
          ping: -1
          // -1: 测试中, -2: 失败, >0: 延迟
        },
        {
          name: "美国",
          url: usURL,
          ping: -1
        },
        {
          name: "Localhost",
          url: devUrl,
          ping: -1
        }
      ]);
      const showModal = vue.ref(false);
      const selectedServer = vue.ref("");
      const currentPing = vue.ref(-1);
      let pingInterval = null;
      const emit = __emit;
      vue.onMounted(() => {
        const savedServer = uni.getStorageSync("selectedServer");
        selectedServer.value = savedServer || cnURL;
        websiteUrl.value = selectedServer.value;
        testCurrentServer();
        startPingInterval();
      });
      vue.onUnmounted(() => {
        clearInterval(pingInterval);
      });
      const startPingInterval = () => {
        pingInterval = setInterval(() => {
          testCurrentServer();
        }, 5e3);
      };
      const testCurrentServer = async () => {
        try {
          const startTime = Date.now();
          await pingServer(selectedServer.value);
          currentPing.value = Date.now() - startTime;
        } catch (error2) {
          currentPing.value = -2;
        }
      };
      vue.watch(showModal, (newVal) => {
        if (newVal) {
          pingAllServers();
        }
      });
      const pingAllServers = async () => {
        for (const server of servers) {
          server.ping = -1;
          try {
            const startTime = Date.now();
            await pingServer(server.url);
            server.ping = Date.now() - startTime;
            if (server.url === selectedServer.value) {
              currentPing.value = server.ping;
            }
          } catch (error2) {
            server.ping = -2;
            if (server.url === selectedServer.value) {
              currentPing.value = -2;
            }
          }
        }
      };
      const pingServer = (url) => {
        return new Promise((resolve, reject) => {
          uni.request({
            url: `${url}/ping`,
            method: "GET",
            timeout: 5e3,
            // 5秒超时
            success: () => resolve(),
            fail: () => reject()
          });
        });
      };
      const selectServer = (server) => {
        if (server.ping === -2) {
          uni.showToast({
            title: "服务器连接失败，无法选择",
            icon: "none"
          });
          return;
        }
        selectedServer.value = server.url;
        websiteUrl.value = server.url;
        uni.setStorageSync("selectedServer", server.url);
        currentPing.value = server.ping;
        uni.showToast({
          title: `已切换到${server.name}`,
          icon: "success"
        });
        showModal.value = false;
        emit("server-change", {
          server,
          ping: server.ping
        });
      };
      const __returned__ = { servers, showModal, selectedServer, currentPing, get pingInterval() {
        return pingInterval;
      }, set pingInterval(v2) {
        pingInterval = v2;
      }, emit, startPingInterval, testCurrentServer, pingAllServers, pingServer, selectServer, ref: vue.ref, reactive: vue.reactive, watch: vue.watch, onMounted: vue.onMounted, onUnmounted: vue.onUnmounted, get cnURL() {
        return cnURL;
      }, get usURL() {
        return usURL;
      }, get devUrl() {
        return devUrl;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1W(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("view", {
          class: "server-selector-trigger",
          onClick: _cache[0] || (_cache[0] = ($event) => $setup.showModal = true)
        }, [
          vue.createVNode(_component_uni_icons, {
            type: "paperplane",
            size: "24",
            color: "#5db7ff"
          }),
          vue.createElementVNode(
            "text",
            null,
            vue.toDisplayString($setup.currentPing >= 0 ? $setup.currentPing + "ms" : "9999ms"),
            1
            /* TEXT */
          )
        ]),
        vue.createVNode(_component_common_modal, {
          visible: $setup.showModal,
          "onUpdate:visible": _cache[1] || (_cache[1] = ($event) => $setup.showModal = $event),
          top: "15vh"
        }, {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "server-modal-content" }, [
              vue.createElementVNode("view", { class: "modal-header" }, [
                vue.createElementVNode("text", { class: "title" }, "选择服务器"),
                vue.createElementVNode(
                  "text",
                  { class: "subtitle" },
                  "点击服务器可切换，当前延迟：" + vue.toDisplayString($setup.currentPing >= 0 ? $setup.currentPing + "ms" : "-1ms"),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "server-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.servers, (server, index) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: index,
                      class: vue.normalizeClass(["server-item", {
                        active: server.url === $setup.selectedServer,
                        loading: server.ping === -1
                      }]),
                      onClick: ($event) => $setup.selectServer(server)
                    }, [
                      vue.createElementVNode("view", { class: "server-info" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "name" },
                          vue.toDisplayString(server.name),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "ping-info" }, [
                        server.ping === -1 ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                          key: 0,
                          type: "spinner-cycle",
                          size: "16",
                          color: "#999",
                          class: "spinner"
                        })) : server.ping === -2 ? (vue.openBlock(), vue.createElementBlock("text", {
                          key: 1,
                          class: "ping-error"
                        }, "请求失败")) : (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 2,
                            class: "ping-value"
                          },
                          vue.toDisplayString(server.ping) + " ms",
                          1
                          /* TEXT */
                        ))
                      ])
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["visible"])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const __easycom_3$7 = /* @__PURE__ */ _export_sfc(_sfc_main$1X, [["render", _sfc_render$1W], ["__scopeId", "data-v-ebbb3777"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/server-selector/server-selector.vue"]]);
  const zStatic = {
    base64Arrow: "data:image/png;base64,",
    base64ArrowWhite: "data:image/png;base64,",
    base64Flower: "data:image/png;base64,",
    base64FlowerWhite: "data:image/png;base64,",
    base64Success: "data:image/png;base64,",
    base64SuccessWhite: "data:image/png;base64,",
    base64Empty: "data:image/png;base64,",
    base64Error: "data:image/png;base64,",
    base64BackToTop: "data:image/png;base64,"
  };
  const _sfc_main$1W = {
    name: "z-paging-empty-view",
    data() {
      return {};
    },
    props: {
      // 空数据描述文字
      emptyViewText: {
        type: String,
        default: "没有数据哦~"
      },
      // 空数据图片
      emptyViewImg: {
        type: String,
        default: ""
      },
      // 是否显示空数据图重新加载按钮
      showEmptyViewReload: {
        type: Boolean,
        default: false
      },
      // 空数据点击重新加载文字
      emptyViewReloadText: {
        type: String,
        default: "重新加载"
      },
      // 是否是加载失败
      isLoadFailed: {
        type: Boolean,
        default: false
      },
      // 空数据图样式
      emptyViewStyle: {
        type: Object,
        default: function() {
          return {};
        }
      },
      // 空数据图img样式
      emptyViewImgStyle: {
        type: Object,
        default: function() {
          return {};
        }
      },
      // 空数据图描述文字样式
      emptyViewTitleStyle: {
        type: Object,
        default: function() {
          return {};
        }
      },
      // 空数据图重新加载按钮样式
      emptyViewReloadStyle: {
        type: Object,
        default: function() {
          return {};
        }
      },
      // 空数据图z-index
      emptyViewZIndex: {
        type: Number,
        default: 9
      },
      // 空数据图片是否使用fixed布局并铺满z-paging
      emptyViewFixed: {
        type: Boolean,
        default: true
      },
      // 空数据图中布局的单位，默认为rpx
      unit: {
        type: String,
        default: "rpx"
      }
    },
    computed: {
      emptyImg() {
        return this.isLoadFailed ? zStatic.base64Error : zStatic.base64Empty;
      },
      finalEmptyViewStyle() {
        this.emptyViewStyle["z-index"] = this.emptyViewZIndex;
        return this.emptyViewStyle;
      }
    },
    methods: {
      // 点击了reload按钮
      reloadClick() {
        this.$emit("reload");
      },
      // 点击了空数据view
      emptyViewClick() {
        this.$emit("viewClick");
      }
    }
  };
  function _sfc_render$1V(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: vue.normalizeClass({ "zp-container": true, "zp-container-fixed": $props.emptyViewFixed }),
        style: vue.normalizeStyle([$options.finalEmptyViewStyle]),
        onClick: _cache[1] || (_cache[1] = (...args) => $options.emptyViewClick && $options.emptyViewClick(...args))
      },
      [
        vue.createElementVNode("view", { class: "zp-main" }, [
          !$props.emptyViewImg.length ? (vue.openBlock(), vue.createElementBlock("image", {
            key: 0,
            class: vue.normalizeClass({ "zp-main-image-rpx": $props.unit === "rpx", "zp-main-image-px": $props.unit === "px" }),
            style: vue.normalizeStyle([$props.emptyViewImgStyle]),
            src: $options.emptyImg
          }, null, 14, ["src"])) : (vue.openBlock(), vue.createElementBlock("image", {
            key: 1,
            class: vue.normalizeClass({ "zp-main-image-rpx": $props.unit === "rpx", "zp-main-image-px": $props.unit === "px" }),
            mode: "aspectFit",
            style: vue.normalizeStyle([$props.emptyViewImgStyle]),
            src: $props.emptyViewImg
          }, null, 14, ["src"])),
          vue.createElementVNode(
            "text",
            {
              class: vue.normalizeClass(["zp-main-title", { "zp-main-title-rpx": $props.unit === "rpx", "zp-main-title-px": $props.unit === "px" }]),
              style: vue.normalizeStyle([$props.emptyViewTitleStyle])
            },
            vue.toDisplayString($props.emptyViewText),
            7
            /* TEXT, CLASS, STYLE */
          ),
          $props.showEmptyViewReload ? (vue.openBlock(), vue.createElementBlock(
            "text",
            {
              key: 2,
              class: vue.normalizeClass({ "zp-main-error-btn": true, "zp-main-error-btn-rpx": $props.unit === "rpx", "zp-main-error-btn-px": $props.unit === "px" }),
              style: vue.normalizeStyle([$props.emptyViewReloadStyle]),
              onClick: _cache[0] || (_cache[0] = vue.withModifiers((...args) => $options.reloadClick && $options.reloadClick(...args), ["stop"]))
            },
            vue.toDisplayString($props.emptyViewReloadText),
            7
            /* TEXT, CLASS, STYLE */
          )) : vue.createCommentVNode("v-if", true)
        ])
      ],
      6
      /* CLASS, STYLE */
    );
  }
  const __easycom_0$8 = /* @__PURE__ */ _export_sfc(_sfc_main$1W, [["render", _sfc_render$1V], ["__scopeId", "data-v-b7999e14"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/z-paging/components/z-paging-empty-view/z-paging-empty-view.vue"]]);
  const c$1 = {
    // 当前版本号
    version: "2.8.8",
    // 延迟操作的通用时间
    delayTime: 100,
    // 请求失败时候全局emit使用的key
    errorUpdateKey: "z-paging-error-emit",
    // 全局emit complete的key
    completeUpdateKey: "z-paging-complete-emit",
    // z-paging缓存的前缀key
    cachePrefixKey: "z-paging-cache",
    // 虚拟列表中列表index的key
    listCellIndexKey: "zp_index",
    // 虚拟列表中列表的唯一key
    listCellIndexUniqueKey: "zp_unique_index"
  };
  const zLocalConfig = {};
  const storageKey = "Z-PAGING-REFRESHER-TIME-STORAGE-KEY";
  let config$1 = null;
  let configLoaded = false;
  let cachedSystemInfo = null;
  const timeoutMap = {};
  function gc(key, defaultValue) {
    return () => {
      _handleDefaultConfig();
      if (!config$1)
        return defaultValue;
      const value = config$1[key];
      return value === void 0 ? defaultValue : value;
    };
  }
  function getTouch(e2) {
    let touch = null;
    if (e2.touches && e2.touches.length) {
      touch = e2.touches[0];
    } else if (e2.changedTouches && e2.changedTouches.length) {
      touch = e2.changedTouches[0];
    } else if (e2.datail && e2.datail != {}) {
      touch = e2.datail;
    } else {
      return { touchX: 0, touchY: 0 };
    }
    return {
      touchX: touch.clientX,
      touchY: touch.clientY
    };
  }
  function getTouchFromZPaging(target) {
    if (target && target.tagName && target.tagName !== "BODY" && target.tagName !== "UNI-PAGE-BODY") {
      const classList = target.classList;
      if (classList && classList.contains("z-paging-content")) {
        return {
          isFromZp: true,
          isPageScroll: classList.contains("z-paging-content-page"),
          isReachedTop: classList.contains("z-paging-reached-top"),
          isUseChatRecordMode: classList.contains("z-paging-use-chat-record-mode")
        };
      } else {
        return getTouchFromZPaging(target.parentNode);
      }
    } else {
      return { isFromZp: false };
    }
  }
  function getParent(parent) {
    if (!parent)
      return null;
    if (parent.$refs.paging)
      return parent;
    return getParent(parent.$parent);
  }
  function consoleErr(err) {
    formatAppLog("error", "at uni_modules/z-paging/components/z-paging/js/z-paging-utils.js:73", `[z-paging]${err}`);
  }
  function delay(callback, ms2 = c$1.delayTime, key) {
    const timeout = setTimeout(callback, ms2);
    if (!!key) {
      timeoutMap[key] && clearTimeout(timeoutMap[key]);
      timeoutMap[key] = timeout;
    }
    return timeout;
  }
  function setRefesrherTime(time, key) {
    const datas = getRefesrherTime() || {};
    datas[key] = time;
    uni.setStorageSync(storageKey, datas);
  }
  function getRefesrherTime() {
    return uni.getStorageSync(storageKey);
  }
  function getRefesrherTimeByKey(key) {
    const datas = getRefesrherTime();
    return datas && datas[key] ? datas[key] : null;
  }
  function getRefesrherFormatTimeByKey(key, textMap) {
    const time = getRefesrherTimeByKey(key);
    const timeText = time ? _timeFormat(time, textMap) : textMap.none;
    return `${textMap.title}${timeText}`;
  }
  function convertToPx(text) {
    const dataType = Object.prototype.toString.call(text);
    if (dataType === "[object Number]")
      return text;
    let isRpx = false;
    if (text.indexOf("rpx") !== -1 || text.indexOf("upx") !== -1) {
      text = text.replace("rpx", "").replace("upx", "");
      isRpx = true;
    } else if (text.indexOf("px") !== -1) {
      text = text.replace("px", "");
    }
    if (!isNaN(text)) {
      if (isRpx)
        return Number(rpx2px(text));
      return Number(text);
    }
    return 0;
  }
  function rpx2px(rpx) {
    return uni.upx2px(rpx);
  }
  function getSystemInfoSync(useCache = false) {
    if (useCache && cachedSystemInfo) {
      return cachedSystemInfo;
    }
    const infoTypes = ["DeviceInfo", "AppBaseInfo", "WindowInfo"];
    const { deviceInfo, appBaseInfo, windowInfo } = infoTypes.reduce((acc, key) => {
      const method = `get${key}`;
      if (uni[method] && uni.canIUse(method)) {
        acc[key.charAt(0).toLowerCase() + key.slice(1)] = uni[method]();
      }
      return acc;
    }, {});
    if (deviceInfo && appBaseInfo && windowInfo) {
      cachedSystemInfo = { ...deviceInfo, ...appBaseInfo, ...windowInfo };
    } else {
      cachedSystemInfo = uni.getSystemInfoSync();
    }
    return cachedSystemInfo;
  }
  function getTime() {
    return (/* @__PURE__ */ new Date()).getTime();
  }
  function getInstanceId() {
    const s2 = [];
    const hexDigits = "0123456789abcdef";
    for (let i2 = 0; i2 < 10; i2++) {
      s2[i2] = hexDigits.substr(Math.floor(Math.random() * 16), 1);
    }
    return s2.join("") + getTime();
  }
  function wait(ms2) {
    return new Promise((resolve) => {
      setTimeout(resolve, ms2);
    });
  }
  function isPromise(func) {
    return Object.prototype.toString.call(func) === "[object Promise]";
  }
  function addUnit(value, unit) {
    if (Object.prototype.toString.call(value) === "[object String]") {
      let tempValue = value;
      tempValue = tempValue.replace("rpx", "").replace("upx", "").replace("px", "");
      if (value.indexOf("rpx") === -1 && value.indexOf("upx") === -1 && value.indexOf("px") !== -1) {
        tempValue = parseFloat(tempValue) * 2;
      }
      value = tempValue;
    }
    return unit === "rpx" ? value + "rpx" : value / 2 + "px";
  }
  function deepCopy(obj) {
    if (typeof obj !== "object" || obj === null)
      return obj;
    let newObj = Array.isArray(obj) ? [] : {};
    for (let key in obj) {
      if (obj.hasOwnProperty(key)) {
        newObj[key] = deepCopy(obj[key]);
      }
    }
    return newObj;
  }
  function useBufferedInsert(fn, delay2 = 50) {
    let buffer = [];
    let timer = null;
    let latestArgs = [];
    return function insertBuffered(data, ...args) {
      const newData = Object.prototype.toString.call(data) !== "[object Array]" ? [data] : data;
      buffer.push(...newData);
      latestArgs = args;
      if (!timer) {
        timer = setTimeout(() => {
          fn(buffer.length === 1 ? buffer[0] : buffer, ...latestArgs);
          buffer = [];
          timer = null;
        }, buffer.length === 1 ? 10 : delay2);
      }
    };
  }
  function _handleDefaultConfig() {
    if (configLoaded)
      return;
    if (zLocalConfig && Object.keys(zLocalConfig).length) {
      config$1 = zLocalConfig;
    }
    if (!config$1 && uni.$zp) {
      config$1 = uni.$zp.config;
    }
    config$1 = config$1 ? Object.keys(config$1).reduce((result, key) => {
      result[_toCamelCase(key)] = config$1[key];
      return result;
    }, {}) : null;
    configLoaded = true;
  }
  function _timeFormat(time, textMap) {
    const date = new Date(time);
    const currentDate = /* @__PURE__ */ new Date();
    const dateDay = new Date(time).setHours(0, 0, 0, 0);
    const currentDateDay = (/* @__PURE__ */ new Date()).setHours(0, 0, 0, 0);
    const disTime = dateDay - currentDateDay;
    let dayStr = "";
    const timeStr = _dateTimeFormat(date);
    if (disTime === 0) {
      dayStr = textMap.today;
    } else if (disTime === -864e5) {
      dayStr = textMap.yesterday;
    } else {
      dayStr = _dateDayFormat(date, date.getFullYear() !== currentDate.getFullYear());
    }
    return `${dayStr} ${timeStr}`;
  }
  function _dateDayFormat(date, showYear = true) {
    const year = date.getFullYear();
    const month = date.getMonth() + 1;
    const day = date.getDate();
    return showYear ? `${year}-${_fullZeroToTwo(month)}-${_fullZeroToTwo(day)}` : `${_fullZeroToTwo(month)}-${_fullZeroToTwo(day)}`;
  }
  function _dateTimeFormat(date) {
    const hour = date.getHours();
    const minute = date.getMinutes();
    return `${_fullZeroToTwo(hour)}:${_fullZeroToTwo(minute)}`;
  }
  function _fullZeroToTwo(str) {
    str = str.toString();
    return str.length === 1 ? "0" + str : str;
  }
  function _toCamelCase(value) {
    return value.replace(/-([a-z])/g, (_2, group1) => group1.toUpperCase());
  }
  const u$1 = {
    gc,
    setRefesrherTime,
    getRefesrherFormatTimeByKey,
    getTouch,
    getTouchFromZPaging,
    getParent,
    convertToPx,
    getTime,
    getInstanceId,
    consoleErr,
    delay,
    wait,
    isPromise,
    addUnit,
    deepCopy,
    rpx2px,
    getSystemInfoSync,
    useBufferedInsert
  };
  const Enum = {
    // 当前加载类型 refresher:下拉刷新 load-more:上拉加载更多
    LoadingType: {
      Refresher: "refresher",
      LoadMore: "load-more"
    },
    // 下拉刷新状态 default:默认状态 release-to-refresh:松手立即刷新 loading:刷新中 complete:刷新结束 go-f2:松手进入二楼
    Refresher: {
      Default: "default",
      ReleaseToRefresh: "release-to-refresh",
      Loading: "loading",
      Complete: "complete",
      GoF2: "go-f2"
    },
    // 底部加载更多状态 default:默认状态 loading:加载中 no-more:没有更多数据 fail:加载失败
    More: {
      Default: "default",
      Loading: "loading",
      NoMore: "no-more",
      Fail: "fail"
    },
    // @query触发来源 user-pull-down:用户主动下拉刷新 reload:通过reload触发 refresh:通过refresh触发 load-more:通过滚动到底部加载更多或点击底部加载更多触发
    QueryFrom: {
      UserPullDown: "user-pull-down",
      Reload: "reload",
      Refresh: "refresh",
      LoadMore: "load-more"
    },
    // 虚拟列表cell高度模式
    CellHeightMode: {
      // 固定高度
      Fixed: "fixed",
      // 动态高度
      Dynamic: "dynamic"
    },
    // 列表缓存模式
    CacheMode: {
      // 默认模式，只会缓存一次
      Default: "default",
      // 总是缓存，每次列表刷新(下拉刷新、调用reload等)都会更新缓存
      Always: "always"
    }
  };
  const _sfc_main$1V = {
    name: "z-paging-refresh",
    data() {
      return {
        R: Enum.Refresher,
        refresherTimeText: "",
        zTheme: {
          title: { white: "#efefef", black: "#555555" },
          arrow: { white: zStatic.base64ArrowWhite, black: zStatic.base64Arrow },
          flower: { white: zStatic.base64FlowerWhite, black: zStatic.base64Flower },
          success: { white: zStatic.base64SuccessWhite, black: zStatic.base64Success },
          indicator: { white: "#eeeeee", black: "#777777" }
        }
      };
    },
    props: [
      "status",
      "defaultThemeStyle",
      "defaultText",
      "pullingText",
      "refreshingText",
      "completeText",
      "goF2Text",
      "defaultImg",
      "pullingImg",
      "refreshingImg",
      "completeImg",
      "refreshingAnimated",
      "showUpdateTime",
      "updateTimeKey",
      "imgStyle",
      "titleStyle",
      "updateTimeStyle",
      "updateTimeTextMap",
      "unit",
      "isIos"
    ],
    computed: {
      ts() {
        return this.defaultThemeStyle;
      },
      // 当前状态Map
      statusTextMap() {
        this.updateTime();
        const { R: R2, defaultText, pullingText, refreshingText, completeText, goF2Text } = this;
        return {
          [R2.Default]: defaultText,
          [R2.ReleaseToRefresh]: pullingText,
          [R2.Loading]: refreshingText,
          [R2.Complete]: completeText,
          [R2.GoF2]: goF2Text
        };
      },
      // 当前状态文字
      currentTitle() {
        return this.statusTextMap[this.status] || this.defaultText;
      },
      // 左侧图片class
      leftImageClass() {
        const preSizeClass = `zp-r-left-image-pre-size-${this.unit}`;
        if (this.status === this.R.Complete)
          return preSizeClass;
        return `zp-r-left-image ${preSizeClass} ${this.status === this.R.Default ? "zp-r-arrow-down" : "zp-r-arrow-top"}`;
      },
      // 左侧图片style
      leftImageStyle() {
        const showUpdateTime = this.showUpdateTime;
        const size = showUpdateTime ? u$1.addUnit(36, this.unit) : u$1.addUnit(34, this.unit);
        return { width: size, height: size, "margin-right": showUpdateTime ? u$1.addUnit(20, this.unit) : u$1.addUnit(9, this.unit) };
      },
      // 左侧图片src
      leftImageSrc() {
        const R2 = this.R;
        const status = this.status;
        if (status === R2.Default) {
          if (!!this.defaultImg)
            return this.defaultImg;
          return this.zTheme.arrow[this.ts];
        } else if (status === R2.ReleaseToRefresh) {
          if (!!this.pullingImg)
            return this.pullingImg;
          if (!!this.defaultImg)
            return this.defaultImg;
          return this.zTheme.arrow[this.ts];
        } else if (status === R2.Loading) {
          if (!!this.refreshingImg)
            return this.refreshingImg;
          return this.zTheme.flower[this.ts];
        } else if (status === R2.Complete) {
          if (!!this.completeImg)
            return this.completeImg;
          return this.zTheme.success[this.ts];
        } else if (status === R2.GoF2) {
          return this.zTheme.arrow[this.ts];
        }
        return "";
      },
      // 右侧文字style
      rightTextStyle() {
        let stl = {};
        stl["color"] = this.zTheme.title[this.ts];
        stl["font-size"] = u$1.addUnit(30, this.unit);
        return stl;
      }
    },
    methods: {
      // 添加单位
      addUnit(value, unit) {
        return u$1.addUnit(value, unit);
      },
      // 更新下拉刷新时间
      updateTime() {
        if (this.showUpdateTime) {
          this.refresherTimeText = u$1.getRefesrherFormatTimeByKey(this.updateTimeKey, this.updateTimeTextMap);
        }
      }
    }
  };
  function _sfc_render$1U(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { style: { "height": "100%" } }, [
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass($props.showUpdateTime ? "zp-r-container zp-r-container-padding" : "zp-r-container")
        },
        [
          vue.createElementVNode("view", { class: "zp-r-left" }, [
            $props.status !== $data.R.Loading ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 0,
              class: vue.normalizeClass($options.leftImageClass),
              style: vue.normalizeStyle([$options.leftImageStyle, $props.imgStyle]),
              src: $options.leftImageSrc
            }, null, 14, ["src"])) : (vue.openBlock(), vue.createElementBlock("image", {
              key: 1,
              class: vue.normalizeClass({ "zp-line-loading-image": $props.refreshingAnimated, "zp-r-left-image": true, "zp-r-left-image-pre-size-rpx": $props.unit === "rpx", "zp-r-left-image-pre-size-px": $props.unit === "px" }),
              style: vue.normalizeStyle([$options.leftImageStyle, $props.imgStyle]),
              src: $options.leftImageSrc
            }, null, 14, ["src"]))
          ]),
          vue.createElementVNode("view", { class: "zp-r-right" }, [
            vue.createElementVNode(
              "text",
              {
                class: "zp-r-right-text",
                style: vue.normalizeStyle([$options.rightTextStyle, $props.titleStyle])
              },
              vue.toDisplayString($options.currentTitle),
              5
              /* TEXT, STYLE */
            ),
            $props.showUpdateTime && $data.refresherTimeText.length ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 0,
                class: vue.normalizeClass(["zp-r-right-text", { "zp-r-right-time-text-rpx": $props.unit === "rpx", "zp-r-right-time-text-px": $props.unit === "px" }]),
                style: vue.normalizeStyle([{ color: $data.zTheme.title[$options.ts] }, $props.updateTimeStyle])
              },
              vue.toDisplayString($data.refresherTimeText),
              7
              /* TEXT, CLASS, STYLE */
            )) : vue.createCommentVNode("v-if", true)
          ])
        ],
        2
        /* CLASS */
      )
    ]);
  }
  const zPagingRefresh = /* @__PURE__ */ _export_sfc(_sfc_main$1V, [["render", _sfc_render$1U], ["__scopeId", "data-v-00a16504"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/z-paging/components/z-paging/components/z-paging-refresh.vue"]]);
  const _sfc_main$1U = {
    name: "z-paging-load-more",
    data() {
      return {
        M: Enum.More,
        zTheme: {
          title: { white: "#efefef", black: "#a4a4a4" },
          line: { white: "#efefef", black: "#eeeeee" },
          circleBorder: { white: "#aaaaaa", black: "#c8c8c8" },
          circleBorderTop: { white: "#ffffff", black: "#444444" },
          flower: { white: zStatic.base64FlowerWhite, black: zStatic.base64Flower },
          indicator: { white: "#eeeeee", black: "#777777" }
        }
      };
    },
    props: ["zConfig"],
    computed: {
      ts() {
        return this.c.defaultThemeStyle;
      },
      // 底部加载更多配置
      c() {
        return this.zConfig || {};
      },
      // 底部加载更多文字
      ownLoadingMoreText() {
        return {
          [this.M.Default]: this.c.defaultText,
          [this.M.Loading]: this.c.loadingText,
          [this.M.NoMore]: this.c.noMoreText,
          [this.M.Fail]: this.c.failText
        }[this.finalStatus];
      },
      // 底部加载更多状态
      finalStatus() {
        if (this.c.defaultAsLoading && this.c.status === this.M.Default)
          return this.M.Loading;
        return this.c.status;
      },
      // 加载更多icon类型
      finalLoadingIconType() {
        return this.c.loadingIconType;
      }
    },
    methods: {
      // 点击了加载更多
      doClick() {
        this.$emit("doClick");
      }
    }
  };
  function _sfc_render$1T(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: vue.normalizeClass(["zp-l-container", { "zp-l-container-rpx": $options.c.unit === "rpx", "zp-l-container-px": $options.c.unit === "px" }]),
        style: vue.normalizeStyle([$options.c.customStyle]),
        onClick: _cache[0] || (_cache[0] = (...args) => $options.doClick && $options.doClick(...args))
      },
      [
        !$options.c.hideContent ? (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 0 },
          [
            $options.c.showNoMoreLine && $options.finalStatus === $data.M.NoMore ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 0,
                class: vue.normalizeClass({ "zp-l-line-rpx": $options.c.unit === "rpx", "zp-l-line-px": $options.c.unit === "px" }),
                style: vue.normalizeStyle([{ backgroundColor: $data.zTheme.line[$options.ts] }, $options.c.noMoreLineCustomStyle])
              },
              null,
              6
              /* CLASS, STYLE */
            )) : vue.createCommentVNode("v-if", true),
            $options.finalStatus === $data.M.Loading && !!$options.c.loadingIconCustomImage ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 1,
              src: $options.c.loadingIconCustomImage,
              style: vue.normalizeStyle([$options.c.iconCustomStyle]),
              class: vue.normalizeClass({ "zp-l-line-loading-custom-image": true, "zp-l-line-loading-custom-image-animated": $options.c.loadingAnimated, "zp-l-line-loading-custom-image-rpx": $options.c.unit === "rpx", "zp-l-line-loading-custom-image-px": $options.c.unit === "px" })
            }, null, 14, ["src"])) : vue.createCommentVNode("v-if", true),
            $options.finalStatus === $data.M.Loading && $options.finalLoadingIconType === "flower" && !$options.c.loadingIconCustomImage.length ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 2,
              class: vue.normalizeClass({ "zp-line-loading-image": true, "zp-line-loading-image-rpx": $options.c.unit === "rpx", "zp-line-loading-image-px": $options.c.unit === "px" }),
              style: vue.normalizeStyle([$options.c.iconCustomStyle]),
              src: $data.zTheme.flower[$options.ts]
            }, null, 14, ["src"])) : vue.createCommentVNode("v-if", true),
            $options.finalStatus === $data.M.Loading && $options.finalLoadingIconType === "circle" && !$options.c.loadingIconCustomImage.length ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 3,
                class: vue.normalizeClass(["zp-l-circle-loading-view", { "zp-l-circle-loading-view-rpx": $options.c.unit === "rpx", "zp-l-circle-loading-view-px": $options.c.unit === "px" }]),
                style: vue.normalizeStyle([{ borderColor: $data.zTheme.circleBorder[$options.ts], borderTopColor: $data.zTheme.circleBorderTop[$options.ts] }, $options.c.iconCustomStyle])
              },
              null,
              6
              /* CLASS, STYLE */
            )) : vue.createCommentVNode("v-if", true),
            !$options.c.isChat || !$options.c.chatDefaultAsLoading && $options.finalStatus === $data.M.Default || $options.finalStatus === $data.M.Fail ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 4,
                class: vue.normalizeClass({ "zp-l-text-rpx": $options.c.unit === "rpx", "zp-l-text-px": $options.c.unit === "px" }),
                style: vue.normalizeStyle([{ color: $data.zTheme.title[$options.ts] }, $options.c.titleCustomStyle])
              },
              vue.toDisplayString($options.ownLoadingMoreText),
              7
              /* TEXT, CLASS, STYLE */
            )) : vue.createCommentVNode("v-if", true),
            $options.c.showNoMoreLine && $options.finalStatus === $data.M.NoMore ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 5,
                class: vue.normalizeClass({ "zp-l-line-rpx": $options.c.unit === "rpx", "zp-l-line-px": $options.c.unit === "px" }),
                style: vue.normalizeStyle([{ backgroundColor: $data.zTheme.line[$options.ts] }, $options.c.noMoreLineCustomStyle])
              },
              null,
              6
              /* CLASS, STYLE */
            )) : vue.createCommentVNode("v-if", true)
          ],
          64
          /* STABLE_FRAGMENT */
        )) : vue.createCommentVNode("v-if", true)
      ],
      6
      /* CLASS, STYLE */
    );
  }
  const zPagingLoadMore = /* @__PURE__ */ _export_sfc(_sfc_main$1U, [["render", _sfc_render$1T], ["__scopeId", "data-v-8cc5c400"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/z-paging/components/z-paging/components/z-paging-load-more.vue"]]);
  const commonLayoutModule = {
    data() {
      return {
        systemInfo: null,
        cssSafeAreaInsetBottom: -1,
        isReadyDestroy: false
      };
    },
    computed: {
      // 顶部可用距离
      windowTop() {
        if (!this.systemInfo)
          return 0;
        return this.systemInfo.windowTop || 0;
      },
      // 底部安全区域高度
      safeAreaBottom() {
        if (!this.systemInfo)
          return 0;
        let safeAreaBottom = 0;
        safeAreaBottom = this.systemInfo.safeAreaInsets.bottom || 0;
        return safeAreaBottom;
      },
      // 是否是比较老的webview，在一些老的webview中，需要进行一些特殊处理
      isOldWebView() {
        try {
          const systemInfos = u$1.getSystemInfoSync(true).system.split(" ");
          const deviceType = systemInfos[0];
          const version = parseInt(systemInfos[1]);
          if (deviceType === "iOS" && version <= 10 || deviceType === "Android" && version <= 6) {
            return true;
          }
        } catch (e2) {
          return false;
        }
        return false;
      },
      // 当前组件的$slots，兼容不同平台
      zSlots() {
        return this.$slots;
      }
    },
    beforeDestroy() {
      this.isReadyDestroy = true;
    },
    unmounted() {
      this.isReadyDestroy = true;
    },
    methods: {
      // 更新fixed模式下z-paging的布局
      updateFixedLayout() {
        this.fixed && this.$nextTick(() => {
          this.systemInfo = u$1.getSystemInfoSync();
        });
      },
      // 获取节点尺寸
      _getNodeClientRect(select, inDom = true, scrollOffset = false) {
        if (this.isReadyDestroy) {
          return Promise.resolve(false);
        }
        let res = !!inDom ? uni.createSelectorQuery().in(inDom === true ? this : inDom) : uni.createSelectorQuery();
        scrollOffset ? res.select(select).scrollOffset() : res.select(select).boundingClientRect();
        return new Promise((resolve, reject) => {
          res.exec((data) => {
            resolve(data && data != "" && data != void 0 && data.length ? data : false);
          });
        });
      },
      // 获取slot="left"和slot="right"宽度并且更新布局
      _updateLeftAndRightWidth(targetStyle, parentNodePrefix) {
        this.$nextTick(() => {
          let delayTime = 0;
          setTimeout(() => {
            ["left", "right"].map((position) => {
              this._getNodeClientRect(`.${parentNodePrefix}-${position}`).then((res) => {
                this.$set(targetStyle, position, res ? res[0].width + "px" : "0px");
              });
            });
          }, delayTime);
        });
      },
      // 通过获取css设置的底部安全区域占位view高度设置bottom距离（直接通过systemInfo在部分平台上无法获取到底部安全区域）
      _getCssSafeAreaInsetBottom(success) {
        this._getNodeClientRect(".zp-safe-area-inset-bottom").then((res) => {
          this.cssSafeAreaInsetBottom = res ? res[0].height : -1;
          res && success && success();
        });
      },
      // 同步获取系统信息，兼容不同平台（供z-paging-swiper使用）
      _getSystemInfoSync(useCache = false) {
        return u$1.getSystemInfoSync(useCache);
      }
    }
  };
  const queryKey = "Query";
  const fetchParamsKey = "FetchParams";
  const fetchResultKey = "FetchResult";
  const language2LocalKey = "Language2Local";
  function handleQuery(callback) {
    _addHandleByKey(queryKey, callback);
    return this;
  }
  function _handleQuery(pageNo, pageSize2, from, lastItem) {
    const callback = _getHandleByKey(queryKey);
    return callback ? callback(pageNo, pageSize2, from, lastItem) : [pageNo, pageSize2, from];
  }
  function handleFetchParams(callback) {
    _addHandleByKey(fetchParamsKey, callback);
    return this;
  }
  function _handleFetchParams(parmas, extraParams) {
    const callback = _getHandleByKey(fetchParamsKey);
    return callback ? callback(parmas, extraParams || {}) : { pageNo: parmas.pageNo, pageSize: parmas.pageSize, ...extraParams || {} };
  }
  function handleFetchResult(callback) {
    _addHandleByKey(fetchResultKey, callback);
    return this;
  }
  function _handleFetchResult(result, paging, params) {
    const callback = _getHandleByKey(fetchResultKey);
    callback && callback(result, paging, params);
    return callback ? true : false;
  }
  function handleLanguage2Local(callback) {
    _addHandleByKey(language2LocalKey, callback);
    return this;
  }
  function _handleLanguage2Local(language, local) {
    const callback = _getHandleByKey(language2LocalKey);
    return callback ? callback(language, local) : local;
  }
  function _getApp() {
    return getApp();
  }
  function _hasGlobalData() {
    return _getApp() && _getApp().globalData;
  }
  function _addHandleByKey(key, callback) {
    try {
      setTimeout(function() {
        if (_hasGlobalData()) {
          _getApp().globalData[`zp_handle${key}Callback`] = callback;
        }
      }, 1);
    } catch (_2) {
    }
  }
  function _getHandleByKey(key) {
    return _hasGlobalData() ? _getApp().globalData[`zp_handle${key}Callback`] : null;
  }
  const interceptor = {
    handleQuery,
    _handleQuery,
    handleFetchParams,
    _handleFetchParams,
    handleFetchResult,
    _handleFetchResult,
    handleLanguage2Local,
    _handleLanguage2Local
  };
  const dataHandleModule = {
    props: {
      // 自定义初始的pageNo，默认为1
      defaultPageNo: {
        type: Number,
        default: u$1.gc("defaultPageNo", 1),
        observer: function(newVal) {
          this.pageNo = newVal;
        }
      },
      // 自定义pageSize，默认为10
      defaultPageSize: {
        type: Number,
        default: u$1.gc("defaultPageSize", 10),
        validator: (value) => {
          if (value <= 0)
            u$1.consoleErr("default-page-size必须大于0！");
          return value > 0;
        }
      },
      // 为保证数据一致，设置当前tab切换时的标识key，并在complete中传递相同key，若二者不一致，则complete将不会生效
      dataKey: {
        type: [Number, String, Object],
        default: u$1.gc("dataKey", null)
      },
      // 使用缓存，若开启将自动缓存第一页的数据，默认为否。请注意，因考虑到切换tab时不同tab数据不同的情况，默认仅会缓存组件首次加载时第一次请求到的数据，后续的下拉刷新操作不会更新缓存。
      useCache: {
        type: Boolean,
        default: u$1.gc("useCache", false)
      },
      // 使用缓存时缓存的key，用于区分不同列表的缓存数据，useCache为true时必须设置，否则缓存无效
      cacheKey: {
        type: String,
        default: u$1.gc("cacheKey", null)
      },
      // 缓存模式，默认仅会缓存组件首次加载时第一次请求到的数据，可设置为always，即代表总是缓存，每次列表刷新(下拉刷新、调用reload等)都会更新缓存
      cacheMode: {
        type: String,
        default: u$1.gc("cacheMode", Enum.CacheMode.Default)
      },
      // 自动注入的list名，可自动修改父view(包含ref="paging")中对应name的list值
      autowireListName: {
        type: String,
        default: u$1.gc("autowireListName", "")
      },
      // 自动注入的query名，可自动调用父view(包含ref="paging")中的query方法
      autowireQueryName: {
        type: String,
        default: u$1.gc("autowireQueryName", "")
      },
      // 获取分页数据Function，功能与@query类似。若设置了fetch则@query将不再触发
      fetch: {
        type: Function,
        default: null
      },
      // fetch的附加参数，fetch配置后有效
      fetchParams: {
        type: Object,
        default: u$1.gc("fetchParams", null)
      },
      // z-paging mounted后自动调用reload方法(mounted后自动调用接口)，默认为是
      auto: {
        type: Boolean,
        default: u$1.gc("auto", true)
      },
      // 用户下拉刷新时是否触发reload方法，默认为是
      reloadWhenRefresh: {
        type: Boolean,
        default: u$1.gc("reloadWhenRefresh", true)
      },
      // reload时自动滚动到顶部，默认为是
      autoScrollToTopWhenReload: {
        type: Boolean,
        default: u$1.gc("autoScrollToTopWhenReload", true)
      },
      // reload时立即自动清空原list，默认为是，若立即自动清空，则在reload之后、请求回调之前页面是空白的
      autoCleanListWhenReload: {
        type: Boolean,
        default: u$1.gc("autoCleanListWhenReload", true)
      },
      // 列表刷新时自动显示下拉刷新view，默认为否
      showRefresherWhenReload: {
        type: Boolean,
        default: u$1.gc("showRefresherWhenReload", false)
      },
      // 列表刷新时自动显示加载更多view，且为加载中状态，默认为否
      showLoadingMoreWhenReload: {
        type: Boolean,
        default: u$1.gc("showLoadingMoreWhenReload", false)
      },
      // 组件created时立即触发reload(可解决一些情况下先看到页面再看到loading的问题)，auto为true时有效。为否时将在mounted+nextTick后触发reload，默认为否
      createdReload: {
        type: Boolean,
        default: u$1.gc("createdReload", false)
      },
      // 本地分页时上拉加载更多延迟时间，单位为毫秒，默认200毫秒
      localPagingLoadingTime: {
        type: [Number, String],
        default: u$1.gc("localPagingLoadingTime", 200)
      },
      // 自动拼接complete中传过来的数组(使用聊天记录模式时无效)
      concat: {
        type: Boolean,
        default: u$1.gc("concat", true)
      },
      // 请求失败是否触发reject，默认为是
      callNetworkReject: {
        type: Boolean,
        default: u$1.gc("callNetworkReject", true)
      },
      // 父组件v-model所绑定的list的值
      value: {
        type: Array,
        default: function() {
          return [];
        }
      },
      modelValue: {
        type: Array,
        default: function() {
          return [];
        }
      }
    },
    data() {
      return {
        currentData: [],
        totalData: [],
        realTotalData: [],
        totalLocalPagingList: [],
        dataPromiseResultMap: {
          reload: null,
          complete: null,
          localPaging: null
        },
        isSettingCacheList: false,
        pageNo: 1,
        currentRefreshPageSize: 0,
        isLocalPaging: false,
        isAddedData: false,
        isTotalChangeFromAddData: false,
        privateConcat: true,
        myParentQuery: -1,
        firstPageLoaded: false,
        pagingLoaded: false,
        loaded: false,
        isUserReload: true,
        fromEmptyViewReload: false,
        queryFrom: "",
        listRendering: false,
        isHandlingRefreshToPage: false,
        isFirstPageAndNoMore: false,
        totalDataChangeThrow: true,
        addDataFromTopBufferedInsert: u$1.useBufferedInsert(this._addDataFromTop)
      };
    },
    computed: {
      pageSize() {
        return this.defaultPageSize;
      },
      finalConcat() {
        return this.concat && this.privateConcat;
      },
      finalUseCache() {
        if (this.useCache && !this.cacheKey) {
          u$1.consoleErr("use-cache为true时，必须设置cache-key，否则缓存无效！");
        }
        return this.useCache && !!this.cacheKey;
      },
      finalCacheKey() {
        return this.cacheKey ? `${c$1.cachePrefixKey}-${this.cacheKey}` : null;
      },
      isFirstPage() {
        return this.pageNo === this.defaultPageNo;
      }
    },
    watch: {
      totalData(newVal, oldVal) {
        const eventThrow = this.isSettingCacheList ? true : this.totalDataChangeThrow;
        this._totalDataChange(newVal, oldVal, eventThrow);
        this.totalDataChangeThrow = true;
      },
      currentData(newVal, oldVal) {
        this._currentDataChange(newVal, oldVal);
      },
      useChatRecordMode(newVal, oldVal) {
        if (newVal) {
          this.nLoadingMoreFixedHeight = false;
        }
      },
      value: {
        handler(newVal) {
          if (newVal !== this.totalData) {
            this.totalDataChangeThrow = false;
            this.totalData = newVal;
          }
        },
        immediate: true
      },
      modelValue: {
        handler(newVal) {
          if (newVal !== this.totalData) {
            this.totalDataChangeThrow = false;
            this.totalData = newVal;
          }
        },
        immediate: true
      }
    },
    methods: {
      // 请求结束(成功或者失败)调用此方法，将请求的结果传递给z-paging处理，第一个参数为请求结果数组，第二个参数为是否成功(默认为是）
      complete(data, success = true) {
        this.customNoMore = -1;
        return this.addData(data, success);
      },
      //【保证数据一致】请求结束(成功或者失败)调用此方法，将请求的结果传递给z-paging处理，第一个参数为请求结果数组，第二个参数为dataKey，需与:data-key绑定的一致，第三个参数为是否成功(默认为是）
      completeByKey(data, dataKey = null, success = true) {
        if (dataKey !== null && this.dataKey !== null && dataKey !== this.dataKey) {
          this.isFirstPage && this.endRefresh();
          return new Promise((resolve) => resolve());
        }
        this.customNoMore = -1;
        return this.addData(data, success);
      },
      //【通过total判断是否有更多数据】请求结束(成功或者失败)调用此方法，将请求的结果传递给z-paging处理，第一个参数为请求结果数组，第二个参数为total(列表总数)，第三个参数为是否成功(默认为是）
      completeByTotal(data, total, success = true) {
        if (total == "undefined") {
          this.customNoMore = -1;
        } else {
          const dataTypeRes = this._checkDataType(data, success, false);
          data = dataTypeRes.data;
          success = dataTypeRes.success;
          if (total >= 0 && success) {
            return new Promise((resolve, reject) => {
              this.$nextTick(() => {
                let nomore = false;
                const realTotalDataCount = this.pageNo == this.defaultPageNo ? 0 : this.realTotalData.length;
                const dataLength = this.privateConcat ? data.length : 0;
                let exceedCount = realTotalDataCount + dataLength - total;
                if (exceedCount >= 0) {
                  nomore = true;
                  exceedCount = this.defaultPageSize - exceedCount;
                  if (this.privateConcat && exceedCount > 0 && exceedCount < data.length) {
                    data = data.splice(0, exceedCount);
                  }
                }
                this.completeByNoMore(data, nomore, success).then((res) => resolve(res)).catch(() => reject());
              });
            });
          }
        }
        return this.addData(data, success);
      },
      //【自行判断是否有更多数据】请求结束(成功或者失败)调用此方法，将请求的结果传递给z-paging处理，第一个参数为请求结果数组，第二个参数为是否没有更多数据，第三个参数为是否成功(默认是是）
      completeByNoMore(data, nomore, success = true) {
        if (nomore != "undefined") {
          this.customNoMore = nomore == true ? 1 : 0;
        }
        return this.addData(data, success);
      },
      // 请求结束且请求失败时调用，支持传入请求失败原因
      completeByError(errorMsg) {
        this.customerEmptyViewErrorText = errorMsg;
        return this.complete(false);
      },
      // 与上方complete方法功能一致，新版本中设置服务端回调数组请使用complete方法
      addData(data, success = true) {
        if (!this.fromCompleteEmit) {
          this.disabledCompleteEmit = true;
          this.fromCompleteEmit = false;
        }
        const currentTimeStamp = u$1.getTime();
        const disTime = currentTimeStamp - this.requestTimeStamp;
        let minDelay = this.minDelay;
        if (this.isFirstPage && this.finalShowRefresherWhenReload) {
          minDelay = Math.max(400, minDelay);
        }
        const addDataDalay = this.requestTimeStamp > 0 && disTime < minDelay ? minDelay - disTime : 0;
        this.$nextTick(() => {
          u$1.delay(() => {
            this._addData(data, success, false);
          }, this.delay > 0 ? this.delay : addDataDalay);
        });
        return new Promise((resolve, reject) => {
          this.dataPromiseResultMap.complete = { resolve, reject };
        });
      },
      // 从顶部添加数据，不会影响分页的pageNo和pageSize
      addDataFromTop(data, toTop = true, toTopWithAnimate = true) {
        (this.finalUseVirtualList ? this.addDataFromTopBufferedInsert : this._addDataFromTop)(
          data,
          toTop,
          toTopWithAnimate
        );
      },
      // 重新设置列表数据，调用此方法不会影响pageNo和pageSize，也不会触发请求。适用场景：当需要删除列表中某一项时，将删除对应项后的数组通过此方法传递给z-paging。(当出现类似的需要修改列表数组的场景时，请使用此方法，请勿直接修改page中:list.sync绑定的数组)
      resetTotalData(data) {
        this.isTotalChangeFromAddData = true;
        data = Object.prototype.toString.call(data) !== "[object Array]" ? [data] : data;
        this.totalData = data;
      },
      // 设置本地分页数据，请求结束(成功或者失败)调用此方法，将请求的结果传递给z-paging作分页处理（若调用了此方法，则上拉加载更多时内部会自动分页，不会触发@query所绑定的事件）
      setLocalPaging(data, success = true) {
        this.isLocalPaging = true;
        this.$nextTick(() => {
          this._addData(data, success, true);
        });
        return new Promise((resolve, reject) => {
          this.dataPromiseResultMap.localPaging = { resolve, reject };
        });
      },
      // 重新加载分页数据，pageNo会恢复为默认值，相当于下拉刷新的效果(animate为true时会展示下拉刷新动画，默认为false)
      reload(animate = this.showRefresherWhenReload) {
        if (animate) {
          this.privateShowRefresherWhenReload = animate;
          this.isUserPullDown = true;
        }
        if (!this.showLoadingMoreWhenReload) {
          this.listRendering = true;
        }
        this.$nextTick(() => {
          this._preReload(animate, false);
        });
        return new Promise((resolve, reject) => {
          this.dataPromiseResultMap.reload = { resolve, reject };
        });
      },
      // 刷新列表数据，pageNo和pageSize不会重置，列表数据会重新从服务端获取。必须保证@query绑定的方法中的pageNo和pageSize和传给服务端的一致
      refresh() {
        return this._handleRefreshWithDisPageNo(this.pageNo - this.defaultPageNo + 1);
      },
      // 刷新列表数据至指定页，例如pageNo=5时则代表刷新列表至第5页，此时pageNo会变为5，列表会展示前5页的数据。必须保证@query绑定的方法中的pageNo和pageSize和传给服务端的一致
      refreshToPage(pageNo) {
        this.isHandlingRefreshToPage = true;
        return this._handleRefreshWithDisPageNo(pageNo + this.defaultPageNo - 1);
      },
      // 手动更新列表缓存数据，将自动截取v-model绑定的list中的前pageSize条覆盖缓存，请确保在list数据更新到预期结果后再调用此方法
      updateCache() {
        if (this.finalUseCache && this.totalData.length) {
          this._saveLocalCache(this.totalData.slice(0, Math.min(this.totalData.length, this.pageSize)));
        }
      },
      // 清空分页数据
      clean() {
        this._reload(true);
        this._addData([], true, false);
      },
      // 清空分页数据
      clear() {
        this.clean();
      },
      // reload之前的一些处理
      _preReload(animate = this.showRefresherWhenReload, isFromMounted = true, retryCount = 0) {
        const showRefresher = this.finalRefresherEnabled && this.useCustomRefresher;
        if (this.customRefresherHeight === -1 && showRefresher) {
          u$1.delay(() => {
            retryCount++;
            if (retryCount % 10 === 0) {
              this._updateCustomRefresherHeight();
            }
            this._preReload(animate, isFromMounted, retryCount);
          }, c$1.delayTime / 2);
          return;
        }
        this.isUserReload = true;
        this.loadingType = Enum.LoadingType.Refresher;
        if (animate) {
          this.privateShowRefresherWhenReload = animate;
          if (this.useCustomRefresher) {
            this._doRefresherRefreshAnimate();
          } else {
            this.refresherTriggered = true;
          }
        } else {
          this._refresherEnd(false, false, false, false);
        }
        this._reload(false, isFromMounted);
      },
      // 重新加载分页数据
      _reload(isClean = false, isFromMounted = false, isUserPullDown = false) {
        this.isAddedData = false;
        this.insideOfPaging = -1;
        this.cacheScrollNodeHeight = -1;
        this.pageNo = this.defaultPageNo;
        this._cleanRefresherEndTimeout();
        !this.privateShowRefresherWhenReload && !isClean && this._startLoading(true);
        this.firstPageLoaded = true;
        this.isTotalChangeFromAddData = false;
        if (!this.isSettingCacheList) {
          this.totalData = [];
        }
        if (!isClean) {
          this._emitQuery(this.pageNo, this.defaultPageSize, isUserPullDown ? Enum.QueryFrom.UserPullDown : Enum.QueryFrom.Reload);
          let delay2 = 0;
          u$1.delay(this._callMyParentQuery, delay2);
          if (!isFromMounted && this.autoScrollToTopWhenReload) {
            this._scrollToTop(false);
          }
        }
      },
      // 处理服务端返回的数组
      _addData(data, success, isLocal) {
        this.isAddedData = true;
        this.fromEmptyViewReload = false;
        this.isTotalChangeFromAddData = true;
        this.refresherTriggered = false;
        this._endSystemLoadingAndRefresh();
        const tempIsUserPullDown = this.isUserPullDown;
        if (this.showRefresherUpdateTime && this.isFirstPage) {
          u$1.setRefesrherTime(u$1.getTime(), this.refresherUpdateTimeKey);
          this.$refs.refresh && this.$refs.refresh.updateTime();
        }
        if (!isLocal && tempIsUserPullDown && this.isFirstPage) {
          this.isUserPullDown = false;
        }
        this.listRendering = true;
        this.$nextTick(() => {
          u$1.delay(() => this.listRendering = false);
        });
        let dataTypeRes = this._checkDataType(data, success, isLocal);
        data = dataTypeRes.data;
        success = dataTypeRes.success;
        let delayTime = c$1.delayTime;
        if (this.useChatRecordMode)
          delayTime = 0;
        this.loadingForNow = false;
        u$1.delay(() => {
          this.pagingLoaded = true;
          this.$nextTick(() => {
            !isLocal && this._refresherEnd(delayTime > 0, true, tempIsUserPullDown);
          });
        });
        if (this.isFirstPage) {
          this.isLoadFailed = !success;
          this.$emit("isLoadFailedChange", this.isLoadFailed);
          if (this.finalUseCache && success && (this.cacheMode === Enum.CacheMode.Always ? true : this.isSettingCacheList)) {
            this._saveLocalCache(data);
          }
        }
        this.isSettingCacheList = false;
        if (success) {
          if (!(this.privateConcat === false && !this.isHandlingRefreshToPage && this.loadingStatus === Enum.More.NoMore)) {
            this.loadingStatus = Enum.More.Default;
          }
          if (isLocal) {
            this.totalLocalPagingList = data;
            const localPageNo = this.defaultPageNo;
            const localPageSize = this.queryFrom !== Enum.QueryFrom.Refresh ? this.defaultPageSize : this.currentRefreshPageSize;
            this._localPagingQueryList(localPageNo, localPageSize, 0, (res) => {
              u$1.delay(() => {
                this.completeByTotal(res, this.totalLocalPagingList.length);
              }, 0);
            });
          } else {
            let dataChangeDelayTime = 0;
            u$1.delay(() => {
              this._currentDataChange(data, this.currentData);
              this._callDataPromise(true, this.totalData);
            }, dataChangeDelayTime);
          }
          if (this.isHandlingRefreshToPage) {
            this.isHandlingRefreshToPage = false;
            this.pageNo = this.defaultPageNo + Math.ceil(data.length / this.pageSize) - 1;
            if (data.length % this.pageSize !== 0) {
              this.customNoMore = 1;
            }
          }
        } else {
          this._currentDataChange(data, this.currentData);
          this._callDataPromise(false);
          this.loadingStatus = Enum.More.Fail;
          this.isHandlingRefreshToPage = false;
          if (this.loadingType === Enum.LoadingType.LoadMore) {
            this.pageNo--;
          }
        }
      },
      // 所有数据改变时调用
      _totalDataChange(newVal, oldVal, eventThrow = true) {
        if ((!this.isUserReload || !this.autoCleanListWhenReload) && this.firstPageLoaded && !newVal.length && oldVal.length) {
          return;
        }
        this._doCheckScrollViewShouldFullHeight(newVal);
        if (!this.realTotalData.length && !newVal.length) {
          eventThrow = false;
        }
        this.realTotalData = newVal;
        if (eventThrow) {
          this.$emit("input", newVal);
          this.$emit("update:modelValue", newVal);
          this.$emit("update:list", newVal);
          this.$emit("listChange", newVal);
          this._callMyParentList(newVal);
        }
        this.firstPageLoaded = false;
        this.isTotalChangeFromAddData = false;
        this.$nextTick(() => {
          u$1.delay(() => {
            this._getNodeClientRect(".zp-paging-container-content").then((res) => {
              res && this.$emit("contentHeightChanged", res[0].height);
            });
          }, c$1.delayTime * (this.isIos ? 1 : 3));
        });
      },
      // 当前数据改变时调用
      _currentDataChange(newVal, oldVal) {
        newVal = [...newVal];
        this.finalUseVirtualList && this._setCellIndex(newVal, "bottom");
        if (this.isFirstPage && this.finalConcat) {
          this.totalData = [];
        }
        if (this.customNoMore !== -1) {
          if (this.customNoMore === 1 || this.customNoMore !== 0 && !newVal.length) {
            this.loadingStatus = Enum.More.NoMore;
          }
        } else {
          if (!newVal.length || newVal.length && newVal.length < this.defaultPageSize) {
            this.loadingStatus = Enum.More.NoMore;
          }
        }
        if (!this.totalData.length) {
          this.totalData = newVal;
        } else {
          if (this.finalConcat) {
            this.oldScrollTop;
            this.totalData = [...this.totalData, ...newVal];
          } else {
            this.totalData = newVal;
          }
        }
        this.privateConcat = true;
      },
      // 根据pageNo处理refresh操作
      _handleRefreshWithDisPageNo(pageNo) {
        if (!this.isHandlingRefreshToPage && !this.realTotalData.length)
          return this.reload();
        if (pageNo >= 1) {
          this.loading = true;
          this.privateConcat = false;
          const totalPageSize = pageNo * this.pageSize;
          this.currentRefreshPageSize = totalPageSize;
          if (this.isLocalPaging && this.isHandlingRefreshToPage) {
            this._localPagingQueryList(this.defaultPageNo, totalPageSize, 0, (res) => {
              this.complete(res);
            });
          } else {
            this._emitQuery(this.defaultPageNo, totalPageSize, Enum.QueryFrom.Refresh);
            this._callMyParentQuery(this.defaultPageNo, totalPageSize);
          }
        }
        return new Promise((resolve, reject) => {
          this.dataPromiseResultMap.reload = { resolve, reject };
        });
      },
      // 本地分页请求
      _localPagingQueryList(pageNo, pageSize2, localPagingLoadingTime, callback) {
        pageNo = Math.max(1, pageNo);
        pageSize2 = Math.max(1, pageSize2);
        const totalPagingList = [...this.totalLocalPagingList];
        const pageNoIndex = (pageNo - 1) * pageSize2;
        const finalPageNoIndex = Math.min(totalPagingList.length, pageNoIndex + pageSize2);
        const resultPagingList = totalPagingList.splice(pageNoIndex, finalPageNoIndex - pageNoIndex);
        u$1.delay(() => callback(resultPagingList), localPagingLoadingTime);
      },
      // 从顶部添加数据，不会影响分页的pageNo和pageSize
      _addDataFromTop(data, toTop = true, toTopWithAnimate = true) {
        let addFromTop = !this.isChatRecordModeAndNotInversion;
        data = Object.prototype.toString.call(data) !== "[object Array]" ? [data] : addFromTop ? data.reverse() : data;
        this.finalUseVirtualList && this._setCellIndex(data, "top");
        this.totalData = addFromTop ? [...data, ...this.totalData] : [...this.totalData, ...data];
        if (toTop) {
          u$1.delay(() => this.useChatRecordMode ? this.scrollToBottom(toTopWithAnimate) : this.scrollToTop(toTopWithAnimate));
        }
      },
      // 存储列表缓存数据
      _saveLocalCache(data) {
        uni.setStorageSync(this.finalCacheKey, data);
      },
      // 通过缓存数据填充列表数据
      _setListByLocalCache() {
        this.totalData = uni.getStorageSync(this.finalCacheKey) || [];
        this.isSettingCacheList = true;
      },
      // 修改父view的list
      _callMyParentList(newVal) {
        if (this.autowireListName.length) {
          const myParent = u$1.getParent(this.$parent);
          if (myParent && myParent[this.autowireListName]) {
            myParent[this.autowireListName] = newVal;
          }
        }
      },
      // 调用父view的query
      _callMyParentQuery(customPageNo = 0, customPageSize = 0) {
        if (this.autowireQueryName) {
          if (this.myParentQuery === -1) {
            const myParent = u$1.getParent(this.$parent);
            if (myParent && myParent[this.autowireQueryName]) {
              this.myParentQuery = myParent[this.autowireQueryName];
            }
          }
          if (this.myParentQuery !== -1) {
            customPageSize > 0 ? this.myParentQuery(customPageNo, customPageSize) : this.myParentQuery(this.pageNo, this.defaultPageSize);
          }
        }
      },
      // emit query事件
      _emitQuery(pageNo, pageSize2, from) {
        this.queryFrom = from;
        this.requestTimeStamp = u$1.getTime();
        const [lastItem] = this.realTotalData.slice(-1);
        if (this.fetch) {
          const fetchParams = interceptor._handleFetchParams({ pageNo, pageSize: pageSize2, from, lastItem: lastItem || null }, this.fetchParams);
          const fetchResult = this.fetch(fetchParams);
          if (!interceptor._handleFetchResult(fetchResult, this, fetchParams)) {
            u$1.isPromise(fetchResult) ? fetchResult.then((res) => {
              this.complete(res);
            }).catch((err) => {
              this.complete(false);
            }) : this.complete(fetchResult);
          }
        } else {
          this.$emit("query", ...interceptor._handleQuery(pageNo, pageSize2, from, lastItem || null));
        }
      },
      // 触发数据改变promise
      _callDataPromise(success, totalList) {
        for (const key in this.dataPromiseResultMap) {
          const obj = this.dataPromiseResultMap[key];
          if (!obj)
            continue;
          success ? obj.resolve({ totalList, noMore: this.loadingStatus === Enum.More.NoMore }) : this.callNetworkReject && obj.reject(`z-paging-${key}-error`);
        }
      },
      // 检查complete data的类型
      _checkDataType(data, success, isLocal) {
        const dataType = Object.prototype.toString.call(data);
        if (dataType === "[object Boolean]") {
          success = data;
          data = [];
        } else if (dataType !== "[object Array]") {
          data = [];
          if (dataType !== "[object Undefined]" && dataType !== "[object Null]") {
            u$1.consoleErr(`${isLocal ? "setLocalPaging" : "complete"}参数类型不正确，第一个参数类型必须为Array!`);
          }
        }
        return { data, success };
      }
    }
  };
  const isObject = (val) => val !== null && typeof val === "object";
  const defaultDelimiters = ["{", "}"];
  class BaseFormatter {
    constructor() {
      this._caches = /* @__PURE__ */ Object.create(null);
    }
    interpolate(message, values, delimiters = defaultDelimiters) {
      if (!values) {
        return [message];
      }
      let tokens = this._caches[message];
      if (!tokens) {
        tokens = parse(message, delimiters);
        this._caches[message] = tokens;
      }
      return compile(tokens, values);
    }
  }
  const RE_TOKEN_LIST_VALUE = /^(?:\d)+/;
  const RE_TOKEN_NAMED_VALUE = /^(?:\w)+/;
  function parse(format, [startDelimiter, endDelimiter]) {
    const tokens = [];
    let position = 0;
    let text = "";
    while (position < format.length) {
      let char = format[position++];
      if (char === startDelimiter) {
        if (text) {
          tokens.push({ type: "text", value: text });
        }
        text = "";
        let sub = "";
        char = format[position++];
        while (char !== void 0 && char !== endDelimiter) {
          sub += char;
          char = format[position++];
        }
        const isClosed = char === endDelimiter;
        const type = RE_TOKEN_LIST_VALUE.test(sub) ? "list" : isClosed && RE_TOKEN_NAMED_VALUE.test(sub) ? "named" : "unknown";
        tokens.push({ value: sub, type });
      } else {
        text += char;
      }
    }
    text && tokens.push({ type: "text", value: text });
    return tokens;
  }
  function compile(tokens, values) {
    const compiled = [];
    let index = 0;
    const mode = Array.isArray(values) ? "list" : isObject(values) ? "named" : "unknown";
    if (mode === "unknown") {
      return compiled;
    }
    while (index < tokens.length) {
      const token = tokens[index];
      switch (token.type) {
        case "text":
          compiled.push(token.value);
          break;
        case "list":
          compiled.push(values[parseInt(token.value, 10)]);
          break;
        case "named":
          if (mode === "named") {
            compiled.push(values[token.value]);
          } else {
            {
              console.warn(`Type of token '${token.type}' and format of value '${mode}' don't match!`);
            }
          }
          break;
        case "unknown":
          {
            console.warn(`Detect 'unknown' type of token!`);
          }
          break;
      }
      index++;
    }
    return compiled;
  }
  const LOCALE_ZH_HANS = "zh-Hans";
  const LOCALE_ZH_HANT = "zh-Hant";
  const LOCALE_EN = "en";
  const LOCALE_FR = "fr";
  const LOCALE_ES = "es";
  const hasOwnProperty = Object.prototype.hasOwnProperty;
  const hasOwn = (val, key) => hasOwnProperty.call(val, key);
  const defaultFormatter = new BaseFormatter();
  function include(str, parts) {
    return !!parts.find((part) => str.indexOf(part) !== -1);
  }
  function startsWith(str, parts) {
    return parts.find((part) => str.indexOf(part) === 0);
  }
  function normalizeLocale(locale, messages2) {
    if (!locale) {
      return;
    }
    locale = locale.trim().replace(/_/g, "-");
    if (messages2 && messages2[locale]) {
      return locale;
    }
    locale = locale.toLowerCase();
    if (locale === "chinese") {
      return LOCALE_ZH_HANS;
    }
    if (locale.indexOf("zh") === 0) {
      if (locale.indexOf("-hans") > -1) {
        return LOCALE_ZH_HANS;
      }
      if (locale.indexOf("-hant") > -1) {
        return LOCALE_ZH_HANT;
      }
      if (include(locale, ["-tw", "-hk", "-mo", "-cht"])) {
        return LOCALE_ZH_HANT;
      }
      return LOCALE_ZH_HANS;
    }
    let locales = [LOCALE_EN, LOCALE_FR, LOCALE_ES];
    if (messages2 && Object.keys(messages2).length > 0) {
      locales = Object.keys(messages2);
    }
    const lang = startsWith(locale, locales);
    if (lang) {
      return lang;
    }
  }
  class I18n {
    constructor({ locale, fallbackLocale, messages: messages2, watcher, formater: formater2 }) {
      this.locale = LOCALE_EN;
      this.fallbackLocale = LOCALE_EN;
      this.message = {};
      this.messages = {};
      this.watchers = [];
      if (fallbackLocale) {
        this.fallbackLocale = fallbackLocale;
      }
      this.formater = formater2 || defaultFormatter;
      this.messages = messages2 || {};
      this.setLocale(locale || LOCALE_EN);
      if (watcher) {
        this.watchLocale(watcher);
      }
    }
    setLocale(locale) {
      const oldLocale = this.locale;
      this.locale = normalizeLocale(locale, this.messages) || this.fallbackLocale;
      if (!this.messages[this.locale]) {
        this.messages[this.locale] = {};
      }
      this.message = this.messages[this.locale];
      if (oldLocale !== this.locale) {
        this.watchers.forEach((watcher) => {
          watcher(this.locale, oldLocale);
        });
      }
    }
    getLocale() {
      return this.locale;
    }
    watchLocale(fn) {
      const index = this.watchers.push(fn) - 1;
      return () => {
        this.watchers.splice(index, 1);
      };
    }
    add(locale, message, override = true) {
      const curMessages = this.messages[locale];
      if (curMessages) {
        if (override) {
          Object.assign(curMessages, message);
        } else {
          Object.keys(message).forEach((key) => {
            if (!hasOwn(curMessages, key)) {
              curMessages[key] = message[key];
            }
          });
        }
      } else {
        this.messages[locale] = message;
      }
    }
    f(message, values, delimiters) {
      return this.formater.interpolate(message, values, delimiters).join("");
    }
    t(key, locale, values) {
      let message = this.message;
      if (typeof locale === "string") {
        locale = normalizeLocale(locale, this.messages);
        locale && (message = this.messages[locale]);
      } else {
        values = locale;
      }
      if (!hasOwn(message, key)) {
        console.warn(`Cannot translate the value of keypath ${key}. Use the value of keypath as default.`);
        return key;
      }
      return this.formater.interpolate(message[key], values).join("");
    }
  }
  function watchAppLocale(appVm, i18n) {
    if (appVm.$watchLocale) {
      appVm.$watchLocale((newLocale) => {
        i18n.setLocale(newLocale);
      });
    } else {
      appVm.$watch(() => appVm.$locale, (newLocale) => {
        i18n.setLocale(newLocale);
      });
    }
  }
  function getDefaultLocale() {
    if (typeof uni !== "undefined" && uni.getLocale) {
      return uni.getLocale();
    }
    if (typeof global !== "undefined" && global.getLocale) {
      return global.getLocale();
    }
    return LOCALE_EN;
  }
  function initVueI18n(locale, messages2 = {}, fallbackLocale, watcher) {
    if (typeof locale !== "string") {
      const options = [
        messages2,
        locale
      ];
      locale = options[0];
      messages2 = options[1];
    }
    if (typeof locale !== "string") {
      locale = getDefaultLocale();
    }
    if (typeof fallbackLocale !== "string") {
      fallbackLocale = typeof __uniConfig !== "undefined" && __uniConfig.fallbackLocale || LOCALE_EN;
    }
    const i18n = new I18n({
      locale,
      fallbackLocale,
      messages: messages2,
      watcher
    });
    let t2 = (key, values) => {
      if (typeof getApp !== "function") {
        t2 = function(key2, values2) {
          return i18n.t(key2, values2);
        };
      } else {
        let isWatchedAppLocale = false;
        t2 = function(key2, values2) {
          const appVm = getApp().$vm;
          if (appVm) {
            appVm.$locale;
            if (!isWatchedAppLocale) {
              isWatchedAppLocale = true;
              watchAppLocale(appVm, i18n);
            }
          }
          return i18n.t(key2, values2);
        };
      }
      return t2(key, values);
    };
    return {
      i18n,
      f(message, values, delimiters) {
        return i18n.f(message, values, delimiters);
      },
      t(key, values) {
        return t2(key, values);
      },
      add(locale2, message, override = true) {
        return i18n.add(locale2, message, override);
      },
      watch(fn) {
        return i18n.watchLocale(fn);
      },
      getLocale() {
        return i18n.getLocale();
      },
      setLocale(newLocale) {
        return i18n.setLocale(newLocale);
      }
    };
  }
  const en$2 = {
    "zp.refresher.default": "Pull down to refresh",
    "zp.refresher.pulling": "Release to refresh",
    "zp.refresher.refreshing": "Refreshing...",
    "zp.refresher.complete": "Refresh succeeded",
    "zp.refresher.f2": "Refresh to enter 2f",
    "zp.loadingMore.default": "Click to load more",
    "zp.loadingMore.loading": "Loading...",
    "zp.loadingMore.noMore": "No more data",
    "zp.loadingMore.fail": "Load failed,click to reload",
    "zp.emptyView.title": "No data",
    "zp.emptyView.reload": "Reload",
    "zp.emptyView.error": "Sorry,load failed",
    "zp.refresherUpdateTime.title": "Last update: ",
    "zp.refresherUpdateTime.none": "None",
    "zp.refresherUpdateTime.today": "Today",
    "zp.refresherUpdateTime.yesterday": "Yesterday",
    "zp.systemLoading.title": "Loading..."
  };
  const zhHans$1 = {
    "zp.refresher.default": "继续下拉刷新",
    "zp.refresher.pulling": "松开立即刷新",
    "zp.refresher.refreshing": "正在刷新...",
    "zp.refresher.complete": "刷新成功",
    "zp.refresher.f2": "松手进入二楼",
    "zp.loadingMore.default": "点击加载更多",
    "zp.loadingMore.loading": "正在加载...",
    "zp.loadingMore.noMore": "没有更多了",
    "zp.loadingMore.fail": "加载失败，点击重新加载",
    "zp.emptyView.title": "没有数据哦~",
    "zp.emptyView.reload": "重新加载",
    "zp.emptyView.error": "很抱歉，加载失败",
    "zp.refresherUpdateTime.title": "最后更新：",
    "zp.refresherUpdateTime.none": "无",
    "zp.refresherUpdateTime.today": "今天",
    "zp.refresherUpdateTime.yesterday": "昨天",
    "zp.systemLoading.title": "加载中..."
  };
  const zhHant$1 = {
    "zp.refresher.default": "繼續下拉重繪",
    "zp.refresher.pulling": "鬆開立即重繪",
    "zp.refresher.refreshing": "正在重繪...",
    "zp.refresher.complete": "重繪成功",
    "zp.refresher.f2": "鬆手進入二樓",
    "zp.loadingMore.default": "點擊加載更多",
    "zp.loadingMore.loading": "正在加載...",
    "zp.loadingMore.noMore": "沒有更多了",
    "zp.loadingMore.fail": "加載失敗，點擊重新加載",
    "zp.emptyView.title": "沒有數據哦~",
    "zp.emptyView.reload": "重新加載",
    "zp.emptyView.error": "很抱歉，加載失敗",
    "zp.refresherUpdateTime.title": "最後更新：",
    "zp.refresherUpdateTime.none": "無",
    "zp.refresherUpdateTime.today": "今天",
    "zp.refresherUpdateTime.yesterday": "昨天",
    "zp.systemLoading.title": "加載中..."
  };
  const messages$1 = {
    en: en$2,
    "zh-Hans": zhHans$1,
    "zh-Hant": zhHant$1
  };
  const { t: t$2 } = initVueI18n(messages$1);
  const i18nModule = {
    computed: {
      finalLanguage() {
        try {
          const local = uni.getLocale();
          const language = this.systemInfo.appLanguage;
          return local === "auto" ? interceptor._handleLanguage2Local(language, this._language2Local(language)) : local;
        } catch (e2) {
          return "zh-Hans";
        }
      },
      // 最终的下拉刷新默认状态的文字
      finalRefresherDefaultText() {
        return this._getI18nText("zp.refresher.default", this.refresherDefaultText);
      },
      // 最终的下拉刷新下拉中的文字
      finalRefresherPullingText() {
        return this._getI18nText("zp.refresher.pulling", this.refresherPullingText);
      },
      // 最终的下拉刷新中文字
      finalRefresherRefreshingText() {
        return this._getI18nText("zp.refresher.refreshing", this.refresherRefreshingText);
      },
      // 最终的下拉刷新完成文字
      finalRefresherCompleteText() {
        return this._getI18nText("zp.refresher.complete", this.refresherCompleteText);
      },
      // 最终的下拉刷新上次更新时间文字
      finalRefresherUpdateTimeTextMap() {
        return {
          title: t$2("zp.refresherUpdateTime.title"),
          none: t$2("zp.refresherUpdateTime.none"),
          today: t$2("zp.refresherUpdateTime.today"),
          yesterday: t$2("zp.refresherUpdateTime.yesterday")
        };
      },
      // 最终的继续下拉进入二楼文字
      finalRefresherGoF2Text() {
        return this._getI18nText("zp.refresher.f2", this.refresherGoF2Text);
      },
      // 最终的底部加载更多默认状态文字
      finalLoadingMoreDefaultText() {
        return this._getI18nText("zp.loadingMore.default", this.loadingMoreDefaultText);
      },
      // 最终的底部加载更多加载中文字
      finalLoadingMoreLoadingText() {
        return this._getI18nText("zp.loadingMore.loading", this.loadingMoreLoadingText);
      },
      // 最终的底部加载更多没有更多数据文字
      finalLoadingMoreNoMoreText() {
        return this._getI18nText("zp.loadingMore.noMore", this.loadingMoreNoMoreText);
      },
      // 最终的底部加载更多加载失败文字
      finalLoadingMoreFailText() {
        return this._getI18nText("zp.loadingMore.fail", this.loadingMoreFailText);
      },
      // 最终的空数据图title
      finalEmptyViewText() {
        return this.isLoadFailed ? this.finalEmptyViewErrorText : this._getI18nText("zp.emptyView.title", this.emptyViewText);
      },
      // 最终的空数据图reload title
      finalEmptyViewReloadText() {
        return this._getI18nText("zp.emptyView.reload", this.emptyViewReloadText);
      },
      // 最终的空数据图加载失败文字
      finalEmptyViewErrorText() {
        return this.customerEmptyViewErrorText || this._getI18nText("zp.emptyView.error", this.emptyViewErrorText);
      },
      // 最终的系统loading title
      finalSystemLoadingText() {
        return this._getI18nText("zp.systemLoading.title", this.systemLoadingText);
      }
    },
    methods: {
      // 获取当前z-paging的语言
      getLanguage() {
        return this.finalLanguage;
      },
      // 获取国际化转换后的文本
      _getI18nText(key, value) {
        const dataType = Object.prototype.toString.call(value);
        if (dataType === "[object Object]") {
          const nextValue = value[this.finalLanguage];
          if (nextValue)
            return nextValue;
        } else if (dataType === "[object String]") {
          return value;
        }
        return t$2(key);
      },
      // 系统language转i18n local
      _language2Local(language) {
        const formatedLanguage = language.toLowerCase().replace(new RegExp("_", ""), "-");
        if (formatedLanguage.indexOf("zh") !== -1) {
          if (formatedLanguage === "zh" || formatedLanguage === "zh-cn" || formatedLanguage.indexOf("zh-hans") !== -1) {
            return "zh-Hans";
          }
          return "zh-Hant";
        }
        if (formatedLanguage.indexOf("en") !== -1)
          return "en";
        return language;
      }
    }
  };
  const nvueModule = {
    props: {},
    data() {
      return {
        nRefresherLoading: false,
        nListIsDragging: false,
        nShowBottom: true,
        nFixFreezing: false,
        nShowRefresherReveal: false,
        nLoadingMoreFixedHeight: false,
        nShowRefresherRevealHeight: 0,
        nOldShowRefresherRevealHeight: -1,
        nRefresherWidth: u$1.rpx2px(750),
        nListHeight: 0,
        nF2Opacity: 0
      };
    },
    computed: {},
    mounted() {
    },
    methods: {}
  };
  const emptyModule = {
    props: {
      // 是否强制隐藏空数据图，默认为否
      hideEmptyView: {
        type: Boolean,
        default: u$1.gc("hideEmptyView", false)
      },
      // 空数据图描述文字，默认为“没有数据哦~”
      emptyViewText: {
        type: [String, Object],
        default: u$1.gc("emptyViewText", null)
      },
      // 是否显示空数据图重新加载按钮(无数据时)，默认为否
      showEmptyViewReload: {
        type: Boolean,
        default: u$1.gc("showEmptyViewReload", false)
      },
      // 加载失败时是否显示空数据图重新加载按钮，默认为是
      showEmptyViewReloadWhenError: {
        type: Boolean,
        default: u$1.gc("showEmptyViewReloadWhenError", true)
      },
      // 空数据图点击重新加载文字，默认为“重新加载”
      emptyViewReloadText: {
        type: [String, Object],
        default: u$1.gc("emptyViewReloadText", null)
      },
      // 空数据图图片，默认使用z-paging内置的图片
      emptyViewImg: {
        type: String,
        default: u$1.gc("emptyViewImg", "")
      },
      // 空数据图“加载失败”描述文字，默认为“很抱歉，加载失败”
      emptyViewErrorText: {
        type: [String, Object],
        default: u$1.gc("emptyViewErrorText", null)
      },
      // 空数据图“加载失败”图片，默认使用z-paging内置的图片
      emptyViewErrorImg: {
        type: String,
        default: u$1.gc("emptyViewErrorImg", "")
      },
      // 空数据图样式
      emptyViewStyle: {
        type: Object,
        default: u$1.gc("emptyViewStyle", {})
      },
      // 空数据图容器样式
      emptyViewSuperStyle: {
        type: Object,
        default: u$1.gc("emptyViewSuperStyle", {})
      },
      // 空数据图img样式
      emptyViewImgStyle: {
        type: Object,
        default: u$1.gc("emptyViewImgStyle", {})
      },
      // 空数据图描述文字样式
      emptyViewTitleStyle: {
        type: Object,
        default: u$1.gc("emptyViewTitleStyle", {})
      },
      // 空数据图重新加载按钮样式
      emptyViewReloadStyle: {
        type: Object,
        default: u$1.gc("emptyViewReloadStyle", {})
      },
      // 空数据图片是否铺满z-paging，默认为否，即填充满z-paging内列表(滚动区域)部分。若设置为否，则为填铺满整个z-paging
      emptyViewFixed: {
        type: Boolean,
        default: u$1.gc("emptyViewFixed", false)
      },
      // 空数据图片是否垂直居中，默认为是，若设置为否即为从空数据容器顶部开始显示。emptyViewFixed为false时有效
      emptyViewCenter: {
        type: Boolean,
        default: u$1.gc("emptyViewCenter", true)
      },
      // 加载中时是否自动隐藏空数据图，默认为是
      autoHideEmptyViewWhenLoading: {
        type: Boolean,
        default: u$1.gc("autoHideEmptyViewWhenLoading", true)
      },
      // 用户下拉列表触发下拉刷新加载中时是否自动隐藏空数据图，默认为是
      autoHideEmptyViewWhenPull: {
        type: Boolean,
        default: u$1.gc("autoHideEmptyViewWhenPull", true)
      },
      // 空数据view的z-index，默认为9
      emptyViewZIndex: {
        type: Number,
        default: u$1.gc("emptyViewZIndex", 9)
      }
    },
    data() {
      return {
        customerEmptyViewErrorText: ""
      };
    },
    computed: {
      finalEmptyViewImg() {
        return this.isLoadFailed ? this.emptyViewErrorImg : this.emptyViewImg;
      },
      finalShowEmptyViewReload() {
        return this.isLoadFailed ? this.showEmptyViewReloadWhenError : this.showEmptyViewReload;
      },
      // 是否展示空数据图
      showEmpty() {
        if (this.isOnly || this.hideEmptyView || this.realTotalData.length)
          return false;
        if (this.autoHideEmptyViewWhenLoading) {
          if (this.isAddedData && !this.firstPageLoaded && !this.loading)
            return true;
        } else {
          return true;
        }
        return !this.autoHideEmptyViewWhenPull && !this.isUserReload;
      }
    },
    methods: {
      // 点击了空数据view重新加载按钮
      _emptyViewReload() {
        let callbacked = false;
        this.$emit("emptyViewReload", (reload) => {
          if (reload === void 0 || reload === true) {
            this.fromEmptyViewReload = true;
            this.reload().catch(() => {
            });
          }
          callbacked = true;
        });
        this.$nextTick(() => {
          if (!callbacked) {
            this.fromEmptyViewReload = true;
            this.reload().catch(() => {
            });
          }
        });
      },
      // 点击了空数据view
      _emptyViewClick() {
        this.$emit("emptyViewClick");
      }
    }
  };
  const refresherModule = {
    props: {
      // 下拉刷新的主题样式，支持black，white，默认black
      refresherThemeStyle: {
        type: String,
        default: u$1.gc("refresherThemeStyle", "")
      },
      // 自定义下拉刷新中左侧图标的样式
      refresherImgStyle: {
        type: Object,
        default: u$1.gc("refresherImgStyle", {})
      },
      // 自定义下拉刷新中右侧状态描述文字的样式
      refresherTitleStyle: {
        type: Object,
        default: u$1.gc("refresherTitleStyle", {})
      },
      // 自定义下拉刷新中右侧最后更新时间文字的样式(show-refresher-update-time为true时有效)
      refresherUpdateTimeStyle: {
        type: Object,
        default: u$1.gc("refresherUpdateTimeStyle", {})
      },
      // 在微信小程序和QQ小程序中，是否实时监听下拉刷新中进度，默认为否
      watchRefresherTouchmove: {
        type: Boolean,
        default: u$1.gc("watchRefresherTouchmove", false)
      },
      // 底部加载更多的主题样式，支持black，white，默认black
      loadingMoreThemeStyle: {
        type: String,
        default: u$1.gc("loadingMoreThemeStyle", "")
      },
      // 是否只使用下拉刷新，设置为true后将关闭mounted自动请求数据、关闭滚动到底部加载更多，强制隐藏空数据图。默认为否
      refresherOnly: {
        type: Boolean,
        default: u$1.gc("refresherOnly", false)
      },
      // 自定义下拉刷新默认状态下回弹动画时间，单位为毫秒，默认为100毫秒，nvue无效
      refresherDefaultDuration: {
        type: [Number, String],
        default: u$1.gc("refresherDefaultDuration", 100)
      },
      // 自定义下拉刷新结束以后延迟回弹的时间，单位为毫秒，默认为0
      refresherCompleteDelay: {
        type: [Number, String],
        default: u$1.gc("refresherCompleteDelay", 0)
      },
      // 自定义下拉刷新结束回弹动画时间，单位为毫秒，默认为300毫秒(refresherEndBounceEnabled为false时，refresherCompleteDuration为设定值的1/3)，nvue无效
      refresherCompleteDuration: {
        type: [Number, String],
        default: u$1.gc("refresherCompleteDuration", 300)
      },
      // 自定义下拉刷新中是否允许列表滚动，默认为是
      refresherRefreshingScrollable: {
        type: Boolean,
        default: u$1.gc("refresherRefreshingScrollable", true)
      },
      // 自定义下拉刷新结束状态下是否允许列表滚动，默认为否
      refresherCompleteScrollable: {
        type: Boolean,
        default: u$1.gc("refresherCompleteScrollable", false)
      },
      // 是否使用自定义的下拉刷新，默认为是，即使用z-paging的下拉刷新。设置为false即代表使用uni scroll-view自带的下拉刷新，h5、App、微信小程序以外的平台不支持uni scroll-view自带的下拉刷新
      useCustomRefresher: {
        type: Boolean,
        default: u$1.gc("useCustomRefresher", true)
      },
      // 自定义下拉刷新下拉帧率，默认为40，过高可能会出现抖动问题
      refresherFps: {
        type: [Number, String],
        default: u$1.gc("refresherFps", 40)
      },
      // 自定义下拉刷新允许触发的最大下拉角度，默认为40度，当下拉角度小于设定值时，自定义下拉刷新动画不会被触发
      refresherMaxAngle: {
        type: [Number, String],
        default: u$1.gc("refresherMaxAngle", 40)
      },
      // 自定义下拉刷新的角度由未达到最大角度变到达到最大角度时，是否继续下拉刷新手势，默认为否
      refresherAngleEnableChangeContinued: {
        type: Boolean,
        default: u$1.gc("refresherAngleEnableChangeContinued", false)
      },
      // 自定义下拉刷新默认状态下的文字
      refresherDefaultText: {
        type: [String, Object],
        default: u$1.gc("refresherDefaultText", null)
      },
      // 自定义下拉刷新松手立即刷新状态下的文字
      refresherPullingText: {
        type: [String, Object],
        default: u$1.gc("refresherPullingText", null)
      },
      // 自定义下拉刷新刷新中状态下的文字
      refresherRefreshingText: {
        type: [String, Object],
        default: u$1.gc("refresherRefreshingText", null)
      },
      // 自定义下拉刷新刷新结束状态下的文字
      refresherCompleteText: {
        type: [String, Object],
        default: u$1.gc("refresherCompleteText", null)
      },
      // 自定义继续下拉进入二楼文字
      refresherGoF2Text: {
        type: [String, Object],
        default: u$1.gc("refresherGoF2Text", null)
      },
      // 自定义下拉刷新默认状态下的图片
      refresherDefaultImg: {
        type: String,
        default: u$1.gc("refresherDefaultImg", null)
      },
      // 自定义下拉刷新松手立即刷新状态下的图片，默认与refresherDefaultImg一致
      refresherPullingImg: {
        type: String,
        default: u$1.gc("refresherPullingImg", null)
      },
      // 自定义下拉刷新刷新中状态下的图片
      refresherRefreshingImg: {
        type: String,
        default: u$1.gc("refresherRefreshingImg", null)
      },
      // 自定义下拉刷新刷新结束状态下的图片
      refresherCompleteImg: {
        type: String,
        default: u$1.gc("refresherCompleteImg", null)
      },
      // 自定义下拉刷新刷新中状态下是否展示旋转动画
      refresherRefreshingAnimated: {
        type: Boolean,
        default: u$1.gc("refresherRefreshingAnimated", true)
      },
      // 是否开启自定义下拉刷新刷新结束回弹效果，默认为是
      refresherEndBounceEnabled: {
        type: Boolean,
        default: u$1.gc("refresherEndBounceEnabled", true)
      },
      // 是否开启自定义下拉刷新，默认为是
      refresherEnabled: {
        type: Boolean,
        default: u$1.gc("refresherEnabled", true)
      },
      // 设置自定义下拉刷新阈值，默认为80rpx
      refresherThreshold: {
        type: [Number, String],
        default: u$1.gc("refresherThreshold", "80rpx")
      },
      // 设置系统下拉刷新默认样式，支持设置 black，white，none，none 表示不使用默认样式，默认为black
      refresherDefaultStyle: {
        type: String,
        default: u$1.gc("refresherDefaultStyle", "black")
      },
      // 设置自定义下拉刷新区域背景
      refresherBackground: {
        type: String,
        default: u$1.gc("refresherBackground", "transparent")
      },
      // 设置固定的自定义下拉刷新区域背景
      refresherFixedBackground: {
        type: String,
        default: u$1.gc("refresherFixedBackground", "transparent")
      },
      // 设置固定的自定义下拉刷新区域高度，默认为0
      refresherFixedBacHeight: {
        type: [Number, String],
        default: u$1.gc("refresherFixedBacHeight", 0)
      },
      // 设置自定义下拉刷新下拉超出阈值后继续下拉位移衰减的比例，范围0-1，值越大代表衰减越多。默认为0.65(nvue无效)
      refresherOutRate: {
        type: Number,
        default: u$1.gc("refresherOutRate", 0.65)
      },
      // 是否开启下拉进入二楼功能，默认为否
      refresherF2Enabled: {
        type: Boolean,
        default: u$1.gc("refresherF2Enabled", false)
      },
      // 下拉进入二楼阈值，默认为200rpx
      refresherF2Threshold: {
        type: [Number, String],
        default: u$1.gc("refresherF2Threshold", "200rpx")
      },
      // 下拉进入二楼动画时间，单位为毫秒，默认为200毫秒
      refresherF2Duration: {
        type: [Number, String],
        default: u$1.gc("refresherF2Duration", 200)
      },
      // 下拉进入二楼状态松手后是否弹出二楼，默认为是
      showRefresherF2: {
        type: Boolean,
        default: u$1.gc("showRefresherF2", true)
      },
      // 设置自定义下拉刷新下拉时实际下拉位移与用户下拉距离的比值，默认为0.75，即代表若用户下拉10px，则实际位移为7.5px(nvue无效)
      refresherPullRate: {
        type: Number,
        default: u$1.gc("refresherPullRate", 0.75)
      },
      // 是否显示最后更新时间，默认为否
      showRefresherUpdateTime: {
        type: Boolean,
        default: u$1.gc("showRefresherUpdateTime", false)
      },
      // 如果需要区别不同页面的最后更新时间，请为不同页面的z-paging的`refresher-update-time-key`设置不同的字符串
      refresherUpdateTimeKey: {
        type: String,
        default: u$1.gc("refresherUpdateTimeKey", "default")
      },
      // 下拉刷新时下拉到“松手立即刷新”或“松手进入二楼”状态时是否使手机短振动，默认为否（h5无效）
      refresherVibrate: {
        type: Boolean,
        default: u$1.gc("refresherVibrate", false)
      },
      // 下拉刷新时是否禁止下拉刷新view跟随用户触摸竖直移动，默认为否。注意此属性只是禁止下拉刷新view移动，其他下拉刷新逻辑依然会正常触发
      refresherNoTransform: {
        type: Boolean,
        default: u$1.gc("refresherNoTransform", false)
      },
      // 是否开启下拉刷新状态栏占位，适用于隐藏导航栏时，下拉刷新需要避开状态栏高度的情况，默认为否
      useRefresherStatusBarPlaceholder: {
        type: Boolean,
        default: u$1.gc("useRefresherStatusBarPlaceholder", false)
      }
    },
    data() {
      return {
        R: Enum.Refresher,
        //下拉刷新状态
        refresherStatus: Enum.Refresher.Default,
        refresherTouchstartY: 0,
        lastRefresherTouchmove: null,
        refresherReachMaxAngle: true,
        refresherTransform: "translateY(0px)",
        refresherTransition: "",
        finalRefresherDefaultStyle: "black",
        refresherRevealStackCount: 0,
        refresherCompleteTimeout: null,
        refresherCompleteSubTimeout: null,
        refresherEndTimeout: null,
        isTouchmovingTimeout: null,
        refresherTriggered: false,
        isTouchmoving: false,
        isTouchEnded: false,
        isUserPullDown: false,
        privateRefresherEnabled: -1,
        privateShowRefresherWhenReload: false,
        customRefresherHeight: -1,
        showCustomRefresher: false,
        doRefreshAnimateAfter: false,
        isRefresherInComplete: false,
        showF2: false,
        f2Transform: "",
        pullDownTimeStamp: 0,
        moveDis: 0,
        oldMoveDis: 0,
        currentDis: 0,
        oldCurrentMoveDis: 0,
        oldRefresherTouchmoveY: 0,
        oldTouchDirection: "",
        oldEmitedTouchDirection: "",
        oldPullingDistance: -1,
        refresherThresholdUpdateTag: 0
      };
    },
    watch: {
      refresherDefaultStyle: {
        handler(newVal) {
          if (newVal.length) {
            this.finalRefresherDefaultStyle = newVal;
          }
        },
        immediate: true
      },
      refresherStatus(newVal) {
        newVal === Enum.Refresher.Loading && this._cleanRefresherEndTimeout();
        this.refresherVibrate && (newVal === Enum.Refresher.ReleaseToRefresh || newVal === Enum.Refresher.GoF2) && this._doVibrateShort();
        this.$emit("refresherStatusChange", newVal);
        this.$emit("update:refresherStatus", newVal);
      },
      // 监听当前下拉刷新启用/禁用状态
      refresherEnabled(newVal) {
        !newVal && this.endRefresh();
      }
    },
    computed: {
      pullDownDisTimeStamp() {
        return 1e3 / this.refresherFps;
      },
      refresherThresholdUnitConverted() {
        return u$1.addUnit(this.refresherThreshold, this.unit);
      },
      finalRefresherEnabled() {
        if (this.layoutOnly || this.useChatRecordMode)
          return false;
        if (this.privateRefresherEnabled === -1)
          return this.refresherEnabled;
        return this.privateRefresherEnabled === 1;
      },
      finalRefresherThreshold() {
        let refresherThreshold = this.refresherThresholdUnitConverted;
        let idDefault = false;
        if (refresherThreshold === u$1.addUnit(80, this.unit)) {
          idDefault = true;
          if (this.showRefresherUpdateTime) {
            refresherThreshold = u$1.addUnit(120, this.unit);
          }
        }
        if (idDefault && this.customRefresherHeight > 0)
          return this.customRefresherHeight + this.finalRefresherThresholdPlaceholder;
        return u$1.convertToPx(refresherThreshold) + this.finalRefresherThresholdPlaceholder;
      },
      finalRefresherF2Threshold() {
        return u$1.convertToPx(u$1.addUnit(this.refresherF2Threshold, this.unit));
      },
      finalRefresherThresholdPlaceholder() {
        return this.useRefresherStatusBarPlaceholder ? this.statusBarHeight : 0;
      },
      finalRefresherFixedBacHeight() {
        return u$1.convertToPx(this.refresherFixedBacHeight);
      },
      finalRefresherThemeStyle() {
        return this.refresherThemeStyle.length ? this.refresherThemeStyle : this.defaultThemeStyle;
      },
      finalRefresherOutRate() {
        let rate = this.refresherOutRate;
        rate = Math.max(0, rate);
        rate = Math.min(1, rate);
        return rate;
      },
      finalRefresherPullRate() {
        let rate = this.refresherPullRate;
        rate = Math.max(0, rate);
        return rate;
      },
      finalRefresherTransform() {
        if (this.refresherNoTransform || this.refresherTransform === "translateY(0px)")
          return "none";
        return this.refresherTransform;
      },
      finalShowRefresherWhenReload() {
        return this.showRefresherWhenReload || this.privateShowRefresherWhenReload;
      },
      finalRefresherTriggered() {
        if (!(this.finalRefresherEnabled && !this.useCustomRefresher))
          return false;
        return this.refresherTriggered;
      },
      showRefresher() {
        const showRefresher = this.finalRefresherEnabled || this.useCustomRefresher && !this.useChatRecordMode;
        this.active && this.customRefresherHeight === -1 && showRefresher && this.updateCustomRefresherHeight();
        return showRefresher;
      },
      hasTouchmove() {
        return this.watchRefresherTouchmove;
      }
    },
    methods: {
      // 终止下拉刷新状态
      endRefresh() {
        this.totalData = this.realTotalData;
        this._refresherEnd();
        this._endSystemLoadingAndRefresh();
        this._handleScrollViewBounce({ bounce: true });
        this.$nextTick(() => {
          this.refresherTriggered = false;
        });
      },
      // 手动更新自定义下拉刷新view高度
      updateCustomRefresherHeight() {
        u$1.delay(() => this.$nextTick(this._updateCustomRefresherHeight));
      },
      // 进入二楼
      goF2() {
        this._handleGoF2();
      },
      // 关闭二楼
      closeF2() {
        this._handleCloseF2();
      },
      // 自定义下拉刷新被触发
      _onRefresh(fromScrollView = false, isUserPullDown = true) {
        if (fromScrollView && !(this.finalRefresherEnabled && !this.useCustomRefresher))
          return;
        this.$emit("onRefresh");
        this.$emit("Refresh");
        if (this.loading || this.isRefresherInComplete)
          return;
        this.loadingType = Enum.LoadingType.Refresher;
        if (this.nShowRefresherReveal)
          return;
        this.isUserPullDown = isUserPullDown;
        this.isUserReload = !isUserPullDown;
        this._startLoading(true);
        this.refresherTriggered = true;
        if (this.reloadWhenRefresh && isUserPullDown) {
          this.useChatRecordMode ? this._onLoadingMore("click") : this._reload(false, false, isUserPullDown);
        }
      },
      // 自定义下拉刷新被复位
      _onRestore() {
        this.refresherTriggered = "restore";
        this.$emit("onRestore");
        this.$emit("Restore");
      },
      // 进一步处理touch开始结果
      _handleRefresherTouchstart(touch) {
        if (!this.loading && this.isTouchEnded) {
          this.isTouchmoving = false;
        }
        this.loadingType = Enum.LoadingType.Refresher;
        this.isTouchmovingTimeout && clearTimeout(this.isTouchmovingTimeout);
        this.isTouchEnded = false;
        this.refresherTransition = "";
        this.refresherTouchstartY = touch.touchY;
        this.$emit("refresherTouchstart", this.refresherTouchstartY);
        this.lastRefresherTouchmove = touch;
        this._cleanRefresherCompleteTimeout();
        this._cleanRefresherEndTimeout();
      },
      // 非app-vue或微信小程序或QQ小程序或h5平台，使用js控制下拉刷新
      // 进一步处理touch中结果
      _handleRefresherTouchmove(moveDis, touch) {
        this.refresherReachMaxAngle = true;
        this.isTouchmovingTimeout && clearTimeout(this.isTouchmovingTimeout);
        this.isTouchmoving = true;
        this.isTouchEnded = false;
        if (moveDis >= this.finalRefresherThreshold) {
          this.refresherStatus = this.refresherF2Enabled && moveDis >= this.finalRefresherF2Threshold ? Enum.Refresher.GoF2 : Enum.Refresher.ReleaseToRefresh;
        } else {
          this.refresherStatus = Enum.Refresher.Default;
        }
        this.moveDis = moveDis;
      },
      // 进一步处理touch结束结果
      _handleRefresherTouchend(moveDis) {
        this.isTouchmovingTimeout && clearTimeout(this.isTouchmovingTimeout);
        this.refresherReachMaxAngle = true;
        this.isTouchEnded = true;
        const refresherThreshold = this.finalRefresherThreshold;
        if (moveDis >= refresherThreshold && [Enum.Refresher.ReleaseToRefresh, Enum.Refresher.GoF2].indexOf(this.refresherStatus) >= 0) {
          if (this.refresherStatus === Enum.Refresher.GoF2) {
            this._handleGoF2();
            this._refresherEnd();
          } else {
            u$1.delay(() => {
              this._emitTouchmove({ pullingDistance: refresherThreshold, dy: this.moveDis - refresherThreshold });
            }, 0.1);
            this.moveDis = refresherThreshold;
            this.refresherStatus = Enum.Refresher.Loading;
            this._doRefresherLoad();
          }
        } else {
          this._refresherEnd();
          this.isTouchmovingTimeout = u$1.delay(() => {
            this.isTouchmoving = false;
          }, this.refresherDefaultDuration);
        }
        this.scrollEnable = true;
        this.$emit("refresherTouchend", moveDis);
      },
      // 处理列表触摸开始事件
      _handleListTouchstart() {
        if (this.useChatRecordMode && this.autoHideKeyboardWhenChat) {
          uni.hideKeyboard();
          this.$emit("hidedKeyboard");
        }
      },
      // 处理scroll-view bounce是否生效
      _handleScrollViewBounce({ bounce }) {
        if (!this.usePageScroll && !this.scrollToTopBounceEnabled) {
          if (this.wxsScrollTop <= 5) {
            this.refresherTransition = "";
            this.scrollEnable = bounce;
          } else if (bounce) {
            this.scrollEnable = bounce;
          }
        }
      },
      // wxs正在下拉状态改变处理
      _handleWxsPullingDownStatusChange(onPullingDown) {
        this.wxsOnPullingDown = onPullingDown;
        if (onPullingDown && !this.useChatRecordMode) {
          this.renderPropScrollTop = 0;
        }
      },
      // wxs正在下拉处理
      _handleWxsPullingDown({ moveDis, diffDis }) {
        this._emitTouchmove({ pullingDistance: moveDis, dy: diffDis });
      },
      // wxs触摸方向改变
      _handleTouchDirectionChange({ direction }) {
        this.$emit("touchDirectionChange", direction);
      },
      // wxs通知更新其props
      _handlePropUpdate() {
        this.wxsPropType = u$1.getTime().toString();
      },
      // 下拉刷新结束
      _refresherEnd(shouldEndLoadingDelay = true, fromAddData = false, isUserPullDown = false, setLoading = true) {
        if (this.loadingType === Enum.LoadingType.Refresher) {
          const refresherCompleteDelay = fromAddData && (isUserPullDown || this.finalShowRefresherWhenReload) ? this.refresherCompleteDelay : 0;
          const refresherStatus = refresherCompleteDelay > 0 ? Enum.Refresher.Complete : Enum.Refresher.Default;
          if (this.finalShowRefresherWhenReload) {
            const stackCount = this.refresherRevealStackCount;
            this.refresherRevealStackCount--;
            if (stackCount > 1)
              return;
          }
          this._cleanRefresherEndTimeout();
          this.refresherEndTimeout = u$1.delay(() => {
            this.refresherStatus = refresherStatus;
            if (refresherStatus !== Enum.Refresher.Complete) {
              this.isRefresherInComplete = false;
            }
          }, this.refresherStatus !== Enum.Refresher.Default && refresherStatus === Enum.Refresher.Default ? this.refresherCompleteDuration : 0);
          if (refresherCompleteDelay > 0) {
            this.isRefresherInComplete = true;
          }
          this._cleanRefresherCompleteTimeout();
          this.refresherCompleteTimeout = u$1.delay(() => {
            let animateDuration = 1;
            const animateType = this.refresherEndBounceEnabled && fromAddData ? "cubic-bezier(0.19,1.64,0.42,0.72)" : "linear";
            if (fromAddData) {
              animateDuration = this.refresherEndBounceEnabled ? this.refresherCompleteDuration / 1e3 : this.refresherCompleteDuration / 3e3;
            }
            this.refresherTransition = `transform ${fromAddData ? animateDuration : this.refresherDefaultDuration / 1e3}s ${animateType}`;
            this.wxsPropType = this.refresherTransition + "end" + u$1.getTime();
            this.moveDis = 0;
            if (refresherStatus === Enum.Refresher.Complete) {
              if (this.refresherCompleteSubTimeout) {
                clearTimeout(this.refresherCompleteSubTimeout);
                this.refresherCompleteSubTimeout = null;
              }
              this.refresherCompleteSubTimeout = u$1.delay(() => {
                this.$nextTick(() => {
                  this.refresherStatus = Enum.Refresher.Default;
                  this.isRefresherInComplete = false;
                });
              }, animateDuration * 800);
            }
            this._emitTouchmove({ pullingDistance: 0, dy: this.moveDis });
          }, refresherCompleteDelay);
        }
        if (setLoading) {
          u$1.delay(() => this.loading = false, shouldEndLoadingDelay ? 10 : 0);
          isUserPullDown && this._onRestore();
        }
      },
      // 处理进入二楼
      _handleGoF2() {
        if (this.showF2 || !this.refresherF2Enabled)
          return;
        this.$emit("refresherF2Change", "go");
        if (!this.showRefresherF2)
          return;
        this.f2Transform = `translateY(${-this.superContentHeight}px)`;
        this.showF2 = true;
        u$1.delay(() => {
          this.f2Transform = "translateY(0px)";
        }, 100, "f2ShowDelay");
      },
      // 处理退出二楼
      _handleCloseF2() {
        if (!this.showF2 || !this.refresherF2Enabled)
          return;
        this.$emit("refresherF2Change", "close");
        if (!this.showRefresherF2)
          return;
        this.f2Transform = `translateY(${-this.superContentHeight}px)`;
        u$1.delay(() => {
          this.showF2 = false;
          this.nF2Opacity = 0;
        }, this.refresherF2Duration, "f2CloseDelay");
      },
      // 模拟用户手动触发下拉刷新
      _doRefresherRefreshAnimate() {
        this._cleanRefresherCompleteTimeout();
        const doRefreshAnimateAfter = !this.doRefreshAnimateAfter && this.finalShowRefresherWhenReload && this.customRefresherHeight === -1 && this.refresherThreshold === u$1.addUnit(80, this.unit);
        if (doRefreshAnimateAfter) {
          this.doRefreshAnimateAfter = true;
          return;
        }
        this.refresherRevealStackCount++;
        this.wxsPropType = "begin" + u$1.getTime();
        this.moveDis = this.finalRefresherThreshold;
        this.refresherStatus = Enum.Refresher.Loading;
        this.isTouchmoving = true;
        this.isTouchmovingTimeout && clearTimeout(this.isTouchmovingTimeout);
        this._doRefresherLoad(false);
      },
      // 触发下拉刷新
      _doRefresherLoad(isUserPullDown = true) {
        this._onRefresh(false, isUserPullDown);
        this.loading = true;
      },
      // 更新自定义下拉刷新view高度
      _updateCustomRefresherHeight() {
        this._getNodeClientRect(".zp-custom-refresher-slot-view").then((res) => {
          this.customRefresherHeight = res ? res[0].height : 0;
          this.showCustomRefresher = this.customRefresherHeight > 0;
          if (this.doRefreshAnimateAfter) {
            this.doRefreshAnimateAfter = false;
            this._doRefresherRefreshAnimate();
          }
        });
      },
      // emit pullingDown事件
      _emitTouchmove(e2) {
        e2.viewHeight = this.finalRefresherThreshold;
        e2.rate = e2.viewHeight > 0 ? e2.pullingDistance / e2.viewHeight : 0;
        this.hasTouchmove && this.oldPullingDistance !== e2.pullingDistance && this.$emit("refresherTouchmove", e2);
        this.oldPullingDistance = e2.pullingDistance;
      },
      // 清除refresherCompleteTimeout
      _cleanRefresherCompleteTimeout() {
        this.refresherCompleteTimeout = this._cleanTimeout(this.refresherCompleteTimeout);
      },
      // 清除refresherEndTimeout
      _cleanRefresherEndTimeout() {
        this.refresherEndTimeout = this._cleanTimeout(this.refresherEndTimeout);
      }
    }
  };
  const loadMoreModule = {
    props: {
      // 自定义底部加载更多样式
      loadingMoreCustomStyle: {
        type: Object,
        default: u$1.gc("loadingMoreCustomStyle", {})
      },
      // 自定义底部加载更多文字样式
      loadingMoreTitleCustomStyle: {
        type: Object,
        default: u$1.gc("loadingMoreTitleCustomStyle", {})
      },
      // 自定义底部加载更多加载中动画样式
      loadingMoreLoadingIconCustomStyle: {
        type: Object,
        default: u$1.gc("loadingMoreLoadingIconCustomStyle", {})
      },
      // 自定义底部加载更多加载中动画图标类型，可选flower或circle，默认为flower
      loadingMoreLoadingIconType: {
        type: String,
        default: u$1.gc("loadingMoreLoadingIconType", "flower")
      },
      // 自定义底部加载更多加载中动画图标图片
      loadingMoreLoadingIconCustomImage: {
        type: String,
        default: u$1.gc("loadingMoreLoadingIconCustomImage", "")
      },
      // 底部加载更多加载中view是否展示旋转动画，默认为是
      loadingMoreLoadingAnimated: {
        type: Boolean,
        default: u$1.gc("loadingMoreLoadingAnimated", true)
      },
      // 是否启用加载更多数据(含滑动到底部加载更多数据和点击加载更多数据)，默认为是
      loadingMoreEnabled: {
        type: Boolean,
        default: u$1.gc("loadingMoreEnabled", true)
      },
      // 是否启用滑动到底部加载更多数据，默认为是
      toBottomLoadingMoreEnabled: {
        type: Boolean,
        default: u$1.gc("toBottomLoadingMoreEnabled", true)
      },
      // 滑动到底部状态为默认状态时，以加载中的状态展示，默认为否。若设置为是，可避免滚动到底部看到默认状态然后立刻变为加载中状态的问题，但分页数量未超过一屏时，不会显示【点击加载更多】
      loadingMoreDefaultAsLoading: {
        type: Boolean,
        default: u$1.gc("loadingMoreDefaultAsLoading", false)
      },
      // 滑动到底部"默认"文字，默认为【点击加载更多】
      loadingMoreDefaultText: {
        type: [String, Object],
        default: u$1.gc("loadingMoreDefaultText", null)
      },
      // 滑动到底部"加载中"文字，默认为【正在加载...】
      loadingMoreLoadingText: {
        type: [String, Object],
        default: u$1.gc("loadingMoreLoadingText", null)
      },
      // 滑动到底部"没有更多"文字，默认为【没有更多了】
      loadingMoreNoMoreText: {
        type: [String, Object],
        default: u$1.gc("loadingMoreNoMoreText", null)
      },
      // 滑动到底部"加载失败"文字，默认为【加载失败，点击重新加载】
      loadingMoreFailText: {
        type: [String, Object],
        default: u$1.gc("loadingMoreFailText", null)
      },
      // 当没有更多数据且分页内容未超出z-paging时是否隐藏没有更多数据的view，默认为否
      hideNoMoreInside: {
        type: Boolean,
        default: u$1.gc("hideNoMoreInside", false)
      },
      // 当没有更多数据且分页数组长度少于这个值时，隐藏没有更多数据的view，默认为0，代表不限制。
      hideNoMoreByLimit: {
        type: Number,
        default: u$1.gc("hideNoMoreByLimit", 0)
      },
      // 是否显示默认的加载更多text，默认为是
      showDefaultLoadingMoreText: {
        type: Boolean,
        default: u$1.gc("showDefaultLoadingMoreText", true)
      },
      // 是否显示没有更多数据的view
      showLoadingMoreNoMoreView: {
        type: Boolean,
        default: u$1.gc("showLoadingMoreNoMoreView", true)
      },
      // 是否显示没有更多数据的分割线，默认为是
      showLoadingMoreNoMoreLine: {
        type: Boolean,
        default: u$1.gc("showLoadingMoreNoMoreLine", true)
      },
      // 自定义底部没有更多数据的分割线样式
      loadingMoreNoMoreLineCustomStyle: {
        type: Object,
        default: u$1.gc("loadingMoreNoMoreLineCustomStyle", {})
      },
      // 当分页未满一屏时，是否自动加载更多，默认为否(nvue无效)
      insideMore: {
        type: Boolean,
        default: u$1.gc("insideMore", false)
      },
      // 距底部/右边多远时（单位px），触发 scrolltolower 事件，默认为100rpx
      lowerThreshold: {
        type: [Number, String],
        default: u$1.gc("lowerThreshold", "100rpx")
      }
    },
    data() {
      return {
        M: Enum.More,
        // 底部加载更多状态
        loadingStatus: Enum.More.Default,
        // 在渲染之后的底部加载更多状态
        loadingStatusAfterRender: Enum.More.Default,
        // 底部加载更多时间戳
        loadingMoreTimeStamp: 0,
        // 底部加载更多slot
        loadingMoreDefaultSlot: null,
        // 是否展示底部加载更多
        showLoadingMore: false,
        // 是否是开发者自定义的加载更多，-1代表交由z-paging自行判断；1代表没有更多了；0代表还有更多数据
        customNoMore: -1
      };
    },
    computed: {
      // 底部加载更多配置
      zLoadMoreConfig() {
        return {
          status: this.loadingStatusAfterRender,
          defaultAsLoading: this.loadingMoreDefaultAsLoading || this.useChatRecordMode && this.chatLoadingMoreDefaultAsLoading,
          defaultThemeStyle: this.finalLoadingMoreThemeStyle,
          customStyle: this.loadingMoreCustomStyle,
          titleCustomStyle: this.loadingMoreTitleCustomStyle,
          iconCustomStyle: this.loadingMoreLoadingIconCustomStyle,
          loadingIconType: this.loadingMoreLoadingIconType,
          loadingIconCustomImage: this.loadingMoreLoadingIconCustomImage,
          loadingAnimated: this.loadingMoreLoadingAnimated,
          showNoMoreLine: this.showLoadingMoreNoMoreLine,
          noMoreLineCustomStyle: this.loadingMoreNoMoreLineCustomStyle,
          defaultText: this.finalLoadingMoreDefaultText,
          loadingText: this.finalLoadingMoreLoadingText,
          noMoreText: this.finalLoadingMoreNoMoreText,
          failText: this.finalLoadingMoreFailText,
          hideContent: !this.loadingMoreDefaultAsLoading && this.listRendering,
          unit: this.unit,
          isChat: this.useChatRecordMode,
          chatDefaultAsLoading: this.chatLoadingMoreDefaultAsLoading
        };
      },
      // 最终的底部加载更多主题
      finalLoadingMoreThemeStyle() {
        return this.loadingMoreThemeStyle.length ? this.loadingMoreThemeStyle : this.defaultThemeStyle;
      },
      // 最终的底部加载更多触发阈值
      finalLowerThreshold() {
        return u$1.convertToPx(this.lowerThreshold);
      },
      // 是否显示默认状态下的底部加载更多
      showLoadingMoreDefault() {
        return this._showLoadingMore("Default");
      },
      // 是否显示加载中状态下的底部加载更多
      showLoadingMoreLoading() {
        return this._showLoadingMore("Loading");
      },
      // 是否显示没有更多了状态下的底部加载更多
      showLoadingMoreNoMore() {
        return this._showLoadingMore("NoMore");
      },
      // 是否显示加载失败状态下的底部加载更多
      showLoadingMoreFail() {
        return this._showLoadingMore("Fail");
      },
      // 是否显示自定义状态下的底部加载更多
      showLoadingMoreCustom() {
        return this._showLoadingMore("Custom");
      },
      // 底部加载更多固定高度
      loadingMoreFixedHeight() {
        return u$1.addUnit("80rpx", this.unit);
      }
    },
    methods: {
      // 页面滚动到底部时通知z-paging进行进一步处理
      pageReachBottom() {
        !this.useChatRecordMode && this.toBottomLoadingMoreEnabled && this._onLoadingMore("toBottom");
      },
      // 手动触发上拉加载更多(非必须，可依据具体需求使用)
      doLoadMore(type) {
        this._onLoadingMore(type);
      },
      // 通过@scroll事件检测是否滚动到了底部(顺带检测下是否滚动到了顶部)
      _checkScrolledToBottom(scrollDiff, checked = false) {
        if (this.cacheScrollNodeHeight === -1) {
          this._getNodeClientRect(".zp-scroll-view").then((res) => {
            if (res) {
              const scrollNodeHeight = res[0].height;
              this.cacheScrollNodeHeight = scrollNodeHeight;
              if (scrollDiff - scrollNodeHeight <= this.finalLowerThreshold) {
                this._onLoadingMore("toBottom");
              }
            }
          });
        } else {
          if (scrollDiff - this.cacheScrollNodeHeight <= this.finalLowerThreshold) {
            this._onLoadingMore("toBottom");
          } else if (scrollDiff - this.cacheScrollNodeHeight <= 500 && !checked) {
            u$1.delay(() => {
              this._getNodeClientRect(".zp-scroll-view", true, true).then((res) => {
                if (res) {
                  this.oldScrollTop = res[0].scrollTop;
                  const newScrollDiff = res[0].scrollHeight - this.oldScrollTop;
                  this._checkScrolledToBottom(newScrollDiff, true);
                }
              });
            }, 150, "checkScrolledToBottomDelay");
          }
          if (this.oldScrollTop <= 150 && this.oldScrollTop !== 0) {
            u$1.delay(() => {
              if (this.oldScrollTop !== 0) {
                this._getNodeClientRect(".zp-scroll-view", true, true).then((res) => {
                  if (res && res[0].scrollTop === 0 && this.oldScrollTop !== 0) {
                    this._onScrollToUpper();
                  }
                });
              }
            }, 150, "checkScrolledToTopDelay");
          }
        }
      },
      // 触发加载更多时调用,from:toBottom-滑动到底部触发；click-点击加载更多触发
      _onLoadingMore(from = "click") {
        if (this.isIos && from === "toBottom" && !this.scrollToBottomBounceEnabled && this.scrollEnable) {
          this.scrollEnable = false;
          this.$nextTick(() => {
            this.scrollEnable = true;
          });
        }
        this._emitScrollEvent("scrolltolower");
        if (this.isOnly || !this.loadingMoreEnabled || !(this.loadingStatus === Enum.More.Default || this.loadingStatus === Enum.More.Fail) || this.loading || this.showEmpty)
          return;
        this._doLoadingMore();
      },
      // 处理开始加载更多
      _doLoadingMore() {
        if (this.pageNo >= this.defaultPageNo && this.loadingStatus !== Enum.More.NoMore) {
          this.pageNo++;
          this._startLoading(false);
          if (this.isLocalPaging) {
            this._localPagingQueryList(this.pageNo, this.defaultPageSize, this.localPagingLoadingTime, (res) => {
              this.completeByTotal(res, this.totalLocalPagingList.length);
              this.queryFrom = Enum.QueryFrom.LoadMore;
            });
          } else {
            this._emitQuery(this.pageNo, this.defaultPageSize, Enum.QueryFrom.LoadMore);
            this._callMyParentQuery();
          }
          this.loadingType = Enum.LoadingType.LoadMore;
        }
      },
      // (预处理)判断当没有更多数据且分页内容未超出z-paging时是否显示没有更多数据的view
      _preCheckShowNoMoreInside(newVal, scrollViewNode, pagingContainerNode) {
        if (this.loadingStatus === Enum.More.NoMore && this.hideNoMoreByLimit > 0 && newVal.length) {
          this.showLoadingMore = newVal.length > this.hideNoMoreByLimit;
        } else if (this.loadingStatus === Enum.More.NoMore && this.hideNoMoreInside && newVal.length || this.insideMore && this.insideOfPaging !== false && newVal.length) {
          this.$nextTick(() => {
            this._checkShowNoMoreInside(newVal, scrollViewNode, pagingContainerNode);
          });
          if (this.insideMore && this.insideOfPaging !== false && newVal.length) {
            this.showLoadingMore = newVal.length;
          }
        } else {
          this.showLoadingMore = newVal.length;
        }
      },
      // 判断当没有更多数据且分页内容未超出z-paging时是否显示没有更多数据的view
      async _checkShowNoMoreInside(totalData, oldScrollViewNode, oldPagingContainerNode) {
        try {
          const scrollViewNode = oldScrollViewNode || await this._getNodeClientRect(".zp-scroll-view");
          if (this.usePageScroll) {
            if (scrollViewNode) {
              const scrollViewTotalH = scrollViewNode[0].top + scrollViewNode[0].height;
              this.insideOfPaging = scrollViewTotalH < this.windowHeight;
              if (this.hideNoMoreInside) {
                this.showLoadingMore = !this.insideOfPaging;
              }
              this._updateInsideOfPaging();
            }
          } else {
            const pagingContainerNode = oldPagingContainerNode || await this._getNodeClientRect(".zp-paging-container-content");
            const pagingContainerH = pagingContainerNode ? pagingContainerNode[0].height : 0;
            const scrollViewH = scrollViewNode ? scrollViewNode[0].height : 0;
            this.insideOfPaging = pagingContainerH < scrollViewH;
            if (this.hideNoMoreInside) {
              this.showLoadingMore = !this.insideOfPaging;
            }
            this._updateInsideOfPaging();
          }
        } catch (e2) {
          this.insideOfPaging = !totalData.length;
          if (this.hideNoMoreInside) {
            this.showLoadingMore = !this.insideOfPaging;
          }
          this._updateInsideOfPaging();
        }
      },
      // 是否要展示上拉加载更多view
      _showLoadingMore(type) {
        if (!this.showLoadingMoreWhenReload && (!(this.loadingStatus === Enum.More.Default ? this.nShowBottom : true) || !this.realTotalData.length))
          return false;
        if ((!this.showLoadingMoreWhenReload || this.isUserPullDown || this.loadingStatus !== Enum.More.Loading) && !this.showLoadingMore || !this.loadingMoreEnabled && (!this.showLoadingMoreWhenReload || this.isUserPullDown || this.loadingStatus !== Enum.More.Loading) || this.isOnly) {
          return false;
        }
        if (this.useChatRecordMode && type !== "Loading")
          return false;
        if (!this.zSlots)
          return false;
        if (type === "Custom") {
          return this.showDefaultLoadingMoreText && !(this.loadingStatus === Enum.More.NoMore && !this.showLoadingMoreNoMoreView);
        }
        const res = this.loadingStatus === Enum.More[type] && this.zSlots[`loadingMore${type}`] && (type === "NoMore" ? this.showLoadingMoreNoMoreView : true);
        return res;
      }
    }
  };
  const loadingModule = {
    props: {
      // 第一次加载后自动隐藏loading slot，默认为是
      autoHideLoadingAfterFirstLoaded: {
        type: Boolean,
        default: u$1.gc("autoHideLoadingAfterFirstLoaded", true)
      },
      // loading slot是否铺满屏幕并固定，默认为否
      loadingFullFixed: {
        type: Boolean,
        default: u$1.gc("loadingFullFixed", false)
      },
      // 是否自动显示系统Loading：即uni.showLoading，若开启则将在刷新列表时(调用reload、refresh时)显示，下拉刷新和滚动到底部加载更多不会显示，默认为false。
      autoShowSystemLoading: {
        type: Boolean,
        default: u$1.gc("autoShowSystemLoading", false)
      },
      // 显示系统Loading时是否显示透明蒙层，防止触摸穿透，默认为是(H5、App、微信小程序、百度小程序有效)
      systemLoadingMask: {
        type: Boolean,
        default: u$1.gc("systemLoadingMask", true)
      },
      // 显示系统Loading时显示的文字，默认为"加载中"
      systemLoadingText: {
        type: [String, Object],
        default: u$1.gc("systemLoadingText", null)
      }
    },
    data() {
      return {
        loading: false,
        loadingForNow: false
      };
    },
    watch: {
      // loading状态
      loadingStatus(newVal) {
        this.$emit("loadingStatusChange", newVal);
        this.$nextTick(() => {
          this.loadingStatusAfterRender = newVal;
        });
        if (this.useChatRecordMode) {
          if (this.isFirstPage && (newVal === Enum.More.NoMore || newVal === Enum.More.Fail)) {
            this.isFirstPageAndNoMore = true;
            return;
          }
        }
        this.isFirstPageAndNoMore = false;
      },
      loading(newVal) {
        if (newVal) {
          this.loadingForNow = newVal;
        }
      }
    },
    computed: {
      // 是否显示loading
      showLoading() {
        if (this.firstPageLoaded || !this.loading || !this.loadingForNow)
          return false;
        if (this.finalShowSystemLoading) {
          uni.showLoading({
            title: this.finalSystemLoadingText,
            mask: this.systemLoadingMask
          });
        }
        return this.autoHideLoadingAfterFirstLoaded ? this.fromEmptyViewReload ? true : !this.pagingLoaded : this.loadingType === Enum.LoadingType.Refresher;
      },
      // 最终的是否显示系统loading
      finalShowSystemLoading() {
        return this.autoShowSystemLoading && this.loadingType === Enum.LoadingType.Refresher;
      }
    },
    methods: {
      // 处理开始加载更多状态
      _startLoading(isReload = false) {
        if (this.showLoadingMoreWhenReload && !this.isUserPullDown || !isReload) {
          this.loadingStatus = Enum.More.Loading;
        }
        this.loading = true;
      },
      // 停止系统loading和refresh
      _endSystemLoadingAndRefresh() {
        this.finalShowSystemLoading && uni.hideLoading();
        !this.useCustomRefresher && uni.stopPullDownRefresh();
      }
    }
  };
  const chatRecordModerModule = {
    props: {
      // 使用聊天记录模式，默认为否
      useChatRecordMode: {
        type: Boolean,
        default: u$1.gc("useChatRecordMode", false)
      },
      // 使用聊天记录模式时滚动到顶部后，列表垂直移动偏移距离。默认0rpx。单位px（暂时无效）
      chatRecordMoreOffset: {
        type: [Number, String],
        default: u$1.gc("chatRecordMoreOffset", "0rpx")
      },
      // 使用聊天记录模式时是否自动隐藏键盘：在用户触摸列表时候自动隐藏键盘，默认为是
      autoHideKeyboardWhenChat: {
        type: Boolean,
        default: u$1.gc("autoHideKeyboardWhenChat", true)
      },
      // 使用聊天记录模式中键盘弹出时是否自动调整slot="bottom"高度，默认为是
      autoAdjustPositionWhenChat: {
        type: Boolean,
        default: u$1.gc("autoAdjustPositionWhenChat", true)
      },
      // 使用聊天记录模式中键盘弹出时占位高度偏移距离。默认0rpx。单位px
      chatAdjustPositionOffset: {
        type: [Number, String],
        default: u$1.gc("chatAdjustPositionOffset", "0rpx")
      },
      // 使用聊天记录模式中键盘弹出时是否自动滚动到底部，默认为否
      autoToBottomWhenChat: {
        type: Boolean,
        default: u$1.gc("autoToBottomWhenChat", false)
      },
      // 使用聊天记录模式中reload时是否显示chatLoading，默认为否
      showChatLoadingWhenReload: {
        type: Boolean,
        default: u$1.gc("showChatLoadingWhenReload", false)
      },
      // 在聊天记录模式中滑动到顶部状态为默认状态时，以加载中的状态展示，默认为是。若设置为否，则默认会显示【点击加载更多】，然后才会显示loading
      chatLoadingMoreDefaultAsLoading: {
        type: Boolean,
        default: u$1.gc("chatLoadingMoreDefaultAsLoading", true)
      }
    },
    data() {
      return {
        // 键盘高度
        keyboardHeight: 0,
        // 键盘高度是否未改变，此时占位高度变化不需要动画效果
        isKeyboardHeightChanged: false
      };
    },
    computed: {
      finalChatRecordMoreOffset() {
        return u$1.convertToPx(this.chatRecordMoreOffset);
      },
      finalChatAdjustPositionOffset() {
        return u$1.convertToPx(this.chatAdjustPositionOffset);
      },
      // 聊天记录模式旋转180度style
      chatRecordRotateStyle() {
        let cellStyle;
        cellStyle = this.useChatRecordMode ? { transform: "scaleY(-1)" } : {};
        this.$emit("update:cellStyle", cellStyle);
        this.$emit("cellStyleChange", cellStyle);
        this.$nextTick(() => {
          if (this.isFirstPage && this.isChatRecordModeAndNotInversion) {
            this.$nextTick(() => {
              this._scrollToBottom(false);
              u$1.delay(() => {
                this._scrollToBottom(false);
                u$1.delay(() => {
                  this._scrollToBottom(false);
                }, 50);
              }, 50);
            });
          }
        });
        return cellStyle;
      },
      // 是否是聊天记录列表并且有配置transform
      isChatRecordModeHasTransform() {
        return this.useChatRecordMode && this.chatRecordRotateStyle && this.chatRecordRotateStyle.transform;
      },
      // 是否是聊天记录列表并且列表未倒置
      isChatRecordModeAndNotInversion() {
        return this.isChatRecordModeHasTransform && this.chatRecordRotateStyle.transform === "scaleY(1)";
      },
      // 是否是聊天记录列表并且列表倒置
      isChatRecordModeAndInversion() {
        return this.isChatRecordModeHasTransform && this.chatRecordRotateStyle.transform === "scaleY(-1)";
      },
      // 最终的聊天记录模式中底部安全区域的高度，如果开启了底部安全区域并且键盘未弹出，则添加底部区域高度
      chatRecordModeSafeAreaBottom() {
        return this.safeAreaInsetBottom && !this.keyboardHeight ? this.safeAreaBottom : 0;
      }
    },
    mounted() {
      this.addKeyboardHeightChangeListener();
    },
    methods: {
      // 添加聊天记录
      addChatRecordData(data, toBottom = true, toBottomWithAnimate = true) {
        if (!this.useChatRecordMode)
          return;
        this.isTotalChangeFromAddData = true;
        this.addDataFromTop(data, toBottom, toBottomWithAnimate);
      },
      // 手动触发滚动到顶部加载更多，聊天记录模式时有效
      doChatRecordLoadMore() {
        this.useChatRecordMode && this._onLoadingMore("click");
      },
      // 手动添加键盘高度变化监听
      addKeyboardHeightChangeListener() {
        if (this.useChatRecordMode) {
          uni.onKeyboardHeightChange(this._handleKeyboardHeightChange);
        }
      },
      // 处理键盘高度变化
      _handleKeyboardHeightChange(res) {
        this.$emit("keyboardHeightChange", res);
        if (this.autoAdjustPositionWhenChat) {
          this.isKeyboardHeightChanged = true;
          this.keyboardHeight = res.height > 0 ? res.height + this.finalChatAdjustPositionOffset : res.height;
        }
        if (this.autoToBottomWhenChat && this.keyboardHeight > 0) {
          u$1.delay(() => {
            this.scrollToBottom(false);
            u$1.delay(() => {
              this.scrollToBottom(false);
            });
          });
        }
      }
    }
  };
  const scrollerModule = {
    props: {
      // 使用页面滚动，默认为否，当设置为是时则使用页面的滚动而非此组件内部的scroll-view的滚动，使用页面滚动时z-paging无需设置确定的高度且对于长列表展示性能更高，但配置会略微繁琐
      usePageScroll: {
        type: Boolean,
        default: u$1.gc("usePageScroll", false)
      },
      // 是否可以滚动，使用内置scroll-view和nvue时有效，默认为是
      scrollable: {
        type: Boolean,
        default: u$1.gc("scrollable", true)
      },
      // 控制是否出现滚动条，默认为是
      showScrollbar: {
        type: Boolean,
        default: u$1.gc("showScrollbar", true)
      },
      // 是否允许横向滚动，默认为否
      scrollX: {
        type: Boolean,
        default: u$1.gc("scrollX", false)
      },
      // iOS设备上滚动到顶部时是否允许回弹效果，默认为否。关闭回弹效果后可使滚动到顶部与下拉刷新更连贯，但是有吸顶view时滚动到顶部时可能出现抖动。
      scrollToTopBounceEnabled: {
        type: Boolean,
        default: u$1.gc("scrollToTopBounceEnabled", false)
      },
      // iOS设备上滚动到底部时是否允许回弹效果，默认为是。
      scrollToBottomBounceEnabled: {
        type: Boolean,
        default: u$1.gc("scrollToBottomBounceEnabled", true)
      },
      // 在设置滚动条位置时使用动画过渡，默认为否
      scrollWithAnimation: {
        type: Boolean,
        default: u$1.gc("scrollWithAnimation", false)
      },
      // 值应为某子元素id（id不能以数字开头）。设置哪个方向可滚动，则在哪个方向滚动到该元素
      scrollIntoView: {
        type: String,
        default: u$1.gc("scrollIntoView", "")
      },
      // z-paging是否使用swiper-item或其他父组件包裹，默认为否，此属性为了解决vue3+(微信小程序或QQ小程序)中，scrollIntoViewById和scrollIntoViewByIndex因无法获取节点信息导致滚动到指定view无效的问题
      inSwiperSlot: {
        type: Boolean,
        default: false
      }
    },
    data() {
      return {
        scrollTop: 0,
        oldScrollTop: 0,
        scrollLeft: 0,
        oldScrollLeft: 0,
        scrollViewStyle: {},
        scrollViewContainerStyle: {},
        scrollViewInStyle: {},
        pageScrollTop: -1,
        scrollEnable: true,
        privateScrollWithAnimation: -1,
        cacheScrollNodeHeight: -1,
        superContentHeight: 0,
        lastScrollHeight: 0,
        lastScrollDirection: "",
        setContentHeightPending: false
      };
    },
    watch: {
      oldScrollTop(newVal) {
        !this.usePageScroll && this._scrollTopChange(newVal, false);
      },
      pageScrollTop(newVal) {
        this.usePageScroll && this._scrollTopChange(newVal, true);
      },
      usePageScroll: {
        handler(newVal) {
          this.loaded && this.autoHeight && this._setAutoHeight(!newVal);
        },
        immediate: true
      },
      finalScrollTop(newVal) {
        this.renderPropScrollTop = newVal < 6 ? 0 : 10;
      }
    },
    computed: {
      finalScrollWithAnimation() {
        if (this.privateScrollWithAnimation !== -1) {
          return this.privateScrollWithAnimation === 1;
        }
        return this.scrollWithAnimation;
      },
      finalScrollViewStyle() {
        if (this.superContentZIndex != 1) {
          this.scrollViewStyle["z-index"] = this.superContentZIndex;
          this.scrollViewStyle["position"] = "relative";
        }
        return this.scrollViewStyle;
      },
      finalScrollTop() {
        return this.usePageScroll ? this.pageScrollTop : this.oldScrollTop;
      },
      // 当前是否是旧版webview
      finalIsOldWebView() {
        return this.isOldWebView && !this.usePageScroll;
      },
      // 当前scroll-view/list-view是否允许滚动
      finalScrollable() {
        return this.scrollable && !this.usePageScroll && this.scrollEnable && (this.refresherCompleteScrollable ? true : this.refresherStatus !== Enum.Refresher.Complete) && (this.refresherRefreshingScrollable ? true : this.refresherStatus !== Enum.Refresher.Loading);
      }
    },
    methods: {
      // 滚动到顶部，animate为是否展示滚动动画，默认为是
      scrollToTop(animate, checkReverse = true) {
        if (this.useChatRecordMode && checkReverse && !this.isChatRecordModeAndNotInversion) {
          this.scrollToBottom(animate, false);
          return;
        }
        this.$nextTick(() => {
          this._scrollToTop(animate, false);
        });
      },
      // 滚动到底部，animate为是否展示滚动动画，默认为是
      scrollToBottom(animate, checkReverse = true) {
        if (this.useChatRecordMode && checkReverse && !this.isChatRecordModeAndNotInversion) {
          this.scrollToTop(animate, false);
          return;
        }
        this.$nextTick(() => {
          this._scrollToBottom(animate);
        });
      },
      // 滚动到指定view(vue中有效)。sel为需要滚动的view的id值，不包含"#"；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollIntoViewById(sel, offset, animate) {
        this._scrollIntoView(sel, offset, animate);
      },
      // 滚动到指定view(vue中有效)。nodeTop为需要滚动的view的top值(通过uni.createSelectorQuery()获取)；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollIntoViewByNodeTop(nodeTop, offset, animate) {
        this.scrollTop = this.oldScrollTop;
        this.$nextTick(() => {
          this._scrollIntoViewByNodeTop(nodeTop, offset, animate);
        });
      },
      // y轴滚动到指定位置(vue中有效)。y为与顶部的距离，单位为px；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollToY(y2, offset, animate) {
        this.scrollTop = this.oldScrollTop;
        this.$nextTick(() => {
          this._scrollToY(y2, offset, animate);
        });
      },
      // x轴滚动到指定位置(非页面滚动且在vue中有效)。x为与左侧的距离，单位为px；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollToX(x, offset, animate) {
        this.scrollLeft = this.oldScrollLeft;
        this.$nextTick(() => {
          this._scrollToX(x, offset, animate);
        });
      },
      // 滚动到指定view(nvue中和虚拟列表中有效)。index为需要滚动的view的index(第几个，从0开始)；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollIntoViewByIndex(index, offset, animate) {
        if (index >= this.realTotalData.length) {
          u$1.consoleErr("当前滚动的index超出已渲染列表长度，请先通过refreshToPage加载到对应index页并等待渲染成功后再调用此方法！");
          return;
        }
        this.$nextTick(() => {
          if (this.finalUseVirtualList) {
            const isCellFixed = this.cellHeightMode === Enum.CellHeightMode.Fixed;
            u$1.delay(() => {
              if (this.finalUseVirtualList) {
                const scrollTop = isCellFixed ? this.virtualCellHeight * index : this.virtualHeightCacheList[index].lastTotalHeight;
                this.scrollToY(scrollTop, offset, animate);
              }
            }, isCellFixed ? 0 : 100);
          }
        });
      },
      // 滚动到指定view(nvue中有效)。view为需要滚动的view(通过`this.$refs.xxx`获取)，不包含"#"；offset为偏移量，单位为px；animate为是否展示滚动动画，默认为否
      scrollIntoViewByView(view, offset, animate) {
        this._scrollIntoView(view, offset, animate);
      },
      // 当使用页面滚动并且自定义下拉刷新时，请在页面的onPageScroll中调用此方法，告知z-paging当前的pageScrollTop，否则会导致在任意位置都可以下拉刷新
      updatePageScrollTop(value) {
        this.pageScrollTop = value;
      },
      // 当使用页面滚动并且设置了slot="top"时，默认初次加载会自动获取其高度，并使内部容器下移，当slot="top"的view高度动态改变时，在其高度需要更新时调用此方法
      updatePageScrollTopHeight() {
        this._updatePageScrollTopOrBottomHeight("top");
      },
      // 当使用页面滚动并且设置了slot="bottom"时，默认初次加载会自动获取其高度，并使内部容器下移，当slot="bottom"的view高度动态改变时，在其高度需要更新时调用此方法
      updatePageScrollBottomHeight() {
        this._updatePageScrollTopOrBottomHeight("bottom");
      },
      // 更新slot="left"和slot="right"宽度，当slot="left"或slot="right"宽度动态改变时调用
      updateLeftAndRightWidth() {
        if (!this.finalIsOldWebView)
          return;
        this.$nextTick(() => this._updateLeftAndRightWidth(this.scrollViewContainerStyle, "zp-page"));
      },
      // 更新z-paging内置scroll-view的scrollTop
      updateScrollViewScrollTop(scrollTop, animate = true) {
        this._updatePrivateScrollWithAnimation(animate);
        this.scrollTop = this.oldScrollTop;
        this.$nextTick(() => {
          this.scrollTop = scrollTop;
          this.oldScrollTop = this.scrollTop;
        });
      },
      // 当滚动到顶部时
      _onScrollToUpper() {
        this._emitScrollEvent("scrolltoupper");
        this.$emit("scrollTopChange", 0);
        this.$nextTick(() => {
          this.oldScrollTop = 0;
        });
      },
      // 当滚动到底部时
      _onScrollToLower(e2) {
        (!e2.detail || !e2.detail.direction || e2.detail.direction === "bottom") && this.toBottomLoadingMoreEnabled && this._onLoadingMore(this.useChatRecordMode ? "click" : "toBottom");
      },
      // 滚动到顶部
      _scrollToTop(animate = true, isPrivate = true) {
        if (this.usePageScroll) {
          this.$nextTick(() => {
            uni.pageScrollTo({
              scrollTop: 0,
              duration: animate ? 100 : 0
            });
          });
          return;
        }
        this._updatePrivateScrollWithAnimation(animate);
        this.scrollTop = this.oldScrollTop;
        this.$nextTick(() => {
          this.scrollTop = 0;
          this.oldScrollTop = this.scrollTop;
        });
        u$1.delay(() => {
          this.scrollTop = this.oldScrollTop;
          this.$nextTick(() => {
            this.scrollTop = 0;
            this.oldScrollTop = this.scrollTop;
          });
        }, 500);
      },
      // 滚动到底部
      async _scrollToBottom(animate = true) {
        if (this.usePageScroll) {
          this.$nextTick(() => {
            uni.pageScrollTo({
              scrollTop: Number.MAX_VALUE,
              duration: animate ? 100 : 0
            });
          });
          return;
        }
        try {
          this._updatePrivateScrollWithAnimation(animate);
          const pagingContainerNode = await this._getNodeClientRect(".zp-paging-container");
          const scrollViewNode = await this._getNodeClientRect(".zp-scroll-view");
          const pagingContainerH = pagingContainerNode ? pagingContainerNode[0].height : 0;
          const scrollViewH = scrollViewNode ? scrollViewNode[0].height : 0;
          if (pagingContainerH > scrollViewH) {
            this.scrollTop = this.oldScrollTop;
            this.$nextTick(() => {
              this.scrollTop = pagingContainerH - scrollViewH + this.virtualPlaceholderTopHeight;
              this.oldScrollTop = this.scrollTop;
            });
          }
        } catch (e2) {
        }
      },
      // 滚动到指定view
      _scrollIntoView(sel, offset = 0, animate = false, finishCallback) {
        try {
          this.scrollTop = this.oldScrollTop;
          this.$nextTick(() => {
            let inDom = false;
            this._getNodeClientRect("#" + sel.replace("#", ""), inDom).then((node2) => {
              if (node2) {
                this._getNodeClientRect(".zp-scroll-view-container").then((svContainerNode) => {
                  if (svContainerNode) {
                    this._scrollIntoViewByNodeTop(node2[0].top - svContainerNode[0].top, offset, animate);
                    finishCallback && finishCallback();
                  }
                });
              } else {
                u$1.consoleErr(`无法获取${sel}的节点信息，请检查！`);
              }
            });
          });
        } catch (e2) {
        }
      },
      // 通过nodeTop滚动到指定view
      _scrollIntoViewByNodeTop(nodeTop, offset = 0, animate = false) {
        if (this.isChatRecordModeAndInversion) {
          this._getNodeClientRect(".zp-scroll-view").then((sNode) => {
            if (sNode) {
              this._scrollToY(sNode[0].height - nodeTop, offset, animate, true);
            }
          });
        } else {
          this._scrollToY(nodeTop, offset, animate, true);
        }
      },
      // y轴滚动到指定位置
      _scrollToY(y2, offset = 0, animate = false, addScrollTop = false) {
        this._updatePrivateScrollWithAnimation(animate);
        u$1.delay(() => {
          if (this.usePageScroll) {
            if (addScrollTop && this.pageScrollTop !== -1) {
              y2 += this.pageScrollTop;
            }
            const scrollTop = y2 - offset;
            uni.pageScrollTo({
              scrollTop,
              duration: animate ? 100 : 0
            });
          } else {
            if (addScrollTop) {
              y2 += this.oldScrollTop;
            }
            this.scrollTop = y2 - offset;
          }
        }, 10);
      },
      // x轴滚动到指定位置
      _scrollToX(x, offset = 0, animate = false) {
        this._updatePrivateScrollWithAnimation(animate);
        u$1.delay(() => {
          if (!this.usePageScroll) {
            this.scrollLeft = x - offset;
          } else {
            u$1.consoleErr("使用页面滚动时不支持scrollToX");
          }
        }, 10);
      },
      // scroll-view滚动中
      _scroll(e2) {
        this.$emit("scroll", e2);
        const { scrollTop, scrollLeft, scrollHeight } = e2.detail;
        if (this.watchScrollDirectionChange) {
          let direction = this.oldScrollTop > scrollTop ? "top" : "bottom";
          if (scrollTop <= 0 || !this.scrollEnable) {
            direction = "top";
          }
          if (scrollTop > this.lastScrollHeight - this.scrollViewHeight - 1 && this.scrollEnable) {
            direction = "bottom";
          }
          if (direction !== this.lastScrollDirection) {
            this.$emit("scrollDirectionChange", direction);
            this.lastScrollDirection = direction;
          }
          if (this.lastScrollHeight !== scrollHeight && !this.setContentHeightPending) {
            this.setContentHeightPending = true;
            u$1.delay(() => {
              this.lastScrollHeight = scrollHeight;
              this.setContentHeightPending = false;
            });
          }
        }
        this.finalUseVirtualList && this._updateVirtualScroll(scrollTop, this.oldScrollTop - scrollTop);
        this.oldScrollTop = scrollTop;
        this.oldScrollLeft = scrollLeft;
        if (!this.isIos) {
          const scrollDiff = e2.detail.scrollHeight - this.oldScrollTop;
          this._checkScrolledToBottom(scrollDiff);
        }
      },
      // emit scrolltolower/scrolltoupper事件
      _emitScrollEvent(type) {
        const reversedType = type === "scrolltolower" ? "scrolltoupper" : "scrolltolower";
        const eventType = this.useChatRecordMode && !this.isChatRecordModeAndNotInversion ? reversedType : type;
        this.$emit(eventType);
      },
      // 更新内置的scroll-view是否启用滚动动画
      _updatePrivateScrollWithAnimation(animate) {
        this.privateScrollWithAnimation = animate ? 1 : 0;
        u$1.delay(() => this.$nextTick(() => {
          this.privateScrollWithAnimation = -1;
        }), 100, "updateScrollWithAnimationDelay");
      },
      // 检测scrollView是否要铺满屏幕
      _doCheckScrollViewShouldFullHeight(totalData) {
        if (this.autoFullHeight && this.usePageScroll && this.isTotalChangeFromAddData) {
          this.$nextTick(() => {
            this._checkScrollViewShouldFullHeight((scrollViewNode, pagingContainerNode) => {
              this._preCheckShowNoMoreInside(totalData, scrollViewNode, pagingContainerNode);
            });
          });
        } else {
          this._preCheckShowNoMoreInside(totalData);
        }
      },
      // 检测z-paging是否要全屏覆盖(当使用页面滚动并且不满全屏时，默认z-paging需要铺满全屏，避免数据过少时内部的empty-view无法正确展示)
      async _checkScrollViewShouldFullHeight(callback) {
        try {
          const scrollViewNode = await this._getNodeClientRect(".zp-scroll-view");
          const pagingContainerNode = await this._getNodeClientRect(".zp-paging-container-content");
          if (!scrollViewNode || !pagingContainerNode)
            return;
          const scrollViewHeight = pagingContainerNode[0].height;
          const scrollViewTop = scrollViewNode[0].top;
          if (this.isAddedData && scrollViewHeight + scrollViewTop <= this.windowHeight) {
            this._setAutoHeight(true, scrollViewNode);
            callback(scrollViewNode, pagingContainerNode);
          } else {
            this._setAutoHeight(false);
            callback(null, null);
          }
        } catch (e2) {
          callback(null, null);
        }
      },
      // 更新缓存中z-paging整个内容容器高度
      async _updateCachedSuperContentHeight() {
        const superContentNode = await this._getNodeClientRect(".z-paging-content");
        if (superContentNode) {
          this.superContentHeight = superContentNode[0].height;
        }
      },
      // scrollTop改变时触发
      _scrollTopChange(newVal, isPageScrollTop) {
        this.$emit("scrollTopChange", newVal);
        this.$emit("update:scrollTop", newVal);
        this._checkShouldShowBackToTop(newVal);
        const scrollTop = newVal > 5 ? 6 : 0;
        if (isPageScrollTop && this.wxsPageScrollTop !== scrollTop) {
          this.wxsPageScrollTop = scrollTop;
        } else if (!isPageScrollTop && this.wxsScrollTop !== scrollTop) {
          this.wxsScrollTop = scrollTop;
          if (scrollTop > 6) {
            this.scrollEnable = true;
          }
        }
      },
      // 更新使用页面滚动时slot="top"或"bottom"插入view的高度
      _updatePageScrollTopOrBottomHeight(type) {
        if (!this.usePageScroll)
          return;
        this._doCheckScrollViewShouldFullHeight(this.realTotalData);
        const node2 = `.zp-page-${type}`;
        const marginText = `margin${type.slice(0, 1).toUpperCase() + type.slice(1)}`;
        const safeAreaInsetBottomAdd = this.safeAreaInsetBottom && !this.zSlots.bottom && !this.useSafeAreaPlaceholder;
        this.$nextTick(() => {
          let delayTime = 0;
          u$1.delay(() => {
            this._getNodeClientRect(node2).then((res) => {
              if (res) {
                let pageScrollNodeHeight = res[0].height;
                if (type === "bottom") {
                  if (safeAreaInsetBottomAdd) {
                    pageScrollNodeHeight += this.safeAreaBottom;
                  }
                } else {
                  this.cacheTopHeight = pageScrollNodeHeight;
                }
                this.$set(this.scrollViewStyle, marginText, `${pageScrollNodeHeight}px`);
              } else if (safeAreaInsetBottomAdd) {
                this.$set(this.scrollViewStyle, marginText, `${this.safeAreaBottom}px`);
              }
            });
          }, delayTime);
        });
      }
    }
  };
  const backToTopModule = {
    props: {
      // 自动显示点击返回顶部按钮，默认为否
      autoShowBackToTop: {
        type: Boolean,
        default: u$1.gc("autoShowBackToTop", false)
      },
      // 点击返回顶部按钮显示/隐藏的阈值(滚动距离)，单位为px，默认为400rpx
      backToTopThreshold: {
        type: [Number, String],
        default: u$1.gc("backToTopThreshold", "400rpx")
      },
      // 点击返回顶部按钮的自定义图片地址，默认使用z-paging内置的图片
      backToTopImg: {
        type: String,
        default: u$1.gc("backToTopImg", "")
      },
      // 点击返回顶部按钮返回到顶部时是否展示过渡动画，默认为是
      backToTopWithAnimate: {
        type: Boolean,
        default: u$1.gc("backToTopWithAnimate", true)
      },
      // 点击返回顶部按钮与底部的距离，注意添加单位px或rpx，默认为160rpx
      backToTopBottom: {
        type: [Number, String],
        default: u$1.gc("backToTopBottom", "160rpx")
      },
      // 点击返回顶部按钮的自定义样式
      backToTopStyle: {
        type: Object,
        default: u$1.gc("backToTopStyle", {})
      },
      // iOS点击顶部状态栏、安卓双击标题栏时，滚动条返回顶部，只支持竖向，默认为是
      enableBackToTop: {
        type: Boolean,
        default: u$1.gc("enableBackToTop", true)
      }
    },
    data() {
      return {
        // 点击返回顶部的class
        backToTopClass: "zp-back-to-top zp-back-to-top-hide",
        // 上次点击返回顶部的时间
        lastBackToTopShowTime: 0,
        // 点击返回顶部显示的class是否在展示中，使得按钮展示/隐藏过度效果更自然
        showBackToTopClass: false
      };
    },
    computed: {
      backToTopThresholdUnitConverted() {
        return u$1.addUnit(this.backToTopThreshold, this.unit);
      },
      backToTopBottomUnitConverted() {
        return u$1.addUnit(this.backToTopBottom, this.unit);
      },
      finalEnableBackToTop() {
        return this.usePageScroll ? false : this.enableBackToTop;
      },
      finalBackToTopThreshold() {
        return u$1.convertToPx(this.backToTopThresholdUnitConverted);
      },
      finalBackToTopStyle() {
        const backToTopStyle = this.backToTopStyle;
        if (!backToTopStyle.bottom) {
          backToTopStyle.bottom = this.windowBottom + u$1.convertToPx(this.backToTopBottomUnitConverted) + "px";
        }
        if (!backToTopStyle.position) {
          backToTopStyle.position = this.usePageScroll ? "fixed" : "absolute";
        }
        return backToTopStyle;
      },
      finalBackToTopClass() {
        return `${this.backToTopClass} zp-back-to-top-${this.unit}`;
      }
    },
    methods: {
      // 点击了返回顶部
      _backToTopClick() {
        let callbacked = false;
        this.$emit("backToTopClick", (toTop) => {
          (toTop === void 0 || toTop === true) && this._handleToTop();
          callbacked = true;
        });
        this.$nextTick(() => {
          !callbacked && this._handleToTop();
        });
      },
      // 处理滚动到顶部（聊天记录模式中为滚动到底部）
      _handleToTop() {
        !this.backToTopWithAnimate && this._checkShouldShowBackToTop(0);
        !this.useChatRecordMode ? this.scrollToTop(this.backToTopWithAnimate) : this.scrollToBottom(this.backToTopWithAnimate);
      },
      // 判断是否要显示返回顶部按钮
      _checkShouldShowBackToTop(scrollTop) {
        if (!this.autoShowBackToTop) {
          this.showBackToTopClass = false;
          return;
        }
        if (scrollTop > this.finalBackToTopThreshold) {
          if (!this.showBackToTopClass) {
            this.showBackToTopClass = true;
            this.lastBackToTopShowTime = (/* @__PURE__ */ new Date()).getTime();
            u$1.delay(() => {
              this.backToTopClass = "zp-back-to-top zp-back-to-top-show";
            }, 300);
          }
        } else {
          if (this.showBackToTopClass) {
            this.backToTopClass = "zp-back-to-top zp-back-to-top-hide";
            u$1.delay(() => {
              this.showBackToTopClass = false;
            }, (/* @__PURE__ */ new Date()).getTime() - this.lastBackToTopShowTime < 500 ? 0 : 300);
          }
        }
      }
    }
  };
  const virtualListModule = {
    props: {
      // 是否使用虚拟列表，默认为否
      useVirtualList: {
        type: Boolean,
        default: u$1.gc("useVirtualList", false)
      },
      // 在使用虚拟列表时，是否使用兼容模式，默认为否
      useCompatibilityMode: {
        type: Boolean,
        default: u$1.gc("useCompatibilityMode", false)
      },
      // 使用兼容模式时传递的附加数据
      extraData: {
        type: Object,
        default: u$1.gc("extraData", {})
      },
      // 是否在z-paging内部循环渲染列表(内置列表)，默认为否。若use-virtual-list为true，则此项恒为true
      useInnerList: {
        type: Boolean,
        default: u$1.gc("useInnerList", false)
      },
      // 强制关闭inner-list，默认为false，如果为true将强制关闭innerList，适用于开启了虚拟列表后需要强制关闭inner-list的情况
      forceCloseInnerList: {
        type: Boolean,
        default: u$1.gc("forceCloseInnerList", false)
      },
      // 内置列表cell的key名称，仅nvue有效，在nvue中开启use-inner-list时必须填此项
      cellKeyName: {
        type: String,
        default: u$1.gc("cellKeyName", "")
      },
      // innerList样式
      innerListStyle: {
        type: Object,
        default: u$1.gc("innerListStyle", {})
      },
      // innerCell样式
      innerCellStyle: {
        type: Object,
        default: u$1.gc("innerCellStyle", {})
      },
      // 预加载的列表可视范围(列表高度)页数，默认为12，即预加载当前页及上下各12页的cell。此数值越大，则虚拟列表中加载的dom越多，内存消耗越大(会维持在一个稳定值)，但增加预加载页面数量可缓解快速滚动短暂白屏问题
      preloadPage: {
        type: [Number, String],
        default: u$1.gc("preloadPage", 12),
        validator: (value) => {
          if (value <= 0)
            u$1.consoleErr("preload-page必须大于0！");
          return value > 0;
        }
      },
      // 虚拟列表cell高度模式，默认为fixed，也就是每个cell高度完全相同，将以第一个cell高度为准进行计算。可选值【dynamic】，即代表高度是动态非固定的，【dynamic】性能低于【fixed】。
      cellHeightMode: {
        type: String,
        default: u$1.gc("cellHeightMode", Enum.CellHeightMode.Fixed)
      },
      // 固定的cell高度，cellHeightMode=fixed才有效，若设置了值，则不计算第一个cell高度而使用设置的cell高度
      fixedCellHeight: {
        type: [Number, String],
        default: u$1.gc("fixedCellHeight", 0)
      },
      // 虚拟列表列数，默认为1。常用于每行有多列的情况，例如每行有2列数据，需要将此值设置为2
      virtualListCol: {
        type: [Number, String],
        default: u$1.gc("virtualListCol", 1)
      },
      // 虚拟列表scroll取样帧率，默认为80，过低容易出现白屏问题，过高容易出现卡顿问题
      virtualScrollFps: {
        type: [Number, String],
        default: u$1.gc("virtualScrollFps", 80)
      },
      // 虚拟列表cell id的前缀，适用于一个页面有多个虚拟列表的情况，用以区分不同虚拟列表cell的id，注意：请勿传数字或以数字开头的字符串。如设置为list1，则cell的id应为：list1-zp-id-${item.zp_index}
      virtualCellIdPrefix: {
        type: String,
        default: u$1.gc("virtualCellIdPrefix", "")
      },
      // 虚拟列表是否使用swiper-item或其他父组件包裹，默认为否，此属性为了解决vue3+(微信小程序或QQ小程序)中，使用非内置列表写法时，若z-paging在swiper-item内存在无法获取slot插入的cell高度进而导致虚拟列表失败的问题
      // 仅vue3+(微信小程序或QQ小程序)+非内置列表写法虚拟列表有效，其他情况此属性设置任何值都无效，所以如果您在swiper-item内使用z-paging的非内置虚拟列表写法，将此属性设置为true即可
      virtualInSwiperSlot: {
        type: Boolean,
        default: false
      }
    },
    data() {
      return {
        virtualListKey: u$1.getInstanceId(),
        virtualCellHeight: 0,
        virtualScrollTimeStamp: 0,
        virtualList: [],
        virtualPlaceholderTopHeight: 0,
        virtualPlaceholderBottomHeight: 0,
        virtualTopRangeIndex: 0,
        virtualBottomRangeIndex: 0,
        lastVirtualTopRangeIndex: 0,
        lastVirtualBottomRangeIndex: 0,
        virtualItemInsertedCount: 0,
        virtualHeightCacheList: [],
        getCellHeightRetryCount: {
          fixed: 0,
          dynamic: 0
        },
        updateVirtualListFromDataChange: false
      };
    },
    watch: {
      // 监听总数据的改变，刷新虚拟列表布局
      realTotalData() {
        this.updateVirtualListRender();
      },
      // 监听虚拟列表渲染数组的改变并emit
      virtualList(newVal) {
        this.$emit("update:virtualList", newVal);
        this.$emit("virtualListChange", newVal);
      },
      // 监听虚拟列表顶部占位高度改变并emit
      virtualPlaceholderTopHeight(newVal) {
        this.$emit("virtualTopHeightChange", newVal);
      }
    },
    computed: {
      virtualCellIndexKey() {
        return c$1.listCellIndexKey;
      },
      finalUseVirtualList() {
        if (this.useVirtualList && this.usePageScroll) {
          u$1.consoleErr("使用页面滚动时，开启虚拟列表无效！");
        }
        return this.useVirtualList && !this.usePageScroll;
      },
      finalUseInnerList() {
        return this.useInnerList || this.finalUseVirtualList && !this.forceCloseInnerList;
      },
      finalCellKeyName() {
        return this.cellKeyName;
      },
      finalVirtualPageHeight() {
        return this.scrollViewHeight > 0 ? this.scrollViewHeight : this.windowHeight;
      },
      finalFixedCellHeight() {
        return u$1.convertToPx(this.fixedCellHeight);
      },
      fianlVirtualCellIdPrefix() {
        const prefix = this.virtualCellIdPrefix ? this.virtualCellIdPrefix + "-" : "";
        return prefix + "zp-id";
      },
      finalPlaceholderTopHeightStyle() {
        return {};
      },
      virtualRangePageHeight() {
        return this.finalVirtualPageHeight * this.preloadPage;
      },
      virtualScrollDisTimeStamp() {
        return 1e3 / this.virtualScrollFps;
      }
    },
    methods: {
      // 在使用动态高度虚拟列表时，若在列表数组中需要插入某个item，需要调用此方法；item:需要插入的item，index:插入的cell位置，若index为2，则插入的item在原list的index=1之后，index从0开始
      doInsertVirtualListItem(item, index) {
        if (this.cellHeightMode !== Enum.CellHeightMode.Dynamic)
          return;
        this.realTotalData.splice(index, 0, item);
        this.realTotalData = [...this.realTotalData];
        this.virtualItemInsertedCount++;
        if (!item || Object.prototype.toString.call(item) !== "[object Object]") {
          item = { item };
        }
        const cellIndexKey = this.virtualCellIndexKey;
        item[cellIndexKey] = `custom-${this.virtualItemInsertedCount}`;
        item[c$1.listCellIndexUniqueKey] = `${this.virtualListKey}-${item[cellIndexKey]}`;
        this.$nextTick(async () => {
          let retryCount = 0;
          while (retryCount <= 10) {
            await u$1.wait(c$1.delayTime);
            const cellNode = await this._getVirtualCellNodeByIndex(item[cellIndexKey]);
            if (!cellNode) {
              retryCount++;
              continue;
            }
            const currentHeight = cellNode ? cellNode[0].height : 0;
            const lastHeightCache = this.virtualHeightCacheList[index - 1];
            const lastTotalHeight = lastHeightCache ? lastHeightCache.totalHeight : 0;
            this.virtualHeightCacheList.splice(index, 0, {
              height: currentHeight,
              lastTotalHeight,
              totalHeight: lastTotalHeight + currentHeight
            });
            for (let i2 = index + 1; i2 < this.virtualHeightCacheList.length; i2++) {
              const thisNode = this.virtualHeightCacheList[i2];
              thisNode.lastTotalHeight += currentHeight;
              thisNode.totalHeight += currentHeight;
            }
            this._updateVirtualScroll(this.oldScrollTop);
            break;
          }
        });
      },
      // 在使用动态高度虚拟列表时，手动更新指定cell的缓存高度(当cell高度在初始化之后再次改变后调用)；index:需要更新的cell在列表中的位置，从0开始
      didUpdateVirtualListCell(index) {
        if (this.cellHeightMode !== Enum.CellHeightMode.Dynamic)
          return;
        const currentNode = this.virtualHeightCacheList[index];
        this.$nextTick(() => {
          this._getVirtualCellNodeByIndex(index).then((cellNode) => {
            const cellNodeHeight = cellNode ? cellNode[0].height : 0;
            const heightDis = cellNodeHeight - currentNode.height;
            currentNode.height = cellNodeHeight;
            currentNode.totalHeight = currentNode.lastTotalHeight + cellNodeHeight;
            for (let i2 = index + 1; i2 < this.virtualHeightCacheList.length; i2++) {
              const thisNode = this.virtualHeightCacheList[i2];
              thisNode.totalHeight += heightDis;
              thisNode.lastTotalHeight += heightDis;
            }
          });
        });
      },
      // 在使用动态高度虚拟列表时，若删除了列表数组中的某个item，需要调用此方法以更新高度缓存数组；index:删除的cell在列表中的位置，从0开始
      didDeleteVirtualListCell(index) {
        if (this.cellHeightMode !== Enum.CellHeightMode.Dynamic)
          return;
        const currentNode = this.virtualHeightCacheList[index];
        for (let i2 = index + 1; i2 < this.virtualHeightCacheList.length; i2++) {
          const thisNode = this.virtualHeightCacheList[i2];
          thisNode.totalHeight -= currentNode.height;
          thisNode.lastTotalHeight -= currentNode.height;
        }
        this.virtualHeightCacheList.splice(index, 1);
      },
      // 手动触发虚拟列表渲染更新，可用于解决例如修改了虚拟列表数组中元素，但展示未更新的情况
      updateVirtualListRender() {
        if (this.finalUseVirtualList) {
          this.updateVirtualListFromDataChange = true;
          this.$nextTick(() => {
            this.getCellHeightRetryCount.fixed = 0;
            if (this.realTotalData.length) {
              this.cellHeightMode === Enum.CellHeightMode.Fixed && this.isFirstPage && this._updateFixedCellHeight();
            } else {
              this._resetDynamicListState(!this.isUserPullDown);
            }
            this._updateVirtualScroll(this.oldScrollTop);
          });
        }
      },
      // cellHeightMode为fixed时获取第一个cell高度
      _updateFixedCellHeight() {
        if (!this.finalFixedCellHeight) {
          this.$nextTick(() => {
            u$1.delay(() => {
              this._getVirtualCellNodeByIndex(0).then((cellNode) => {
                if (!cellNode) {
                  if (this.getCellHeightRetryCount.fixed > 10)
                    return;
                  this.getCellHeightRetryCount.fixed++;
                  this._updateFixedCellHeight();
                } else {
                  this.virtualCellHeight = cellNode[0].height;
                  this._updateVirtualScroll(this.oldScrollTop);
                }
              });
            }, c$1.delayTime, "updateFixedCellHeightDelay");
          });
        } else {
          this.virtualCellHeight = this.finalFixedCellHeight;
        }
      },
      // cellHeightMode为dynamic时获取每个cell高度
      _updateDynamicCellHeight(list, dataFrom = "bottom") {
        const dataFromTop = dataFrom === "top";
        const heightCacheList = this.virtualHeightCacheList;
        const currentCacheList = dataFromTop ? [] : heightCacheList;
        let listTotalHeight = 0;
        this.$nextTick(() => {
          u$1.delay(async () => {
            for (let i2 = 0; i2 < list.length; i2++) {
              const cellNode = await this._getVirtualCellNodeByIndex(list[i2][this.virtualCellIndexKey]);
              const currentHeight = cellNode ? cellNode[0].height : 0;
              if (!cellNode) {
                if (this.getCellHeightRetryCount.dynamic <= 10) {
                  heightCacheList.splice(heightCacheList.length - i2, i2);
                  this.getCellHeightRetryCount.dynamic++;
                  this._updateDynamicCellHeight(list, dataFrom);
                }
                return;
              }
              const lastHeightCache = currentCacheList.length ? currentCacheList.slice(-1)[0] : null;
              const lastTotalHeight = lastHeightCache ? lastHeightCache.totalHeight : 0;
              currentCacheList.push({
                height: currentHeight,
                lastTotalHeight,
                totalHeight: lastTotalHeight + currentHeight
              });
              if (dataFromTop) {
                listTotalHeight += currentHeight;
              }
            }
            if (dataFromTop && list.length) {
              for (let i2 = 0; i2 < heightCacheList.length; i2++) {
                const heightCacheItem = heightCacheList[i2];
                heightCacheItem.lastTotalHeight += listTotalHeight;
                heightCacheItem.totalHeight += listTotalHeight;
              }
              this.virtualHeightCacheList = currentCacheList.concat(heightCacheList);
            }
            this._updateVirtualScroll(this.oldScrollTop);
          }, c$1.delayTime, "updateDynamicCellHeightDelay");
        });
      },
      // 设置cellItem的index
      _setCellIndex(list, dataFrom = "bottom") {
        let currentItemIndex = 0;
        const cellIndexKey = this.virtualCellIndexKey;
        dataFrom === "bottom" && [Enum.QueryFrom.Refresh, Enum.QueryFrom.Reload].indexOf(this.queryFrom) >= 0 && this._resetDynamicListState();
        if (this.totalData.length && this.queryFrom !== Enum.QueryFrom.Refresh) {
          if (dataFrom === "bottom") {
            currentItemIndex = this.realTotalData.length;
            const lastItem = this.realTotalData.length ? this.realTotalData.slice(-1)[0] : null;
            if (lastItem && lastItem[cellIndexKey] !== void 0) {
              currentItemIndex = lastItem[cellIndexKey] + 1;
            }
          } else if (dataFrom === "top") {
            const firstItem = this.realTotalData.length ? this.realTotalData[0] : null;
            if (firstItem && firstItem[cellIndexKey] !== void 0) {
              currentItemIndex = firstItem[cellIndexKey] - list.length;
            }
          }
        } else {
          this._resetDynamicListState();
        }
        for (let i2 = 0; i2 < list.length; i2++) {
          let item = list[i2];
          if (!item || Object.prototype.toString.call(item) !== "[object Object]") {
            item = { item };
          }
          if (item[c$1.listCellIndexUniqueKey]) {
            item = u$1.deepCopy(item);
          }
          item[cellIndexKey] = currentItemIndex + i2;
          item[c$1.listCellIndexUniqueKey] = `${this.virtualListKey}-${item[cellIndexKey]}`;
          list[i2] = item;
        }
        this.getCellHeightRetryCount.dynamic = 0;
        this.cellHeightMode === Enum.CellHeightMode.Dynamic && this._updateDynamicCellHeight(list, dataFrom);
      },
      // 更新scroll滚动（虚拟列表滚动时触发）
      _updateVirtualScroll(scrollTop, scrollDiff = 0) {
        const currentTimeStamp = u$1.getTime();
        scrollTop === 0 && this._resetTopRange();
        if (scrollTop !== 0 && this.virtualScrollTimeStamp && currentTimeStamp - this.virtualScrollTimeStamp <= this.virtualScrollDisTimeStamp) {
          return;
        }
        this.virtualScrollTimeStamp = currentTimeStamp;
        let scrollIndex = 0;
        const cellHeightMode = this.cellHeightMode;
        if (cellHeightMode === Enum.CellHeightMode.Fixed) {
          scrollIndex = parseInt(scrollTop / this.virtualCellHeight) || 0;
          this._updateFixedTopRangeIndex(scrollIndex);
          this._updateFixedBottomRangeIndex(scrollIndex);
        } else if (cellHeightMode === Enum.CellHeightMode.Dynamic) {
          const scrollDirection = scrollDiff > 0 ? "top" : "bottom";
          const rangePageHeight = this.virtualRangePageHeight;
          const topRangePageOffset = scrollTop - rangePageHeight;
          const bottomRangePageOffset = scrollTop + this.finalVirtualPageHeight + rangePageHeight;
          let virtualBottomRangeIndex = 0;
          let virtualPlaceholderBottomHeight = 0;
          let reachedLimitBottom = false;
          const heightCacheList = this.virtualHeightCacheList;
          const lastHeightCache = !!heightCacheList ? heightCacheList.slice(-1)[0] : null;
          let startTopRangeIndex = this.virtualTopRangeIndex;
          if (scrollDirection === "bottom") {
            for (let i2 = startTopRangeIndex; i2 < heightCacheList.length; i2++) {
              const heightCacheItem = heightCacheList[i2];
              if (heightCacheItem && heightCacheItem.totalHeight > topRangePageOffset) {
                this.virtualTopRangeIndex = i2;
                this.virtualPlaceholderTopHeight = heightCacheItem.lastTotalHeight;
                break;
              }
            }
          } else {
            let topRangeMatched = false;
            for (let i2 = startTopRangeIndex; i2 >= 0; i2--) {
              const heightCacheItem = heightCacheList[i2];
              if (heightCacheItem && heightCacheItem.totalHeight < topRangePageOffset) {
                this.virtualTopRangeIndex = i2;
                this.virtualPlaceholderTopHeight = heightCacheItem.lastTotalHeight;
                topRangeMatched = true;
                break;
              }
            }
            !topRangeMatched && this._resetTopRange();
          }
          for (let i2 = this.virtualTopRangeIndex; i2 < heightCacheList.length; i2++) {
            const heightCacheItem = heightCacheList[i2];
            if (heightCacheItem && heightCacheItem.totalHeight > bottomRangePageOffset) {
              virtualBottomRangeIndex = i2;
              virtualPlaceholderBottomHeight = lastHeightCache.totalHeight - heightCacheItem.totalHeight;
              reachedLimitBottom = true;
              break;
            }
          }
          if (!reachedLimitBottom || this.virtualBottomRangeIndex === 0) {
            this.virtualBottomRangeIndex = this.realTotalData.length ? this.realTotalData.length - 1 : this.pageSize;
            this.virtualPlaceholderBottomHeight = 0;
          } else {
            this.virtualBottomRangeIndex = virtualBottomRangeIndex;
            this.virtualPlaceholderBottomHeight = virtualPlaceholderBottomHeight;
          }
          this._updateVirtualList();
        }
      },
      // 更新fixedCell模式下topRangeIndex&placeholderTopHeight
      _updateFixedTopRangeIndex(scrollIndex) {
        let virtualTopRangeIndex = this.virtualCellHeight === 0 ? 0 : scrollIndex - (parseInt(this.finalVirtualPageHeight / this.virtualCellHeight) || 1) * this.preloadPage;
        virtualTopRangeIndex *= this.virtualListCol;
        virtualTopRangeIndex = Math.max(0, virtualTopRangeIndex);
        this.virtualTopRangeIndex = virtualTopRangeIndex;
        this.virtualPlaceholderTopHeight = virtualTopRangeIndex / this.virtualListCol * this.virtualCellHeight;
      },
      // 更新fixedCell模式下bottomRangeIndex&placeholderBottomHeight
      _updateFixedBottomRangeIndex(scrollIndex) {
        let virtualBottomRangeIndex = this.virtualCellHeight === 0 ? this.pageSize : scrollIndex + (parseInt(this.finalVirtualPageHeight / this.virtualCellHeight) || 1) * (this.preloadPage + 1);
        virtualBottomRangeIndex *= this.virtualListCol;
        virtualBottomRangeIndex = Math.min(this.realTotalData.length, virtualBottomRangeIndex);
        this.virtualBottomRangeIndex = virtualBottomRangeIndex;
        this.virtualPlaceholderBottomHeight = (this.realTotalData.length - virtualBottomRangeIndex) * this.virtualCellHeight / this.virtualListCol;
        this._updateVirtualList();
      },
      // 更新virtualList
      _updateVirtualList() {
        const shouldUpdateList = this.updateVirtualListFromDataChange || (this.lastVirtualTopRangeIndex !== this.virtualTopRangeIndex || this.lastVirtualBottomRangeIndex !== this.virtualBottomRangeIndex);
        if (shouldUpdateList) {
          this.updateVirtualListFromDataChange = false;
          this.lastVirtualTopRangeIndex = this.virtualTopRangeIndex;
          this.lastVirtualBottomRangeIndex = this.virtualBottomRangeIndex;
          this.virtualList = this.realTotalData.slice(this.virtualTopRangeIndex, this.virtualBottomRangeIndex + 1);
        }
      },
      // 重置动态cell模式下的高度缓存数据、虚拟列表和滚动状态
      _resetDynamicListState(resetVirtualList = false) {
        this.virtualHeightCacheList = [];
        if (resetVirtualList) {
          this.virtualList = [];
        }
        this.virtualTopRangeIndex = 0;
        this.virtualPlaceholderTopHeight = 0;
      },
      // 重置topRangeIndex和placeholderTopHeight
      _resetTopRange() {
        this.virtualTopRangeIndex = 0;
        this.virtualPlaceholderTopHeight = 0;
        this._updateVirtualList();
      },
      // 检测虚拟列表当前滚动位置，如发现滚动位置不正确则重新计算虚拟列表相关参数(为解决在App中可能出现的长时间进入后台后打开App白屏的问题)
      _checkVirtualListScroll() {
        if (this.finalUseVirtualList) {
          this.$nextTick(() => {
            this._getNodeClientRect(".zp-paging-touch-view").then((node2) => {
              const currentTop = node2 ? node2[0].top : 0;
              if (!node2 || currentTop === this.pagingOrgTop && this.virtualPlaceholderTopHeight !== 0) {
                this._updateVirtualScroll(0);
              }
            });
          });
        }
      },
      // 获取对应index的虚拟列表cell节点信息
      _getVirtualCellNodeByIndex(index) {
        let inDom = this.finalUseInnerList;
        return this._getNodeClientRect(`#${this.fianlVirtualCellIdPrefix}-${index}`, inDom);
      },
      // 处理使用内置列表时点击了cell事件
      _innerCellClick(item, index) {
        this.$emit("innerCellClick", item, index);
      }
    }
  };
  const systemInfo$2 = u$1.getSystemInfoSync();
  const _sfc_main$1T = {
    name: "z-paging",
    components: {
      zPagingRefresh,
      zPagingLoadMore,
      zPagingEmptyView: __easycom_0$8
    },
    mixins: [
      commonLayoutModule,
      dataHandleModule,
      i18nModule,
      nvueModule,
      emptyModule,
      refresherModule,
      loadMoreModule,
      loadingModule,
      chatRecordModerModule,
      scrollerModule,
      backToTopModule,
      virtualListModule
    ],
    data() {
      return {
        // --------------静态资源---------------
        base64BackToTop: zStatic.base64BackToTop,
        // -------------全局数据相关--------------
        // 当前加载类型
        loadingType: Enum.LoadingType.Refresher,
        requestTimeStamp: 0,
        wxsPropType: "",
        renderPropScrollTop: -1,
        checkScrolledToBottomTimeOut: null,
        cacheTopHeight: -1,
        statusBarHeight: systemInfo$2.statusBarHeight,
        scrollViewHeight: 0,
        pagingOrgTop: -1,
        // --------------状态&判断---------------
        insideOfPaging: -1,
        isLoadFailed: false,
        isIos: systemInfo$2.platform === "ios",
        disabledBounce: false,
        fromCompleteEmit: false,
        disabledCompleteEmit: false,
        pageLaunched: false,
        active: false,
        // ---------------wxs相关---------------
        wxsIsScrollTopInTopRange: true,
        wxsScrollTop: 0,
        wxsPageScrollTop: 0,
        wxsOnPullingDown: false
      };
    },
    props: {
      // 调用complete后延迟处理的时间，单位为毫秒，默认0毫秒，优先级高于minDelay
      delay: {
        type: [Number, String],
        default: u$1.gc("delay", 0)
      },
      // 触发@query后最小延迟处理的时间，单位为毫秒，默认0毫秒，优先级低于delay（假设设置为300毫秒，若分页请求时间小于300毫秒，则在调用complete后延迟[300毫秒-请求时长]；若请求时长大于300毫秒，则不延迟），当show-refresher-when-reload为true或reload(true)时，其最小值为400
      minDelay: {
        type: [Number, String],
        default: u$1.gc("minDelay", 0)
      },
      // 设置z-paging的style，部分平台(如微信小程序)无法直接修改组件的style，可使用此属性代替
      pagingStyle: {
        type: Object,
        default: u$1.gc("pagingStyle", {})
      },
      // 设置z-paging的class，优先级低于pagingStyle和height、width、maxWidth、bgColor
      pagingClass: {
        type: [String, Array, Object],
        default: u$1.gc("pagingClass", "")
      },
      // z-paging的高度，优先级低于pagingStyle中设置的height；传字符串，如100px、100rpx、100%
      height: {
        type: String,
        default: u$1.gc("height", "")
      },
      // z-paging的宽度，优先级低于pagingStyle中设置的width；传字符串，如100px、100rpx、100%
      width: {
        type: String,
        default: u$1.gc("width", "")
      },
      // z-paging的最大宽度，优先级低于pagingStyle中设置的max-width；传字符串，如100px、100rpx、100%。默认为空，也就是铺满窗口宽度，若设置了特定值则会自动添加margin: 0 auto
      maxWidth: {
        type: String,
        default: u$1.gc("maxWidth", "")
      },
      // z-paging的背景色，优先级低于pagingStyle中设置的background。传字符串，如"#ffffff"
      bgColor: {
        type: String,
        default: u$1.gc("bgColor", "")
      },
      // 设置z-paging的容器(插槽的父view)的style
      pagingContentStyle: {
        type: Object,
        default: u$1.gc("pagingContentStyle", {})
      },
      // z-paging是否自动高度，若自动高度则会自动铺满屏幕
      autoHeight: {
        type: Boolean,
        default: u$1.gc("autoHeight", false)
      },
      // z-paging是否自动高度时，附加的高度，注意添加单位px或rpx，若需要减少高度，则传负数
      autoHeightAddition: {
        type: [Number, String],
        default: u$1.gc("autoHeightAddition", "0px")
      },
      // loading(下拉刷新、上拉加载更多)的主题样式，支持black，white，默认black
      defaultThemeStyle: {
        type: String,
        default: u$1.gc("defaultThemeStyle", "black")
      },
      // z-paging是否使用fixed布局，若使用fixed布局，则z-paging的父view无需固定高度，z-paging高度默认为100%，默认为是(当使用内置scroll-view滚动时有效)
      fixed: {
        type: Boolean,
        default: u$1.gc("fixed", true)
      },
      // 是否开启底部安全区域适配
      safeAreaInsetBottom: {
        type: Boolean,
        default: u$1.gc("safeAreaInsetBottom", false)
      },
      // 开启底部安全区域适配后，是否使用placeholder形式实现，默认为否。为否时滚动区域会自动避开底部安全区域，也就是所有滚动内容都不会挡住底部安全区域，若设置为是，则滚动时滚动内容会挡住底部安全区域，但是当滚动到底部时才会避开底部安全区域
      useSafeAreaPlaceholder: {
        type: Boolean,
        default: u$1.gc("useSafeAreaPlaceholder", false)
      },
      // z-paging bottom的背景色，默认透明，传字符串，如"#ffffff"
      bottomBgColor: {
        type: String,
        default: u$1.gc("bottomBgColor", "")
      },
      // slot="top"的view的z-index，默认为99，仅使用页面滚动时有效
      topZIndex: {
        type: Number,
        default: u$1.gc("topZIndex", 99)
      },
      // z-paging内容容器父view的z-index，默认为1
      superContentZIndex: {
        type: Number,
        default: u$1.gc("superContentZIndex", 1)
      },
      // z-paging内容容器部分的z-index，默认为1
      contentZIndex: {
        type: Number,
        default: u$1.gc("contentZIndex", 1)
      },
      // z-paging二楼的z-index，默认为100
      f2ZIndex: {
        type: Number,
        default: u$1.gc("f2ZIndex", 100)
      },
      // 使用页面滚动时，是否在不满屏时自动填充满屏幕，默认为是
      autoFullHeight: {
        type: Boolean,
        default: u$1.gc("autoFullHeight", true)
      },
      // 是否监听列表触摸方向改变，默认为否
      watchTouchDirectionChange: {
        type: Boolean,
        default: u$1.gc("watchTouchDirectionChange", false)
      },
      // 是否监听列表滚动方向改变，默认为否
      watchScrollDirectionChange: {
        type: Boolean,
        default: u$1.gc("watchScrollDirectionChange", false)
      },
      // 是否只使用基础布局，设置为true后将关闭mounted自动请求数据、关闭下拉刷新和滚动到底部加载更多，强制隐藏空数据图。默认为否
      layoutOnly: {
        type: Boolean,
        default: u$1.gc("layoutOnly", false)
      },
      // z-paging中布局的单位，默认为rpx
      unit: {
        type: String,
        default: u$1.gc("unit", "rpx")
      }
    },
    created() {
      if (this.createdReload && !this.isOnly && this.auto) {
        this._startLoading();
        this.$nextTick(this._preReload);
      }
    },
    mounted() {
      this.active = true;
      this.wxsPropType = u$1.getTime().toString();
      this.renderJsIgnore;
      if (!this.createdReload && !this.isOnly && this.auto) {
        u$1.delay(() => this.$nextTick(this._preReload), 0);
      }
      this.finalUseCache && this._setListByLocalCache();
      this.systemInfo = u$1.getSystemInfoSync();
      this.$nextTick(() => {
        this.systemInfo = u$1.getSystemInfoSync();
        !this.usePageScroll && this.autoHeight && this._setAutoHeight();
        this.loaded = true;
        u$1.delay(() => {
          this.updateFixedLayout();
          this._updateCachedSuperContentHeight();
          this._updateScrollViewHeight();
        });
      });
      this.updatePageScrollTopHeight();
      this.updatePageScrollBottomHeight();
      this.updateLeftAndRightWidth();
      if (this.finalRefresherEnabled && this.useCustomRefresher) {
        this.$nextTick(() => {
          this.isTouchmoving = true;
        });
      }
      if (!this.layoutOnly) {
        this._onEmit();
      }
    },
    destroyed() {
      this._handleUnmounted();
    },
    unmounted() {
      this._handleUnmounted();
    },
    watch: {
      defaultThemeStyle: {
        handler(newVal) {
          if (newVal.length) {
            this.finalRefresherDefaultStyle = newVal;
          }
        },
        immediate: true
      },
      autoHeight(newVal) {
        this.loaded && !this.usePageScroll && this._setAutoHeight(newVal);
      },
      autoHeightAddition(newVal) {
        this.loaded && !this.usePageScroll && this.autoHeight && this._setAutoHeight(newVal);
      }
    },
    computed: {
      // 当前z-paging的内置样式
      finalPagingStyle() {
        const pagingStyle = { ...this.pagingStyle };
        if (!this.systemInfo)
          return pagingStyle;
        const { windowTop, windowBottom } = this;
        if (!this.usePageScroll && this.fixed) {
          if (windowTop && !pagingStyle.top) {
            pagingStyle.top = windowTop + "px";
          }
          if (windowBottom && !pagingStyle.bottom) {
            pagingStyle.bottom = windowBottom + "px";
          }
        }
        if (this.bgColor.length && !pagingStyle["background"]) {
          pagingStyle["background"] = this.bgColor;
        }
        if (this.height.length && !pagingStyle["height"]) {
          pagingStyle["height"] = this.height;
        }
        if (this.width.length && !pagingStyle["width"]) {
          pagingStyle["width"] = this.width;
        }
        if (this.maxWidth.length && !pagingStyle["max-width"]) {
          pagingStyle["max-width"] = this.maxWidth;
          pagingStyle["margin"] = "0 auto";
        }
        return pagingStyle;
      },
      // 当前z-paging内容的样式
      finalPagingContentStyle() {
        if (this.contentZIndex != 1) {
          this.pagingContentStyle["z-index"] = this.contentZIndex;
          this.pagingContentStyle["position"] = "relative";
        }
        return this.pagingContentStyle;
      },
      // 最终的当前开启安全区域适配后，是否使用placeholder形式实现。如果slot=bottom存在，则应当交由固定在底部的view处理，因此需排除此情况
      finalUseSafeAreaPlaceholder() {
        return this.useSafeAreaPlaceholder && !this.zSlots.bottom;
      },
      renderJsIgnore() {
        if (this.usePageScroll && this.useChatRecordMode || !this.refresherEnabled && this.scrollable || !this.useCustomRefresher) {
          this.$nextTick(() => {
            this.renderPropScrollTop = 10;
          });
        }
        return 0;
      },
      windowHeight() {
        if (!this.systemInfo)
          return 0;
        return this.systemInfo.windowHeight || 0;
      },
      windowBottom() {
        if (!this.systemInfo)
          return 0;
        return this.systemInfo.windowBottom || 0;
      },
      // 是否是ios+h5
      isIosAndH5() {
        return false;
      },
      // 是否是只使用基础布局或者只使用下拉刷新
      isOnly() {
        return this.layoutOnly || this.refresherOnly;
      }
    },
    methods: {
      // 当前版本号
      getVersion() {
        return `z-paging v${c$1.version}`;
      },
      // 设置nvue List的specialEffects
      setSpecialEffects(args) {
        this.setListSpecialEffects(args);
      },
      // 与setSpecialEffects等效，兼容旧版本
      setListSpecialEffects(args) {
        this.nFixFreezing = args && Object.keys(args).length;
        if (this.isIos) {
          this.privateRefresherEnabled = 0;
        }
        !this.usePageScroll && this.$refs["zp-n-list"].setSpecialEffects(args);
      },
      // 当app长时间进入后台后进入前台，因系统内存管理导致app重新加载时，进行一些适配处理
      _handlePageLaunch() {
        if (this.pageLaunched) {
          this.refresherThresholdUpdateTag = 1;
          this.$nextTick(() => {
            this.refresherThresholdUpdateTag = 0;
          });
          this._checkVirtualListScroll();
        }
        this.pageLaunched = true;
      },
      // 使手机发生较短时间的振动（15ms）
      _doVibrateShort() {
        if (this.isIos) {
          const UISelectionFeedbackGenerator = plus.ios.importClass("UISelectionFeedbackGenerator");
          const feedbackGenerator = new UISelectionFeedbackGenerator();
          feedbackGenerator.init();
          setTimeout(() => {
            feedbackGenerator.selectionChanged();
          }, 0);
        } else {
          plus.device.vibrate(15);
        }
      },
      // 设置z-paging高度
      async _setAutoHeight(shouldFullHeight = true, scrollViewNode = null) {
        const heightKey = "min-height";
        try {
          if (shouldFullHeight) {
            let finalScrollViewNode = scrollViewNode || await this._getNodeClientRect(".zp-scroll-view");
            let finalScrollBottomNode = await this._getNodeClientRect(".zp-page-bottom");
            if (finalScrollViewNode) {
              const scrollViewTop = finalScrollViewNode[0].top;
              let scrollViewHeight = this.windowHeight - scrollViewTop;
              scrollViewHeight -= finalScrollBottomNode ? finalScrollBottomNode[0].height : 0;
              const additionHeight = u$1.convertToPx(this.autoHeightAddition);
              let importantSuffix = " !important";
              const finalHeight = scrollViewHeight + additionHeight - (this.insideMore ? 1 : 0) + "px" + importantSuffix;
              this.$set(this.scrollViewStyle, heightKey, finalHeight);
              this.$set(this.scrollViewInStyle, heightKey, finalHeight);
            }
          } else {
            this.$delete(this.scrollViewStyle, heightKey);
            this.$delete(this.scrollViewInStyle, heightKey);
          }
        } catch (e2) {
        }
      },
      // 更新scroll-view高度
      async _updateScrollViewHeight() {
        const scrollViewNode = await this._getNodeClientRect(".zp-scroll-view");
        if (scrollViewNode) {
          const scrollViewNodeHeight = scrollViewNode[0].height;
          this.scrollViewHeight = scrollViewNodeHeight;
          this.pagingOrgTop = scrollViewNode[0].top;
        }
      },
      // 组件销毁后续处理
      _handleUnmounted() {
        this.active = false;
        if (!this.layoutOnly) {
          this._offEmit();
        }
        this.useChatRecordMode && uni.offKeyboardHeightChange(this._handleKeyboardHeightChange);
      },
      // 触发更新是否超出页面状态
      _updateInsideOfPaging() {
        this.insideMore && this.insideOfPaging === true && setTimeout(this.doLoadMore, 200);
      },
      // 清除timeout
      _cleanTimeout(timeout) {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
        return timeout;
      },
      // 添加全局emit监听
      _onEmit() {
        uni.$on(c$1.errorUpdateKey, (errorMsg) => {
          if (this.loading) {
            if (!!errorMsg) {
              this.customerEmptyViewErrorText = errorMsg;
            }
            this.complete(false).catch(() => {
            });
          }
        });
        uni.$on(c$1.completeUpdateKey, (data) => {
          setTimeout(() => {
            if (this.loading) {
              if (!this.disabledCompleteEmit) {
                const type = data.type || "normal";
                const list = data.list || data;
                const rule = data.rule;
                this.fromCompleteEmit = true;
                switch (type) {
                  case "normal":
                    this.complete(list);
                    break;
                  case "total":
                    this.completeByTotal(list, rule);
                    break;
                  case "nomore":
                    this.completeByNoMore(list, rule);
                    break;
                  case "key":
                    this.completeByKey(list, rule);
                    break;
                }
              } else {
                this.disabledCompleteEmit = false;
              }
            }
          }, 1);
        });
      },
      // 销毁全局emit和listener监听
      _offEmit() {
        uni.$off(c$1.errorUpdateKey);
        uni.$off(c$1.completeUpdateKey);
      }
    }
  };
  const block0$3 = (Comp) => {
    (Comp.$renderjs || (Comp.$renderjs = [])).push("pagingRenderjs");
    (Comp.$renderjsModules || (Comp.$renderjsModules = {}))["pagingRenderjs"] = "463e3cc3";
  };
  const block1$1 = (Comp) => {
    (Comp.$wxs || (Comp.$wxs = [])).push("pagingWxs");
    (Comp.$wxsModules || (Comp.$wxsModules = {}))["pagingWxs"] = "42613348";
  };
  function _sfc_render$1S(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_z_paging_refresh = vue.resolveComponent("z-paging-refresh");
    const _component_z_paging_load_more = vue.resolveComponent("z-paging-load-more");
    const _component_z_paging_empty_view = resolveEasycom(vue.resolveDynamicComponent("z-paging-empty-view"), __easycom_0$8);
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: vue.normalizeClass([{ "z-paging-content": true, "z-paging-content-full": !_ctx.usePageScroll, "z-paging-content-fixed": !_ctx.usePageScroll && _ctx.fixed, "z-paging-content-page": _ctx.usePageScroll, "z-paging-reached-top": _ctx.renderPropScrollTop < 1, "z-paging-use-chat-record-mode": _ctx.useChatRecordMode }, _ctx.pagingClass]),
        style: vue.normalizeStyle([_ctx.finalPagingStyle])
      },
      [
        _ctx.showF2 && _ctx.showRefresherF2 ? (vue.openBlock(), vue.createElementBlock(
          "view",
          {
            key: 0,
            onTouchmove: _cache[0] || (_cache[0] = vue.withModifiers(() => {
            }, ["stop", "prevent"])),
            class: "zp-f2-content",
            style: vue.normalizeStyle([{ "transform": _ctx.f2Transform, "transition": `transform .2s linear`, "height": _ctx.superContentHeight + "px", "z-index": _ctx.f2ZIndex }])
          },
          [
            vue.renderSlot(_ctx.$slots, "f2", {}, void 0, true)
          ],
          36
          /* STYLE, NEED_HYDRATION */
        )) : vue.createCommentVNode("v-if", true),
        _ctx.zSlots.top ? (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 1 },
          [
            !_ctx.usePageScroll ? vue.renderSlot(_ctx.$slots, "top", { key: 0 }, void 0, true) : (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 1,
                class: "zp-page-top",
                onTouchmove: _cache[1] || (_cache[1] = vue.withModifiers(() => {
                }, ["stop", "prevent"])),
                style: vue.normalizeStyle([{ "top": `${_ctx.windowTop}px`, "z-index": _ctx.topZIndex }])
              },
              [
                vue.renderSlot(_ctx.$slots, "top", {}, void 0, true)
              ],
              36
              /* STYLE, NEED_HYDRATION */
            ))
          ],
          64
          /* STABLE_FRAGMENT */
        )) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass({ "zp-view-super": true, "zp-scroll-view-super": !_ctx.usePageScroll }),
            style: vue.normalizeStyle([_ctx.finalScrollViewStyle])
          },
          [
            _ctx.zSlots.left ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 0,
                class: vue.normalizeClass({ "zp-page-left": true, "zp-absoulte": _ctx.finalIsOldWebView })
              },
              [
                vue.renderSlot(_ctx.$slots, "left", {}, void 0, true)
              ],
              2
              /* CLASS */
            )) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass({ "zp-scroll-view-container": true, "zp-absoulte": _ctx.finalIsOldWebView }),
                style: vue.normalizeStyle([_ctx.scrollViewContainerStyle])
              },
              [
                vue.createElementVNode("scroll-view", {
                  ref: "zp-scroll-view",
                  class: vue.normalizeClass({ "zp-scroll-view": true, "zp-scroll-view-absolute": !_ctx.usePageScroll, "zp-scroll-view-hide-scrollbar": !_ctx.showScrollbar }),
                  style: vue.normalizeStyle([_ctx.chatRecordRotateStyle]),
                  "scroll-top": _ctx.scrollTop,
                  "scroll-left": _ctx.scrollLeft,
                  "scroll-x": _ctx.scrollX,
                  "scroll-y": _ctx.finalScrollable,
                  "enable-back-to-top": _ctx.finalEnableBackToTop,
                  "show-scrollbar": _ctx.showScrollbar,
                  "scroll-with-animation": _ctx.finalScrollWithAnimation,
                  "scroll-into-view": _ctx.scrollIntoView,
                  "lower-threshold": _ctx.finalLowerThreshold,
                  "upper-threshold": 5,
                  "refresher-enabled": _ctx.finalRefresherEnabled && !_ctx.useCustomRefresher,
                  "refresher-threshold": _ctx.finalRefresherThreshold,
                  "refresher-default-style": _ctx.finalRefresherDefaultStyle,
                  "refresher-background": _ctx.refresherBackground,
                  "refresher-triggered": _ctx.finalRefresherTriggered,
                  onScroll: _cache[12] || (_cache[12] = (...args) => _ctx._scroll && _ctx._scroll(...args)),
                  onScrolltolower: _cache[13] || (_cache[13] = (...args) => _ctx._onScrollToLower && _ctx._onScrollToLower(...args)),
                  onScrolltoupper: _cache[14] || (_cache[14] = (...args) => _ctx._onScrollToUpper && _ctx._onScrollToUpper(...args)),
                  onRefresherrestore: _cache[15] || (_cache[15] = (...args) => _ctx._onRestore && _ctx._onRestore(...args)),
                  onRefresherrefresh: _cache[16] || (_cache[16] = ($event) => _ctx._onRefresh(true))
                }, [
                  vue.createElementVNode(
                    "view",
                    {
                      class: "zp-paging-touch-view",
                      onTouchstart: _cache[4] || (_cache[4] = (...args) => _ctx.pagingWxs.touchstart && _ctx.pagingWxs.touchstart(...args)),
                      onTouchmove: _cache[5] || (_cache[5] = (...args) => _ctx.pagingWxs.touchmove && _ctx.pagingWxs.touchmove(...args)),
                      onTouchend: _cache[6] || (_cache[6] = (...args) => _ctx.pagingWxs.touchend && _ctx.pagingWxs.touchend(...args)),
                      onTouchcancel: _cache[7] || (_cache[7] = (...args) => _ctx.pagingWxs.touchend && _ctx.pagingWxs.touchend(...args)),
                      onMousedown: _cache[8] || (_cache[8] = (...args) => _ctx.pagingWxs.mousedown && _ctx.pagingWxs.mousedown(...args)),
                      onMousemove: _cache[9] || (_cache[9] = (...args) => _ctx.pagingWxs.mousemove && _ctx.pagingWxs.mousemove(...args)),
                      onMouseup: _cache[10] || (_cache[10] = (...args) => _ctx.pagingWxs.mouseup && _ctx.pagingWxs.mouseup(...args)),
                      onMouseleave: _cache[11] || (_cache[11] = (...args) => _ctx.pagingWxs.mouseleave && _ctx.pagingWxs.mouseleave(...args))
                    },
                    [
                      _ctx.finalRefresherFixedBacHeight > 0 ? (vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: 0,
                          class: "zp-fixed-bac-view",
                          style: vue.normalizeStyle([{ "background": _ctx.refresherFixedBackground, "height": `${_ctx.finalRefresherFixedBacHeight}px` }])
                        },
                        null,
                        4
                        /* STYLE */
                      )) : vue.createCommentVNode("v-if", true),
                      vue.createElementVNode("view", {
                        class: "zp-paging-main",
                        style: vue.normalizeStyle([_ctx.scrollViewInStyle, { "transform": _ctx.finalRefresherTransform, "transition": _ctx.refresherTransition }]),
                        "change:prop": _ctx.pagingWxs.propObserver,
                        prop: vue.wp(_ctx.wxsPropType),
                        "data-refresherThreshold": _ctx.finalRefresherThreshold,
                        "data-refresherF2Enabled": _ctx.refresherF2Enabled,
                        "data-refresherF2Threshold": _ctx.finalRefresherF2Threshold,
                        "data-isIos": _ctx.isIos,
                        "data-loading": _ctx.loading || _ctx.isRefresherInComplete,
                        "data-useChatRecordMode": _ctx.useChatRecordMode,
                        "data-refresherEnabled": _ctx.finalRefresherEnabled,
                        "data-useCustomRefresher": _ctx.useCustomRefresher,
                        "data-pageScrollTop": _ctx.wxsPageScrollTop,
                        "data-scrollTop": _ctx.wxsScrollTop,
                        "data-refresherMaxAngle": _ctx.refresherMaxAngle,
                        "data-refresherNoTransform": _ctx.refresherNoTransform,
                        "data-refresherAecc": _ctx.refresherAngleEnableChangeContinued,
                        "data-usePageScroll": _ctx.usePageScroll,
                        "data-watchTouchDirectionChange": _ctx.watchTouchDirectionChange,
                        "data-oldIsTouchmoving": _ctx.isTouchmoving,
                        "data-refresherOutRate": _ctx.finalRefresherOutRate,
                        "data-refresherPullRate": _ctx.finalRefresherPullRate,
                        "data-hasTouchmove": _ctx.hasTouchmove,
                        "change:renderPropIsIosAndH5": _ctx.pagingRenderjs.renderPropIsIosAndH5Change,
                        renderPropIsIosAndH5: vue.wp(_ctx.isIosAndH5)
                      }, [
                        _ctx.showRefresher ? (vue.openBlock(), vue.createElementBlock(
                          "view",
                          {
                            key: 0,
                            class: "zp-custom-refresher-view",
                            style: vue.normalizeStyle([{ "margin-top": `-${_ctx.finalRefresherThreshold + _ctx.refresherThresholdUpdateTag}px`, "background": _ctx.refresherBackground, "opacity": _ctx.isTouchmoving ? 1 : 0 }])
                          },
                          [
                            vue.createElementVNode(
                              "view",
                              {
                                class: "zp-custom-refresher-container",
                                style: vue.normalizeStyle([{ "height": `${_ctx.finalRefresherThreshold}px`, "background": _ctx.refresherBackground }])
                              },
                              [
                                _ctx.useRefresherStatusBarPlaceholder ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 0,
                                    class: "zp-custom-refresher-status-bar-placeholder",
                                    style: vue.normalizeStyle([{ "height": `${_ctx.statusBarHeight}px` }])
                                  },
                                  null,
                                  4
                                  /* STYLE */
                                )) : vue.createCommentVNode("v-if", true),
                                vue.createElementVNode("view", { class: "zp-custom-refresher-slot-view" }, [
                                  !(_ctx.zSlots.refresherComplete && _ctx.refresherStatus === _ctx.R.Complete) && !(_ctx.zSlots.refresherF2 && _ctx.refresherStatus === _ctx.R.GoF2) ? vue.renderSlot(_ctx.$slots, "refresher", {
                                    key: 0,
                                    refresherStatus: _ctx.refresherStatus
                                  }, void 0, true) : vue.createCommentVNode("v-if", true)
                                ]),
                                _ctx.zSlots.refresherComplete && _ctx.refresherStatus === _ctx.R.Complete ? vue.renderSlot(_ctx.$slots, "refresherComplete", { key: 1 }, void 0, true) : _ctx.zSlots.refresherF2 && _ctx.refresherStatus === _ctx.R.GoF2 ? vue.renderSlot(_ctx.$slots, "refresherF2", { key: 2 }, void 0, true) : !_ctx.showCustomRefresher ? (vue.openBlock(), vue.createBlock(_component_z_paging_refresh, {
                                  key: 3,
                                  ref: "refresh",
                                  class: "zp-custom-refresher-refresh",
                                  style: vue.normalizeStyle([{ "height": `${_ctx.finalRefresherThreshold - _ctx.finalRefresherThresholdPlaceholder}px` }]),
                                  status: _ctx.refresherStatus,
                                  defaultThemeStyle: _ctx.finalRefresherThemeStyle,
                                  defaultText: _ctx.finalRefresherDefaultText,
                                  isIos: _ctx.isIos,
                                  pullingText: _ctx.finalRefresherPullingText,
                                  refreshingText: _ctx.finalRefresherRefreshingText,
                                  completeText: _ctx.finalRefresherCompleteText,
                                  goF2Text: _ctx.finalRefresherGoF2Text,
                                  defaultImg: _ctx.refresherDefaultImg,
                                  pullingImg: _ctx.refresherPullingImg,
                                  refreshingImg: _ctx.refresherRefreshingImg,
                                  completeImg: _ctx.refresherCompleteImg,
                                  refreshingAnimated: _ctx.refresherRefreshingAnimated,
                                  showUpdateTime: _ctx.showRefresherUpdateTime,
                                  updateTimeKey: _ctx.refresherUpdateTimeKey,
                                  updateTimeTextMap: _ctx.finalRefresherUpdateTimeTextMap,
                                  imgStyle: _ctx.refresherImgStyle,
                                  titleStyle: _ctx.refresherTitleStyle,
                                  updateTimeStyle: _ctx.refresherUpdateTimeStyle,
                                  unit: _ctx.unit
                                }, null, 8, ["style", "status", "defaultThemeStyle", "defaultText", "isIos", "pullingText", "refreshingText", "completeText", "goF2Text", "defaultImg", "pullingImg", "refreshingImg", "completeImg", "refreshingAnimated", "showUpdateTime", "updateTimeKey", "updateTimeTextMap", "imgStyle", "titleStyle", "updateTimeStyle", "unit"])) : vue.createCommentVNode("v-if", true)
                              ],
                              4
                              /* STYLE */
                            )
                          ],
                          4
                          /* STYLE */
                        )) : vue.createCommentVNode("v-if", true),
                        vue.createElementVNode(
                          "view",
                          {
                            class: "zp-paging-container",
                            style: vue.normalizeStyle([{ justifyContent: _ctx.useChatRecordMode ? "flex-end" : "flex-start" }])
                          },
                          [
                            _ctx.showLoading && _ctx.zSlots.loading && !_ctx.loadingFullFixed ? vue.renderSlot(_ctx.$slots, "loading", { key: 0 }, void 0, true) : vue.createCommentVNode("v-if", true),
                            vue.createElementVNode(
                              "view",
                              {
                                class: "zp-paging-container-content",
                                style: vue.normalizeStyle([_ctx.finalPlaceholderTopHeightStyle, _ctx.finalPagingContentStyle])
                              },
                              [
                                _ctx.useVirtualList ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 0,
                                    class: "zp-virtual-placeholder",
                                    style: vue.normalizeStyle([{ height: _ctx.virtualPlaceholderTopHeight + "px" }])
                                  },
                                  null,
                                  4
                                  /* STYLE */
                                )) : vue.createCommentVNode("v-if", true),
                                vue.renderSlot(_ctx.$slots, "default", {}, void 0, true),
                                _ctx.finalUseInnerList ? (vue.openBlock(), vue.createElementBlock(
                                  vue.Fragment,
                                  { key: 1 },
                                  [
                                    vue.renderSlot(_ctx.$slots, "header", {}, void 0, true),
                                    vue.createElementVNode(
                                      "view",
                                      {
                                        class: "zp-list-container",
                                        style: vue.normalizeStyle([_ctx.innerListStyle])
                                      },
                                      [
                                        _ctx.finalUseVirtualList ? (vue.openBlock(true), vue.createElementBlock(
                                          vue.Fragment,
                                          { key: 0 },
                                          vue.renderList(_ctx.virtualList, (item, index) => {
                                            return vue.openBlock(), vue.createElementBlock("view", {
                                              class: "zp-list-cell",
                                              style: vue.normalizeStyle([_ctx.innerCellStyle]),
                                              id: `${_ctx.fianlVirtualCellIdPrefix}-${item[_ctx.virtualCellIndexKey]}`,
                                              key: item["zp_unique_index"],
                                              onClick: ($event) => _ctx._innerCellClick(item, _ctx.virtualTopRangeIndex + index)
                                            }, [
                                              _ctx.useCompatibilityMode ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, "使用兼容模式请在组件源码z-paging.vue第105行中注释这一行，并打开下面一行注释")) : vue.renderSlot(_ctx.$slots, "cell", {
                                                key: 1,
                                                item,
                                                index: _ctx.virtualTopRangeIndex + index
                                              }, void 0, true)
                                            ], 12, ["id", "onClick"]);
                                          }),
                                          128
                                          /* KEYED_FRAGMENT */
                                        )) : (vue.openBlock(true), vue.createElementBlock(
                                          vue.Fragment,
                                          { key: 1 },
                                          vue.renderList(_ctx.realTotalData, (item, index) => {
                                            return vue.openBlock(), vue.createElementBlock("view", {
                                              class: "zp-list-cell",
                                              key: index,
                                              onClick: ($event) => _ctx._innerCellClick(item, index)
                                            }, [
                                              vue.renderSlot(_ctx.$slots, "cell", {
                                                item,
                                                index
                                              }, void 0, true)
                                            ], 8, ["onClick"]);
                                          }),
                                          128
                                          /* KEYED_FRAGMENT */
                                        ))
                                      ],
                                      4
                                      /* STYLE */
                                    ),
                                    vue.renderSlot(_ctx.$slots, "footer", {}, void 0, true)
                                  ],
                                  64
                                  /* STABLE_FRAGMENT */
                                )) : vue.createCommentVNode("v-if", true),
                                _ctx.useChatRecordMode && _ctx.realTotalData.length >= _ctx.defaultPageSize && (_ctx.loadingStatus !== _ctx.M.NoMore || _ctx.zSlots.chatNoMore) && (_ctx.realTotalData.length || _ctx.showChatLoadingWhenReload && _ctx.showLoading) && !_ctx.isFirstPageAndNoMore ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 2,
                                    style: vue.normalizeStyle([_ctx.chatRecordRotateStyle])
                                  },
                                  [
                                    _ctx.loadingStatus === _ctx.M.NoMore && _ctx.zSlots.chatNoMore ? vue.renderSlot(_ctx.$slots, "chatNoMore", { key: 0 }, void 0, true) : (vue.openBlock(), vue.createElementBlock(
                                      vue.Fragment,
                                      { key: 1 },
                                      [
                                        _ctx.zSlots.chatLoading ? vue.renderSlot(_ctx.$slots, "chatLoading", {
                                          key: 0,
                                          loadingMoreStatus: _ctx.loadingStatus
                                        }, void 0, true) : (vue.openBlock(), vue.createBlock(_component_z_paging_load_more, {
                                          key: 1,
                                          onDoClick: _cache[2] || (_cache[2] = ($event) => _ctx._onLoadingMore("click")),
                                          zConfig: _ctx.zLoadMoreConfig
                                        }, null, 8, ["zConfig"]))
                                      ],
                                      64
                                      /* STABLE_FRAGMENT */
                                    ))
                                  ],
                                  4
                                  /* STYLE */
                                )) : vue.createCommentVNode("v-if", true),
                                _ctx.useVirtualList ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 3,
                                    class: "zp-virtual-placeholder",
                                    style: vue.normalizeStyle([{ height: _ctx.virtualPlaceholderBottomHeight + "px" }])
                                  },
                                  null,
                                  4
                                  /* STYLE */
                                )) : vue.createCommentVNode("v-if", true),
                                _ctx.showLoadingMoreDefault ? vue.renderSlot(_ctx.$slots, "loadingMoreDefault", { key: 4 }, void 0, true) : _ctx.showLoadingMoreLoading ? vue.renderSlot(_ctx.$slots, "loadingMoreLoading", { key: 5 }, void 0, true) : _ctx.showLoadingMoreNoMore ? vue.renderSlot(_ctx.$slots, "loadingMoreNoMore", { key: 6 }, void 0, true) : _ctx.showLoadingMoreFail ? vue.renderSlot(_ctx.$slots, "loadingMoreFail", { key: 7 }, void 0, true) : _ctx.showLoadingMoreCustom ? (vue.openBlock(), vue.createBlock(_component_z_paging_load_more, {
                                  key: 8,
                                  onDoClick: _cache[3] || (_cache[3] = ($event) => _ctx._onLoadingMore("click")),
                                  zConfig: _ctx.zLoadMoreConfig
                                }, null, 8, ["zConfig"])) : vue.createCommentVNode("v-if", true),
                                _ctx.safeAreaInsetBottom && _ctx.finalUseSafeAreaPlaceholder && !_ctx.useChatRecordMode ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 9,
                                    class: "zp-safe-area-placeholder",
                                    style: vue.normalizeStyle([{ height: _ctx.safeAreaBottom + "px" }])
                                  },
                                  null,
                                  4
                                  /* STYLE */
                                )) : vue.createCommentVNode("v-if", true)
                              ],
                              4
                              /* STYLE */
                            ),
                            _ctx.showEmpty ? (vue.openBlock(), vue.createElementBlock(
                              "view",
                              {
                                key: 1,
                                class: vue.normalizeClass({ "zp-empty-view": true, "zp-empty-view-center": _ctx.emptyViewCenter }),
                                style: vue.normalizeStyle([_ctx.emptyViewSuperStyle, _ctx.chatRecordRotateStyle])
                              },
                              [
                                _ctx.zSlots.empty ? vue.renderSlot(_ctx.$slots, "empty", {
                                  key: 0,
                                  isLoadFailed: _ctx.isLoadFailed
                                }, void 0, true) : (vue.openBlock(), vue.createBlock(_component_z_paging_empty_view, {
                                  key: 1,
                                  emptyViewImg: _ctx.finalEmptyViewImg,
                                  emptyViewText: _ctx.finalEmptyViewText,
                                  showEmptyViewReload: _ctx.finalShowEmptyViewReload,
                                  emptyViewReloadText: _ctx.finalEmptyViewReloadText,
                                  isLoadFailed: _ctx.isLoadFailed,
                                  emptyViewStyle: _ctx.emptyViewStyle,
                                  emptyViewTitleStyle: _ctx.emptyViewTitleStyle,
                                  emptyViewImgStyle: _ctx.emptyViewImgStyle,
                                  emptyViewReloadStyle: _ctx.emptyViewReloadStyle,
                                  emptyViewZIndex: _ctx.emptyViewZIndex,
                                  emptyViewFixed: _ctx.emptyViewFixed,
                                  unit: _ctx.unit,
                                  onReload: _ctx._emptyViewReload,
                                  onViewClick: _ctx._emptyViewClick
                                }, null, 8, ["emptyViewImg", "emptyViewText", "showEmptyViewReload", "emptyViewReloadText", "isLoadFailed", "emptyViewStyle", "emptyViewTitleStyle", "emptyViewImgStyle", "emptyViewReloadStyle", "emptyViewZIndex", "emptyViewFixed", "unit", "onReload", "onViewClick"]))
                              ],
                              6
                              /* CLASS, STYLE */
                            )) : vue.createCommentVNode("v-if", true)
                          ],
                          4
                          /* STYLE */
                        )
                      ], 12, ["change:prop", "prop", "data-refresherThreshold", "data-refresherF2Enabled", "data-refresherF2Threshold", "data-isIos", "data-loading", "data-useChatRecordMode", "data-refresherEnabled", "data-useCustomRefresher", "data-pageScrollTop", "data-scrollTop", "data-refresherMaxAngle", "data-refresherNoTransform", "data-refresherAecc", "data-usePageScroll", "data-watchTouchDirectionChange", "data-oldIsTouchmoving", "data-refresherOutRate", "data-refresherPullRate", "data-hasTouchmove", "change:renderPropIsIosAndH5", "renderPropIsIosAndH5"])
                    ],
                    32
                    /* NEED_HYDRATION */
                  )
                ], 46, ["scroll-top", "scroll-left", "scroll-x", "scroll-y", "enable-back-to-top", "show-scrollbar", "scroll-with-animation", "scroll-into-view", "lower-threshold", "refresher-enabled", "refresher-threshold", "refresher-default-style", "refresher-background", "refresher-triggered"])
              ],
              6
              /* CLASS, STYLE */
            ),
            _ctx.zSlots.right ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 1,
                class: vue.normalizeClass({ "zp-page-right": true, "zp-absoulte zp-right": _ctx.finalIsOldWebView })
              },
              [
                vue.renderSlot(_ctx.$slots, "right", {}, void 0, true)
              ],
              2
              /* CLASS */
            )) : vue.createCommentVNode("v-if", true)
          ],
          6
          /* CLASS, STYLE */
        ),
        vue.createElementVNode(
          "view",
          {
            class: "zp-page-bottom-container",
            style: vue.normalizeStyle({ "background": _ctx.bottomBgColor })
          },
          [
            _ctx.zSlots.bottom ? (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              { key: 0 },
              [
                !_ctx.usePageScroll ? vue.renderSlot(_ctx.$slots, "bottom", { key: 0 }, void 0, true) : (vue.openBlock(), vue.createElementBlock(
                  "view",
                  {
                    key: 1,
                    class: "zp-page-bottom",
                    onTouchmove: _cache[17] || (_cache[17] = vue.withModifiers(() => {
                    }, ["stop", "prevent"])),
                    style: vue.normalizeStyle([{ "bottom": `${_ctx.windowBottom}px`, "background": _ctx.bottomBgColor }])
                  },
                  [
                    vue.renderSlot(_ctx.$slots, "bottom", {}, void 0, true),
                    _ctx.safeAreaInsetBottom ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        style: vue.normalizeStyle([{ height: _ctx.safeAreaBottom + "px" }])
                      },
                      null,
                      4
                      /* STYLE */
                    )) : vue.createCommentVNode("v-if", true)
                  ],
                  36
                  /* STYLE, NEED_HYDRATION */
                ))
              ],
              64
              /* STABLE_FRAGMENT */
            )) : vue.createCommentVNode("v-if", true),
            _ctx.safeAreaInsetBottom && !_ctx.usePageScroll && !_ctx.finalUseSafeAreaPlaceholder && !_ctx.useChatRecordMode ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 1,
                style: vue.normalizeStyle([{ height: _ctx.safeAreaBottom + "px" }])
              },
              null,
              4
              /* STYLE */
            )) : vue.createCommentVNode("v-if", true),
            _ctx.useChatRecordMode && _ctx.autoAdjustPositionWhenChat ? (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              { key: 2 },
              [
                vue.createElementVNode(
                  "view",
                  {
                    style: vue.normalizeStyle([{ height: _ctx.chatRecordModeSafeAreaBottom + "px" }])
                  },
                  null,
                  4
                  /* STYLE */
                ),
                vue.createElementVNode(
                  "view",
                  {
                    class: "zp-page-bottom-keyboard-placeholder-animate",
                    style: vue.normalizeStyle([{ height: _ctx.keyboardHeight + "px" }])
                  },
                  null,
                  4
                  /* STYLE */
                )
              ],
              64
              /* STABLE_FRAGMENT */
            )) : vue.createCommentVNode("v-if", true)
          ],
          4
          /* STYLE */
        ),
        _ctx.showBackToTopClass ? (vue.openBlock(), vue.createElementBlock(
          "view",
          {
            key: 2,
            class: vue.normalizeClass(_ctx.finalBackToTopClass),
            style: vue.normalizeStyle([_ctx.finalBackToTopStyle]),
            onClick: _cache[18] || (_cache[18] = vue.withModifiers((...args) => _ctx._backToTopClick && _ctx._backToTopClick(...args), ["stop"]))
          },
          [
            _ctx.zSlots.backToTop ? vue.renderSlot(_ctx.$slots, "backToTop", { key: 0 }, void 0, true) : (vue.openBlock(), vue.createElementBlock("image", {
              key: 1,
              class: vue.normalizeClass(["zp-back-to-top-img", { "zp-back-to-top-img-inversion": _ctx.useChatRecordMode && !_ctx.backToTopImg.length }]),
              src: _ctx.backToTopImg.length ? _ctx.backToTopImg : _ctx.base64BackToTop
            }, null, 10, ["src"]))
          ],
          6
          /* CLASS, STYLE */
        )) : vue.createCommentVNode("v-if", true),
        _ctx.showLoading && _ctx.zSlots.loading && _ctx.loadingFullFixed ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "zp-loading-fixed"
        }, [
          vue.renderSlot(_ctx.$slots, "loading", {}, void 0, true)
        ])) : vue.createCommentVNode("v-if", true)
      ],
      6
      /* CLASS, STYLE */
    );
  }
  if (typeof block0$3 === "function")
    block0$3(_sfc_main$1T);
  if (typeof block1$1 === "function")
    block1$1(_sfc_main$1T);
  const __easycom_0$7 = /* @__PURE__ */ _export_sfc(_sfc_main$1T, [["render", _sfc_render$1S], ["__scopeId", "data-v-1aa372d7"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/z-paging/components/z-paging/z-paging.vue"]]);
  const _sfc_main$1S = {
    __name: "index-brand",
    props: ["brand", "key"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      function jumpBrand() {
        uni.navigateTo({
          url: "/pages/brand/brand?brand_id=" + props.brand.id
        });
      }
      function jumpGoods(id) {
        uni.navigateTo({
          url: "/pages/goods/goods?goods_id=" + id
        });
      }
      const __returned__ = { props, jumpBrand, jumpGoods };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1R(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", {
      class: "brand-card",
      key: $props.key
    }, [
      vue.createElementVNode("view", {
        class: "brand-header",
        onClick: $setup.jumpBrand
      }, [
        $props.brand.logo_image ? (vue.openBlock(), vue.createElementBlock("image", {
          key: 0,
          class: "brand-logo",
          src: $props.brand.logo_image,
          mode: "aspectFit"
        }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "brand-logo",
          style: { "background": "linear-gradient(180deg, #d8c1b3 0%, #d3f5ff 100%)", "align-items": "center" }
        }, [
          vue.createElementVNode("text", { style: { "color": "rgb(255, 255, 255)", "font-size": "100rpx", "display": "block", "text-align": "center" } }, "?")
        ])),
        vue.createElementVNode("view", { class: "brand-meta" }, [
          vue.createElementVNode("view", { class: "title-row" }, [
            vue.createElementVNode("image", {
              src: $props.brand.brand_name_image,
              mode: "heightFix",
              style: { "height": "40rpx" }
            }, null, 8, ["src"]),
            vue.createElementVNode(
              "text",
              { class: "brand-tag" },
              vue.toDisplayString($props.brand.country_name) + "·" + vue.toDisplayString($props.brand.type),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "text",
              { class: "goods-count" },
              "收录" + vue.toDisplayString($props.brand.total_goods) + "款",
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode(
            "text",
            { class: "brand-desc" },
            vue.toDisplayString($props.brand.description),
            1
            /* TEXT */
          )
        ])
      ]),
      vue.createElementVNode("scroll-view", {
        class: "goods-scroll",
        "scroll-x": "",
        "show-scrollbar": false
      }, [
        vue.createElementVNode("view", { class: "goods-list" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($props.brand.goods, (doll, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: "goods-item",
                key: doll.id,
                onClick: ($event) => $setup.jumpGoods(doll.id)
              }, [
                vue.createElementVNode("image", {
                  class: "goods-image",
                  src: doll.goods_images && doll.goods_images[0],
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode(
                  "text",
                  { class: "goods-name" },
                  vue.toDisplayString(doll.name),
                  1
                  /* TEXT */
                )
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ])
    ]);
  }
  const indexBrand = /* @__PURE__ */ _export_sfc(_sfc_main$1S, [["render", _sfc_render$1R], ["__scopeId", "data-v-aa9d9d58"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/index-brand/index-brand.vue"]]);
  const _sfc_main$1R = {
    name: "BottomPopup",
    props: {
      show: {
        type: Boolean,
        default: false
      }
    },
    watch: {
      show(newVal) {
        if (typeof uni === "undefined")
          return;
        uni.getSystemInfoSync().platform;
        if (newVal) {
          uni.hideTabBar();
        } else {
          uni.showTabBar();
        }
      }
    },
    methods: {
      closePopup() {
        this.$emit("close");
      },
      moveHandle() {
        return false;
      }
    }
  };
  function _sfc_render$1Q(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      $props.show ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 0,
          class: vue.normalizeClass(["mask", { "mask-show": $props.show }]),
          onClick: _cache[0] || (_cache[0] = (...args) => $options.closePopup && $options.closePopup(...args)),
          onTouchmove: _cache[1] || (_cache[1] = vue.withModifiers((...args) => $options.moveHandle && $options.moveHandle(...args), ["stop", "prevent"]))
        },
        null,
        34
        /* CLASS, NEED_HYDRATION */
      )) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["popup-container", { "popup-show": $props.show }]),
          onTouchmove: _cache[2] || (_cache[2] = vue.withModifiers((...args) => $options.moveHandle && $options.moveHandle(...args), ["stop", "prevent"]))
        },
        [
          vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
        ],
        34
        /* CLASS, NEED_HYDRATION */
      )
    ]);
  }
  const __easycom_4$3 = /* @__PURE__ */ _export_sfc(_sfc_main$1R, [["render", _sfc_render$1Q], ["__scopeId", "data-v-f4186061"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/bottom-popup/bottom-popup.vue"]]);
  const _imports_0$i = "/static/empty-artist.png";
  const _sfc_main$1Q = {
    __name: "index-artist",
    props: {
      list: {
        type: Array,
        default: () => []
      },
      loading: {
        type: Boolean,
        default: false
      }
    },
    emits: ["filter-change"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const roleType = vue.ref("artist");
      const statusFilters = vue.ref([
        { label: "全部", value: 0 },
        { label: "长期接单", value: 1 },
        { label: "限时手速", value: 2 },
        { label: "限时抽选", value: 3 },
        { label: "黑箱卡", value: 4 },
        { label: "关闭接单", value: 9 }
      ]);
      const activeStatus = vue.ref(0);
      const minPrice = vue.ref("");
      const maxPrice = vue.ref("");
      const accept2D = vue.ref(false);
      const accept3D = vue.ref(false);
      const helpModalVisible = vue.ref(false);
      const filterPopupVisible = vue.ref(false);
      const filteredArtists = vue.computed(() => props.list || []);
      const emitFilterChange = () => {
        emit("filter-change", {
          roleType: roleType.value,
          status: activeStatus.value,
          minPrice: minPrice.value,
          maxPrice: maxPrice.value,
          accept2D: accept2D.value,
          accept3D: accept3D.value
        });
      };
      vue.onMounted(() => {
        emitFilterChange();
      });
      const switchRole = (role) => {
        if (roleType.value === role)
          return;
        roleType.value = role;
        emitFilterChange();
      };
      const showFilterPopup = () => {
        filterPopupVisible.value = true;
      };
      const showHelpModal = () => {
        helpModalVisible.value = true;
      };
      const applyFilter = () => {
        filterPopupVisible.value = false;
        emitFilterChange();
      };
      const resetFilters = () => {
        activeStatus.value = 0;
        minPrice.value = "";
        maxPrice.value = "";
        accept2D.value = false;
        accept3D.value = false;
        emitFilterChange();
      };
      const navigateToArtistDetail = (artist) => {
        if (roleType.value === "artist") {
          uni.navigateTo({
            url: "/pages/artist_info/bjd_faceup_artist?brand_id=" + artist.brand_id
          });
        } else {
          uni.navigateTo({
            url: "/pages/artist_info/artist_info?brand_id=" + artist.brand_id
          });
        }
      };
      const statusClass = (status) => {
        switch (status) {
          case 1:
            return "status-long-term";
          case 2:
            return "status-speed";
          case 3:
            return "status-lottery";
          case 4:
            return "status-black-card";
          case 9:
            return "status-closed";
          default:
            return "";
        }
      };
      const __returned__ = { props, emit, roleType, statusFilters, activeStatus, minPrice, maxPrice, accept2D, accept3D, helpModalVisible, filterPopupVisible, filteredArtists, emitFilterChange, switchRole, showFilterPopup, showHelpModal, applyFilter, resetFilters, navigateToArtistDetail, statusClass, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1P(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "artist-list-container" }, [
      vue.createElementVNode("view", { class: "top-actions" }, [
        vue.createElementVNode("view", { class: "role-toggle" }, [
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["role-item font-title", { active: $setup.roleType === "artist" }]),
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchRole("artist"))
            },
            "妆师",
            2
            /* CLASS */
          ),
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["role-item font-title", { active: $setup.roleType === "hairstylist" }]),
              onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchRole("hairstylist"))
            },
            "毛娘",
            2
            /* CLASS */
          )
        ]),
        vue.createElementVNode("view", { class: "actions-right" }, [
          vue.createElementVNode("view", {
            class: "filter-btn",
            onClick: $setup.showFilterPopup
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "tune",
              size: "18",
              color: "#fff"
            }),
            vue.createElementVNode("text", {
              style: { "color": "#fff", "font-size": "22rpx" },
              class: "font-alimamashuhei"
            }, "筛选")
          ]),
          vue.createElementVNode("view", {
            class: "help-btn",
            onClick: $setup.showHelpModal
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "help",
              size: "24",
              color: "#808080"
            })
          ])
        ])
      ]),
      vue.createElementVNode("view", { class: "artist-list" }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.filteredArtists, (artist, index) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: index,
              class: "artist-card",
              onClick: ($event) => $setup.navigateToArtistDetail(artist)
            }, [
              vue.createElementVNode("view", { class: "artist-info" }, [
                vue.createElementVNode("image", {
                  src: artist.logo_image,
                  class: "artist-avatar",
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode("view", { class: "artist-details" }, [
                  vue.createElementVNode(
                    "view",
                    { class: "artist-name" },
                    vue.toDisplayString(artist.brand_name),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("view", { class: "artist-status" }, [
                    vue.createElementVNode(
                      "text",
                      {
                        class: vue.normalizeClass(["status-badge", $setup.statusClass(artist.status)])
                      },
                      vue.toDisplayString(artist.status_text),
                      3
                      /* TEXT, CLASS */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "artist-price" }, [
                    vue.createElementVNode("text", { class: "price-label" }, "基础价格:"),
                    vue.createElementVNode(
                      "text",
                      { class: "price-value" },
                      "¥" + vue.toDisplayString(artist.base_price),
                      1
                      /* TEXT */
                    )
                  ]),
                  artist.description ? (vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: 0,
                      class: "artist-desc"
                    },
                    vue.toDisplayString(artist.description),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ]),
              artist.highlight_img && artist.highlight_img.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "highlight-works"
              }, [
                vue.createElementVNode("scroll-view", {
                  "scroll-x": "",
                  class: "works-scroll"
                }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList(artist.highlight_img, (img, imgIndex) => {
                      return vue.openBlock(), vue.createElementBlock("view", {
                        key: imgIndex,
                        class: "work-item"
                      }, [
                        vue.createElementVNode("image", {
                          src: img,
                          class: "work-image",
                          mode: "aspectFill"
                        }, null, 8, ["src"])
                      ]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])
              ])) : (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "no-works"
              }, " 暂无作品展示 "))
            ], 8, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      $setup.filteredArtists.length === 0 && !$props.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "empty-state"
      }, [
        vue.createElementVNode("image", {
          src: _imports_0$i,
          class: "empty-image"
        }),
        vue.createElementVNode(
          "text",
          { class: "empty-text" },
          "未找到符合条件的" + vue.toDisplayString($setup.roleType === "artist" ? "妆师" : "毛娘"),
          1
          /* TEXT */
        ),
        vue.createElementVNode("button", {
          class: "reset-btn",
          onClick: $setup.resetFilters
        }, "重置筛选")
      ])) : vue.createCommentVNode("v-if", true),
      vue.createVNode(_component_common_modal, {
        visible: $setup.helpModalVisible,
        "onUpdate:visible": _cache[3] || (_cache[3] = ($event) => $setup.helpModalVisible = $event),
        top: "15vh"
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("scroll-view", {
            "scroll-y": "true",
            class: "help-modal-content"
          }, [
            vue.createElementVNode("view", { class: "modal-title" }, "妆师/毛娘信息说明"),
            vue.createElementVNode("view", { class: "modal-text" }, [
              vue.createElementVNode("text", { class: "bold" }, "基础妆费说明："),
              vue.createElementVNode("text", null, "基础妆费是妆师妆则中费用最低的一个选项，例如：自由妆200、半指定300、全指定400，选取自由妆费作为展示。")
            ]),
            vue.createElementVNode("view", { class: "modal-text" }, [
              vue.createElementVNode("text", { class: "bold" }, "价格说明："),
              vue.createElementVNode("text", null, "受限于数据收集困难等原因，妆师妆费价格与实际价格可能存在差距，如有错误请在妆师主页留言订正，我们会很快核对更新。")
            ]),
            vue.createElementVNode("view", { class: "modal-text" }, [
              vue.createElementVNode("text", { class: "bold" }, "交易说明："),
              vue.createElementVNode("text", null, "当前软件版本暂不支持在线交易，交易详情、责任与规则以联系本人为准。")
            ]),
            vue.createElementVNode("view", { class: "modal-text" }, [
              vue.createElementVNode("text", { class: "bold" }, "接管词条："),
              vue.createElementVNode("text", null, "在【我的-作者入驻】中申请")
            ]),
            vue.createElementVNode("view", {
              class: "modal-close",
              onClick: _cache[2] || (_cache[2] = ($event) => $setup.helpModalVisible = false)
            }, "我知道了")
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["visible"]),
      vue.createVNode(_component_bottom_popup, {
        show: $setup.filterPopupVisible,
        onClose: _cache[9] || (_cache[9] = ($event) => $setup.filterPopupVisible = false)
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "filter-popup-content" }, [
            vue.createElementVNode("view", { class: "popup-header" }, [
              vue.createElementVNode("text", { class: "popup-title" }, "筛选条件"),
              vue.createVNode(_component_uni_icons, {
                type: "closeempty",
                size: "24",
                color: "#666",
                onClick: _cache[4] || (_cache[4] = ($event) => $setup.filterPopupVisible = false)
              })
            ]),
            vue.createElementVNode("view", { class: "filter-section" }, [
              vue.createElementVNode("view", { class: "section-title" }, "接单状态"),
              vue.createElementVNode("view", { class: "status-buttons" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.statusFilters, (status, index) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: index,
                      class: vue.normalizeClass(["status-btn", { active: $setup.activeStatus === status.value }]),
                      onClick: ($event) => $setup.activeStatus = status.value
                    }, vue.toDisplayString(status.label), 11, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ]),
            vue.createElementVNode("view", { class: "filter-section" }, [
              vue.createElementVNode("view", { class: "section-title" }, "价格区间"),
              vue.createElementVNode("view", { class: "price-inputs" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.minPrice = $event),
                    type: "number",
                    placeholder: "最低价",
                    class: "price-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.minPrice]
                ]),
                vue.createElementVNode("text", { class: "price-separator" }, "-"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[6] || (_cache[6] = ($event) => $setup.maxPrice = $event),
                    type: "number",
                    placeholder: "最高价",
                    class: "price-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.maxPrice]
                ])
              ])
            ]),
            $setup.roleType === "artist" ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "filter-section"
            }, [
              vue.createElementVNode("view", { class: "section-title" }, "可接妆面类型"),
              vue.createElementVNode("view", { class: "switch-row" }, [
                vue.createElementVNode("text", { class: "switch-label" }, "显示可接二次元妆面"),
                vue.createElementVNode("switch", {
                  checked: $setup.accept2D,
                  onChange: _cache[7] || (_cache[7] = ($event) => $setup.accept2D = $event.detail.value),
                  color: "#65C3D6"
                }, null, 40, ["checked"])
              ]),
              vue.createElementVNode("view", { class: "switch-row" }, [
                vue.createElementVNode("text", { class: "switch-label" }, "显示可接三次元妆面"),
                vue.createElementVNode("switch", {
                  checked: $setup.accept3D,
                  onChange: _cache[8] || (_cache[8] = ($event) => $setup.accept3D = $event.detail.value),
                  color: "#65C3D6"
                }, null, 40, ["checked"])
              ]),
              vue.createElementVNode("view", { class: "switch-tips" }, "提示：开启后只显示“支持该类型”的妆师；关闭则不限定该条件。")
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "popup-actions" }, [
              vue.createElementVNode("button", {
                class: "popup-btn reset",
                onClick: $setup.resetFilters
              }, "重置"),
              vue.createElementVNode("button", {
                class: "popup-btn apply",
                onClick: $setup.applyFilter
              }, "应用")
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["show"])
    ]);
  }
  const indexArtist = /* @__PURE__ */ _export_sfc(_sfc_main$1Q, [["render", _sfc_render$1P], ["__scopeId", "data-v-ced2d0e2"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/index-artist/index-artist.vue"]]);
  const _imports_0$h = "/static/new-icon/title.gif";
  const _imports_1$b = "/static/new-icon/telphone.gif";
  const _imports_2$6 = "/static/new-icon/read.gif";
  const _imports_3$3 = "/static/new-icon/collocation.gif";
  const _imports_5 = "/static/default-banner.jpg";
  const _imports_6 = "/static/noname.png";
  const _sfc_main$1P = {
    __name: "index",
    setup(__props, { expose: __expose }) {
      __expose();
      vue.useCssVars((_ctx) => ({
        "1cf27b2a-headerHeight": headerHeight.value
      }));
      const systemInfo2 = uni.getSystemInfoSync();
      const windowHeight = systemInfo2.windowHeight || 0;
      const navBarHeight = vue.computed(() => {
        return 0;
      });
      const headerHeight = vue.computed(() => {
        const statusBarHeight = systemInfo2.statusBarHeight || 0;
        return `${statusBarHeight}px`;
      });
      const showLoadding = vue.ref(false);
      const activeTab = vue.ref("news");
      const showBackTop = vue.ref(false);
      const paging = vue.ref(null);
      const newsList = vue.ref([]);
      const brandsList = vue.ref([]);
      const hotList = vue.ref([]);
      const treeholeList = vue.ref([]);
      const hotLoading = vue.ref(false);
      const artistFilter = vue.ref({
        roleType: "artist",
        status: 0,
        minPrice: "",
        maxPrice: "",
        accept2D: false,
        accept3D: false
      });
      const bannerList = vue.ref([]);
      const tabs = vue.ref([
        { label: "中国娃社", value: 1 },
        { label: "个人作者", value: 2 },
        { label: "外国娃社", value: 3 }
      ]);
      const activeSearchType = vue.ref(1);
      const pagingList = vue.computed({
        get() {
          switch (activeTab.value) {
            case "news":
              return newsList.value;
            case "brands":
              return brandsList.value;
            case "hot":
              return hotList.value;
            case "second":
              return treeholeList.value;
            default:
              return newsList.value;
          }
        },
        set(val) {
          switch (activeTab.value) {
            case "news":
              newsList.value = val;
              break;
            case "brands":
              brandsList.value = val;
              break;
            case "hot":
              hotList.value = val;
              break;
            case "second":
              treeholeList.value = val;
              break;
          }
        }
      });
      const switchTab = (tab) => {
        if (activeTab.value === tab)
          return;
        activeTab.value = tab;
        showBackTop.value = false;
        formatAppLog("log", "at pages/index/index.vue:486", "[switchTab]", tab);
        paging.value && paging.value.reload();
      };
      const queryPaging = (pageNo, pageSize2) => {
        formatAppLog("log", "at pages/index/index.vue:492", "[queryPaging]", "tab=", activeTab.value, "pageNo=", pageNo, "pageSize=", pageSize2);
        if (activeTab.value === "news") {
          fetchNews(pageNo, pageSize2);
        } else if (activeTab.value === "brands") {
          fetchBrands(pageNo, pageSize2);
        } else if (activeTab.value === "second") {
          fetchTreehole(pageNo, pageSize2);
        } else if (activeTab.value === "hot") {
          fetchArtists(pageNo, pageSize2);
        }
      };
      const handlePagingScroll = (event) => {
        const detail = (event == null ? void 0 : event.detail) || {};
        const scrollTop = typeof detail.scrollTop === "number" ? detail.scrollTop : 0;
        showBackTop.value = scrollTop > windowHeight;
      };
      const handleBackTop = () => {
        if (paging.value && typeof paging.value.scrollToTop === "function") {
          paging.value.scrollToTop(true);
        } else {
          uni.pageScrollTo({
            scrollTop: 0,
            duration: 300
          });
        }
        showBackTop.value = false;
      };
      const formatTime = (timestamp) => {
        const date = new Date((timestamp || 0) * 1e3);
        const y2 = date.getFullYear();
        const m2 = String(date.getMonth() + 1).padStart(2, "0");
        const d2 = String(date.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${d2}`;
      };
      const goSearch = () => {
        uni.navigateTo({
          url: "/pages/search/search?tabs=1,2,3,4&mode=jump"
        });
      };
      const getArticles = () => {
        uni.request({
          url: websiteUrl.value + "/articles",
          data: {
            page: 1,
            page_size: 10,
            status: 1
          },
          success: (res) => {
            if (res.data.status === "success") {
              bannerList.value = res.data.data.list || [];
            } else {
              bannerList.value = [];
            }
          },
          fail: () => {
            bannerList.value = [];
          }
        });
      };
      const handleBannerClick = (item) => {
        uni.navigateTo({
          url: `/pages/article_detail/article_detail?id=${item.id}`
        });
      };
      const jump2saleNews = (item) => {
        uni.navigateTo({
          url: "/pages/sale_news/sale_news?id=" + item.id + "&brand_id=" + item.brand_id
        });
      };
      const fetchNews = (pageNo, pageSize2) => {
        showLoadding.value = true;
        uni.request({
          url: websiteUrl.value + "/sale-news",
          data: {
            page: pageNo,
            pageSize: pageSize2
          },
          success: (res) => {
            const body = res.data || {};
            const data = body.data || {};
            const listRaw = data.news_list || [];
            const list = listRaw.map((item) => {
              const imgs = item.image_list || [];
              return {
                ...item,
                image_list: imgs,
                imagesLoaded: imgs.length === 0,
                // 没有图片直接认为加载完成
                loadCount: 0
              };
            });
            const total = data.total;
            formatAppLog("log", "at pages/index/index.vue:601", "total:", total);
            paging.value && paging.value.complete(list, total);
            setTimeout(() => {
              newsList.value.forEach((it2) => {
                if (!it2.imagesLoaded)
                  it2.imagesLoaded = true;
              });
            }, 3e3);
          },
          fail: (err) => {
            formatAppLog("error", "at pages/index/index.vue:612", err);
            uni.showToast({
              title: "加载失败",
              icon: "none"
            });
            paging.value && paging.value.complete(false);
          },
          complete: () => {
            showLoadding.value = false;
          }
        });
      };
      const handleNewsImageLoad = (item) => {
        if (!item.image_list || item.image_list.length === 0) {
          item.imagesLoaded = true;
          return;
        }
        item.loadCount = (item.loadCount || 0) + 1;
        if (item.loadCount >= item.image_list.length) {
          item.imagesLoaded = true;
        }
      };
      const handleNewsImageError = (item, idx) => {
        formatAppLog(
          "error",
          "at pages/index/index.vue:637",
          "新闻图片加载失败:",
          item.image_list && item.image_list[idx]
        );
        item.loadCount = (item.loadCount || 0) + 1;
        if (!item.image_list || item.loadCount >= item.image_list.length) {
          item.imagesLoaded = true;
        }
      };
      const cursor = vue.ref("");
      const handleTabClick = (value) => {
        if (activeSearchType.value === value) {
          activeSearchType.value = null;
        } else {
          activeSearchType.value = value;
        }
        cursor.value = "";
        if (activeTab.value === "brands") {
          paging.value && paging.value.reload();
        }
      };
      const fetchBrands = (pageNo, pageSize2) => {
        showLoadding.value = true;
        const params = { pageSize: pageSize2 };
        if (cursor.value) {
          params.cursor = cursor.value;
        } else if (activeSearchType.value) {
          params.searchType = activeSearchType.value;
        }
        uni.request({
          url: websiteUrl.value + "/brands-info-list",
          data: params,
          success: (res) => {
            if (res.data.status !== "success") {
              uni.showToast({
                title: "加载失败",
                icon: "none"
              });
              paging.value && paging.value.complete(false);
              return;
            }
            const respData = res.data.data || {};
            const newData = respData.data || [];
            const hasMore = !!respData.has_more;
            cursor.value = respData.next_cursor || "";
            const list = newData;
            const total = pageNo * pageSize2 + (hasMore ? 1 : 0);
            paging.value && paging.value.complete(list, total);
          },
          fail: (err) => {
            formatAppLog("error", "at pages/index/index.vue:695", err);
            uni.showToast({
              title: "网络错误",
              icon: "none"
            });
            paging.value && paging.value.complete(false);
          },
          complete: () => {
            showLoadding.value = false;
          }
        });
      };
      const handleArtistFilterChange = (payload) => {
        artistFilter.value = payload;
        formatAppLog("log", "at pages/index/index.vue:712", "[handleArtistFilterChange]", payload);
        if (activeTab.value === "hot") {
          paging.value && paging.value.reload();
        }
      };
      const fetchArtists = (pageNo, pageSize2) => {
        hotLoading.value = true;
        const filter = artistFilter.value || {};
        const params = {
          page: pageNo,
          size: pageSize2,
          artist_type: filter.roleType === "hairstylist" ? 2 : 1
          // 1=妆师, 2=毛娘
        };
        if (filter.status && filter.status !== 0) {
          params.status = filter.status;
        }
        if (filter.minPrice)
          params.min_price = filter.minPrice;
        if (filter.maxPrice)
          params.max_price = filter.maxPrice;
        if (filter.roleType === "artist") {
          if (filter.accept2D)
            params.accept_2d = 1;
          if (filter.accept3D)
            params.accept_3d = 1;
        }
        uni.request({
          url: `${websiteUrl.value}/brand-artist-list`,
          method: "GET",
          data: params,
          success: (res) => {
            var _a, _b;
            const data = res.data || {};
            if (data.status === "success") {
              const list = ((_a = data.data) == null ? void 0 : _a.list) || [];
              const total = ((_b = data.data) == null ? void 0 : _b.total) || 0;
              paging.value && paging.value.complete(list, total);
            } else {
              uni.showToast({ title: data.msg || "获取数据失败", icon: "none" });
              paging.value && paging.value.complete(false);
            }
          },
          fail: (e2) => {
            formatAppLog("error", "at pages/index/index.vue:757", "获取妆师毛娘列表失败", e2);
            uni.showToast({ title: "网络请求失败", icon: "none" });
            paging.value && paging.value.complete(false);
          },
          complete: () => {
            hotLoading.value = false;
          }
        });
      };
      const fetchTreehole = (pageNo, pageSize2) => {
        showLoadding.value = true;
        uni.request({
          url: websiteUrl.value + "/treehole-submissions",
          data: {
            page: pageNo,
            pageSize: pageSize2
          },
          success: (res) => {
            var _a;
            const body = res.data || {};
            const raw = ((_a = body.data) == null ? void 0 : _a.submission_list) || [];
            const list = raw.map((item) => ({
              ...item,
              images: item.images || [],
              imagesLoaded: (item.images || []).length === 0,
              loadCount: 0
            }));
            const total = pageNo * pageSize2 + (list.length < pageSize2 ? 0 : 1);
            paging.value && paging.value.complete(list, total);
            setTimeout(() => {
              treeholeList.value.forEach((it2) => {
                if (!it2.imagesLoaded)
                  it2.imagesLoaded = true;
              });
            }, 3e3);
          },
          fail: (err) => {
            formatAppLog("error", "at pages/index/index.vue:797", err);
            uni.showToast({
              title: "加载失败",
              icon: "none"
            });
            paging.value && paging.value.complete(false);
          },
          complete: () => {
            showLoadding.value = false;
          }
        });
      };
      const jump2treeholeDetail = (item) => {
        uni.navigateTo({
          url: "/pages/treehole_detail/treehole_detail?id=" + item.id
        });
      };
      const handleImageLoad = (item) => {
        if (!item.images || item.images.length === 0) {
          item.imagesLoaded = true;
          return;
        }
        item.loadCount = (item.loadCount || 0) + 1;
        if (item.loadCount >= item.images.length) {
          item.imagesLoaded = true;
        }
      };
      const handleImageError = (item, idx) => {
        formatAppLog("error", "at pages/index/index.vue:828", "树洞图片加载失败", item.images && item.images[idx]);
        item.loadCount = (item.loadCount || 0) + 1;
        if (!item.images || item.loadCount >= item.images.length) {
          item.imagesLoaded = true;
        }
      };
      const previewImage = (images, index) => {
        uni.previewImage({
          current: index,
          urls: images
        });
      };
      const handlePublish = () => {
        if (!global$1.isLogin) {
          uni.showModal({
            title: "提示",
            content: "需要登录后才能发布树洞",
            success: (res) => {
              if (res.confirm) {
                uni.navigateTo({
                  url: "/pages/login/login"
                });
              }
            }
          });
          return;
        }
        uni.navigateTo({
          url: "/pages/treehole_publish/treehole_publish"
        });
      };
      const handleLike = (item) => {
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        uni.request({
          url: websiteUrl.value + (item.has_liked ? "/with-state/unlike" : "/with-state/add-like"),
          method: "POST",
          header: {
            Authorization: token
          },
          data: {
            id: item.id,
            type: 5
          },
          success: (res) => {
            if (res.data.status === "success") {
              item.has_liked = !item.has_liked;
              item.like_count += item.has_liked ? 1 : -1;
            }
          }
        });
      };
      const copyUrl = (item) => {
        const url = "http://m.dogdogdoll.com/#/pages/treehole_detail/treehole_detail?id=" + item.id;
        uni.setClipboardData({
          data: url,
          success() {
            uni.showToast({
              title: "复制链接成功",
              icon: "none"
            });
          }
        });
      };
      const jump2userWhenNotAnonymous = (item) => {
        if (item.is_anonymous === 1)
          return;
        uni.navigateTo({
          url: "/pages/user_page/user_page?uid=" + item.uid
        });
      };
      const handleServerChange = () => {
        formatAppLog("log", "at pages/index/index.vue:923", "[handleServerChange] reload current tab");
        paging.value && paging.value.reload();
      };
      onLoad(() => {
        getUserInfo();
        getArticles();
      });
      onReady(() => {
        formatAppLog("log", "at pages/index/index.vue:935", "[onReady] paging reload");
        paging.value && paging.value.reload();
      });
      const __returned__ = { systemInfo: systemInfo2, windowHeight, navBarHeight, headerHeight, showLoadding, activeTab, showBackTop, paging, newsList, brandsList, hotList, treeholeList, hotLoading, artistFilter, bannerList, tabs, activeSearchType, pagingList, switchTab, queryPaging, handlePagingScroll, handleBackTop, formatTime, goSearch, getArticles, handleBannerClick, jump2saleNews, fetchNews, handleNewsImageLoad, handleNewsImageError, cursor, handleTabClick, fetchBrands, handleArtistFilterChange, fetchArtists, fetchTreehole, jump2treeholeDetail, handleImageLoad, handleImageError, previewImage, handlePublish, handleLike, copyUrl, jump2userWhenNotAnonymous, handleServerChange, get onLoad() {
        return onLoad;
      }, get onReady() {
        return onReady;
      }, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, indexBrand, indexArtist };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1O(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_privacy_permission_modal = resolveEasycom(vue.resolveDynamicComponent("privacy-permission-modal"), __easycom_1$e);
    const _component_loading_toast = resolveEasycom(vue.resolveDynamicComponent("loading-toast"), __easycom_3$8);
    const _component_server_selector = resolveEasycom(vue.resolveDynamicComponent("server-selector"), __easycom_3$7);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_z_paging = resolveEasycom(vue.resolveDynamicComponent("z-paging"), __easycom_0$7);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("meta", {
          name: "theme-color",
          content: "#def9ff"
        }),
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", null, [
          vue.createVNode(_component_privacy_permission_modal),
          vue.createVNode(_component_loading_toast, { show: $setup.showLoadding }, null, 8, ["show"]),
          vue.createVNode(_component_view_logs),
          vue.createElementVNode("view", { class: "body" }, [
            vue.createVNode(_component_z_paging, {
              ref: "paging",
              modelValue: $setup.pagingList,
              "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.pagingList = $event),
              "refresher-enabled": true,
              auto: false,
              "loading-more-enabled": true,
              "to-bottom-loading-more-enabled": true,
              "lower-threshold": 200,
              "inside-more": true,
              onQuery: $setup.queryPaging,
              onScroll: $setup.handlePagingScroll
            }, {
              default: vue.withCtx(() => [
                vue.createElementVNode("view", { class: "index_header" }, [
                  vue.createElementVNode("view", { class: "header-placeholders" }),
                  vue.createElementVNode("view", null, [
                    vue.createElementVNode("view", { style: { "display": "inline-block", "margin-left": "20rpx" } }, [
                      vue.createElementVNode("image", {
                        src: _imports_0$h,
                        mode: "widthFix",
                        style: { "width": "300rpx", "max-height": "150rpx", "position": "relative", "left": "-20rpx", "margin-bottom": "10rpx" }
                      }),
                      vue.createVNode(_component_server_selector, { onServerChange: $setup.handleServerChange })
                    ]),
                    vue.createElementVNode("view", {
                      class: "fake-search",
                      onClick: $setup.goSearch,
                      "hover-class": "fake-search--hover"
                    }, [
                      vue.createVNode(_component_uni_icons, {
                        type: "search",
                        size: "18",
                        color: "#9aa0a6"
                      }),
                      vue.createElementVNode("text", { class: "fake-search__placeholder" }, "搜索娃物 / 店铺 / 妆师 / 毛娘")
                    ])
                  ]),
                  vue.createElementVNode("view", { style: { "margin": "20rpx 0rpx 0rpx 0rpx", "padding": "5px 10px 0px 10px", "border-radius": "20px 20px 0 0" } }, [
                    vue.createElementVNode("view", { class: "index_page_article" }, [
                      vue.createElementVNode(
                        "view",
                        {
                          class: vue.normalizeClass(["swich_box", { active: $setup.activeTab === "news" }]),
                          onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchTab("news"))
                        },
                        [
                          vue.createElementVNode("image", {
                            src: _imports_1$b,
                            mode: "aspectFill"
                          }),
                          vue.createElementVNode("text", { class: "font-alimamashuhei" }, "新情报！")
                        ],
                        2
                        /* CLASS */
                      ),
                      vue.createElementVNode(
                        "view",
                        {
                          class: vue.normalizeClass(["swich_box", { active: $setup.activeTab === "brands" }]),
                          onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchTab("brands"))
                        },
                        [
                          vue.createElementVNode("image", {
                            src: _imports_2$6,
                            mode: "aspectFill"
                          }),
                          vue.createElementVNode("text", { class: "font-alimamashuhei" }, "娃物图鉴")
                        ],
                        2
                        /* CLASS */
                      ),
                      vue.createElementVNode(
                        "view",
                        {
                          class: vue.normalizeClass(["swich_box", { active: $setup.activeTab === "hot" }]),
                          onClick: _cache[2] || (_cache[2] = ($event) => $setup.switchTab("hot"))
                        },
                        [
                          vue.createElementVNode("image", {
                            src: _imports_3$3,
                            mode: "aspectFill",
                            style: { "width": "90rpx", "height": "90rpx", "position": "relative", "bottom": "0rpx" }
                          }),
                          vue.createElementVNode("text", {
                            class: "font-alimamashuhei",
                            style: { "position": "relative", "bottom": "5rpx" }
                          }, "妆师毛娘")
                        ],
                        2
                        /* CLASS */
                      ),
                      vue.createElementVNode(
                        "view",
                        {
                          class: vue.normalizeClass(["swich_box", { active: $setup.activeTab === "second" }]),
                          onClick: _cache[3] || (_cache[3] = ($event) => $setup.switchTab("second"))
                        },
                        [
                          vue.createElementVNode("image", {
                            src: _imports_4$1,
                            mode: "aspectFill"
                          }),
                          vue.createElementVNode("text", { class: "font-alimamashuhei" }, "匿名版")
                        ],
                        2
                        /* CLASS */
                      )
                    ])
                  ])
                ]),
                vue.createElementVNode("view", { class: "body_container" }, [
                  vue.createElementVNode("view", { style: { "height": "40rpx" } }),
                  vue.createElementVNode("view", { class: "body_list fade-list" }, [
                    $setup.activeTab === "news" ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "news-box"
                    }, [
                      vue.createElementVNode("view", { style: { "position": "relative", "height": "250rpx", "margin-bottom": "20rpx", "display": "none" } }, [
                        vue.createElementVNode("swiper", {
                          interval: 3e3,
                          duration: 200,
                          class: "banner_swiper",
                          "indicator-active-color": "#4cbbd0",
                          "indicator-color": "rgba(255,255,255,0.5)"
                        }, [
                          (vue.openBlock(true), vue.createElementBlock(
                            vue.Fragment,
                            null,
                            vue.renderList($setup.bannerList, (item) => {
                              return vue.openBlock(), vue.createElementBlock("swiper-item", {
                                key: item.id,
                                class: "banner_swiper_item"
                              }, [
                                vue.createElementVNode("view", { class: "swiper-item" }, [
                                  vue.createElementVNode("image", {
                                    src: item.banner,
                                    mode: "aspectFill",
                                    style: { "width": "100%", "height": "250rpx" },
                                    onClick: ($event) => $setup.handleBannerClick(item)
                                  }, null, 8, ["src", "onClick"])
                                ])
                              ]);
                            }),
                            128
                            /* KEYED_FRAGMENT */
                          )),
                          $setup.bannerList.length === 0 ? (vue.openBlock(), vue.createElementBlock("swiper-item", { key: 0 }, [
                            vue.createElementVNode("view", { class: "empty-banner" }, [
                              vue.createElementVNode("image", {
                                src: _imports_5,
                                mode: "aspectFill",
                                style: { "width": "100%", "height": "250rpx" }
                              })
                            ])
                          ])) : vue.createCommentVNode("v-if", true)
                        ])
                      ]),
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.newsList, (item) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: item.id,
                            class: "news-item",
                            onClick: ($event) => $setup.jump2saleNews(item)
                          }, [
                            vue.createElementVNode("view", { class: "news-images" }, [
                              item.image_list && item.image_list.length > 0 ? (vue.openBlock(), vue.createElementBlock("swiper", {
                                key: 0,
                                class: "image-swiper",
                                autoplay: item.imagesLoaded,
                                circular: true
                              }, [
                                (vue.openBlock(true), vue.createElementBlock(
                                  vue.Fragment,
                                  null,
                                  vue.renderList(item.image_list, (img, idx) => {
                                    return vue.openBlock(), vue.createElementBlock("swiper-item", { key: idx }, [
                                      vue.createElementVNode("view", { class: "image-container" }, [
                                        vue.createElementVNode("image", {
                                          src: img,
                                          mode: "aspectFill",
                                          class: vue.normalizeClass(["swiper-image", { loaded: item.imagesLoaded }]),
                                          onLoad: ($event) => $setup.handleNewsImageLoad(item),
                                          onError: ($event) => $setup.handleNewsImageError(item, idx)
                                        }, null, 42, ["src", "onLoad", "onError"]),
                                        !item.imagesLoaded ? (vue.openBlock(), vue.createElementBlock("view", {
                                          key: 0,
                                          class: "loading-overlay"
                                        }, [
                                          vue.createVNode(_component_uni_icons, {
                                            type: "loop",
                                            size: "28",
                                            color: "rgb(185 185 185)",
                                            class: "loading-icon"
                                          })
                                        ])) : vue.createCommentVNode("v-if", true)
                                      ])
                                    ]);
                                  }),
                                  128
                                  /* KEYED_FRAGMENT */
                                ))
                              ], 8, ["autoplay"])) : vue.createCommentVNode("v-if", true)
                            ]),
                            vue.createElementVNode("view", { class: "news-content" }, [
                              vue.createElementVNode(
                                "text",
                                { class: "news-title" },
                                vue.toDisplayString(item.title),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "news-desc" },
                                vue.toDisplayString(item.content),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode("view", { class: "news-footer" }, [
                                vue.createElementVNode(
                                  "text",
                                  { class: "brand-tag" },
                                  vue.toDisplayString(item.brand_name),
                                  1
                                  /* TEXT */
                                ),
                                vue.createElementVNode(
                                  "text",
                                  { class: "news-time" },
                                  vue.toDisplayString($setup.formatTime(item.created_at)),
                                  1
                                  /* TEXT */
                                )
                              ])
                            ])
                          ], 8, ["onClick"]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : $setup.activeTab === "brands" ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "brand_box"
                    }, [
                      vue.createElementVNode("view", { class: "filter-tabs" }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.tabs, (tab, index) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              key: index,
                              class: vue.normalizeClass(["tab-item", { active: $setup.activeSearchType === tab.value }]),
                              onClick: ($event) => $setup.handleTabClick(tab.value)
                            }, vue.toDisplayString(tab.label), 11, ["onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ]),
                      vue.createElementVNode("view", {
                        class: "brand_type_description",
                        style: { "display": "block" }
                      }, [
                        $setup.activeSearchType === 1 ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, " 中国公司制作的BJD在打磨、分模线等工艺的处理上比较优秀，价格也比外社低很多。 ")) : $setup.activeSearchType === 2 ? (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, " 个人作者在贩售娃物前基本都是圈内玩家，在设计方面花的心思很多。 ")) : $setup.activeSearchType === 3 ? (vue.openBlock(), vue.createElementBlock("text", { key: 2 }, " 国外娃社起步较早，风格设计也比较多样化。 ")) : (vue.openBlock(), vue.createElementBlock("text", { key: 3 }, "全部品牌"))
                      ]),
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.brandsList, (item, index) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: item.id || index
                          }, [
                            vue.createVNode($setup["indexBrand"], { brand: item }, null, 8, ["brand"])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : $setup.activeTab === "hot" ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 2,
                      class: "hot-box"
                    }, [
                      vue.createVNode($setup["indexArtist"], {
                        list: $setup.hotList,
                        loading: $setup.hotLoading,
                        onFilterChange: $setup.handleArtistFilterChange
                      }, null, 8, ["list", "loading"])
                    ])) : $setup.activeTab === "second" ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 3,
                      class: "treehole-box"
                    }, [
                      vue.createElementVNode("view", {
                        class: "publish-btn",
                        onClick: $setup.handlePublish
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "plusempty",
                          size: "30",
                          color: "#fff"
                        })
                      ]),
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.treeholeList, (item) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: item.id,
                            class: "treehole-item",
                            onClick: ($event) => $setup.jump2treeholeDetail(item)
                          }, [
                            vue.createElementVNode("view", {
                              class: "user-info",
                              onClick: vue.withModifiers(($event) => $setup.jump2userWhenNotAnonymous(item), ["stop"])
                            }, [
                              item.avatar !== "" ? (vue.openBlock(), vue.createElementBlock("image", {
                                key: 0,
                                src: item.avatar,
                                class: "avatar",
                                mode: "aspectFill"
                              }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("image", {
                                key: 1,
                                src: _imports_6,
                                class: "avatar",
                                mode: "scaleToFill"
                              })),
                              vue.createElementVNode(
                                "text",
                                { class: "username" },
                                vue.toDisplayString(item.is_anonymous ? "匿名用户" : item.author_name),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "time" },
                                vue.toDisplayString($setup.formatTime(item.created_at)),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode("text", { class: "cid" })
                            ], 8, ["onClick"]),
                            vue.createElementVNode(
                              "text",
                              { class: "content" },
                              vue.toDisplayString(item.content),
                              1
                              /* TEXT */
                            ),
                            item.images && item.images.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                              key: 0,
                              class: "image-grid"
                            }, [
                              vue.createElementVNode("swiper", {
                                class: "image-swiper",
                                autoplay: item.imagesLoaded,
                                circular: true,
                                "indicator-dots": item.images.length > 1,
                                duration: 300
                              }, [
                                (vue.openBlock(true), vue.createElementBlock(
                                  vue.Fragment,
                                  null,
                                  vue.renderList(item.images, (img, idx) => {
                                    return vue.openBlock(), vue.createElementBlock("swiper-item", { key: idx }, [
                                      vue.createElementVNode("view", { class: "image-container" }, [
                                        vue.createElementVNode("image", {
                                          src: img,
                                          mode: "aspectFill",
                                          class: vue.normalizeClass(["swiper-image", { loaded: item.imagesLoaded }]),
                                          onClick: vue.withModifiers(($event) => $setup.previewImage(item.images, idx), ["stop"]),
                                          onLoad: ($event) => $setup.handleImageLoad(item),
                                          onError: ($event) => $setup.handleImageError(item, idx)
                                        }, null, 42, ["src", "onClick", "onLoad", "onError"]),
                                        !item.imagesLoaded ? (vue.openBlock(), vue.createElementBlock("view", {
                                          key: 0,
                                          class: "loading-overlay"
                                        }, [
                                          vue.createVNode(_component_uni_icons, {
                                            type: "spinner-cycle",
                                            size: "28",
                                            color: "#4cbbd0",
                                            class: "loading-icon"
                                          })
                                        ])) : vue.createCommentVNode("v-if", true)
                                      ])
                                    ]);
                                  }),
                                  128
                                  /* KEYED_FRAGMENT */
                                ))
                              ], 8, ["autoplay", "indicator-dots"])
                            ])) : vue.createCommentVNode("v-if", true),
                            vue.createElementVNode("view", { class: "action-bar" }, [
                              vue.createElementVNode("view", {
                                class: "action-item",
                                onClick: ($event) => $setup.handleLike(item)
                              }, [
                                vue.createVNode(_component_uni_icons, {
                                  type: item.has_liked ? "hand-up-filled" : "hand-up",
                                  size: "18",
                                  color: item.has_liked ? "#ff4d4f" : "#666"
                                }, null, 8, ["type", "color"]),
                                vue.createElementVNode(
                                  "text",
                                  { class: "count" },
                                  vue.toDisplayString(item.like_count || 0),
                                  1
                                  /* TEXT */
                                )
                              ], 8, ["onClick"]),
                              vue.createElementVNode("view", { class: "action-item" }, [
                                vue.createVNode(_component_uni_icons, {
                                  type: "chat",
                                  size: "18",
                                  color: "#666"
                                }),
                                vue.createElementVNode(
                                  "text",
                                  { class: "count" },
                                  vue.toDisplayString(item.comment_count || 0),
                                  1
                                  /* TEXT */
                                )
                              ]),
                              vue.createElementVNode("view", {
                                class: "action-item",
                                onClick: vue.withModifiers(($event) => $setup.copyUrl(item), ["stop"])
                              }, [
                                vue.createVNode(_component_uni_icons, {
                                  type: "redo",
                                  size: "18",
                                  color: "#666"
                                }),
                                vue.createElementVNode("text", { class: "count" }, "分享")
                              ], 8, ["onClick"])
                            ])
                          ], 8, ["onClick"]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : vue.createCommentVNode("v-if", true)
                  ])
                ])
              ]),
              _: 1
              /* STABLE */
            }, 8, ["modelValue"]),
            $setup.showBackTop ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "back-top-btn",
              onClick: $setup.handleBackTop
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "arrow-up",
                size: "24",
                color: "#222"
              })
            ])) : vue.createCommentVNode("v-if", true)
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesIndexIndex = /* @__PURE__ */ _export_sfc(_sfc_main$1P, [["render", _sfc_render$1O], ["__scopeId", "data-v-1cf27b2a"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/index/index.vue"]]);
  const _imports_0$g = "/static/search.png";
  const _imports_2$5 = "/static/cancel.png";
  const _sfc_main$1O = /* @__PURE__ */ Object.assign({ inheritAttrs: false }, {
    __name: "common-search",
    props: {
      mode: { type: String, default: "jump", validator: (v2) => ["jump", "fill"].includes(v2) },
      width: { type: String, default: "100%" },
      background: { type: String, default: "" },
      fontSize: { type: String, default: "" },
      hiddenIcon: { type: Boolean, default: false },
      showIndexSelector: { type: Boolean, default: false },
      indexOptions: {
        type: Array,
        default: () => [
          { label: "店铺", value: "" },
          { label: "妆师", value: "bjd_artist" },
          { label: "毛娘", value: "hairstylist" }
        ]
      },
      defaultIndex: { type: String, default: "" },
      hideHintText: { type: Boolean, default: false }
    },
    emits: ["select", "close-associate", "index-change"],
    setup(__props, { expose: __expose, emit: __emit }) {
      var _a;
      __expose();
      const props = __props;
      const emit = __emit;
      const searchTerm = vue.ref("");
      const results = vue.ref([]);
      const currentIndex = vue.ref(props.defaultIndex || (((_a = props.indexOptions[0]) == null ? void 0 : _a.value) || ""));
      const showTip = vue.ref(false);
      let tipTimer = null;
      const suppressSearchOnce = vue.ref(false);
      let inputDebounceTimer = null;
      const triggerTip = () => {
        if (!props.showIndexSelector)
          return;
        clearTimeout(tipTimer);
        showTip.value = true;
        tipTimer = setTimeout(() => showTip.value = false, 2e3);
      };
      vue.watch(() => props.showIndexSelector, (val) => {
        if (val)
          triggerTip();
      });
      vue.onMounted(() => {
        if (props.showIndexSelector)
          triggerTip();
      });
      vue.onUnmounted(() => {
        clearTimeout(tipTimer);
        clearTimeout(inputDebounceTimer);
      });
      const currentIndexLabel = vue.computed(() => {
        const option = props.indexOptions.find((opt) => opt.value === currentIndex.value);
        return option ? option.label : "选择类型";
      });
      const indexLabels = vue.computed(() => props.indexOptions.map((opt) => opt.label));
      const selectedIndex = vue.computed(
        () => props.indexOptions.findIndex((opt) => opt.value === currentIndex.value)
      );
      const closeAssociate = () => {
        results.value = [];
        suppressSearchOnce.value = true;
        emit("close-associate", searchTerm.value);
        emit("select", 0, searchTerm.value);
      };
      const handleIndexChange = (e2) => {
        var _a2;
        const index = e2.detail.value;
        currentIndex.value = ((_a2 = props.indexOptions[index]) == null ? void 0 : _a2.value) || "";
        emit("index-change", currentIndex.value);
        showTip.value = false;
        if (searchTerm.value.trim() !== "" && !suppressSearchOnce.value) {
          onSearchInput();
        } else {
          suppressSearchOnce.value = false;
        }
      };
      const onSearchInput = async () => {
        if (suppressSearchOnce.value) {
          suppressSearchOnce.value = false;
          return;
        }
        const kw = searchTerm.value.trim();
        if (kw === "") {
          results.value = [];
          emit("select", 0, "");
          return;
        }
        emit("select", 0, kw);
        clearTimeout(inputDebounceTimer);
        inputDebounceTimer = setTimeout(async () => {
          var _a2;
          let url = `${websiteUrl.value}/search-brand?search=${encodeURIComponent(kw)}`;
          if (currentIndex.value)
            url += `&index=${currentIndex.value}`;
          try {
            const res = await uni.request({
              url,
              method: "GET",
              header: { "Content-Type": "application/json" }
            });
            if (((_a2 = res.data) == null ? void 0 : _a2.status) == "success") {
              results.value = res.data.data || [];
            } else {
              results.value = [];
            }
          } catch (error2) {
            formatAppLog("error", "at components/common-search/common-search.vue:202", "请求错误:", error2);
            results.value = [];
          }
        }, 150);
      };
      const onTap = (brandId, brandName) => {
        if (props.mode === "jump") {
          uni.navigateTo({ url: `/pages/brand/brand?brand_id=${brandId}` });
        } else if (props.mode === "fill") {
          searchTerm.value = brandName;
          results.value = [];
          emit("select", brandId, brandName);
          suppressSearchOnce.value = true;
        }
      };
      function cancel() {
        searchTerm.value = "";
        results.value = [];
        suppressSearchOnce.value = true;
      }
      const __returned__ = { props, emit, searchTerm, results, currentIndex, showTip, get tipTimer() {
        return tipTimer;
      }, set tipTimer(v2) {
        tipTimer = v2;
      }, suppressSearchOnce, get inputDebounceTimer() {
        return inputDebounceTimer;
      }, set inputDebounceTimer(v2) {
        inputDebounceTimer = v2;
      }, triggerTip, currentIndexLabel, indexLabels, selectedIndex, closeAssociate, handleIndexChange, onSearchInput, onTap, cancel, ref: vue.ref, computed: vue.computed, watch: vue.watch, onMounted: vue.onMounted, onUnmounted: vue.onUnmounted, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  });
  function _sfc_render$1N(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { style: { "width": "100%" } }, [
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["search_tab", _ctx.$attrs.class]),
          style: vue.normalizeStyle({ background: $props.background || "#fff" })
        },
        [
          !$setup.props.hiddenIcon && $setup.props.mode == "jump" ? (vue.openBlock(), vue.createElementBlock("image", {
            key: 0,
            class: "icon_image",
            src: _imports_0$g
          })) : vue.createCommentVNode("v-if", true),
          $props.showIndexSelector ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "index-selector"
          }, [
            vue.createElementVNode("picker", {
              mode: "selector",
              value: $setup.selectedIndex,
              range: $setup.indexLabels,
              onChange: $setup.handleIndexChange
            }, [
              vue.createElementVNode("view", { class: "selector-content" }, [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString($setup.currentIndexLabel),
                  1
                  /* TEXT */
                )
              ])
            ], 40, ["value", "range"]),
            $setup.showTip ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "selector-tip"
            }, "点击切换")) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "common_search_input",
              placeholder: "可以模糊搜索 o( ❛ᴗ❛ )o …",
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.searchTerm = $event),
              onInput: $setup.onSearchInput,
              ignoreCompositionEvent: false
            },
            null,
            544
            /* NEED_HYDRATION, NEED_PATCH */
          ), [
            [vue.vModelText, $setup.searchTerm]
          ]),
          $setup.results.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "search-info-tap"
          }, [
            $setup.results.length > 0 && $setup.searchTerm.length < 5 && !$setup.props.hideHintText ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "拼音、缩写、别名都可以搜")) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("image", {
              class: "icon_image",
              src: _imports_2$5,
              onClick: $setup.cancel
            })
          ])) : vue.createCommentVNode("v-if", true)
        ],
        6
        /* CLASS, STYLE */
      ),
      vue.createElementVNode("view", { style: { "position": "relative" } }, [
        $setup.results.length > 0 ? (vue.openBlock(), vue.createElementBlock(
          "scroll-view",
          {
            key: 0,
            class: "search_results",
            style: vue.normalizeStyle([{ "height": "600rpx" }, { width: $props.width }]),
            "scroll-y": ""
          },
          [
            vue.createElementVNode("view", {
              class: "close-associate result_item",
              onClick: $setup.closeAssociate,
              style: { "padding-top": "0", "text-align": "center" }
            }, [
              vue.createElementVNode("text", null, "关闭联想")
            ]),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.results, (item) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "result_item",
                  onClick: ($event) => $setup.onTap(item.id, item.name)
                }, vue.toDisplayString(item.name), 9, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ],
          4
          /* STYLE */
        )) : vue.createCommentVNode("v-if", true)
      ])
    ]);
  }
  const __easycom_2$a = /* @__PURE__ */ _export_sfc(_sfc_main$1O, [["render", _sfc_render$1N], ["__scopeId", "data-v-e5a069da"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-search/common-search.vue"]]);
  const _imports_0$f = "/static/message.png";
  const _imports_1$a = "/static/like.png";
  const _sfc_main$1N = {};
  function _sfc_render$1M(_ctx, _cache) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode("view", { class: "list_item_main_info" }, [
        vue.createElementVNode("text", null, "Puypoodoll - momo"),
        vue.createElementVNode("text", null, "21分钟前"),
        vue.createElementVNode("view", { class: "list_view_and_like_box" }, [
          vue.createElementVNode("view", { class: "posion_bottom" }, [
            vue.createElementVNode("view", { class: "icon_inline_text" }, [
              vue.createElementVNode("image", {
                class: "icon_image",
                src: _imports_0$f
              }),
              vue.createElementVNode("text", null, "10")
            ]),
            vue.createElementVNode("view", { class: "icon_inline_text" }, [
              vue.createElementVNode("image", {
                class: "icon_image",
                src: _imports_1$a
              }),
              vue.createElementVNode("text", null, "20")
            ])
          ])
        ])
      ])
    ]);
  }
  const __easycom_2$9 = /* @__PURE__ */ _export_sfc(_sfc_main$1N, [["render", _sfc_render$1M], ["__scopeId", "data-v-704f716d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/homepage-list-item/homepage-list-item.vue"]]);
  const _imports_0$e = "/static/pixsun.png";
  const _imports_1$9 = "/static/arrow_down.png";
  const _sfc_main$1M = {
    __name: "homepage",
    setup(__props, { expose: __expose }) {
      __expose();
      let selectSizeArray = ["所有尺寸", "一分", "二分", "三分", "四分", "六分", "八分", "十分", "十二分", "其它大尺寸", "棉花娃"];
      let index = vue.ref(0);
      let menuList = [
        { name: "上新推送" },
        { name: "BJD整娃" },
        { name: "单头" },
        { name: "假发" },
        { name: "娃衣" },
        { name: "眼珠" },
        { name: "鞋子" },
        { name: "支架" },
        { name: "其它配件" }
      ];
      function bindPickerChange(e2) {
        formatAppLog("log", "at pages/homepage/homepage.vue:69", "picker发送选择改变，携带值为", e2.detail.value);
        index.value = e2.detail.value;
      }
      const systemInfo2 = uni.getSystemInfoSync();
      const statusBarHeight = vue.ref(systemInfo2.statusBarHeight);
      formatAppLog("log", "at pages/homepage/homepage.vue:75", "状态栏高度" + statusBarHeight.value);
      onReachBottom(() => {
        formatAppLog("log", "at pages/homepage/homepage.vue:78", "onEachBottom!");
      });
      function go2goods() {
        uni.navigateTo({
          url: "/pages/goods/goods"
        });
      }
      const onShareAppMessage2 = () => ({
        title: "BJD娃圈你想知道的这里都有~",
        path: "/pages/news/news",
        success(res) {
          formatAppLog("log", "at pages/homepage/homepage.vue:91", "分享成功", res);
        },
        fail(err) {
          formatAppLog("log", "at pages/homepage/homepage.vue:94", "分享失败", err);
        },
        mp: {
          wxpath: "/pages/index/index.html"
        }
      });
      const __returned__ = { get selectSizeArray() {
        return selectSizeArray;
      }, set selectSizeArray(v2) {
        selectSizeArray = v2;
      }, get index() {
        return index;
      }, set index(v2) {
        index = v2;
      }, get menuList() {
        return menuList;
      }, set menuList(v2) {
        menuList = v2;
      }, bindPickerChange, systemInfo: systemInfo2, statusBarHeight, go2goods, onShareAppMessage: onShareAppMessage2, ref: vue.ref, get onReachBottom() {
        return onReachBottom;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1L(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_common_search = resolveEasycom(vue.resolveDynamicComponent("common-search"), __easycom_2$a);
    const _component_homepage_list_item = resolveEasycom(vue.resolveDynamicComponent("homepage-list-item"), __easycom_2$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "backbody" }, [
          vue.createElementVNode("view", { class: "pageinfo_userinfo" }, [
            vue.createElementVNode("image", {
              mode: "aspectFill",
              class: "avatar mine_avatar",
              src: _imports_0$e
            }),
            vue.createElementVNode("view", { class: "pageinfo_infobox" }, [
              vue.createElementVNode("text", { class: "mine_username font-alimamashuhei" }, "用户名"),
              vue.createElementVNode("view", null, [
                vue.createElementVNode("text", { class: "mine_friends" }, [
                  vue.createTextVNode("好友 "),
                  vue.createElementVNode("text", { class: "mine_info_number" }, "10")
                ]),
                vue.createElementVNode("text", { class: "mine_message" }, [
                  vue.createTextVNode("消息 "),
                  vue.createElementVNode("text", { class: "mine_info_number" }, "10")
                ])
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "pageinfo_body" }, [
            vue.createElementVNode("view", null, [
              vue.createElementVNode("view", null, [
                vue.createElementVNode("view", { class: "uni-list-cell-db pick_size font-alimamashuhei" }, [
                  vue.createElementVNode("picker", {
                    onChange: $setup.bindPickerChange,
                    value: $setup.index,
                    range: $setup.selectSizeArray
                  }, [
                    vue.createElementVNode("view", { class: "uni-input select_size" }, [
                      vue.createTextVNode(
                        vue.toDisplayString($setup.selectSizeArray[$setup.index]) + " ",
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("image", {
                        class: "icon_image down",
                        src: _imports_1$9
                      })
                    ])
                  ], 40, ["value", "range"])
                ]),
                vue.createElementVNode("icon", { class: "" })
              ]),
              vue.createElementVNode("view", { class: "left_menu" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.menuList, (item, key) => {
                    return vue.openBlock(), vue.createElementBlock("view", null, [
                      (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          class: "menu_text font-alimamashuhei",
                          key
                        },
                        vue.toDisplayString(item.name),
                        1
                        /* TEXT */
                      ))
                    ]);
                  }),
                  256
                  /* UNKEYED_FRAGMENT */
                ))
              ]),
              vue.createElementVNode("view", { class: "right_body" }, [
                vue.createVNode(_component_common_search),
                vue.createElementVNode("view", null, [
                  vue.createVNode(_component_homepage_list_item, { onClick: $setup.go2goods })
                ])
              ])
            ])
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesHomepageHomepage = /* @__PURE__ */ _export_sfc(_sfc_main$1M, [["render", _sfc_render$1L], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/homepage/homepage.vue"]]);
  const _sfc_main$1L = {
    __name: "shmily-drag-image",
    props: {
      value: {
        type: Array,
        default: () => []
      },
      customClick: {
        type: Boolean,
        default: false
      },
      modelValue: {
        type: Array,
        default: () => []
      },
      keyName: {
        type: String,
        default: "image_url"
        // 默认使用image_url作为图片字段
      },
      number: {
        type: Number,
        default: 6
      },
      imageWidth: {
        type: Number,
        default: 0
      },
      cols: {
        type: Number,
        default: 3
      },
      borderRadius: {
        type: String,
        default: 0
      },
      padding: {
        type: Number,
        default: 10
      },
      scale: {
        type: Number,
        default: 1.1
      },
      opacity: {
        type: Number,
        default: 0.7
      },
      // 新增：每个项目之间的边距
      itemMargin: {
        type: Number,
        default: 10
      },
      // 新增：图片与文字的比例
      imageRatio: {
        type: Number,
        default: 0.7
        // 图片区域占总高度的70%
      },
      // 显示item的信息
      showItemInfo: {
        type: Boolean,
        default: true
      },
      showDelete: {
        type: Boolean,
        default: false
      },
      /* 付款状态标签（新增能力） */
      showPaymentTag: { type: Boolean, default: false },
      paymentField: { type: String, default: "payment_status" },
      paymentMap: {
        type: Object,
        default: () => ({ 1: "全款", 2: "定金", 3: "未买" })
      }
    },
    emits: ["input", "update:modelValue", "sort-change", "delete", "item-click"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const imageList = vue.ref([]);
      const width = vue.ref(0);
      const add = vue.ref({ x: 0, y: 0 });
      const colsValue = vue.ref(0);
      const viewWidth = vue.ref(0);
      const viewHeight = vue.ref(0);
      const tempItem = vue.ref(null);
      const timer = vue.ref(null);
      const changeStatus = vue.ref(true);
      const preStatus = vue.ref(true);
      const first = vue.ref(true);
      const touchStartY = vue.ref(0);
      const longPressTimer = vue.ref(null);
      const isDragging = vue.ref(false);
      const isMounted = vue.ref(false);
      const instanceRef = vue.ref(null);
      const imgObservers = /* @__PURE__ */ new Map();
      const areaHeight = vue.computed(() => {
        if (imageList.value.length < props.number) {
          return Math.ceil((imageList.value.length + 1) / colsValue.value) * viewHeight.value + "px";
        }
        return Math.ceil(imageList.value.length / colsValue.value) * viewHeight.value + "px";
      });
      const childWidth = vue.computed(() => {
        return viewWidth.value - rpx2px2(props.padding) * 2 + "px";
      });
      const childHeight = vue.computed(() => {
        return viewHeight.value - rpx2px2(props.padding) * 2 + "px";
      });
      const deleteImage = (item) => {
        emit("delete", item.id);
        const index = imageList.value.findIndex((img) => img.id === item.id);
        if (index !== -1) {
          destroyImgObserver(item.id);
          imageList.value.splice(index, 1);
          updateItemsPosition();
          sortList();
        }
      };
      const getSrc = (item) => {
        return props.keyName !== null ? item[props.keyName] : item;
      };
      const getItemData = (item) => {
        var _a, _b;
        const ps2 = item[props.paymentField] ?? item.payStatus ?? 1;
        return {
          id: item.id,
          src: getSrc(item),
          name: item.name,
          price: item.price,
          type: item.type,
          // 预计算，拖拽时不会变
          payStatus: ps2,
          _payText: ((_a = props.paymentMap) == null ? void 0 : _a[Number(ps2)]) || ((_b = props.paymentMap) == null ? void 0 : _b[1]) || "已全款"
        };
      };
      const onChange = (e2, item) => {
        if (!item)
          return;
        item.oldX = e2.detail.x;
        item.oldY = e2.detail.y;
        if (e2.detail.source === "touch") {
          if (item.moveEnd) {
            item.offset = Math.sqrt(
              Math.pow(item.oldX - item.absX * viewWidth.value, 2) + Math.pow(item.oldY - item.absY * viewHeight.value, 2)
            );
          }
          let x = Math.floor((e2.detail.x + viewWidth.value / 2) / viewWidth.value);
          if (x >= colsValue.value)
            return;
          let y2 = Math.floor((e2.detail.y + viewHeight.value / 2) / viewHeight.value);
          let index = colsValue.value * y2 + x;
          if (item.index !== index && index < imageList.value.length) {
            changeStatus.value = false;
            imageList.value.forEach((obj) => {
              if (item.index > index && obj.index >= index && obj.index < item.index) {
                changeObj(obj, 1);
              } else if (item.index < index && obj.index <= index && obj.index > item.index) {
                changeObj(obj, -1);
              } else if (obj.id !== item.id) {
                obj.offset = 0;
                obj.x = obj.oldX;
                obj.y = obj.oldY;
                setTimeout(() => {
                  vue.nextTick(() => {
                    obj.x = obj.absX * viewWidth.value;
                    obj.y = obj.absY * viewHeight.value;
                  });
                }, 0);
              }
            });
            moveItem(item, index);
            item.index = index;
            item.absX = x;
            item.absY = y2;
            if (!item.moveEnd) {
              setTimeout(() => {
                vue.nextTick(() => {
                  item.x = item.absX * viewWidth.value;
                  item.y = item.absY * viewHeight.value;
                });
              }, 0);
            }
            sortList();
          }
        }
      };
      const moveItem = (item, newIndex) => {
        const oldIndex = imageList.value.findIndex((i2) => i2.id === item.id);
        if (oldIndex === -1 || oldIndex === newIndex)
          return;
        imageList.value.splice(newIndex, 0, imageList.value.splice(oldIndex, 1)[0]);
        imageList.value.forEach((item2, idx) => {
          item2.index = idx;
        });
        sortList();
      };
      const changeObj = (obj, i2) => {
        obj.index += i2;
        obj.offset = 0;
        obj.x = obj.oldX;
        obj.y = obj.oldY;
        obj.absX = obj.index % colsValue.value;
        obj.absY = Math.floor(obj.index / colsValue.value);
        setTimeout(() => {
          vue.nextTick(() => {
            obj.x = obj.absX * viewWidth.value;
            obj.y = obj.absY * viewHeight.value;
          });
        }, 0);
      };
      const onLongPressStart = (item, e2) => {
        touchStartY.value = e2.touches[0].clientY;
        if (longPressTimer.value) {
          clearTimeout(longPressTimer.value);
          longPressTimer.value = null;
        }
        imageList.value.forEach((item2) => {
          item2.ready = false;
          item2.disable = true;
        });
        formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:347", "长按开始");
        longPressTimer.value = setTimeout(() => {
          formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:351", "长按成功！");
          uni.vibrateShort({
            success: () => {
              formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:354", "触感反馈");
            }
          });
          item.ready = true;
          item.disable = false;
          isDragging.value = true;
          touchstart(item);
        }, 240);
      };
      const touchstart = (item, e2) => {
        formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:365", "进入touchstart");
        imageList.value.forEach((v2) => {
          v2.zIndex = v2.index + 9;
        });
        item.zIndex = 99;
        item.moveEnd = true;
        tempItem.value = item;
        timer.value = setTimeout(() => {
          item.scale = props.scale;
          item.opacity = props.opacity;
          clearTimeout(timer.value);
          timer.value = null;
        }, 200);
      };
      const touchend = (item) => {
        item.scale = 1;
        item.opacity = 1;
        item.x = item.oldX;
        item.y = item.oldY;
        item.offset = 0;
        item.moveEnd = false;
        formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:390", "结束点击，清理ready");
        imageList.value.forEach((item2) => {
          item2.ready = false;
          item2.disable = true;
        });
        if (isDragging.value == true) {
          formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:396", "来自于拖拽的结束,上报排序事件");
          const sortedIds = imageList.value.map((item2) => item2.id);
          emit("sort-change", sortedIds);
          formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:402", "排序后的ID", sortedIds);
        }
        isDragging.value = false;
        if (longPressTimer.value) {
          clearTimeout(longPressTimer.value);
          longPressTimer.value = null;
        }
        setTimeout(() => {
          imageList.value.forEach((obj) => {
            if (obj.ready)
              return;
            obj.x = obj.absX * viewWidth.value;
            obj.y = obj.absY * viewHeight.value;
            obj.offset = 0;
            obj.moveEnd = false;
          });
          vue.nextTick(() => {
            item.x = item.absX * viewWidth.value;
            item.y = item.absY * viewHeight.value;
            tempItem.value = null;
            changeStatus.value = true;
          });
        }, 50);
      };
      const getFirstImage = (imageUrls) => {
        var _a;
        if (!imageUrls)
          return "";
        const urls = imageUrls.split(",");
        const firstUrl = ((_a = urls[0]) == null ? void 0 : _a.trim()) || "";
        if (firstUrl.startsWith("http")) {
          return firstUrl;
        }
        return "";
      };
      const previewImage = (item) => {
        if (timer.value && preStatus.value && changeStatus.value && item.offset < 28.28) {
          clearTimeout(timer.value);
          timer.value = null;
          const list = props.modelValue.length ? props.modelValue : props.value;
          let srcList = list.map((v2) => getSrc(v2));
          uni.previewImage({
            urls: srcList,
            current: item.src,
            success: () => {
              preStatus.value = false;
              setTimeout(() => {
                preStatus.value = true;
              }, 600);
            },
            fail: (e2) => {
              formatAppLog("log", "at components/shmily-drag-image/shmily-drag-image.vue:465", e2);
            }
          });
        } else if (timer.value) {
          clearTimeout(timer.value);
          timer.value = null;
        }
      };
      const mouseenter = () => {
      };
      const mouseleave = () => {
      };
      const onTouchMove = (e2, item) => {
        if (isDragging.value)
          return;
        const touchY = e2.touches[0].clientY;
        const deltaY = Math.abs(touchY - touchStartY.value);
        if (deltaY > 10 && longPressTimer.value) {
          clearTimeout(longPressTimer.value);
          longPressTimer.value = null;
        }
      };
      const delImageHandle = (item, index) => {
        destroyImgObserver(item.id);
        imageList.value.splice(index, 1);
        imageList.value.forEach((obj) => {
          if (obj.index > item.index) {
            obj.index -= 1;
            obj.x = obj.oldX;
            obj.y = obj.oldY;
            obj.absX = obj.index % colsValue.value;
            obj.absY = Math.floor(obj.index / colsValue.value);
            vue.nextTick(() => {
              obj.x = obj.absX * viewWidth.value;
              obj.y = obj.absY * viewHeight.value;
            });
          }
        });
        add.value.x = imageList.value.length % colsValue.value * viewWidth.value;
        add.value.y = Math.floor(imageList.value.length / colsValue.value) * viewHeight.value;
        sortList();
      };
      const delImageMp = (item, index) => {
      };
      function go2editor(id) {
        uni.navigateTo({
          url: "/pkg-stock/account_book_form/account_book_form?account_book_id=" + id
        });
      }
      const handleItemClick = (item) => {
        emit("item-click", item);
        if (!props.customClick) {
          go2preview(item.id);
        }
      };
      function go2preview(id) {
        uni.navigateTo({
          url: "/pkg-stock/account_book_preview/account_book_preview?account_book_id=" + id
        });
      }
      const onLongPressCancel = () => {
        if (longPressTimer.value) {
          clearTimeout(longPressTimer.value);
          longPressTimer.value = null;
        }
      };
      const updateAllItemsPosition = () => {
        listItems.value.forEach((item) => {
          const newAbsX = item.index % colsValue.value;
          const newAbsY = Math.floor(item.index / colsValue.value);
          const targetX = newAbsX * viewWidth.value;
          const targetY = newAbsY * viewWidth.value;
          item.x = targetX;
          item.y = targetY;
          item.originX = targetX;
          item.originY = targetY;
          item.absX = newAbsX;
          item.absY = newAbsY;
        });
      };
      const sortList = () => {
        const result = [];
        const source = props.modelValue.length ? props.modelValue : props.value;
        const list = [...imageList.value].sort((a2, b2) => a2.index - b2.index);
        for (let s2 of list) {
          const item = source.find((d2) => getSrc(d2) === s2.src);
          if (item) {
            result.push(item);
          } else {
            if (props.keyName !== null) {
              result.push({
                [props.keyName]: s2.src
              });
            } else {
              result.push(s2.src);
            }
          }
        }
        emit("input", result);
        emit("update:modelValue", result);
      };
      const addProperties = (item) => {
        const data = getItemData(item);
        const absX = imageList.value.length % colsValue.value;
        const absY = Math.floor(imageList.value.length / colsValue.value);
        const x = absX * viewWidth.value;
        const y2 = absY * viewHeight.value;
        imageList.value.push({
          ...data,
          x,
          y: y2,
          oldX: x,
          oldY: y2,
          absX,
          absY,
          scale: 1,
          zIndex: 9,
          opacity: 1,
          index: imageList.value.length,
          id: item.id,
          disable: false,
          offset: 0,
          moveEnd: false,
          ready: false,
          _imgLoaded: false,
          // ✅ 懒加载：默认未加载真实图片
          __imgBroken: false
        });
        add.value.x = imageList.value.length % colsValue.value * viewWidth.value;
        add.value.y = Math.floor(imageList.value.length / colsValue.value) * viewHeight.value;
      };
      const rpx2px2 = (v2) => {
        return width.value * v2 / 750;
      };
      const guid = (len = 32) => {
        const chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz".split("");
        const uuid = [];
        const radix = chars.length;
        for (let i2 = 0; i2 < len; i2++)
          uuid[i2] = chars[0 | Math.random() * radix];
        uuid.shift();
        return `u${uuid.join("")}`;
      };
      vue.onMounted(() => {
        width.value = uni.getSystemInfoSync().windowWidth;
        instanceRef.value = vue.getCurrentInstance();
        vue.nextTick(() => {
          const query = uni.createSelectorQuery().in(instanceRef.value.proxy);
          query.select(".con").boundingClientRect((data) => {
            if (!data) {
              formatAppLog("error", "at components/shmily-drag-image/shmily-drag-image.vue:688", "未找到 .con 元素");
              return;
            }
            colsValue.value = props.cols;
            viewWidth.value = data.width / props.cols;
            viewHeight.value = viewWidth.value * 1.3;
            if (props.imageWidth > 0) {
              viewWidth.value = rpx2px2(props.imageWidth);
              viewHeight.value = viewWidth.value * 1.3;
              colsValue.value = Math.floor(data.width / viewWidth.value);
            }
            const list = props.modelValue.length ? props.modelValue : props.value;
            list.forEach((item) => {
              addProperties(item);
            });
            first.value = false;
            setupLazyLoadForAll();
          }).exec();
        });
        isMounted.value = true;
      });
      vue.onBeforeUnmount(() => {
        imgObservers.forEach((ob) => {
          try {
            ob.disconnect();
          } catch (e2) {
          }
        });
        imgObservers.clear();
      });
      const updateItemsPosition = () => {
        imageList.value.forEach((item, index) => {
          item.index = index;
          const newAbsX = index % colsValue.value;
          const newAbsY = Math.floor(index / colsValue.value);
          item.x = newAbsX * viewWidth.value;
          item.y = newAbsY * viewHeight.value;
          item.oldX = item.x;
          item.oldY = item.y;
          item.absX = newAbsX;
          item.absY = newAbsY;
        });
      };
      const initViewSize = () => {
        return new Promise((resolve) => {
          if (!instanceRef.value || !instanceRef.value.proxy) {
            formatAppLog("warn", "at components/shmily-drag-image/shmily-drag-image.vue:751", "Component instance not available for selector query");
            return resolve();
          }
          const query = uni.createSelectorQuery().in(instanceRef.value.proxy);
          query.select(".con").boundingClientRect((data) => {
            if (!data)
              return resolve();
            colsValue.value = props.cols;
            viewWidth.value = data.width / props.cols;
            viewHeight.value = viewWidth.value * 1.3;
            if (props.imageWidth > 0) {
              viewWidth.value = rpx2px2(props.imageWidth);
              viewHeight.value = viewWidth.value * 1.3;
              colsValue.value = Math.floor(data.width / viewWidth.value);
            }
            resolve();
          }).exec();
        });
      };
      vue.watch(
        () => props.modelValue,
        (newVal) => {
          if (isDragging.value)
            return;
          if (isMounted.value) {
            vue.nextTick(() => {
              initViewSize().then(() => {
                updateImageList(newVal);
                setupLazyLoadForAll();
              });
            });
          }
        },
        {
          deep: true,
          immediate: true
        }
      );
      const updateImageList = (newList) => {
        const oldItems = imageList.value.reduce((map, item) => {
          map[item.id] = item;
          return map;
        }, {});
        imageList.value = newList.map((item) => {
          const existing = oldItems[item.id];
          if (existing) {
            return {
              ...existing,
              ...getItemData(item)
              // 更新可能变化的数据
            };
          }
          return createNewItem(item);
        });
        updateItemsPosition();
      };
      const createNewItem = (item) => {
        const data = getItemData(item);
        const absX = imageList.value.length % colsValue.value;
        const absY = Math.floor(imageList.value.length / colsValue.value);
        return {
          ...data,
          x: absX * viewWidth.value,
          y: absY * viewHeight.value,
          oldX: absX * viewWidth.value,
          oldY: absY * viewHeight.value,
          absX,
          absY,
          scale: 1,
          zIndex: 9,
          opacity: 1,
          index: imageList.value.length,
          disable: false,
          offset: 0,
          moveEnd: false,
          ready: false,
          _imgLoaded: false,
          // ✅ 默认未加载真实图片
          __imgBroken: false
        };
      };
      const NO_IMG = "data:image/svg+xml;utf8," + encodeURIComponent(
        `<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400">
	      <rect width="100%" height="100%" fill="#e9ebef"/>
	      <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle"
	        fill="#9aa0a6" font-size="40" font-family="Arial">No Image</text>
	    </svg>`
      );
      function normalizeFirstImage(s2) {
        if (!s2)
          return "";
        const first2 = String(s2).split(",")[0].trim();
        const low = first2.toLowerCase();
        if (!first2 || low.includes("/default") || low.endsWith("default.png") || low.includes("noimage"))
          return "";
        return first2;
      }
      function getDisplayImg(item) {
        if (item.__imgBroken)
          return NO_IMG;
        const src = normalizeFirstImage(item.src);
        return src || NO_IMG;
      }
      function onImgError(item) {
        item.__imgBroken = true;
      }
      function getDisplayPrice(price) {
        if (price === null || price === void 0)
          return "";
        return String(price).replace(/【.*?】/g, "").trim();
      }
      function getPaymentText(item) {
        var _a, _b;
        const st2 = Number((item == null ? void 0 : item.payStatus) ?? 1);
        return ((_a = props.paymentMap) == null ? void 0 : _a[st2]) || ((_b = props.paymentMap) == null ? void 0 : _b[1]) || "已全款";
      }
      const createImgObserver = (id) => {
        if (!instanceRef.value || imgObservers.has(id))
          return;
        const observer = uni.createIntersectionObserver(instanceRef.value, {
          thresholds: [0.01]
        });
        observer.relativeToViewport({ bottom: 50 }).observe(`#drag-img-${id}`, (res) => {
          if (!res)
            return;
          if (res.intersectionRatio > 0) {
            const target = imageList.value.find((i2) => i2.id === id);
            if (target) {
              target._imgLoaded = true;
            }
            try {
              observer.disconnect();
            } catch (e2) {
            }
            imgObservers.delete(id);
          }
        });
        imgObservers.set(id, observer);
      };
      const setupLazyLoadForAll = () => {
        if (!instanceRef.value)
          return;
        vue.nextTick(() => {
          imageList.value.forEach((item) => {
            if (!item._imgLoaded) {
              createImgObserver(item.id);
            }
          });
        });
      };
      const destroyImgObserver = (id) => {
        const ob = imgObservers.get(id);
        if (ob) {
          try {
            ob.disconnect();
          } catch (e2) {
          }
          imgObservers.delete(id);
        }
      };
      const __returned__ = { props, emit, imageList, width, add, colsValue, viewWidth, viewHeight, tempItem, timer, changeStatus, preStatus, first, touchStartY, longPressTimer, isDragging, isMounted, instanceRef, imgObservers, areaHeight, childWidth, childHeight, deleteImage, getSrc, getItemData, onChange, moveItem, changeObj, onLongPressStart, touchstart, touchend, getFirstImage, previewImage, mouseenter, mouseleave, onTouchMove, delImageHandle, delImageMp, go2editor, handleItemClick, go2preview, onLongPressCancel, updateAllItemsPosition, sortList, addProperties, rpx2px: rpx2px2, guid, updateItemsPosition, initViewSize, updateImageList, createNewItem, NO_IMG, normalizeFirstImage, getDisplayImg, onImgError, getDisplayPrice, getPaymentText, createImgObserver, setupLazyLoadForAll, destroyImgObserver, ref: vue.ref, computed: vue.computed, watch: vue.watch, onMounted: vue.onMounted, onBeforeUnmount: vue.onBeforeUnmount, nextTick: vue.nextTick, getCurrentInstance: vue.getCurrentInstance, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1K(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "con" }, [
      $setup.viewWidth ? (vue.openBlock(), vue.createElementBlock(
        "movable-area",
        {
          key: 0,
          class: "area",
          style: vue.normalizeStyle({ height: $setup.areaHeight }),
          onMouseenter: $setup.mouseenter,
          onMouseleave: $setup.mouseleave
        },
        [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.imageList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("movable-view", {
                key: item.id,
                class: "view",
                direction: "all",
                y: item.y,
                x: item.x,
                damping: 40,
                disabled: item.disable || !item.ready,
                onChange: ($event) => $setup.onChange($event, item),
                onTouchstart: ($event) => $setup.onLongPressStart(item, $event),
                onTouchend: ($event) => $setup.touchend(item),
                onClick: ($event) => $setup.handleItemClick(item),
                onTouchmove: ($event) => $setup.onTouchMove($event, item),
                style: vue.normalizeStyle({
                  width: $setup.viewWidth + "px",
                  height: $setup.viewHeight + "px",
                  "z-index": item.zIndex,
                  opacity: item.opacity
                })
              }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["area-con", { "ready": !item.disable && item.ready }]),
                    style: vue.normalizeStyle({
                      width: $setup.childWidth,
                      height: $setup.childHeight,
                      borderRadius: $props.borderRadius + "rpx",
                      transform: "scale(" + item.scale + ")",
                      margin: $props.itemMargin + "px"
                    })
                  },
                  [
                    $setup.props.showDelete ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "delete-btn",
                      onClick: vue.withModifiers(($event) => $setup.deleteImage(item), ["stop"])
                    }, [
                      vue.createVNode(_component_uni_icons, {
                        type: "clear",
                        color: "#9b9b9b",
                        size: "30"
                      })
                    ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("image", {
                      class: "pre-image",
                      id: "drag-img-" + item.id,
                      src: item._imgLoaded ? $setup.getDisplayImg(item) : $setup.NO_IMG,
                      mode: "aspectFill",
                      "lazy-load": true,
                      onError: ($event) => $setup.onImgError(item)
                    }, null, 40, ["id", "src", "onError"]),
                    $setup.props.showItemInfo ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "info-container"
                    }, [
                      vue.createElementVNode(
                        "text",
                        { class: "name" },
                        vue.toDisplayString(item.name),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "price" },
                        vue.toDisplayString($setup.getDisplayPrice(item.price)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "type" },
                        vue.toDisplayString(item.type),
                        1
                        /* TEXT */
                      )
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.withDirectives(vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["pay-badge candy", ["s-" + (item.payStatus || 1)]])
                      },
                      vue.toDisplayString(item._payText),
                      3
                      /* TEXT, CLASS */
                    ), [
                      [vue.vShow, $setup.props.showPaymentTag && item.payStatus]
                    ])
                  ],
                  6
                  /* CLASS, STYLE */
                )
              ], 44, ["y", "x", "disabled", "onChange", "onTouchstart", "onTouchend", "onClick", "onTouchmove"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ],
        36
        /* STYLE, NEED_HYDRATION */
      )) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_3$6 = /* @__PURE__ */ _export_sfc(_sfc_main$1L, [["render", _sfc_render$1K], ["__scopeId", "data-v-96f535cf"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/shmily-drag-image/shmily-drag-image.vue"]]);
  const _imports_0$d = "/static/empty.jpg";
  const PRICE_VISIBLE_KEY = "accountBookPriceVisible";
  const SELECTED_TYPE_KEY = "accountBookSelectedType";
  const _sfc_main$1K = {
    __name: "stock-myitems",
    props: {
      accountBookData: Object,
      activeTab: { type: Number, default: 1 }
    },
    emits: ["go2editor", "update-type", "init-request", "update:accountBookData", "open-type-manager"],
    setup(__props, { expose: __expose, emit: __emit }) {
      const props = __props;
      const emit = __emit;
      const isPriceVisible = vue.ref(true);
      vue.watch(isPriceVisible, (v2) => uni.setStorageSync(PRICE_VISIBLE_KEY, String(v2)));
      const customTypes = vue.ref([]);
      const defaultTypes = ["全部"];
      const selectedType = vue.ref(0);
      const selectedTypeName = vue.ref("全部");
      const typeOptions = vue.computed(() => [...defaultTypes, ...customTypes.value.map((t2) => t2.name)]);
      const baseList = vue.computed(() => {
        var _a;
        return ((_a = props.accountBookData) == null ? void 0 : _a.account_books) || [];
      });
      const totalPrice = vue.computed(() => {
        var _a;
        if (!((_a = props.accountBookData) == null ? void 0 : _a.account_books))
          return "0.00";
        return props.accountBookData.account_books.reduce((sum, item) => sum + (parseFloat(item.price) || 0), 0).toFixed(2);
      });
      async function getAccountTypes() {
        var _a;
        const token = uni.getStorageSync("token");
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/account-types",
            method: "GET",
            header: { "Authorization": token }
          });
          const list = ((_a = res.data) == null ? void 0 : _a.data) || [];
          customTypes.value = list;
          const names = list.map((t2) => t2.name);
          if (selectedTypeName.value !== "全部" && !names.includes(selectedTypeName.value)) {
            selectedTypeName.value = "全部";
            emit("update-type", "");
          }
        } catch (e2) {
          formatAppLog("error", "at components/stock-myitems/stock-myitems.vue:125", "获取分类失败:", e2);
        }
      }
      __expose({ getAccountTypes });
      function updateSelectedType(e2) {
        selectedType.value = e2.detail.value;
        selectedTypeName.value = typeOptions.value[selectedType.value];
        uni.setStorageSync(SELECTED_TYPE_KEY, selectedTypeName.value);
        emit("update-type", selectedTypeName.value === "全部" ? "" : selectedTypeName.value);
      }
      vue.watch([customTypes, selectedTypeName], () => {
        const list = typeOptions.value;
        const want = selectedTypeName.value || "全部";
        const idx = list.findIndex((n2) => n2 === want);
        selectedType.value = idx >= 0 ? idx : 0;
      });
      function openTypeManager() {
        emit("open-type-manager");
      }
      function handleSortChange(sortedIds) {
        const token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/sort-account-book",
          method: "POST",
          header: { "Authorization": token, "Content-Type": "application/json" },
          data: { sorted_ids: sortedIds }
        });
      }
      function openSearch() {
        const list = JSON.parse(JSON.stringify(baseList.value || []));
        try {
          uni.setStorageSync("__stockList", list);
        } catch (e2) {
        }
        try {
          history.replaceState({ ...history.state || {}, __stockList: list }, "");
        } catch {
        }
        try {
          localStorage.setItem("__stockList", JSON.stringify(list));
        } catch {
        }
        uni.navigateTo({
          url: "/pkg-stock/stock-myitems-search/stock-myitems-search",
          success(res) {
            const ec = res.eventChannel;
            const push = () => ec && ec.emit && ec.emit("initList", { list });
            setTimeout(push, 0);
          }
        });
      }
      const safeTop = vue.ref(0);
      onShow(async () => {
        var _a;
        const saved = uni.getStorageSync(PRICE_VISIBLE_KEY);
        if (saved !== "")
          isPriceVisible.value = saved === "true";
        selectedTypeName.value = uni.getStorageSync(SELECTED_TYPE_KEY) || "全部";
        await getAccountTypes();
        emit("update-type", selectedTypeName.value === "全部" ? "" : selectedTypeName.value);
        try {
          const wi = uni.getWindowInfo && uni.getWindowInfo() || uni.getSystemInfoSync();
          safeTop.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.top) ?? (wi == null ? void 0 : wi.statusBarHeight) ?? 0;
        } catch {
          safeTop.value = 20;
        }
      });
      const __returned__ = { props, emit, isPriceVisible, PRICE_VISIBLE_KEY, customTypes, defaultTypes, selectedType, selectedTypeName, SELECTED_TYPE_KEY, typeOptions, baseList, totalPrice, getAccountTypes, updateSelectedType, openTypeManager, handleSortChange, openSearch, safeTop, ref: vue.ref, computed: vue.computed, watch: vue.watch, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1J(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_shmily_drag_image = resolveEasycom(vue.resolveDynamicComponent("shmily-drag-image"), __easycom_3$6);
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: "tab_body_1st",
        style: vue.normalizeStyle({ "--safe-top": $setup.safeTop + "px" })
      },
      [
        vue.createElementVNode("view", { class: "type-header" }, [
          vue.createElementVNode("view", { class: "selector-container" }, [
            vue.createElementVNode("picker", {
              class: "type-picker",
              mode: "selector",
              value: $setup.selectedType,
              range: $setup.typeOptions,
              onChange: $setup.updateSelectedType
            }, [
              vue.createElementVNode("view", { class: "selector-content" }, [
                vue.createElementVNode("text", { class: "selector-label" }, "分类:"),
                vue.createElementVNode(
                  "text",
                  { class: "selector-value" },
                  vue.toDisplayString($setup.typeOptions[$setup.selectedType]),
                  1
                  /* TEXT */
                ),
                vue.createVNode(_component_uni_icons, {
                  type: "arrowdown",
                  size: "14",
                  color: "#747EE5",
                  class: "selector-icon"
                })
              ])
            ], 40, ["value", "range"]),
            vue.createElementVNode("text", {
              class: "manage-btn",
              onClick: $setup.openTypeManager
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "gear",
                size: "16",
                color: "#fff"
              }),
              vue.createElementVNode("text", null, "管理分类")
            ]),
            vue.createElementVNode("view", {
              class: "search-icon-btn",
              onClick: $setup.openSearch
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "search",
                size: "18",
                color: "#fff"
              })
            ])
          ])
        ]),
        vue.createElementVNode("view", { class: "summary-container" }, [
          vue.createElementVNode("view", { class: "summary-content" }, [
            vue.createVNode(_component_uni_icons, {
              type: "money",
              size: "18",
              color: "#74c9e5"
            }),
            vue.createElementVNode("text", { class: "total-text" }, [
              vue.createTextVNode(" 当前分类合计： "),
              $setup.isPriceVisible ? (vue.openBlock(), vue.createElementBlock(
                "text",
                { key: 0 },
                "¥" + vue.toDisplayString($setup.totalPrice),
                1
                /* TEXT */
              )) : (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "******"))
            ]),
            vue.createVNode(_component_uni_icons, {
              type: $setup.isPriceVisible ? "eye" : "eye-slash",
              size: "18",
              color: "#74c9e5",
              class: "toggle-eye",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.isPriceVisible = !$setup.isPriceVisible)
            }, null, 8, ["type"]),
            vue.createElementVNode("text", { style: { "position": "absolute", "right": "30px" } }, "长按排序")
          ])
        ]),
        $setup.baseList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "content"
        }, [
          vue.createVNode(_component_shmily_drag_image, {
            modelValue: $setup.props.accountBookData.account_books,
            "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.props.accountBookData.account_books = $event),
            "border-radius": "20",
            onSortChange: $setup.handleSortChange,
            "show-payment-tag": true,
            "payment-field": "payment_status"
          }, null, 8, ["modelValue"])
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "empty-state"
        }, [
          vue.createElementVNode("image", {
            class: "empty-icon",
            src: _imports_0$d
          }),
          vue.createElementVNode("text", { class: "empty-text" }, "空空如也～"),
          vue.createElementVNode("text", { class: "empty-tip" }, "点击下方按钮添加第一个物品吧！")
        ]))
      ],
      4
      /* STYLE */
    );
  }
  const __easycom_2$8 = /* @__PURE__ */ _export_sfc(_sfc_main$1K, [["render", _sfc_render$1J], ["__scopeId", "data-v-30803138"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/stock-myitems/stock-myitems.vue"]]);
  const _imports_0$c = "/static/folder-empty.png";
  const _sfc_main$1J = {
    __name: "item-category-manager",
    props: {
      modelValue: { type: Boolean, default: false },
      list: { type: Array, default: () => [] }
    },
    emits: ["update:modelValue", "updated", "refresh"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const customTypes = vue.ref([]);
      const newTypeName = vue.ref("");
      const editId = vue.ref(null);
      const editName = vue.ref("");
      const savingOne = vue.ref(false);
      const savingSort = vue.ref(false);
      const originalOrder = vue.ref([]);
      const listScrollHeight = vue.ref("60vh");
      vue.onMounted(() => {
        listScrollHeight.value = "60vh";
      });
      vue.watch(() => props.list, (val) => {
        customTypes.value = JSON.parse(JSON.stringify(val || []));
        originalOrder.value = customTypes.value.map((t2) => t2.id);
      }, { immediate: true });
      vue.watch(() => props.modelValue, async (show) => {
        if (show) {
          await fetchTypes();
        }
      });
      const token = () => uni.getStorageSync("token") || "";
      const fetchTypes = async () => {
        var _a;
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/account-types",
            method: "GET",
            header: { "Authorization": token() }
          });
          const list = ((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) || [];
          customTypes.value = list;
          originalOrder.value = customTypes.value.map((t2) => t2.id);
          emit("updated", list);
          emit("refresh");
        } catch (err) {
          formatAppLog("error", "at components/item-category-manager/item-category-manager.vue:173", "获取分类失败:", err);
          uni.showToast({ title: "获取分类失败", icon: "none" });
        }
      };
      const addNewType = async () => {
        var _a, _b;
        if (!newTypeName.value) {
          uni.showToast({ title: "请输入分类名称", icon: "none" });
          return;
        }
        if (newTypeName.value.length > 10) {
          uni.showToast({ title: "不能超过10个字符", icon: "none" });
          return;
        }
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/add-account-type",
            method: "POST",
            header: { "Authorization": token() },
            data: { name: newTypeName.value }
          });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) !== "success") {
            throw new Error(((_b = res == null ? void 0 : res.data) == null ? void 0 : _b.msg) || "添加失败");
          }
          newTypeName.value = "";
          await fetchTypes();
          uni.showToast({ title: "添加成功" });
        } catch (err) {
          uni.showToast({ title: (err == null ? void 0 : err.message) || "添加失败", icon: "none" });
        }
      };
      const startEdit = (type) => {
        editId.value = type.id;
        editName.value = type.name;
      };
      const cancelEdit = () => {
        editId.value = null;
        editName.value = "";
      };
      const onEditBlur = (type) => {
        if (editId.value === type.id)
          saveEdit(type);
      };
      const saveEdit = async (type) => {
        var _a, _b;
        if (savingOne.value)
          return;
        const name = (editName.value || "").trim();
        if (!name) {
          uni.showToast({ title: "请输入分类名称", icon: "none" });
          return;
        }
        if (name.length > 10) {
          uni.showToast({ title: "不能超过10个字符", icon: "none" });
          return;
        }
        savingOne.value = true;
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/update-account-book-type",
            method: "POST",
            header: { "Authorization": token(), "Content-Type": "application/json" },
            data: { id: type.id, name }
          });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) !== "success") {
            throw new Error(((_b = res == null ? void 0 : res.data) == null ? void 0 : _b.msg) || "更新失败");
          }
          await fetchTypes();
          uni.showToast({ title: "修改成功" });
        } catch (err) {
          uni.showToast({ title: (err == null ? void 0 : err.message) || "修改失败", icon: "none" });
        } finally {
          savingOne.value = false;
          cancelEdit();
        }
      };
      const deleteType = async (id) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除该分类吗？",
          success: async (ret) => {
            var _a, _b;
            if (!ret.confirm)
              return;
            try {
              const res = await uni.request({
                url: websiteUrl.value + "/with-state/delete-account-type",
                method: "POST",
                header: { "Authorization": token(), "Content-Type": "application/json" },
                data: { id }
              });
              if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) !== "success") {
                throw new Error(((_b = res == null ? void 0 : res.data) == null ? void 0 : _b.msg) || "删除失败");
              }
              await fetchTypes();
              uni.showToast({ title: "删除成功" });
            } catch (err) {
              uni.showToast({ title: (err == null ? void 0 : err.message) || "删除失败", icon: "none" });
            }
          }
        });
      };
      const move = (arr, from, to) => {
        if (from === to)
          return;
        const item = arr.splice(from, 1)[0];
        arr.splice(to, 0, item);
      };
      const moveUp = (i2) => {
        if (i2 > 0)
          move(customTypes.value, i2, i2 - 1);
      };
      const moveDown = (i2) => {
        if (i2 < customTypes.value.length - 1)
          move(customTypes.value, i2, i2 + 1);
      };
      const hasSortChanged = vue.computed(() => {
        const now = customTypes.value.map((t2) => t2.id);
        if (now.length !== originalOrder.value.length)
          return true;
        for (let i2 = 0; i2 < now.length; i2++) {
          if (now[i2] !== originalOrder.value[i2])
            return true;
        }
        return false;
      });
      const saveSort = async () => {
        var _a, _b;
        if (!hasSortChanged.value || savingSort.value)
          return;
        savingSort.value = true;
        try {
          const sorted_ids = customTypes.value.map((t2) => t2.id);
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/sort-account-book-type",
            method: "POST",
            header: { "Authorization": token(), "Content-Type": "application/json" },
            data: { sorted_ids }
          });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) !== "success") {
            throw new Error(((_b = res == null ? void 0 : res.data) == null ? void 0 : _b.msg) || "保存失败");
          }
          originalOrder.value = [...sorted_ids];
          emit("updated", JSON.parse(JSON.stringify(customTypes.value)));
          emit("refresh");
          uni.showToast({ title: "排序已保存" });
        } catch (err) {
          uni.showToast({ title: (err == null ? void 0 : err.message) || "保存失败", icon: "none" });
        } finally {
          savingSort.value = false;
        }
      };
      const onClose = () => emit("update:modelValue", false);
      const __returned__ = { props, emit, customTypes, newTypeName, editId, editName, savingOne, savingSort, originalOrder, listScrollHeight, token, fetchTypes, addNewType, startEdit, cancelEdit, onEditBlur, saveEdit, deleteType, move, moveUp, moveDown, hasSortChanged, saveSort, onClose, ref: vue.ref, watch: vue.watch, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1I(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    return vue.openBlock(), vue.createBlock(_component_bottom_popup, {
      show: $props.modelValue,
      onClose: $setup.onClose
    }, {
      default: vue.withCtx(() => [
        vue.createElementVNode("view", { class: "popup-panel" }, [
          vue.createElementVNode("view", { class: "popup-header" }, [
            vue.createElementVNode("text", { class: "popup-title" }, "分类管理"),
            vue.createElementVNode("view", { class: "header-actions" }, [
              vue.createElementVNode("button", {
                class: "btn ghost plain sm",
                onClick: $setup.onClose
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "closeempty",
                  size: "16",
                  color: "#5f6b8a"
                }),
                vue.createElementVNode("text", null, "关闭")
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "add-row" }, [
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.newTypeName = $event),
                placeholder: "输入新分类名称（≤10字）",
                class: "input",
                maxlength: "10"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [
                vue.vModelText,
                $setup.newTypeName,
                void 0,
                { trim: true }
              ]
            ]),
            vue.createElementVNode("button", {
              class: "btn primary",
              onClick: $setup.addNewType,
              disabled: !$setup.newTypeName
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plus",
                size: "16",
                color: "#fff"
              }),
              vue.createElementVNode("text", null, "添加")
            ], 8, ["disabled"])
          ]),
          vue.createElementVNode(
            "scroll-view",
            {
              "scroll-y": "",
              class: "list-scroll",
              style: vue.normalizeStyle({ height: $setup.listScrollHeight })
            },
            [
              vue.createElementVNode("view", { class: "type-list-modern" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.customTypes, (type, index) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: type.id,
                      class: "type-card"
                    }, [
                      vue.createElementVNode("view", { class: "type-main" }, [
                        $setup.editId === type.id ? vue.withDirectives((vue.openBlock(), vue.createElementBlock("input", {
                          key: 0,
                          "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.editName = $event),
                          class: "edit-input",
                          maxlength: "10",
                          "confirm-type": "done",
                          onConfirm: ($event) => $setup.saveEdit(type),
                          onBlur: ($event) => $setup.onEditBlur(type),
                          placeholder: "输入分类名称"
                        }, null, 40, ["onConfirm", "onBlur"])), [
                          [
                            vue.vModelText,
                            $setup.editName,
                            void 0,
                            { trim: true }
                          ]
                        ]) : (vue.openBlock(), vue.createElementBlock(
                          vue.Fragment,
                          { key: 1 },
                          [
                            vue.createElementVNode(
                              "text",
                              { class: "type-name" },
                              vue.toDisplayString(type.name),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "type-sub" },
                              "ID: " + vue.toDisplayString(type.id),
                              1
                              /* TEXT */
                            )
                          ],
                          64
                          /* STABLE_FRAGMENT */
                        ))
                      ]),
                      vue.createElementVNode("view", { class: "type-actions-modern" }, [
                        $setup.editId === type.id ? (vue.openBlock(), vue.createElementBlock(
                          vue.Fragment,
                          { key: 0 },
                          [
                            vue.createElementVNode("button", {
                              class: "btn ok sm",
                              loading: $setup.savingOne,
                              disabled: $setup.savingOne,
                              onClick: ($event) => $setup.saveEdit(type)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "checkmarkempty",
                                size: "14",
                                color: "#fff"
                              }),
                              vue.createElementVNode("text", null, "保存")
                            ], 8, ["loading", "disabled", "onClick"]),
                            vue.createElementVNode("button", {
                              class: "btn ghost sm",
                              disabled: $setup.savingOne,
                              onClick: $setup.cancelEdit
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "closeempty",
                                size: "14",
                                color: "#5f6b8a"
                              }),
                              vue.createElementVNode("text", null, "取消")
                            ], 8, ["disabled"])
                          ],
                          64
                          /* STABLE_FRAGMENT */
                        )) : (vue.openBlock(), vue.createElementBlock(
                          vue.Fragment,
                          { key: 1 },
                          [
                            vue.createElementVNode("button", {
                              class: "icon-btn",
                              disabled: index === 0 || $setup.savingSort,
                              onClick: ($event) => $setup.moveUp(index)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "arrow-up",
                                size: "18",
                                color: "#74c9e5"
                              })
                            ], 8, ["disabled", "onClick"]),
                            vue.createElementVNode("button", {
                              class: "icon-btn",
                              disabled: index === $setup.customTypes.length - 1 || $setup.savingSort,
                              onClick: ($event) => $setup.moveDown(index)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "arrow-down",
                                size: "18",
                                color: "#74c9e5"
                              })
                            ], 8, ["disabled", "onClick"]),
                            vue.createElementVNode("button", {
                              class: "icon-btn",
                              onClick: ($event) => $setup.startEdit(type)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "compose",
                                size: "18",
                                color: "#5f6b8a"
                              })
                            ], 8, ["onClick"]),
                            vue.createElementVNode("button", {
                              class: "icon-btn",
                              onClick: ($event) => $setup.deleteType(type.id)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: "trash",
                                size: "18",
                                color: "#ff7777"
                              })
                            ], 8, ["onClick"])
                          ],
                          64
                          /* STABLE_FRAGMENT */
                        ))
                      ])
                    ]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                )),
                $setup.customTypes.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "empty-types"
                }, [
                  vue.createElementVNode("image", {
                    class: "empty-folder",
                    src: _imports_0$c
                  }),
                  vue.createElementVNode("text", { class: "empty-text" }, "暂无自定义分类"),
                  vue.createElementVNode("text", { class: "empty-tip" }, "请在上方输入后点击“添加”")
                ])) : vue.createCommentVNode("v-if", true)
              ]),
              vue.createElementVNode("view", { style: { "height": "120rpx" } })
            ],
            4
            /* STYLE */
          ),
          vue.createElementVNode("view", { class: "dock" }, [
            vue.createElementVNode(
              "text",
              { class: "dock-info" },
              "共 " + vue.toDisplayString($setup.customTypes.length) + " 个分类",
              1
              /* TEXT */
            ),
            vue.createElementVNode("button", {
              class: "btn primary strong dock-save",
              loading: $setup.savingSort,
              disabled: $setup.savingSort || !$setup.hasSortChanged,
              onClick: $setup.saveSort
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "save",
                size: "16",
                color: "#fff"
              }),
              vue.createElementVNode("text", null, "保存排序")
            ], 8, ["loading", "disabled"])
          ])
        ])
      ]),
      _: 1
      /* STABLE */
    }, 8, ["show"]);
  }
  const __easycom_1$d = /* @__PURE__ */ _export_sfc(_sfc_main$1J, [["render", _sfc_render$1I], ["__scopeId", "data-v-c1304070"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/item-category-manager/item-category-manager.vue"]]);
  const _sfc_main$1I = {
    __name: "common-page",
    props: {
      head_color: {
        type: String,
        default: "#ffffff"
      }
    },
    emits: ["scroll"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      vue.useCssVars((_ctx) => ({
        "957ceee2-headerHeight": headerHeight.value,
        "957ceee2-head_color": __props.head_color,
        "957ceee2-footerHeight": footerHeight.value
      }));
      const emit = __emit;
      const systemInfo2 = uni.getSystemInfoSync();
      const navBarHeight = vue.computed(() => {
        return 0;
      });
      const props = __props;
      const headerHeight = vue.computed(() => {
        const statusBarHeight = systemInfo2.statusBarHeight || 0;
        return `${statusBarHeight}px`;
      });
      const footerHeight = vue.computed(() => {
        var _a;
        const safeBottom = ((_a = systemInfo2.safeAreaInsets) == null ? void 0 : _a.bottom) || 0;
        return `${safeBottom + 20}px`;
      });
      const __returned__ = { emit, systemInfo: systemInfo2, navBarHeight, props, headerHeight, footerHeight, computed: vue.computed };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1H(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "page-container" }, [
      vue.createElementVNode("view", { class: "header-placeholder" }),
      vue.createElementVNode("view", { class: "content-container" }, [
        vue.createElementVNode("view", { class: "main-content" }, [
          vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
        ])
      ]),
      vue.createElementVNode("view", { class: "footer-placeholder" })
    ]);
  }
  const __easycom_2$7 = /* @__PURE__ */ _export_sfc(_sfc_main$1I, [["render", _sfc_render$1H], ["__scopeId", "data-v-957ceee2"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-page/common-page.vue"]]);
  const _imports_0$b = "/static/new-icon/myitems.png";
  const _imports_1$8 = "/static/new-icon/showcase.png";
  const _imports_2$4 = "/static/new-icon/calendar.png";
  const _imports_3$2 = "/static/eye.png";
  const _imports_4 = "/static/6.png";
  const _sfc_main$1H = {
    __name: "stock",
    setup(__props, { expose: __expose }) {
      __expose();
      const typeModalVisible = vue.ref(false);
      const typeList = vue.ref([]);
      const myItemsRef = vue.ref(null);
      const systemInfo2 = uni.getSystemInfoSync();
      const statusBarHeight = vue.ref(systemInfo2.statusBarHeight);
      const activeTab = vue.ref(1);
      const previousTab = vue.ref(1);
      function transitionName() {
        return activeTab.value > 1 ? ["fade", "slide-left"] : ["fade", "slide-right"];
      }
      function switch_tab(index) {
        previousTab.value = activeTab.value;
        activeTab.value = index;
        if (activeTab.value !== 1)
          typeModalVisible.value = false;
        switch (index) {
          case 1:
            getAccountBookData();
            break;
          case 2:
            getShowcaseData();
            break;
          case 3:
            getBillData();
            break;
        }
      }
      const accountBookData = vue.ref({});
      const showcaseData = vue.ref({});
      const billData = vue.ref({});
      function countPaid(bills) {
        let totalAmount = 0, paidAmount = 0;
        bills.forEach((b2) => {
          const p2 = parseFloat(b2.price) || 0;
          totalAmount += p2;
          if (b2.status === 1)
            paidAmount += p2;
        });
        return `${paidAmount.toFixed(1)}/${totalAmount.toFixed(1)}`;
      }
      const handleTypeUpdate = (type) => {
        getAccountBookData(type);
      };
      function onTypesUpdated(list) {
        typeList.value = list || [];
        myItemsRef.value && myItemsRef.value.getAccountTypes && myItemsRef.value.getAccountTypes();
      }
      function onTypesRefresh() {
        getAccountBookData();
      }
      function handleFloatingButton() {
        switch (activeTab.value) {
          case 1:
            go2addAccountBook();
            break;
          case 2:
            go2addShowCase();
            break;
          case 3:
            go2addBill(false);
            break;
        }
      }
      function getAccountBookData(type) {
        if (!global$1.isLogin) {
          accountBookData.value = {};
          showcaseData.value = {};
          billData.value = {};
          return;
        }
        const token = uni.getStorageSync("token");
        let url = websiteUrl.value + "/with-state/account-book";
        if (type && type !== "全部")
          url = websiteUrl.value + "/with-state/account-book?type=" + encodeURIComponent(type);
        uni.request({
          url,
          method: "GET",
          header: { "Authorization": token },
          success: (res) => {
            accountBookData.value = res.data.data || {};
          }
        });
      }
      function getShowcaseData() {
        if (!global$1.isLogin)
          return;
        const token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/showcase",
          method: "GET",
          header: { "Authorization": token },
          success: (res) => {
            showcaseData.value = res.data.data || {};
          }
        });
      }
      function getBillData() {
        if (!global$1.isLogin)
          return;
        const token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/tail-bill",
          method: "GET",
          header: { "Authorization": token },
          success: (res) => {
            billData.value = res.data.data || {};
          }
        });
      }
      function jump2test() {
        uni.navigateTo({ url: "/pages/drag-image-test/drag-image-test" });
      }
      function go2addAccountBook() {
        uni.navigateTo({ url: "/pkg-stock/stock/account_book_form/account_book_form" });
      }
      function go2editor(id) {
        uni.navigateTo({ url: "/pkg-stock/stock/account_book_form/account_book_form?account_book_id=" + id });
      }
      function go2addShowCase() {
        uni.navigateTo({ url: "/pkg-stock/stock/showcase_form/showcase_form" });
      }
      function go2editorShowCase(id) {
        uni.navigateTo({ url: "/pkg-stock/stock/showcase_form/showcase_form?showcase_id=" + id });
      }
      function go2addBill(id) {
        if (id === false)
          return uni.navigateTo({ url: "/pkg-stock/stock/bill_form/bill_form" });
        uni.navigateTo({ url: "/pkg-stock/stock/bill_form/bill_form?bill_id=" + id });
      }
      function getStatusText(display) {
        const map = { 0: "审核中", 2: "人工复核中", 3: "违规隐藏中" };
        return map[display] || "";
      }
      onShow(() => {
        asyncGetUserInfo().then(() => {
          switch (activeTab.value) {
            case 1:
              getAccountBookData();
              break;
            case 2:
              getShowcaseData();
              break;
            case 3:
              getBillData();
              break;
          }
        });
      });
      const __returned__ = { typeModalVisible, typeList, myItemsRef, systemInfo: systemInfo2, statusBarHeight, activeTab, previousTab, transitionName, switch_tab, accountBookData, showcaseData, billData, countPaid, handleTypeUpdate, onTypesUpdated, onTypesRefresh, handleFloatingButton, getAccountBookData, getShowcaseData, getBillData, jump2test, go2addAccountBook, go2editor, go2addShowCase, go2editorShowCase, go2addBill, getStatusText, ref: vue.ref, computed: vue.computed, watch: vue.watch, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1G(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    const _component_stock_myitems = resolveEasycom(vue.resolveDynamicComponent("stock-myitems"), __easycom_2$8);
    const _component_uni_transition = resolveEasycom(vue.resolveDynamicComponent("uni-transition"), __easycom_3$9);
    const _component_item_category_manager = resolveEasycom(vue.resolveDynamicComponent("item-category-manager"), __easycom_1$d);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_page = resolveEasycom(vue.resolveDynamicComponent("common-page"), __easycom_2$7);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("meta", {
          name: "theme-color",
          content: "#d8deff"
        }),
        vue.createVNode(_component_view_logs),
        vue.createVNode(_component_bottom_popup, { show: false }),
        vue.createVNode(_component_common_page, { head_color: "#d8deff" }, {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "container",
              style: { "overflow": "hidden" }
            }, [
              vue.createElementVNode("view", { class: "head_container" }, [
                vue.createElementVNode("view", { class: "switch_tab" }, [
                  vue.createElementVNode(
                    "button",
                    {
                      onClick: _cache[0] || (_cache[0] = ($event) => $setup.switch_tab(1)),
                      class: vue.normalizeClass(["font-alimamashuhei", { "active": $setup.activeTab === 1 }])
                    },
                    [
                      vue.createElementVNode("image", {
                        class: "tab_img",
                        src: _imports_0$b,
                        mode: "widthFix",
                        style: { "width": "160rpx", "opacity": "0.9" }
                      })
                    ],
                    2
                    /* CLASS */
                  ),
                  vue.createElementVNode(
                    "button",
                    {
                      onClick: _cache[1] || (_cache[1] = ($event) => $setup.switch_tab(2)),
                      class: vue.normalizeClass(["font-alimamashuhei", { "active": $setup.activeTab === 2 }])
                    },
                    [
                      vue.createElementVNode("image", {
                        class: "tab_img",
                        src: _imports_1$8,
                        mode: "widthFix",
                        style: { "width": "130rpx", "opacity": "0.9" }
                      })
                    ],
                    2
                    /* CLASS */
                  ),
                  vue.createElementVNode(
                    "button",
                    {
                      onClick: _cache[2] || (_cache[2] = ($event) => $setup.switch_tab(3)),
                      class: vue.normalizeClass(["font-alimamashuhei", { "active": $setup.activeTab === 3 }])
                    },
                    [
                      vue.createElementVNode("image", {
                        class: "tab_img",
                        src: _imports_2$4,
                        mode: "widthFix",
                        style: { "width": "160rpx", "opacity": "0.9" }
                      })
                    ],
                    2
                    /* CLASS */
                  )
                ])
              ]),
              vue.createElementVNode("view", { class: "data_body" }, [
                vue.createVNode(_component_uni_transition, {
                  name: $setup.transitionName(),
                  "mode-class": ["fade", "slide-bottom"],
                  duration: 300,
                  show: $setup.activeTab === 1
                }, {
                  default: vue.withCtx(() => [
                    vue.createVNode(_component_stock_myitems, {
                      ref: "myItemsRef",
                      accountBookData: $setup.accountBookData,
                      "active-tab": $setup.activeTab,
                      onGo2editor: $setup.go2editor,
                      onUpdateType: $setup.handleTypeUpdate,
                      onOpenTypeManager: _cache[3] || (_cache[3] = ($event) => $setup.typeModalVisible = true)
                    }, null, 8, ["accountBookData", "active-tab"])
                  ]),
                  _: 1
                  /* STABLE */
                }, 8, ["name", "show"]),
                vue.createVNode(_component_uni_transition, {
                  name: $setup.transitionName(),
                  "mode-class": ["fade", "slide-bottom"],
                  duration: 300,
                  show: $setup.activeTab === 2
                }, {
                  default: vue.withCtx(() => [
                    vue.createElementVNode("view", { class: "tab_body_sec" }, [
                      $setup.showcaseData.showcases && $setup.showcaseData.showcases.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "showcase-container"
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.showcaseData.showcases, (item, index) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              key: index,
                              class: "showcase-card",
                              onClick: ($event) => $setup.go2editorShowCase(item.id)
                            }, [
                              item.display !== 1 ? (vue.openBlock(), vue.createElementBlock("view", {
                                key: 0,
                                class: "status-overlay"
                              }, [
                                vue.createElementVNode(
                                  "text",
                                  { class: "status-text" },
                                  vue.toDisplayString($setup.getStatusText(item.display)),
                                  1
                                  /* TEXT */
                                )
                              ])) : vue.createCommentVNode("v-if", true),
                              vue.createElementVNode(
                                "view",
                                {
                                  class: vue.normalizeClass(["card-content", { "blur-effect": item.display !== 1 }])
                                },
                                [
                                  vue.createElementVNode("image", {
                                    src: item.image_url_list[0],
                                    mode: "aspectFill",
                                    class: "showcase-image"
                                  }, null, 8, ["src"]),
                                  vue.createElementVNode("view", { class: "showcase-info" }, [
                                    vue.createElementVNode(
                                      "text",
                                      { class: "showcase-title one_line_text" },
                                      vue.toDisplayString(item.name),
                                      1
                                      /* TEXT */
                                    ),
                                    vue.createElementVNode(
                                      "text",
                                      { class: "showcase-description multi_line_text" },
                                      vue.toDisplayString(item.description),
                                      1
                                      /* TEXT */
                                    ),
                                    vue.createElementVNode("view", { class: "interaction-bar" }, [
                                      vue.createElementVNode("view", { class: "interaction-item" }, [
                                        vue.createElementVNode("image", {
                                          src: _imports_3$2,
                                          class: "interaction-icon"
                                        }),
                                        vue.createElementVNode(
                                          "text",
                                          { class: "interaction-count" },
                                          vue.toDisplayString(item.views || 0),
                                          1
                                          /* TEXT */
                                        )
                                      ]),
                                      vue.createElementVNode("view", { class: "interaction-item" }, [
                                        vue.createElementVNode("image", {
                                          src: _imports_4,
                                          class: "interaction-icon"
                                        }),
                                        vue.createElementVNode(
                                          "text",
                                          { class: "interaction-count" },
                                          vue.toDisplayString(item.like_count || 0),
                                          1
                                          /* TEXT */
                                        )
                                      ])
                                    ])
                                  ])
                                ],
                                2
                                /* CLASS */
                              )
                            ], 8, ["onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])) : (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "empty-state"
                      }, [
                        vue.createElementVNode("image", {
                          class: "empty-icon",
                          src: _imports_0$d
                        }),
                        vue.createElementVNode("text", { class: "empty-text" }, "展示柜空空如也"),
                        vue.createElementVNode("text", { class: "empty-tip" }, "快来创建你的展示空间吧！")
                      ]))
                    ])
                  ]),
                  _: 1
                  /* STABLE */
                }, 8, ["name", "show"]),
                vue.createVNode(_component_uni_transition, {
                  name: $setup.transitionName(),
                  "mode-class": ["fade", "slide-bottom"],
                  duration: 300,
                  show: $setup.activeTab === 3
                }, {
                  default: vue.withCtx(() => [
                    vue.createElementVNode("view", { class: "tab_body_3th" }, [
                      Object.keys($setup.billData).length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "calendar-container"
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.billData, (bills, month) => {
                            return vue.openBlock(), vue.createElementBlock("view", { key: month }, [
                              vue.createElementVNode("view", { class: "month-header-container" }, [
                                vue.createElementVNode(
                                  "text",
                                  { class: "month-header font-alimamashuhei" },
                                  vue.toDisplayString(month) + " 账单",
                                  1
                                  /* TEXT */
                                ),
                                vue.createElementVNode(
                                  "text",
                                  { class: "month-stats" },
                                  vue.toDisplayString($setup.countPaid(bills)),
                                  1
                                  /* TEXT */
                                )
                              ]),
                              (vue.openBlock(true), vue.createElementBlock(
                                vue.Fragment,
                                null,
                                vue.renderList(bills, (bill) => {
                                  return vue.openBlock(), vue.createElementBlock("view", {
                                    key: bill.id,
                                    class: "bill-item",
                                    onClick: ($event) => $setup.go2addBill(bill.id)
                                  }, [
                                    vue.createElementVNode("view", { class: "bill-left" }, [
                                      vue.createElementVNode("view", { class: "bill-name-container" }, [
                                        vue.createElementVNode(
                                          "text",
                                          { class: "bill-name" },
                                          vue.toDisplayString(bill.name),
                                          1
                                          /* TEXT */
                                        )
                                      ]),
                                      vue.createElementVNode("view", { class: "due-amount-container" }, [
                                        vue.createElementVNode("text", { class: "due-label" }, "待补款:"),
                                        vue.createElementVNode(
                                          "text",
                                          { class: "due-amount" },
                                          "¥" + vue.toDisplayString(bill.price),
                                          1
                                          /* TEXT */
                                        )
                                      ])
                                    ]),
                                    vue.createElementVNode("view", { class: "bill-right" }, [
                                      vue.createElementVNode("view", { class: "date-container" }, [
                                        vue.createElementVNode(
                                          "text",
                                          { class: "date-text" },
                                          "日期: " + vue.toDisplayString(bill.ymd),
                                          1
                                          /* TEXT */
                                        )
                                      ]),
                                      vue.createElementVNode("view", { class: "status-container" }, [
                                        vue.createElementVNode(
                                          "text",
                                          {
                                            class: vue.normalizeClass(["status-text", { "paid": bill.status === 1 }])
                                          },
                                          vue.toDisplayString(bill.status === 0 ? "未补款" : "已补款"),
                                          3
                                          /* TEXT, CLASS */
                                        )
                                      ])
                                    ])
                                  ], 8, ["onClick"]);
                                }),
                                128
                                /* KEYED_FRAGMENT */
                              ))
                            ]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])) : (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "empty-state"
                      }, [
                        vue.createElementVNode("image", {
                          class: "empty-icon",
                          src: _imports_0$d
                        }),
                        vue.createElementVNode("text", { class: "empty-text" }, "暂无待补尾款"),
                        vue.createElementVNode("text", { class: "empty-tip" }, "增加添加一个到账本试试吧～")
                      ]))
                    ])
                  ]),
                  _: 1
                  /* STABLE */
                }, 8, ["name", "show"])
              ])
            ]),
            vue.createVNode(_component_item_category_manager, {
              modelValue: $setup.typeModalVisible,
              "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.typeModalVisible = $event),
              list: $setup.typeList,
              onUpdated: $setup.onTypesUpdated,
              onRefresh: $setup.onTypesRefresh
            }, null, 8, ["modelValue", "list"]),
            vue.createElementVNode("view", {
              class: "unified-floating-button",
              onClick: $setup.handleFloatingButton
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plusempty",
                size: "30",
                color: "#fff"
              })
            ])
          ]),
          _: 1
          /* STABLE */
        })
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesStockStock = /* @__PURE__ */ _export_sfc(_sfc_main$1H, [["render", _sfc_render$1G], ["__scopeId", "data-v-4829e7f4"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/stock/stock.vue"]]);
  const pageSize$6 = 20;
  const _sfc_main$1G = {
    __name: "orders-panel",
    props: {
      active: {
        type: Boolean,
        default: true
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const statusTabs = [
        { key: "all", label: "全部" },
        { key: "ongoing", label: "交易中" },
        { key: "finished", label: "交易结束" }
      ];
      const activeStatusTab = vue.ref("all");
      const submissionList = vue.ref([]);
      const page = vue.ref(1);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const finished = vue.computed(
        () => total.value > 0 && submissionList.value.length >= total.value
      );
      function getOrderType(sub) {
        if (!sub)
          return 0;
        return Number(sub.order_type ?? sub.OrderType ?? 0);
      }
      const totalBjd = vue.computed(
        () => submissionList.value.filter((row) => getOrderType(row.submission) === 1).length
      );
      const totalHair = vue.computed(
        () => submissionList.value.filter((row) => getOrderType(row.submission) === 2).length
      );
      async function ensureLogin() {
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          global$1.isLogin = false;
          return false;
        }
        if (!global$1.isLogin && typeof asyncGetUserInfo === "function") {
          await asyncGetUserInfo();
        }
        return !!global$1.isLogin;
      }
      function getSubmissionStatus(sub) {
        if (!sub)
          return 0;
        return Number(sub.status ?? sub.Status ?? 0);
      }
      function isOngoing(sub) {
        const st2 = getSubmissionStatus(sub);
        return st2 === 0 || st2 === 1 || st2 === 2;
      }
      function isFinishedStatus(sub) {
        const st2 = getSubmissionStatus(sub);
        return st2 === 3 || st2 === 4 || st2 === 5;
      }
      const filteredList = vue.computed(() => {
        let list = submissionList.value;
        if (activeStatusTab.value === "ongoing") {
          list = list.filter((row) => isOngoing(row.submission));
        } else if (activeStatusTab.value === "finished") {
          list = list.filter((row) => isFinishedStatus(row.submission));
        }
        return list;
      });
      function changeStatusTab(key) {
        activeStatusTab.value = key;
      }
      async function fetchList(reset = false) {
        var _a, _b;
        if (loading2.value)
          return;
        const ok = await ensureLogin();
        if (!ok) {
          if (reset) {
            submissionList.value = [];
            page.value = 1;
            total.value = 0;
          }
          uni.stopPullDownRefresh();
          return;
        }
        if (reset) {
          page.value = 1;
          submissionList.value = [];
          total.value = 0;
        }
        loading2.value = true;
        try {
          const token = uni.getStorageSync("token") || "";
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/my-submissions`,
            method: "GET",
            header: {
              Authorization: token
            },
            data: {
              page: page.value,
              page_size: pageSize$6,
              size: pageSize$6
            }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败", icon: "none" });
            return;
          }
          const data = res.data.data || {};
          const rows = data.list || [];
          const p2 = data.page || {};
          if (reset) {
            submissionList.value = rows;
          } else {
            submissionList.value = submissionList.value.concat(rows);
          }
          total.value = Number(p2.total || total.value || rows.length || 0);
          const currentPage = Number(p2.page || page.value);
          page.value = currentPage + 1;
        } catch (e2) {
          formatAppLog("error", "at components/orders-panel/orders-panel.vue:283", "加载投递列表失败", e2);
          uni.showToast({ title: "网络异常，加载失败", icon: "none" });
        } finally {
          loading2.value = false;
          uni.stopPullDownRefresh();
        }
      }
      function getBrandObj(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        return sub.brand || sub.Brand || {};
      }
      function brandName(row) {
        const b2 = getBrandObj(row);
        return b2.brand_name || b2.BrandName || "未知品牌";
      }
      function brandLogo(row) {
        const b2 = getBrandObj(row);
        return b2.logo_image || b2.LogoImage || "";
      }
      function firstRefImage(it2) {
        const raw = it2.ref_images || it2.RefImages || "";
        if (!raw)
          return "";
        const arr = String(raw).split(",");
        return arr[0].trim();
      }
      function statusText(row) {
        const st2 = getSubmissionStatus(row == null ? void 0 : row.submission);
        switch (st2) {
          case 0:
            return "排队中";
          case 1:
            return "已抢到名额";
          case 2:
            return "投递成功";
          case 3:
            return "未抢到名额";
          case 4:
            return "已取消";
          case 5:
            return "支付超时";
          default:
            return "未知状态";
        }
      }
      function cardCover(row) {
        const items = row.items || [];
        if (items.length > 0) {
          const img = firstRefImage(items[0]);
          if (img)
            return img;
        }
        const logo = brandLogo(row);
        if (logo)
          return logo;
        return "";
      }
      function mainTitle(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        return sub.title || sub.Title || sub.plan_title || sub.PlanTitle || brandName(row);
      }
      function itemsCount(row) {
        const items = row.items || [];
        return items.length || 0;
      }
      function cardAmount(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        const totalVal = sub.price_total ?? sub.PriceTotal ?? sub.total_price ?? sub.TotalPrice ?? 0;
        const n2 = Number(totalVal) || 0;
        return n2.toFixed(2).replace(/\.00$/, "");
      }
      function statusPillText(row) {
        const txt = statusText(row);
        if (txt === "已抢到名额")
          return "已中选";
        return txt;
      }
      function goSubmissionDetail(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        const id = sub.id || sub.ID;
        if (!id)
          return;
        const url = `/pkg-creator/creator_order/submission_detail/submission_detail?submission_id=${id}`;
        uni.navigateTo({ url });
      }
      function jumpToAnnouncement() {
        uni.navigateTo({
          url: "/pages/article_detail/article_detail?id=3"
        });
      }
      vue.onMounted(() => {
        if (props.active) {
          fetchList(true);
        }
      });
      onShow(() => {
        if (props.active) {
          fetchList(true);
        }
      });
      vue.watch(
        () => props.active,
        (val) => {
          if (val) {
            fetchList(true);
          }
        }
      );
      onPullDownRefresh(() => {
        if (!props.active) {
          uni.stopPullDownRefresh();
          return;
        }
        fetchList(true);
      });
      onReachBottom(() => {
        if (!props.active || finished.value)
          return;
        fetchList(false);
      });
      const __returned__ = { props, statusTabs, activeStatusTab, submissionList, page, pageSize: pageSize$6, total, loading: loading2, finished, getOrderType, totalBjd, totalHair, ensureLogin, getSubmissionStatus, isOngoing, isFinishedStatus, filteredList, changeStatusTab, fetchList, getBrandObj, brandName, brandLogo, firstRefImage, statusText, cardCover, mainTitle, itemsCount, cardAmount, statusPillText, goSubmissionDetail, jumpToAnnouncement, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, watch: vue.watch, get onShow() {
        return onShow;
      }, get onPullDownRefresh() {
        return onPullDownRefresh;
      }, get onReachBottom() {
        return onReachBottom;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1F(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "orders-panel" }, [
      vue.createElementVNode("view", { class: "summary-card" }, [
        vue.createElementVNode("view", { class: "summary-item" }, [
          vue.createElementVNode(
            "text",
            { class: "summary-number font-alimama" },
            vue.toDisplayString($setup.totalBjd),
            1
            /* TEXT */
          ),
          vue.createElementVNode("text", { class: "summary-label font-alimama" }, "BJD约妆")
        ]),
        vue.createElementVNode("view", { class: "summary-item" }, [
          vue.createElementVNode(
            "text",
            { class: "summary-number font-alimama" },
            vue.toDisplayString($setup.totalHair),
            1
            /* TEXT */
          ),
          vue.createElementVNode("text", { class: "summary-label font-alimama" }, "手改毛投递")
        ])
      ]),
      vue.createElementVNode("view", { class: "second-tabs" }, [
        (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.statusTabs, (tab) => {
            return vue.createElementVNode("view", {
              key: tab.key,
              class: vue.normalizeClass(["second-tab", { "second-tab-active": $setup.activeStatusTab === tab.key }]),
              onClick: ($event) => $setup.changeStatusTab(tab.key)
            }, [
              vue.createElementVNode(
                "text",
                { class: "second-tab-text font-alimama" },
                vue.toDisplayString(tab.label),
                1
                /* TEXT */
              )
            ], 10, ["onClick"]);
          }),
          64
          /* STABLE_FRAGMENT */
        ))
      ]),
      vue.createElementVNode("view", { class: "orders-list-wrap" }, [
        !$setup.loading && $setup.filteredList.length === 0 && $setup.submissionList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "empty-wrap"
        }, [
          vue.createElementVNode("view", { class: "empty-card" }, [
            vue.createElementVNode("text", { class: "empty-title" }, "还没有投递记录"),
            vue.createElementVNode("text", { class: "empty-desc" }, " 去找喜欢的妆师 / 毛娘，投递你的第一个方案吧。 ")
          ])
        ])) : !$setup.loading && $setup.filteredList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "empty-wrap status-empty"
        }, [
          vue.createElementVNode("view", { class: "empty-card" }, [
            vue.createElementVNode("text", { class: "empty-title" }, "当前状态暂无投递"),
            vue.createElementVNode("text", { class: "empty-desc" }, "换个筛选看看其他投递记录。")
          ])
        ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 2 }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.filteredList, (row) => {
              var _a, _b;
              return vue.openBlock(), vue.createElementBlock("view", {
                key: ((_a = row.submission) == null ? void 0 : _a.id) || ((_b = row.submission) == null ? void 0 : _b.ID),
                class: "sub-card",
                onClick: ($event) => $setup.goSubmissionDetail(row)
              }, [
                vue.createElementVNode("view", { class: "sub-card-left" }, [
                  vue.createElementVNode("image", {
                    class: "sub-thumb",
                    src: $setup.cardCover(row),
                    mode: "aspectFill"
                  }, null, 8, ["src"])
                ]),
                vue.createElementVNode("view", { class: "sub-card-middle" }, [
                  vue.createElementVNode("view", { class: "sub-title-row" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "status-pill font-alimama" },
                      vue.toDisplayString($setup.statusPillText(row)),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "sub-main-title" },
                      vue.toDisplayString($setup.mainTitle(row)),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "sub-subtitle-row" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "sub-meta-text" },
                      vue.toDisplayString($setup.itemsCount(row)) + "个投递项 ",
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "sub-meta-text price-text" },
                      vue.toDisplayString($setup.cardAmount(row)) + "¥ ",
                      1
                      /* TEXT */
                    )
                  ])
                ]),
                vue.createElementVNode("view", { class: "sub-card-right" }, [
                  vue.createElementVNode("text", { class: "more-dots" }, "···"),
                  vue.createVNode(_component_uni_icons, {
                    type: "right",
                    size: "26",
                    color: "#444444"
                  })
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])),
        vue.createElementVNode("view", { class: "list-footer" }, [
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", {
            key: 0,
            class: "footer-text"
          }, "加载中...")) : $setup.finished && $setup.submissionList.length > 0 ? (vue.openBlock(), vue.createElementBlock("text", {
            key: 1,
            class: "footer-text"
          }, " 已经到底了 ")) : vue.createCommentVNode("v-if", true)
        ])
      ]),
      vue.createElementVNode("view", {
        class: "platform-announcement",
        onClick: $setup.jumpToAnnouncement
      }, [
        vue.createElementVNode("text", { class: "announcement-text" }, "平台公告")
      ])
    ]);
  }
  const __easycom_1$c = /* @__PURE__ */ _export_sfc(_sfc_main$1G, [["render", _sfc_render$1F], ["__scopeId", "data-v-7233b275"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/orders-panel/orders-panel.vue"]]);
  const _sfc_main$1F = {
    __name: "loading-jump-text",
    setup(__props, { expose: __expose }) {
      __expose();
      const chars = ["加", "载", "中"];
      const __returned__ = { chars };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1E(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "loading-jump font-alimamashuhei" }, [
      (vue.openBlock(), vue.createElementBlock(
        vue.Fragment,
        null,
        vue.renderList($setup.chars, (ch, index) => {
          return vue.createElementVNode(
            "text",
            {
              key: index,
              class: "loading-char",
              style: vue.normalizeStyle({ animationDelay: index * 120 + "ms" })
            },
            vue.toDisplayString(ch),
            5
            /* TEXT, STYLE */
          );
        }),
        64
        /* STABLE_FRAGMENT */
      )),
      vue.createElementVNode("text", { class: "loading-dots" }, "...")
    ]);
  }
  const LoadingJumpText = /* @__PURE__ */ _export_sfc(_sfc_main$1F, [["render", _sfc_render$1E], ["__scopeId", "data-v-e2c1fb30"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/loading-jump-text/loading-jump-text.vue"]]);
  const brandPageSize = 10;
  const goodsPageSize = 10;
  const defaultImg$1 = "https://images1.fantuanpu.com/home/jpt.gif";
  const _sfc_main$1E = {
    __name: "info-follow-tab",
    props: {
      active: {
        type: Boolean,
        default: true
      }
    },
    emits: ["update-stats"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const emit = __emit;
      const props = __props;
      const subTabs = [
        { key: "brand", label: "品牌" },
        { key: "goods", label: "商品" },
        { key: "mix", label: "搭配" },
        { key: "display", label: "展示柜" }
      ];
      const activeSubTab = vue.ref("brand");
      const brandList = vue.ref([]);
      const brandPage = vue.ref(1);
      const brandTotal = vue.ref(0);
      const brandLoading = vue.ref(false);
      const brandFinished = vue.ref(false);
      const goodsList = vue.ref([]);
      const goodsPage = vue.ref(1);
      const goodsTotal = vue.ref(0);
      const goodsLoading = vue.ref(false);
      const goodsFinished = vue.ref(false);
      function resolveImage(url) {
        if (!url)
          return defaultImg$1;
        if (typeof url === "string" && url.indexOf("http") === 0)
          return url;
        return image1Url + url;
      }
      function formatTime(ts2) {
        if (!ts2)
          return "--";
        const d2 = new Date(ts2 * 1e3);
        if (isNaN(d2.getTime()))
          return "--";
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0");
        const mm = String(d2.getMinutes()).padStart(2, "0");
        return y2 + "-" + m2 + "-" + day + " " + hh + ":" + mm;
      }
      function getWaitingCount(brand) {
        const list = brand.waiting_goods || [];
        return Array.isArray(list) ? list.length : 0;
      }
      function itemTitle(item) {
        return item.title || item.goods_title || item.goods_name || item.name || "未命名商品";
      }
      function brandItemStyle(index) {
        const delay2 = index * 80;
        return {
          animationDelay: delay2 + "ms"
        };
      }
      function goodsItemStyle(index) {
        const delay2 = index * 80;
        return {
          animationDelay: delay2 + "ms"
        };
      }
      function changeSubTab(key) {
        activeSubTab.value = key;
      }
      vue.watch(
        () => activeSubTab.value,
        (key) => {
          if (key === "brand" && brandList.value.length === 0) {
            loadBrand(true);
          }
          if (key === "goods" && goodsList.value.length === 0) {
            loadGoods(true);
          }
        }
      );
      function loadBrand(reset) {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        if (brandLoading.value)
          return;
        if (!reset && brandFinished.value)
          return;
        if (reset) {
          brandPage.value = 1;
          brandFinished.value = false;
          brandList.value = [];
        }
        brandLoading.value = true;
        uni.request({
          url: websiteUrl.value + "/with-state/my-liked-brands",
          method: "GET",
          header: {
            Authorization: token
          },
          data: {
            page: brandPage.value,
            page_size: brandPageSize
          },
          success: (res) => {
            const resp = res.data || {};
            if (resp.status !== "success") {
              uni.showToast({
                title: resp.msg || "获取关注品牌失败",
                icon: "none"
              });
              return;
            }
            const data = resp.data || {};
            const list = data.list || [];
            const total = Number(data.total || 0);
            if (reset) {
              brandList.value = list;
            } else {
              brandList.value = brandList.value.concat(list);
            }
            brandTotal.value = total;
            if (brandList.value.length >= total || list.length < brandPageSize) {
              brandFinished.value = true;
            } else {
              brandPage.value = brandPage.value + 1;
            }
            emit("update-stats", { followTotal: total });
          },
          fail: () => {
            uni.showToast({
              title: "网络错误，获取关注品牌失败",
              icon: "none"
            });
          },
          complete: () => {
            brandLoading.value = false;
          }
        });
      }
      function loadBrandMore() {
        loadBrand(false);
      }
      function loadGoods(reset) {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        if (goodsLoading.value)
          return;
        if (!reset && goodsFinished.value)
          return;
        if (reset) {
          goodsPage.value = 1;
          goodsFinished.value = false;
          goodsList.value = [];
        }
        goodsLoading.value = true;
        uni.request({
          url: websiteUrl.value + "/with-state/my-liked-goods",
          method: "GET",
          header: {
            Authorization: token
          },
          data: {
            page: goodsPage.value,
            page_size: goodsPageSize
          },
          success: (res) => {
            const resp = res.data || {};
            if (resp.status !== "success") {
              uni.showToast({
                title: resp.msg || "获取关注商品失败",
                icon: "none"
              });
              return;
            }
            const data = resp.data || {};
            const list = data.list || [];
            const total = Number(data.total || 0);
            if (reset) {
              goodsList.value = list;
            } else {
              goodsList.value = goodsList.value.concat(list);
            }
            goodsTotal.value = total;
            if (goodsList.value.length >= total || list.length < goodsPageSize) {
              goodsFinished.value = true;
            } else {
              goodsPage.value = goodsPage.value + 1;
            }
          },
          fail: () => {
            uni.showToast({
              title: "网络错误，获取关注商品失败",
              icon: "none"
            });
          },
          complete: () => {
            goodsLoading.value = false;
          }
        });
      }
      function loadGoodsMore() {
        loadGoods(false);
      }
      function jump2brand(id) {
        uni.navigateTo({ url: "/pages/brand/brand?brand_id=" + id });
      }
      function goBrandDetail(item) {
        if (!item || !item.id)
          return;
        jump2brand(item.id);
      }
      function goGoodsDetail(item) {
        uni.showToast({
          title: "商品详情开发中",
          icon: "none"
        });
      }
      vue.onMounted(() => {
        if (props.active && activeSubTab.value === "brand") {
          loadBrand(true);
        }
      });
      const __returned__ = { emit, props, subTabs, activeSubTab, brandList, brandPage, brandPageSize, brandTotal, brandLoading, brandFinished, goodsList, goodsPage, goodsPageSize, goodsTotal, goodsLoading, goodsFinished, defaultImg: defaultImg$1, resolveImage, formatTime, getWaitingCount, itemTitle, brandItemStyle, goodsItemStyle, changeSubTab, loadBrand, loadBrandMore, loadGoods, loadGoodsMore, jump2brand, goBrandDetail, goGoodsDetail, ref: vue.ref, watch: vue.watch, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1D(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_loading_jump_text = resolveEasycom(vue.resolveDynamicComponent("loading-jump-text"), LoadingJumpText);
    return vue.openBlock(), vue.createElementBlock("view", { class: "follow-tab" }, [
      vue.createElementVNode("view", { class: "follow-sub-tabs" }, [
        (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.subTabs, (tab) => {
            return vue.createElementVNode("view", {
              key: tab.key,
              class: vue.normalizeClass(["sub-tab-item", { "sub-tab-item-active": $setup.activeSubTab === tab.key }]),
              onClick: ($event) => $setup.changeSubTab(tab.key)
            }, [
              vue.createElementVNode(
                "text",
                { class: "sub-tab-text font-alimamashuhei" },
                vue.toDisplayString(tab.label),
                1
                /* TEXT */
              )
            ], 10, ["onClick"]);
          }),
          64
          /* STABLE_FRAGMENT */
        ))
      ]),
      $setup.activeSubTab === "brand" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "list-wrap"
      }, [
        !$setup.brandLoading && $setup.brandList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "empty-box"
        }, [
          vue.createElementVNode("text", { class: "empty-text font-alimamashuhei" }, "还没有关注任何品牌哦")
        ])) : vue.createCommentVNode("v-if", true),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.brandList, (item, index) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: item.id,
              class: "brand-item",
              style: vue.normalizeStyle($setup.brandItemStyle(index)),
              onClick: ($event) => $setup.goBrandDetail(item)
            }, [
              vue.createElementVNode("view", { class: "brand-avatar" }, [
                vue.createElementVNode("image", {
                  class: "brand-avatar-img",
                  src: $setup.resolveImage(item.logo_image),
                  mode: "aspectFill"
                }, null, 8, ["src"])
              ]),
              vue.createElementVNode("view", { class: "brand-main" }, [
                vue.createElementVNode(
                  "text",
                  { class: "brand-name font-alimamashuhei" },
                  vue.toDisplayString(item.brand_name || "未命名品牌"),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("view", { class: "brand-meta" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "meta-tag font-alimamashuhei" },
                    "待开售 " + vue.toDisplayString($setup.getWaitingCount(item)),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("text", { class: "meta-label font-alimamashuhei" }, "新情报"),
                  vue.createElementVNode(
                    "text",
                    { class: "meta-time font-alimamashuhei" },
                    vue.toDisplayString($setup.formatTime(item.latest_news_time)),
                    1
                    /* TEXT */
                  )
                ])
              ]),
              vue.createElementVNode("view", { class: "brand-arrow" }, [
                vue.createElementVNode("text", { class: "arrow-text font-alimamashuhei" }, "去看看"),
                vue.createElementVNode("text", { class: "arrow-symbol font-alimamashuhei" }, "→")
              ])
            ], 12, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        )),
        $setup.brandLoading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "loading-box"
        }, [
          vue.createVNode(_component_loading_jump_text)
        ])) : !$setup.brandFinished && $setup.brandList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "load-more-box",
          onClick: $setup.loadBrandMore
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, "点击加载更多")
        ])) : $setup.brandFinished && $setup.brandList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "load-more-box"
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, "已经到底啦")
        ])) : vue.createCommentVNode("v-if", true)
      ])) : $setup.activeSubTab === "goods" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "list-wrap"
      }, [
        !$setup.goodsLoading && $setup.goodsList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "empty-box"
        }, [
          vue.createElementVNode("text", { class: "empty-text font-alimamashuhei" }, "还没有关注任何商品哦")
        ])) : vue.createCommentVNode("v-if", true),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.goodsList, (item, index) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: item.id,
              class: "goods-item",
              style: vue.normalizeStyle($setup.goodsItemStyle(index)),
              onClick: ($event) => $setup.goGoodsDetail(item)
            }, [
              vue.createElementVNode("view", { class: "goods-thumb" }, [
                vue.createElementVNode("image", {
                  class: "goods-thumb-img",
                  src: $setup.resolveImage(item.first_image),
                  mode: "aspectFill"
                }, null, 8, ["src"])
              ]),
              vue.createElementVNode("view", { class: "goods-main" }, [
                vue.createElementVNode(
                  "text",
                  { class: "goods-title font-alimamashuhei" },
                  vue.toDisplayString($setup.itemTitle(item)),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "goods-brand font-alimamashuhei" },
                  vue.toDisplayString(item.brand_name || "未知品牌"),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "brand-arrow" }, [
                vue.createElementVNode("text", { class: "arrow-text font-alimamashuhei" }, "去看看"),
                vue.createElementVNode("text", { class: "arrow-symbol font-alimamashuhei" }, "→")
              ])
            ], 12, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        )),
        $setup.goodsLoading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "loading-box"
        }, [
          vue.createVNode(_component_loading_jump_text)
        ])) : !$setup.goodsFinished && $setup.goodsList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "load-more-box",
          onClick: $setup.loadGoodsMore
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, "点击加载更多")
        ])) : $setup.goodsFinished && $setup.goodsList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "load-more-box"
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, "已经到底啦")
        ])) : vue.createCommentVNode("v-if", true)
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        class: "empty-box"
      }, [
        vue.createElementVNode("text", { class: "empty-text font-alimamashuhei" }, "该模块后续接入～")
      ]))
    ]);
  }
  const __easycom_0$6 = /* @__PURE__ */ _export_sfc(_sfc_main$1E, [["render", _sfc_render$1D], ["__scopeId", "data-v-2b3f4e8b"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/info-follow-tab/info-follow-tab.vue"]]);
  let mpMixins = {};
  mpMixins = {
    data() {
      return {
        is_show: "none"
      };
    },
    watch: {
      show(newVal) {
        this.is_show = this.show;
      }
    },
    created() {
      this.swipeaction = this.getSwipeAction();
      if (this.swipeaction && Array.isArray(this.swipeaction.children)) {
        this.swipeaction.children.push(this);
      }
    },
    mounted() {
      this.is_show = this.show;
    },
    methods: {
      // wxs 中调用
      closeSwipe(e2) {
        if (this.autoClose && this.swipeaction) {
          this.swipeaction.closeOther(this);
        }
      },
      change(e2) {
        this.$emit("change", e2.open);
        if (this.is_show !== e2.open) {
          this.is_show = e2.open;
        }
      },
      appTouchStart(e2) {
        const {
          clientX
        } = e2.changedTouches[0];
        this.clientX = clientX;
        this.timestamp = (/* @__PURE__ */ new Date()).getTime();
      },
      appTouchEnd(e2, index, item, position) {
        const {
          clientX
        } = e2.changedTouches[0];
        let diff = Math.abs(this.clientX - clientX);
        let time = (/* @__PURE__ */ new Date()).getTime() - this.timestamp;
        if (diff < 40 && time < 300) {
          this.$emit("click", {
            content: item,
            index,
            position
          });
        }
      },
      onClickForPC(index, item, position) {
        return;
      }
    }
  };
  const mpwxs = mpMixins;
  let bindIngXMixins = {};
  let otherMixins = {};
  const block0$2 = (Comp) => {
    (Comp.$wxs || (Comp.$wxs = [])).push("wxsswipe");
    (Comp.$wxsModules || (Comp.$wxsModules = {}))["wxsswipe"] = "afd46426";
  };
  const block1 = (Comp) => {
    (Comp.$renderjs || (Comp.$renderjs = [])).push("renderswipe");
    (Comp.$renderjsModules || (Comp.$renderjsModules = {}))["renderswipe"] = "5a1e922e";
  };
  const _sfc_main$1D = {
    mixins: [mpwxs, bindIngXMixins, otherMixins],
    emits: ["click", "change"],
    props: {
      // 控制开关
      show: {
        type: String,
        default: "none"
      },
      // 禁用
      disabled: {
        type: Boolean,
        default: false
      },
      // 是否自动关闭
      autoClose: {
        type: Boolean,
        default: true
      },
      // 滑动缺省距离
      threshold: {
        type: Number,
        default: 20
      },
      // 左侧按钮内容
      leftOptions: {
        type: Array,
        default() {
          return [];
        }
      },
      // 右侧按钮内容
      rightOptions: {
        type: Array,
        default() {
          return [];
        }
      }
    },
    // TODO vue3
    unmounted() {
      this.__isUnmounted = true;
      this.uninstall();
    },
    methods: {
      uninstall() {
        if (this.swipeaction) {
          this.swipeaction.children.forEach((item, index) => {
            if (item === this) {
              this.swipeaction.children.splice(index, 1);
            }
          });
        }
      },
      /**
       * 获取父元素实例
       */
      getSwipeAction(name = "uniSwipeAction") {
        let parent = this.$parent;
        let parentName = parent.$options.name;
        while (parentName !== name) {
          parent = parent.$parent;
          if (!parent)
            return false;
          parentName = parent.$options.name;
        }
        return parent;
      }
    }
  };
  function _sfc_render$1C(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "uni-swipe" }, [
      vue.createElementVNode("view", {
        class: "uni-swipe_box",
        "change:prop": _ctx.wxsswipe.showWatch,
        prop: vue.wp(_ctx.is_show),
        "data-threshold": $props.threshold,
        "data-disabled": $props.disabled,
        onTouchstart: _cache[2] || (_cache[2] = (...args) => _ctx.wxsswipe.touchstart && _ctx.wxsswipe.touchstart(...args)),
        onTouchmove: _cache[3] || (_cache[3] = (...args) => _ctx.wxsswipe.touchmove && _ctx.wxsswipe.touchmove(...args)),
        onTouchend: _cache[4] || (_cache[4] = (...args) => _ctx.wxsswipe.touchend && _ctx.wxsswipe.touchend(...args))
      }, [
        vue.createElementVNode("view", { class: "uni-swipe_button-group button-group--left" }, [
          vue.renderSlot(_ctx.$slots, "left", {}, () => [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($props.leftOptions, (item, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  style: vue.normalizeStyle({
                    backgroundColor: item.style && item.style.backgroundColor ? item.style.backgroundColor : "#C7C6CD"
                  }),
                  class: "uni-swipe_button button-hock",
                  onTouchstart: _cache[0] || (_cache[0] = vue.withModifiers((...args) => _ctx.appTouchStart && _ctx.appTouchStart(...args), ["stop"])),
                  onTouchend: vue.withModifiers(($event) => _ctx.appTouchEnd($event, index, item, "left"), ["stop"]),
                  onClick: vue.withModifiers(($event) => _ctx.onClickForPC(index, item, "left"), ["stop"])
                }, [
                  vue.createElementVNode(
                    "text",
                    {
                      class: "uni-swipe_button-text",
                      style: vue.normalizeStyle({ color: item.style && item.style.color ? item.style.color : "#FFFFFF", fontSize: item.style && item.style.fontSize ? item.style.fontSize : "16px" })
                    },
                    vue.toDisplayString(item.text),
                    5
                    /* TEXT, STYLE */
                  )
                ], 44, ["onTouchend", "onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ], true)
        ]),
        vue.createElementVNode("view", { class: "uni-swipe_text--center" }, [
          vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
        ]),
        vue.createElementVNode("view", { class: "uni-swipe_button-group button-group--right" }, [
          vue.renderSlot(_ctx.$slots, "right", {}, () => [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($props.rightOptions, (item, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  style: vue.normalizeStyle({
                    backgroundColor: item.style && item.style.backgroundColor ? item.style.backgroundColor : "#C7C6CD"
                  }),
                  class: "uni-swipe_button button-hock",
                  onTouchstart: _cache[1] || (_cache[1] = vue.withModifiers((...args) => _ctx.appTouchStart && _ctx.appTouchStart(...args), ["stop"])),
                  onTouchend: vue.withModifiers(($event) => _ctx.appTouchEnd($event, index, item, "right"), ["stop"]),
                  onClick: vue.withModifiers(($event) => _ctx.onClickForPC(index, item, "right"), ["stop"])
                }, [
                  vue.createElementVNode(
                    "text",
                    {
                      class: "uni-swipe_button-text",
                      style: vue.normalizeStyle({ color: item.style && item.style.color ? item.style.color : "#FFFFFF", fontSize: item.style && item.style.fontSize ? item.style.fontSize : "16px" })
                    },
                    vue.toDisplayString(item.text),
                    5
                    /* TEXT, STYLE */
                  )
                ], 44, ["onTouchend", "onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ], true)
        ])
      ], 40, ["change:prop", "prop", "data-threshold", "data-disabled"])
    ]);
  }
  if (typeof block0$2 === "function")
    block0$2(_sfc_main$1D);
  if (typeof block1 === "function")
    block1(_sfc_main$1D);
  const __easycom_1$b = /* @__PURE__ */ _export_sfc(_sfc_main$1D, [["render", _sfc_render$1C], ["__scopeId", "data-v-8ff2a577"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-swipe-action/components/uni-swipe-action-item/uni-swipe-action-item.vue"]]);
  const _sfc_main$1C = {
    name: "uniSwipeAction",
    data() {
      return {};
    },
    created() {
      this.children = [];
    },
    methods: {
      // 公开给用户使用，重制组件样式
      resize() {
      },
      // 公开给用户使用，关闭全部 已经打开的组件
      closeAll() {
        this.children.forEach((vm) => {
          vm.is_show = "none";
        });
      },
      closeOther(vm) {
        if (this.openItem && this.openItem !== vm) {
          this.openItem.is_show = "none";
        }
        this.openItem = vm;
      }
    }
  };
  function _sfc_render$1B(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.renderSlot(_ctx.$slots, "default")
    ]);
  }
  const __easycom_2$6 = /* @__PURE__ */ _export_sfc(_sfc_main$1C, [["render", _sfc_render$1B], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-swipe-action/components/uni-swipe-action/uni-swipe-action.vue"]]);
  const pageSize$5 = 10;
  const _sfc_main$1B = {
    __name: "info-message-list",
    props: {
      // 预留，如果以后需要根据 active 决定是否刷新
      active: {
        type: Boolean,
        default: true
      }
    },
    emits: ["update-stats"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const emit = __emit;
      const messageList = vue.ref([]);
      const unreadCount = vue.ref(0);
      const page = vue.ref(1);
      const loading2 = vue.ref(false);
      const finished = vue.ref(false);
      const swipeOptions = (item) => {
        return [
          {
            text: "删除",
            style: {
              backgroundColor: "#dd524d"
            },
            data: {
              type: "delete",
              id: item.id
            }
          },
          {
            text: item.is_read ? "已读" : "标记已读",
            style: {
              backgroundColor: "#6fd0d4"
            },
            data: {
              type: "read",
              id: item.id
            }
          }
        ];
      };
      function itemStyle(index) {
        const delay2 = index * 80;
        return {
          animationDelay: delay2 + "ms"
        };
      }
      vue.onMounted(async () => {
        await asyncGetUserInfo();
        loadMessages(true);
        fetchUnreadCount();
      });
      function loadMessages(reset) {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        if (loading2.value)
          return;
        if (!reset && finished.value)
          return;
        if (reset) {
          page.value = 1;
          finished.value = false;
          messageList.value = [];
        }
        loading2.value = true;
        uni.request({
          url: `${websiteUrl.value}/with-state/user-messages`,
          method: "GET",
          header: {
            Authorization: token
          },
          data: {
            page: page.value
          },
          success: (res) => {
            const resp = res.data || {};
            if (resp.status !== "success") {
              uni.showToast({
                title: resp.msg || "消息加载失败",
                icon: "none"
              });
              return;
            }
            const data = resp.data || {};
            const list = data.message_list || [];
            if (reset) {
              messageList.value = list;
            } else {
              messageList.value = messageList.value.concat(list);
            }
            if (list.length < pageSize$5) {
              finished.value = true;
            } else {
              page.value = page.value + 1;
            }
          },
          fail: () => {
            uni.showToast({
              title: "网络错误，消息加载失败",
              icon: "none"
            });
          },
          complete: () => {
            loading2.value = false;
          }
        });
      }
      function loadMore() {
        loadMessages(false);
      }
      function fetchUnreadCount() {
        const token = uni.getStorageSync("token");
        if (!token)
          return;
        uni.request({
          url: `${websiteUrl.value}/with-state/unread-message-count`,
          method: "GET",
          header: {
            Authorization: token
          },
          success: (res) => {
            const resp = res.data || {};
            if (resp.status !== "success")
              return;
            const count = resp.data && resp.data.count || 0;
            unreadCount.value = Number(count) || 0;
            emit("update-stats", {
              messageTotal: unreadCount.value
            });
          }
        });
      }
      function handleSwipeClick(e2) {
        const { type, id } = e2.content.data || {};
        if (!id)
          return;
        if (type === "delete") {
          showDeleteConfirm(id);
        } else if (type === "read") {
          markAsRead(id);
        }
      }
      function showDeleteConfirm(messageId) {
        uni.showModal({
          title: "删除确认",
          content: "确定要删除这条消息吗？",
          success: (res) => {
            if (res.confirm) {
              deleteMessage(messageId);
            }
          }
        });
      }
      function deleteMessage(messageId) {
        const token = uni.getStorageSync("token");
        if (!token)
          return;
        uni.request({
          url: `${websiteUrl.value}/with-state/delete-message`,
          method: "POST",
          header: {
            Authorization: token,
            "Content-Type": "application/json"
          },
          data: {
            message_id: messageId
          },
          success: () => {
            messageList.value = messageList.value.filter((item) => item.id !== messageId);
            fetchUnreadCount();
            uni.showToast({
              title: "删除成功"
            });
          },
          fail: () => {
            uni.showToast({
              title: "删除失败",
              icon: "none"
            });
          }
        });
      }
      function markAsRead(messageId) {
        const token = uni.getStorageSync("token");
        if (!token)
          return;
        uni.request({
          url: `${websiteUrl.value}/with-state/mark-message-read`,
          method: "POST",
          header: {
            Authorization: token,
            "Content-Type": "application/json"
          },
          data: {
            message_id: messageId
          },
          success: () => {
            const item = messageList.value.find((i2) => i2.id === messageId);
            if (item && !item.is_read) {
              item.is_read = 1;
              unreadCount.value = Math.max(0, unreadCount.value - 1);
              emit("update-stats", {
                messageTotal: unreadCount.value
              });
            }
          },
          fail: () => {
            uni.showToast({
              title: "标记失败",
              icon: "none"
            });
          }
        });
      }
      function gotoDetail(item) {
        if (!item || !item.id)
          return;
        uni.navigateTo({
          url: `/pages/message_info/message_info?message_id=${item.id}`
        });
      }
      function formatTime(timestamp) {
        if (!timestamp)
          return "--";
        const date = new Date(timestamp * 1e3);
        const y2 = date.getFullYear();
        const m2 = String(date.getMonth() + 1).padStart(2, "0");
        const d2 = String(date.getDate()).padStart(2, "0");
        const hh = String(date.getHours()).padStart(2, "0");
        const mm = String(date.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${d2} ${hh}:${mm}`;
      }
      const __returned__ = { emit, messageList, unreadCount, page, pageSize: pageSize$5, loading: loading2, finished, swipeOptions, itemStyle, loadMessages, loadMore, fetchUnreadCount, handleSwipeClick, showDeleteConfirm, deleteMessage, markAsRead, gotoDetail, formatTime, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1A(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_swipe_action_item = resolveEasycom(vue.resolveDynamicComponent("uni-swipe-action-item"), __easycom_1$b);
    const _component_uni_swipe_action = resolveEasycom(vue.resolveDynamicComponent("uni-swipe-action"), __easycom_2$6);
    const _component_loading_jump_text = resolveEasycom(vue.resolveDynamicComponent("loading-jump-text"), LoadingJumpText);
    return vue.openBlock(), vue.createElementBlock("view", { class: "message-tab" }, [
      !$setup.loading && $setup.messageList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "empty-box"
      }, [
        vue.createElementVNode("text", { class: "empty-text font-alimamashuhei" }, " 还没有收到任何站内信哦 ")
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "list-wrap"
      }, [
        vue.createVNode(_component_uni_swipe_action, null, {
          default: vue.withCtx(() => [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.messageList, (item, index) => {
                return vue.openBlock(), vue.createBlock(_component_uni_swipe_action_item, {
                  key: item.id,
                  "right-options": $setup.swipeOptions(item),
                  threshold: 0.4,
                  onClick: $setup.handleSwipeClick
                }, {
                  default: vue.withCtx(() => [
                    vue.createElementVNode("view", {
                      class: "message-item",
                      style: vue.normalizeStyle($setup.itemStyle(index)),
                      onClick: ($event) => $setup.gotoDetail(item)
                    }, [
                      vue.createElementVNode("image", {
                        class: "cover-image",
                        src: item.cover_image,
                        mode: "aspectFill"
                      }, null, 8, ["src"]),
                      vue.createElementVNode("view", { class: "content" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "title font-alimamashuhei" },
                          vue.toDisplayString(item.title),
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          {
                            class: vue.normalizeClass(["msg font-alimamashuhei", { unread: !item.is_read }])
                          },
                          vue.toDisplayString(item.msg),
                          3
                          /* TEXT, CLASS */
                        )
                      ]),
                      vue.createElementVNode(
                        "text",
                        { class: "time font-alimamashuhei" },
                        vue.toDisplayString($setup.formatTime(item.created_at)),
                        1
                        /* TEXT */
                      )
                    ], 12, ["onClick"])
                  ]),
                  _: 2
                  /* DYNAMIC */
                }, 1032, ["right-options"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]),
          _: 1
          /* STABLE */
        }),
        $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "loading-box"
        }, [
          vue.createVNode(_component_loading_jump_text)
        ])) : !$setup.finished && $setup.messageList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "load-more-box",
          onClick: $setup.loadMore
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, " 点击加载更多 ")
        ])) : $setup.finished && $setup.messageList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "load-more-box"
        }, [
          vue.createElementVNode("text", { class: "load-more-text font-alimamashuhei" }, " 已经到底啦 ")
        ])) : vue.createCommentVNode("v-if", true)
      ]))
    ]);
  }
  const __easycom_1$a = /* @__PURE__ */ _export_sfc(_sfc_main$1B, [["render", _sfc_render$1A], ["__scopeId", "data-v-3b4101f1"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/info-message-list/info-message-list.vue"]]);
  const _sfc_main$1A = {
    __name: "info-hub-panel",
    props: {
      active: {
        type: Boolean,
        default: true
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const activeSecondTab = vue.ref("follow");
      const followCount = vue.ref(0);
      const messageCount = vue.ref(0);
      function changeSecondTab(key) {
        activeSecondTab.value = key;
      }
      function handleStatsUpdate(payload) {
        if (!payload)
          return;
        if (typeof payload.followTotal === "number") {
          followCount.value = payload.followTotal;
        }
        if (typeof payload.messageTotal === "number") {
          messageCount.value = payload.messageTotal;
        }
      }
      const __returned__ = { activeSecondTab, followCount, messageCount, changeSecondTab, handleStatsUpdate, ref: vue.ref };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1z(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_info_follow_tab = resolveEasycom(vue.resolveDynamicComponent("info-follow-tab"), __easycom_0$6);
    const _component_info_message_list = resolveEasycom(vue.resolveDynamicComponent("info-message-list"), __easycom_1$a);
    return vue.openBlock(), vue.createElementBlock("view", { class: "info-hub-panel" }, [
      vue.createElementVNode("view", { class: "stats-card" }, [
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["stat-item", { "stat-item-active": $setup.activeSecondTab === "follow" }]),
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.changeSecondTab("follow"))
          },
          [
            vue.createElementVNode(
              "text",
              { class: "stat-number font-alimamashuhei" },
              vue.toDisplayString($setup.followCount),
              1
              /* TEXT */
            ),
            vue.createElementVNode("text", { class: "stat-label font-alimamashuhei" }, " 关注 ")
          ],
          2
          /* CLASS */
        ),
        vue.createElementVNode("view", { class: "stat-divider" }),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["stat-item", { "stat-item-active": $setup.activeSecondTab === "message" }]),
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.changeSecondTab("message"))
          },
          [
            vue.createElementVNode(
              "text",
              { class: "stat-number font-alimamashuhei" },
              vue.toDisplayString($setup.messageCount),
              1
              /* TEXT */
            ),
            vue.createElementVNode("text", { class: "stat-label font-alimamashuhei" }, " 站内信 ")
          ],
          2
          /* CLASS */
        )
      ]),
      vue.createElementVNode("view", { class: "info-content" }, [
        $setup.activeSecondTab === "follow" ? (vue.openBlock(), vue.createBlock(_component_info_follow_tab, {
          key: 0,
          onUpdateStats: $setup.handleStatsUpdate
        })) : (vue.openBlock(), vue.createBlock(_component_info_message_list, {
          key: 1,
          onUpdateStats: $setup.handleStatsUpdate
        }))
      ])
    ]);
  }
  const __easycom_2$5 = /* @__PURE__ */ _export_sfc(_sfc_main$1A, [["render", _sfc_render$1z], ["__scopeId", "data-v-95975baf"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/info-hub-panel/info-hub-panel.vue"]]);
  const _sfc_main$1z = {
    __name: "mine-workspace-panel",
    setup(__props, { expose: __expose }) {
      __expose();
      const loading2 = vue.ref(true);
      const userInfo = vue.ref({});
      const isSettled = vue.computed(() => {
        return !!userInfo.value && Number(userInfo.value.brand_id || 0) > 0;
      });
      const isShop = vue.ref(false);
      const isArtist2 = vue.ref(false);
      const isHairstylist2 = vue.ref(false);
      const updating = vue.ref(false);
      const showOrderHome = vue.computed(() => isArtist2.value || isHairstylist2.value);
      const showBrandHome = vue.computed(() => isShop.value);
      const receivedCount = vue.ref(0);
      const allFunctions = [
        {
          key: "shop-settings",
          label: "店铺设置",
          desc: "设置店铺基础信息与对外展示",
          icon: "shop",
          needShop: true
        },
        {
          key: "goods-manage",
          label: "商品管理",
          desc: "上新、编辑你的贩售商品",
          icon: "gift",
          // gift
          needShop: true
        },
        {
          key: "makeup-gallery",
          label: "妆图展示",
          desc: "管理妆面作品展示页",
          icon: "heart",
          // 妆师设计
          needArtist: true
        },
        {
          key: "hair-gallery",
          label: "手改毛展示",
          desc: "管理手改毛作品展示页",
          icon: "staff",
          // 手改毛图
          needHairstylist: true
        },
        {
          key: "publish-plan",
          label: "发布开单",
          desc: "发布妆面 / 手改毛开单计划",
          icon: "calendar",
          needArtistOrHairstylist: true
        },
        {
          key: "artist-settings",
          label: "妆师设置",
          desc: "设置妆师接单信息与规则",
          icon: "person",
          needArtist: true
        },
        {
          key: "hairstylist-settings",
          label: "毛娘设置",
          desc: "设置毛娘接单信息与规则",
          icon: "heart-filled",
          // 毛娘设置
          needHairstylist: true
        },
        {
          key: "article-publish",
          label: "发布文章",
          desc: "发布图透、成品展示和公告",
          icon: "compose",
          always: true
        }
      ];
      const functionThemeMap = {
        "shop-settings": "fc-theme-1",
        // 原配色1
        "goods-manage": "fc-theme-2",
        // 原配色2
        "article-publish": "fc-theme-3",
        // 原配色3
        "artist-settings": "fc-theme-4",
        // 原配色4
        "makeup-gallery": "fc-theme-5",
        // 新配色1：#e5e3ed + #e19cbb
        "hair-gallery": "fc-theme-6",
        // 新配色2：#003153 + #e5e3ed
        "publish-plan": "fc-theme-7",
        // 新配色3：#ecf2ec + #008c8d
        "hairstylist-settings": "fc-theme-8"
        // 新配色4：#efdfce + #7e041d
      };
      const themeIconColorMap = {
        "fc-theme-1": "#006a57",
        // #ffb400 + #006a57
        "fc-theme-2": "#dfd7c2",
        // #ef856d + #dfd7c2
        "fc-theme-3": "#fff6e1",
        // #81D8cf + #fff6e1
        "fc-theme-4": "#c7e1fa",
        // #28517f + #c7e1fa
        "fc-theme-5": "#e19cbb",
        // #e5e3ed + #e19cbb
        "fc-theme-6": "#e5e3ed",
        // #003153 + #e5e3ed
        "fc-theme-7": "#008c8d",
        // #ecf2ec + #008c8d
        "fc-theme-8": "#7e041d"
        // #efdfce + #7e041d
      };
      function getFunctionThemeClass(key) {
        return functionThemeMap[key] || "fc-theme-1";
      }
      function getFunctionIconColor(key) {
        const theme = functionThemeMap[key] || "fc-theme-1";
        return themeIconColorMap[theme] || "#3c3b48";
      }
      const visibleFunctions = vue.computed(() => {
        const list = [];
        const added = /* @__PURE__ */ new Set();
        allFunctions.forEach((item) => {
          let ok = false;
          if (item.always) {
            ok = true;
          } else {
            if (item.needShop && isShop.value)
              ok = true;
            if (item.needArtist && isArtist2.value)
              ok = true;
            if (item.needHairstylist && isHairstylist2.value)
              ok = true;
            if (item.needArtistOrHairstylist && (isArtist2.value || isHairstylist2.value))
              ok = true;
          }
          if (ok && !added.has(item.key)) {
            added.add(item.key);
            list.push(item);
          }
        });
        return list;
      });
      vue.onMounted(async () => {
        loading2.value = true;
        try {
          const u2 = await asyncGetUserInfo();
          userInfo.value = u2 || {};
          if (isSettled.value) {
            fetchBrandIdentity();
          }
        } catch (e2) {
          formatAppLog("error", "at components/mine-workspace-panel/mine-workspace-panel.vue:287", "加载工作台失败", e2);
          uni.showToast({
            title: "工作台加载失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      });
      function fetchBrandIdentity() {
        const id = userInfo.value.brand_id;
        if (!id)
          return;
        uni.request({
          url: `${websiteUrl.value}/brand-info?id=${id}`,
          method: "GET",
          success: (res) => {
            const resp = res.data || {};
            if (resp.status !== "success" || !resp.data)
              return;
            const d2 = resp.data;
            isShop.value = d2.is_brand === 1;
            isArtist2.value = d2.is_bjd_artist === 1;
            isHairstylist2.value = d2.is_bjd_hairstylist === 1;
            fetchReceivedCount();
          },
          fail: () => {
            uni.showToast({
              title: "获取身份信息失败",
              icon: "none"
            });
          }
        });
      }
      function fetchReceivedCount() {
        if (!showOrderHome.value) {
          receivedCount.value = 0;
          return;
        }
        const token = uni.getStorageSync("token");
        if (!token) {
          receivedCount.value = 0;
          return;
        }
        uni.request({
          url: `${websiteUrl.value}/with-state/artist-order/received`,
          method: "GET",
          header: {
            Authorization: token
          },
          data: {
            page: 1,
            page_size: 1
            // 只要 total，不需要列表
          },
          success: (res) => {
            const resp = res.data || {};
            if (resp.status === "success" && resp.data && resp.data.page) {
              receivedCount.value = resp.data.page.total || 0;
            } else {
              receivedCount.value = 0;
            }
          },
          fail: () => {
            receivedCount.value = 0;
          }
        });
      }
      function onToggle(type, e2) {
        const val = !!e2.detail.value;
        if (type === "shop") {
          updateBrandIdentity(
            "is_brand",
            val,
            () => {
              isShop.value = val;
            },
            () => {
              isShop.value = !val;
            }
          );
        } else if (type === "artist") {
          updateBrandIdentity(
            "is_bjd_artist",
            val,
            () => {
              isArtist2.value = val;
            },
            () => {
              isArtist2.value = !val;
            }
          );
        } else if (type === "hairstylist") {
          updateBrandIdentity(
            "is_bjd_hairstylist",
            val,
            () => {
              isHairstylist2.value = val;
            },
            () => {
              isHairstylist2.value = !val;
            }
          );
        }
      }
      function updateBrandIdentity(field, value, onSuccess, onRollback) {
        if (updating.value)
          return;
        updating.value = true;
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          updating.value = false;
          onRollback && onRollback();
          return;
        }
        uni.showLoading({ title: "更新中...", mask: true });
        const data = {};
        data[field] = value ? 1 : 0;
        uni.request({
          url: `${websiteUrl.value}/brand-manager/update-brand-identity`,
          method: "POST",
          header: {
            Authorization: token,
            "Content-Type": "application/json"
          },
          data,
          success: (res) => {
            const resp = res.data || {};
            if (resp.status === "success") {
              onSuccess && onSuccess();
              uni.showToast({ title: "已更新", icon: "success" });
              fetchReceivedCount();
            } else {
              onRollback && onRollback();
              uni.showToast({
                title: resp.msg || "更新失败",
                icon: "none"
              });
            }
          },
          fail: () => {
            onRollback && onRollback();
            uni.showToast({
              title: "网络错误",
              icon: "none"
            });
          },
          complete: () => {
            updating.value = false;
            uni.hideLoading();
          }
        });
      }
      function goBecomeCreator() {
        uni.navigateTo({
          url: "/pkg-creator/creator_base/brand_manager/brand_manager"
        });
      }
      function goOrderHome() {
        const bid = userInfo.value.brand_id || "";
        if (!bid) {
          uni.showToast({
            title: "暂无品牌信息",
            icon: "none"
          });
          return;
        }
        uni.navigateTo({
          url: `/pages/artist_info/artist_info?brand_id=${bid}`
        });
      }
      function goBrandHome() {
        const bid = userInfo.value.brand_id || "";
        if (!bid) {
          uni.showToast({
            title: "暂无品牌信息",
            icon: "none"
          });
          return;
        }
        uni.navigateTo({
          url: `/pages/brand/brand?brand_id=${bid}`
        });
      }
      function goReceivedSubmissions() {
        uni.navigateTo({
          url: "/pkg-creator/creator_order/received-list/received-list"
        });
      }
      function handleFunctionClick(key) {
        let url = "";
        switch (key) {
          case "shop-settings":
            url = "/pkg-creator/creator_base/brand_edit/brand_edit";
            break;
          case "goods-manage":
            url = "/pkg-creator/creator_base/goods_list/goods_list";
            break;
          case "makeup-gallery":
            url = "/pkg-creator/creator_base/set_showcase/set_showcase";
            break;
          case "hair-gallery":
            url = "/pkg-creator/creator_base/set_hair_showcase/set_hair_showcase";
            break;
          case "publish-plan":
            url = "/pkg-creator/creator_base/order_plane/order_plane";
            break;
          case "artist-settings":
            url = "/pkg-creator/creator_base/artist_setting/artist_setting";
            break;
          case "hairstylist-settings":
            url = "/pkg-creator/creator_base/hairstylist_setting/hairstylist_setting";
            break;
          case "article-publish":
            url = "/pkg-creator/creator_base/news/news";
            break;
          default:
            url = "";
        }
        if (!url)
          return;
        uni.navigateTo({ url });
      }
      const __returned__ = { loading: loading2, userInfo, isSettled, isShop, isArtist: isArtist2, isHairstylist: isHairstylist2, updating, showOrderHome, showBrandHome, receivedCount, allFunctions, functionThemeMap, themeIconColorMap, getFunctionThemeClass, getFunctionIconColor, visibleFunctions, fetchBrandIdentity, fetchReceivedCount, onToggle, updateBrandIdentity, goBecomeCreator, goOrderHome, goBrandHome, goReceivedSubmissions, handleFunctionClick, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1y(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_loading_jump_text = resolveEasycom(vue.resolveDynamicComponent("loading-jump-text"), LoadingJumpText);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "workspace-panel font-alimamashuhei" }, [
      $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "ws-loading"
      }, [
        vue.createVNode(_component_loading_jump_text)
      ])) : !$setup.isSettled ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "creator-empty-card"
      }, [
        vue.createElementVNode("text", { class: "creator-title" }, "还不是入驻作者"),
        vue.createElementVNode("text", { class: "creator-desc" }, " 入驻后可以开启店铺 / 妆师 / 毛娘身份，拥有自己的工作台和主页入口。 "),
        vue.createElementVNode("view", {
          class: "creator-btn",
          onClick: $setup.goBecomeCreator
        }, " 去成为入驻作者 ")
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        class: "ws-content"
      }, [
        $setup.showOrderHome ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "card received-card",
          onClick: $setup.goReceivedSubmissions
        }, [
          vue.createElementVNode("view", { class: "received-row" }, [
            vue.createElementVNode("view", { class: "received-main" }, [
              vue.createElementVNode("text", { class: "received-label" }, "我收到的投递"),
              vue.createElementVNode(
                "text",
                { class: "received-count" },
                "（" + vue.toDisplayString($setup.receivedCount) + "）",
                1
                /* TEXT */
              )
            ]),
            vue.createVNode(_component_uni_icons, {
              type: "arrow-right",
              size: "18",
              color: "#e84a52"
            })
          ])
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "card identity-card" }, [
          vue.createElementVNode("view", { class: "card-header-row" }, [
            vue.createElementVNode("text", { class: "card-title" }, "我的身份设置")
          ]),
          vue.createElementVNode("view", { class: "identity-row" }, [
            vue.createElementVNode("text", { class: "identity-label" }, "店铺"),
            vue.createElementVNode("switch", {
              checked: $setup.isShop,
              onChange: _cache[0] || (_cache[0] = ($event) => $setup.onToggle("shop", $event)),
              color: "#8fecff"
            }, null, 40, ["checked"])
          ]),
          vue.createElementVNode("view", { class: "identity-row" }, [
            vue.createElementVNode("text", { class: "identity-label" }, "妆师"),
            vue.createElementVNode("switch", {
              checked: $setup.isArtist,
              onChange: _cache[1] || (_cache[1] = ($event) => $setup.onToggle("artist", $event)),
              color: "#8fecff"
            }, null, 40, ["checked"])
          ]),
          vue.createElementVNode("view", { class: "identity-row" }, [
            vue.createElementVNode("text", { class: "identity-label" }, "毛娘"),
            vue.createElementVNode("switch", {
              checked: $setup.isHairstylist,
              onChange: _cache[2] || (_cache[2] = ($event) => $setup.onToggle("hairstylist", $event)),
              color: "#8fecff"
            }, null, 40, ["checked"])
          ]),
          vue.createElementVNode("text", { class: "identity-tip" }, " 开启对应身份，代表您经营对应业务，可以在搜索框中被搜索到。 ")
        ]),
        vue.createElementVNode("view", { class: "card entry-card" }, [
          $setup.showOrderHome ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "entry-row",
            onClick: $setup.goOrderHome
          }, [
            vue.createElementVNode("view", { class: "entry-main" }, [
              vue.createElementVNode("text", { class: "entry-title" }, "前往我的接单主页")
            ]),
            vue.createVNode(_component_uni_icons, {
              type: "arrow-right",
              size: "18",
              color: "#999"
            })
          ])) : vue.createCommentVNode("v-if", true),
          $setup.showBrandHome ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "entry-row",
            onClick: $setup.goBrandHome
          }, [
            vue.createElementVNode("view", { class: "entry-main" }, [
              vue.createElementVNode("text", { class: "entry-title" }, "前往我的品牌主页")
            ]),
            vue.createVNode(_component_uni_icons, {
              type: "arrow-right",
              size: "18",
              color: "#999"
            })
          ])) : vue.createCommentVNode("v-if", true)
        ]),
        vue.createElementVNode("view", { class: "card function-card" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.visibleFunctions, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: item.key,
                class: "function-item",
                onClick: ($event) => $setup.handleFunctionClick(item.key)
              }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["function-icon", $setup.getFunctionThemeClass(item.key)])
                  },
                  [
                    vue.createVNode(_component_uni_icons, {
                      type: item.icon,
                      size: "22",
                      color: $setup.getFunctionIconColor(item.key)
                    }, null, 8, ["type", "color"])
                  ],
                  2
                  /* CLASS */
                ),
                vue.createElementVNode("view", { class: "function-info" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "function-title" },
                    vue.toDisplayString(item.label),
                    1
                    /* TEXT */
                  ),
                  item.desc ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "function-desc"
                    },
                    vue.toDisplayString(item.desc),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ]),
                vue.createElementVNode("view", { class: "function-arrow" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "arrow-right",
                    size: "18",
                    color: "#999"
                  })
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ]))
    ]);
  }
  const __easycom_3$5 = /* @__PURE__ */ _export_sfc(_sfc_main$1z, [["render", _sfc_render$1y], ["__scopeId", "data-v-60d597dd"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/mine-workspace-panel/mine-workspace-panel.vue"]]);
  const _sfc_main$1y = {
    __name: "mine-settings-panel",
    emits: ["action"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const emit = __emit;
      const items = [
        { key: "profile", label: "基本信息设置" },
        { key: "address", label: "地址设置" },
        { key: "logout", label: "退出账号" }
      ];
      const showLogoutConfirm = vue.ref(false);
      function handleClick(key) {
        if (key === "logout") {
          showLogoutConfirm.value = true;
        } else {
          emit("action", key);
        }
      }
      function onConfirmLogout() {
        showLogoutConfirm.value = false;
        uni.removeStorageSync("token");
        global$1.isLogin = false;
        global$1.userInfo = {};
        uni.showToast({ title: "已退出", icon: "none" });
      }
      function onCancelLogout() {
        showLogoutConfirm.value = false;
      }
      const itemStyle = (index) => {
        const delay2 = index * 80;
        return {
          animationDelay: `${delay2}ms`
        };
      };
      const __returned__ = { emit, items, showLogoutConfirm, handleClick, onConfirmLogout, onCancelLogout, itemStyle, ref: vue.ref, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get getUserInfo() {
        return getUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1x(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock("view", { class: "settings-panel font-alimamashuhei" }, [
      (vue.openBlock(), vue.createElementBlock(
        vue.Fragment,
        null,
        vue.renderList($setup.items, (item, index) => {
          return vue.createElementVNode("view", {
            key: item.key,
            class: "settings-item",
            style: vue.normalizeStyle($setup.itemStyle(index)),
            onClick: ($event) => $setup.handleClick(item.key)
          }, [
            vue.createElementVNode(
              "text",
              { class: "settings-label" },
              vue.toDisplayString(item.label),
              1
              /* TEXT */
            ),
            vue.createVNode(_component_uni_icons, {
              class: "settings-arrow-icon",
              type: "arrow-right",
              size: "18",
              color: "#666"
            })
          ], 12, ["onClick"]);
        }),
        64
        /* STABLE_FRAGMENT */
      )),
      vue.createVNode(_component_common_modal, {
        visible: $setup.showLogoutConfirm,
        "onUpdate:visible": _cache[0] || (_cache[0] = ($event) => $setup.showLogoutConfirm = $event),
        top: "32%",
        width: "80vw"
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "logout-modal font-alimamashuhei" }, [
            vue.createElementVNode("view", { class: "logout-title" }, "确认退出账号？"),
            vue.createElementVNode("view", { class: "logout-desc" }, " 退出后将返回未登录状态，下次使用需要重新登录。 "),
            vue.createElementVNode("view", { class: "logout-actions" }, [
              vue.createElementVNode("view", {
                class: "logout-btn logout-btn-cancel",
                onClick: $setup.onCancelLogout
              }, " 再想想 "),
              vue.createElementVNode("view", {
                class: "logout-btn logout-btn-confirm",
                onClick: $setup.onConfirmLogout
              }, " 退出登录 ")
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["visible"])
    ]);
  }
  const __easycom_4$2 = /* @__PURE__ */ _export_sfc(_sfc_main$1y, [["render", _sfc_render$1x], ["__scopeId", "data-v-900404e1"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/mine-settings-panel/mine-settings-panel.vue"]]);
  const _imports_0$a = "/static/user.png";
  const _imports_1$7 = "/static/key.png";
  const _sfc_main$1x = {
    __name: "login-panel",
    emits: ["success"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const emit = __emit;
      const inputPhone = vue.ref("");
      const inputPassword = vue.ref("");
      const agree = vue.ref(false);
      const scene = getScene();
      function handleAgreeChange(e2) {
        agree.value = e2.detail.value.length > 0;
      }
      function goToPrivacy() {
        uni.navigateTo({
          url: "/pages/private/private"
        });
      }
      function goToPermissions() {
        uni.navigateTo({
          url: "/pages/permission/permission"
        });
      }
      function jump2register() {
        uni.navigateTo({
          url: "/pages/register/register"
        });
      }
      function jump2forgetPassword() {
        uni.navigateTo({
          url: "/pages/reset_password/reset_password"
        });
      }
      function jump2index() {
        uni.showTabBar({ animation: false });
        uni.switchTab({
          url: "/pages/index/index"
        });
      }
      function login() {
        if (!agree.value) {
          uni.showToast({
            title: "请先阅读并同意协议",
            icon: "none"
          });
          return;
        }
        if (!inputPhone.value || !inputPassword.value) {
          uni.showToast({
            title: "请输入手机号和密码",
            icon: "none"
          });
          return;
        }
        if (!/^1[3456789]\d{9}$/.test(inputPhone.value)) {
          uni.showToast({
            title: "手机号格式错误",
            icon: "none"
          });
          return;
        }
        if (inputPassword.value.length < 6) {
          uni.showToast({
            title: "密码必须大于6位",
            icon: "none"
          });
          return;
        }
        const phone = inputPhone.value;
        const password = inputPassword.value;
        uni.request({
          url: websiteUrl.value + "/login",
          method: "POST",
          data: {
            account: phone,
            password
          },
          success: (res) => {
            const data = res.data.data;
            const status = res.data.status;
            if (status !== "success") {
              uni.showToast({
                title: res.data.msg || "登录失败",
                icon: "none"
              });
              return;
            }
            uni.setStorageSync("token", data.jwt_token);
            uni.setStorageSync("openid", data.open_id);
            uni.setStorageSync("session_key", data.session_key);
            if (data.jwt_token) {
              getUserInfo();
              emit("success");
            }
          },
          fail: () => {
            uni.showToast({
              title: "登录失败",
              icon: "none"
            });
          }
        });
      }
      const __returned__ = { emit, inputPhone, inputPassword, agree, scene, handleAgreeChange, goToPrivacy, goToPermissions, jump2register, jump2forgetPassword, jump2index, login, ref: vue.ref, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get getScene() {
        return getScene;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1w(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "unlogin-container" }, [
      vue.createElementVNode("image", {
        src: "https://images1.fantuanpu.com/home/jpt.gif",
        mode: "aspectFit",
        class: "welcome-img"
      }),
      vue.createElementVNode("text", { class: "welcome-text" }, "欢迎使用娃圈狗狗助手"),
      vue.createElementVNode("view", { class: "input-group" }, [
        vue.createElementVNode("view", { class: "input-with-icon" }, [
          vue.createElementVNode("image", {
            class: "icon",
            src: _imports_0$a
          }),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              type: "text",
              placeholder: "请输入手机号",
              class: "inputer",
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.inputPhone = $event)
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.inputPhone]
          ])
        ]),
        vue.createElementVNode("view", { class: "input-with-icon" }, [
          vue.createElementVNode("image", {
            class: "icon",
            src: _imports_1$7
          }),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              type: "password",
              placeholder: "请输入密码",
              class: "inputer",
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.inputPassword = $event)
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.inputPassword]
          ])
        ])
      ]),
      vue.createElementVNode("view", { class: "action-links" }, [
        vue.createElementVNode("text", {
          class: "float-left",
          onClick: $setup.jump2register
        }, "注册账号"),
        vue.createElementVNode("text", {
          class: "float-right",
          onClick: $setup.jump2forgetPassword
        }, "忘记密码"),
        vue.createElementVNode("view", { class: "clearfix" })
      ]),
      vue.createElementVNode("button", {
        class: "submit-btn",
        onClick: $setup.login
      }, "立即登录"),
      $setup.scene === 4 ? (vue.openBlock(), vue.createElementBlock("button", {
        key: 0,
        class: "submit-btn",
        onClick: _cache[2] || (_cache[2] = (...args) => $setup.wechatSignLogin && $setup.wechatSignLogin(...args))
      }, " 微信登录 ")) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("button", {
        class: "submit-btn",
        onClick: $setup.jump2index,
        style: { "background": "linear-gradient(135deg, #cdcdcd, #b1b1b1)" }
      }, " 随便看看→ "),
      vue.createElementVNode("view", { class: "agreement-container" }, [
        vue.createElementVNode(
          "checkbox-group",
          { onChange: $setup.handleAgreeChange },
          [
            vue.createElementVNode("label", { class: "agreement-label" }, [
              vue.createElementVNode("checkbox", {
                checked: $setup.agree,
                color: "#65C3D6",
                style: { "transform": "scale(0.8)" }
              }, null, 8, ["checked"]),
              vue.createElementVNode("text", { class: "agreement-text" }, "我已阅读并同意")
            ])
          ],
          32
          /* NEED_HYDRATION */
        ),
        vue.createElementVNode("view", { class: "agreement-links" }, [
          vue.createElementVNode("text", {
            class: "agreement-link",
            onClick: $setup.goToPrivacy
          }, "《隐私政策》"),
          vue.createElementVNode("text", {
            class: "agreement-link",
            onClick: $setup.goToPermissions
          }, "《用户协议》")
        ])
      ])
    ]);
  }
  const __easycom_5 = /* @__PURE__ */ _export_sfc(_sfc_main$1x, [["render", _sfc_render$1w], ["__scopeId", "data-v-c8638695"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/login-panel/login-panel.vue"]]);
  let ws$1 = null;
  let hbTimer = 0;
  let reconnectTimer = 0;
  let reconnectDelay = 2500;
  const listeners = /* @__PURE__ */ new Set();
  const unreadTotal = vue.ref(0);
  const activeSessions = /* @__PURE__ */ new Set();
  const SEEN_MAX = 500;
  const seenMsgIds = [];
  const seenSet = /* @__PURE__ */ new Set();
  function rememberSeen(id) {
    if (!id)
      return;
    if (seenSet.has(id))
      return;
    seenSet.add(id);
    seenMsgIds.push(id);
    if (seenMsgIds.length > SEEN_MAX) {
      const old = seenMsgIds.shift();
      seenSet.delete(old);
    }
  }
  function isActiveSession(sess) {
    if (sess === void 0 || sess === null)
      return false;
    return activeSessions.has(String(sess));
  }
  function getSelfId() {
    try {
      const u2 = uni.getStorageSync("userInfo") || {};
      const id = Number((u2 == null ? void 0 : u2.id) || (u2 == null ? void 0 : u2.Id) || 0);
      if (id)
        return id;
    } catch (_2) {
    }
    try {
      const legacy = Number(uni.getStorageSync("uid") || 0);
      if (legacy)
        return legacy;
    } catch (_2) {
    }
    return 0;
  }
  function log(...args) {
    formatAppLog("log", "at common/im.js:44", "[IM]", ...args);
  }
  function parseBaseForWS() {
    let base = websiteUrl && websiteUrl.value ? String(websiteUrl.value).trim() : "";
    if (!base) {
      return { scheme: "wss", hostPort: "api.fantuanpu.com" };
    }
    let scheme = "wss";
    if (/^https:\/\//i.test(base)) {
      scheme = "wss";
      base = base.replace(/^https:\/\//i, "");
    } else if (/^http:\/\//i.test(base)) {
      scheme = "ws";
      base = base.replace(/^http:\/\//i, "");
    } else {
      scheme = "wss";
    }
    const slashIndex = base.indexOf("/");
    let hostPort = slashIndex >= 0 ? base.slice(0, slashIndex) : base;
    hostPort = hostPort.replace(/\/+$/, "");
    if (!hostPort)
      hostPort = "api.fantuanpu.com";
    return { scheme, hostPort };
  }
  function buildWSUrl() {
    const token = uni.getStorageSync("token") || "";
    const { scheme, hostPort } = parseBaseForWS();
    return `${scheme}://${hostPort}/v1/websocket?token=${encodeURIComponent(token)}`;
  }
  function createWS(url) {
    if (typeof WebSocket === "function") {
      try {
        return new WebSocket(url);
      } catch (e2) {
        formatAppLog("error", "at common/im.js:101", "[IM] new WebSocket failed:", e2);
      }
    }
    if (typeof uni !== "undefined" && typeof uni.connectSocket === "function") {
      const socketTask = uni.connectSocket({
        url,
        success() {
        },
        // 👈 这一行非常重要
        fail(err) {
          formatAppLog("error", "at common/im.js:112", "[IM] connectSocket fail", err);
        }
      });
      if (socketTask && typeof socketTask.onOpen === "function") {
        const wrapper = {
          _task: socketTask,
          readyState: 0,
          // 0-连接中, 1-已打开, 2-关闭中, 3-已关闭
          send(data) {
            try {
              if (this.readyState !== 1) {
                formatAppLog("warn", "at common/im.js:125", "[IM] send skipped, ws not open");
                return;
              }
              this._task.send({ data });
            } catch (e2) {
              formatAppLog("error", "at common/im.js:131", "[IM] send error", e2);
            }
          },
          close(code = 1e3, reason = "client close") {
            try {
              this.readyState = 2;
              this._task.close({ code, reason });
            } catch (e2) {
              formatAppLog("error", "at common/im.js:140", "[IM] close error", e2);
            }
          },
          // 供外部赋值的回调（connectIM 那边会写 ws.onopen / ws.onmessage 等）
          onopen: null,
          onmessage: null,
          onclose: null,
          onerror: null
        };
        socketTask.onOpen((res) => {
          wrapper.readyState = 1;
          wrapper.onopen && wrapper.onopen(res);
        });
        socketTask.onMessage((res) => {
          wrapper.onmessage && wrapper.onmessage({ data: res.data });
        });
        socketTask.onClose((res) => {
          wrapper.readyState = 3;
          wrapper.onclose && wrapper.onclose(res);
        });
        socketTask.onError((err) => {
          wrapper.onerror && wrapper.onerror(err);
        });
        return wrapper;
      }
      return socketTask;
    }
    formatAppLog("error", "at common/im.js:174", "[IM] 当前环境不支持 WebSocket / uni.connectSocket");
    return null;
  }
  function startHeartbeat() {
    stopHeartbeat();
    hbTimer = setInterval(() => {
      try {
        if (ws$1 && ws$1.readyState === 1)
          ws$1.send(JSON.stringify({ type: "ping" }));
      } catch (_2) {
      }
    }, 3e4);
  }
  function stopHeartbeat() {
    if (hbTimer) {
      clearInterval(hbTimer);
      hbTimer = 0;
    }
  }
  function scheduleReconnect() {
    if (reconnectTimer)
      return;
    reconnectTimer = setTimeout(() => {
      reconnectTimer = 0;
      connectIM(true);
    }, reconnectDelay);
  }
  let _ruInflight = null;
  async function refreshUnread() {
    const token = uni.getStorageSync("token") || "";
    if (!token) {
      unreadTotal.value = 0;
      return 0;
    }
    if (_ruInflight)
      return _ruInflight;
    _ruInflight = new Promise((resolve) => {
      uni.request({
        url: `${websiteUrl.value}/with-state/im/unread-count`,
        method: "GET",
        header: { Authorization: token },
        timeout: 5e3,
        success: (res) => {
          var _a;
          const data = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data;
          const n2 = Number((data == null ? void 0 : data.count) ?? 0);
          unreadTotal.value = Number.isFinite(n2) ? n2 : 0;
          resolve(unreadTotal.value);
        },
        fail: () => resolve(unreadTotal.value),
        complete: () => {
          setTimeout(() => {
            _ruInflight = null;
          }, 100);
        }
      });
    });
    return _ruInflight;
  }
  function connectIM(force = false) {
    if (!force && ws$1 && (ws$1.readyState === 0 || ws$1.readyState === 1))
      return ws$1;
    const token = uni.getStorageSync("token") || "";
    if (!token) {
      log("no token, skip ws connect");
      return null;
    }
    const url = buildWSUrl();
    log("connecting...", url);
    const socket = createWS(url);
    if (!socket) {
      log("createWS failed");
      return null;
    }
    ws$1 = socket;
    try {
      if (typeof window !== "undefined")
        window.__IM_WS__ = ws$1;
    } catch (_2) {
    }
    ws$1.onopen = async () => {
      log("open");
      startHeartbeat();
      await refreshUnread();
    };
    ws$1.onmessage = (evt) => {
      let payload = null;
      try {
        payload = JSON.parse(evt.data);
      } catch {
        payload = evt.data;
      }
      log("onmessage", payload);
      listeners.forEach((fn) => {
        try {
          fn(payload);
        } catch (e2) {
          formatAppLog("error", "at common/im.js:250", "[IM] listener error", e2);
        }
      });
      try {
        if (!payload || typeof payload !== "object")
          return;
        if ((payload.type || "").toLowerCase() !== "im.event")
          return;
        const evtName = (payload.event || "").toLowerCase();
        if (evtName !== "message" && evtName !== "notify")
          return;
        const data = payload.data || {};
        const sess = data.session_id !== void 0 && data.session_id !== null ? String(data.session_id) : "";
        const msg = data.message || {};
        const mid = Number(msg.id || 0);
        const from = Number(msg.sender_id || 0);
        const self2 = getSelfId();
        if (mid) {
          if (seenSet.has(mid))
            return;
          rememberSeen(mid);
        }
        if (from && self2 && from !== self2) {
          if (!isActiveSession(sess)) {
            unreadTotal.value = Math.max(0, Number(unreadTotal.value || 0) + 1);
          }
        }
      } catch (_2) {
      }
    };
    ws$1.onclose = (evt) => {
      formatAppLog("log", "at common/im.js:280", evt);
      log("close", evt.code, evt.reason);
      stopHeartbeat();
      scheduleReconnect();
    };
    ws$1.onerror = (err) => {
      log("error", err);
    };
    return ws$1;
  }
  function disconnectIM() {
    try {
      stopHeartbeat();
    } catch (_2) {
    }
    try {
      if (reconnectTimer) {
        clearTimeout(reconnectTimer);
        reconnectTimer = 0;
      }
    } catch (_2) {
    }
    try {
      if (ws$1) {
        const s2 = ws$1.readyState;
        if (s2 === 0 || s2 === 1)
          ws$1.close(1e3, "client close");
      }
    } catch (_2) {
    }
    ws$1 = null;
  }
  function closeIM() {
    return disconnectIM();
  }
  function onIMEvent(cb) {
    if (typeof cb === "function")
      listeners.add(cb);
    return () => {
      listeners.delete(cb);
    };
  }
  function getWS() {
    return ws$1;
  }
  function setActiveSession(sessionId) {
    if (sessionId === void 0 || sessionId === null)
      return;
    activeSessions.add(String(sessionId));
  }
  function clearActiveSession(sessionId) {
    if (sessionId === void 0 || sessionId === null)
      return;
    activeSessions.delete(String(sessionId));
  }
  async function listSessions({ page = 1, page_size = 20 } = {}) {
    const token = uni.getStorageSync("token") || "";
    if (!token)
      return { list: [], has_more: false };
    const url = `${websiteUrl.value}/with-state/im/session-list?page=${page}&page_size=${page_size}`;
    return new Promise((resolve, reject) => {
      uni.request({
        url,
        method: "GET",
        header: { Authorization: token },
        timeout: 8e3,
        success: (res) => {
          var _a;
          const data = ((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) ?? {};
          const list = Array.isArray(data.sessions) ? data.sessions : [];
          const total = Number(data.total ?? 0);
          const pg = Number(data.page ?? page);
          const ps2 = Number(data.page_size ?? page_size);
          const has_more = pg * ps2 < total;
          resolve({ list, has_more });
        },
        fail: (err) => reject(err)
      });
    });
  }
  const FLAG = "__IM_FAB_BOUND__";
  const _sfc_main$1w = {
    __name: "im-fab",
    setup(__props, { expose: __expose }) {
      __expose();
      const badge = vue.computed(() => Number(unreadTotal.value || 0));
      function goMessageList() {
        uni.navigateTo({ url: "/pkg-im/message_list/message_list" });
      }
      vue.onMounted(() => {
        connectIM();
        if (!globalThis[FLAG]) {
          onIMEvent(() => {
          });
          globalThis[FLAG] = true;
        }
      });
      const __returned__ = { badge, goMessageList, FLAG, computed: vue.computed, onMounted: vue.onMounted, get unreadTotal() {
        return unreadTotal;
      }, get connectIM() {
        return connectIM;
      }, get onIMEvent() {
        return onIMEvent;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1v(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", {
      class: "im-fab",
      onClick: $setup.goMessageList,
      "hover-class": "im-fab--hover"
    }, [
      vue.createElementVNode("view", { class: "im-fab-glow" }),
      vue.createElementVNode("view", { class: "im-fab-inner" }, [
        vue.createVNode(_component_uni_icons, {
          type: "chat-filled",
          size: 28,
          color: "#141a23"
        }),
        $setup.badge > 0 ? (vue.openBlock(), vue.createElementBlock(
          "view",
          {
            key: 0,
            class: "dot"
          },
          vue.toDisplayString($setup.badge > 99 ? "99+" : $setup.badge),
          1
          /* TEXT */
        )) : vue.createCommentVNode("v-if", true)
      ])
    ]);
  }
  const __easycom_6 = /* @__PURE__ */ _export_sfc(_sfc_main$1w, [["render", _sfc_render$1v], ["__scopeId", "data-v-9d04d4fa"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/im-fab/im-fab.vue"]]);
  const defaultAvatar$2 = "https://images1.fantuanpu.com/home/jpt.gif";
  const _sfc_main$1v = {
    __name: "mine",
    setup(__props, { expose: __expose }) {
      __expose();
      vue.useCssVars((_ctx) => ({
        "7c2ebfa5-headerHeight": headerHeight.value
      }));
      const mainTabs = [
        { key: "orders", label: "订单集" },
        { key: "inbox", label: "信息集" },
        { key: "workspace", label: "工作台" },
        { key: "settings", label: "设置我" }
      ];
      const activeMainTab = vue.ref("orders");
      const isLogin = vue.computed(() => !!global$1.isLogin);
      const systemInfo2 = uni.getSystemInfoSync();
      const navBarHeight = vue.computed(() => {
        return 0;
      });
      const headerHeight = vue.computed(() => {
        const statusBarHeight = systemInfo2.statusBarHeight || 0;
        return `${statusBarHeight}px`;
      });
      vue.watch(
        () => global$1.isLogin,
        (newVal) => {
          if (newVal) {
            uni.showTabBar({ animation: false });
          } else {
            uni.hideTabBar({ animation: false });
          }
        }
      );
      function changeMainTab(key) {
        activeMainTab.value = key;
      }
      function chooseImage2() {
        return new Promise((resolve) => {
          uni.chooseImage({
            count: 1,
            success: (res) => {
              resolve(res.tempFilePaths[0]);
            }
          });
        });
      }
      function jumpToCroper2() {
        chooseImage2().then((src) => {
          uni.navigateTo({
            url: `/pages/pop_croper/pop_croper?src=${decodeURIComponent(src)}`
          });
        });
      }
      function selectAvatar(croperPath) {
        const token = uni.getStorageSync("token");
        let qnToken = "";
        uni.request({
          url: websiteUrl.value + "/with-state/qiniu-token",
          method: "POST",
          header: {
            Authorization: token
          },
          success: (res) => {
            if (!res.data.data || !res.data.data.token) {
              uni.showToast({
                title: "获取上传凭证失败",
                icon: "none"
              });
              return;
            }
            qnToken = res.data.data.token;
            const fileName = res.data.data.path;
            uni.uploadFile({
              url: "https://up-cn-east-2.qiniup.com",
              name: "file",
              method: "POST",
              filePath: croperPath,
              fileType: "image",
              formData: {
                token: qnToken,
                key: fileName,
                scope: "hobby-box:" + fileName
              },
              success: () => {
                updateUserInfo("avatar", "https://images1.fantuanpu.com/" + fileName);
              },
              fail: () => {
                uni.showToast({
                  title: "上传失败",
                  icon: "none"
                });
              }
            });
          },
          fail: () => {
            uni.showToast({
              title: "获取上传凭证失败",
              icon: "none"
            });
          }
        });
      }
      function updateUserInfo(key, value) {
        const token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/update-profile",
          method: "POST",
          header: {
            Authorization: token
          },
          data: {
            [key]: value
          },
          success: () => {
            getUserInfo();
          },
          fail: () => {
            uni.showToast({
              title: "更新失败",
              icon: "none"
            });
          }
        });
      }
      function jumpSetting() {
        uni.navigateTo({
          url: "/pages/setting/setting"
        });
      }
      function handleSettingsAction(type) {
        if (type === "profile") {
          jumpSetting();
        } else if (type === "address") {
          uni.showToast({
            title: "地址设置开发中",
            icon: "none"
          });
        }
      }
      function handleLoginSuccess() {
        asyncGetUserInfo();
      }
      onShow(async () => {
        await asyncGetUserInfo();
        if (global$1.isLogin) {
          uni.showTabBar({ animation: false });
        } else {
          uni.hideTabBar({ animation: false });
        }
        const pages2 = getCurrentPages();
        const currentPage = pages2[pages2.length - 1];
        if (currentPage.returnParam) {
          selectAvatar(currentPage.returnParam);
          currentPage.returnParam = "";
        }
      });
      const __returned__ = { defaultAvatar: defaultAvatar$2, mainTabs, activeMainTab, isLogin, systemInfo: systemInfo2, navBarHeight, headerHeight, changeMainTab, chooseImage: chooseImage2, jumpToCroper: jumpToCroper2, selectAvatar, updateUserInfo, jumpSetting, handleSettingsAction, handleLoginSuccess, ref: vue.ref, computed: vue.computed, watch: vue.watch, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get getUserInfo() {
        return getUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1u(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_orders_panel = resolveEasycom(vue.resolveDynamicComponent("orders-panel"), __easycom_1$c);
    const _component_info_hub_panel = resolveEasycom(vue.resolveDynamicComponent("info-hub-panel"), __easycom_2$5);
    const _component_mine_workspace_panel = resolveEasycom(vue.resolveDynamicComponent("mine-workspace-panel"), __easycom_3$5);
    const _component_mine_settings_panel = resolveEasycom(vue.resolveDynamicComponent("mine-settings-panel"), __easycom_4$2);
    const _component_login_panel = resolveEasycom(vue.resolveDynamicComponent("login-panel"), __easycom_5);
    const _component_im_fab = resolveEasycom(vue.resolveDynamicComponent("im-fab"), __easycom_6);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("meta", {
          name: "theme-color",
          content: "#e0f3ff"
        }),
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { head_color: "url('/static/bg/bg2.jpg')" }, [
          vue.createElementVNode("view", { class: "my-center-page" }, [
            $setup.isLogin ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "login-wrap"
            }, [
              vue.createElementVNode("view", { class: "headbg" }, [
                vue.createElementVNode("view", { class: "header-placeholders" }),
                vue.createElementVNode("view", { class: "profile-header" }, [
                  vue.createElementVNode("view", {
                    class: "avatar-circle",
                    onClick: $setup.jumpToCroper
                  }, [
                    vue.createElementVNode("image", {
                      class: "avatar-img",
                      mode: "aspectFill",
                      src: $setup.global.userInfo.avatar || $setup.defaultAvatar
                    }, null, 8, ["src"])
                  ]),
                  vue.createElementVNode(
                    "text",
                    { class: "profile-name font-alimama" },
                    vue.toDisplayString($setup.global.userInfo.username || "未设置昵称"),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("text", { class: "profile-id" }, [
                    vue.createElementVNode("text", { class: "font-alimama id-label" }, "ID"),
                    vue.createElementVNode(
                      "text",
                      { class: "font-title" },
                      vue.toDisplayString($setup.global.userInfo.id || "-"),
                      1
                      /* TEXT */
                    )
                  ])
                ]),
                vue.createElementVNode("view", { class: "main-tabs" }, [
                  (vue.openBlock(), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.mainTabs, (tab) => {
                      return vue.createElementVNode("view", {
                        key: tab.key,
                        class: vue.normalizeClass(["main-tab", { "main-tab-active": $setup.activeMainTab === tab.key }]),
                        onClick: ($event) => $setup.changeMainTab(tab.key)
                      }, [
                        vue.createElementVNode(
                          "text",
                          { class: "main-tab-text font-alimama" },
                          vue.toDisplayString(tab.label),
                          1
                          /* TEXT */
                        ),
                        $setup.activeMainTab === tab.key ? (vue.openBlock(), vue.createElementBlock("view", {
                          key: 0,
                          class: "main-tab-underline"
                        })) : vue.createCommentVNode("v-if", true)
                      ], 10, ["onClick"]);
                    }),
                    64
                    /* STABLE_FRAGMENT */
                  ))
                ])
              ]),
              $setup.activeMainTab === "orders" ? (vue.openBlock(), vue.createBlock(_component_orders_panel, {
                key: 0,
                active: $setup.activeMainTab === "orders"
              }, null, 8, ["active"])) : $setup.activeMainTab === "inbox" ? (vue.openBlock(), vue.createBlock(_component_info_hub_panel, {
                key: 1,
                active: $setup.activeMainTab === "inbox"
              }, null, 8, ["active"])) : $setup.activeMainTab === "workspace" ? (vue.openBlock(), vue.createBlock(_component_mine_workspace_panel, { key: 2 })) : $setup.activeMainTab === "settings" ? (vue.openBlock(), vue.createBlock(_component_mine_settings_panel, {
                key: 3,
                onAction: $setup.handleSettingsAction
              })) : vue.createCommentVNode("v-if", true)
            ])) : (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "login-wrap"
            }, [
              vue.createElementVNode("view", { class: "header-placeholders" }),
              vue.createVNode(_component_login_panel, { onSuccess: $setup.handleLoginSuccess })
            ]))
          ]),
          vue.createVNode(_component_im_fab)
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesMineMine = /* @__PURE__ */ _export_sfc(_sfc_main$1v, [["render", _sfc_render$1u], ["__scopeId", "data-v-7c2ebfa5"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/mine/mine.vue"]]);
  const _sfc_main$1u = {
    __name: "watch_demo",
    setup(__props, { expose: __expose }) {
      __expose();
      const person = vue.ref({ name: "john", age: 18 });
      vue.watch(() => person.value.name, (newVal, oldVal) => {
        formatAppLog("log", "at pages/watch_demo/watch_demo.vue:14", "newVal", newVal);
        formatAppLog("log", "at pages/watch_demo/watch_demo.vue:15", "oldVal", oldVal);
      }, { deep: true });
      const __returned__ = { person, ref: vue.ref, watch: vue.watch };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1t(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.withDirectives(vue.createElementVNode(
        "input",
        {
          type: "text",
          "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.person.name = $event)
        },
        null,
        512
        /* NEED_PATCH */
      ), [
        [vue.vModelText, $setup.person.name]
      ]),
      vue.createElementVNode(
        "text",
        null,
        vue.toDisplayString($setup.person.name),
        1
        /* TEXT */
      ),
      vue.createElementVNode("footer")
    ]);
  }
  const PagesWatchDemoWatchDemo = /* @__PURE__ */ _export_sfc(_sfc_main$1u, [["render", _sfc_render$1t], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/watch_demo/watch_demo.vue"]]);
  const _imports_0$9 = "/static/zhouWei-navBar/icon_back_white.png";
  const _imports_1$6 = "/static/zhouWei-navBar/icon_back_black.png";
  const _imports_2$3 = "/static/zhouWei-navBar/icon_home_white.png";
  const _imports_3$1 = "/static/zhouWei-navBar/icon_home_black.png";
  const mainPagePath = ["pages/home/home", "pages/my/my", "pages/demo/common", "pages/template/common", "pages/apiDemo/common", "pages/template/addTemplate"];
  const homePath = "/pages/template/addTemplate";
  const whiteList = ["#FFF", "#fff", "#FFFFFF", "#ffffff", "white", "rgb(255,255,255)", "rgba(255,255,255,1)"];
  const _sfc_main$1t = {
    props: {
      //是否显示返回按钮
      // 1000 显示返回按钮
      // 2000 不显示返回按钮
      // 3000 自定义返回按钮方法，点击返回箭头后会发送一个backClick事件
      backState: {
        type: [String, Number],
        default: function() {
          return 1e3;
        }
      },
      //是否显示返回首页按钮
      // 1000 显示首页按钮
      // 2000 不显示首页按钮
      // 3000 自定义首页按钮方法，点击首页箭头后会发送一个homeClick事件
      homeState: {
        type: [String, Number],
        default: function() {
          return 2e3;
        }
      },
      //导航背景色，支持渐变
      bgColor: {
        type: [String, Array],
        default: function() {
          return "#FFFFFF";
        }
      },
      // 导航背景色渐变角度
      bgColorAngle: {
        type: [String, Number],
        default: function() {
          return 90;
        }
      },
      //导航字体颜色，字体颜色为白色的时候会把手机状态栏设置为白色，否则为黑色
      fontColor: {
        type: String,
        default: function() {
          return "#000000";
        }
      },
      //标题是否居中
      titleCenter: {
        type: Boolean,
        default: function() {
          return true;
        }
      },
      //标题
      title: {
        type: String,
        default: function() {
          return "";
        }
      },
      //类型 fixed为固定 默认
      // ordinary 普通的 不固定
      // transparent 透明不固定的
      //transparentFixed  透明固定的
      type: {
        type: String,
        default: function() {
          return "fixed";
        }
      },
      //透明固定的时候字体颜色
      transparentFixedFontColor: {
        type: String,
        default: function() {
          return "#000000";
        }
      },
      // 屏幕滑动距离顶部距离(透明固定导航比传)
      scrollTop: {
        type: Number,
        default: function() {
          return 0;
        }
      },
      // 是否显示阴影
      shadow: {
        type: Boolean,
        default: function() {
          return false;
        }
      }
    },
    data() {
      return {
        //当前页面是否是第一个页面
        firstPage: false,
        //透明度值
        transparentValue: 1,
        //标题
        navTitle: "",
        //字体色
        navFontColor: "#000000",
        //背景色
        navBgColor: "",
        //透明底字体色
        navTransparentFixedFontColor: "#000000",
        // 导航栏高度
        statusBarHeight: 0,
        // 上次显示的导航栏颜色
        lastFrontColor: "",
        themeBgColorName: ""
      };
    },
    computed: {
      back() {
        return this.backState == 1e3 || this.backState == 3e3;
      },
      home() {
        return this.homeState == 1e3;
      },
      //导航固定
      navFixed() {
        if (this.type == "transparentFixed" || this.type == "fixed") {
          return true;
        } else {
          return false;
        }
      },
      //导航底部线是否显示
      navShadow() {
        if (this.bgColor && typeof this.bgColor == "string") {
          return this.shadow && this.type !== "transparent" && whiteList.includes(this.bgColor);
        } else {
          return false;
        }
      },
      //导航字体是否是白色颜色
      isWhite() {
        return whiteList.includes(this.navFontColor);
      },
      //右上角是否有两个按钮
      isTwoBtn() {
        return (this.backState == 1e3 || this.backState == 3e3) && this.homeState == 1e3 && !this.firstPage;
      }
    },
    watch: {
      title(val) {
        this.navTitle = val;
      },
      fontColor(val) {
        this.navFontColor = val;
        this.settingColor();
      },
      bgColor(val) {
        this.getNavBgColor(val);
      },
      transparentFixedFontColor(val) {
        this.navTransparentFixedFontColor = val;
      },
      scrollTop(val) {
        this.pageScroll({
          scrollTop: val
        });
      }
    },
    //第一次加载
    created() {
      this.navTitle = this.title;
      this.navFontColor = this.fontColor;
      this.getNavBgColor(this.bgColor);
      this.navTransparentFixedFontColor = this.transparentFixedFontColor;
      this.statusBarHeight = uni.getSystemInfoSync()["statusBarHeight"];
      this.pageScroll({
        scrollTop: this.scrollTop
      });
      let currentPages = getCurrentPages();
      let pageLen = currentPages.length;
      if (pageLen == 1 && !mainPagePath.includes(currentPages[0].route)) {
        this.firstPage = true;
      }
    },
    //方法
    methods: {
      //返回上一页面
      onBackPage() {
        if (this.backState == 3e3) {
          this.$emit("backClick");
        } else {
          uni.navigateBack();
        }
      },
      //返回首页
      onBackHome() {
        if (this.homeState == 3e3) {
          this.$emit("homeClick");
        } else {
          uni.reLaunch({
            url: homePath
          });
        }
      },
      pageScroll(e2) {
        if (this.type == "transparentFixed") {
          if (e2.scrollTop && e2.scrollTop > 0) {
            if (e2.scrollTop > 180) {
              this.transparentValue = 1;
            } else {
              this.transparentValue = e2.scrollTop / 180;
            }
          } else {
            this.transparentValue = 0;
          }
          this.settingColor();
        }
      },
      // 获取导航背景颜色
      getNavBgColor(val) {
        if (typeof val == "string") {
          if (this.type == "transparent") {
            this.navBgColor = "";
          } else if (/^#|rgb\(|rgba\(/.test(val)) {
            this.navBgColor = "linear-gradient(90deg," + val + "," + val + ")";
          } else {
            this.themeBgColorName = val;
            this.navBgColor = "";
          }
        } else if (Array.isArray(val) && val.length >= 2) {
          let navBgColor = "linear-gradient(" + this.bgColorAngle + "deg";
          val.forEach((item) => {
            if (typeof item == "string") {
              navBgColor += "," + item;
            } else if (typeof item == "object") {
              navBgColor += "," + item.color + " " + item.scale;
            }
          });
          navBgColor += ")";
          this.navBgColor = navBgColor;
        }
      },
      //设置手机状态栏颜色
      settingColor() {
        let navColor = this.navFontColor;
        if (this.type == "transparentFixed" && this.transparentValue <= 0.5) {
          navColor = this.navTransparentFixedFontColor;
        }
        let frontColor = "#000000";
        if (whiteList.includes(navColor)) {
          frontColor = "#ffffff";
        }
        if (this.lastFrontColor == frontColor) {
          return;
        }
        setTimeout(() => {
          this.lastFrontColor = frontColor;
          uni.setNavigationBarColor({
            frontColor,
            backgroundColor: "#FFFFFF"
          });
        }, 150);
      }
    }
  };
  function _sfc_render$1s(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass([$options.navFixed ? "header_fixed" : "", $props.type == "transparent" ? "header_absolute" : "", $options.navShadow ? "header_shadow" : "", $data.themeBgColorName]),
          style: vue.normalizeStyle([$data.navBgColor ? { backgroundImage: $data.navBgColor } : {}, { paddingTop: $data.statusBarHeight + "px", color: $data.navFontColor, opacity: $data.transparentValue }])
        },
        [
          vue.createElementVNode("view", { class: "header_content" }, [
            vue.createElementVNode("view", { class: "header_left_box" }, [
              vue.renderSlot(_ctx.$slots, "left", {}, () => [
                $options.back || _ctx.$slots.left || $options.home ? (vue.openBlock(), vue.createElementBlock(
                  "view",
                  {
                    key: 0,
                    class: vue.normalizeClass(["header_left_info", [$options.isTwoBtn ? "header_btnMongol" : "", $options.isWhite && $options.isTwoBtn ? "header_colorWhite_btnMongol" : ""]])
                  },
                  [
                    $options.back && !$data.firstPage ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        class: vue.normalizeClass(["header_left_back", [$options.isTwoBtn ? "header_btnMongol_left_back" : ""]]),
                        onClick: _cache[0] || (_cache[0] = (...args) => $options.onBackPage && $options.onBackPage(...args))
                      },
                      [
                        $options.isWhite ? (vue.openBlock(), vue.createElementBlock("image", {
                          key: 0,
                          class: "header_icon",
                          src: _imports_0$9,
                          mode: "aspectFit"
                        })) : (vue.openBlock(), vue.createElementBlock("image", {
                          key: 1,
                          class: "header_icon",
                          src: _imports_1$6,
                          mode: "aspectFit"
                        }))
                      ],
                      2
                      /* CLASS */
                    )) : vue.createCommentVNode("v-if", true),
                    $options.isTwoBtn ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 1,
                        class: vue.normalizeClass(["header_left_line", [$options.isWhite ? "header_colorWhite_left_line" : ""]])
                      },
                      null,
                      2
                      /* CLASS */
                    )) : vue.createCommentVNode("v-if", true),
                    $data.firstPage && $options.back || $options.home ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 2,
                        class: vue.normalizeClass(["header_left_home", [$options.isTwoBtn ? "header_btnMongol_left_home" : ""]]),
                        onClick: _cache[1] || (_cache[1] = (...args) => $options.onBackHome && $options.onBackHome(...args))
                      },
                      [
                        $options.isWhite ? (vue.openBlock(), vue.createElementBlock("image", {
                          key: 0,
                          class: "header_icon",
                          src: _imports_2$3,
                          mode: "aspectFit"
                        })) : (vue.openBlock(), vue.createElementBlock("image", {
                          key: 1,
                          class: "header_icon",
                          src: _imports_3$1,
                          mode: "aspectFit"
                        }))
                      ],
                      2
                      /* CLASS */
                    )) : vue.createCommentVNode("v-if", true)
                  ],
                  2
                  /* CLASS */
                )) : vue.createCommentVNode("v-if", true)
              ], true),
              !$props.titleCenter && (_ctx.$slots.default || $data.navTitle) ? (vue.openBlock(), vue.createElementBlock(
                "view",
                {
                  key: 0,
                  class: "header_title",
                  style: vue.normalizeStyle({ color: $data.navFontColor })
                },
                [
                  vue.renderSlot(_ctx.$slots, "default", {}, () => [
                    vue.createElementVNode(
                      "text",
                      {
                        style: vue.normalizeStyle({ color: $data.navFontColor })
                      },
                      vue.toDisplayString($data.navTitle),
                      5
                      /* TEXT, STYLE */
                    )
                  ], true)
                ],
                4
                /* STYLE */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            $props.titleCenter && (_ctx.$slots.default || $data.navTitle) ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 0,
                class: "header_title header_title_center",
                style: vue.normalizeStyle({ color: $data.navFontColor })
              },
              [
                vue.renderSlot(_ctx.$slots, "default", {}, () => [
                  vue.createElementVNode(
                    "text",
                    {
                      style: vue.normalizeStyle({ color: $data.navFontColor })
                    },
                    vue.toDisplayString($data.navTitle),
                    5
                    /* TEXT, STYLE */
                  )
                ], true)
              ],
              4
              /* STYLE */
            )) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "header_right_info" }, [
              vue.renderSlot(_ctx.$slots, "right", {}, void 0, true)
            ])
          ])
        ],
        6
        /* CLASS, STYLE */
      ),
      $props.type == "transparentFixed" ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 0,
          class: "header_transparentFixed header_fixed",
          style: vue.normalizeStyle({ paddingTop: $data.statusBarHeight + "px", color: $data.navTransparentFixedFontColor, opacity: 1 - $data.transparentValue, zIndex: $data.transparentValue < 0.3 ? 100 : 90 })
        },
        [
          vue.createElementVNode("view", { class: "header_content" }, [
            vue.createElementVNode("view", { class: "header_left_box" }, [
              vue.renderSlot(_ctx.$slots, "transparentFixedLeft", {}, () => [
                $options.back || _ctx.$slots.left || $options.home ? (vue.openBlock(), vue.createElementBlock(
                  "view",
                  {
                    key: 0,
                    class: vue.normalizeClass(["header_left_info header_transparentFixed_left_info", [$options.isWhite ? "header_transparentFixed_colorWhite_left_info" : ""]])
                  },
                  [
                    $options.back && !$data.firstPage ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        class: vue.normalizeClass(["header_left_back", [$options.isTwoBtn ? "header_btnMongol_left_back" : ""]]),
                        onClick: _cache[2] || (_cache[2] = (...args) => $options.onBackPage && $options.onBackPage(...args))
                      },
                      [
                        $options.isWhite ? (vue.openBlock(), vue.createElementBlock("image", {
                          key: 0,
                          class: "header_icon",
                          src: _imports_0$9,
                          mode: "aspectFit"
                        })) : (vue.openBlock(), vue.createElementBlock("image", {
                          key: 1,
                          class: "header_icon",
                          src: _imports_1$6,
                          mode: "aspectFit"
                        }))
                      ],
                      2
                      /* CLASS */
                    )) : vue.createCommentVNode("v-if", true),
                    $options.isTwoBtn ? (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "header_left_line"
                    })) : vue.createCommentVNode("v-if", true),
                    $data.firstPage && $options.back || $options.home ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 2,
                        class: vue.normalizeClass(["header_left_home", [$options.isTwoBtn ? "header_btnMongol_left_home" : ""]]),
                        onClick: _cache[3] || (_cache[3] = (...args) => $options.onBackHome && $options.onBackHome(...args))
                      },
                      [
                        $options.isWhite ? (vue.openBlock(), vue.createElementBlock("image", {
                          key: 0,
                          class: "header_icon",
                          src: _imports_2$3,
                          mode: "aspectFit"
                        })) : (vue.openBlock(), vue.createElementBlock("image", {
                          key: 1,
                          class: "header_icon",
                          src: _imports_3$1,
                          mode: "aspectFit"
                        }))
                      ],
                      2
                      /* CLASS */
                    )) : vue.createCommentVNode("v-if", true)
                  ],
                  2
                  /* CLASS */
                )) : vue.createCommentVNode("v-if", true)
              ], true),
              !$props.titleCenter && ($data.navTitle || _ctx.$slots.transparentFixed) ? (vue.openBlock(), vue.createElementBlock(
                "view",
                {
                  key: 0,
                  class: "header_title",
                  style: vue.normalizeStyle({ color: $data.navTransparentFixedFontColor })
                },
                [
                  vue.renderSlot(_ctx.$slots, "transparentFixed", {}, () => [
                    vue.createElementVNode(
                      "text",
                      {
                        style: vue.normalizeStyle({ color: $data.navTransparentFixedFontColor })
                      },
                      vue.toDisplayString($data.navTitle),
                      5
                      /* TEXT, STYLE */
                    )
                  ], true)
                ],
                4
                /* STYLE */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            $props.titleCenter && ($data.navTitle || _ctx.$slots.transparentFixed) ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 0,
                class: "header_title header_title_center",
                style: vue.normalizeStyle({ color: $data.navTransparentFixedFontColor })
              },
              [
                vue.renderSlot(_ctx.$slots, "transparentFixed", {}, () => [
                  vue.createElementVNode(
                    "text",
                    {
                      style: vue.normalizeStyle({ color: $data.navTransparentFixedFontColor })
                    },
                    vue.toDisplayString($data.navTitle),
                    5
                    /* TEXT, STYLE */
                  )
                ], true)
              ],
              4
              /* STYLE */
            )) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "header_right_info" }, [
              vue.renderSlot(_ctx.$slots, "transparentFixedRight", {}, void 0, true)
            ])
          ])
        ],
        4
        /* STYLE */
      )) : vue.createCommentVNode("v-if", true),
      $props.type == "fixed" ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 1,
          style: vue.normalizeStyle({ paddingTop: $data.statusBarHeight + "px" })
        },
        [
          vue.createElementVNode("view", { class: "header_station" })
        ],
        4
        /* STYLE */
      )) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_0$5 = /* @__PURE__ */ _export_sfc(_sfc_main$1t, [["render", _sfc_render$1s], ["__scopeId", "data-v-10ff15a4"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/zhouWei-navBar/components/zhouWei-navBar/zhouWei-navBar.vue"]]);
  const _sfc_main$1s = {
    __name: "item-impression",
    props: {
      target_id: {
        type: String,
        required: true
      },
      type: {
        type: String,
        required: true
      },
      goodsType: {
        type: Number,
        required: true
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const impressions = vue.ref([]);
      const defaultImpressionTitles = vue.ref([]);
      const totalCount = vue.ref(0);
      const newImpression = vue.ref("");
      const modalVisible = vue.ref(false);
      const userInfo = vue.ref(null);
      const allImpressions = vue.computed(() => {
        const existingMap = {};
        impressions.value.forEach((item) => {
          existingMap[item.content] = item;
        });
        const defaultItems = defaultImpressionTitles.value.map((title) => {
          if (existingMap[title]) {
            return {
              ...existingMap[title],
              isDefault: true
            };
          }
          return {
            content: title,
            count: 0,
            selected: false,
            isDefault: true
          };
        });
        const uniqueExisting = impressions.value.filter(
          (item) => !defaultImpressionTitles.value.includes(item.content)
        );
        const merged = [...defaultItems, ...uniqueExisting];
        return merged.sort((a2, b2) => b2.count - a2.count);
      });
      const fetchDefaultImpressions = async () => {
        try {
          let impressionType = 9;
          switch (props.goodsType) {
            case "整体":
              impressionType = 1;
              break;
            case "单头":
              impressionType = 2;
              break;
            case "娃衣":
              impressionType = 3;
              break;
            case "眼珠":
              impressionType = 4;
              break;
            case "假发":
              impressionType = 5;
              break;
            default:
              impressionType = 9;
          }
          const res = await uni.request({
            url: `${websiteUrl.value}/impression/default`,
            method: "GET",
            data: {
              type: impressionType
            }
          });
          if (res.statusCode === 200 && res.data.status === "success") {
            defaultImpressionTitles.value = res.data.data;
          } else {
            formatAppLog("error", "at components/item-impression/item-impression.vue:160", "获取默认表态失败:", res.data);
            uni.showToast({
              title: "获取默认表态失败",
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at components/item-impression/item-impression.vue:167", "获取默认表态失败:", error2);
          uni.showToast({
            title: "获取默认表态失败",
            icon: "none"
          });
        }
      };
      const fetchImpressionData = async () => {
        try {
          userInfo.value = await asyncGetUserInfo();
          const countsRes = await uni.request({
            url: `${websiteUrl.value}/impression/counts`,
            method: "GET",
            data: {
              target_id: parseInt(props.target_id, 10),
              type_id: parseInt(props.type, 10)
            }
          });
          const statusRes = await uni.request({
            url: `${websiteUrl.value}/with-state/impression/status`,
            method: "GET",
            data: {
              target_id: parseInt(props.target_id, 10),
              type_id: parseInt(props.type, 10)
            },
            header: {
              Authorization: uni.getStorageSync("token")
            }
          });
          if (countsRes.statusCode === 200 && countsRes.data.status === "success") {
            const countsData = countsRes.data.data;
            totalCount.value = countsData.reduce((sum, item) => sum + item.count, 0);
            const userSelected = statusRes.statusCode === 200 && statusRes.data.status === "success" ? statusRes.data.data.map((item) => item.id) : [];
            impressions.value = countsData.map((item) => ({
              ...item,
              selected: userSelected.includes(item.id)
            }));
          }
        } catch (error2) {
          formatAppLog("error", "at components/item-impression/item-impression.vue:222", "获取表态数据失败:", error2);
          uni.showToast({
            title: "获取表态数据失败",
            icon: "none"
          });
        }
      };
      const handleImpressionClick = async (item) => {
        if (!userInfo.value) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        try {
          const data = {
            target_id: parseInt(props.target_id, 10),
            type_id: parseInt(props.type, 10)
          };
          if (item.isDefault && !item.id) {
            data.content = item.content;
          } else {
            data.impression_id = item.id;
          }
          if (item.selected) {
            const res = await uni.request({
              url: `${websiteUrl.value}/with-state/impression/remove`,
              method: "POST",
              header: {
                Authorization: uni.getStorageSync("token"),
                "Content-Type": "application/json"
              },
              data: {
                impression_id: item.id
              }
            });
            if (res.statusCode === 200 && res.data.status === "success") {
              updateImpressionState(item, false);
            }
          } else {
            const res = await uni.request({
              url: `${websiteUrl.value}/with-state/impression/add`,
              method: "POST",
              header: {
                Authorization: uni.getStorageSync("token"),
                "Content-Type": "application/json"
              },
              data
            });
            if (res.statusCode === 200 && res.data.status === "success") {
              updateImpressionState(item, true, res.data.data.impression_id);
            }
          }
        } catch (error2) {
          formatAppLog("error", "at components/item-impression/item-impression.vue:291", "操作失败:", error2);
          uni.showToast({
            title: "操作失败",
            icon: "none"
          });
        }
      };
      const updateImpressionState = (item, isSelected, newId = null) => {
        const impressionIndex = impressions.value.findIndex(
          (imp) => imp.id === item.id || imp.content === item.content
        );
        if (impressionIndex === -1 && isSelected) {
          const newImpression2 = {
            id: newId,
            content: item.content,
            count: 1,
            selected: true
          };
          impressions.value = [...impressions.value, newImpression2];
          totalCount.value++;
          return;
        }
        if (impressionIndex !== -1) {
          const updatedImpressions = [...impressions.value];
          const updatedItem = { ...updatedImpressions[impressionIndex] };
          if (isSelected) {
            updatedItem.selected = true;
            updatedItem.count = (updatedItem.count || 0) + 1;
            if (newId)
              updatedItem.id = newId;
            totalCount.value++;
          } else {
            updatedItem.selected = false;
            updatedItem.count = Math.max(0, updatedItem.count - 1);
            totalCount.value--;
          }
          updatedImpressions[impressionIndex] = updatedItem;
          impressions.value = updatedImpressions;
        }
      };
      const showAddModal = () => {
        if (!userInfo.value) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        newImpression.value = "";
        modalVisible.value = true;
      };
      const cancelAdd = () => {
        modalVisible.value = false;
      };
      const confirmAdd = async () => {
        if (!newImpression.value.trim()) {
          uni.showToast({
            title: "请输入表态内容",
            icon: "none"
          });
          return;
        }
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/impression/add`,
            method: "POST",
            header: {
              Authorization: uni.getStorageSync("token"),
              "Content-Type": "application/json"
            },
            data: {
              target_id: parseInt(props.target_id, 10),
              type_id: parseInt(props.type, 10),
              content: newImpression.value.trim()
            }
          });
          if (res.statusCode === 200 && res.data.status === "success") {
            impressions.value = [
              ...impressions.value,
              {
                id: res.data.data.impression_id,
                content: newImpression.value.trim(),
                count: 1,
                selected: true
              }
            ];
            totalCount.value++;
            modalVisible.value = false;
            uni.showToast({
              title: "添加成功",
              icon: "success"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at components/item-impression/item-impression.vue:411", "添加表态失败:", error2);
          uni.showToast({
            title: "添加表态失败",
            icon: "none"
          });
        }
      };
      vue.watch(() => [props.target_id, props.type], fetchImpressionData, {
        immediate: true
      });
      vue.onMounted(() => {
        fetchDefaultImpressions();
        fetchImpressionData();
      });
      const __returned__ = { props, impressions, defaultImpressionTitles, totalCount, newImpression, modalVisible, userInfo, allImpressions, fetchDefaultImpressions, fetchImpressionData, handleImpressionClick, updateImpressionState, showAddModal, cancelAdd, confirmAdd, ref: vue.ref, onMounted: vue.onMounted, watch: vue.watch, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1r(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock("view", { class: "impression-container" }, [
      vue.createElementVNode("view", { class: "impression-header" }, [
        vue.createElementVNode("text", { class: "title" }, "你对它的印象怎样？"),
        vue.createElementVNode("text", { class: "subtitle" }, "（轻触表态）")
      ]),
      vue.createElementVNode("view", { class: "impression-list" }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.allImpressions, (item, index) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: item.id || "default-" + item.content,
              class: vue.normalizeClass(["impression-item", { "active": item.selected }]),
              onClick: ($event) => $setup.handleImpressionClick(item)
            }, [
              vue.createElementVNode(
                "text",
                { class: "impression-text" },
                vue.toDisplayString(item.content),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                {
                  class: vue.normalizeClass(["impression-count", { highlight: item.count > 0 }])
                },
                vue.toDisplayString(item.count),
                3
                /* TEXT, CLASS */
              )
            ], 10, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        )),
        vue.createElementVNode("view", {
          class: "add-impression",
          onClick: $setup.showAddModal
        }, [
          vue.createElementVNode("text", { class: "plus-icon" }, "+"),
          vue.createElementVNode("text", { class: "add-text" }, "添加表态")
        ])
      ]),
      vue.createVNode(_component_common_modal, {
        visible: $setup.modalVisible,
        "onUpdate:visible": _cache[1] || (_cache[1] = ($event) => $setup.modalVisible = $event),
        width: "80%",
        top: "30%"
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "modal-content" }, [
            vue.createElementVNode("text", { class: "modal-title" }, "添加新表态"),
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input-field",
                "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.newImpression = $event),
                placeholder: "请输入表态内容",
                "placeholder-style": "color: #aaa;",
                onConfirm: $setup.confirmAdd
              },
              null,
              544
              /* NEED_HYDRATION, NEED_PATCH */
            ), [
              [vue.vModelText, $setup.newImpression]
            ]),
            vue.createElementVNode("view", { class: "modal-actions" }, [
              vue.createElementVNode("button", {
                class: "cancel-btn",
                onClick: $setup.cancelAdd
              }, "取消"),
              vue.createElementVNode("button", {
                class: "confirm-btn",
                onClick: $setup.confirmAdd
              }, "确定")
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["visible"])
    ]);
  }
  const __easycom_3$4 = /* @__PURE__ */ _export_sfc(_sfc_main$1s, [["render", _sfc_render$1r], ["__scopeId", "data-v-883b6a8c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/item-impression/item-impression.vue"]]);
  const _sfc_main$1r = {
    __name: "report-button",
    props: {
      // 举报类型（1=展示柜,2=搭配,3=树洞发帖,4=用户，5=回帖）
      reportType: {
        type: Number,
        required: true
      },
      // 关联ID（被举报内容的ID）
      relationId: {
        type: Number,
        required: true
      },
      // 按钮文本
      buttonText: {
        type: String,
        default: "举报"
      },
      // 图标类型
      iconType: {
        type: String,
        default: ""
      },
      // 图标大小
      iconSize: {
        type: String,
        default: 24
      },
      // 图标颜色
      iconColor: {
        type: String,
        default: "#999"
      },
      // 主题颜色
      themeColor: {
        type: String,
        default: "#64c6dc"
      },
      // 是否自动聚焦文本输入框
      autoFocus: {
        type: Boolean,
        default: false
      }
    },
    emits: ["success", "error", "cancel", "before-submit"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const reportVisible = vue.ref(false);
      const reportReasons = vue.ref([]);
      const selectedReason = vue.ref("");
      const reportDetails = vue.ref("");
      const isSubmitting = vue.ref(false);
      const selectReason = (reason) => {
        if (selectedReason.value === reason) {
          selectedReason.value = "";
        } else {
          selectedReason.value = reason;
        }
      };
      const fetchReportReasons = async () => {
        try {
          const token = uni.getStorageSync("token");
          const res = await uni.request({
            url: `${websiteUrl.value}/report/reasons?type=${props.reportType}`,
            method: "GET",
            header: {
              "Authorization": token
            }
          });
          if (res.data.status === "success") {
            reportReasons.value = res.data.data;
            return true;
          } else {
            uni.showToast({
              title: "获取举报原因失败: " + (res.data.msg || ""),
              icon: "none"
            });
            return false;
          }
        } catch (error2) {
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
          return false;
        }
      };
      const handleClick = async () => {
        const success = await fetchReportReasons();
        if (!success)
          return;
        selectedReason.value = "";
        reportDetails.value = "";
        reportVisible.value = true;
      };
      const submitReport = async () => {
        if (isSubmitting.value)
          return;
        isSubmitting.value = true;
        try {
          if (!selectedReason.value) {
            uni.showToast({
              title: "请选择举报原因",
              icon: "none"
            });
            return;
          }
          emit("before-submit", {
            type: props.reportType,
            relationId: props.relationId,
            reason: selectedReason.value,
            details: reportDetails.value
          });
          const token = uni.getStorageSync("token");
          const reportData = {
            type: props.reportType,
            relation_id: props.relationId,
            reason: selectedReason.value,
            details: reportDetails.value
          };
          let url = `${websiteUrl.value}/with-state/report/submit`;
          if (token == "") {
            url = `${websiteUrl.value}/report/submit`;
          }
          const res = await uni.request({
            url,
            method: "POST",
            header: {
              "Authorization": token,
              "Content-Type": "application/json"
            },
            data: reportData,
            timeout: 1e4
            // 10秒超时
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "举报提交成功",
              icon: "success",
              duration: 2e3
            });
            reportVisible.value = false;
            emit("success", res.data);
          } else {
            uni.showToast({
              title: res.data.msg || "举报提交失败",
              icon: "none"
            });
            emit("error", res.data);
          }
        } catch (error2) {
          uni.showToast({
            title: "举报提交失败: " + (error2.errMsg || "网络错误"),
            icon: "none"
          });
          emit("error", error2);
        } finally {
          isSubmitting.value = false;
        }
      };
      vue.watch(reportVisible, (newVal) => {
        if (!newVal) {
          emit("cancel");
        }
      });
      const __returned__ = { props, emit, reportVisible, reportReasons, selectedReason, reportDetails, isSubmitting, selectReason, fetchReportReasons, handleClick, submitReport, ref: vue.ref, computed: vue.computed, watch: vue.watch, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1q(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("view", {
          class: "report-button",
          onClick: $setup.handleClick
        }, [
          vue.renderSlot(_ctx.$slots, "default", {}, () => [
            vue.createElementVNode("view", { class: "default-button horizontal" }, [
              vue.createVNode(_component_uni_icons, {
                "v-if": $props.iconType !== "",
                type: $props.iconType,
                size: parseInt($props.iconSize),
                color: $props.iconColor
              }, null, 8, ["v-if", "type", "size", "color"]),
              vue.createElementVNode(
                "text",
                { class: "button-text" },
                vue.toDisplayString($props.buttonText),
                1
                /* TEXT */
              )
            ])
          ], true)
        ]),
        vue.createVNode(_component_common_modal, {
          visible: $setup.reportVisible,
          "onUpdate:visible": _cache[3] || (_cache[3] = (val) => $setup.reportVisible = val),
          top: "10vh"
        }, {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "report-dialog" }, [
              vue.createElementVNode("view", { class: "dialog-header" }, [
                vue.createElementVNode("text", { class: "dialog-title" }, "举报内容"),
                vue.createVNode(_component_uni_icons, {
                  type: "closeempty",
                  size: "24",
                  color: "#999",
                  onClick: _cache[0] || (_cache[0] = ($event) => $setup.reportVisible = false)
                })
              ]),
              vue.createElementVNode("view", { class: "reason-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.reportReasons, (reason) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: reason.id,
                      class: vue.normalizeClass(["reason-item", { "selected": $setup.selectedReason === reason.label }]),
                      onClick: ($event) => $setup.selectReason(reason.label)
                    }, [
                      vue.createElementVNode("radio", {
                        value: reason.label,
                        checked: $setup.selectedReason === reason.label,
                        color: $props.themeColor
                      }, null, 8, ["value", "checked", "color"]),
                      vue.createElementVNode(
                        "text",
                        { class: "reason-label" },
                        vue.toDisplayString(reason.label),
                        1
                        /* TEXT */
                      )
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ]),
              vue.createElementVNode("view", { class: "details-box" }, [
                vue.withDirectives(vue.createElementVNode("textarea", {
                  "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.reportDetails = $event),
                  placeholder: "请详细描述举报原因（选填）",
                  class: "details-input",
                  maxlength: "200",
                  focus: $props.autoFocus
                }, null, 8, ["focus"]), [
                  [vue.vModelText, $setup.reportDetails]
                ]),
                vue.createElementVNode(
                  "text",
                  { class: "word-count" },
                  vue.toDisplayString($setup.reportDetails.length) + "/200",
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "button-group" }, [
                vue.createElementVNode("button", {
                  class: "cancel-btn",
                  onClick: _cache[2] || (_cache[2] = ($event) => $setup.reportVisible = false)
                }, "取消"),
                vue.createElementVNode(
                  "button",
                  {
                    class: "submit-btn",
                    onClick: $setup.submitReport,
                    style: vue.normalizeStyle({ backgroundColor: $props.themeColor })
                  },
                  "提交举报",
                  4
                  /* STYLE */
                )
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["visible"])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const __easycom_1$9 = /* @__PURE__ */ _export_sfc(_sfc_main$1r, [["render", _sfc_render$1q], ["__scopeId", "data-v-b620d6d3"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/report-button/report-button.vue"]]);
  const _sfc_main$1q = {
    __name: "attitude-widget",
    props: {
      targetId: Number,
      type: Number,
      attitudeStatus: {
        type: Number,
        default: 0
      },
      attitudeCounts: {
        type: Object,
        default: () => ({})
      }
    },
    emits: ["change"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const showPanel = vue.ref(false);
      const currentStatus = vue.ref(props.attitudeStatus);
      const currentCounts = vue.ref({});
      const attitudeTypes = vue.ref([
        {
          emoji: "😝",
          value: 1,
          label: "很有趣"
        },
        {
          emoji: "😨",
          value: 2,
          label: "这个..."
        },
        {
          emoji: "🤤",
          value: 3,
          label: "我也想要"
        },
        {
          emoji: "🤦‍♀️",
          value: 4,
          label: "难以直视"
        },
        {
          emoji: "🔴",
          value: 5,
          label: "谁的鼻子掉了?"
        }
      ]);
      vue.watch(() => props.attitudeCounts, (newVal) => {
        attitudeTypes.value.forEach((t2) => {
          currentCounts.value[t2.value] = Number(newVal[t2.value] ?? 0);
        });
        currentCounts.value = {
          ...currentCounts.value
        };
      }, {
        immediate: true,
        deep: true
      });
      const visibleGlobalCounts = vue.computed(() => {
        return attitudeTypes.value.map((t2) => ({
          ...t2,
          count: currentCounts.value[t2.value] || 0
        })).filter((t2) => t2.count > 0 || t2.value === currentStatus.value).filter((t2) => t2.count > 0);
      });
      const togglePanel = () => {
        showPanel.value = !showPanel.value;
      };
      const handleAction = async (actionType) => {
        try {
          const token = uni.getStorageSync("token");
          formatAppLog("log", "at components/attitude-widget/attitude-widget.vue:130", token);
          if (!token) {
            uni.showToast({
              title: "请先登录",
              icon: "none"
            });
            return;
          }
          const isActive = currentStatus.value === actionType;
          const apiUrl = `${websiteUrl.value}/with-state/attitude/${isActive ? "remove" : "add"}`;
          const res = await uni.request({
            url: apiUrl,
            method: "POST",
            data: {
              target_id: props.targetId,
              type: props.type,
              action_type: actionType
            },
            header: {
              Authorization: token
            }
          });
          if (res.data.status === "success") {
            if (isActive) {
              currentCounts.value[actionType]--;
              currentStatus.value = 0;
            } else {
              if (currentStatus.value > 0) {
                currentCounts.value[currentStatus.value]--;
              }
              currentStatus.value = actionType;
              currentCounts.value[actionType] = (currentCounts.value[actionType] || 0) + 1;
            }
            emit("change", {
              status: currentStatus.value,
              counts: {
                ...currentCounts.value
              }
            });
            showPanel.value = false;
          }
        } catch (err) {
          formatAppLog("log", "at components/attitude-widget/attitude-widget.vue:178", err);
          uni.showToast({
            title: "操作失败",
            icon: "none"
          });
        }
      };
      const __returned__ = { props, emit, showPanel, currentStatus, currentCounts, attitudeTypes, visibleGlobalCounts, togglePanel, handleAction, ref: vue.ref, computed: vue.computed, watch: vue.watch, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getScene() {
        return getScene;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1p(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "attitude-container" }, [
      $setup.showPanel ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 0,
          class: "mask",
          onClick: vue.withModifiers($setup.togglePanel, ["stop"]),
          onTouchmove: _cache[0] || (_cache[0] = vue.withModifiers(() => {
          }, ["stop", "prevent"]))
        },
        null,
        32
        /* NEED_HYDRATION */
      )) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "counts-and-expand" }, [
        vue.createElementVNode("view", { class: "global-counts" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.visibleGlobalCounts, (action) => {
              return vue.openBlock(), vue.createElementBlock(
                "view",
                {
                  key: action.value,
                  class: vue.normalizeClass(["count-item", {
                    "active": $setup.currentStatus === action.value,
                    "has-count": action.count > 0
                  }])
                },
                [
                  vue.createElementVNode(
                    "text",
                    { class: "emoji" },
                    vue.toDisplayString(action.emoji),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "count" },
                    vue.toDisplayString(action.count),
                    1
                    /* TEXT */
                  )
                ],
                2
                /* CLASS */
              );
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ]),
        vue.createElementVNode("view", {
          class: "expand-btn",
          onClick: vue.withModifiers($setup.togglePanel, ["stop"])
        }, [
          vue.createVNode(_component_uni_icons, {
            type: $setup.showPanel ? "arrowup" : "plus",
            size: "16",
            color: "#888"
          }, null, 8, ["type"]),
          vue.createElementVNode(
            "text",
            { class: "btn-text" },
            vue.toDisplayString($setup.showPanel ? "收起" : "表态"),
            1
            /* TEXT */
          )
        ])
      ]),
      $setup.showPanel ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "attitude-panel"
      }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.attitudeTypes, (action) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: action.value,
              class: vue.normalizeClass(["attitude-btn", { active: $setup.currentStatus === action.value }]),
              onClick: vue.withModifiers(($event) => $setup.handleAction(action.value), ["stop"])
            }, [
              vue.createElementVNode(
                "text",
                { class: "emoji" },
                vue.toDisplayString(action.emoji),
                1
                /* TEXT */
              ),
              vue.createElementVNode("view", { class: "text-container" }, [
                vue.createElementVNode(
                  "text",
                  { class: "label" },
                  vue.toDisplayString(action.label),
                  1
                  /* TEXT */
                )
              ])
            ], 10, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_2$4 = /* @__PURE__ */ _export_sfc(_sfc_main$1q, [["render", _sfc_render$1p], ["__scopeId", "data-v-f15d1ff3"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/attitude-widget/attitude-widget.vue"]]);
  const _sfc_main$1p = {
    __name: "comment-list",
    props: {
      type: {
        type: Number,
        required: true
      },
      relationId: {
        type: Number,
        required: true
      }
    },
    emits: ["reply"],
    setup(__props, { expose: __expose, emit: __emit }) {
      const props = __props;
      const commentList = vue.ref([]);
      const currentPage = vue.ref(1);
      const mainCommentsTotal = vue.ref(0);
      const hasMore = vue.ref(true);
      const subCommentPages = vue.reactive({});
      const emit = __emit;
      const loading2 = vue.ref(false);
      const tempCommentMap = vue.ref({});
      const handleAttitudeChange = (comment, {
        status,
        counts
      }) => {
        comment.attitudeStatus = status;
        comment.attitudeCounts = counts;
      };
      __expose({
        // 添加主评论（支持临时评论）
        addNewComment: (comment) => {
          const tempComment = {
            ...comment,
            isTemp: true,
            showAll: false,
            localChildren: [],
            childTotal: 0
          };
          tempCommentMap.value[comment.id] = tempComment;
          commentList.value.unshift(tempComment);
        },
        // 添加回复评论（支持临时评论）
        addReplyComment: (reply) => {
          const parentId = reply.parent_id;
          const parent = commentList.value.find((c2) => c2.id === parentId);
          if (parent) {
            const tempReply = {
              ...reply,
              isTemp: true
            };
            tempCommentMap.value[reply.id] = tempReply;
            if (!parent.localChildren) {
              parent.localChildren = [];
            }
            if (typeof parent.childTotal !== "number") {
              parent.childTotal = 0;
            }
            parent.localChildren.unshift(tempReply);
            parent.childTotal += 1;
            if (!parent.showAll && parent.childTotal <= 5) {
              parent.showAll = true;
            }
          }
        },
        // 更新临时评论为真实评论
        updateTempComment: (tempId, newComment) => {
          const mainIndex = commentList.value.findIndex((c2) => c2.id === tempId);
          if (mainIndex !== -1) {
            commentList.value[mainIndex] = {
              ...newComment,
              showAll: commentList.value[mainIndex].showAll,
              localChildren: commentList.value[mainIndex].localChildren || [],
              childTotal: commentList.value[mainIndex].childTotal || 0
            };
            delete tempCommentMap.value[tempId];
            return;
          }
          for (const parent of commentList.value) {
            if (parent.localChildren && parent.localChildren.length) {
              const childIndex = parent.localChildren.findIndex((c2) => c2.id === tempId);
              if (childIndex !== -1) {
                parent.localChildren[childIndex] = {
                  ...newComment,
                  // 保留原有的临时属性
                  parent_id: parent.localChildren[childIndex].parent_id
                };
                delete tempCommentMap.value[tempId];
                return;
              }
            }
          }
        },
        // 移除临时评论
        removeTempComment: (tempId) => {
          const mainIndex = commentList.value.findIndex((c2) => c2.id === tempId);
          if (mainIndex !== -1) {
            commentList.value.splice(mainIndex, 1);
            delete tempCommentMap.value[tempId];
            return;
          }
          for (const parent of commentList.value) {
            if (parent.localChildren && parent.localChildren.length) {
              const childIndex = parent.localChildren.findIndex((c2) => c2.id === tempId);
              if (childIndex !== -1) {
                parent.localChildren.splice(childIndex, 1);
                parent.childTotal = Math.max(0, parent.childTotal - 1);
                delete tempCommentMap.value[tempId];
                return;
              }
            }
          }
        }
      });
      vue.onMounted(() => {
        loadMainComments();
      });
      const visibleChildren = (comment) => {
        if (!comment.showAll && comment.localChildren.length > 5) {
          return comment.localChildren.slice(0, 5);
        }
        return comment.localChildren;
      };
      const loadMoreMainComments = async () => {
        if (loading2.value || !hasMore.value) {
          return;
        }
        try {
          loading2.value = true;
          currentPage.value += 1;
          await loadMainComments();
        } finally {
          loading2.value = false;
        }
      };
      const previewImage = (url) => {
        uni.previewImage({
          urls: [url],
          current: url
        });
      };
      const handleLike = async (comment) => {
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        try {
          const token = uni.getStorageSync("token");
          const url = `${websiteUrl.value}/with-state/${comment.user_like ? "unlike" : "add-like"}`;
          const res = await uni.request({
            url,
            method: "POST",
            header: {
              Authorization: token
            },
            data: {
              id: comment.id,
              type: 6
              // 评论类型
            }
          });
          if (res.data.status === "success") {
            comment.user_like = !comment.user_like;
            if (comment.user_like) {
              comment.like_count = (comment.like_count || 0) + 1;
            } else {
              comment.like_count = Math.max(0, (comment.like_count || 0) - 1);
            }
            uni.showToast({
              title: comment.user_like ? "点赞成功" : "已取消点赞",
              icon: "none"
            });
          } else {
            uni.showToast({
              title: res.data.msg || "操作失败",
              icon: "none"
            });
          }
        } catch (err) {
          formatAppLog("error", "at components/comment-list/comment-list.vue:398", "点赞失败:", err);
          uni.showToast({
            title: "操作失败",
            icon: "none"
          });
        }
      };
      const jump2user = (uid) => {
        uni.navigateTo({
          url: "/pages/user_page/user_page?uid=" + uid
        });
      };
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        return `${date.getMonth() + 1}-${date.getDate()} ${date.getHours()}:${date.getMinutes()}`;
      };
      const formatUsername = (name) => {
        if (!name)
          return "未知用户";
        return name.length > 12 ? name.slice(0, 12) + "..." : name;
      };
      const loadMainComments = async () => {
        try {
          loading2.value = true;
          let token = uni.getStorageSync("token");
          const res = await uni.request({
            url: `${websiteUrl.value}/get-comments`,
            data: {
              relation_id: props.relationId,
              type: props.type,
              page: currentPage.value,
              page_size: 10
              // 添加分页大小
            },
            header: {
              "Authorization": token
            }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            const newComments = data.comment_list.map((c2) => ({
              ...c2,
              showAll: false,
              localChildren: c2.children || [],
              childTotal: c2.childTotal || (c2.children ? c2.children.length : 0),
              // 新增表态相关字段
              attitudeStatus: c2.user_attitude || 0,
              // 当前用户表态状态
              attitudeCounts: c2.attitude_counts || {
                1: c2.count_1 || 0,
                2: c2.count_2 || 0,
                3: c2.count_3 || 0,
                4: c2.count_4 || 0,
                5: c2.count_5 || 0
              }
            }));
            if (currentPage.value === 1) {
              commentList.value = newComments;
            } else {
              commentList.value.push(...newComments);
            }
            formatAppLog("log", "at components/comment-list/comment-list.vue:464", "total", data.total);
            hasMore.value = data.total > commentList.value.length;
            mainCommentsTotal.value = data.total;
            formatAppLog("log", "at components/comment-list/comment-list.vue:468", "是否还有更多?", hasMore.value);
          }
        } catch (err) {
          uni.showToast({
            title: "加载失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const handleReply = (parent, target = null) => {
        emit("reply", {
          parent,
          target,
          relationId: props.relationId
        });
      };
      const getAssociationTypeText = (type) => {
        return type === 1 ? "娃物" : type === 2 ? "店铺" : "关联内容";
      };
      const navigateToAssociation = (comment) => {
        if (!comment.association_id)
          return;
        if (comment.association_type === 1) {
          uni.navigateTo({
            url: `/pages/goods/goods?goods_id=${comment.association_id}`
          });
        } else if (comment.association_type === 2) {
          uni.navigateTo({
            url: `/pages/brand/brand?brand_id=${comment.association_id}`
          });
        }
      };
      const shouldShowMore = (comment) => {
        return comment.childTotal > 5 && !comment.showAll;
      };
      const remainingCount = (comment) => {
        return comment.childTotal - Math.min(comment.localChildren.length, 5);
      };
      const loadMore = async (comment) => {
        if (comment.localChildren.length < comment.childTotal) {
          const nextPage = Math.ceil(comment.localChildren.length / 5) + 1;
          try {
            const res = await uni.request({
              url: `${websiteUrl.value}/get-comments-by-parent-id`,
              data: {
                parent_id: comment.id,
                page: nextPage
              }
            });
            if (res.data.status === "success") {
              comment.localChildren = [
                ...comment.localChildren,
                ...res.data.data.comment_list
              ];
              comment.childTotal = res.data.data.total;
              if (comment.localChildren.length >= 5 && !comment.showAll) {
                comment.showAll = true;
              }
            }
          } catch (err) {
            uni.showToast({
              title: "加载失败",
              icon: "none"
            });
          }
        } else {
          comment.showAll = !comment.showAll;
        }
      };
      const __returned__ = { props, commentList, currentPage, mainCommentsTotal, hasMore, subCommentPages, emit, loading: loading2, tempCommentMap, handleAttitudeChange, visibleChildren, loadMoreMainComments, previewImage, handleLike, jump2user, formatTime, formatUsername, loadMainComments, handleReply, getAssociationTypeText, navigateToAssociation, shouldShowMore, remainingCount, loadMore, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, reactive: vue.reactive, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getScene() {
        return getScene;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1o(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_report_button = resolveEasycom(vue.resolveDynamicComponent("report-button"), __easycom_1$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_attitude_widget = resolveEasycom(vue.resolveDynamicComponent("attitude-widget"), __easycom_2$4);
    return vue.openBlock(), vue.createElementBlock("view", { class: "comment-container" }, [
      $setup.commentList.length > 0 ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 0,
          class: "comment-count"
        },
        " 共" + vue.toDisplayString($setup.mainCommentsTotal) + "条回复 ",
        1
        /* TEXT */
      )) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "empty-tips"
      }, [
        vue.createElementVNode("text", null, "-- 暂无回复 --")
      ])),
      (vue.openBlock(true), vue.createElementBlock(
        vue.Fragment,
        null,
        vue.renderList($setup.commentList, (comment) => {
          return vue.openBlock(), vue.createElementBlock("view", {
            key: comment.id,
            class: "comment-card"
          }, [
            vue.createElementVNode("view", { class: "main-comment" }, [
              vue.createElementVNode("image", {
                onClick: ($event) => $setup.jump2user(comment.uid),
                src: comment.avatar,
                class: "avatar",
                mode: "aspectFill"
              }, null, 8, ["onClick", "src"]),
              vue.createElementVNode("view", { class: "content" }, [
                vue.createElementVNode("view", { class: "header" }, [
                  vue.createElementVNode("view", null, [
                    vue.createElementVNode("text", {
                      class: "username",
                      onClick: ($event) => $setup.jump2user(comment.uid)
                    }, vue.toDisplayString($setup.formatUsername(comment.username)), 9, ["onClick"]),
                    vue.createElementVNode(
                      "text",
                      { class: "floor" },
                      "#" + vue.toDisplayString(comment.floor),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createVNode(_component_report_button, {
                    "report-type": 5,
                    "relation-id": comment.id,
                    "button-text": "举报",
                    "icon-color": "#999",
                    "theme-color": "#64c6dc",
                    "icon-size": "24"
                  }, null, 8, ["relation-id"])
                ]),
                comment.image_url ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "comment-images"
                }, [
                  vue.createElementVNode("image", {
                    src: comment.image_url,
                    mode: "aspectFill",
                    class: "comment-image",
                    onClick: ($event) => $setup.previewImage(comment.image_url)
                  }, null, 8, ["src", "onClick"])
                ])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("text", {
                  class: "comment-text",
                  onClick: ($event) => $setup.handleReply(comment)
                }, vue.toDisplayString(comment.comment), 9, ["onClick"]),
                comment.association_id ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 1,
                  class: "association-card",
                  onClick: ($event) => $setup.navigateToAssociation(comment)
                }, [
                  vue.createElementVNode("image", {
                    src: comment.association_image,
                    mode: "aspectFill",
                    class: "association-image"
                  }, null, 8, ["src"]),
                  vue.createElementVNode("view", { class: "association-info" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "association-name" },
                      vue.toDisplayString(comment.association_name),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "association-type" },
                      vue.toDisplayString($setup.getAssociationTypeText(comment.association_type)),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createVNode(_component_uni_icons, {
                    type: "arrow-right",
                    size: "20",
                    color: "#999",
                    class: "association-arrow"
                  })
                ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "footer" }, [
                  vue.createElementVNode("view", { class: "left-actions" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "time" },
                      vue.toDisplayString($setup.formatTime(comment.created_at)),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("view", { class: "like-container" }, [
                      vue.createElementVNode("view", {
                        class: "like-btn",
                        onClick: ($event) => $setup.handleLike(comment)
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: comment.user_like ? "hand-up-filled" : "hand-up",
                          size: "18",
                          color: comment.user_like ? "rgb(100 198 220)" : "#999"
                        }, null, 8, ["type", "color"]),
                        vue.createElementVNode(
                          "text",
                          {
                            class: vue.normalizeClass(["like-count", { liked: comment.user_like }])
                          },
                          vue.toDisplayString(comment.like_count || 0),
                          3
                          /* TEXT, CLASS */
                        )
                      ], 8, ["onClick"])
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "right-actions" }, [
                    vue.createVNode(_component_attitude_widget, {
                      "target-id": comment.id,
                      type: 6,
                      "attitude-status": comment.attitudeStatus,
                      "attitude-counts": comment.attitudeCounts,
                      onChange: ($event) => $setup.handleAttitudeChange(comment, $event)
                    }, null, 8, ["target-id", "attitude-status", "attitude-counts", "onChange"]),
                    vue.createElementVNode("text", {
                      class: "reply-btn",
                      onClick: ($event) => $setup.handleReply(comment)
                    }, "回复", 8, ["onClick"])
                  ])
                ])
              ])
            ]),
            comment.localChildren && comment.localChildren.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "sub-comments"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.visibleChildren(comment), (child, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: child.id,
                    class: "sub-comment"
                  }, [
                    vue.createElementVNode("image", {
                      onClick: ($event) => $setup.jump2user(child.uid),
                      src: child.avatar,
                      class: "avatar",
                      mode: "aspectFill"
                    }, null, 8, ["onClick", "src"]),
                    vue.createElementVNode("view", { class: "content" }, [
                      vue.createElementVNode("view", { class: "header" }, [
                        vue.createElementVNode("text", {
                          onClick: ($event) => $setup.jump2user(child.uid),
                          class: "username"
                        }, vue.toDisplayString($setup.formatUsername(child.username)), 9, ["onClick"]),
                        vue.createVNode(_component_report_button, {
                          "report-type": 5,
                          "relation-id": child.id,
                          "button-text": "举报",
                          "icon-color": "#999",
                          "theme-color": "#64c6dc"
                        }, null, 8, ["relation-id"])
                      ]),
                      child.image_url ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "comment-images"
                      }, [
                        vue.createElementVNode("image", {
                          src: child.image_url,
                          mode: "aspectFill",
                          class: "comment-image",
                          onClick: ($event) => $setup.previewImage(child.image_url)
                        }, null, 8, ["src", "onClick"])
                      ])) : vue.createCommentVNode("v-if", true),
                      vue.createElementVNode("text", {
                        class: "comment-text",
                        onClick: ($event) => $setup.handleReply(comment)
                      }, vue.toDisplayString(child.comment), 9, ["onClick"]),
                      child.association_id ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "association-card",
                        onClick: ($event) => $setup.navigateToAssociation(child)
                      }, [
                        vue.createElementVNode("image", {
                          src: child.association_image,
                          mode: "aspectFill",
                          class: "association-image"
                        }, null, 8, ["src"]),
                        vue.createElementVNode("view", { class: "association-info" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "association-name" },
                            vue.toDisplayString(child.association_name),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "association-type" },
                            vue.toDisplayString($setup.getAssociationTypeText(child.association_type)),
                            1
                            /* TEXT */
                          )
                        ]),
                        vue.createVNode(_component_uni_icons, {
                          type: "arrow-right",
                          size: "20",
                          color: "#999",
                          class: "association-arrow"
                        })
                      ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                      vue.createElementVNode("view", { class: "footer" }, [
                        vue.createElementVNode("view", { class: "left-actions" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "time" },
                            vue.toDisplayString($setup.formatTime(child.created_at)),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode("view", { class: "like-container" }, [
                            vue.createElementVNode("view", {
                              class: "like-btn",
                              onClick: ($event) => $setup.handleLike(child)
                            }, [
                              vue.createVNode(_component_uni_icons, {
                                type: child.user_like ? "hand-up-filled" : "hand-up",
                                size: "18",
                                color: child.user_like ? "rgb(100 198 220)" : "#999"
                              }, null, 8, ["type", "color"]),
                              vue.createElementVNode(
                                "text",
                                {
                                  class: vue.normalizeClass(["like-count", { liked: child.user_like }])
                                },
                                vue.toDisplayString(child.like_count || 0),
                                3
                                /* TEXT, CLASS */
                              )
                            ], 8, ["onClick"])
                          ])
                        ]),
                        vue.createElementVNode("view", { class: "right-actions" }, [
                          vue.createVNode(_component_attitude_widget, {
                            "target-id": child.id,
                            type: 6,
                            "attitude-status": comment.attitudeStatus,
                            "attitude-counts": comment.attitudeCounts,
                            onChange: ($event) => $setup.handleAttitudeChange(comment, $event)
                          }, null, 8, ["target-id", "attitude-status", "attitude-counts", "onChange"]),
                          vue.createElementVNode("text", {
                            class: "reply-btn",
                            onClick: ($event) => $setup.handleReply(comment, child)
                          }, "回复", 8, ["onClick"])
                        ])
                      ])
                    ])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              )),
              $setup.shouldShowMore(comment) ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "load-more",
                onClick: ($event) => $setup.loadMore(comment)
              }, [
                vue.createElementVNode(
                  "text",
                  null,
                  "展开" + vue.toDisplayString($setup.remainingCount(comment)) + "条回复",
                  1
                  /* TEXT */
                ),
                vue.createVNode(_component_uni_icons, {
                  type: "arrow-down",
                  size: "18",
                  color: "#007AFF"
                })
              ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
            ])) : vue.createCommentVNode("v-if", true)
          ]);
        }),
        128
        /* KEYED_FRAGMENT */
      )),
      $setup.commentList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        class: "load-more-box"
      }, [
        $setup.hasMore ? (vue.openBlock(), vue.createElementBlock(
          "view",
          {
            key: 0,
            class: vue.normalizeClass(["load-more-btn", { loading: $setup.loading }]),
            onClick: $setup.loadMoreMainComments
          },
          [
            !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
              vue.createElementVNode("text", null, " 加载更多 "),
              vue.createVNode(_component_uni_icons, {
                type: "arrow-down",
                size: "18",
                color: "#007AFF"
              })
            ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
              vue.createVNode(_component_uni_icons, {
                type: "spinner-cycle",
                size: "16",
                color: "#888"
              })
            ]))
          ],
          2
          /* CLASS */
        )) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "no-more"
        }, [
          vue.createElementVNode("text", null, "-- 没有更多了 --")
        ]))
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_3$3 = /* @__PURE__ */ _export_sfc(_sfc_main$1p, [["render", _sfc_render$1o], ["__scopeId", "data-v-ea2f609f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/comment-list/comment-list.vue"]]);
  const _sfc_main$1o = {
    __name: "goods-search",
    props: {
      modelValue: {
        type: String,
        default: ""
      },
      width: {
        type: String,
        default: "100%"
      },
      background: {
        type: String,
        default: ""
      },
      fontSize: {
        type: String,
        default: ""
      },
      tagColor: {
        type: String,
        default: "#666"
        // 新增标签颜色控制
      },
      mode: {
        type: String,
        default: "jump",
        validator: (value) => ["jump", "fill"].includes(value)
      },
      hiddenIcon: {
        type: Boolean,
        default: true
      },
      brandId: {
        // 新增brandId属性
        type: Number,
        default: 0
      }
    },
    emits: ["select", "update:modelValue"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const showResults = vue.ref(false);
      const props = __props;
      const emit = __emit;
      const results = vue.ref([]);
      const inputValue = vue.computed({
        get: () => props.modelValue,
        set: (val) => emit("update:modelValue", val)
      });
      vue.watch(results, (newVal) => {
        showResults.value = newVal.length > 0;
      });
      const closeResults = () => {
        results.value = [];
        showResults.value = false;
      };
      const onSearchInput = async (e2) => {
        var _a;
        inputValue.value = e2.detail.value;
        const searchValue = e2.detail.value.trim();
        showResults.value = true;
        if (!searchValue) {
          results.value = [];
          return;
        }
        let url = `${websiteUrl.value}/search-goods?search=${encodeURIComponent(searchValue)}`;
        if (props.brandId && props.brandId !== 0) {
          url += `&brand_id=${props.brandId}`;
        }
        try {
          const res = await uni.request({
            url,
            method: "GET"
          });
          results.value = ((_a = res.data) == null ? void 0 : _a.status) === "success" ? res.data.data || [] : [];
        } catch (error2) {
          formatAppLog("error", "at components/goods-search/goods-search.vue:120", "搜索失败:", error2);
          results.value = [];
        }
      };
      const onTap = (goods) => {
        if (props.mode === "jump") {
          uni.navigateTo({
            url: `/pages/goods/goods?goods_id=${goods.id}`
          });
        }
        if (props.mode === "fill") {
          emit("select", goods);
          inputValue.value = goods.name;
          results.value = [];
        }
        closeResults();
      };
      const cancel = () => {
        closeResults();
      };
      const __returned__ = { showResults, props, emit, results, inputValue, closeResults, onSearchInput, onTap, cancel, ref: vue.ref, computed: vue.computed, watch: vue.watch, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1n(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        $setup.showResults ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "mask-layer",
          onClick: $setup.closeResults
        })) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["search_tab", _ctx.$attrs.class]),
            style: vue.normalizeStyle({ background: $props.background || "#fff" })
          },
          [
            !$props.hiddenIcon ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 0,
              class: "icon_image",
              src: _imports_0$g
            })) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("input", {
              class: "common_search_input",
              placeholder: "请输入娃物名称 (ㅇㅅㅇ❀)...",
              value: $setup.inputValue,
              onInput: $setup.onSearchInput,
              style: vue.normalizeStyle({ fontSize: $props.fontSize || "23rpx" })
            }, null, 44, ["value"]),
            $setup.results.length > 0 ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 1,
              class: "icon_image",
              src: _imports_2$5,
              onClick: $setup.cancel
            })) : vue.createCommentVNode("v-if", true)
          ],
          6
          /* CLASS, STYLE */
        ),
        $setup.results.length > 0 ? (vue.openBlock(), vue.createElementBlock(
          "scroll-view",
          {
            key: 1,
            class: "search_results",
            style: vue.normalizeStyle({ width: $props.width }),
            "scroll-y": ""
          },
          [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.results, (item) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "result_item",
                  onClick: ($event) => $setup.onTap(item)
                }, [
                  item.brand_name ? (vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: 0,
                      class: "brand-tag"
                    },
                    vue.toDisplayString(item.brand_name),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode(
                    "text",
                    { class: "goods-name" },
                    vue.toDisplayString(item.name),
                    1
                    /* TEXT */
                  )
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ],
          4
          /* STYLE */
        )) : vue.createCommentVNode("v-if", true)
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const __easycom_3$2 = /* @__PURE__ */ _export_sfc(_sfc_main$1o, [["render", _sfc_render$1n], ["__scopeId", "data-v-445b599f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/goods-search/goods-search.vue"]]);
  const _sfc_main$1n = {
    __name: "switch-search",
    props: {
      mode: {
        type: String,
        default: "jump",
        validator: (value) => ["jump", "fill"].includes(value)
      },
      width: {
        type: String,
        default: "100%"
      },
      background: {
        type: String,
        default: "#fff"
      },
      fontSize: {
        type: String,
        default: "28rpx"
      }
    },
    emits: ["submit"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      vue.useCssVars((_ctx) => ({
        "946fdc68-props.width": props.width
      }));
      const searchType = vue.ref("goods");
      const goodsResults = vue.ref([]);
      const brandResults = vue.ref([]);
      const goodsSearchRef = vue.ref(null);
      const commonSearchRef = vue.ref(null);
      const hasResults = vue.computed(() => {
        return searchType.value === "goods" && goodsResults.value.length > 0 || searchType.value === "brand" && brandResults.value.length > 0;
      });
      const toggleSearchType = () => {
        if (searchType.value === "goods") {
          goodsResults.value = [];
        } else {
          brandResults.value = [];
        }
        searchType.value = searchType.value === "goods" ? "brand" : "goods";
      };
      const handleGoodsSelect = (goods) => {
        goodsResults.value = goods ? [goods] : [];
        if (goods) {
          emit("submit", {
            type: "goods",
            data: goods
          });
        }
      };
      const handleBrandSelect = (id, name) => {
        if (id === 0) {
          brandResults.value = [];
        } else {
          brandResults.value = [{
            id,
            name
          }];
          emit("submit", {
            type: "brand",
            data: {
              id,
              name
            }
          });
        }
      };
      const selectGoods = (goods) => {
        if (props.mode === "jump") {
          uni.navigateTo({
            url: `/pages/goods/goods?goods_id=${goods.id}`
          });
        }
      };
      const selectBrand = (brand) => {
        if (props.mode === "jump") {
          uni.navigateTo({
            url: `/pages/brand/brand?brand_id=${brand.id}`
          });
        }
      };
      const props = __props;
      const searchWidth = vue.computed(() => {
        return "700rpx";
      });
      const emit = __emit;
      const __returned__ = { searchType, goodsResults, brandResults, goodsSearchRef, commonSearchRef, hasResults, toggleSearchType, handleGoodsSelect, handleBrandSelect, selectGoods, selectBrand, props, searchWidth, emit, ref: vue.ref, computed: vue.computed };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1m(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_goods_search = resolveEasycom(vue.resolveDynamicComponent("goods-search"), __easycom_3$2);
    const _component_common_search = resolveEasycom(vue.resolveDynamicComponent("common-search"), __easycom_2$a);
    return vue.openBlock(), vue.createElementBlock("view", { class: "integrated-search-container" }, [
      vue.createElementVNode("view", { class: "search-header" }, [
        vue.createElementVNode(
          "view",
          {
            class: "search-wrapper",
            style: vue.normalizeStyle({ width: $setup.searchWidth })
          },
          [
            $setup.searchType === "goods" ? (vue.openBlock(), vue.createBlock(_component_goods_search, {
              key: 0,
              ref: "goodsSearchRef",
              width: $setup.searchWidth,
              background: $props.background,
              "hidden-icon": false,
              mode: $props.mode,
              onSelect: $setup.handleGoodsSelect
            }, null, 8, ["width", "background", "mode"])) : (vue.openBlock(), vue.createBlock(_component_common_search, {
              key: 1,
              ref: "commonSearchRef",
              width: $setup.searchWidth,
              background: $props.background,
              fontSize: $props.fontSize,
              hiddenIcon: true,
              mode: $props.mode,
              onSelect: $setup.handleBrandSelect,
              "show-index-selector": ""
            }, null, 8, ["width", "background", "fontSize", "mode"]))
          ],
          4
          /* STYLE */
        ),
        vue.createElementVNode("view", {
          class: "toggle-button",
          onClick: $setup.toggleSearchType
        }, [
          vue.createElementVNode(
            "text",
            null,
            vue.toDisplayString($setup.searchType === "goods" ? "娃物" : "作者"),
            1
            /* TEXT */
          )
        ])
      ]),
      $setup.hasResults ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "results-container"
      }, [
        $setup.searchType === "goods" && $setup.goodsResults.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "goods-results"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.goodsResults, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: item.id,
                class: "result-item",
                onClick: ($event) => $setup.selectGoods(item)
              }, [
                vue.createElementVNode(
                  "view",
                  { class: "brand-tag" },
                  vue.toDisplayString(item.brand_name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "goods-name" },
                  vue.toDisplayString(item.name),
                  1
                  /* TEXT */
                )
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : $setup.searchType === "brand" && $setup.brandResults.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "brand-results"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.brandResults, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: item.id,
                class: "result-item",
                onClick: ($event) => $setup.selectBrand(item)
              }, vue.toDisplayString(item.name), 9, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : vue.createCommentVNode("v-if", true)
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_1$8 = /* @__PURE__ */ _export_sfc(_sfc_main$1n, [["render", _sfc_render$1m], ["__scopeId", "data-v-946fdc68"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/switch-search/switch-search.vue"]]);
  function chooseImage() {
    return new Promise((resolve) => {
      uni.chooseImage({
        count: 1,
        success: (res) => {
          resolve(res.tempFilePaths[0]);
        }
      });
    });
  }
  function chooseImageList(count = 9) {
    return new Promise((resolve, reject) => {
      uni.chooseImage({
        count,
        success: (res) => {
          resolve(res.tempFilePaths);
        },
        fail: (err) => {
          reject(err);
        }
      });
    });
  }
  function jumpToCroper() {
    chooseImage().then((src) => {
      uni.navigateTo({
        url: `/pages/pop_croper/pop_croper?src=${decodeURIComponent(src)}`
      });
    });
  }
  function getQiniuToken() {
    return new Promise((resolve, reject) => {
      let token = uni.getStorageSync("token");
      uni.request({
        url: websiteUrl.value + "/with-state/qiniu-token",
        method: "POST",
        header: {
          "Authorization": token
        },
        success: (res) => {
          if (res.data.data && res.data.data.token) {
            formatAppLog("log", "at common/image.js:54", "获取到的七牛token：" + res.data.data.token);
            resolve(res.data.data);
          } else {
            uni.showToast({
              title: "获取上传凭证失败",
              icon: "none"
            });
            reject("获取上传凭证失败");
          }
        },
        fail: (err) => {
          uni.showToast({
            title: "获取上传凭证失败",
            icon: "none"
          });
          reject("获取上传凭证失败");
        }
      });
    });
  }
  function uploadImageToQiniu(croperPath, qnToken, fileName) {
    let authorization = uni.getStorageSync("token");
    return new Promise((resolve, reject) => {
      uni.uploadFile({
        url: "https://up-cn-east-2.qiniup.com",
        name: "file",
        method: "POST",
        filePath: croperPath,
        fileType: "image",
        formData: {
          token: qnToken,
          key: fileName,
          scope: "hobby-box:" + fileName
        },
        success: async (res) => {
          try {
            const fullUrl = image1Url + fileName;
            const logRes = await uni.request({
              url: `${websiteUrl.value}/with-state/add-image-log`,
              method: "POST",
              header: {
                "Content-Type": "application/json",
                "Authorization": authorization
              },
              data: {
                image_url: fullUrl
              }
            });
            formatAppLog("log", "at common/image.js:108", logRes);
            if (logRes.data.status !== "success") {
              throw new Error("上传图片失败");
            }
            resolve({ qiniuRes: res, imageUrl: fullUrl });
          } catch (logErr) {
            formatAppLog("error", "at common/image.js:115", "日志记录失败:", logErr);
            uni.showToast({
              title: "日志记录失败",
              icon: "none"
            });
            reject("日志记录失败");
          }
        },
        fail: (err) => {
          uni.showToast({
            title: "上传失败",
            icon: "none"
          });
          reject("上传失败");
        }
      });
    });
  }
  const _sfc_main$1m = {
    __name: "comment-input",
    props: {
      replyInfo: {
        type: Object,
        default: () => ({})
      },
      targetId: {
        type: String,
        required: true
      },
      safeBottom: {
        type: Number,
        default: 10
      }
    },
    emits: ["submit", "update:replyInfo"],
    setup(__props, { expose: __expose, emit: __emit }) {
      const showPopup = vue.ref(false);
      const props = __props;
      const inputRef = vue.ref(null);
      const isFocused = vue.ref(false);
      const emit = __emit;
      const instance = vue.getCurrentInstance();
      const commentText = vue.ref("");
      const displayMask = vue.ref(false);
      const clickedInside = vue.ref(false);
      const showActionBar = vue.ref(false);
      const isAnonymous = vue.ref(false);
      const selectedAssociation = vue.ref(null);
      const uploadedImageUrl = vue.ref("");
      const keyboardHeight = vue.ref(0);
      const systemInfo2 = uni.getSystemInfoSync();
      const footerBottomHeight = vue.computed(() => {
        var _a, _b;
        let safeBottomVar = ((_a = systemInfo2.safeAreaInsets) == null ? void 0 : _a.bottom) || 10;
        formatAppLog("log", "at components/comment-input/comment-input.vue:138", "底部安全距离1:", (_b = systemInfo2.safeAreaInsets) == null ? void 0 : _b.bottom, "=>", safeBottomVar);
        let safeBottom = safeBottomVar + keyboardHeight.value;
        formatAppLog("log", "at components/comment-input/comment-input.vue:141", "底部最终距离2:", safeBottom);
        return `${safeBottom}px`;
      });
      const keyboardHeightChangeHandler2 = (res) => {
        formatAppLog("log", "at components/comment-input/comment-input.vue:146", "键盘高度变化", res);
        keyboardHeight.value = res.height;
      };
      const toggleAnonymous = (event) => {
        formatAppLog("log", "at components/comment-input/comment-input.vue:151", "切换匿名状态", isAnonymous.value);
        isAnonymous.value = !isAnonymous.value;
        if (isAnonymous.value) {
          uni.showToast({
            title: "进入匿名状态",
            icon: "none"
          });
        } else {
          uni.showToast({
            title: "退出匿名状态",
            icon: "none"
          });
        }
      };
      const handleFocus = () => {
        displayMask.value = true;
        isFocused.value = true;
        showActionBar.value = true;
      };
      const handleBlur = () => {
        setTimeout(() => {
          if (!clickedInside.value) {
            if (!showActionBar.value) {
              displayMask.value = false;
            }
            isFocused.value = false;
          }
          clickedInside.value = false;
        }, 200);
      };
      async function selectImage() {
        uni.chooseImage({
          count: 1,
          success: async (res) => {
            const tempFilePaths = res.tempFilePaths;
            for (const filePath of tempFilePaths) {
              try {
                const tokenData = await getQiniuToken();
                const uploadRes = await uploadImageToQiniu(filePath, tokenData.token, tokenData.path);
                formatAppLog("log", "at components/comment-input/comment-input.vue:203", "res:", uploadRes);
                if (uploadRes.qiniuRes.statusCode === 200) {
                  const imageUrl = image1Url + tokenData.path;
                  uploadedImageUrl.value = imageUrl;
                } else {
                  formatAppLog("error", "at components/comment-input/comment-input.vue:208", "上传失败:", filePath);
                }
              } catch (error2) {
                formatAppLog("error", "at components/comment-input/comment-input.vue:211", "上传错误:", error2);
              }
            }
          }
        });
      }
      const handleMaskTap = () => {
        formatAppLog("log", "at components/comment-input/comment-input.vue:221", "蒙版被点击");
        displayMask.value = false;
        isFocused.value = false;
        showActionBar.value = false;
        uni.hideKeyboard();
      };
      const handleActionBarClick = () => {
        clickedInside.value = true;
      };
      function handleSearchSelect(event) {
        let edata = {};
        if (event.type === "goods") {
          formatAppLog("log", "at components/comment-input/comment-input.vue:235", "选择了商品:", event.data);
          showPopup.value = false;
          edata.type = 1;
          edata.id = event.data.id;
          edata.name = event.data.name;
        } else if (event.type === "brand") {
          formatAppLog("log", "at components/comment-input/comment-input.vue:243", "选择了品牌:", event.data);
          showPopup.value = false;
          edata.type = 2;
          edata.id = event.data.id;
          edata.name = event.data.name;
        }
        selectedAssociation.value = edata;
      }
      const openAssociationPopup = () => {
        handleActionBarClick();
      };
      const handleSubmit = () => {
        if (!commentText.value.trim() && !uploadedImageUrl.value) {
          uni.showToast({
            title: "请输入评论内容或上传图片",
            icon: "none"
          });
          return;
        }
        let scene = getScene();
        formatAppLog("log", "at components/comment-input/comment-input.vue:267", "scene", scene);
        const submitData = {
          content: commentText.value,
          target_id: props.targetId,
          type: 4,
          replyInfo: props.replyInfo,
          origin: scene,
          // 添加图片和关联信息
          image_url: uploadedImageUrl.value || "",
          association_id: selectedAssociation.value ? selectedAssociation.value.id : 0,
          association_type: selectedAssociation.value ? selectedAssociation.value.type : 0,
          is_anonymous: isAnonymous.value ? 1 : 0
          // 1表示匿名，0表示正常
        };
        emit("submit", submitData);
        commentText.value = "";
        uploadedImageUrl.value = "";
        selectedAssociation.value = null;
        isAnonymous.value = false;
        emit("update:replyInfo", {});
        handleMaskTap();
      };
      const focusInput = () => {
        isFocused.value = true;
      };
      __expose({
        focusInput
      });
      vue.onMounted(() => {
        formatAppLog("log", "at components/comment-input/comment-input.vue:312", "注册键盘弹出事件");
        uni.onKeyboardHeightChange(keyboardHeightChangeHandler2);
      });
      const __returned__ = { showPopup, props, inputRef, isFocused, emit, instance, commentText, displayMask, clickedInside, showActionBar, isAnonymous, selectedAssociation, uploadedImageUrl, keyboardHeight, systemInfo: systemInfo2, footerBottomHeight, keyboardHeightChangeHandler: keyboardHeightChangeHandler2, toggleAnonymous, handleFocus, handleBlur, selectImage, handleMaskTap, handleActionBarClick, handleSearchSelect, openAssociationPopup, handleSubmit, focusInput, ref: vue.ref, computed: vue.computed, getCurrentInstance: vue.getCurrentInstance, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get getScene() {
        return getScene;
      }, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get chooseImage() {
        return chooseImage;
      }, get jumpToCroper() {
        return jumpToCroper;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1l(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_switch_search = resolveEasycom(vue.resolveDynamicComponent("switch-search"), __easycom_1$8);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.withDirectives(vue.createElementVNode(
          "view",
          {
            class: "mask",
            onClick: $setup.handleMaskTap
          },
          null,
          512
          /* NEED_PATCH */
        ), [
          [vue.vShow, $setup.displayMask]
        ]),
        vue.createElementVNode(
          "view",
          {
            class: "bottom_tab",
            "adjust-position": false,
            style: vue.normalizeStyle({ paddingBottom: $setup.footerBottomHeight })
          },
          [
            vue.createElementVNode("view", { class: "about_info" }, [
              $setup.selectedAssociation ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "association-container"
              }, [
                vue.createElementVNode("view", { class: "association-item" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "paperclip",
                    size: "20",
                    color: "#fff"
                  }),
                  vue.createElementVNode(
                    "text",
                    { class: "association-text" },
                    vue.toDisplayString($setup.selectedAssociation.name),
                    1
                    /* TEXT */
                  ),
                  vue.createVNode(_component_uni_icons, {
                    type: "close",
                    size: "20",
                    color: "#fff",
                    onClick: _cache[0] || (_cache[0] = vue.withModifiers(($event) => $setup.selectedAssociation = null, ["stop"])),
                    class: "close-icon"
                  })
                ])
              ])) : vue.createCommentVNode("v-if", true),
              $setup.uploadedImageUrl ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "image-container"
              }, [
                vue.createElementVNode("view", { class: "image-item" }, [
                  vue.createElementVNode("image", {
                    src: $setup.uploadedImageUrl,
                    mode: "aspectFill",
                    class: "uploaded-image"
                  }, null, 8, ["src"]),
                  vue.createElementVNode("view", {
                    class: "delete-icon",
                    onClick: _cache[1] || (_cache[1] = vue.withModifiers(($event) => $setup.uploadedImageUrl = "", ["stop"]))
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "close",
                      size: "18",
                      color: "#fff"
                    })
                  ])
                ])
              ])) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "bottom_input" }, [
              vue.withDirectives(vue.createElementVNode("textarea", {
                "disable-default-padding": true,
                focus: $setup.isFocused,
                class: vue.normalizeClass(["comment_input unified-textarea", { expanded: $setup.isFocused }]),
                ref: "inputRef",
                "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.commentText = $event),
                onFocus: $setup.handleFocus,
                onBlur: $setup.handleBlur,
                "adjust-position": false,
                placeholder: $props.replyInfo.username ? "回复@" + $props.replyInfo.username + " " : "写评论..."
              }, null, 42, ["focus", "placeholder"]), [
                [vue.vModelText, $setup.commentText]
              ]),
              vue.createElementVNode("button", { onClick: $setup.handleSubmit }, "写评论")
            ]),
            vue.withDirectives(vue.createElementVNode(
              "view",
              { class: "action-bar" },
              [
                vue.createElementVNode("view", {
                  onClick: vue.withModifiers($setup.toggleAnonymous, ["stop"])
                }, [
                  $setup.isAnonymous ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                    key: 0,
                    type: "eye-slash",
                    size: "22",
                    color: $setup.isAnonymous ? "#007aff" : ""
                  }, null, 8, ["color"])) : (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                    key: 1,
                    type: "eye",
                    size: "22",
                    color: "#696a6c"
                  }))
                ]),
                vue.createElementVNode("view", {
                  onClick: _cache[3] || (_cache[3] = vue.withModifiers(($event) => $setup.showPopup = true, ["stop"]))
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "plus",
                    size: "22",
                    color: "#696a6c"
                  })
                ]),
                vue.createElementVNode("view", {
                  onClick: vue.withModifiers($setup.selectImage, ["stop"])
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "image",
                    size: "22",
                    color: "#696a6c"
                  })
                ])
              ],
              512
              /* NEED_PATCH */
            ), [
              [vue.vShow, $setup.showActionBar]
            ]),
            vue.createElementVNode("view"),
            vue.createVNode(_component_bottom_popup, {
              show: $setup.showPopup,
              onClose: _cache[4] || (_cache[4] = ($event) => $setup.showPopup = false)
            }, {
              default: vue.withCtx(() => [
                $setup.showPopup ? (vue.openBlock(), vue.createBlock(_component_switch_search, {
                  key: 0,
                  "target-id": $setup.props.targetId,
                  "reply-info": $setup.props.replyInfo,
                  mode: "fill",
                  onSubmit: $setup.handleSearchSelect
                }, null, 8, ["target-id", "reply-info"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { style: { "height": "900rpx" } })
              ]),
              _: 1
              /* STABLE */
            }, 8, ["show"])
          ],
          4
          /* STYLE */
        )
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const __easycom_4$1 = /* @__PURE__ */ _export_sfc(_sfc_main$1m, [["render", _sfc_render$1l], ["__scopeId", "data-v-88f35a0c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/comment-input/comment-input.vue"]]);
  const _imports_1$5 = "/static/right2.png";
  const NAV_HEIGHT_RPX = 100;
  const _sfc_main$1l = {
    __name: "goods",
    props: ["goods_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      function goBack() {
        const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
        if (pages2.length > 1)
          uni.navigateBack({ delta: 1 });
        else
          uni.switchTab({ url: "/pages/index/index" });
      }
      const navHeightPx = uni.upx2px(NAV_HEIGHT_RPX);
      const topSafeSpacePx = vue.ref(toPx(getWindowTop() + navHeightPx));
      const loading2 = vue.ref(false);
      const error2 = vue.ref(false);
      const errorMsg = vue.ref("");
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      let replyForItem = vue.ref({});
      let goods = vue.ref({});
      let swiperIndex = vue.ref(1);
      let hasLike = vue.ref(false);
      let collocationList = vue.ref(false);
      let wishLoading = vue.ref(false);
      let hasWish = vue.ref(false);
      let wishCount = vue.ref(0);
      const swiperHeight = vue.ref(400);
      const imageHeights = vue.ref([]);
      const maxHeight = vue.ref(uni.upx2px(1e3));
      const faceupList = vue.ref([]);
      const faceupLoading = vue.ref(false);
      const showFaceupSection = vue.computed(
        () => goods.value.type === "单头" || goods.value.type === "整体"
      );
      const currentId = vue.ref(String(props.goods_id || ""));
      const lastGoodsId = vue.ref(String(props.goods_id || ""));
      function reloadById(id, reason = "unknown") {
        const newId = String(id || "");
        if (!newId) {
          formatAppLog("log", "at pages/goods/goods.vue:459", "【商品】reloadById 失败：ID 为空，原因=", reason);
          return;
        }
        formatAppLog("log", "at pages/goods/goods.vue:462", "【商品】准备按ID重新拉取，原因=", reason, " ID=", newId);
        currentId.value = newId;
        lastGoodsId.value = newId;
        goods.value = {};
        faceupList.value = [];
        imageHeights.value = [];
        swiperIndex.value = 1;
        swiperHeight.value = 400;
        hasLike.value = false;
        hasWish.value = false;
        wishCount.value = 0;
        replyForItem.value = {};
        uni.showLoading({ title: "加载中" });
        getGoods(newId);
        getCollocation(newId);
        asyncGetUserInfo().then(() => {
          getHasLike(newId);
        });
        getWishInfo(newId);
      }
      function getGoods(id = currentId.value) {
        uni.request({
          url: websiteUrl.value + "/goods?id=" + id,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            goods.value = res.data.data || {};
            formatAppLog("log", "at pages/goods/goods.vue:493", "【商品】详情加载完成：id=", id, " name=", goods.value.name);
            getWishInfo(id);
            if (showFaceupSection.value)
              getFaceupList(id);
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: () => uni.hideLoading()
        });
      }
      function getHasLike(id = currentId.value) {
        const token = uni.getStorageSync("token");
        if (!token)
          return;
        uni.request({
          url: websiteUrl.value + "/with-state/hasLike?id=" + id + "&type=1",
          method: "POST",
          header: { Authorization: token },
          success: (res) => {
            if (res.data.status === "success") {
              hasLike.value = res.data.data.id > 0;
            }
          }
        });
      }
      function getWishInfo(id = currentId.value) {
        const token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/wish-info?goods_id=" + id,
          method: "GET",
          header: token ? { Authorization: token } : {},
          success: (res) => {
            if (res.data.status === "success") {
              hasWish.value = res.data.data.user_has_wished;
              wishCount.value = res.data.data.wish_count;
            }
          }
        });
      }
      function getCollocation(id = currentId.value) {
        uni.request({
          url: websiteUrl.value + "/goods-collocation?goods_id=" + id,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            collocationList.value = res.data.data;
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      const getFaceupList = async (id = currentId.value) => {
        if (!showFaceupSection.value)
          return;
        faceupLoading.value = true;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/rand-bjd-faceup?goods_id=${id}`,
            method: "GET",
            timeout: 5e3
          });
          if (res.data.status === "success") {
            faceupList.value = res.data.data || [];
          } else {
            uni.showToast({ title: res.data.msg || "获取妆图失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "网络请求失败", icon: "none" });
        } finally {
          faceupLoading.value = false;
        }
      };
      const refreshFaceupList = () => {
        if (!faceupLoading.value)
          getFaceupList();
      };
      const getFirstImage = (s2) => s2 ? s2.split(",")[0].trim() : "";
      function jump2faceup(id) {
        uni.navigateTo({ url: "/pages/artwork/artwork?id=" + id });
      }
      const groupedSizes = vue.computed(() => {
        if (!goods.value.sizes || goods.value.sizes.length === 0)
          return [];
        const groups = {};
        goods.value.sizes.forEach((i2) => {
          const cat = i2.goods_size;
          if (!groups[cat])
            groups[cat] = [];
          if (i2.size_detail)
            groups[cat].push(i2.size_detail);
        });
        return Object.keys(groups).map((category) => ({
          category,
          details: groups[category]
        }));
      });
      function onImageLoad(index) {
        const query = uni.createSelectorQuery();
        setTimeout(() => {
          query.select(`.swiper-image-${index}`).boundingClientRect((rect) => {
            if (!rect)
              return;
            imageHeights.value[index] = rect.height;
            const valid = imageHeights.value.filter((h3) => h3 > 0);
            if (!valid.length)
              return;
            const h2 = Math.max(...valid);
            swiperHeight.value = Math.min(h2, maxHeight.value);
          }).exec();
        }, 20);
      }
      function jumpBrand(id) {
        uni.navigateTo({ url: "/pages/brand/brand?brand_id=" + id });
      }
      function onChange(e2) {
        swiperIndex.value = e2.detail.current + 1;
      }
      function formatNumber(n2) {
        if (!n2 || n2 < 0)
          return "0";
        if (n2 < 1e3)
          return "" + n2;
        const k = Math.floor(n2 / 1e3);
        return `${k}k+`;
      }
      function formatTimestamp(ts2) {
        if (!ts2 || ts2 <= 0)
          return "未知";
        const d2 = new Date(ts2 * 1e3);
        const p2 = (n2) => ("" + n2).padStart(2, "0");
        return `${d2.getFullYear()}-${p2(d2.getMonth() + 1)}-${p2(d2.getDate())} ${p2(d2.getHours())}:${p2(d2.getMinutes())}`;
      }
      function viewFullImage(i2) {
        const urls = goods.value.goods_images || [];
        if (urls.length) {
          uni.previewImage({ current: urls[i2], urls });
        }
      }
      function likeFn() {
        const id = parseInt(currentId.value);
        const token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const requestData = { id, type: 1 };
        const url = hasLike.value ? "/with-state/unlike" : "/with-state/add-like";
        uni.request({
          url: websiteUrl.value + url,
          method: "POST",
          header: { Authorization: token },
          data: requestData,
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "操作成功", icon: "success" });
              hasLike.value = !hasLike.value;
              getHasLike(id);
              getGoods(id);
            } else {
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      function jump2collocation() {
        const id = currentId.value;
        uni.navigateTo({
          url: "/pages/collocation/collocation?goods_id=" + id + "&brand_id=" + goods.value.brand_id + "&goods_name=" + goods.value.name + "&brand_name=" + goods.value.brand_name + "&type=" + goods.value.type
        });
      }
      function jump2collectionDetail(id, origin) {
        uni.navigateTo({
          url: "/pages/collocation_share/collocation_share?collocation_id=" + id + "&origin=" + origin
        });
      }
      function getImageForList(s2) {
        if (!s2)
          return "";
        return s2.split(",")[0].trim();
      }
      function wishResale() {
        if (wishLoading.value)
          return;
        const token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        wishLoading.value = true;
        uni.request({
          url: websiteUrl.value + "/with-state/wish-restock",
          method: "POST",
          header: { Authorization: token, "Content-Type": "application/json" },
          data: { goods_id: parseInt(currentId.value) },
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "许愿成功", icon: "success" });
              hasWish.value = true;
              wishCount.value = res.data.data.wish_count || wishCount.value + 1;
            } else {
              uni.showToast({ title: res.data.msg || "许愿失败", icon: "none" });
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: () => {
            wishLoading.value = false;
          }
        });
      }
      const handleReplyComment = ({ parent, target }) => {
        var _a;
        const item = target ?? parent;
        if (replyForItem.value.id === item.id) {
          replyForItem.value = {};
          return;
        }
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      const handleCommentSubmit = (submitData) => {
        var _a, _b, _c;
        const token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: parseInt(currentId.value),
          type: 2,
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          ...submitData.is_anonymous ? {
            avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
            username: "匿名用户",
            is_anonymous: 1
          } : {
            avatar: global$1.userInfo.avatar,
            username: global$1.userInfo.nickname,
            is_anonymous: 0
          },
          ...submitData.association_id && {
            association_id: submitData.association_id,
            association_type: submitData.association_type
          },
          ...submitData.image_url && { image_url: submitData.image_url },
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id) {
          (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(tempComment);
        } else if (replyForItem.value.parent_id === 0) {
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.id
          });
        } else {
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.parent_id
          });
        }
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: { Authorization: token },
          data: requestData,
          success: (res) => {
            var _a2, _b2;
            if (res.data.status === "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? {
                  avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
                  username: "匿名用户",
                  is_anonymous: 1
                } : {
                  avatar: global$1.userInfo.avatar,
                  username: global$1.userInfo.nickname,
                  is_anonymous: 0
                }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous) {
                finalComment.reply_username = "匿名用户";
              }
              (_a2 = commentListRef.value) == null ? void 0 : _a2.updateTempComment(tempComment.id, finalComment);
              uni.showToast({ title: "评论成功", icon: "success" });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => {
            var _a2;
            (_a2 = commentListRef.value) == null ? void 0 : _a2.removeTempComment(tempComment.id);
            uni.showToast({ title: "网络请求失败", icon: "none" });
          }
        });
      };
      function addToStock() {
        const gid = parseInt(currentId.value);
        if (!gid) {
          uni.showToast({ title: "缺少商品ID", icon: "none" });
          return;
        }
        uni.navigateTo({
          url: `/pkg-stock/stock/account_book_form/account_book_form?goods_id=${gid}`
        });
      }
      function addToShowcase() {
        const g2 = goods.value || {};
        if (!g2.id || !g2.name || !g2.brand_id || !g2.brand_name || !g2.type) {
          uni.showToast({ title: "商品信息不完整", icon: "none" });
          return;
        }
        const params = {
          goods_id: g2.id,
          goods_name: g2.name,
          brand_id: g2.brand_id,
          brand_name: g2.brand_name,
          type: g2.type
        };
        const q2 = Object.keys(params).map((k) => `${k}=${params[k]}`).join("&");
        uni.navigateTo({
          url: `/pkg-stock/stock/showcase_form/showcase_form?${q2}`
        });
      }
      function createBill(sale) {
        const params = {
          amount: sale.fin_amount,
          currency: sale.currency,
          name: goods.value.name,
          sale_id: sale.id
        };
        const q2 = Object.keys(params).map((k) => `${k}=${params[k]}`).join("&");
        uni.navigateTo({
          url: `/pkg-stock/stock/bill_form/bill_form?${q2}`
        });
      }
      onLoad((options = {}) => {
        const idFromLoad = (options == null ? void 0 : options.goods_id) || props.goods_id;
        formatAppLog("log", "at pages/goods/goods.vue:888", "【商品】onLoad，检测到ID=", idFromLoad);
        reloadById(idFromLoad, "onLoad");
      });
      onShow(() => {
        var _a;
        const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
        const top = pages2[pages2.length - 1];
        const idFromRoute = (_a = top == null ? void 0 : top.options) == null ? void 0 : _a.goods_id;
        const newId = String(idFromRoute || props.goods_id || "");
        formatAppLog("log", "at pages/goods/goods.vue:897", "路径ID" + newId);
        if (newId && newId !== lastGoodsId.value) {
          formatAppLog(
            "log",
            "at pages/goods/goods.vue:899",
            "【商品】onShow 检测到ID变更：",
            lastGoodsId.value,
            "→",
            newId
          );
          reloadById(newId, "onShow");
        } else {
          formatAppLog(
            "log",
            "at pages/goods/goods.vue:907",
            "【商品】onShow，无ID变化，跳过拉取（当前=",
            lastGoodsId.value,
            "）"
          );
        }
      });
      vue.watch(
        () => props.goods_id,
        (val, old) => {
          const v2 = String(val || "");
          const o2 = String(old || "");
          if (v2 && v2 !== o2) {
            formatAppLog("log", "at pages/goods/goods.vue:921", "【商品】props.goods_id 变化：", o2, "→", v2);
            reloadById(v2, "watch props.goods_id");
          }
        }
      );
      function selectType() {
      }
      function selectSize() {
      }
      const __returned__ = { props, scrollTop, goBack, NAV_HEIGHT_RPX, navHeightPx, topSafeSpacePx, loading: loading2, error: error2, errorMsg, commentListRef, commentInputRef, get replyForItem() {
        return replyForItem;
      }, set replyForItem(v2) {
        replyForItem = v2;
      }, get goods() {
        return goods;
      }, set goods(v2) {
        goods = v2;
      }, get swiperIndex() {
        return swiperIndex;
      }, set swiperIndex(v2) {
        swiperIndex = v2;
      }, get hasLike() {
        return hasLike;
      }, set hasLike(v2) {
        hasLike = v2;
      }, get collocationList() {
        return collocationList;
      }, set collocationList(v2) {
        collocationList = v2;
      }, get wishLoading() {
        return wishLoading;
      }, set wishLoading(v2) {
        wishLoading = v2;
      }, get hasWish() {
        return hasWish;
      }, set hasWish(v2) {
        hasWish = v2;
      }, get wishCount() {
        return wishCount;
      }, set wishCount(v2) {
        wishCount = v2;
      }, swiperHeight, imageHeights, maxHeight, faceupList, faceupLoading, showFaceupSection, currentId, lastGoodsId, reloadById, getGoods, getHasLike, getWishInfo, getCollocation, getFaceupList, refreshFaceupList, getFirstImage, jump2faceup, groupedSizes, onImageLoad, jumpBrand, onChange, formatNumber, formatTimestamp, viewFullImage, likeFn, jump2collocation, jump2collectionDetail, getImageForList, wishResale, handleReplyComment, handleCommentSubmit, addToStock, addToShowcase, createBill, selectType, selectSize, ref: vue.ref, computed: vue.computed, watch: vue.watch, get onPageScroll() {
        return onPageScroll;
      }, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get getWindowTop() {
        return getWindowTop;
      }, get toPx() {
        return toPx;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1k(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_item_impression = resolveEasycom(vue.resolveDynamicComponent("item-impression"), __easycom_3$4);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_zhouWei_navBar, {
          type: "transparentFixed",
          backState: 1e3,
          homeState: 2e3,
          fontColor: "#000",
          transparentFixedFontColor: "#000",
          scrollTop: $setup.scrollTop
        }, {
          transparentFixedLeft: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          transparentFixed: vue.withCtx(() => [
            vue.createElementVNode(
              "text",
              { class: "nav-title-ellipsis" },
              vue.toDisplayString($setup.goods.name || $setup.goods.goods_name || "商品详情"),
              1
              /* TEXT */
            )
          ]),
          transparentFixedRight: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-right-like",
              onClick: $setup.likeFn
            }, [
              vue.createVNode(_component_uni_icons, {
                type: $setup.hasLike ? "heart-filled" : "heart",
                size: "20",
                color: "#ff4d4f"
              }, null, 8, ["type"]),
              vue.createElementVNode(
                "text",
                { class: "nav-like-text" },
                vue.toDisplayString($setup.formatNumber($setup.goods.goods_like_count || 0)),
                1
                /* TEXT */
              )
            ])
          ]),
          left: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          default: vue.withCtx(() => [
            $setup.goods.goods_brand_name_image ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "nav-brand-wrap",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.jumpBrand($setup.goods.brand_id))
            }, [
              vue.createElementVNode("image", {
                src: $setup.goods.goods_brand_name_image,
                mode: "heightFix",
                class: "nav-brand-img"
              }, null, 8, ["src"])
            ])) : (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 1,
                class: "nav-brand-name",
                onClick: _cache[1] || (_cache[1] = ($event) => $setup.jumpBrand($setup.goods.brand_id))
              },
              vue.toDisplayString($setup.goods.brand_name || "品牌"),
              1
              /* TEXT */
            ))
          ]),
          right: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-right-like",
              onClick: $setup.likeFn
            }, [
              vue.createVNode(_component_uni_icons, {
                type: $setup.hasLike ? "heart-filled" : "heart",
                size: "20",
                color: "#ff4d4f"
              }, null, 8, ["type"]),
              vue.createElementVNode(
                "text",
                { class: "nav-like-text" },
                vue.toDisplayString($setup.formatNumber($setup.goods.goods_like_count || 0)),
                1
                /* TEXT */
              )
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["scrollTop"]),
        $setup.goods.name ? (vue.openBlock(), vue.createElementBlock("view", {
          class: "goods-detail-container",
          key: $setup.currentId
        }, [
          vue.createElementVNode("meta", {
            name: "theme-color",
            content: "#F8F8F8"
          }),
          vue.createVNode(_component_view_logs),
          vue.createElementVNode(
            "view",
            {
              class: "swiper-safe-bg",
              style: vue.normalizeStyle({ height: $setup.topSafeSpacePx })
            },
            null,
            4
            /* STYLE */
          ),
          vue.createElementVNode("view", { class: "swiper-container" }, [
            vue.createElementVNode(
              "swiper",
              {
                interval: 3e3,
                duration: 200,
                onChange: $setup.onChange,
                class: "banner-swiper",
                style: vue.normalizeStyle({
                  height: $setup.swiperHeight + "px",
                  "min-height": "200px",
                  "max-height": $setup.maxHeight + "px"
                })
              },
              [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.goods.goods_images, (item, key) => {
                    return vue.openBlock(), vue.createElementBlock("swiper-item", {
                      key,
                      class: "swiper-item-container"
                    }, [
                      vue.createElementVNode("view", { class: "swiper-item" }, [
                        vue.createElementVNode("image", {
                          src: item,
                          mode: "widthFix",
                          class: vue.normalizeClass("swiper-image-" + key),
                          onLoad: ($event) => $setup.onImageLoad(key),
                          onClick: ($event) => $setup.viewFullImage(key)
                        }, null, 42, ["src", "onLoad", "onClick"])
                      ])
                    ]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ],
              36
              /* STYLE, NEED_HYDRATION */
            ),
            vue.createElementVNode("view", { class: "swiper-index" }, [
              vue.createElementVNode(
                "text",
                { style: { "color": "#fff" } },
                vue.toDisplayString($setup.swiperIndex) + " / " + vue.toDisplayString($setup.goods.goods_images.length),
                1
                /* TEXT */
              )
            ])
          ]),
          vue.createElementVNode("view", { class: "goods-info" }, [
            vue.createElementVNode("view", { class: "action-buttons" }, [
              vue.createElementVNode("button", {
                class: "action-btn add-to-stock",
                onClick: $setup.addToStock
              }, [
                vue.createElementVNode("view", { class: "btn-content" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "plus",
                    size: "18",
                    color: "#fff",
                    style: { "height": "36rpx", "margin-bottom": "10rpx" }
                  }),
                  vue.createElementVNode("text", null, "放入物品栏")
                ])
              ]),
              vue.createElementVNode("button", {
                class: "action-btn wish-resale",
                onClick: $setup.wishResale
              }, [
                vue.createElementVNode("view", { class: "btn-content" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "star",
                    size: "18",
                    color: "#fff",
                    style: { "height": "36rpx", "margin-bottom": "10rpx" }
                  }),
                  vue.createElementVNode("text", null, "期望再贩")
                ])
              ]),
              vue.createElementVNode("button", {
                class: "action-btn add-showcase",
                onClick: $setup.addToShowcase
              }, [
                vue.createElementVNode("view", { class: "btn-content" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "vip",
                    size: "18",
                    color: "#fff",
                    style: { "height": "36rpx", "margin-bottom": "10rpx" }
                  }),
                  vue.createElementVNode("text", null, "加入展示柜")
                ])
              ])
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "名称"),
              vue.createElementVNode("image", {
                src: $setup.goods.goods_name_image,
                mode: "heightFix",
                class: "img_info"
              }, null, 8, ["src"])
            ]),
            vue.createElementVNode("view", {
              class: "info-item",
              onClick: _cache[2] || (_cache[2] = ($event) => $setup.selectType($setup.goods.type))
            }, [
              vue.createElementVNode("text", { class: "label" }, "类型"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.type),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "初贩定价"),
              $setup.goods.goods_price_image ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 0,
                src: $setup.goods.goods_price_image,
                mode: "heightFix",
                class: "img_info"
              }, null, 8, ["src"])) : $setup.goods.goods_sales && $setup.goods.goods_sales.length > 0 ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 1,
                  class: "value"
                },
                vue.toDisplayString($setup.goods.goods_sales[0].sub_amount + $setup.goods.goods_sales[0].fin_amount) + " " + vue.toDisplayString($setup.goods.goods_sales[0].currency),
                1
                /* TEXT */
              )) : (vue.openBlock(), vue.createElementBlock("text", {
                key: 2,
                class: "value"
              }, "未知"))
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "初贩时间"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.sub_time1 > 0 ? $setup.formatTimestamp($setup.goods.sub_time1) : "未知"),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", {
              class: "info-item",
              onClick: _cache[3] || (_cache[3] = ($event) => $setup.selectSize($setup.goods.size))
            }, [
              vue.createElementVNode("text", { class: "label" }, "尺寸"),
              vue.createElementVNode("view", { class: "value" }, [
                $setup.goods.sizes && $setup.goods.sizes.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.groupedSizes, (group, index) => {
                      return vue.openBlock(), vue.createElementBlock("view", {
                        key: index,
                        class: "size-group"
                      }, [
                        vue.createElementVNode(
                          "text",
                          { class: "size-category" },
                          vue.toDisplayString(group.category) + "：",
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          { class: "size-details" },
                          vue.toDisplayString(group.details.join("、")),
                          1
                          /* TEXT */
                        )
                      ]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])) : (vue.openBlock(), vue.createElementBlock(
                  "text",
                  { key: 1 },
                  vue.toDisplayString($setup.goods.size) + " / " + vue.toDisplayString($setup.goods.size_detail),
                  1
                  /* TEXT */
                ))
              ])
            ]),
            $setup.goods.type === "单体" || $setup.goods.type === "整体" || $setup.goods.type === "单头" ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "info-item"
            }, [
              vue.createElementVNode("text", { class: "label" }, "可选体型"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.body_size || "未知"),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "可选颜色"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.skin),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "制作方"),
              $setup.goods.goods_brand_name_image ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 0,
                onClick: _cache[4] || (_cache[4] = ($event) => $setup.jumpBrand($setup.goods.brand_id)),
                src: $setup.goods.goods_brand_name_image,
                mode: "heightFix",
                class: "img_info",
                style: { "background": "rgb(169 231 255)", "padding": "0rpx 20rpx" }
              }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "材质"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.doll_material),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "info-item" }, [
              vue.createElementVNode("text", { class: "label" }, "备注"),
              vue.createElementVNode(
                "text",
                { class: "value" },
                vue.toDisplayString($setup.goods.desc_content || "暂无备注信息"),
                1
                /* TEXT */
              )
            ])
          ]),
          $setup.goods.goods_sales && $setup.goods.goods_sales.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "sales-info"
          }, [
            vue.createElementVNode("text", { class: "section-title" }, "贩售情报"),
            vue.createElementVNode("view", { class: "sales-list" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.goods.goods_sales, (sale, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: sale.id,
                    class: "sale-item"
                  }, [
                    vue.createElementVNode("view", { class: "sale-header" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "sale-type" },
                        vue.toDisplayString(sale.sale_type),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "sale-price" },
                        vue.toDisplayString(sale.sub_amount + sale.fin_amount) + " " + vue.toDisplayString(sale.currency),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode("view", { class: "sale-period" }, [
                      vue.createElementVNode(
                        "text",
                        null,
                        vue.toDisplayString($setup.formatTimestamp(sale.sub_time)),
                        1
                        /* TEXT */
                      ),
                      sale.sub_time_end > 0 ? (vue.openBlock(), vue.createElementBlock("text", {
                        key: 0,
                        class: "separator"
                      }, "至")) : vue.createCommentVNode("v-if", true),
                      sale.sub_time_end > 0 ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        { key: 1 },
                        vue.toDisplayString($setup.formatTimestamp(sale.sub_time_end)),
                        1
                        /* TEXT */
                      )) : (vue.openBlock(), vue.createElementBlock("text", {
                        key: 2,
                        class: "separator"
                      }, "开定"))
                    ]),
                    vue.createElementVNode("view", { class: "sale-size" }, [
                      vue.createElementVNode(
                        "text",
                        null,
                        vue.toDisplayString(sale.size) + " · " + vue.toDisplayString(sale.size_detail),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode("view", {
                      class: "bill-action",
                      onClick: ($event) => $setup.createBill(sale)
                    }, [
                      vue.createVNode(_component_uni_icons, {
                        type: "plus",
                        size: "16",
                        color: "#64c6dc"
                      }),
                      vue.createElementVNode("text", null, "创建一个尾款账单")
                    ], 8, ["onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ])) : (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "no-sales"
          }, [
            vue.createElementVNode("text", { class: "section-title" }, "贩售情报"),
            vue.createElementVNode("text", { class: "empty-text" }, "暂无贩售信息")
          ])),
          (vue.openBlock(), vue.createBlock(_component_item_impression, {
            key: $setup.currentId,
            target_id: parseInt($setup.currentId),
            type: "2",
            "goods-type": Number($setup.goods.type) || 0
          }, null, 8, ["target_id", "goods-type"])),
          vue.createElementVNode("view", { class: "collocation-section" }, [
            vue.createElementVNode("view", { class: "section-header" }, [
              vue.createElementVNode("text", { class: "section-title" }, "搭配参考"),
              vue.createElementVNode("view", {
                class: "more-link",
                onClick: $setup.jump2collocation
              }, [
                vue.createElementVNode("text", null, "查看更多"),
                vue.createElementVNode("image", { src: _imports_1$5 })
              ])
            ]),
            vue.createElementVNode("scroll-view", {
              "scroll-x": "true",
              class: "collocation-list"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.collocationList.collocation_relation_list, (collocation) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: collocation.collocation_id,
                    class: "collocation-item",
                    onClick: ($event) => $setup.jump2collectionDetail(
                      collocation.collocation_id,
                      collocation.relation_origin
                    )
                  }, [
                    vue.createElementVNode("image", {
                      src: $setup.getImageForList(collocation.image_urls),
                      mode: "aspectFill"
                    }, null, 8, ["src"])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            vue.createElementVNode("view", {
              onClick: $setup.jump2collocation,
              class: "upload-collocation"
            }, [
              vue.createElementVNode("text", null, "返图帮助其它娃友?"),
              vue.createVNode(_component_uni_icons, {
                type: "upload",
                size: "20",
                color: "#ccc",
                style: { "margin-left": "10rpx" }
              })
            ])
          ]),
          $setup.showFaceupSection ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "faceup-section"
          }, [
            vue.createElementVNode("view", { class: "section-header" }, [
              vue.createElementVNode("text", { class: "section-title" }, "妆图展示"),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["refresh-btn", { loading: $setup.faceupLoading }]),
                  onClick: $setup.refreshFaceupList
                },
                [
                  vue.createElementVNode(
                    "text",
                    { class: "refresh-text" },
                    vue.toDisplayString($setup.faceupLoading ? "换一批中…" : "换一批"),
                    1
                    /* TEXT */
                  ),
                  !$setup.faceupLoading ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                    key: 0,
                    type: "refresh",
                    size: "16",
                    color: "#64c6dc",
                    style: { "margin-left": "8rpx" }
                  })) : vue.createCommentVNode("v-if", true)
                ],
                2
                /* CLASS */
              )
            ]),
            vue.createElementVNode("view", { class: "faceup-grid" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.faceupList, (faceup, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: "faceup-card",
                    onClick: ($event) => $setup.jump2faceup(faceup.id)
                  }, [
                    vue.createElementVNode("image", {
                      src: $setup.getFirstImage(faceup.face_up_image_urls),
                      mode: "aspectFill",
                      class: "faceup-cover"
                    }, null, 8, ["src"]),
                    vue.createElementVNode(
                      "view",
                      { class: "faceup-title" },
                      vue.toDisplayString(faceup.title),
                      1
                      /* TEXT */
                    )
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            !$setup.faceupLoading && $setup.faceupList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "faceup-empty"
            }, " 暂无妆图～ ")) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { class: "comment-section" }, [
            (vue.openBlock(), vue.createBlock(_component_comment_list, {
              key: $setup.currentId,
              ref: "commentListRef",
              type: 2,
              "relation-id": parseInt($setup.currentId),
              onReply: $setup.handleReplyComment
            }, null, 8, ["relation-id"]))
          ]),
          vue.createVNode(_component_comment_input, {
            ref: "commentInputRef",
            "reply-info": $setup.replyForItem,
            "target-id": parseInt($setup.currentId),
            onSubmit: $setup.handleCommentSubmit,
            "onUpdate:replyInfo": _cache[5] || (_cache[5] = (val) => $setup.replyForItem = val)
          }, null, 8, ["reply-info", "target-id"]),
          vue.createElementVNode("view", { class: "bottom-placeholder" })
        ])) : vue.createCommentVNode("v-if", true)
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesGoodsGoods = /* @__PURE__ */ _export_sfc(_sfc_main$1l, [["render", _sfc_render$1k], ["__scopeId", "data-v-7e2880f6"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/goods/goods.vue"]]);
  const _sfc_main$1k = {
    name: "UniRate",
    props: {
      isFill: {
        // 星星的类型，是否镂空
        type: [Boolean, String],
        default: true
      },
      color: {
        // 星星未选中的颜色
        type: String,
        default: "#ececec"
      },
      activeColor: {
        // 星星选中状态颜色
        type: String,
        default: "#ffca3e"
      },
      disabledColor: {
        // 星星禁用状态颜色
        type: String,
        default: "#c0c0c0"
      },
      size: {
        // 星星的大小
        type: [Number, String],
        default: 24
      },
      value: {
        // 当前评分
        type: [Number, String],
        default: 0
      },
      modelValue: {
        // 当前评分
        type: [Number, String],
        default: 0
      },
      max: {
        // 最大评分
        type: [Number, String],
        default: 5
      },
      margin: {
        // 星星的间距
        type: [Number, String],
        default: 0
      },
      disabled: {
        // 是否可点击
        type: [Boolean, String],
        default: false
      },
      readonly: {
        // 是否只读
        type: [Boolean, String],
        default: false
      },
      allowHalf: {
        // 是否显示半星
        type: [Boolean, String],
        default: false
      },
      touchable: {
        // 是否支持滑动手势
        type: [Boolean, String],
        default: true
      }
    },
    data() {
      return {
        valueSync: "",
        userMouseFristMove: true,
        userRated: false,
        userLastRate: 1
      };
    },
    watch: {
      value(newVal) {
        this.valueSync = Number(newVal);
      },
      modelValue(newVal) {
        this.valueSync = Number(newVal);
      }
    },
    computed: {
      stars() {
        const value = this.valueSync ? this.valueSync : 0;
        const starList = [];
        const floorValue = Math.floor(value);
        const ceilValue = Math.ceil(value);
        for (let i2 = 0; i2 < this.max; i2++) {
          if (floorValue > i2) {
            starList.push({
              activeWitch: "100%"
            });
          } else if (ceilValue - 1 === i2) {
            starList.push({
              activeWitch: (value - floorValue) * 100 + "%"
            });
          } else {
            starList.push({
              activeWitch: "0"
            });
          }
        }
        return starList;
      },
      marginNumber() {
        return Number(this.margin);
      }
    },
    created() {
      this.valueSync = Number(this.value || this.modelValue);
      this._rateBoxLeft = 0;
      this._oldValue = null;
    },
    mounted() {
      setTimeout(() => {
        this._getSize();
      }, 100);
    },
    methods: {
      touchstart(e2) {
        if (this.readonly || this.disabled)
          return;
        const {
          clientX,
          screenX
        } = e2.changedTouches[0];
        this._getRateCount(clientX || screenX);
      },
      touchmove(e2) {
        if (this.readonly || this.disabled || !this.touchable)
          return;
        const {
          clientX,
          screenX
        } = e2.changedTouches[0];
        this._getRateCount(clientX || screenX);
      },
      /**
       * 兼容 PC @tian
       */
      mousedown(e2) {
      },
      mousemove(e2) {
      },
      mouseleave(e2) {
      },
      /**
       * 获取星星个数
       */
      _getRateCount(clientX) {
        this._getSize();
        const size = Number(this.size);
        if (isNaN(size)) {
          return new Error("size 属性只能设置为数字");
        }
        const rateMoveRange = clientX - this._rateBoxLeft;
        let index = parseInt(rateMoveRange / (size + this.marginNumber));
        index = index < 0 ? 0 : index;
        index = index > this.max ? this.max : index;
        const range = parseInt(rateMoveRange - (size + this.marginNumber) * index);
        let value = 0;
        if (this._oldValue === index && !this.PC)
          return;
        this._oldValue = index;
        if (this.allowHalf) {
          if (range > size / 2) {
            value = index + 1;
          } else {
            value = index + 0.5;
          }
        } else {
          value = index + 1;
        }
        value = Math.max(0.5, Math.min(value, this.max));
        this.valueSync = value;
        this._onChange();
      },
      /**
       * 触发动态修改
       */
      _onChange() {
        this.$emit("input", this.valueSync);
        this.$emit("update:modelValue", this.valueSync);
        this.$emit("change", {
          value: this.valueSync
        });
      },
      /**
       * 获取星星距离屏幕左侧距离
       */
      _getSize() {
        uni.createSelectorQuery().in(this).select(".uni-rate").boundingClientRect().exec((ret) => {
          if (ret) {
            this._rateBoxLeft = ret[0].left;
          }
        });
      }
    }
  };
  function _sfc_render$1j(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode(
        "view",
        {
          ref: "uni-rate",
          class: "uni-rate"
        },
        [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($options.stars, (star, index) => {
              return vue.openBlock(), vue.createElementBlock(
                "view",
                {
                  class: vue.normalizeClass(["uni-rate__icon", { "uni-cursor-not-allowed": $props.disabled }]),
                  style: vue.normalizeStyle({ "margin-right": $options.marginNumber + "px" }),
                  key: index,
                  onTouchstart: _cache[0] || (_cache[0] = vue.withModifiers((...args) => $options.touchstart && $options.touchstart(...args), ["stop"])),
                  onTouchmove: _cache[1] || (_cache[1] = vue.withModifiers((...args) => $options.touchmove && $options.touchmove(...args), ["stop"])),
                  onMousedown: _cache[2] || (_cache[2] = vue.withModifiers((...args) => $options.mousedown && $options.mousedown(...args), ["stop"])),
                  onMousemove: _cache[3] || (_cache[3] = vue.withModifiers((...args) => $options.mousemove && $options.mousemove(...args), ["stop"])),
                  onMouseleave: _cache[4] || (_cache[4] = (...args) => $options.mouseleave && $options.mouseleave(...args))
                },
                [
                  vue.createVNode(_component_uni_icons, {
                    color: $props.color,
                    size: $props.size,
                    type: $props.isFill ? "star-filled" : "star"
                  }, null, 8, ["color", "size", "type"]),
                  vue.createElementVNode(
                    "view",
                    {
                      style: vue.normalizeStyle({ width: star.activeWitch }),
                      class: "uni-rate__icon-on"
                    },
                    [
                      vue.createVNode(_component_uni_icons, {
                        color: $props.disabled ? $props.disabledColor : $props.activeColor,
                        size: $props.size,
                        type: "star-filled"
                      }, null, 8, ["color", "size"])
                    ],
                    4
                    /* STYLE */
                  )
                ],
                38
                /* CLASS, STYLE, NEED_HYDRATION */
              );
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ],
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const __easycom_1$7 = /* @__PURE__ */ _export_sfc(_sfc_main$1k, [["render", _sfc_render$1j], ["__scopeId", "data-v-5c8fbdf3"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-rate/components/uni-rate/uni-rate.vue"]]);
  const _sfc_main$1j = {
    __name: "brand",
    props: ["brand_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const currentBrandId = vue.ref(String(props.brand_id || ""));
      const lastBrandId = vue.ref(currentBrandId.value);
      const numericBrandId = vue.computed(() => parseInt(currentBrandId.value || "0"));
      function parseBrandIdFromRoute() {
        var _a;
        try {
          const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
          const top = pages2[pages2.length - 1];
          if ((_a = top == null ? void 0 : top.options) == null ? void 0 : _a.brand_id)
            return String(top.options.brand_id);
        } catch {
        }
        if (typeof location !== "undefined" && location.hash) {
          const m2 = location.hash.match(/[?&]brand_id=(\d+)/);
          if (m2 && m2[1])
            return m2[1];
        }
        return "";
      }
      async function reloadByBrandId(newId, source = "unknown") {
        formatAppLog("log", "at pages/brand/brand.vue:147", `【品牌】reloadByBrandId 来源=${source} id=${newId}`);
        currentBrandId.value = String(newId);
        lastBrandId.value = String(newId);
        page.value = 1;
        goods.value = { goods_list: [], page_index: 1, total: 0 };
        newsList.value = [];
        newsPage.value = { page_index: 1, page_size: 10, total: 0 };
        rateValue.value = 0;
        myRateValue.value = 0;
        hasLikeBrand.value = false;
        getBrandsInfo(currentBrandId.value);
        getBrandGoods(false, currentBrandId.value);
        getBrandNews(currentBrandId.value);
        if (global$1.isLogin) {
          getMyScore(1, currentBrandId.value);
          getHasLikeBrand(currentBrandId.value);
        } else {
          hasLikeBrand.value = false;
        }
      }
      const activeTab = vue.ref(0);
      const hasAddedArtistTab = vue.ref(false);
      const tabs = vue.ref([{ label: "贩售作品" }, { label: "消息动态" }]);
      const switchTab = (i2) => {
        activeTab.value = i2;
      };
      const hasLikeBrand = vue.ref(false);
      let newsList = vue.ref([]);
      let newsPage = vue.ref({ page_index: 1, page_size: 10, total: 0 });
      uni.showLoading({ title: "加载中" });
      const rateValue = vue.ref(0);
      const myRateValue = vue.ref(0);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      let replyForItem = vue.ref({});
      const showCommentInput = vue.ref(false);
      vue.onActivated(() => {
        showCommentInput.value = true;
      });
      vue.onDeactivated(() => {
        showCommentInput.value = false;
      });
      const handleReplyComment = ({ parent, target }) => {
        var _a;
        const item = target ?? parent;
        if (replyForItem.value.id === item.id) {
          replyForItem.value = {};
          return;
        }
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      const handleCommentSubmit = (submitData) => {
        var _a, _b, _c;
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: numericBrandId.value,
          type: 1,
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          ...submitData.is_anonymous ? {
            avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
            username: "匿名用户",
            is_anonymous: 1
          } : {
            avatar: global$1.userInfo.avatar,
            username: global$1.userInfo.nickname,
            is_anonymous: 0
          },
          ...submitData.association_id && { association_id: submitData.association_id, association_type: submitData.association_type },
          ...submitData.image_url && { image_url: submitData.image_url },
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id)
          (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(tempComment);
        else if (replyForItem.value.parent_id === 0)
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({ ...tempComment, parent_id: replyForItem.value.id });
        else
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({ ...tempComment, parent_id: replyForItem.value.parent_id });
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: { "Authorization": token },
          data: requestData,
          success: (res) => {
            var _a2, _b2;
            if (res.data.status === "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? {
                  avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
                  username: "匿名用户",
                  is_anonymous: 1
                } : {
                  avatar: global$1.userInfo.avatar,
                  username: global$1.userInfo.nickname,
                  is_anonymous: 0
                }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous)
                finalComment.reply_username = "匿名用户";
              (_a2 = commentListRef.value) == null ? void 0 : _a2.updateTempComment(tempComment.id, finalComment);
              uni.showToast({ title: "评论成功", icon: "success" });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => {
            var _a2;
            (_a2 = commentListRef.value) == null ? void 0 : _a2.removeTempComment(tempComment.id);
            uni.showToast({ title: "网络请求失败", icon: "none" });
          }
        });
      };
      const onRateChange = (e2) => {
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          rateValue.value = 0;
          return;
        }
        rateValue.value = e2.value;
        myRateValue.value = e2.value;
        voteScoreProxy();
      };
      async function voteScoreProxy() {
        if (rateValue.value == 0) {
          uni.showToast({ title: "请先评分", icon: "none" });
          return;
        }
        let token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        try {
          uni.showLoading({ title: "提交中..." });
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/add-vote-score",
            method: "POST",
            header: { "Authorization": token, "Content-Type": "application/json" },
            data: { type: 1, score: rateValue.value, target_id: numericBrandId.value }
          });
          uni.hideLoading();
          if (res.data.status === "success") {
            getBrandsInfo(currentBrandId.value);
            uni.showToast({ title: "评分成功", icon: "success" });
          } else {
            uni.showToast({ title: res.data.msg || "评分失败", icon: "none" });
          }
        } catch (err) {
          uni.hideLoading();
          uni.showToast({ title: "评分失败，请重试", icon: "none" });
        }
      }
      let goods = vue.ref({});
      let page = vue.ref(1);
      let brand = vue.ref({});
      function getBrandNews(brandId = currentBrandId.value) {
        uni.request({
          url: `${websiteUrl.value}/brand-news-list?brand_id=${brandId}&page=${newsPage.value.page_index}&page_size=${newsPage.value.page_size}`,
          method: "GET",
          success: (res) => {
            if (res.data.status === "success") {
              const data = res.data.data;
              newsList.value = [...newsList.value, ...data.news_list || []];
              newsPage.value.total = data.total || 0;
              if (data.news_list && data.news_list.length > 0)
                newsPage.value.page_index += 1;
            } else {
              uni.showToast({ title: res.data.msg || "获取图透失败", icon: "none" });
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      function getBrandsInfo(brandId = currentBrandId.value) {
        uni.request({
          url: websiteUrl.value + "/brand-info?id=" + brandId,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            brand.value = res.data.data || {};
            uni.setNavigationBarTitle({ title: brand.value.brand_name || "品牌" });
            getHasLikeBrand(brandId);
            if (!hasAddedArtistTab.value && (brand.value.is_bjd_artist == 1 || brand.value.is_bjd_hairstylist == 1)) {
              tabs.value.push({ label: "接单主页" });
              hasAddedArtistTab.value = true;
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: () => uni.hideLoading()
        });
      }
      function getBrandGoods(isLoadMore = false, brandId = currentBrandId.value) {
        uni.request({
          url: websiteUrl.value + "/brand-goods?brand_id=" + brandId + "&page=" + page.value,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            const data = res.data.data || { goods_list: [], total: 0, page_index: 1 };
            goods.value.page_index = data.page_index;
            goods.value.total = data.total;
            if (isLoadMore)
              goods.value.goods_list = (goods.value.goods_list || []).concat(data.goods_list || []);
            else
              goods.value.goods_list = data.goods_list || [];
            if (data.goods_list && data.goods_list.length > 0)
              page.value += 1;
            if (isLoadMore && (!data.goods_list || data.goods_list.length === 0) && page.value > 1) {
              uni.showToast({ title: "没有更多数据了", icon: "none" });
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      const jumpToArtistPage = () => {
        uni.navigateTo({ url: `/pages/artist_info/artist_info?brand_id=${currentBrandId.value}` });
      };
      const likeBrand = async () => {
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        try {
          const url = `${websiteUrl.value}/with-state/${hasLikeBrand.value ? "unlike" : "add-like"}`;
          const res = await uni.request({
            url,
            method: "POST",
            header: { Authorization: token },
            data: { id: numericBrandId.value, type: 2 }
          });
          if (res.data.status === "success") {
            hasLikeBrand.value = !hasLikeBrand.value;
            uni.showToast({ title: hasLikeBrand.value ? "关注成功" : "已取消关注", icon: "none" });
            getBrandsInfo(currentBrandId.value);
          } else {
            uni.showToast({ title: res.data.msg || "操作失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "操作失败", icon: "none" });
        }
      };
      const getHasLikeBrand = async (brandId = currentBrandId.value) => {
        var _a, _b;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/hasLike?id=${parseInt(brandId)}&type=2`,
            method: "POST",
            header: { Authorization: uni.getStorageSync("token") || "" }
          });
          hasLikeBrand.value = !!(((_b = (_a = res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.id) > 0);
        } catch (err) {
        }
      };
      function formatTimestamp(ts2) {
        const d2 = new Date(ts2 * 1e3);
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        const h2 = String(d2.getHours()).padStart(2, "0");
        const mm = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${day} ${h2}:${mm}`;
      }
      function jumpGoods(id) {
        uni.navigateTo({ url: "/pages/goods/goods?goods_id=" + id });
      }
      function jump2saleNews(item) {
        uni.navigateTo({ url: `/pages/sale_news/sale_news?id=${item.id}&brand_id=${item.brand_id}` });
      }
      const copyUrl = (url) => {
        if (!url) {
          uni.showToast({ title: "暂无贩售地址", icon: "none" });
          return;
        }
        if (myRateValue.value === 0) {
          uni.showToast({ title: "请先评分后再复制", icon: "none" });
          return;
        }
        uni.setClipboardData({
          data: url,
          success: () => uni.showToast({ title: "已复制贩售地址", icon: "success" }),
          fail: () => uni.showToast({ title: "复制失败", icon: "none" })
        });
      };
      function getMyScore(type, targetId) {
        let token = uni.getStorageSync("token");
        if (!token || !global$1.isLogin)
          return;
        uni.request({
          url: websiteUrl.value + "/with-state/my-vote-record",
          method: "GET",
          header: { "Authorization": token, "Content-Type": "application/json" },
          data: { target_id: parseInt(targetId), type },
          success: (res) => {
            if (res.data.status === "success")
              myRateValue.value = res.data.data.score || 0;
          }
        });
      }
      onShow(() => {
        getUserInfo();
        const idFromRoute = parseBrandIdFromRoute();
        const newId = String(idFromRoute || currentBrandId.value || "");
        formatAppLog("log", "at pages/brand/brand.vue:461", "[品牌] 当前 URL/路由 brand_id =", newId);
        if (newId && newId !== lastBrandId.value) {
          formatAppLog("log", "at pages/brand/brand.vue:464", "【品牌】onShow 检测到ID变化：", lastBrandId.value, "→", newId);
          reloadByBrandId(newId, "onShow");
        } else {
          formatAppLog("log", "at pages/brand/brand.vue:467", "【品牌】onShow 无ID变化，轻刷新。");
          if (!brand.value.id)
            getBrandsInfo(currentBrandId.value);
          if (newsList.value.length === 0)
            getBrandNews(currentBrandId.value);
          if (!goods.value.goods_list || goods.value.goods_list.length === 0)
            getBrandGoods(false, currentBrandId.value);
          if (global$1.isLogin && myRateValue.value === 0)
            getMyScore(1, currentBrandId.value);
        }
      });
      const __returned__ = { props, currentBrandId, lastBrandId, numericBrandId, parseBrandIdFromRoute, reloadByBrandId, activeTab, hasAddedArtistTab, tabs, switchTab, hasLikeBrand, get newsList() {
        return newsList;
      }, set newsList(v2) {
        newsList = v2;
      }, get newsPage() {
        return newsPage;
      }, set newsPage(v2) {
        newsPage = v2;
      }, rateValue, myRateValue, commentListRef, commentInputRef, get replyForItem() {
        return replyForItem;
      }, set replyForItem(v2) {
        replyForItem = v2;
      }, showCommentInput, handleReplyComment, handleCommentSubmit, onRateChange, voteScoreProxy, get goods() {
        return goods;
      }, set goods(v2) {
        goods = v2;
      }, get page() {
        return page;
      }, set page(v2) {
        page = v2;
      }, get brand() {
        return brand;
      }, set brand(v2) {
        brand = v2;
      }, getBrandNews, getBrandsInfo, getBrandGoods, jumpToArtistPage, likeBrand, getHasLikeBrand, formatTimestamp, jumpGoods, jump2saleNews, copyUrl, getMyScore, ref: vue.ref, computed: vue.computed, watch: vue.watch, onActivated: vue.onActivated, onDeactivated: vue.onDeactivated, get websiteUrl() {
        return websiteUrl;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get onShow() {
        return onShow;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1i(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_rate = resolveEasycom(vue.resolveDynamicComponent("uni-rate"), __easycom_1$7);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("image", {
        src: $setup.brand.logo_image,
        mode: "aspectFit",
        class: "brand_logo"
      }, null, 8, ["src"]),
      vue.createElementVNode("view", { class: "body" }, [
        vue.createElementVNode("view", null, [
          vue.createElementVNode("image", {
            src: $setup.brand.brand_name_image,
            mode: "heightFix",
            style: { "height": "60rpx" }
          }, null, 8, ["src"]),
          vue.createElementVNode(
            "text",
            { style: { "float": "right", "margin": "5px 0px" } },
            vue.toDisplayString($setup.brand.country_name) + " / " + vue.toDisplayString($setup.brand.type),
            1
            /* TEXT */
          ),
          vue.createElementVNode("view", { style: { "clear": "both" } })
        ]),
        vue.createElementVNode("view", { style: { "margin": "20rpx 0rpx", "display": "flex", "flex-wrap": "wrap", "align-items": "center", "justify-content": "space-between" } }, [
          vue.createElementVNode("view", { style: { "display": "flex", "align-items": "center", "flex-grow": "1" } }, [
            vue.createVNode(_component_uni_rate, {
              style: { "margin-top": "5px" },
              value: $setup.brand.score,
              "allow-half": "true",
              activeColor: "#65C3D6",
              onChange: $setup.onRateChange,
              "is-fill": "false"
            }, null, 8, ["value"]),
            vue.createElementVNode(
              "text",
              { style: { "margin-left": "8rpx", "position": "relative", "top": "5px" } },
              vue.toDisplayString($setup.brand.score) + "（" + vue.toDisplayString($setup.brand.vote_number) + "次评分) ",
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode(
            "text",
            {
              class: "follow",
              onClick: $setup.likeBrand,
              style: vue.normalizeStyle({ background: $setup.hasLikeBrand ? "#ff6a6c" : "#65C3D6" })
            },
            vue.toDisplayString($setup.hasLikeBrand ? "已关注" : "+ 关注品牌"),
            5
            /* TEXT, STYLE */
          )
        ]),
        vue.createElementVNode("view", { style: { "margin-top": "15px" } }, [
          vue.createElementVNode(
            "text",
            null,
            "别名：" + vue.toDisplayString($setup.brand.nickname_list || "暂无设置"),
            1
            /* TEXT */
          )
        ]),
        vue.createElementVNode("view", { style: { "margin-top": "10px" } }, [
          vue.createElementVNode(
            "text",
            null,
            "简介：" + vue.toDisplayString($setup.brand.description || "暂未设置"),
            1
            /* TEXT */
          )
        ]),
        vue.createElementVNode("view", { style: { "margin-top": "10px" } }, [
          vue.createElementVNode("text", {
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.copyUrl($setup.brand.website_url))
          }, "贩售地址：点击复制")
        ]),
        vue.createElementVNode("view", { class: "tabs-container" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.tabs, (tab, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: vue.normalizeClass(["tab-item", { "active-tab": $setup.activeTab === index }]),
                onClick: ($event) => $setup.switchTab(index)
              }, [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString(tab.label),
                  1
                  /* TEXT */
                )
              ], 10, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ]),
        vue.withDirectives(vue.createElementVNode(
          "view",
          null,
          [
            vue.createElementVNode("view", { class: "brand_goods" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.goods.goods_list, (item, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "brand_goods_item",
                    key: item.id
                  }, [
                    vue.createElementVNode("navigator", {
                      onClick: ($event) => $setup.jumpGoods(item.id),
                      style: { "width": "100%", "height": "100%" }
                    }, [
                      vue.createElementVNode("view", { style: { "width": "100%", "height": "calc(100% - 20px)" } }, [
                        vue.createElementVNode("image", {
                          style: { "width": "100%", "height": "100%" },
                          src: item.goods_images[0],
                          mode: "aspectFill",
                          class: "brand_goods_image"
                        }, null, 8, ["src"])
                      ]),
                      vue.createElementVNode(
                        "text",
                        { style: { "display": "block", "width": "100%", "text-align": "center", "font-weight": "900", "color": "#586f88" } },
                        vue.toDisplayString(item.name),
                        1
                        /* TEXT */
                      )
                    ], 8, ["onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            vue.createElementVNode("button", {
              class: "load_more",
              onClick: _cache[1] || (_cache[1] = ($event) => $setup.getBrandGoods(true))
            }, "加载更多")
          ],
          512
          /* NEED_PATCH */
        ), [
          [vue.vShow, $setup.activeTab === 0]
        ]),
        vue.withDirectives(vue.createElementVNode(
          "view",
          null,
          [
            vue.createElementVNode("view", {
              class: "news-list",
              style: { "display": "flex", "flex-wrap": "wrap", "justify-content": "space-between" }
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.newsList, (item, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "news-item",
                    style: { "position": "relative", "width": "calc(50% - 10px)", "margin-bottom": "20px" },
                    key: item.id
                  }, [
                    vue.createElementVNode("view", { class: "news-header" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "news-header-title" },
                        vue.toDisplayString(item.title),
                        1
                        /* TEXT */
                      )
                    ]),
                    item.image_list && item.image_list.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "news-images"
                    }, [
                      vue.createElementVNode("swiper", {
                        class: "image-swiper",
                        autoplay: true,
                        circular: true,
                        "indicator-dots": ""
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(item.image_list, (img, imgIndex) => {
                            return vue.openBlock(), vue.createElementBlock("swiper-item", { key: imgIndex }, [
                              vue.createElementVNode("image", {
                                src: img,
                                mode: "aspectFill",
                                class: "swiper-image",
                                onClick: ($event) => $setup.jump2saleNews(item)
                              }, null, 8, ["src", "onClick"])
                            ]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode(
                      "view",
                      { class: "news-content" },
                      vue.toDisplayString(item.content),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("view", { class: "news-footer" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "news-time" },
                        vue.toDisplayString($setup.formatTimestamp(item.created_at)),
                        1
                        /* TEXT */
                      )
                    ])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ],
          512
          /* NEED_PATCH */
        ), [
          [vue.vShow, $setup.activeTab === 1]
        ]),
        vue.withDirectives(vue.createElementVNode(
          "view",
          null,
          [
            vue.createElementVNode("view", { style: { "text-align": "center", "padding": "50rpx 0" } }, [
              vue.createElementVNode("button", {
                class: "artist-button",
                onClick: $setup.jumpToArtistPage
              }, [
                vue.createElementVNode("text", null, "前往接单主页"),
                vue.createVNode(_component_uni_icons, {
                  type: "arrow-right",
                  size: "20",
                  color: "#65C3D6"
                })
              ])
            ])
          ],
          512
          /* NEED_PATCH */
        ), [
          [vue.vShow, $setup.activeTab === 2]
        ]),
        vue.createElementVNode("view", null, [
          vue.createVNode(_component_comment_list, {
            ref: "commentListRef",
            type: 1,
            "relation-id": $setup.numericBrandId,
            onReply: $setup.handleReplyComment
          }, null, 8, ["relation-id"])
        ])
      ]),
      vue.createElementVNode("view", { style: { "height": "80px" } }),
      $setup.showCommentInput ? (vue.openBlock(), vue.createBlock(_component_comment_input, {
        key: 0,
        ref: "commentInputRef",
        "reply-info": $setup.replyForItem,
        "target-id": String($setup.currentBrandId),
        onSubmit: $setup.handleCommentSubmit,
        "onUpdate:replyInfo": _cache[2] || (_cache[2] = (val) => $setup.replyForItem = val)
      }, null, 8, ["reply-info", "target-id"])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PagesBrandBrand = /* @__PURE__ */ _export_sfc(_sfc_main$1j, [["render", _sfc_render$1i], ["__scopeId", "data-v-1a297a1d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/brand/brand.vue"]]);
  const _imports_1$4 = "/static/empty-box.png";
  const stickyShowThreshold$1 = 80;
  const _sfc_main$1i = {
    __name: "calendar-tab",
    setup(__props, { expose: __expose }) {
      __expose();
      const scrollTop = vue.ref(0);
      const safeTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
        scheduleScrollSpy();
      });
      const goBack = () => {
        var _a;
        try {
          const pages2 = getCurrentPages && getCurrentPages();
          if (pages2 && pages2.length > 1)
            uni.navigateBack({ delta: 1 });
          else
            ((_a = uni.switchTab) == null ? void 0 : _a.call(uni, { url: "/pages/index/index" })) || uni.reLaunch({ url: "/pages/index/index" });
        } catch {
          uni.navigateBack({ delta: 1 });
        }
      };
      function goHome() {
        uni.switchTab({ url: "/pages/index/index" });
      }
      const navBarPx = toPx(getNavBarHeight());
      const headerPadPx = vue.computed(() => toPx(getStatusBarHeight() + getNavBarHeight()));
      const activeTab = vue.ref("sale");
      const switchTab = async (t2) => {
        if (activeTab.value === t2)
          return;
        activeTab.value = t2;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:311", "【日历】切换Tab为：", t2);
        if (t2 === "makeup") {
          if (!Object.keys(makeupCalendar.value).length)
            await fetchMakeupCalendar();
        } else if (t2 === "hair") {
          if (!Object.keys(hairCalendar.value).length)
            await fetchHairCalendar();
        } else {
          if (!Object.keys(saleCalendar.value).length)
            await fetchSaleCalendar();
        }
        updateSelectedItem();
        resetStickySoon();
      };
      const tabList = vue.ref(["全部", "整体", "单头", "单体", "娃衣", "眼珠", "娃鞋", "手型", "脚型", "袜子", "配饰"]);
      const sizeList = vue.ref(["全部尺寸", "四分", "六分", "三分", "其它大尺寸", "八分", "十二分", "一分", "二分", "五分", "棉花娃", "眼珠"]);
      let activeType = vue.ref("全部");
      let activeSize = vue.ref("全部尺寸");
      const saleCalendar = vue.ref({});
      const makeupCalendar = vue.ref({});
      const hairCalendar = vue.ref({});
      const expandStates = vue.ref({});
      const toggleFold = async (id) => {
        expandStates.value[id] = !expandStates.value[id];
        await vue.nextTick();
        resetStickySoon();
        setTimeout(resetStickySoon, 360);
        setTimeout(resetStickySoon, 720);
      };
      const getHighlightImages = (s2) => !s2 ? [] : s2.split(",").map((i2) => i2.trim()).filter(Boolean);
      function getPlanImages(plan) {
        const info = (plan == null ? void 0 : plan.artist_info) || {};
        if (activeTab.value === "hair") {
          const hairStr = info.hairstylist_highlight_images || info.HairstylistHighlightImages;
          const fallback = info.artist_highlight_images || info.ArtistHighlightImages;
          const imgs = getHighlightImages(hairStr || fallback);
          return imgs;
        }
        const makeupStr = info.artist_highlight_images || info.ArtistHighlightImages;
        return getHighlightImages(makeupStr);
      }
      const previewImage = (url) => uni.previewImage({ urls: [url], current: url });
      const todayFormat = vue.ref("");
      function computeTodayFormat() {
        const d2 = /* @__PURE__ */ new Date();
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${day}`;
      }
      const screenWidth = uni.getSystemInfoSync().screenWidth;
      const itemWidth = screenWidth / 7;
      let scrollLeft = vue.ref(0);
      let chooseDate = vue.ref("");
      let chooseItem = vue.ref({});
      let loading2 = vue.ref(true);
      const filteredSaleCalendar = vue.computed(() => {
        const out = {};
        Object.entries(saleCalendar.value).forEach(([date, info]) => {
          const c2 = { ...info };
          if (c2.goods) {
            c2.goods = c2.goods.filter((g2) => {
              const typeOk = activeType.value === "全部" || g2.type === activeType.value;
              let sizeOk = activeSize.value === "全部尺寸";
              if (!sizeOk && g2.sizes && g2.sizes.length)
                sizeOk = g2.sizes.some((s2) => s2.goods_size === activeSize.value);
              else if (!sizeOk)
                sizeOk = g2.size === activeSize.value;
              return typeOk && sizeOk;
            });
            c2.goods_number = c2.goods.length;
            if (!c2.goods.length)
              c2.goods = null;
          }
          out[date] = c2;
        });
        return out;
      });
      const currentCalendar = vue.computed(() => {
        if (activeTab.value === "sale")
          return filteredSaleCalendar.value;
        if (activeTab.value === "makeup")
          return makeupCalendar.value;
        return hairCalendar.value;
      });
      const selectDate = (d2) => {
        chooseDate.value = d2;
        updateSelectedItem();
        resetStickySoon();
      };
      vue.watch([activeType, activeSize], () => {
        if (activeTab.value === "sale")
          updateSelectedItem();
      });
      vue.watch(() => filteredSaleCalendar.value, () => {
        if (activeTab.value === "sale")
          updateSelectedItem();
      });
      vue.watch(() => makeupCalendar.value, () => {
        if (activeTab.value === "makeup")
          updateSelectedItem();
      });
      vue.watch(() => hairCalendar.value, () => {
        if (activeTab.value === "hair")
          updateSelectedItem();
      });
      const fetchSaleCalendar = () => new Promise((resolve) => {
        loading2.value = true;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:423", "【日历】开始请求贩售日历");
        uni.request({
          url: websiteUrl.value + "/goods-news",
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            var _a;
            saleCalendar.value = ((_a = res.data) == null ? void 0 : _a.data) || {};
            formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:430", "【日历】贩售日历成功，天数=", Object.keys(saleCalendar.value).length);
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: async () => {
            loading2.value = false;
            if (!chooseDate.value)
              chooseDate.value = todayFormat.value;
            updateSelectedItem();
            scrollLeft.value = itemWidth * 7 - 5;
            await vue.nextTick();
            resetStickySoon();
            resolve();
          }
        });
      });
      const fetchMakeupCalendar = () => new Promise((resolve) => {
        loading2.value = true;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:448", "【日历】开始请求约妆日历");
        uni.request({
          url: websiteUrl.value + "/bjd-makeup-news?type=1",
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            var _a;
            makeupCalendar.value = ((_a = res.data) == null ? void 0 : _a.data) || {};
            formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:455", "【日历】约妆日历成功，天数=", Object.keys(makeupCalendar.value).length);
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: async () => {
            loading2.value = false;
            if (!chooseDate.value)
              chooseDate.value = todayFormat.value;
            updateSelectedItem();
            await vue.nextTick();
            resetStickySoon();
            resolve();
          }
        });
      });
      const fetchHairCalendar = () => new Promise((resolve) => {
        loading2.value = true;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:472", "【日历】开始请求约毛日历");
        uni.request({
          url: websiteUrl.value + "/bjd-makeup-news?type=2",
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            var _a;
            hairCalendar.value = ((_a = res.data) == null ? void 0 : _a.data) || {};
            formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:479", "【日历】约毛日历成功，天数=", Object.keys(hairCalendar.value).length);
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" }),
          complete: async () => {
            loading2.value = false;
            if (!chooseDate.value)
              chooseDate.value = todayFormat.value;
            updateSelectedItem();
            await vue.nextTick();
            resetStickySoon();
            resolve();
          }
        });
      });
      const handleTabClick = (t2) => {
        activeType.value = t2;
      };
      const handleSizeClick = (s2) => {
        activeSize.value = s2;
      };
      const isToday = (d2) => d2 === todayFormat.value;
      function updateSelectedItem() {
        var _a, _b, _c, _d;
        const cal = currentCalendar.value;
        const d2 = chooseDate.value;
        const goodsLen = ((_b = (_a = cal[d2]) == null ? void 0 : _a.goods) == null ? void 0 : _b.length) || 0;
        const plansLen = ((_d = (_c = cal[d2]) == null ? void 0 : _c.plans) == null ? void 0 : _d.length) || 0;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:502", "【日历】更新选中项：tab=", activeTab.value, " 日期=", d2, " goods#=", goodsLen, " plans#=", plansLen);
        if (cal[d2]) {
          chooseItem.value = cal[d2];
          return;
        }
        const first = Object.entries(cal).find(([_2, v2]) => activeTab.value === "sale" && v2.goods || activeTab.value !== "sale" && v2.plans);
        if (first) {
          chooseDate.value = first[0];
          chooseItem.value = first[1];
        } else {
          chooseItem.value = activeTab.value === "sale" ? { goods: null } : { plans: null };
        }
      }
      function formatTimestamp(ts2) {
        const d2 = new Date(ts2 * 1e3);
        const M2 = String(d2.getMonth() + 1).padStart(2, "0");
        const D2 = String(d2.getDate()).padStart(2, "0");
        const h2 = String(d2.getHours()).padStart(2, "0");
        const m2 = String(d2.getMinutes()).padStart(2, "0");
        return `${M2}/${D2} ${h2}:${m2}`;
      }
      function getTiers(plan) {
        try {
          return JSON.parse(plan.order_config).tiers || [];
        } catch {
          return [];
        }
      }
      function getAddons(plan) {
        try {
          return JSON.parse(plan.order_config).addons || [];
        } catch {
          return [];
        }
      }
      function getOrderTypeText(status) {
        const map = { 0: "未知", 1: "长期接单", 2: "限时手速投递", 3: "限时抽选投递", 4: "限时黑箱卡投递", 9: "关闭投递" };
        return map[Number(status)] ?? "未知";
      }
      function getSizeGroups(sizes) {
        if (!sizes || !sizes.length)
          return [];
        const g2 = {};
        sizes.forEach((it2) => {
          const k = it2.goods_size;
          if (!g2[k])
            g2[k] = [];
          if (it2.size_detail)
            g2[k].push(it2.size_detail);
        });
        return Object.keys(g2).map((k) => ({ category: k, details: g2[k] }));
      }
      function getPriceRange(plan) {
        const tiers = getTiers(plan) || [];
        const ps2 = tiers.map((t2) => parseFloat(t2.price)).filter((n2) => !isNaN(n2));
        if (!ps2.length)
          return "待定";
        const min = Math.min(...ps2), max = Math.max(...ps2);
        return min === max ? `${min}元` : `${min}-${max}元`;
      }
      function jumpGoods(id, goodsId) {
        uni.request({ url: websiteUrl.value + "/sale-record-click?id=" + id, method: "POST", header: { "Content-Type": "application/json" } });
        uni.navigateTo({ url: "/pages/goods/goods?goods_id=" + goodsId });
      }
      const navigateToArtistDetail = (artist) => {
        const brandId = (artist == null ? void 0 : artist.brand_id) || (artist == null ? void 0 : artist.BrandId) || 0;
        if (!brandId)
          return;
        if (activeTab.value === "makeup") {
          uni.navigateTo({
            url: "/pages/artist_info/bjd_faceup_artist?brand_id=" + artist.brand_id
          });
        } else {
          uni.navigateTo({ url: "/pages/artist_info/artist_info?brand_id=" + brandId });
        }
      };
      const stickyTitle = vue.ref("");
      const stickyTarget = vue.ref({ kind: "sale", id: 0 });
      const showStickyTitle = vue.computed(() => scrollTop.value > stickyShowThreshold$1 && !!stickyTitle.value);
      const labelMap = { sale: "贩售", makeup: "约妆", hair: "约毛" };
      const planLabel = vue.computed(() => activeTab.value === "hair" ? "约毛" : "约妆");
      const priceLabel = vue.computed(() => activeTab.value === "hair" ? "毛费" : "妆费");
      const selectorCtx = vue.ref(null);
      vue.onMounted(() => {
        var _a;
        selectorCtx.value = ((_a = vue.getCurrentInstance()) == null ? void 0 : _a.proxy) || null;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:568", "【日历】mounted：查询上下文可用=", !!selectorCtx.value);
      });
      onReady(() => {
        var _a;
        if (!selectorCtx.value)
          selectorCtx.value = ((_a = vue.getCurrentInstance()) == null ? void 0 : _a.proxy) || null;
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:572", "【日历】ready：查询上下文（兜底）可用=", !!selectorCtx.value);
      });
      function computeStickyFromViewport() {
        const selector = activeTab.value === "sale" ? ".goods-card" : ".plan-card";
        const topGuard = (safeTop.value || 0) + parseInt(navBarPx) + 8;
        if (!selectorCtx.value)
          return;
        try {
          uni.createSelectorQuery().in(selectorCtx.value).selectAll(selector).fields({ rect: true, dataset: true }, (nodes) => {
            var _a, _b, _c;
            const rects = Array.isArray(nodes) ? nodes : [];
            if (!rects.length) {
              stickyTitle.value = "";
              return;
            }
            let idx = -1;
            let minTop = Number.POSITIVE_INFINITY;
            rects.forEach((r2, i2) => {
              const t2 = r2.top;
              if (t2 >= topGuard && t2 < minTop) {
                minTop = t2;
                idx = i2;
              }
            });
            if (idx < 0)
              rects.forEach((r2, i2) => {
                const t2 = r2.top;
                if (t2 <= topGuard && topGuard - t2 < minTop) {
                  minTop = topGuard - t2;
                  idx = i2;
                }
              });
            if (idx < 0) {
              stickyTitle.value = "";
              return;
            }
            const node2 = rects[idx];
            const kind = ((_a = node2.dataset) == null ? void 0 : _a.kind) || activeTab.value;
            const bid = Number(((_b = node2.dataset) == null ? void 0 : _b.brandId) || 0);
            const bname = ((_c = node2.dataset) == null ? void 0 : _c.brandName) || "";
            stickyTarget.value = { kind, id: bid };
            stickyTitle.value = `${labelMap[kind] || "贩售"} · ${bname || ""}`;
          }).exec();
        } catch (e2) {
          formatAppLog("warn", "at components/calendar-tab/calendar-tab.vue:606", "【日历】计算吸顶异常：", e2);
        }
      }
      let jumping = false;
      async function goStickyTarget() {
        if (jumping)
          return;
        jumping = true;
        const selector = activeTab.value === "sale" ? ".goods-card" : ".plan-card";
        const topGuard = (safeTop.value || 0) + parseInt(navBarPx) + 8;
        const resolved = await new Promise((resolve) => {
          try {
            uni.createSelectorQuery().in(selectorCtx.value).selectAll(selector).fields({ rect: true, dataset: true }, (nodes) => {
              var _a, _b;
              const rects = Array.isArray(nodes) ? nodes : [];
              let idx = -1;
              let minTop = Number.POSITIVE_INFINITY;
              rects.forEach((r2, i2) => {
                const t2 = r2.top;
                if (t2 >= topGuard && t2 < minTop) {
                  minTop = t2;
                  idx = i2;
                }
              });
              if (idx < 0)
                rects.forEach((r2, i2) => {
                  const t2 = r2.top;
                  if (t2 <= topGuard && topGuard - t2 < minTop) {
                    minTop = topGuard - t2;
                    idx = i2;
                  }
                });
              if (idx < 0)
                return resolve(null);
              const n2 = rects[idx];
              resolve({ kind: ((_a = n2.dataset) == null ? void 0 : _a.kind) || activeTab.value, id: Number(((_b = n2.dataset) == null ? void 0 : _b.brandId) || 0) });
            }).exec();
          } catch {
            resolve(null);
          }
        });
        const kind = (resolved == null ? void 0 : resolved.kind) || stickyTarget.value.kind;
        const id = Number((resolved == null ? void 0 : resolved.id) || stickyTarget.value.id || 0);
        if (!id) {
          uni.showToast({ title: "未获取到品牌ID", icon: "none" });
          jumping = false;
          return;
        }
        if (spyTimer) {
          clearTimeout(spyTimer);
          spyTimer = null;
        }
        if (kind === "makeup" || kind === "hair") {
          uni.navigateTo({ url: `/pages/artist_info/artist_info?brand_id=${id}` });
        } else {
          uni.navigateTo({ url: `/pages/brand/brand?brand_id=${id}` });
        }
        setTimeout(() => jumping = false, 500);
      }
      let spyTimer = null;
      function scheduleScrollSpy() {
        if (spyTimer)
          return;
        spyTimer = setTimeout(() => {
          spyTimer = null;
          computeStickyFromViewport();
        }, 120);
      }
      function resetStickySoon() {
        setTimeout(() => computeStickyFromViewport(), 0);
        setTimeout(() => computeStickyFromViewport(), 200);
        setTimeout(() => computeStickyFromViewport(), 600);
      }
      async function refreshAll() {
        formatAppLog("log", "at components/calendar-tab/calendar-tab.vue:669", "【日历】开始刷新全量数据，tab=", activeTab.value);
        todayFormat.value = computeTodayFormat();
        if (!chooseDate.value)
          chooseDate.value = todayFormat.value;
        if (activeTab.value === "makeup")
          await fetchMakeupCalendar();
        else if (activeTab.value === "hair")
          await fetchHairCalendar();
        else
          await fetchSaleCalendar();
        await vue.nextTick();
        resetStickySoon();
      }
      onLoad((options = {}) => {
        var _a;
        try {
          const wi = uni.getWindowInfo && uni.getWindowInfo();
          safeTop.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.top) ?? (wi == null ? void 0 : wi.statusBarHeight) ?? 0;
        } catch {
          safeTop.value = 20;
        }
        const tab = String(options.tab || "").toLowerCase();
        activeTab.value = tab === "makeup" || tab === "sale" || tab === "hair" ? tab : "sale";
        todayFormat.value = computeTodayFormat();
        chooseDate.value = todayFormat.value;
      });
      onShow(async () => {
        if (spyTimer) {
          clearTimeout(spyTimer);
          spyTimer = null;
        }
        await refreshAll();
      });
      onHide(() => {
        if (spyTimer) {
          clearTimeout(spyTimer);
          spyTimer = null;
        }
      });
      onUnload(() => {
        if (spyTimer) {
          clearTimeout(spyTimer);
          spyTimer = null;
        }
      });
      onPullDownRefresh(async () => {
        try {
          await refreshAll();
        } finally {
          uni.stopPullDownRefresh();
        }
      });
      vue.watch(() => chooseItem.value, () => {
        resetStickySoon();
      }, { deep: true });
      const __returned__ = { scrollTop, safeTop, goBack, goHome, navBarPx, headerPadPx, activeTab, switchTab, tabList, sizeList, get activeType() {
        return activeType;
      }, set activeType(v2) {
        activeType = v2;
      }, get activeSize() {
        return activeSize;
      }, set activeSize(v2) {
        activeSize = v2;
      }, saleCalendar, makeupCalendar, hairCalendar, expandStates, toggleFold, getHighlightImages, getPlanImages, previewImage, todayFormat, computeTodayFormat, screenWidth, itemWidth, get scrollLeft() {
        return scrollLeft;
      }, set scrollLeft(v2) {
        scrollLeft = v2;
      }, get chooseDate() {
        return chooseDate;
      }, set chooseDate(v2) {
        chooseDate = v2;
      }, get chooseItem() {
        return chooseItem;
      }, set chooseItem(v2) {
        chooseItem = v2;
      }, get loading() {
        return loading2;
      }, set loading(v2) {
        loading2 = v2;
      }, filteredSaleCalendar, currentCalendar, selectDate, fetchSaleCalendar, fetchMakeupCalendar, fetchHairCalendar, handleTabClick, handleSizeClick, isToday, updateSelectedItem, formatTimestamp, getTiers, getAddons, getOrderTypeText, getSizeGroups, getPriceRange, jumpGoods, navigateToArtistDetail, stickyTitle, stickyTarget, stickyShowThreshold: stickyShowThreshold$1, showStickyTitle, labelMap, planLabel, priceLabel, selectorCtx, computeStickyFromViewport, get jumping() {
        return jumping;
      }, set jumping(v2) {
        jumping = v2;
      }, goStickyTarget, get spyTimer() {
        return spyTimer;
      }, set spyTimer(v2) {
        spyTimer = v2;
      }, scheduleScrollSpy, resetStickySoon, refreshAll, ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick, getCurrentInstance: vue.getCurrentInstance, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get onHide() {
        return onHide;
      }, get onUnload() {
        return onUnload;
      }, get onPullDownRefresh() {
        return onPullDownRefresh;
      }, get onPageScroll() {
        return onPageScroll;
      }, get onReady() {
        return onReady;
      }, get websiteUrl() {
        return websiteUrl;
      }, get getStatusBarHeight() {
        return getStatusBarHeight;
      }, get getNavBarHeight() {
        return getNavBarHeight;
      }, get toPx() {
        return toPx;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1h(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_loading_toast = resolveEasycom(vue.resolveDynamicComponent("loading-toast"), __easycom_3$8);
    return vue.openBlock(), vue.createElementBlock("view", { class: "summary-wrap" }, [
      vue.createVNode(_component_view_logs),
      vue.withDirectives(vue.createElementVNode(
        "view",
        {
          class: "sticky-titlebar",
          style: vue.normalizeStyle({ paddingTop: $setup.safeTop + "px" })
        },
        [
          vue.createElementVNode("view", { class: "st-left" }, [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#222"
              })
            ])
          ]),
          vue.createElementVNode("view", {
            class: "st-center",
            onClick: $setup.goStickyTarget,
            "aria-label": "打开品牌/作者"
          }, [
            vue.createElementVNode(
              "text",
              { class: "sticky-title" },
              vue.toDisplayString($setup.stickyTitle),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "st-right" })
        ],
        4
        /* STYLE */
      ), [
        [vue.vShow, $setup.showStickyTitle]
      ]),
      vue.createElementVNode("view", {
        class: "header-gradient",
        onClick: $setup.goHome,
        "aria-label": "回主页"
      }, [
        vue.createElementVNode(
          "view",
          {
            class: "nav-placeholder",
            style: vue.normalizeStyle({ height: $setup.headerPadPx })
          },
          null,
          4
          /* STYLE */
        ),
        vue.createElementVNode("view", {
          class: "tabs-underline",
          onClick: _cache[3] || (_cache[3] = vue.withModifiers(() => {
          }, ["stop"]))
        }, [
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["tab", { active: $setup.activeTab === "sale" }]),
              onClick: _cache[0] || (_cache[0] = vue.withModifiers(($event) => $setup.switchTab("sale"), ["stop"]))
            },
            [
              vue.createElementVNode("text", null, "贩售"),
              $setup.activeTab === "sale" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "underline"
              })) : vue.createCommentVNode("v-if", true)
            ],
            2
            /* CLASS */
          ),
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["tab", { active: $setup.activeTab === "makeup" }]),
              onClick: _cache[1] || (_cache[1] = vue.withModifiers(($event) => $setup.switchTab("makeup"), ["stop"]))
            },
            [
              vue.createElementVNode("text", null, "约妆"),
              $setup.activeTab === "makeup" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "underline"
              })) : vue.createCommentVNode("v-if", true)
            ],
            2
            /* CLASS */
          ),
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["tab", { active: $setup.activeTab === "hair" }]),
              onClick: _cache[2] || (_cache[2] = vue.withModifiers(($event) => $setup.switchTab("hair"), ["stop"]))
            },
            [
              vue.createElementVNode("text", null, "约毛"),
              $setup.activeTab === "hair" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "underline"
              })) : vue.createCommentVNode("v-if", true)
            ],
            2
            /* CLASS */
          )
        ])
      ]),
      $setup.activeTab === "sale" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "category-container"
      }, [
        vue.createElementVNode("scroll-view", {
          "scroll-x": "",
          class: "category-scroll",
          "show-scrollbar": false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.tabList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: vue.normalizeClass(["category-item", { active: $setup.activeType === item }]),
                key: index,
                onClick: ($event) => $setup.handleTabClick(item)
              }, vue.toDisplayString(item), 11, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ])) : vue.createCommentVNode("v-if", true),
      $setup.activeTab === "sale" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "size-container"
      }, [
        vue.createElementVNode("scroll-view", {
          "scroll-x": "",
          class: "size-scroll",
          "show-scrollbar": false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.sizeList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: vue.normalizeClass(["size-item", { active: $setup.activeSize === item }]),
                key: index,
                onClick: ($event) => $setup.handleSizeClick(item)
              }, vue.toDisplayString(item), 11, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ])) : vue.createCommentVNode("v-if", true),
      $setup.activeTab !== "sale" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        style: { "height": "50rpx", "background": "#f5f6f8" }
      })) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "date-picker-container" }, [
        vue.createElementVNode("scroll-view", {
          class: "date-scroll",
          "scroll-x": "",
          "scroll-left": $setup.scrollLeft,
          "show-scrollbar": false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.currentCalendar, (item, date) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: date,
                class: "date-item",
                onClick: ($event) => $setup.selectDate(date)
              }, [
                ($setup.activeTab === "sale" ? item.goods_number > 0 : item.plans_number > 0) ? (vue.openBlock(), vue.createElementBlock(
                  "view",
                  {
                    key: 0,
                    class: "date-badge"
                  },
                  vue.toDisplayString($setup.activeTab === "sale" ? item.goods_number : item.plans_number),
                  1
                  /* TEXT */
                )) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["weekday", { "weekend": item.weekday === "周日" || item.weekday === "周六" }])
                  },
                  vue.toDisplayString(item.weekday),
                  3
                  /* TEXT, CLASS */
                ),
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["day-number", { "selected": $setup.chooseDate === date }])
                  },
                  vue.toDisplayString($setup.isToday(date) ? "今天" : item.day_number),
                  3
                  /* TEXT, CLASS */
                )
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ], 8, ["scroll-left"])
      ]),
      vue.createElementVNode("view", { class: "goods-container" }, [
        $setup.activeTab === "sale" ? (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 0 },
          [
            vue.createElementVNode("view", { class: "date-title" }, [
              vue.createElementVNode(
                "text",
                { class: "highlight" },
                vue.toDisplayString($setup.chooseDate) + " 日",
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                null,
                " 星期" + vue.toDisplayString($setup.chooseItem.weekday) + "上新",
                1
                /* TEXT */
              )
            ]),
            $setup.chooseItem.goods == null ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "empty-tip"
            }, [
              vue.createElementVNode("image", {
                src: _imports_1$4,
                class: "empty-icon"
              }),
              vue.createElementVNode("text", null, "这天没有上新呢...")
            ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.chooseItem.goods, (good) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: good.id,
                    class: "goods-card",
                    "data-brand-id": good.brand_id,
                    "data-brand-name": good.brand_name,
                    "data-kind": "sale",
                    onClick: ($event) => $setup.jumpGoods(good.id, good.goods_id)
                  }, [
                    vue.createElementVNode("view", { class: "goods-image-container" }, [
                      vue.createElementVNode("image", {
                        src: good.goods_image,
                        mode: "aspectFill",
                        class: "goods-image"
                      }, null, 8, ["src"]),
                      vue.createElementVNode("view", { class: "goods-tags" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "tag sale-type" },
                          vue.toDisplayString(good.type) + " · " + vue.toDisplayString(good.sale_type),
                          1
                          /* TEXT */
                        ),
                        good.is_first_sale_day == 1 ? (vue.openBlock(), vue.createElementBlock("text", {
                          key: 0,
                          class: "tag first-day"
                        }, "首日")) : vue.createCommentVNode("v-if", true)
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "goods-info" }, [
                      vue.createElementVNode("view", null, [
                        vue.createElementVNode(
                          "text",
                          { class: "brand-name" },
                          vue.toDisplayString(good.brand_name),
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          { class: "goods-name" },
                          vue.toDisplayString(good.goods_name),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "goods-details" }, [
                        good.sizes && good.sizes.length ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                          (vue.openBlock(true), vue.createElementBlock(
                            vue.Fragment,
                            null,
                            vue.renderList($setup.getSizeGroups(good.sizes), (g2, i2) => {
                              return vue.openBlock(), vue.createElementBlock("view", {
                                key: i2,
                                class: "size-group"
                              }, [
                                vue.createElementVNode(
                                  "text",
                                  { class: "size-category" },
                                  vue.toDisplayString(g2.category) + "：",
                                  1
                                  /* TEXT */
                                ),
                                vue.createElementVNode(
                                  "text",
                                  { class: "size-details" },
                                  vue.toDisplayString(g2.details.join("、")),
                                  1
                                  /* TEXT */
                                )
                              ]);
                            }),
                            128
                            /* KEYED_FRAGMENT */
                          ))
                        ])) : (vue.openBlock(), vue.createElementBlock(
                          "text",
                          { key: 1 },
                          vue.toDisplayString(good.size) + " · " + vue.toDisplayString(good.size_detail),
                          1
                          /* TEXT */
                        ))
                      ]),
                      vue.createElementVNode("view", { class: "time-info" }, [
                        vue.createElementVNode(
                          "text",
                          null,
                          "开定 " + vue.toDisplayString($setup.formatTimestamp(good.sub_time)),
                          1
                          /* TEXT */
                        ),
                        good.sub_time_end ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          { key: 0 },
                          "截止 " + vue.toDisplayString($setup.formatTimestamp(good.sub_time_end)),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ]),
                      vue.createElementVNode("view", { class: "price-info" }, [
                        good.sale_type != "限量现货" && good.sale_type != "不限量现货" ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                          vue.createElementVNode("view", { class: "price-group" }, [
                            vue.createElementVNode("text", { class: "price-label" }, "定金"),
                            vue.createElementVNode(
                              "text",
                              { class: "deposit-price" },
                              vue.toDisplayString(good.sub_amount),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "currency" },
                              "(" + vue.toDisplayString(good.currency) + ")",
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "price-group" }, [
                            vue.createElementVNode("text", { class: "price-label" }, "尾款"),
                            vue.createElementVNode(
                              "text",
                              { class: "final-price" },
                              vue.toDisplayString(good.fin_amount),
                              1
                              /* TEXT */
                            )
                          ])
                        ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
                          vue.createElementVNode("view", { class: "price-group" }, [
                            vue.createElementVNode("text", { class: "price-label" }, "全款"),
                            vue.createElementVNode(
                              "text",
                              { class: "full-price" },
                              vue.toDisplayString(good.sub_amount + good.fin_amount),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "currency" },
                              "(" + vue.toDisplayString(good.currency) + ")",
                              1
                              /* TEXT */
                            )
                          ])
                        ]))
                      ])
                    ])
                  ], 8, ["data-brand-id", "data-brand-name", "onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]))
          ],
          64
          /* STABLE_FRAGMENT */
        )) : (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 1 },
          [
            vue.createElementVNode("view", { class: "date-title" }, [
              vue.createElementVNode(
                "text",
                { class: "highlight" },
                vue.toDisplayString($setup.chooseDate) + " 日",
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                null,
                " 星期" + vue.toDisplayString($setup.chooseItem.weekday) + vue.toDisplayString($setup.planLabel),
                1
                /* TEXT */
              )
            ]),
            $setup.chooseItem.plans == null || $setup.chooseItem.plans.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "empty-tip"
            }, [
              vue.createElementVNode("image", {
                src: _imports_1$4,
                class: "empty-icon"
              }),
              vue.createElementVNode(
                "text",
                null,
                "这天没有" + vue.toDisplayString($setup.planLabel) + "计划呢...",
                1
                /* TEXT */
              )
            ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.chooseItem.plans, (plan) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "plan-card",
                    key: plan.id,
                    "data-brand-id": plan.artist_info && (plan.artist_info.brand_id || plan.artist_info.BrandId) || plan.brand_id,
                    "data-brand-name": plan.artist_name || plan.artist_info && (plan.artist_info.BrandName || plan.artist_info.brand_name) || "",
                    "data-kind": $setup.activeTab,
                    onClick: ($event) => $setup.navigateToArtistDetail(plan.artist_info)
                  }, [
                    vue.createElementVNode("view", { class: "artist-header" }, [
                      vue.createElementVNode("view", { class: "artist-info" }, [
                        plan.artist_info && (plan.artist_info.LogoImage || plan.artist_info.logo_image) ? (vue.openBlock(), vue.createElementBlock("image", {
                          key: 0,
                          src: plan.artist_info.LogoImage || plan.artist_info.logo_image,
                          mode: "aspectFill",
                          class: "artist-avatar"
                        }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                        vue.createElementVNode("view", { class: "artist-details" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "artist-name" },
                            vue.toDisplayString(plan.artist_name),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode("view", { class: "makeup-price-range" }, [
                            vue.createElementVNode(
                              "text",
                              null,
                              vue.toDisplayString($setup.priceLabel) + ": " + vue.toDisplayString($setup.getPriceRange(plan)),
                              1
                              /* TEXT */
                            )
                          ])
                        ])
                      ])
                    ]),
                    plan.artist_info && (plan.artist_info.Description || plan.artist_info.description) ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        class: "artist-description"
                      },
                      vue.toDisplayString(plan.artist_info.Description || plan.artist_info.description),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    $setup.getPlanImages(plan).length ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "highlight-images"
                    }, [
                      vue.createElementVNode("scroll-view", {
                        "scroll-x": "",
                        class: "image-scroll"
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.getPlanImages(plan), (img, i2) => {
                            return vue.openBlock(), vue.createElementBlock("image", {
                              key: i2,
                              src: img,
                              mode: "aspectFill",
                              class: "highlight-image",
                              onClick: vue.withModifiers(($event) => $setup.previewImage(img), ["stop"])
                            }, null, 8, ["src", "onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["foldable-section", { expanded: $setup.expandStates[plan.id] }])
                      },
                      [
                        vue.createElementVNode("view", { class: "plan-info" }, [
                          vue.createElementVNode("view", { class: "info-row" }, [
                            vue.createElementVNode("text", { class: "label" }, "开放时间:"),
                            vue.createElementVNode(
                              "text",
                              { class: "value" },
                              vue.toDisplayString($setup.formatTimestamp(plan.open_time)) + " - " + vue.toDisplayString($setup.formatTimestamp(plan.close_time)),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "info-row" }, [
                            vue.createElementVNode("text", { class: "label" }, "接单类型:"),
                            vue.createElementVNode(
                              "text",
                              { class: "value" },
                              vue.toDisplayString($setup.getOrderTypeText(plan.order_type)),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "info-row" }, [
                            vue.createElementVNode("text", { class: "label" }, "最大人数:"),
                            vue.createElementVNode(
                              "text",
                              { class: "value" },
                              vue.toDisplayString(plan.max_participants === 0 ? "不限量" : plan.max_participants),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "info-row" }, [
                            vue.createElementVNode("text", { class: "label" }, "每人限投:"),
                            vue.createElementVNode(
                              "text",
                              { class: "value" },
                              vue.toDisplayString(plan.max_submissions_per_user) + "次",
                              1
                              /* TEXT */
                            )
                          ])
                        ]),
                        vue.createElementVNode("view", { class: "plan-config" }, [
                          vue.createElementVNode("view", { class: "config-section" }, [
                            vue.createElementVNode("text", { class: "section-title" }, "档位选择"),
                            (vue.openBlock(true), vue.createElementBlock(
                              vue.Fragment,
                              null,
                              vue.renderList($setup.getTiers(plan), (tier) => {
                                return vue.openBlock(), vue.createElementBlock("view", {
                                  key: tier.title,
                                  class: "tier-item"
                                }, [
                                  vue.createElementVNode(
                                    "text",
                                    { class: "tier-title" },
                                    vue.toDisplayString(tier.title),
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode(
                                    "text",
                                    { class: "tier-price" },
                                    vue.toDisplayString(tier.price) + "元",
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode(
                                    "text",
                                    { class: "tier-desc" },
                                    vue.toDisplayString(tier.description),
                                    1
                                    /* TEXT */
                                  )
                                ]);
                              }),
                              128
                              /* KEYED_FRAGMENT */
                            ))
                          ]),
                          $setup.getAddons(plan).length ? (vue.openBlock(), vue.createElementBlock("view", {
                            key: 0,
                            class: "config-section"
                          }, [
                            vue.createElementVNode("text", { class: "section-title" }, "加购服务"),
                            (vue.openBlock(true), vue.createElementBlock(
                              vue.Fragment,
                              null,
                              vue.renderList($setup.getAddons(plan), (addon) => {
                                return vue.openBlock(), vue.createElementBlock("view", {
                                  key: addon.title,
                                  class: "addon-item"
                                }, [
                                  vue.createElementVNode(
                                    "text",
                                    { class: "addon-title" },
                                    vue.toDisplayString(addon.title),
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode(
                                    "text",
                                    { class: "addon-price" },
                                    vue.toDisplayString(addon.price) + "元",
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode(
                                    "text",
                                    { class: "addon-desc" },
                                    vue.toDisplayString(addon.description),
                                    1
                                    /* TEXT */
                                  )
                                ]);
                              }),
                              128
                              /* KEYED_FRAGMENT */
                            ))
                          ])) : vue.createCommentVNode("v-if", true)
                        ])
                      ],
                      2
                      /* CLASS */
                    ),
                    vue.createElementVNode("view", {
                      class: "fold-toggle",
                      onClick: vue.withModifiers(($event) => $setup.toggleFold(plan.id), ["stop"])
                    }, [
                      vue.createElementVNode(
                        "text",
                        null,
                        vue.toDisplayString($setup.expandStates[plan.id] ? "收起" : "展开"),
                        1
                        /* TEXT */
                      ),
                      vue.createVNode(_component_uni_icons, {
                        type: $setup.expandStates[plan.id] ? "arrowup" : "arrowdown",
                        size: "16",
                        color: "#81D8cf"
                      }, null, 8, ["type"])
                    ], 8, ["onClick"])
                  ], 8, ["data-brand-id", "data-brand-name", "data-kind", "onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]))
          ],
          64
          /* STABLE_FRAGMENT */
        ))
      ]),
      vue.createVNode(_component_loading_toast, { show: $setup.loading }, null, 8, ["show"])
    ]);
  }
  const __easycom_2$3 = /* @__PURE__ */ _export_sfc(_sfc_main$1i, [["render", _sfc_render$1h], ["__scopeId", "data-v-aa462a5c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/calendar-tab/calendar-tab.vue"]]);
  const _sfc_main$1h = {
    __name: "calendar",
    setup(__props, { expose: __expose }) {
      __expose();
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      const headerPadPx = vue.computed(() => toPx(getStatusBarHeight() + getNavBarHeight()));
      function goBack() {
        const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
        if (pages2.length > 1) {
          uni.navigateBack({ delta: 1 });
        } else {
          uni.switchTab({ url: "/pages/index/index" });
        }
      }
      const __returned__ = { scrollTop, headerPadPx, goBack, ref: vue.ref, computed: vue.computed, get onPageScroll() {
        return onPageScroll;
      }, get getStatusBarHeight() {
        return getStatusBarHeight;
      }, get getNavBarHeight() {
        return getNavBarHeight;
      }, get toPx() {
        return toPx;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1g(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_calendar_tab = resolveEasycom(vue.resolveDynamicComponent("calendar-tab"), __easycom_2$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "calendar-page" }, [
      vue.createVNode(_component_zhouWei_navBar, {
        type: "transparentFixed",
        backState: 2e3,
        homeState: 2e3,
        fontColor: "#000",
        transparentFixedFontColor: "#000",
        scrollTop: $setup.scrollTop,
        titleCenter: true
      }, {
        left: vue.withCtx(() => [
          vue.createElementVNode("view", {
            class: "nav-back-pill",
            onClick: $setup.goBack
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "left",
              size: "22",
              color: "#000"
            })
          ])
        ]),
        transparentFixedLeft: vue.withCtx(() => [
          vue.createElementVNode("view", {
            class: "nav-back-pill",
            onClick: $setup.goBack
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "left",
              size: "22",
              color: "#000"
            })
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["scrollTop"]),
      vue.createVNode(_component_calendar_tab)
    ]);
  }
  const PagesCalendarCalendar = /* @__PURE__ */ _export_sfc(_sfc_main$1h, [["render", _sfc_render$1g], ["__scopeId", "data-v-6e8913ab"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/calendar/calendar.vue"]]);
  const STEP_MS = 700;
  const STORAGE_DATE_KEY = "summary_page_date_key";
  const _sfc_main$1g = {
    __name: "summary-tab",
    setup(__props, { expose: __expose }) {
      __expose();
      const todayList = vue.ref([]);
      const todayLoading = vue.ref(true);
      const hotList = vue.ref([]);
      const hot7List = vue.ref([]);
      const hotMode = vue.ref("today");
      const hotLoading = vue.ref(true);
      const isRefreshing = vue.ref(false);
      const themeGroups = vue.ref([]);
      const themeLoading = vue.ref(true);
      const activeThemeKey = vue.ref("");
      const likeBrands = vue.ref([]);
      const likesLoading = vue.ref(true);
      const hasToken = vue.ref(false);
      const timeline = vue.ref([]);
      const timelineLoading = vue.ref(true);
      const opsList = vue.ref([]);
      const opsLoading = vue.ref(true);
      const visibleCount = vue.ref(0);
      const staggerStarted = vue.ref(false);
      let revealTimer = null;
      const instance = vue.getCurrentInstance();
      let tlObserver = null;
      function startStaggerReveal() {
        if (staggerStarted.value)
          return;
        staggerStarted.value = true;
        visibleCount.value = 0;
        const total = timeline.value.length || 0;
        if (total === 0)
          return;
        const tick = () => {
          if (visibleCount.value < total) {
            visibleCount.value += 1;
            revealTimer = setTimeout(tick, STEP_MS);
          } else {
            stopStaggerReveal();
          }
        };
        tick();
      }
      function stopStaggerReveal() {
        if (revealTimer) {
          clearTimeout(revealTimer);
          revealTimer = null;
        }
      }
      function initTlObserver() {
        destroyTlObserver();
        tlObserver = uni.createIntersectionObserver(instance);
        tlObserver.relativeToViewport({ top: 160 });
        tlObserver.observe(".timeline", (res) => {
          if (!res)
            return;
          const ratio = (res == null ? void 0 : res.intersectionRatio) || 0;
          if (ratio > 0) {
            startStaggerReveal();
            destroyTlObserver();
          }
        });
      }
      function destroyTlObserver() {
        if (tlObserver && typeof tlObserver.disconnect === "function") {
          tlObserver.disconnect();
        }
        tlObserver = null;
      }
      function resetTimelineRevealAndObserve() {
        stopStaggerReveal();
        visibleCount.value = 0;
        staggerStarted.value = false;
        vue.nextTick(() => {
          initTlObserver();
          forceRevealIfStuck();
        });
      }
      function forceRevealIfStuck() {
        setTimeout(() => {
          if (timeline.value.length > 0 && visibleCount.value === 0 && !staggerStarted.value) {
            visibleCount.value = timeline.value.length;
          }
        }, 800);
      }
      const waitingList = vue.ref([]);
      const waitingLoading = vue.ref(true);
      function normalizePlainGoods(list) {
        return (list || []).map((it2) => {
          var _a;
          const cover = it2.goods_image || (Array.isArray(it2.goods_images) ? it2.goods_images[0] : "");
          const sizes = Array.isArray(it2.sizes) ? it2.sizes : [];
          return {
            recordId: 0,
            // 关键：不产生记录ID，避免 /sale-record-click
            goodsId: it2.goods_id || it2.id,
            cover,
            brand_name: it2.brand_name || ((_a = it2.brand) == null ? void 0 : _a.brand_name) || "",
            goods_name: it2.goods_name || it2.name || "",
            type: it2.type || "",
            views: it2.views || 0,
            sizeText: sizeText(sizes)
          };
        });
      }
      function fetchWaitingSale() {
        waitingLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/goods/waiting-sale",
          method: "GET",
          data: { page: 1, page_size: 12, watermark: 1 },
          timeout: 5e3
        }).then((res) => {
          var _a, _b, _c, _d, _e2;
          const list = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.goods_list) || ((_d = (_c = res == null ? void 0 : res.data) == null ? void 0 : _c.data) == null ? void 0 : _d.list) || ((_e2 = res == null ? void 0 : res.data) == null ? void 0 : _e2.data) || [];
          waitingList.value = normalizePlainGoods(list);
        }).finally(() => {
          waitingLoading.value = false;
        });
      }
      const sectionMap = [
        { sel: ".s-today", key: "today" },
        { sel: ".s-waiting", key: "waiting" },
        { sel: ".s-hot", key: "hot" },
        { sel: ".s-themes", key: "themes" },
        { sel: ".s-ops", key: "ops" },
        { sel: ".s-likes", key: "likes" },
        { sel: ".s-timeline", key: "timeline" }
      ];
      const secInView = vue.ref({
        today: false,
        waiting: false,
        hot: false,
        themes: false,
        ops: false,
        likes: false,
        timeline: false
      });
      const lastRatios = vue.ref({});
      const activeSection = vue.ref("");
      let secObservers = [];
      let activeSetTimer = null;
      function pickActiveByMaxRatio() {
        let bestKey = "";
        let bestRatio = 0;
        for (const { key } of sectionMap) {
          const r2 = lastRatios.value[key] || 0;
          if (r2 > bestRatio) {
            bestRatio = r2;
            bestKey = key;
          }
        }
        activeSection.value = bestRatio > 0.02 ? bestKey : "";
      }
      function schedulePickActive() {
        if (activeSetTimer)
          clearTimeout(activeSetTimer);
        activeSetTimer = setTimeout(() => {
          pickActiveByMaxRatio();
          activeSetTimer = null;
        }, 80);
      }
      function initSectionInViewObservers() {
        destroySectionInViewObservers();
        sectionMap.forEach(({ sel, key }) => {
          const ob = uni.createIntersectionObserver(instance);
          ob.relativeToViewport({ top: 120, bottom: 120 });
          ob.observe(sel, (res) => {
            const ratio = (res == null ? void 0 : res.intersectionRatio) || 0;
            secInView.value[key] = ratio > 0;
            lastRatios.value[key] = ratio;
            schedulePickActive();
          });
          secObservers.push(ob);
        });
      }
      function destroySectionInViewObservers() {
        secObservers.forEach((o2) => {
          try {
            o2.disconnect();
          } catch (_2) {
          }
        });
        secObservers = [];
      }
      function readToken() {
        return uni.getStorageSync("Authorization") || uni.getStorageSync("token") || "";
      }
      function refreshAuthFlag() {
        hasToken.value = !!readToken();
      }
      async function ensureAuth() {
        try {
          if (typeof asyncGetUserInfo === "function") {
            await asyncGetUserInfo();
          }
        } catch (_2) {
        }
        refreshAuthFlag();
      }
      function getAuthHeader() {
        const t2 = readToken();
        return t2 ? { Authorization: t2 } : {};
      }
      function todayKey() {
        const d2 = /* @__PURE__ */ new Date();
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const dd = String(d2.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${dd}`;
      }
      async function refreshAll(isInit = false) {
        try {
          await Promise.all([
            fetchTodaySales(),
            // 今日上新
            fetchWaitingSale(),
            // 等待贩售
            fetchHotToday(),
            // 今日热榜
            fetchThemes(),
            // 主题合集
            fetchLikes(),
            // 订阅品牌
            fetchTimeline(),
            // 即将上新时间线
            fetchOpsFeedRecent()
            // 最近入库
          ]);
        } finally {
          uni.setStorageSync(STORAGE_DATE_KEY, todayKey());
        }
      }
      async function onTapSummary() {
        if (isRefreshing.value)
          return;
        isRefreshing.value = true;
        try {
          uni.showNavigationBarLoading();
          uni.showToast({ title: "正在刷新", icon: "none", duration: 700 });
          await refreshAll(false);
          uni.showToast({ title: "已更新", icon: "success", duration: 800 });
          try {
            uni.vibrateShort({});
          } catch (_2) {
          }
        } finally {
          uni.hideNavigationBarLoading();
          isRefreshing.value = false;
        }
      }
      function onTapCalendar() {
        goCalendar();
      }
      function normalizeGoodsList(list) {
        return (list || []).map((it2) => {
          var _a;
          const cover = it2.goods_image || (Array.isArray(it2.goods_images) ? it2.goods_images[0] : "");
          const sizes = Array.isArray(it2.sizes) ? it2.sizes : [];
          return {
            recordId: it2.id,
            goodsId: it2.goods_id || it2.id,
            cover,
            brand_name: it2.brand_name || ((_a = it2.brand) == null ? void 0 : _a.brand_name) || "",
            goods_name: it2.goods_name || it2.name || "",
            type: it2.type || "",
            views: it2.views || it2.click_count || 0,
            sizeText: sizeText(sizes)
          };
        });
      }
      function fetchTodaySales() {
        todayLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/sales-start-today",
          method: "GET",
          data: { limit: 12 },
          timeout: 5e3
        }).then((res) => {
          var _a, _b, _c;
          const list = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.list) || ((_c = res == null ? void 0 : res.data) == null ? void 0 : _c.data) || [];
          todayList.value = normalizeGoodsList(list);
        }).finally(() => {
          todayLoading.value = false;
        });
      }
      function fetchHotToday() {
        hotLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/goods-hot-today",
          method: "GET",
          data: { limit: 6 },
          timeout: 5e3
        }).then((res) => {
          var _a, _b;
          const list = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.list) || [];
          hotList.value = normalizeGoodsList(list);
        }).finally(() => {
          hotLoading.value = false;
        });
      }
      function fetchHot7Days() {
        hotLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/goods-hot-7days",
          method: "GET",
          data: { limit: 6 },
          timeout: 5e3
        }).then((res) => {
          var _a, _b;
          const list = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.list) || [];
          hot7List.value = normalizeGoodsList(list);
        }).finally(() => {
          hotLoading.value = false;
        });
      }
      function fetchThemes() {
        themeLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/goods-themes",
          method: "GET",
          timeout: 5e3
        }).then((res) => {
          var _a;
          const groups = ((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) || [];
          themeGroups.value = Array.isArray(groups) ? groups : [];
          if (!activeThemeKey.value && themeGroups.value.length) {
            activeThemeKey.value = themeGroups.value[0].key;
          }
        }).finally(() => {
          themeLoading.value = false;
        });
      }
      function fetchLikes() {
        likesLoading.value = true;
        refreshAuthFlag();
        if (!hasToken.value) {
          likesLoading.value = false;
          likeBrands.value = [];
          return Promise.resolve();
        }
        return uni.request({
          url: websiteUrl.value + "/with-state/user-likes",
          method: "GET",
          data: { type: 2, page: 1, page_size: 8 },
          header: getAuthHeader(),
          timeout: 5e3
        }).then((res) => {
          var _a, _b;
          likeBrands.value = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.list) || [];
        }).finally(() => {
          likesLoading.value = false;
        });
      }
      function fetchTimeline() {
        timelineLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/goods-timeline",
          method: "GET",
          timeout: 5e3
        }).then((res) => {
          var _a;
          const raw = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data;
          const list = Array.isArray(raw) ? raw : (raw == null ? void 0 : raw.list) || [];
          const now = Math.floor(Date.now() / 1e3);
          const from = now - 3600;
          const to = now + 86400;
          const norm = list.map((it2) => {
            var _a2;
            const time = deriveTime(it2);
            const cover = it2.goods_image || (Array.isArray(it2.goods_images) ? it2.goods_images[0] : "");
            const sizes = Array.isArray(it2.sizes) ? it2.sizes : [];
            return {
              key: `${it2.id}-${time || 0}`,
              recordId: it2.id,
              goodsId: it2.goods_id || it2.id,
              time: time || 0,
              cover,
              brand_name: it2.brand_name || ((_a2 = it2.brand) == null ? void 0 : _a2.brand_name) || "",
              goods_name: it2.goods_name || it2.name || "",
              type: it2.type || "",
              views: it2.views || it2.click_count || 0,
              sizeText: sizeText(sizes),
              sizeChips: sizeChips(sizes)
            };
          }).filter((x) => x.time >= from && x.time <= to);
          norm.sort((a2, b2) => a2.time - b2.time);
          timeline.value = norm;
          resetTimelineRevealAndObserve();
          setTimeout(() => {
            if (!staggerStarted.value) {
              uni.createSelectorQuery().in(instance).select(".timeline").boundingClientRect((rect) => {
                try {
                  const vh = uni.getSystemInfoSync().windowHeight || 0;
                  if (rect && typeof rect.top === "number" && rect.top < vh - 160) {
                    startStaggerReveal();
                  }
                } catch (_2) {
                }
              }).exec();
            }
          }, 0);
        }).finally(() => {
          timelineLoading.value = false;
        });
      }
      function fetchOpsFeedRecent() {
        opsLoading.value = true;
        return uni.request({
          url: websiteUrl.value + "/ops-feed/recent",
          method: "GET",
          data: { hours: 24, page: 1, page_size: 12 },
          timeout: 5e3
        }).then((res) => {
          var _a, _b;
          const rows = ((_b = (_a = res == null ? void 0 : res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.list) || [];
          opsList.value = rows.map((r2) => ({
            id: r2.id,
            op_type: r2.op_type,
            target_id: r2.target_id,
            brand_id: r2.brand_id,
            brand_name: r2.brand_name || "",
            goods_id: r2.goods_id,
            goods_name: r2.goods_name || "",
            summary: r2.summary || "",
            image_url: r2.image_url || "",
            time_ago: r2.time_ago || ""
          }));
        }).finally(() => {
          opsLoading.value = false;
        });
      }
      const themeChips = vue.computed(
        () => (themeGroups.value || []).map((g2) => ({ key: g2.key, title: g2.title }))
      );
      const filteredThemeItems = vue.computed(() => {
        const g2 = (themeGroups.value || []).find((x) => x.key === activeThemeKey.value);
        if (!g2 || !Array.isArray(g2.items))
          return [];
        return g2.items.map((it2) => {
          const info = it2.goods_info || {};
          const sizes = it2.sizes || info.sizes || [];
          const cover = it2.goods_image || (Array.isArray(info.goods_images) ? info.goods_images[0] : "") || (Array.isArray(it2.goods_images) ? it2.goods_images[0] : "") || "";
          return {
            key: `${it2.id}-${it2.goods_id || info.id || ""}`,
            recordId: it2.id,
            goodsId: it2.goods_id || info.id || it2.id,
            cover,
            goods_name: it2.goods_name || it2.name || info.name || "",
            brand_name: it2.brand_name || info.brand_name || "",
            type: it2.type || info.type || "",
            sizeText: sizeText(sizes)
          };
        });
      });
      function onSearchSubmit() {
      }
      function sizeText(sizes) {
        if (!Array.isArray(sizes) || sizes.length === 0)
          return "";
        const groups = {};
        sizes.forEach((s2) => {
          var _a;
          (groups[_a = s2.goods_size] || (groups[_a] = /* @__PURE__ */ new Set())).add((s2.size_detail || "").trim());
        });
        return Object.keys(groups).map((k) => {
          const arr = Array.from(groups[k]).filter(Boolean);
          return arr.length ? `${k}(${arr.join("、")})` : k;
        }).join("，");
      }
      function sizeChips(sizes) {
        if (!Array.isArray(sizes) || sizes.length === 0)
          return [];
        const groups = {};
        sizes.forEach((s2) => {
          const key = (s2.goods_size || "").trim();
          const detail = (s2.size_detail || "").trim();
          if (!key)
            return;
          groups[key] || (groups[key] = /* @__PURE__ */ new Set());
          if (detail)
            groups[key].add(detail);
        });
        return Object.keys(groups).map((k) => {
          const arr = Array.from(groups[k]);
          return arr.length ? `${k}（${arr.join("、")}）` : k;
        });
      }
      function go2recentFeed() {
        uni.navigateTo({ url: "/pages/recent-feed/recent-feed" });
      }
      function goOpsFeed() {
        uni.navigateTo({ url: "/pages/ops_feed/ops_feed" });
      }
      function goCalendar() {
        uni.navigateTo({ url: "/pages/calendar/calendar" });
      }
      function deriveTime(it2) {
        const cands = [];
        if (it2.sub_time)
          cands.push(+it2.sub_time);
        for (let i2 = 1; i2 <= 5; i2++) {
          const v2 = it2[`sub_time${i2}`];
          if (v2)
            cands.push(+v2);
        }
        if (it2.start_time)
          cands.push(+it2.start_time);
        return cands.filter((x) => x > 0).sort((a2, b2) => a2 - b2)[0] || 0;
      }
      function fmtHM(ts2) {
        const d2 = new Date(ts2 * 1e3);
        const h2 = String(d2.getHours()).padStart(2, "0");
        const m2 = String(d2.getMinutes()).padStart(2, "0");
        return `${h2}:${m2}`;
      }
      function fmtMD(ts2) {
        const d2 = new Date(ts2 * 1e3);
        const M2 = String(d2.getMonth() + 1).padStart(2, "0");
        const D2 = String(d2.getDate()).padStart(2, "0");
        return `${M2}/${D2}`;
      }
      function formatViews(views) {
        if (views >= 1e4)
          return (views / 1e4).toFixed(1) + "w";
        if (views >= 1e3)
          return (views / 1e3).toFixed(1) + "k";
        return views;
      }
      function goGoods(goodsId, recordId) {
        if (recordId) {
          uni.request({
            url: websiteUrl.value + "/sale-record-click?id=" + recordId,
            method: "POST",
            header: { "Content-Type": "application/json" }
          });
        }
        if (!goodsId)
          return;
        uni.navigateTo({ url: `/pages/goods/goods?goods_id=${goodsId}` });
      }
      function goBrand(brandId) {
        uni.navigateTo({ url: `/pages/brand/brand?brand_id=${brandId}` });
      }
      function goOpsItem(op) {
        if (!op)
          return;
        if (op.goods_id && Number(op.goods_id) > 0) {
          goGoods(op.goods_id);
          return;
        }
        if (op.brand_id && Number(op.brand_id) > 0) {
          goBrand(op.brand_id);
          return;
        }
        if (op.op_type === 1 && op.target_id && Number(op.target_id) > 0) {
          goBrand(op.target_id);
          return;
        }
      }
      function goSearchPage() {
        const tabs = "1,2,3,4";
        const mode = "jump";
        uni.navigateTo({
          url: `/pages/search/search?mode=${mode}&tabs=${tabs}`
        });
      }
      async function handlePullDownRefresh() {
        if (isRefreshing.value)
          return;
        isRefreshing.value = true;
        try {
          uni.showNavigationBarLoading();
          await refreshAll(false);
          if (timeline.value.length > 0 && visibleCount.value === 0) {
            resetTimelineRevealAndObserve();
            startStaggerReveal();
          }
        } catch (e2) {
        } finally {
          uni.stopPullDownRefresh();
          uni.hideNavigationBarLoading();
          isRefreshing.value = false;
        }
      }
      onPullDownRefresh(handlePullDownRefresh);
      vue.onMounted(async () => {
        await ensureAuth();
        await refreshAll(true);
        initTlObserver();
        initSectionInViewObservers();
      });
      onShow(() => {
        try {
          const last = uni.getStorageSync(STORAGE_DATE_KEY);
          const now = todayKey();
          if (last && last !== now) {
            refreshAll(false).then(() => {
              if (timeline.value.length > 0) {
                resetTimelineRevealAndObserve();
                startStaggerReveal();
              }
            });
          }
        } catch (_2) {
        }
      });
      vue.watch(hotMode, (m2) => {
        if (m2 === "7days" && hot7List.value.length === 0) {
          fetchHot7Days();
        }
      });
      vue.onBeforeUnmount(() => {
        stopStaggerReveal();
        destroyTlObserver();
        destroySectionInViewObservers();
      });
      const __returned__ = { todayList, todayLoading, hotList, hot7List, hotMode, hotLoading, isRefreshing, themeGroups, themeLoading, activeThemeKey, likeBrands, likesLoading, hasToken, timeline, timelineLoading, opsList, opsLoading, visibleCount, staggerStarted, get revealTimer() {
        return revealTimer;
      }, set revealTimer(v2) {
        revealTimer = v2;
      }, STEP_MS, instance, get tlObserver() {
        return tlObserver;
      }, set tlObserver(v2) {
        tlObserver = v2;
      }, startStaggerReveal, stopStaggerReveal, initTlObserver, destroyTlObserver, resetTimelineRevealAndObserve, forceRevealIfStuck, waitingList, waitingLoading, normalizePlainGoods, fetchWaitingSale, sectionMap, secInView, lastRatios, activeSection, get secObservers() {
        return secObservers;
      }, set secObservers(v2) {
        secObservers = v2;
      }, get activeSetTimer() {
        return activeSetTimer;
      }, set activeSetTimer(v2) {
        activeSetTimer = v2;
      }, pickActiveByMaxRatio, schedulePickActive, initSectionInViewObservers, destroySectionInViewObservers, readToken, refreshAuthFlag, ensureAuth, getAuthHeader, STORAGE_DATE_KEY, todayKey, refreshAll, onTapSummary, onTapCalendar, normalizeGoodsList, fetchTodaySales, fetchHotToday, fetchHot7Days, fetchThemes, fetchLikes, fetchTimeline, fetchOpsFeedRecent, themeChips, filteredThemeItems, onSearchSubmit, sizeText, sizeChips, go2recentFeed, goOpsFeed, goCalendar, deriveTime, fmtHM, fmtMD, formatViews, goGoods, goBrand, goOpsItem, goSearchPage, handlePullDownRefresh, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, onBeforeUnmount: vue.onBeforeUnmount, getCurrentInstance: vue.getCurrentInstance, watch: vue.watch, nextTick: vue.nextTick, get onShow() {
        return onShow;
      }, get onPullDownRefresh() {
        return onPullDownRefresh;
      }, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1f(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "summary-wrap" }, [
      vue.createElementVNode("view", { class: "header" }, [
        vue.createElementVNode("view", {
          class: "top-tabs",
          onClick: _cache[0] || (_cache[0] = vue.withModifiers(() => {
          }, ["stop"]))
        }, [
          vue.createElementVNode("view", {
            class: "t-tab active",
            onClick: $setup.onTapSummary
          }, [
            vue.createElementVNode("text", null, "摘要"),
            vue.createElementVNode("view", { class: "underline" })
          ]),
          vue.createElementVNode("view", {
            class: "t-tab",
            onClick: $setup.onTapCalendar
          }, [
            vue.createElementVNode("text", null, "日历")
          ])
        ]),
        vue.createElementVNode("view", { class: "search-box" }, [
          vue.createElementVNode("view", {
            class: "fake-search",
            onClick: $setup.goSearchPage
          }, [
            vue.createElementVNode("image", {
              class: "icon",
              src: _imports_0$g,
              mode: "widthFix"
            }),
            vue.createElementVNode("text", { class: "placeholder" }, "搜索娃物 / 店铺 / 妆师 / 毛娘"),
            vue.createElementVNode("view", { class: "action" }, "搜 索")
          ])
        ])
      ]),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-today", [{ "is-inview": $setup.secInView.today }, { "is-active": $setup.activeSection === "today" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "今日"),
            vue.createElementVNode("view", { class: "sub sub-row" }, [
              $setup.todayLoading ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "loading-mini"
              }, "加载中…")) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("text", {
                class: "link-calendar",
                onClick: $setup.goCalendar
              }, "查看全部")
            ])
          ]),
          $setup.todayList.length === 0 && !$setup.todayLoading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              src: _imports_1$4,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", null, "今天暂无开售")
          ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
            key: 1,
            "scroll-x": "",
            class: "theme-scroll",
            "show-scrollbar": false
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.todayList, (it2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  class: "goods-mini",
                  key: "today-" + it2.goodsId + "-" + it2.recordId,
                  onClick: ($event) => $setup.goGoods(it2.goodsId, it2.recordId)
                }, [
                  vue.createElementVNode("image", {
                    src: it2.cover,
                    mode: "aspectFill"
                  }, null, 8, ["src"]),
                  it2.type ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "type-badge"
                    },
                    vue.toDisplayString(it2.type),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode(
                    "view",
                    { class: "gname ellipsis-2" },
                    vue.toDisplayString(it2.goods_name),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("scroll-view", {
                    class: "meta",
                    "scroll-y": "true"
                  }, [
                    it2.sizeText ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "size"
                      },
                      vue.toDisplayString(it2.sizeText),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ])
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]))
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-waiting", [{ "is-inview": $setup.secInView.waiting }, { "is-active": $setup.activeSection === "waiting" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "蹲开售"),
            vue.createElementVNode("view", { class: "sub sub-row" }, [
              $setup.waitingLoading ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "loading-mini"
              }, "加载中…")) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          $setup.waitingList.length === 0 && !$setup.waitingLoading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              src: _imports_1$4,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", null, "暂无等待贩售的商品")
          ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
            key: 1,
            "scroll-x": "",
            class: "theme-scroll",
            "show-scrollbar": false
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.waitingList, (it2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  class: "goods-mini",
                  key: "wait-" + it2.goodsId,
                  onClick: ($event) => $setup.goGoods(it2.goodsId)
                }, [
                  vue.createElementVNode("image", {
                    src: it2.cover,
                    mode: "aspectFill"
                  }, null, 8, ["src"]),
                  it2.type ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "type-badge"
                    },
                    vue.toDisplayString(it2.type),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode(
                    "view",
                    { class: "gname ellipsis-2" },
                    vue.toDisplayString(it2.goods_name),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("scroll-view", {
                    class: "meta",
                    "scroll-y": "true"
                  }, [
                    it2.sizeText ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "size"
                      },
                      vue.toDisplayString(it2.sizeText),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ])
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]))
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-hot", [{ "is-inview": $setup.secInView.hot }, { "is-active": $setup.activeSection === "hot" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "热榜"),
            vue.createElementVNode("view", { class: "sub sub-row" }, [
              vue.createElementVNode(
                "text",
                {
                  class: vue.normalizeClass(["tab-mini", { active: $setup.hotMode === "today" }]),
                  onClick: _cache[1] || (_cache[1] = ($event) => $setup.hotMode = "today")
                },
                "今日",
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "text",
                {
                  class: vue.normalizeClass(["tab-mini", { active: $setup.hotMode === "7days" }]),
                  onClick: _cache[2] || (_cache[2] = ($event) => $setup.hotMode = "7days")
                },
                "近7日",
                2
                /* CLASS */
              ),
              $setup.hotLoading ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "loading-mini"
              }, "加载中…")) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("text", {
                class: "link-calendar",
                onClick: $setup.goCalendar
              }, "查看日历")
            ])
          ]),
          $setup.hotMode === "today" ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
            $setup.hotList.length === 0 && !$setup.hotLoading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "empty"
            }, [
              vue.createElementVNode("image", {
                src: _imports_1$4,
                mode: "aspectFit"
              }),
              vue.createElementVNode("text", null, "今天还没有上榜商品")
            ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
              key: 1,
              "scroll-x": "",
              class: "hot-scroll",
              "show-scrollbar": false
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.hotList, (it2, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "hot-card",
                    key: it2.goodsId + "-" + idx,
                    onClick: ($event) => $setup.goGoods(it2.goodsId, it2.recordId)
                  }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["rank", "r" + (idx + 1)])
                      },
                      vue.toDisplayString(idx + 1),
                      3
                      /* TEXT, CLASS */
                    ),
                    vue.createElementVNode("image", {
                      class: "cover",
                      src: it2.cover,
                      mode: "aspectFill"
                    }, null, 8, ["src"]),
                    vue.createElementVNode("view", { class: "info" }, [
                      vue.createElementVNode(
                        "view",
                        { class: "brand-tag" },
                        vue.toDisplayString(it2.brand_name),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "view",
                        { class: "name ellipsis-2" },
                        vue.toDisplayString(it2.goods_name),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("view", { class: "meta" }, [
                        vue.createElementVNode("view", { class: "hotness" }, [
                          vue.createVNode(_component_uni_icons, {
                            type: "fire",
                            size: "22",
                            color: "#ff5a6e"
                          }),
                          it2.views ? (vue.openBlock(), vue.createElementBlock(
                            "text",
                            {
                              key: 0,
                              class: "views"
                            },
                            vue.toDisplayString($setup.formatViews(it2.views)),
                            1
                            /* TEXT */
                          )) : vue.createCommentVNode("v-if", true)
                        ])
                      ])
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]))
          ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
            $setup.hot7List.length === 0 && !$setup.hotLoading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "empty"
            }, [
              vue.createElementVNode("image", {
                src: _imports_1$4,
                mode: "aspectFit"
              }),
              vue.createElementVNode("text", null, "最近7日暂无上榜商品")
            ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
              key: 1,
              "scroll-x": "",
              class: "hot-scroll",
              "show-scrollbar": false
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.hot7List, (it2, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "hot-card",
                    key: it2.goodsId + "-" + idx,
                    onClick: ($event) => $setup.goGoods(it2.goodsId, it2.recordId)
                  }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["rank", "r" + (idx + 1)])
                      },
                      vue.toDisplayString(idx + 1),
                      3
                      /* TEXT, CLASS */
                    ),
                    vue.createElementVNode("image", {
                      class: "cover",
                      src: it2.cover,
                      mode: "aspectFill"
                    }, null, 8, ["src"]),
                    vue.createElementVNode("view", { class: "info" }, [
                      vue.createElementVNode(
                        "view",
                        { class: "brand-tag" },
                        vue.toDisplayString(it2.brand_name),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "view",
                        { class: "name ellipsis-2" },
                        vue.toDisplayString(it2.goods_name),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("view", { class: "meta" }, [
                        vue.createElementVNode("view", { class: "hotness" }, [
                          vue.createVNode(_component_uni_icons, {
                            type: "fire",
                            size: "22",
                            color: "#ff5a6e"
                          }),
                          it2.views ? (vue.openBlock(), vue.createElementBlock(
                            "text",
                            {
                              key: 0,
                              class: "views"
                            },
                            vue.toDisplayString($setup.formatViews(it2.views)),
                            1
                            /* TEXT */
                          )) : vue.createCommentVNode("v-if", true)
                        ])
                      ])
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]))
          ]))
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-themes", [{ "is-inview": $setup.secInView.themes }, { "is-active": $setup.activeSection === "themes" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "主题合集"),
            $setup.themeChips.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
              key: 0,
              "scroll-x": "",
              class: "chips-scroll",
              "show-scrollbar": false
            }, [
              vue.createElementVNode("view", { class: "chips" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.themeChips, (c2) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: c2.key,
                      class: vue.normalizeClass(["chip", { active: $setup.activeThemeKey === c2.key }]),
                      onClick: ($event) => $setup.activeThemeKey = c2.key
                    }, vue.toDisplayString(c2.title), 11, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          vue.createElementVNode("view", { class: "themes" }, [
            $setup.filteredThemeItems.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
              key: 0,
              "scroll-x": "",
              class: "theme-scroll",
              "show-scrollbar": false
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.filteredThemeItems, (it2) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "goods-mini",
                    key: it2.key,
                    onClick: ($event) => $setup.goGoods(it2.goodsId, it2.recordId)
                  }, [
                    vue.createElementVNode("image", {
                      src: it2.cover,
                      mode: "aspectFill"
                    }, null, 8, ["src"]),
                    it2.type ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "type-badge"
                      },
                      vue.toDisplayString(it2.type),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode(
                      "view",
                      { class: "gname ellipsis-2" },
                      vue.toDisplayString(it2.goods_name),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("scroll-view", {
                      class: "meta",
                      "scroll-y": "true"
                    }, [
                      it2.sizeText ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 0,
                          class: "size"
                        },
                        vue.toDisplayString(it2.sizeText),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : !$setup.themeLoading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "empty-slim"
            }, "该主题暂无在售")) : vue.createCommentVNode("v-if", true)
          ])
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-ops", [{ "is-inview": $setup.secInView.ops }, { "is-active": $setup.activeSection === "ops" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "最近入库"),
            vue.createElementVNode("view", {
              class: "sub",
              style: { "display": "flex", "gap": "12rpx", "align-items": "center" }
            }, [
              $setup.opsLoading ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中…")) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("text", {
                class: "link-more",
                onClick: $setup.go2recentFeed
              }, "查看更多")
            ])
          ]),
          $setup.opsList.length === 0 && !$setup.opsLoading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              src: _imports_1$4,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", null, "最近暂无入库动态")
          ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
            key: 1,
            "scroll-x": "",
            class: "hot-scroll",
            "show-scrollbar": false
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.opsList, (op, idx) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  class: "hot-card",
                  key: op.id + "-" + idx,
                  onClick: ($event) => $setup.goOpsItem(op)
                }, [
                  vue.createElementVNode("image", {
                    class: "cover",
                    src: op.image_url,
                    mode: "aspectFill"
                  }, null, 8, ["src"]),
                  vue.createElementVNode("view", { class: "info" }, [
                    vue.createElementVNode(
                      "view",
                      { class: "brand-tag" },
                      vue.toDisplayString(op.brand_name),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "view",
                      { class: "name ellipsis-2" },
                      vue.toDisplayString(op.summary),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("view", { class: "meta" }, [
                      vue.createElementVNode(
                        "text",
                        { style: { "color": "#9aa4b2", "font-size": "22rpx" } },
                        vue.toDisplayString(op.time_ago),
                        1
                        /* TEXT */
                      )
                    ])
                  ])
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]))
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-likes", [{ "is-inview": $setup.secInView.likes }, { "is-active": $setup.activeSection === "likes" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "订阅品牌"),
            $setup.likesLoading ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 0,
              class: "sub"
            }, "加载中…")) : vue.createCommentVNode("v-if", true)
          ]),
          !$setup.hasToken ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "login-tip"
          }, [
            vue.createElementVNode("text", null, "登录后可查看你的订阅品牌")
          ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
            key: 1,
            class: "brand-scroll",
            "scroll-x": "",
            "show-scrollbar": false
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.likeBrands, (it2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  class: "brand-item",
                  key: it2.id,
                  onClick: ($event) => $setup.goBrand(it2.brand.id)
                }, [
                  vue.createElementVNode("image", {
                    src: it2.brand.logo_image,
                    mode: "aspectFill",
                    class: "brand-logo"
                  }, null, 8, ["src"]),
                  vue.createElementVNode(
                    "text",
                    { class: "brand-name ellipsis-1" },
                    vue.toDisplayString(it2.brand.brand_name),
                    1
                    /* TEXT */
                  )
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.likeBrands.length === 0 && !$setup.likesLoading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "brand-empty"
            }, "还没有订阅品牌~")) : vue.createCommentVNode("v-if", true)
          ]))
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["section s-timeline", [{ "is-inview": $setup.secInView.timeline }, { "is-active": $setup.activeSection === "timeline" }]])
        },
        [
          vue.createElementVNode("view", { class: "section-hd" }, [
            vue.createElementVNode("text", { class: "title" }, "即将上新"),
            vue.createElementVNode("view", { class: "sub sub-row" }, [
              $setup.timelineLoading ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "loading-mini"
              }, "加载中…")) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("text", {
                class: "link-calendar",
                onClick: $setup.goCalendar
              }, "查看日历")
            ])
          ]),
          $setup.timeline.length === 0 && !$setup.timelineLoading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              src: _imports_1$4,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", null, "暂无即将开始的上新")
          ])) : (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "timeline"
          }, [
            vue.createElementVNode("view", { class: "timeline-connector" }),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.timeline, (r2, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  class: vue.normalizeClass(["tl-item", { "is-visible": index < $setup.visibleCount }]),
                  key: r2.key,
                  onClick: ($event) => $setup.goGoods(r2.goodsId, r2.recordId)
                }, [
                  vue.createElementVNode("view", { class: "tl-left" }, [
                    vue.createElementVNode(
                      "view",
                      { class: "tl-time" },
                      vue.toDisplayString($setup.fmtHM(r2.time)),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "view",
                      { class: "tl-date" },
                      vue.toDisplayString($setup.fmtMD(r2.time)),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "tl-middle" }, [
                    vue.createElementVNode("view", { class: "dot" })
                  ]),
                  vue.createElementVNode("view", { class: "tl-card" }, [
                    vue.createElementVNode("view", { class: "tl-card-grid" }, [
                      vue.createElementVNode("image", {
                        src: r2.cover,
                        mode: "aspectFill",
                        class: "thumb"
                      }, null, 8, ["src"]),
                      vue.createElementVNode("view", { class: "tl-info" }, [
                        vue.createElementVNode("view", { class: "row-1" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "brand-tag" },
                            vue.toDisplayString(r2.brand_name),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "gname ellipsis-1" },
                            vue.toDisplayString(r2.goods_name),
                            1
                            /* TEXT */
                          )
                        ]),
                        vue.createElementVNode("view", { class: "row-2" }, [
                          r2.type ? (vue.openBlock(), vue.createElementBlock(
                            "text",
                            {
                              key: 0,
                              class: "type-badge"
                            },
                            vue.toDisplayString(r2.type),
                            1
                            /* TEXT */
                          )) : vue.createCommentVNode("v-if", true),
                          vue.createElementVNode("view", { class: "hotness" }, [
                            vue.createVNode(_component_uni_icons, {
                              type: "fire",
                              size: "22",
                              color: "#ff5a6e"
                            }),
                            r2.views ? (vue.openBlock(), vue.createElementBlock(
                              "text",
                              {
                                key: 0,
                                class: "views"
                              },
                              vue.toDisplayString($setup.formatViews(r2.views)),
                              1
                              /* TEXT */
                            )) : vue.createCommentVNode("v-if", true)
                          ])
                        ]),
                        r2.sizeChips && r2.sizeChips.length ? (vue.openBlock(), vue.createElementBlock("view", {
                          key: 0,
                          class: "sizes"
                        }, [
                          (vue.openBlock(true), vue.createElementBlock(
                            vue.Fragment,
                            null,
                            vue.renderList(r2.sizeChips, (s2, i2) => {
                              return vue.openBlock(), vue.createElementBlock(
                                "text",
                                {
                                  class: "size-chip",
                                  key: i2
                                },
                                vue.toDisplayString(s2),
                                1
                                /* TEXT */
                              );
                            }),
                            128
                            /* KEYED_FRAGMENT */
                          ))
                        ])) : vue.createCommentVNode("v-if", true)
                      ])
                    ])
                  ])
                ], 10, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]))
        ],
        2
        /* CLASS */
      )
    ]);
  }
  const __easycom_1$6 = /* @__PURE__ */ _export_sfc(_sfc_main$1g, [["render", _sfc_render$1f], ["__scopeId", "data-v-1c5472c3"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/summary-tab/summary-tab.vue"]]);
  const _sfc_main$1f = {
    __name: "summary",
    setup(__props, { expose: __expose }) {
      __expose();
      const active = vue.ref("summary");
      const __returned__ = { active, ref: vue.ref };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1e(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_summary_tab = resolveEasycom(vue.resolveDynamicComponent("summary-tab"), __easycom_1$6);
    const _component_common_page = resolveEasycom(vue.resolveDynamicComponent("common-page"), __easycom_2$7);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createVNode(_component_common_page, { head_color: "#def9ff" }, {
          default: vue.withCtx(() => [
            $setup.active === "summary" ? (vue.openBlock(), vue.createBlock(_component_summary_tab, { key: 0 })) : vue.createCommentVNode("v-if", true)
          ]),
          _: 1
          /* STABLE */
        })
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSummarySummary = /* @__PURE__ */ _export_sfc(_sfc_main$1f, [["render", _sfc_render$1e], ["__scopeId", "data-v-43873f68"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/summary/summary.vue"]]);
  const systemInfo$1 = uni.getSystemInfoSync();
  function parseUnit(size) {
    if (typeof size == "number" || !isNaN(Number(size))) {
      return uni.upx2px(size);
    } else if (typeof size === "string") {
      if (size.endsWith("rpx")) {
        return parseUnit(size.replace("rpx", ""));
      } else if (size.endsWith("px")) {
        return Number(size.replace("px", ""));
      } else if (size.endsWith("vw")) {
        return Number(size.replace("vw", "")) * systemInfo$1.screenWidth / 100;
      } else if (size.endsWith("vh")) {
        return Number(size.replace("vh", "")) * systemInfo$1.screenHeight / 100;
      }
    }
    return 0;
  }
  const block0$1 = (Comp) => {
    (Comp.$wxs || (Comp.$wxs = [])).push("wxsModule");
    (Comp.$wxsModules || (Comp.$wxsModules = {}))["wxsModule"] = "8ab5efd6";
  };
  const _sfc_main$1e = {
    name: "bt-cropper",
    props: {
      // 图片路径，支持网络路径和本地路径
      imageSrc: {
        type: String,
        default: "",
        required: true
      },
      mask: {
        type: String,
        default: ""
      },
      // 手动指定容器的大小
      containerSize: {
        type: Object,
        default: null
      },
      // 输出图片的格式，默认jpg
      fileType: {
        type: String,
        default: "png"
      },
      // 生成的图片的宽度,不传或者传0表示按照原始分辨率裁切
      dWidth: Number,
      maxWidth: {
        type: Number,
        default: 2e3
      },
      // 裁切比例，0表示自由
      ratio: {
        type: Number,
        default: 0,
        validator(value) {
          if (typeof value === "number") {
            if (value < 0) {
              return false;
            }
          } else {
            return false;
          }
          return true;
        }
      },
      // 旋转角度
      rotate: Number,
      // 是否展示网格
      showGrid: {
        type: Boolean,
        default: false
      },
      // 图片质量，0-1 越大质量越好
      quality: {
        type: Number,
        default: 1
      },
      canvas2d: {
        type: Boolean,
        default: false
      },
      // 初始的图片位置
      initPosition: {
        type: Object,
        default() {
          return null;
        }
      },
      // 是否开启操作结束后自动放大
      autoZoom: {
        type: Boolean,
        default: true
      }
    },
    data() {
      return {
        canvasId: "bt-cropper",
        containerRect: null,
        imageInfo: null,
        operationHistory: [],
        operationIndex: 0,
        anim: false,
        timer: null,
        // 是否使用2D canvas
        type2d: false,
        pixel: 1,
        imageRect: null,
        cropperRect: null,
        target: {
          width: 0,
          height: 0
        }
      };
    },
    watch: {
      imageSrc: {
        handler(src) {
          if (typeof src === "string" && src !== "") {
            this.imageInit(src);
          } else {
            this.imageInfo = null;
          }
        },
        immediate: true
      },
      ratio() {
        if (this.ratio != 0) {
          this.startAnim();
          this.init();
        }
      }
    },
    computed: {
      containerStyle() {
        if (this.containerSize && this.containerRect) {
          return {
            width: this.containerRect.width + "px",
            height: this.containerRect.height + "px"
          };
        }
        return {};
      },
      rotateAngle() {
        const angel = Number(this.rotate);
        if (isNaN(angel)) {
          return 0;
        } else {
          return angel % 360;
        }
      }
    },
    methods: {
      startAnim() {
        this.stopAnim();
        this.anim = true;
        this.timer = setTimeout(() => {
          this.anim = false;
        }, 200);
      },
      stopAnim() {
        this.anim = false;
        clearTimeout(this.timer);
      },
      imageInit(src) {
        uni.showLoading({
          title: "载入中..."
        });
        uni.getImageInfo({
          src,
          success: (res) => {
            this.imageInfo = res;
            this.$nextTick(() => {
              this.getContainer().then((rect) => {
                this.containerRect = rect;
                this.init();
              });
            });
          },
          fail: (err) => {
            this.$emit("loadFail", err);
            uni.showToast({
              title: "图片下载失败!",
              icon: "none"
            });
          },
          complete(res) {
            uni.hideLoading();
          }
        });
      },
      initCropper() {
        const imageRate = this.imageInfo.width / this.imageInfo.height;
        const containerRate = this.containerRect.width / this.containerRect.height;
        const imageRect = {};
        let cropperRate = this.ratio;
        if (cropperRate == 0) {
          if (this.cropperRect) {
            cropperRate = this.cropperRect.width / this.cropperRect.height;
          } else {
            cropperRate = 1;
          }
        }
        const cropperRect = {};
        if (containerRate > cropperRate) {
          cropperRect.height = this.containerRect.height * 0.85;
          cropperRect.width = cropperRect.height * cropperRate;
        } else {
          cropperRect.width = this.containerRect.width * 0.85;
          cropperRect.height = cropperRect.width / cropperRate;
        }
        if (cropperRate > imageRate) {
          imageRect.width = cropperRect.width;
          imageRect.height = imageRect.width / imageRate;
        } else {
          imageRect.height = cropperRect.height;
          imageRect.width = imageRect.height * imageRate;
        }
        imageRect.left = (this.containerRect.width - imageRect.width) / 2;
        imageRect.top = (this.containerRect.height - imageRect.height) / 2;
        cropperRect.left = imageRect.left + (imageRect.width - cropperRect.width) / 2;
        cropperRect.top = imageRect.top + (imageRect.height - cropperRect.height) / 2;
        return {
          imageRect,
          cropperRect
        };
      },
      init() {
        const { imageRect, cropperRect } = this.initCropper();
        if (this.initPosition) {
          const scale = this.imageInfo.width / imageRect.width;
          const { left, top, width, height } = this.initPosition;
          if (left !== void 0 && top !== void 0 && width !== void 0 && height !== void 0) {
            cropperRect.width = width / scale;
            cropperRect.height = height / scale;
            cropperRect.left = left / scale;
            cropperRect.top = top / scale;
            this.$nextTick(this.zoomToFill);
          }
        }
        this.imageRect = imageRect;
        this.cropperRect = cropperRect;
        this.operationHistory = [{ imageRect, cropperRect }];
        this.operationIndex = 0;
        this.setTarget();
        this.type2d = false;
      },
      // 设置目标图像的大小
      setTarget() {
        const ratio = this.cropperRect.width / this.cropperRect.height;
        if (!!this.dWidth) {
          this.target = {
            width: this.dWidth,
            height: this.dWidth / (ratio || 1)
          };
        } else {
          const width = Math.min(this.maxWidth, this.cropperRect.width * (this.imageInfo.width / this.imageRect.width));
          this.target = {
            width,
            height: width / (ratio || 1)
          };
        }
      },
      addHistory({ imageRect, cropperRect }) {
        if (this.operationIndex !== this.operationHistory.length - 1) {
          this.operationHistory = this.operationHistory.slice(0, this.operationIndex);
        }
        this.operationHistory.push({
          imageRect,
          cropperRect
        });
        if (this.operationHistory.length > 10) {
          this.operationHistory.shift();
        }
        this.operationIndex = this.operationHistory.length - 1;
      },
      updateData(data) {
        this.imageRect = data.imageRect;
        this.cropperRect = data.cropperRect;
        this.addHistory(data);
        this.setTarget();
        if (this.autoZoom) {
          this.timer = setTimeout(() => {
            this.zoomToFill();
          }, 600);
        }
        const { imageRect, cropperRect } = data;
        const scale = imageRect.width / this.imageInfo.width;
        this.$emit("change", {
          left: (cropperRect.left - imageRect.left) / scale,
          top: (cropperRect.top - imageRect.top) / scale,
          width: cropperRect.width / scale,
          height: cropperRect.height / scale
        });
      },
      getContainer() {
        if (this.containerSize !== null && typeof this.containerSize == "object") {
          const { width, height } = this.containerSize;
          return Promise.resolve({
            width: parseUnit(width),
            height: parseUnit(height)
          });
        } else {
          return new Promise((resolve) => {
            const query = uni.createSelectorQuery().in(this);
            query.select(".mainContent").boundingClientRect((rect) => {
              resolve(rect);
            }).exec();
          });
        }
      },
      zoomToFill() {
        this.startAnim();
        const beforeCropper = {
          ...this.cropperRect
        };
        ({
          imageRect: this.imageRect,
          cropperRect: this.cropperRect
        });
        this.cropperRect = this.initCropper().cropperRect;
        const scale = this.cropperRect.width / beforeCropper.width;
        const ox = beforeCropper.left - this.imageRect.left;
        const oy = beforeCropper.top - this.imageRect.top;
        this.imageRect = {
          width: this.imageRect.width * scale,
          height: this.imageRect.height * scale,
          left: this.imageRect.left + (this.cropperRect.left - beforeCropper.left) - (scale - 1) * ox,
          top: this.imageRect.top + (this.cropperRect.top - beforeCropper.top) - (scale - 1) * oy
        };
      },
      onTouchStart() {
        this.stopAnim();
      },
      // 撤销
      undo() {
        if (this.operationIndex > 0) {
          this.operationIndex--;
          this.imageRect = this.operationHistory[this.operationIndex].imageRect;
          this.cropperRect = this.operationHistory[this.operationIndex].cropperRect;
          return true;
        }
        return false;
      },
      // 重做
      resume() {
        if (this.operationIndex < this.operationHistory.length - 1) {
          this.operationIndex++;
          this.imageRect = this.operationHistory[this.operationIndex].imageRect;
          this.cropperRect = this.operationHistory[this.operationIndex].cropperRect;
          return true;
        }
        return false;
      },
      async drawImage(ctx, image, x, y2, w2, h2) {
        if (this.type2d) {
          await new Promise((resolve) => image.onload = resolve);
          ctx.drawImage(image, x * this.pixel, y2 * this.pixel, w2 * this.pixel, h2 * this.pixel);
        } else {
          const path = await new Promise((resolve) => {
            uni.getImageInfo({
              src: image,
              success({ path: path2 }) {
                resolve(path2);
              }
            });
          });
          ctx.drawImage(path, x * this.pixel, y2 * this.pixel, w2 * this.pixel, h2 * this.pixel);
          await new Promise((resolve) => ctx.draw(false, resolve));
        }
      },
      async crop() {
        let ctx;
        let canvas;
        this.$emit("cropStart");
        this.setTarget();
        if (this.type2d) {
          const query = uni.createSelectorQuery().in(this);
          canvas = await new Promise(
            (resolve) => query.select(".bt-canvas").node(({ node: node2 }) => resolve(node2)).exec()
          );
          canvas.width = this.target.width * this.pixel;
          canvas.height = this.target.height * this.pixel;
          ctx = canvas.getContext("2d");
        } else {
          ctx = uni.createCanvasContext(this.canvasId, this);
        }
        const scale = this.cropperRect.width / this.target.width;
        const dx = (this.cropperRect.left - this.imageRect.left) / scale;
        const dy = (this.cropperRect.top - this.imageRect.top) / scale;
        let image;
        if (this.type2d) {
          image = canvas.createImage();
          image.src = this.imageSrc;
        } else {
          image = this.imageSrc;
        }
        const x = -dx, y2 = -dy, w2 = this.imageRect.width / scale, h2 = this.imageRect.height / scale;
        ctx.save();
        ctx.translate(x + w2 / 2, y2 + h2 / 2);
        ctx.rotate(this.rotateAngle * Math.PI / 180);
        ctx.translate(-(x + w2 / 2), -(y2 + h2 / 2));
        await this.drawImage(ctx, image, x, y2, w2, h2);
        ctx.restore();
        if (this.mask !== "") {
          let imageData;
          if (this.type2d) {
            imageData = ctx.getImageData(0, 0, this.target.width, this.target.height);
          } else {
            imageData = await new Promise((resolve) => {
              uni.canvasGetImageData({
                canvasId: this.canvasId,
                x: 0,
                y: 0,
                width: this.target.width,
                height: this.target.height,
                success(res) {
                  resolve(res);
                }
              }, this);
            });
          }
          ctx.clearRect(0, 0, this.target.width, this.target.height);
          if (this.type2d) {
            image.src = this.mask;
          } else {
            image = this.mask;
          }
          await this.drawImage(ctx, image, 0, 0, this.target.width, this.target.height);
          let maskData;
          if (this.type2d) {
            maskData = ctx.getImageData(0, 0, this.target.width, this.target.height);
          } else {
            maskData = await new Promise((resolve, reject) => {
              uni.canvasGetImageData({
                canvasId: this.canvasId,
                x: 0,
                y: 0,
                width: this.target.width,
                height: this.target.height,
                success(res) {
                  resolve(res);
                }
              }, this);
            });
          }
          ctx.clearRect(0, 0, this.target.width, this.target.height);
          for (let index = 3; index < maskData.data.length; index += 4) {
            const alpha = maskData.data[index];
            if (alpha !== 0) {
              imageData.data[index] = 0;
            }
          }
          if (this.type2d) {
            ctx.putImageData(imageData, 0, 0);
          } else {
            await new Promise((resolve) => {
              uni.canvasPutImageData({
                canvasId: this.canvasId,
                x: 0,
                y: 0,
                width: imageData.width,
                height: imageData.height,
                data: imageData.data,
                complete: (res) => {
                  resolve(res);
                }
              }, this);
            });
          }
        }
        return new Promise((resolve, reject) => {
          const params = {};
          if (this.type2d) {
            params.canvas = canvas;
          } else {
            params.canvasId = this.canvasId;
          }
          uni.canvasToTempFilePath({
            ...params,
            destWidth: this.target.width,
            destHeight: this.target.height,
            quality: Number(this.quality) || 1,
            fileType: this.fileType,
            success: ({ tempFilePath }) => {
              resolve(tempFilePath);
            },
            fail(err) {
              formatAppLog("log", "at uni_modules/bt-cropper/components/bt-cropper/bt-cropper.vue:593", "保存失败，错误信息：", err);
              reject(err);
            }
          }, this);
        });
      }
    }
  };
  function _sfc_render$1d(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: "bt-container",
        style: vue.normalizeStyle([$options.containerStyle])
      },
      [
        vue.createElementVNode(
          "view",
          {
            class: "mainContent",
            "data-type": "image",
            onTouchstart: _cache[8] || (_cache[8] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
            onTouchmove: _cache[9] || (_cache[9] = (...args) => _ctx.wxsModule.touchMove && _ctx.wxsModule.touchMove(...args)),
            onTouchend: _cache[10] || (_cache[10] = (...args) => _ctx.wxsModule.touchEnd && _ctx.wxsModule.touchEnd(...args))
          },
          [
            $data.imageRect && $data.cropperRect ? (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              { key: 0 },
              [
                vue.createElementVNode("image", {
                  mode: "aspectFit",
                  src: $props.imageSrc,
                  class: vue.normalizeClass(["image", { anim: $data.anim }]),
                  rotateAngle: vue.wp($options.rotateAngle),
                  "change:rotateAngle": _ctx.wxsModule.changeRotateAngle,
                  "change:imageRect": _ctx.wxsModule.changeImageRect,
                  imageRect: vue.wp($data.imageRect)
                }, null, 10, ["src", "rotateAngle", "change:rotateAngle", "change:imageRect", "imageRect"]),
                vue.createElementVNode("view", {
                  class: vue.normalizeClass(["cropper", { anim: $data.anim }]),
                  "change:cropperRect": _ctx.wxsModule.changeCropper,
                  cropperRect: vue.wp($data.cropperRect),
                  "change:ratio": _ctx.wxsModule.changeRatio,
                  ratio: vue.wp($props.ratio)
                }, [
                  vue.createElementVNode("image", {
                    class: "mask",
                    src: $props.mask
                  }, null, 8, ["src"]),
                  $props.showGrid ? (vue.openBlock(), vue.createElementBlock(
                    vue.Fragment,
                    { key: 0 },
                    [
                      vue.createElementVNode("view", { class: "line row row1" }),
                      vue.createElementVNode("view", { class: "line row row2" }),
                      vue.createElementVNode("view", { class: "line col col1" }),
                      vue.createElementVNode("view", { class: "line col col2" })
                    ],
                    64
                    /* STABLE_FRAGMENT */
                  )) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller vertical left",
                      onTouchstart: _cache[0] || (_cache[0] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller vertical right",
                      onTouchstart: _cache[1] || (_cache[1] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller horizon top",
                      onTouchstart: _cache[2] || (_cache[2] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller horizon bottom",
                      onTouchstart: _cache[3] || (_cache[3] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller left top",
                      onTouchstart: _cache[4] || (_cache[4] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller left bottom",
                      onTouchstart: _cache[5] || (_cache[5] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller right top",
                      onTouchstart: _cache[6] || (_cache[6] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "controller right bottom",
                      onTouchstart: _cache[7] || (_cache[7] = (...args) => _ctx.wxsModule.touchStart && _ctx.wxsModule.touchStart(...args)),
                      "data-type": "controller"
                    },
                    null,
                    32
                    /* NEED_HYDRATION */
                  )
                ], 10, ["change:cropperRect", "cropperRect", "change:ratio", "ratio"])
              ],
              64
              /* STABLE_FRAGMENT */
            )) : vue.createCommentVNode("v-if", true)
          ],
          32
          /* NEED_HYDRATION */
        ),
        $data.type2d ? (vue.openBlock(), vue.createElementBlock("canvas", {
          key: 0,
          type: "2d",
          class: "bt-canvas",
          width: $data.target.width,
          height: $data.target.height
        }, null, 8, ["width", "height"])) : (vue.openBlock(), vue.createElementBlock("canvas", {
          key: 1,
          "canvas-id": $data.canvasId,
          class: "bt-canvas",
          style: vue.normalizeStyle({
            width: $data.target.width + "px",
            height: $data.target.height + "px"
          }),
          width: $data.target.width * $data.pixel,
          height: $data.target.height * $data.pixel
        }, null, 12, ["canvas-id", "width", "height"]))
      ],
      4
      /* STYLE */
    );
  }
  if (typeof block0$1 === "function")
    block0$1(_sfc_main$1e);
  const __easycom_0$4 = /* @__PURE__ */ _export_sfc(_sfc_main$1e, [["render", _sfc_render$1d], ["__scopeId", "data-v-51ff46c2"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/bt-cropper/components/bt-cropper/bt-cropper.vue"]]);
  const _sfc_main$1d = {
    name: "uniPopup",
    components: {},
    emits: ["change", "maskClick"],
    props: {
      // 开启动画
      animation: {
        type: Boolean,
        default: true
      },
      // 弹出层类型，可选值，top: 顶部弹出层；bottom：底部弹出层；center：全屏弹出层
      // message: 消息提示 ; dialog : 对话框
      type: {
        type: String,
        default: "center"
      },
      // maskClick
      isMaskClick: {
        type: Boolean,
        default: null
      },
      // TODO 2 个版本后废弃属性 ，使用 isMaskClick
      maskClick: {
        type: Boolean,
        default: null
      },
      backgroundColor: {
        type: String,
        default: "none"
      },
      safeArea: {
        type: Boolean,
        default: true
      },
      maskBackgroundColor: {
        type: String,
        default: "rgba(0, 0, 0, 0.4)"
      },
      borderRadius: {
        type: String
      }
    },
    watch: {
      /**
       * 监听type类型
       */
      type: {
        handler: function(type) {
          if (!this.config[type])
            return;
          this[this.config[type]](true);
        },
        immediate: true
      },
      isDesktop: {
        handler: function(newVal) {
          if (!this.config[newVal])
            return;
          this[this.config[this.type]](true);
        },
        immediate: true
      },
      /**
       * 监听遮罩是否可点击
       * @param {Object} val
       */
      maskClick: {
        handler: function(val) {
          this.mkclick = val;
        },
        immediate: true
      },
      isMaskClick: {
        handler: function(val) {
          this.mkclick = val;
        },
        immediate: true
      },
      // H5 下禁止底部滚动
      showPopup(show) {
      }
    },
    data() {
      return {
        duration: 300,
        ani: [],
        showPopup: false,
        showTrans: false,
        popupWidth: 0,
        popupHeight: 0,
        config: {
          top: "top",
          bottom: "bottom",
          center: "center",
          left: "left",
          right: "right",
          message: "top",
          dialog: "center",
          share: "bottom"
        },
        maskClass: {
          position: "fixed",
          bottom: 0,
          top: 0,
          left: 0,
          right: 0,
          backgroundColor: "rgba(0, 0, 0, 0.4)"
        },
        transClass: {
          backgroundColor: "transparent",
          borderRadius: this.borderRadius || "0",
          position: "fixed",
          left: 0,
          right: 0
        },
        maskShow: true,
        mkclick: true,
        popupstyle: "top"
      };
    },
    computed: {
      getStyles() {
        let res = { backgroundColor: this.bg };
        if (this.borderRadius || "0") {
          res = Object.assign(res, { borderRadius: this.borderRadius });
        }
        return res;
      },
      isDesktop() {
        return this.popupWidth >= 500 && this.popupHeight >= 500;
      },
      bg() {
        if (this.backgroundColor === "" || this.backgroundColor === "none") {
          return "transparent";
        }
        return this.backgroundColor;
      }
    },
    mounted() {
      const fixSize = () => {
        const {
          windowWidth: windowWidth2,
          windowHeight,
          windowTop,
          safeArea: safeArea2,
          screenHeight,
          safeAreaInsets
        } = uni.getSystemInfoSync();
        this.popupWidth = windowWidth2;
        this.popupHeight = windowHeight + (windowTop || 0);
        if (safeArea2 && this.safeArea) {
          this.safeAreaInsets = safeAreaInsets.bottom;
        } else {
          this.safeAreaInsets = 0;
        }
      };
      fixSize();
    },
    // TODO vue3
    unmounted() {
      this.setH5Visible();
    },
    activated() {
      this.setH5Visible(!this.showPopup);
    },
    deactivated() {
      this.setH5Visible(true);
    },
    created() {
      if (this.isMaskClick === null && this.maskClick === null) {
        this.mkclick = true;
      } else {
        this.mkclick = this.isMaskClick !== null ? this.isMaskClick : this.maskClick;
      }
      if (this.animation) {
        this.duration = 300;
      } else {
        this.duration = 0;
      }
      this.messageChild = null;
      this.clearPropagation = false;
      this.maskClass.backgroundColor = this.maskBackgroundColor;
    },
    methods: {
      setH5Visible(visible = true) {
      },
      /**
       * 公用方法，不显示遮罩层
       */
      closeMask() {
        this.maskShow = false;
      },
      /**
       * 公用方法，遮罩层禁止点击
       */
      disableMask() {
        this.mkclick = false;
      },
      // TODO nvue 取消冒泡
      clear(e2) {
        e2.stopPropagation();
        this.clearPropagation = true;
      },
      open(direction) {
        if (this.showPopup) {
          return;
        }
        let innerType = ["top", "center", "bottom", "left", "right", "message", "dialog", "share"];
        if (!(direction && innerType.indexOf(direction) !== -1)) {
          direction = this.type;
        }
        if (!this.config[direction]) {
          formatAppLog("error", "at uni_modules/uni-popup/components/uni-popup/uni-popup.vue:310", "缺少类型：", direction);
          return;
        }
        this[this.config[direction]]();
        this.$emit("change", {
          show: true,
          type: direction
        });
      },
      close(type) {
        this.showTrans = false;
        this.$emit("change", {
          show: false,
          type: this.type
        });
        clearTimeout(this.timer);
        this.timer = setTimeout(() => {
          this.showPopup = false;
        }, 300);
      },
      // TODO 处理冒泡事件，头条的冒泡事件有问题 ，先这样兼容
      touchstart() {
        this.clearPropagation = false;
      },
      onTap() {
        if (this.clearPropagation) {
          this.clearPropagation = false;
          return;
        }
        this.$emit("maskClick");
        if (!this.mkclick)
          return;
        this.close();
      },
      /**
       * 顶部弹出样式处理
       */
      top(type) {
        this.popupstyle = this.isDesktop ? "fixforpc-top" : "top";
        this.ani = ["slide-top"];
        this.transClass = {
          position: "fixed",
          left: 0,
          right: 0,
          backgroundColor: this.bg,
          borderRadius: this.borderRadius || "0"
        };
        if (type)
          return;
        this.showPopup = true;
        this.showTrans = true;
        this.$nextTick(() => {
          this.showPoptrans();
          if (this.messageChild && this.type === "message") {
            this.messageChild.timerClose();
          }
        });
      },
      /**
       * 底部弹出样式处理
       */
      bottom(type) {
        this.popupstyle = "bottom";
        this.ani = ["slide-bottom"];
        this.transClass = {
          position: "fixed",
          left: 0,
          right: 0,
          bottom: 0,
          paddingBottom: this.safeAreaInsets + "px",
          backgroundColor: this.bg,
          borderRadius: this.borderRadius || "0"
        };
        if (type)
          return;
        this.showPoptrans();
      },
      /**
       * 中间弹出样式处理
       */
      center(type) {
        this.popupstyle = "center";
        this.ani = ["zoom-out", "fade"];
        this.transClass = {
          position: "fixed",
          display: "flex",
          flexDirection: "column",
          bottom: 0,
          left: 0,
          right: 0,
          top: 0,
          justifyContent: "center",
          alignItems: "center",
          borderRadius: this.borderRadius || "0"
        };
        if (type)
          return;
        this.showPoptrans();
      },
      left(type) {
        this.popupstyle = "left";
        this.ani = ["slide-left"];
        this.transClass = {
          position: "fixed",
          left: 0,
          bottom: 0,
          top: 0,
          backgroundColor: this.bg,
          borderRadius: this.borderRadius || "0",
          display: "flex",
          flexDirection: "column"
        };
        if (type)
          return;
        this.showPoptrans();
      },
      right(type) {
        this.popupstyle = "right";
        this.ani = ["slide-right"];
        this.transClass = {
          position: "fixed",
          bottom: 0,
          right: 0,
          top: 0,
          backgroundColor: this.bg,
          borderRadius: this.borderRadius || "0",
          display: "flex",
          flexDirection: "column"
        };
        if (type)
          return;
        this.showPoptrans();
      },
      showPoptrans() {
        this.$nextTick(() => {
          this.showPopup = true;
          this.showTrans = true;
        });
      }
    }
  };
  function _sfc_render$1c(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_transition = resolveEasycom(vue.resolveDynamicComponent("uni-transition"), __easycom_3$9);
    return $data.showPopup ? (vue.openBlock(), vue.createElementBlock(
      "view",
      {
        key: 0,
        class: vue.normalizeClass(["uni-popup", [$data.popupstyle, $options.isDesktop ? "fixforpc-z-index" : ""]])
      },
      [
        vue.createElementVNode(
          "view",
          {
            onTouchstart: _cache[1] || (_cache[1] = (...args) => $options.touchstart && $options.touchstart(...args))
          },
          [
            $data.maskShow ? (vue.openBlock(), vue.createBlock(_component_uni_transition, {
              key: "1",
              name: "mask",
              "mode-class": "fade",
              styles: $data.maskClass,
              duration: $data.duration,
              show: $data.showTrans,
              onClick: $options.onTap
            }, null, 8, ["styles", "duration", "show", "onClick"])) : vue.createCommentVNode("v-if", true),
            vue.createVNode(_component_uni_transition, {
              key: "2",
              "mode-class": $data.ani,
              name: "content",
              styles: $data.transClass,
              duration: $data.duration,
              show: $data.showTrans,
              onClick: $options.onTap
            }, {
              default: vue.withCtx(() => [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["uni-popup__wrapper", [$data.popupstyle]]),
                    style: vue.normalizeStyle($options.getStyles),
                    onClick: _cache[0] || (_cache[0] = (...args) => $options.clear && $options.clear(...args))
                  },
                  [
                    vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
                  ],
                  6
                  /* CLASS, STYLE */
                )
              ]),
              _: 3
              /* FORWARDED */
            }, 8, ["mode-class", "styles", "duration", "show", "onClick"])
          ],
          32
          /* NEED_HYDRATION */
        )
      ],
      2
      /* CLASS */
    )) : vue.createCommentVNode("v-if", true);
  }
  const __easycom_0$3 = /* @__PURE__ */ _export_sfc(_sfc_main$1d, [["render", _sfc_render$1c], ["__scopeId", "data-v-4dd3c44b"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-popup/components/uni-popup/uni-popup.vue"]]);
  const _sfc_main$1c = {
    data() {
      return {
        imageSrc: "",
        // mask: "https://vkceyugu.cdn.bspapp.com/VKCEYUGU-a3b890b4-7cb2-4b29-aa78-e652572bdef6/d6bc69ee-cdc0-4a13-a744-d79db42e0dbe.png",
        dist: "",
        mask: "",
        ratio: 1,
        // 如果在弹窗中使用，必须手动指定裁剪器的大小
        containerSize: {
          width: 600,
          height: 600
        },
        // 保存操作后的状态以供下次编辑
        initPosition: null
      };
    },
    onLoad(options) {
      this.imageSrc = decodeURIComponent(options.src);
      formatAppLog("log", "at pages/pop_croper/pop_croper.vue:41", this.imageSrc);
      this.$nextTick(() => {
        this.openPopup();
      });
    },
    onPullDownRefresh() {
      uni.stopPullDownRefresh();
    },
    methods: {
      openPopup() {
        this.$refs.pop.open();
      },
      change() {
        uni.chooseImage({
          count: 1,
          sizeType: ["original"],
          success: (res) => {
            let tempFilePaths = res.tempFilePaths[0];
            this.src = tempFilePaths;
            this.initPosition = null;
          }
        });
      },
      close() {
        this.$refs.pop.close();
      },
      onChange(ev) {
        this.initPosition = ev;
      },
      // 撤销
      undo() {
        this.$refs.cropper.undo();
      },
      // 重做
      resume() {
        this.$refs.cropper.resume();
      },
      crop() {
        this.$refs.cropper.crop().then((res) => {
          this.dist = res;
          const pages2 = getCurrentPages();
          const parentPage = pages2[pages2.length - 2];
          formatAppLog("log", "at pages/pop_croper/pop_croper.vue:87", parentPage);
          parentPage.returnParam = res;
          uni.navigateBack({
            delta: 1
          });
        });
      }
    }
  };
  function _sfc_render$1b(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_bt_cropper = resolveEasycom(vue.resolveDynamicComponent("bt-cropper"), __easycom_0$4);
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(
        _component_uni_popup,
        { ref: "pop" },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "content" }, [
              vue.createVNode(_component_bt_cropper, {
                ref: "cropper",
                imageSrc: $data.imageSrc,
                mask: $data.mask,
                ratio: $data.ratio,
                initPosition: $data.initPosition,
                dWidth: 800,
                fileType: "png",
                containerSize: $data.containerSize,
                onChange: $options.onChange
              }, null, 8, ["imageSrc", "mask", "ratio", "initPosition", "containerSize", "onChange"]),
              vue.createElementVNode("view", { class: "btns" }, [
                vue.createElementVNode("button", {
                  class: "button",
                  onClick: _cache[0] || (_cache[0] = (...args) => $options.crop && $options.crop(...args))
                }, "裁剪"),
                vue.createElementVNode("button", {
                  class: "button",
                  onClick: _cache[1] || (_cache[1] = (...args) => $options.undo && $options.undo(...args))
                }, "撤销")
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const PagesPopCroperPopCroper = /* @__PURE__ */ _export_sfc(_sfc_main$1c, [["render", _sfc_render$1b], ["__scopeId", "data-v-ec2e1a43"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/pop_croper/pop_croper.vue"]]);
  const IGNORE_SHELL_KEY = "ignoreShellUpdateUntil";
  const IGNORE_COMPAT_KEY = "ignoreUpdateTime";
  const PENDING_SHELL_INFO_KEY = "pendingShellUpdate";
  const IOS_APPSTORE_URL = "https://apps.apple.com/app/id6747564362";
  const _sfc_main$1b = {
    __name: "version-check",
    props: {
      // 是否显示“已是最新版本”的提示
      showUpToDateToast: { type: Boolean, default: false },
      // 当前版本号（可选，未使用，保留兼容）
      currentVersion: { type: String, required: false },
      // 是否自动检查
      autoCheck: { type: Boolean, default: true }
    },
    setup(__props, { expose: __expose }) {
      const props = __props;
      const modalVisible = vue.ref(false);
      const newVersionInfo = vue.ref(null);
      const showUpToDateToast = vue.ref(false);
      const cv = vue.ref("1.0.0");
      const isApkDownloading = vue.ref(false);
      const apkProgress = vue.ref(0);
      const apkDownloaded = vue.ref(0);
      const apkTotal = vue.ref(0);
      const apkSpeedText = vue.ref("");
      const apkEtaText = vue.ref("");
      let _lastBytes = 0;
      let _lastTs = 0;
      const platform2 = vue.computed(() => {
        const s2 = uni.getSystemInfoSync();
        return (s2.platform || "unknown").toLowerCase();
      });
      const formatTime = (timestamp) => {
        if (!timestamp)
          return "未知日期";
        const date = new Date(Number(timestamp) * 1e3);
        const y2 = date.getFullYear();
        const m2 = String(date.getMonth() + 1).padStart(2, "0");
        const d2 = String(date.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${d2}`;
      };
      function formatSize(bytes = 0) {
        if (!bytes || bytes <= 0)
          return "0 B";
        const kb = 1024;
        const mb = kb * 1024;
        const gb = mb * 1024;
        if (bytes >= gb)
          return (bytes / gb).toFixed(2) + " GB";
        if (bytes >= mb)
          return (bytes / mb).toFixed(2) + " MB";
        if (bytes >= kb)
          return (bytes / kb).toFixed(2) + " KB";
        return bytes + " B";
      }
      function formatSpeed(bps = 0) {
        if (bps <= 0)
          return "0 B/s";
        const kb = 1024;
        const mb = kb * 1024;
        if (bps >= mb)
          return (bps / mb).toFixed(2) + " MB/s";
        if (bps >= kb)
          return (bps / kb).toFixed(2) + " KB/s";
        return bps.toFixed(0) + " B/s";
      }
      function formatETA(sec = 0) {
        if (!isFinite(sec) || sec <= 0)
          return "";
        const s2 = Math.round(sec);
        const m2 = Math.floor(s2 / 60);
        const r2 = s2 % 60;
        if (m2 <= 0)
          return `${r2}秒`;
        return `${m2}分${r2}秒`;
      }
      function isIgnoredShell() {
        const now = Date.now();
        const v1 = Number(uni.getStorageSync(IGNORE_SHELL_KEY) || 0);
        const v2 = Number(uni.getStorageSync(IGNORE_COMPAT_KEY) || 0);
        const until = Math.max(v1, v2) || 0;
        formatAppLog("log", "at components/version-check/version-check.vue:172", "[Update][Ignore] isIgnoredShell() -> now=", now, "v1=", v1, "v2=", v2, "pickedUntil=", until, "ignored=", until > now);
        if (until > 0) {
          formatAppLog("log", "at components/version-check/version-check.vue:174", "[Update][Ignore] untilReadable=", new Date(until).toISOString());
          formatAppLog("log", "at components/version-check/version-check.vue:175", "[Update][Ignore] sourceKey=", v1 >= v2 ? IGNORE_SHELL_KEY : IGNORE_COMPAT_KEY);
        } else {
          formatAppLog("log", "at components/version-check/version-check.vue:177", "[Update][Ignore] no ignore timestamp stored");
        }
        return until > now;
      }
      function setIgnoreShell3Days() {
        const until = Date.now() + 3 * 24 * 60 * 60 * 1e3;
        uni.setStorageSync(IGNORE_SHELL_KEY, until);
        uni.setStorageSync(IGNORE_COMPAT_KEY, until);
        formatAppLog("log", "at components/version-check/version-check.vue:192", "[Update][Ignore] setIgnoreShell3Days() -> set until:", until, "(", new Date(until).toISOString(), ")");
        formatAppLog("log", "at components/version-check/version-check.vue:193", "[Update][Ignore] keys written:", IGNORE_SHELL_KEY, ",", IGNORE_COMPAT_KEY);
      }
      function safeGetShellVersion() {
        var _a;
        try {
          if (typeof getAppShellVersion === "function") {
            const v3 = getAppShellVersion();
            formatAppLog("log", "at components/version-check/version-check.vue:202", "[Update] safeGetShellVersion() ->", v3);
            return v3;
          }
        } catch (e2) {
          formatAppLog("warn", "at components/version-check/version-check.vue:206", "[Update] safeGetShellVersion() fallback error:", e2);
        }
        const info = ((_a = uni.getAppBaseInfo) == null ? void 0 : _a.call(uni)) || {};
        const v2 = info.appVersion || dogdogdollVersion;
        formatAppLog("log", "at components/version-check/version-check.vue:210", "[Update] safeGetShellVersion() fallback ->", v2);
        return v2;
      }
      function ensurePlusAvailable() {
        if (typeof plus === "undefined" || !plus.runtime) {
          uni.showToast({ title: "仅 App 内可更新", icon: "none" });
          formatAppLog("warn", "at components/version-check/version-check.vue:218", "[Update] ensurePlusAvailable(): plus.runtime is not available");
          return false;
        }
        return true;
      }
      function downloadAndInstallWGT(wgtUrl, onSuccess) {
        formatAppLog("log", "at components/version-check/version-check.vue:228", `[热更新] 进入逻辑, 目标地址: ${wgtUrl}`);
        if (!wgtUrl) {
          formatAppLog("warn", "at components/version-check/version-check.vue:231", "[热更新] 未提供 wgtUrl, 跳过更新逻辑");
          onSuccess && onSuccess();
          return;
        }
        if (!ensurePlusAvailable()) {
          formatAppLog("error", "at components/version-check/version-check.vue:237", "[热更新] plus 环境不可用，终止执行");
          return;
        }
        formatAppLog("log", "at components/version-check/version-check.vue:241", `[热更新] 准备开始下载 wgt 包: ${wgtUrl}`);
        const dtask = plus.downloader.createDownload(
          wgtUrl,
          { filename: "_doc/update/" },
          // 可指定临时目录
          (d2, status) => {
            formatAppLog("log", "at components/version-check/version-check.vue:247", `[热更新] 下载任务回调触发, 状态码: ${status}`);
            if (status === 200) {
              formatAppLog("log", "at components/version-check/version-check.vue:250", `[热更新] 下载完成, 文件路径: ${d2.filename}`);
              formatAppLog("log", "at components/version-check/version-check.vue:251", "[热更新] 开始执行安装...");
              plus.runtime.install(
                d2.filename,
                { force: true },
                () => {
                  formatAppLog("log", "at components/version-check/version-check.vue:257", "[热更新] 安装成功, 即将重启应用");
                  onSuccess && onSuccess();
                  plus.runtime.restart();
                },
                (e2) => {
                  formatAppLog("error", "at components/version-check/version-check.vue:262", "[热更新] wgt 安装失败:", JSON.stringify(e2));
                  uni.showToast({ title: "热更安装失败", icon: "none" });
                }
              );
            } else {
              formatAppLog("error", "at components/version-check/version-check.vue:267", `[热更新] wgt 下载失败, HTTP 状态码: ${status}`);
              uni.showToast({ title: "热更下载失败", icon: "none" });
            }
          }
        );
        dtask.addEventListener("statechanged", (download) => {
          switch (download.state) {
            case 1:
              formatAppLog("log", "at components/version-check/version-check.vue:277", "[热更新] 下载任务已开始");
              break;
            case 2:
              formatAppLog("log", "at components/version-check/version-check.vue:280", "[热更新] 已连接服务器");
              break;
            case 3:
              formatAppLog("log", "at components/version-check/version-check.vue:283", `[热更新] 下载中... 已完成: ${download.downloadedSize}/${download.totalSize}`);
              break;
            case 4:
              formatAppLog("log", "at components/version-check/version-check.vue:286", "[热更新] 下载完成，等待回调处理");
              break;
          }
        });
        formatAppLog("log", "at components/version-check/version-check.vue:291", "[热更新] 开始执行下载任务");
        dtask.start();
      }
      function installShellPackage(androidApkUrl) {
        const scene = getScene();
        if (scene === 2) {
          formatAppLog("log", "at components/version-check/version-check.vue:301", "[Update] installShellPackage(): iOS -> open App Store");
          plus.runtime.openURL(IOS_APPSTORE_URL);
          return;
        }
        if (scene === 3) {
          if (!androidApkUrl) {
            formatAppLog("warn", "at components/version-check/version-check.vue:308", "[Update] installShellPackage(): androidApkUrl empty, open website fallback");
            plus.runtime.openURL("https://www.dogdogdoll.com/");
            return;
          }
          if (!ensurePlusAvailable())
            return;
          isApkDownloading.value = true;
          apkProgress.value = 0;
          apkDownloaded.value = 0;
          apkTotal.value = 0;
          apkSpeedText.value = "";
          apkEtaText.value = "";
          _lastBytes = 0;
          _lastTs = Date.now();
          formatAppLog("log", "at components/version-check/version-check.vue:324", "[Update][APK] start download ->", androidApkUrl);
          const dtask = plus.downloader.createDownload(androidApkUrl, { filename: "_doc/update/" }, (d2, status) => {
            isApkDownloading.value = false;
            formatAppLog("log", "at components/version-check/version-check.vue:329", "[Update][APK] download finished, status=", status, "file=", d2 == null ? void 0 : d2.filename);
            if (status === 200) {
              try {
                plus.runtime.openFile(d2.filename);
                formatAppLog("log", "at components/version-check/version-check.vue:334", "[Update][APK] openFile success");
              } catch (e2) {
                formatAppLog("error", "at components/version-check/version-check.vue:336", "[Update][APK] openFile fail:", e2);
                plus.runtime.openURL(androidApkUrl);
              }
            } else {
              formatAppLog("error", "at components/version-check/version-check.vue:340", "[Update][APK] 下载失败", status);
              uni.showToast({ title: "整包下载失败", icon: "none" });
            }
          });
          dtask.addEventListener("statechanged", (download) => {
            if (download.state === 3) {
              const total = download.totalSize || 0;
              const done = download.downloadedSize || 0;
              apkTotal.value = total;
              apkDownloaded.value = done;
              if (total > 0) {
                apkProgress.value = Math.max(0, Math.min(100, Math.floor(done * 100 / total)));
              } else {
                apkProgress.value = 0;
              }
              const now = Date.now();
              const deltaB = done - _lastBytes;
              const deltaT = (now - _lastTs) / 1e3;
              if (deltaT > 0 && deltaB >= 0) {
                const bps = deltaB / deltaT;
                apkSpeedText.value = formatSpeed(bps);
                const remain = total > 0 ? (total - done) / (bps || 1) : 0;
                apkEtaText.value = formatETA(remain);
                _lastBytes = done;
                _lastTs = now;
              }
            }
          });
          dtask.start();
          return;
        }
        formatAppLog("warn", "at components/version-check/version-check.vue:379", "[Update] installShellPackage(): unexpected scene -> show modal");
        uni.showModal({
          title: "更新提示",
          content: "请前往应用商店更新最新版本",
          showCancel: false
        });
      }
      async function fallbackCheckOld() {
        var _a;
        formatAppLog("log", "at components/version-check/version-check.vue:394", "[Update] fallbackCheckOld(): call /latest-version");
        const res = await uni.request({
          url: `${websiteUrl.value}/latest-version?version=${dogdogdollVersion}`,
          method: "GET"
        });
        const ok = ((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success" && res.data.data;
        formatAppLog("log", "at components/version-check/version-check.vue:400", "[Update] fallbackCheckOld() -> ok=", ok);
        if (ok) {
          newVersionInfo.value = res.data.data;
          modalVisible.value = true;
        } else if (props.showUpToDateToast) {
          showUpToDateToast.value = true;
          setTimeout(() => showUpToDateToast.value = false, 2e3);
        }
      }
      async function checkV2(shellVersion, wgtVersion) {
        var _a, _b;
        formatAppLog("log", "at components/version-check/version-check.vue:415", "[Update] checkV2(): shell=", shellVersion, "wgt=", wgtVersion, "url=", websiteUrl.value);
        const res = await uni.request({
          url: `${websiteUrl.value}/latest-version/v2?shell_version=${encodeURIComponent(shellVersion)}&wgt_version=${encodeURIComponent(wgtVersion)}`,
          method: "GET"
        });
        const ok = ((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success";
        const data = (_b = res == null ? void 0 : res.data) == null ? void 0 : _b.data;
        formatAppLog("log", "at components/version-check/version-check.vue:422", "[Update] checkV2() -> ok=", ok, "data=", data);
        if (!ok || !data) {
          await fallbackCheckOld();
          return;
        }
        const type = String(data.update_type || "").toLowerCase();
        formatAppLog("log", "at components/version-check/version-check.vue:430", "[Update] checkV2() -> update_type=", type);
        if (type === "wgt") {
          formatAppLog("log", "at components/version-check/version-check.vue:433", "[Update] wgt only: start download+install");
          downloadAndInstallWGT(data.wgt_url);
          return;
        }
        if (type === "shell") {
          const payloadForNextLaunch = {
            version: data.version,
            note: data.note,
            created_at: data.created_at,
            android_pkg_url: data.android_pkg_url || "",
            wgt_url: data.wgt_url || "",
            // 用于本地“立即更新”判断
            update_type: "shell"
          };
          uni.setStorageSync(PENDING_SHELL_INFO_KEY, JSON.stringify(payloadForNextLaunch));
          formatAppLog("log", "at components/version-check/version-check.vue:450", "[Update] shell update: write pending ->", payloadForNextLaunch);
          if (data.wgt_url) {
            formatAppLog("log", "at components/version-check/version-check.vue:453", "[Update] shell+WGT: first apply WGT then restart (pending kept)");
            downloadAndInstallWGT(data.wgt_url, () => {
            });
          } else {
            const ignored = isIgnoredShell();
            formatAppLog("log", "at components/version-check/version-check.vue:460", "[Update] shell(no wgt): ignored=", ignored);
            if (!ignored) {
              newVersionInfo.value = payloadForNextLaunch;
              modalVisible.value = true;
              formatAppLog("log", "at components/version-check/version-check.vue:464", "[Update] shell(no wgt): show modal immediately");
            } else {
              formatAppLog("log", "at components/version-check/version-check.vue:466", "[Update] shell(no wgt): ignored by window");
            }
          }
          return;
        }
        if (props.showUpToDateToast) {
          showUpToDateToast.value = true;
          setTimeout(() => showUpToDateToast.value = false, 2e3);
        }
      }
      const checkVersion = async () => {
        try {
          const scene = getScene();
          formatAppLog("log", "at components/version-check/version-check.vue:484", "[Update] checkVersion(): scene=", scene);
          if (scene === 1 || scene === 4) {
            formatAppLog("log", "at components/version-check/version-check.vue:486", "H5/MP 场景不检查版本，scene=", scene);
            return;
          }
          const ignored = isIgnoredShell();
          formatAppLog("log", "at components/version-check/version-check.vue:493", "[Update] checkVersion(): ignored=", ignored);
          if (!ignored) {
            const text = uni.getStorageSync(PENDING_SHELL_INFO_KEY);
            formatAppLog("log", "at components/version-check/version-check.vue:498", "[Update] checkVersion(): pendingText exists=", !!text);
            if (text) {
              try {
                const info = JSON.parse(text || "{}");
                formatAppLog("log", "at components/version-check/version-check.vue:502", "[Update] checkVersion(): parsed pending ->", info);
                newVersionInfo.value = info || {};
                modalVisible.value = true;
                return;
              } catch (e2) {
                formatAppLog("warn", "at components/version-check/version-check.vue:509", "[Update] checkVersion(): pending parse error -> clear key", e2);
                uni.removeStorageSync(PENDING_SHELL_INFO_KEY);
              }
            }
          } else {
            formatAppLog("log", "at components/version-check/version-check.vue:514", "[Update] checkVersion(): ignored -> skip pending-shell showing at this time");
          }
          const shellVersion = safeGetShellVersion();
          const wgtVersion = dogdogdollVersion;
          await checkV2(shellVersion, wgtVersion);
        } catch (err) {
          formatAppLog("error", "at components/version-check/version-check.vue:522", "版本检查失败：", err);
        }
      };
      function handleUpdate() {
        setIgnoreShell3Days();
        const info = newVersionInfo.value || {};
        const pendingText = uni.getStorageSync(PENDING_SHELL_INFO_KEY);
        if (pendingText && !info.android_pkg_url) {
          try {
            const p2 = JSON.parse(pendingText);
            info.android_pkg_url = info.android_pkg_url || p2.android_pkg_url;
          } catch (e2) {
            formatAppLog("warn", "at components/version-check/version-check.vue:541", "[Update] handleUpdate(): parse pending error", e2);
          }
        }
        formatAppLog("log", "at components/version-check/version-check.vue:544", "[Update] handleUpdate(): start install shell package ->", info.android_pkg_url);
        installShellPackage(info.android_pkg_url);
        uni.removeStorageSync(PENDING_SHELL_INFO_KEY);
        formatAppLog("log", "at components/version-check/version-check.vue:550", "[Update] handleUpdate(): remove PENDING key");
      }
      function ignoreUpdate() {
        setIgnoreShell3Days();
        modalVisible.value = false;
        uni.removeStorageSync(PENDING_SHELL_INFO_KEY);
        formatAppLog("log", "at components/version-check/version-check.vue:559", "[Update] ignoreUpdate(): set ignore & remove PENDING key");
      }
      vue.onMounted(() => {
        var _a;
        cv.value = ((_a = uni.getAppBaseInfo) == null ? void 0 : _a.call(uni).appVersion) || cv.value;
        if (props.autoCheck) {
          setTimeout(() => {
            checkVersion();
          }, 1500);
        }
      });
      const apkDownloadedText = vue.computed(() => formatSize(apkDownloaded.value));
      const apkTotalText = vue.computed(() => apkTotal.value > 0 ? formatSize(apkTotal.value) : "未知大小");
      function debugClearIgnore() {
        uni.removeStorageSync(IGNORE_SHELL_KEY);
        uni.removeStorageSync(IGNORE_COMPAT_KEY);
        formatAppLog("log", "at components/version-check/version-check.vue:583", "[Debug] debugClearIgnore(): removed keys ->", IGNORE_SHELL_KEY, ",", IGNORE_COMPAT_KEY);
      }
      function debugExpireIgnoreNow() {
        const past = Date.now() - 1e3;
        uni.setStorageSync(IGNORE_SHELL_KEY, past);
        uni.setStorageSync(IGNORE_COMPAT_KEY, past);
        formatAppLog("log", "at components/version-check/version-check.vue:590", "[Debug] debugExpireIgnoreNow(): set past ->", past, new Date(past).toISOString());
      }
      function debugForceShowShellUpdate() {
        const mock = {
          version: "9.9.9",
          note: "【调试】这是一个模拟的版本说明",
          created_at: Math.floor(Date.now() / 1e3),
          android_pkg_url: "https://images1.fantuanpu.com/apk/dogdogdoll-v1.1.4.apk",
          wgt_url: "",
          update_type: "shell"
        };
        uni.setStorageSync(PENDING_SHELL_INFO_KEY, JSON.stringify(mock));
        debugExpireIgnoreNow();
        formatAppLog("log", "at components/version-check/version-check.vue:604", "[Debug] debugForceShowShellUpdate(): write mock pending & expire ignore, then show modal");
        newVersionInfo.value = mock;
        modalVisible.value = true;
      }
      __expose({
        checkVersion,
        debugClearIgnore,
        debugExpireIgnoreNow,
        debugForceShowShellUpdate
      });
      const __returned__ = { props, modalVisible, newVersionInfo, showUpToDateToast, cv, isApkDownloading, apkProgress, apkDownloaded, apkTotal, apkSpeedText, apkEtaText, get _lastBytes() {
        return _lastBytes;
      }, set _lastBytes(v2) {
        _lastBytes = v2;
      }, get _lastTs() {
        return _lastTs;
      }, set _lastTs(v2) {
        _lastTs = v2;
      }, platform: platform2, IGNORE_SHELL_KEY, IGNORE_COMPAT_KEY, PENDING_SHELL_INFO_KEY, IOS_APPSTORE_URL, formatTime, formatSize, formatSpeed, formatETA, isIgnoredShell, setIgnoreShell3Days, safeGetShellVersion, ensurePlusAvailable, downloadAndInstallWGT, installShellPackage, fallbackCheckOld, checkV2, checkVersion, handleUpdate, ignoreUpdate, apkDownloadedText, apkTotalText, debugClearIgnore, debugExpireIgnoreNow, debugForceShowShellUpdate, ref: vue.ref, onMounted: vue.onMounted, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get getScene() {
        return getScene;
      }, get dogdogdollVersion() {
        return dogdogdollVersion;
      }, get getAppShellVersion() {
        return getAppShellVersion;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1a(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createVNode(_component_common_modal, {
        visible: $setup.modalVisible,
        "onUpdate:visible": _cache[0] || (_cache[0] = ($event) => $setup.modalVisible = $event),
        top: "15vh"
      }, {
        default: vue.withCtx(() => {
          var _a, _b, _c;
          return [
            vue.createElementVNode("view", { class: "version-update-container" }, [
              vue.createElementVNode(
                "view",
                { class: "update-title" },
                " 发现新版本 v" + vue.toDisplayString(((_a = $setup.newVersionInfo) == null ? void 0 : _a.version) || "-") + " 当前 v" + vue.toDisplayString($setup.dogdogdollVersion),
                1
                /* TEXT */
              ),
              vue.createElementVNode("scroll-view", {
                class: "update-content",
                "scroll-y": ""
              }, [
                vue.createElementVNode("view", { class: "update-section" }, [
                  vue.createElementVNode("view", { class: "section-title" }, "更新内容"),
                  vue.createElementVNode(
                    "view",
                    { class: "section-content" },
                    vue.toDisplayString(((_b = $setup.newVersionInfo) == null ? void 0 : _b.note) || "优化用户体验，修复已知问题"),
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "update-section" }, [
                  vue.createElementVNode("view", { class: "section-title" }, "更新日期"),
                  vue.createElementVNode(
                    "view",
                    { class: "section-content" },
                    vue.toDisplayString($setup.formatTime((_c = $setup.newVersionInfo) == null ? void 0 : _c.created_at)),
                    1
                    /* TEXT */
                  )
                ])
              ]),
              $setup.isApkDownloading ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "download-panel"
              }, [
                vue.createElementVNode("view", { class: "progress-row" }, [
                  vue.createElementVNode("view", { class: "progress-bar" }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: "progress-inner",
                        style: vue.normalizeStyle({ width: $setup.apkProgress + "%" })
                      },
                      null,
                      4
                      /* STYLE */
                    )
                  ]),
                  vue.createElementVNode(
                    "text",
                    { class: "progress-percent" },
                    vue.toDisplayString($setup.apkProgress) + "%",
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "progress-meta" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "meta-item" },
                    vue.toDisplayString($setup.apkDownloadedText) + " / " + vue.toDisplayString($setup.apkTotalText),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("text", { class: "meta-dot" }, "·"),
                  vue.createElementVNode(
                    "text",
                    { class: "meta-item" },
                    vue.toDisplayString($setup.apkSpeedText),
                    1
                    /* TEXT */
                  ),
                  $setup.apkEtaText ? (vue.openBlock(), vue.createElementBlock("text", {
                    key: 0,
                    class: "meta-dot"
                  }, "·")) : vue.createCommentVNode("v-if", true),
                  $setup.apkEtaText ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 1,
                      class: "meta-item"
                    },
                    "约 " + vue.toDisplayString($setup.apkEtaText) + " 完成",
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ]),
                vue.createElementVNode("view", { class: "progress-tips" }, "请勿退出应用或锁屏，以免下载被系统暂停")
              ])) : (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "update-buttons"
              }, [
                vue.createElementVNode("button", {
                  class: "update-btn ignore",
                  onClick: $setup.ignoreUpdate
                }, "暂不更新"),
                vue.createElementVNode("button", {
                  class: "update-btn confirm",
                  onClick: $setup.handleUpdate
                }, "立即更新")
              ]))
            ])
          ];
        }),
        _: 1
        /* STABLE */
      }, 8, ["visible"]),
      $setup.showUpToDateToast ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "toast-message"
      }, [
        vue.createElementVNode("text", null, "已是最新版本")
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_1$5 = /* @__PURE__ */ _export_sfc(_sfc_main$1b, [["render", _sfc_render$1a], ["__scopeId", "data-v-3cade7b0"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/version-check/version-check.vue"]]);
  const _imports_0$8 = "/static/right.png";
  const _sfc_main$1a = {
    __name: "setting",
    setup(__props, { expose: __expose }) {
      __expose();
      uni.setNavigationBarTitle({ title: "设置" });
      const versionCheckRef = vue.ref(null);
      const menuItems = vue.computed(() => [
        {
          label: "更改用户名",
          action: jump2username,
          status: !!global$1.userInfo.password,
          displayValue: global$1.userInfo.password ? "去修改" : "未设置"
        },
        {
          label: "更改密码",
          action: jump2password,
          status: !!global$1.userInfo.password,
          displayValue: global$1.userInfo.password ? "去修改" : "未设置"
        },
        {
          label: "绑定手机号",
          action: jump2telphone,
          status: !!global$1.userInfo.tel_phone,
          // 修正字段名
          displayValue: global$1.userInfo.tel_phone ? global$1.userInfo.tel_phone.replace(/(\d{3})\d{4}(\d{4})/, "$1****$2") : "去绑定"
        },
        {
          label: "绑定微信",
          action: jump2wechat,
          status: !!global$1.userInfo.wechat_open_id,
          displayValue: global$1.userInfo.wechat_open_id ? "已绑定" : "去绑定"
        },
        {
          label: "检查更新",
          action: checkUpdate,
          // 点击时会触发“清空忽略期 + 主动检查”
          status: !!(newVersionInfo.value && newVersionInfo.value.version),
          // 有新版本时高亮显示
          displayValue: updateStatusText.value
        }
      ]);
      let userInfo = vue.ref({});
      let newVersionInfo = vue.ref({});
      const hasAppliedDeletion = vue.ref(false);
      let needUpdate = vue.ref(false);
      const updateStatusText = vue.ref("检查更新");
      let click = vue.ref(0);
      function jump2password() {
        uni.navigateTo({ url: "/pages/setting/password/password" });
      }
      function jump2telphone() {
        uni.navigateTo({ url: "/pages/setting/tel_phone/tel_phone" });
      }
      function jump2username() {
        uni.navigateTo({ url: "/pages/setting/username/username" });
      }
      function jump2wechat() {
        if (global$1.userInfo.wechat_open_id) {
          uni.showToast({ title: "已绑定微信", icon: "none" });
        } else {
          bindWechat().then(() => {
            formatAppLog("log", "at pages/setting/setting.vue:129", "微信绑定成功");
            uni.showToast({ title: "微信绑定成功", icon: "none" });
          }).catch((err) => {
            formatAppLog("error", "at pages/setting/setting.vue:133", "微信绑定失败:", err);
            uni.showToast({ title: "微信绑定失败", icon: "none" });
          });
        }
      }
      const checkUpdate = async (...args) => {
        var _a, _b, _c;
        const userTriggered = args && args.length > 0;
        click.value++;
        if (userTriggered) {
          formatAppLog("log", "at pages/setting/setting.vue:150", "[Settings] 主动检查更新：准备清空忽略期并触发 version-check");
          updateStatusText.value = "检查中...";
          try {
            (_b = (_a = versionCheckRef.value) == null ? void 0 : _a.debugClearIgnore) == null ? void 0 : _b.call(_a);
            uni.showToast({ title: "忽略期已清空，开始检查", icon: "none" });
            await vue.nextTick();
            if ((_c = versionCheckRef.value) == null ? void 0 : _c.checkVersion) {
              versionCheckRef.value.checkVersion();
            } else {
              formatAppLog("warn", "at pages/setting/setting.vue:162", "[Settings] version-check 组件未就绪，走回退查询");
              await lightweightQueryForDisplay();
            }
          } catch (e2) {
            formatAppLog("error", "at pages/setting/setting.vue:166", "[Settings] 主动检查更新失败：", e2);
            await lightweightQueryForDisplay();
          } finally {
            setTimeout(() => {
              if (updateStatusText.value === "检查中...") {
                updateStatusText.value = "检查更新";
              }
            }, 5e3);
          }
          return;
        }
        await lightweightQueryForDisplay();
      };
      async function lightweightQueryForDisplay() {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/latest-version?version=${dogdogdollVersion}`,
            method: "GET"
          });
          if (res && res.data) {
            if (res.data.status === "success" && res.data.data) {
              const info = res.data.data || {};
              updateStatusText.value = `有新版本: ${info.version || "-"}`;
              newVersionInfo.value = info;
            } else {
              updateStatusText.value = "已是最新";
              newVersionInfo.value = {};
              if (click.value > 1) {
                uni.showToast({ title: "当前已是最新版本", icon: "none" });
              }
            }
          }
        } catch (err) {
          formatAppLog("error", "at pages/setting/setting.vue:210", "[Settings] 轻量查询失败：", err);
          uni.showToast({ title: "检查失败，请稍后重试", icon: "none" });
        }
      }
      async function handleAccountDeletion() {
        if (hasAppliedDeletion.value) {
          uni.showModal({
            title: "取消注销申请",
            content: "确定要取消账号注销申请吗？",
            success: async (res) => {
              if (res.confirm) {
                await cancelAccountDeletion();
              }
            }
          });
        } else {
          uni.showModal({
            title: "申请注销账号",
            content: "确定要申请注销账号吗？注销后账号将不可恢复！",
            confirmText: "确认注销",
            confirmColor: "#f56c6c",
            success: async (res) => {
              if (res.confirm) {
                await applyForAccountDeletion();
              }
            }
          });
        }
      }
      async function applyForAccountDeletion() {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        uni.showLoading({ title: "处理中...", mask: true });
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/apply-delete`,
            method: "POST",
            header: { "Authorization": token }
          });
          uni.hideLoading();
          if (res.data.status === "success") {
            hasAppliedDeletion.value = true;
            global$1.userInfo.deleteApplyAt = Math.floor(Date.now() / 1e3);
            uni.showToast({ title: "已申请注销账号", icon: "success", duration: 3e3 });
            setTimeout(() => {
              uni.showModal({
                title: "注销申请已提交",
                content: "您的账号将在30天后正式注销。在此期间您可以正常使用所有功能，并可随时取消注销申请。",
                showCancel: false,
                confirmText: "知道了"
              });
            }, 1e3);
          } else {
            throw new Error(res.data.msg || "申请注销失败");
          }
        } catch (error2) {
          uni.hideLoading();
          uni.showToast({
            title: error2.message || "操作失败，请稍后重试",
            icon: "none",
            duration: 3e3
          });
        }
      }
      async function cancelAccountDeletion() {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        uni.showLoading({ title: "处理中...", mask: true });
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/cancel-delete`,
            method: "POST",
            header: { "Authorization": token }
          });
          uni.hideLoading();
          if (res.data.status === "success") {
            hasAppliedDeletion.value = false;
            global$1.userInfo.deleteApplyAt = 0;
            uni.showToast({ title: "已取消注销申请", icon: "success", duration: 3e3 });
          } else {
            throw new Error(res.data.msg || "取消注销失败");
          }
        } catch (error2) {
          uni.hideLoading();
          uni.showToast({
            title: error2.message || "操作失败，请稍后重试",
            icon: "none",
            duration: 3e3
          });
        }
      }
      vue.onMounted(() => {
        asyncGetUserInfo().then((user) => {
          hasAppliedDeletion.value = (user == null ? void 0 : user.delete_apply_at) > 0;
        });
        checkUpdate();
      });
      const __returned__ = { versionCheckRef, menuItems, get userInfo() {
        return userInfo;
      }, set userInfo(v2) {
        userInfo = v2;
      }, get newVersionInfo() {
        return newVersionInfo;
      }, set newVersionInfo(v2) {
        newVersionInfo = v2;
      }, hasAppliedDeletion, get needUpdate() {
        return needUpdate;
      }, set needUpdate(v2) {
        needUpdate = v2;
      }, updateStatusText, get click() {
        return click;
      }, set click(v2) {
        click = v2;
      }, jump2password, jump2telphone, jump2username, jump2wechat, checkUpdate, lightweightQueryForDisplay, handleAccountDeletion, applyForAccountDeletion, cancelAccountDeletion, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, nextTick: vue.nextTick, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get bindWechat() {
        return bindWechat;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get dogdogdollVersion() {
        return dogdogdollVersion;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$19(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_version_check = resolveEasycom(vue.resolveDynamicComponent("version-check"), __easycom_1$5);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "settings-container" }, [
          vue.createElementVNode("view", { style: { "display": "none" } }, [
            vue.createVNode(
              _component_version_check,
              {
                ref: "versionCheckRef",
                "auto-check": false,
                "show-up-to-date-toast": false
              },
              null,
              512
              /* NEED_PATCH */
            )
          ]),
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.menuItems, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "menu-item",
                onClick: ($event) => item.action(item)
              }, [
                vue.createElementVNode(
                  "text",
                  { class: "menu-label" },
                  vue.toDisplayString(item.label),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("view", { class: "menu-value" }, [
                  vue.createElementVNode(
                    "text",
                    {
                      class: vue.normalizeClass(item.status ? "active" : "inactive")
                    },
                    vue.toDisplayString(item.displayValue),
                    3
                    /* TEXT, CLASS */
                  ),
                  vue.createElementVNode("image", {
                    class: "arrow-icon",
                    src: _imports_0$8
                  })
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          vue.createElementVNode("view", { class: "logout-section" }, [
            vue.createElementVNode(
              "view",
              {
                class: "logout-btn",
                onClick: $setup.handleAccountDeletion,
                style: vue.normalizeStyle({ backgroundColor: $setup.hasAppliedDeletion ? "#f56c6c" : "#cfcad8" })
              },
              [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString($setup.hasAppliedDeletion ? "取消注销申请" : "申请注销账号"),
                  1
                  /* TEXT */
                )
              ],
              4
              /* STYLE */
            ),
            $setup.hasAppliedDeletion ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 0,
              class: "deletion-notice"
            }, " 已申请注销，30天后账号将正式注销。在此之前可以正常使用，并可随时取消注销申请。 ")) : vue.createCommentVNode("v-if", true)
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSettingSetting = /* @__PURE__ */ _export_sfc(_sfc_main$1a, [["render", _sfc_render$19], ["__scopeId", "data-v-018cdf56"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/setting/setting.vue"]]);
  const _sfc_main$19 = {
    __name: "password",
    setup(__props, { expose: __expose }) {
      __expose();
      uni.setNavigationBarTitle({
        title: "设置密码"
      });
      let oldPassword = vue.ref("");
      let newPassword = vue.ref("");
      let newPassword2 = vue.ref("");
      const isFormValid = vue.computed(() => {
        return newPassword.value && newPassword2.value && newPassword.value === newPassword2.value;
      });
      const strengthText = vue.computed(() => {
        if (newPassword.value.length < 6) {
          return "密码过于简单";
        } else if (newPassword.value.length < 10) {
          return "密码强度一般";
        } else {
          return "密码强度较高";
        }
      });
      const getStrengthClass = (level) => {
        if (newPassword.value.length < 6) {
          return "weak";
        } else if (newPassword.value.length < 10) {
          return "medium";
        } else {
          return "strong";
        }
      };
      function updatePassword() {
        if (newPassword.value != newPassword2.value) {
          uni.showToast({
            title: "两次密码输入不一致",
            icon: "none"
          });
          return;
        }
        if (global$1.userInfo.password && !oldPassword.value) {
          uni.showToast({
            title: "请输入原密码",
            icon: "none"
          });
          return;
        }
        uni.request({
          url: websiteUrl.value + "/with-state/setting-password",
          method: "POST",
          header: {
            "Authorization": uni.getStorageSync("token")
          },
          data: {
            old_password: oldPassword.value,
            new_password: newPassword.value
          },
          success: (res) => {
            formatAppLog("log", "at pages/setting/password/password.vue:134", res.data);
            if (res.data.status == "success") {
              uni.showToast({
                title: "修改成功",
                icon: "success"
              });
            } else {
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
            }
          }
        });
      }
      getUserInfo();
      const __returned__ = { get oldPassword() {
        return oldPassword;
      }, set oldPassword(v2) {
        oldPassword = v2;
      }, get newPassword() {
        return newPassword;
      }, set newPassword(v2) {
        newPassword = v2;
      }, get newPassword2() {
        return newPassword2;
      }, set newPassword2(v2) {
        newPassword2 = v2;
      }, isFormValid, strengthText, getStrengthClass, updatePassword, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$18(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "password-container" }, [
          vue.createElementVNode("view", { class: "form-wrapper" }, [
            $setup.global.userInfo.password ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "input-group"
            }, [
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "password",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.oldPassword = $event)
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.oldPassword]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "原密码"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "locked",
                  size: "18",
                  color: "#999"
                })
              ]),
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "password",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.newPassword = $event)
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.newPassword]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "新密码"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "gear",
                  size: "18",
                  color: "#999"
                })
              ]),
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "password",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.newPassword2 = $event)
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.newPassword2]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "确认新密码"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "checkmarkempty",
                  size: "18",
                  color: "#999"
                })
              ])
            ])) : (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "input-group"
            }, [
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "password",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.newPassword = $event)
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.newPassword]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "设置密码"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "locked",
                  size: "18",
                  color: "#999"
                })
              ]),
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "password",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.newPassword2 = $event)
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.newPassword2]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "确认密码"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "checkmarkempty",
                  size: "18",
                  color: "#999"
                })
              ])
            ])),
            $setup.newPassword ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 2,
              class: "password-strength"
            }, [
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["strength-bar", $setup.getStrengthClass(1)])
                },
                null,
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["strength-bar", $setup.getStrengthClass(2)])
                },
                null,
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["strength-bar", $setup.getStrengthClass(3)])
                },
                null,
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "text",
                { class: "strength-text" },
                vue.toDisplayString($setup.strengthText),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("button", {
              class: vue.normalizeClass(["submit-btn", { active: $setup.isFormValid }]),
              onClick: $setup.updatePassword,
              disabled: !$setup.isFormValid
            }, vue.toDisplayString($setup.global.userInfo.password ? "更新密码" : "设置密码"), 11, ["disabled"])
          ]),
          vue.createElementVNode("view", { class: "security-tips" }, [
            vue.createVNode(_component_uni_icons, {
              type: "info",
              size: "16",
              color: "#666"
            }),
            vue.createElementVNode("text", null, "建议使用8位以上包含字母和数字的组合")
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSettingPasswordPassword = /* @__PURE__ */ _export_sfc(_sfc_main$19, [["render", _sfc_render$18], ["__scopeId", "data-v-b825db0b"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/setting/password/password.vue"]]);
  const _sfc_main$18 = {
    __name: "tel_phone",
    setup(__props, { expose: __expose }) {
      __expose();
      uni.setNavigationBarTitle({ title: "手机号" });
      const telPhone = vue.ref("");
      const code = vue.ref("");
      const buttonMsg = vue.ref("发送验证码");
      let timer = null;
      function updateTelPhone() {
        uni.request({
          url: websiteUrl.value + "/with-state/update-profile",
          method: "POST",
          header: { Authorization: uni.getStorageSync("token") },
          data: { tel_phone: telPhone.value, code: code.value },
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "修改成功", icon: "success" });
              getUserInfo();
            } else {
              uni.showToast({ title: res.data.msg || "提交失败", icon: "none" });
            }
          }
        });
      }
      function sendCode() {
        if (buttonMsg.value !== "发送验证码") {
          uni.showToast({ title: "请勿重复发送", icon: "none" });
          return;
        }
        if (!telPhone.value) {
          uni.showToast({ title: "请输入手机号", icon: "none" });
          return;
        }
        if (!/^1[3-9]\d{9}$/.test(telPhone.value)) {
          uni.showToast({ title: "手机号格式错误", icon: "none" });
          return;
        }
        buttonMsg.value = "发送中…";
        uni.request({
          url: websiteUrl.value + "/send-sms-code",
          method: "POST",
          header: { Authorization: uni.getStorageSync("token") },
          data: { tel_phone: telPhone.value },
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "发送成功", icon: "success" });
              startCountdown(60);
            } else {
              uni.showToast({ title: res.data.msg || "发送失败", icon: "none" });
              buttonMsg.value = "发送验证码";
            }
          },
          fail: () => {
            buttonMsg.value = "发送验证码";
          }
        });
      }
      function startCountdown(seconds) {
        let left = seconds;
        clearInterval(timer);
        timer = setInterval(() => {
          left -= 1;
          if (left <= 0) {
            clearInterval(timer);
            buttonMsg.value = "发送验证码";
          } else {
            buttonMsg.value = `${left}s后重发`;
          }
        }, 1e3);
      }
      getUserInfo();
      const __returned__ = { telPhone, code, buttonMsg, get timer() {
        return timer;
      }, set timer(v2) {
        timer = v2;
      }, updateTelPhone, sendCode, startCountdown, ref: vue.ref, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$17(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "page" }, [
          $setup.global.userInfo.tel_phone ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
            vue.createElementVNode("input", {
              type: "text",
              class: "input input--disabled",
              value: $setup.global.userInfo.tel_phone,
              disabled: ""
            }, null, 8, ["value"]),
            vue.createElementVNode("text", { class: "tip" }, "已绑定，如需修改请联系管理员操作。")
          ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
            vue.createElementVNode("view", { class: "field" }, [
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "input",
                  type: "text",
                  placeholder: "请输入手机号",
                  "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.telPhone = $event)
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.telPhone]
              ])
            ]),
            vue.createElementVNode("view", { class: "field code-field" }, [
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "input flex-1",
                  type: "text",
                  placeholder: "请输入验证码",
                  "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.code = $event)
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.code]
              ]),
              vue.createElementVNode("button", {
                class: vue.normalizeClass(["code-btn", { "is-disabled": $setup.buttonMsg !== "发送验证码" }]),
                disabled: $setup.buttonMsg !== "发送验证码",
                onClick: $setup.sendCode
              }, [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString($setup.buttonMsg),
                  1
                  /* TEXT */
                )
              ], 10, ["disabled"])
            ])
          ])),
          !$setup.global.userInfo.tel_phone ? (vue.openBlock(), vue.createElementBlock("button", {
            key: 2,
            class: "primary-btn",
            onClick: $setup.updateTelPhone
          }, "提交")) : vue.createCommentVNode("v-if", true)
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSettingTelPhoneTelPhone = /* @__PURE__ */ _export_sfc(_sfc_main$18, [["render", _sfc_render$17], ["__scopeId", "data-v-25dd367f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/setting/tel_phone/tel_phone.vue"]]);
  const _sfc_main$17 = {
    __name: "common-name-picker",
    props: {
      dataList: {
        type: Array,
        default: () => []
      },
      placeholder: {
        type: String,
        default: "请选择"
      },
      defaultValue: {
        type: String,
        default: ""
      }
    },
    emits: ["select"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const isOpen = vue.ref(false);
      const selectedValue = vue.ref(props.defaultValue);
      const toggleOpen = () => {
        isOpen.value = !isOpen.value;
      };
      const selectItem = (item) => {
        selectedValue.value = item;
        isOpen.value = false;
        emit("select", item);
      };
      vue.onMounted(() => {
        formatAppLog("log", "at components/common-name-picker/common-name-picker.vue:69", "初始值", isOpen.value);
        isOpen.value = false;
      });
      const __returned__ = { props, emit, isOpen, selectedValue, toggleOpen, selectItem, ref: vue.ref, onMounted: vue.onMounted };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$16(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: "select-container",
        ref: "containerRef"
      },
      [
        vue.createElementVNode("view", {
          class: "select-input",
          onClick: $setup.toggleOpen
        }, [
          vue.createElementVNode(
            "text",
            { class: "selected-value" },
            vue.toDisplayString($setup.selectedValue || $props.placeholder),
            1
            /* TEXT */
          ),
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["arrow-icon", { "rotate": $setup.isOpen }])
            },
            [
              vue.createVNode(_component_uni_icons, {
                type: "arrowdown",
                size: "16",
                color: "#666"
              })
            ],
            2
            /* CLASS */
          )
        ]),
        vue.createElementVNode("view", { class: "popup-container" }, [
          $setup.isOpen ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "mask",
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.toggleOpen())
          })) : vue.createCommentVNode("v-if", true),
          $setup.isOpen ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "options-wrapper"
          }, [
            vue.createElementVNode("scroll-view", {
              "scroll-y": "true",
              class: "select-options"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($props.dataList, (item, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: vue.normalizeClass(["option-item", { "selected": $setup.selectedValue === item }]),
                    onClick: vue.withModifiers(($event) => $setup.selectItem(item), ["stop"])
                  }, [
                    vue.createTextVNode(
                      vue.toDisplayString(item) + " ",
                      1
                      /* TEXT */
                    ),
                    $setup.selectedValue === item ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                      key: 0,
                      type: "checkmarkempty",
                      size: "18",
                      color: "#007AFF",
                      class: "check-icon"
                    })) : vue.createCommentVNode("v-if", true)
                  ], 10, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ])) : vue.createCommentVNode("v-if", true)
        ])
      ],
      512
      /* NEED_PATCH */
    );
  }
  const __easycom_1$4 = /* @__PURE__ */ _export_sfc(_sfc_main$17, [["render", _sfc_render$16], ["__scopeId", "data-v-c7d66d43"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-name-picker/common-name-picker.vue"]]);
  const _sfc_main$16 = {
    __name: "custom-picker",
    props: {
      dataList: {
        type: Array,
        required: true
      },
      margin: {
        type: String,
        default: "0"
      }
    },
    emits: ["select"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const inputValue = vue.ref("");
      const isOpen = vue.ref(false);
      const filteredList = vue.computed(() => {
        return props.dataList.filter(
          (item) => item.name.toLowerCase().includes(inputValue.value.toLowerCase())
        );
      });
      const callbackValue = () => {
        emit("select", 0, inputValue.value);
      };
      const toggleOpen = () => {
        isOpen.value = !isOpen.value;
      };
      const selectOption = (item) => {
        inputValue.value = item.name;
        isOpen.value = false;
        emit("select", item.id, item.name);
      };
      const __returned__ = { props, emit, inputValue, isOpen, filteredList, callbackValue, toggleOpen, selectOption, ref: vue.ref, computed: vue.computed };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$15(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "custom-select" }, [
      vue.withDirectives(vue.createElementVNode(
        "input",
        {
          "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.inputValue = $event),
          onClick: $setup.toggleOpen,
          onInput: $setup.callbackValue,
          placeholder: "请选择或输入",
          class: "select-input",
          style: vue.normalizeStyle({ margin: $props.margin })
        },
        null,
        36
        /* STYLE, NEED_HYDRATION */
      ), [
        [vue.vModelText, $setup.inputValue]
      ]),
      $setup.isOpen ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "dropdown"
      }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.filteredList, (item) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: item.id,
              onClick: ($event) => $setup.selectOption(item),
              class: "dropdown-item"
            }, vue.toDisplayString(item.name), 9, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_3$1 = /* @__PURE__ */ _export_sfc(_sfc_main$16, [["render", _sfc_render$15], ["__scopeId", "data-v-edb43aad"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/custom-picker/custom-picker.vue"]]);
  const _sfc_main$15 = {
    __name: "relation-picker",
    props: {
      typeList: {
        type: Array,
        default: () => []
      },
      visible: {
        type: Boolean,
        default: false
      },
      hideType: {
        type: Boolean,
        default: false
      }
    },
    emits: [
      "update:visible",
      "confirm",
      "cancel"
    ],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const goodsList = vue.ref([]);
      const typePicker = vue.ref();
      const brandSearch = vue.ref();
      const goodsPicker = vue.ref();
      const fuzzySearch = vue.ref();
      const internalVisible = vue.ref(false);
      const isFuzzyMode = vue.ref(true);
      const selectedData = vue.ref({
        type: null,
        brand: {
          id: 0,
          name: ""
        },
        goods: {
          id: 0,
          name: "",
          image: ""
        }
      });
      const searchKeyword = vue.ref("");
      const switchMode = (isFuzzy) => {
        isFuzzyMode.value = isFuzzy;
        resetAllComponents();
        selectedData.value = {
          type: null,
          brand: {
            id: 0,
            name: ""
          },
          goods: {
            id: 0,
            name: "",
            image: ""
          }
        };
      };
      const handleComponentToggle = (component) => {
        const components = {
          type: typePicker,
          brand: brandSearch,
          goods: goodsPicker,
          fuzzy: fuzzySearch
        };
        Object.entries(components).forEach(([key, ref]) => {
          var _a;
          if (key !== component && ((_a = ref.value) == null ? void 0 : _a.close)) {
            ref.value.close();
          }
        });
      };
      const resetAllComponents = () => {
        [typePicker, brandSearch, goodsPicker, fuzzySearch].forEach((comp) => {
          var _a;
          if ((_a = comp.value) == null ? void 0 : _a.reset)
            comp.value.reset();
        });
      };
      const handleFuzzySelect = async (goods) => {
        var _a;
        try {
          if (!(goods == null ? void 0 : goods.id)) {
            searchKeyword.value = goods.name;
            selectedData.value = {
              type: "未知类型",
              brand: {
                id: 0,
                name: ""
              },
              goods: {
                id: 0,
                name: goods.name,
                image: ""
              }
            };
            return;
          }
          const detail = await getGoodsInfo(goods.id);
          searchKeyword.value = goods.name;
          selectedData.value = {
            type: detail.type || "未知类型",
            brand: {
              id: detail.brand_id || 0,
              name: detail.brand_name || ""
            },
            goods: {
              id: goods.id,
              name: goods.name,
              image: ((_a = detail == null ? void 0 : detail.goods_images) == null ? void 0 : _a[0]) || ""
            }
          };
        } catch (error2) {
          formatAppLog("error", "at components/relation-picker/relation-picker.vue:206", "商品信息获取失败", error2);
          uni.showToast({
            title: "详细信息获取失败，已保存基本信息",
            icon: "none"
          });
          searchKeyword.value = goods.name;
          selectedData.value = {
            type: "未知类型",
            brand: {
              id: 0,
              name: ""
            },
            goods: {
              id: (goods == null ? void 0 : goods.id) || 0,
              name: goods.name,
              image: ""
            }
          };
        }
      };
      const closePicker = () => {
        internalVisible.value = false;
        searchKeyword.value = "";
        resetAllComponents();
        vue.nextTick(() => {
          selectedData.value = {
            type: null,
            brand: {
              id: 0,
              name: ""
            },
            goods: {
              id: 0,
              name: "",
              image: ""
            }
          };
        });
      };
      vue.watch(() => props.visible, (val) => {
        internalVisible.value = val;
      });
      vue.watch(internalVisible, (val) => {
        emit("update:visible", val);
      });
      const handleModalVisibilityChange = (newVal) => {
        internalVisible.value = newVal;
        if (!newVal) {
          closePicker();
        }
      };
      const handleTypeSelect = (type) => {
        selectedData.value.type = type;
      };
      const getGoodsInfo = (id) => {
        return new Promise((resolve, reject) => {
          uni.request({
            url: websiteUrl.value + "/goods?id=" + id,
            method: "GET",
            timeout: 5e3,
            success: (res) => {
              resolve(res.data.data);
            },
            fail: (err) => {
              formatAppLog("error", "at components/relation-picker/relation-picker.vue:290", "商品详情获取失败", err);
              reject(err);
            }
          });
        });
      };
      const handleBrandSelect = (id, name) => {
        selectedData.value.brand = {
          id: id || 0,
          name
        };
        if (id)
          getGoods(id);
      };
      const handleGoodsSelect = async (id, name) => {
        var _a;
        try {
          selectedData.value.goods = {
            // 先设置默认值
            id: id || 0,
            name,
            image: ""
          };
          if (id && id !== 0) {
            const detail = await getGoodsInfo(id);
            selectedData.value.goods.image = ((_a = detail == null ? void 0 : detail.goods_images) == null ? void 0 : _a[0]) || "";
          }
        } catch (error2) {
          formatAppLog("error", "at components/relation-picker/relation-picker.vue:356", "商品信息获取失败", error2);
          uni.showToast({
            title: "图片信息获取失败，已保存基本信息",
            icon: "none"
          });
        }
      };
      function getGoods(id) {
        uni.request({
          url: websiteUrl.value + "/goods-name-list?id=" + id,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            formatAppLog("log", "at components/relation-picker/relation-picker.vue:371", res.data.data);
            goodsList.value = res.data.data;
            formatAppLog("log", "at components/relation-picker/relation-picker.vue:373", goodsList.value);
          },
          fail: (err) => {
            formatAppLog("log", "at components/relation-picker/relation-picker.vue:377", err);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      }
      const handleConfirm = async () => {
        var _a, _b;
        try {
          let result = null;
          if (isFuzzyMode.value) {
            if (selectedData.value.goods.id && selectedData.value.goods.id !== 0) {
              try {
                const detail = await getGoodsInfo(selectedData.value.goods.id);
                result = {
                  isFuzzy: true,
                  keyword: searchKeyword.value,
                  goods: {
                    id: detail.id || selectedData.value.goods.id,
                    name: detail.goods_name || selectedData.value.goods.name,
                    image: ((_a = detail == null ? void 0 : detail.goods_images) == null ? void 0 : _a[0]) || ""
                  },
                  brand: {
                    id: detail.brand_id || 0,
                    name: detail.brand_name || ""
                  },
                  // 添加默认类型
                  type: detail.type || "未知类型"
                  // 确保这里使用默认值
                };
              } catch (error2) {
                formatAppLog("error", "at components/relation-picker/relation-picker.vue:439", "商品信息获取失败，使用缓存数据", error2);
                result = {
                  isFuzzy: true,
                  keyword: searchKeyword.value,
                  goods: selectedData.value.goods,
                  brand: selectedData.value.brand,
                  // 确保这里使用默认值
                  type: selectedData.value.type || "未知类型"
                };
              }
            } else {
              result = {
                isFuzzy: true,
                keyword: searchKeyword.value,
                goods: {
                  id: 0,
                  name: searchKeyword.value,
                  image: ""
                },
                brand: {
                  id: 0,
                  name: ""
                },
                // 明确设置默认类型
                type: "未知类型"
              };
            }
          } else {
            formatAppLog("log", "at components/relation-picker/relation-picker.vue:469", "jjjjjjjjjjjjjjjj");
            if (selectedData.value.goods.id && selectedData.value.goods.id !== 0) {
              try {
                const detail = await getGoodsInfo(selectedData.value.goods.id);
                selectedData.value.goods.image = ((_b = detail == null ? void 0 : detail.goods_images) == null ? void 0 : _b[0]) || "";
                selectedData.value.type = detail.type || selectedData.value.type;
              } catch (error2) {
                formatAppLog("error", "at components/relation-picker/relation-picker.vue:478", "商品信息获取失败，使用缓存数据", error2);
              }
            }
            if (!selectedData.value.type)
              throw new Error("请选择商品类型");
            if (!selectedData.value.brand.name)
              throw new Error("请填写或选择品牌");
            if (!selectedData.value.goods.name)
              throw new Error("请填写或选择商品");
            result = {
              isFuzzy: false,
              type: selectedData.value.type,
              brand: selectedData.value.brand,
              goods: selectedData.value.goods
            };
          }
          formatAppLog("log", "at components/relation-picker/relation-picker.vue:494", "确认数据:", result);
          emit("confirm", result);
          closePicker();
        } catch (error2) {
          uni.showToast({
            title: error2.message,
            icon: "none"
          });
        }
      };
      const handleCancel = () => {
        emit("cancel");
        closePicker();
      };
      const __returned__ = { props, emit, goodsList, typePicker, brandSearch, goodsPicker, fuzzySearch, internalVisible, isFuzzyMode, selectedData, searchKeyword, switchMode, handleComponentToggle, resetAllComponents, handleFuzzySelect, closePicker, handleModalVisibilityChange, handleTypeSelect, getGoodsInfo, handleBrandSelect, handleGoodsSelect, getGoods, handleConfirm, handleCancel, ref: vue.ref, watch: vue.watch, nextTick: vue.nextTick, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get getScene() {
        return getScene;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$14(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_name_picker = resolveEasycom(vue.resolveDynamicComponent("common-name-picker"), __easycom_1$4);
    const _component_common_search = resolveEasycom(vue.resolveDynamicComponent("common-search"), __easycom_2$a);
    const _component_custom_picker = resolveEasycom(vue.resolveDynamicComponent("custom-picker"), __easycom_3$1);
    const _component_goods_search = resolveEasycom(vue.resolveDynamicComponent("goods-search"), __easycom_3$2);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createBlock(_component_common_modal, {
      visible: $setup.internalVisible,
      "onUpdate:visible": $setup.handleModalVisibilityChange,
      top: "100rpx"
    }, {
      default: vue.withCtx(() => [
        vue.createElementVNode("view", { class: "relation-picker-container" }, [
          vue.createElementVNode("view", { class: "picker-header" }, [
            vue.createElementVNode("view", { class: "mode-switch" }, [
              $setup.isFuzzyMode ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "switch-btn precision-mode",
                onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchMode(false))
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "search",
                  size: "18",
                  color: "#fff"
                }),
                vue.createElementVNode("text", null, "切换到精确关联")
              ])) : vue.createCommentVNode("v-if", true),
              !$setup.isFuzzyMode ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "switch-btn fuzzy-mode",
                onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchMode(true))
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "list",
                  size: "18",
                  color: "#fff"
                }),
                vue.createElementVNode("text", null, "切换到模糊关联")
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          vue.createElementVNode("view", { class: "picker-body" }, [
            !$setup.isFuzzyMode ? (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              { key: 0 },
              [
                !$props.hideType ? (vue.openBlock(), vue.createBlock(_component_common_name_picker, {
                  key: 0,
                  ref: "typePicker",
                  dataList: $props.typeList,
                  onSelect: $setup.handleTypeSelect,
                  onToggle: _cache[2] || (_cache[2] = ($event) => $setup.handleComponentToggle("type")),
                  class: "type-picker"
                }, null, 8, ["dataList"])) : vue.createCommentVNode("v-if", true),
                vue.createVNode(
                  _component_common_search,
                  {
                    ref: "brandSearch",
                    background: "#f8f8f8",
                    mode: "fill",
                    onSelect: $setup.handleBrandSelect,
                    onToggle: _cache[3] || (_cache[3] = ($event) => $setup.handleComponentToggle("brand")),
                    class: "brand-search",
                    style: { "padding": "0px!important" }
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ),
                vue.createVNode(_component_custom_picker, {
                  ref: "goodsPicker",
                  background: "#f8f8f8",
                  dataList: $setup.goodsList,
                  onSelect: $setup.handleGoodsSelect,
                  onToggle: _cache[4] || (_cache[4] = ($event) => $setup.handleComponentToggle("goods")),
                  class: "goods-picker"
                }, null, 8, ["dataList"])
              ],
              64
              /* STABLE_FRAGMENT */
            )) : (vue.openBlock(), vue.createBlock(_component_goods_search, {
              key: 1,
              ref: "fuzzySearch",
              mode: "fill",
              onSelect: $setup.handleFuzzySelect,
              modelValue: $setup.searchKeyword,
              "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.searchKeyword = $event),
              width: "550rpx",
              onToggle: _cache[6] || (_cache[6] = ($event) => $setup.handleComponentToggle("fuzzy")),
              class: "fuzzy-search"
            }, null, 8, ["modelValue"]))
          ]),
          vue.createElementVNode("view", { class: "picker-footer" }, [
            vue.createElementVNode("button", {
              class: "cancel-btn",
              onClick: $setup.handleCancel
            }, "取消"),
            vue.createElementVNode("button", {
              class: "confirm-btn",
              onClick: $setup.handleConfirm
            }, "确认")
          ])
        ])
      ]),
      _: 1
      /* STABLE */
    }, 8, ["visible"]);
  }
  const __easycom_1$3 = /* @__PURE__ */ _export_sfc(_sfc_main$15, [["render", _sfc_render$14], ["__scopeId", "data-v-0488033a"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/relation-picker/relation-picker.vue"]]);
  const _imports_0$7 = "/static/add2.png";
  const _sfc_main$14 = {
    __name: "collocation",
    props: ["goods_id", "goods_name", "brand_id", "brand_name", "type", "collocation_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const isEdit = vue.ref(false);
      const uploadList = vue.ref([]);
      const goodsList = vue.ref([]);
      const typeList = vue.ref([]);
      const showSelectTab = vue.ref(false);
      const saveCollocationDataList = vue.ref([]);
      const title = vue.ref("");
      const content = vue.ref("");
      async function fetchCollocationDetail() {
        if (!props.collocation_id)
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/view-collocation?collocation_id=${props.collocation_id}&origin=1`,
            method: "GET",
            timeout: 5e3
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            title.value = data.title || "";
            content.value = data.content || "";
            uploadList.value = data.image_url_list || [];
            if (data.collocation_relation_list && data.collocation_relation_list.length > 0) {
              saveCollocationDataList.value = data.collocation_relation_list.map((item) => ({
                goods_id: item.relation_goods_id,
                goods_name: item.relation_goods_name,
                brand_id: item.relation_brand_id,
                brand_name: item.relation_brand_name,
                type: item.type,
                goods_image: item.relation_goods_image || ""
              }));
            }
          }
        } catch (error2) {
          formatAppLog("error", "at pages/collocation/collocation.vue:122", "获取搭配详情失败:", error2);
          uni.showToast({
            title: "加载搭配详情失败",
            icon: "none"
          });
        }
      }
      vue.onMounted(() => {
        getTypes();
        if (props.collocation_id) {
          isEdit.value = true;
          fetchCollocationDetail();
        }
        if (props.goods_id && props.goods_name && props.brand_id && props.brand_name && props.type) {
          getGoodsInfo(props.goods_id).then((res) => {
            let data = {
              "brand_id": parseInt(props.brand_id, 10),
              "goods_id": parseInt(props.goods_id, 10),
              "brand_name": props.brand_name,
              "goods_name": props.goods_name,
              "goods_image": res.data.goods_images[0],
              "type": props.type
            };
            const exists = saveCollocationDataList.value.some(
              (item) => item.goods_id === data.goods_id && item.brand_id === data.brand_id
            );
            if (!exists) {
              saveCollocationDataList.value.push(data);
            }
          });
        }
      });
      const handleRelationConfirm = (data) => {
        try {
          const relationData = {
            goods_id: data.goods.id || 0,
            goods_name: data.goods.name,
            goods_image: data.goods.image || "",
            brand_id: data.brand.id || 0,
            brand_name: data.brand.name || (data.isFuzzy ? "" : "未知品牌"),
            type: data.type || (data.isFuzzy ? "未知类型" : "")
          };
          const isExist = saveCollocationDataList.value.some(
            (item) => item.goods_id !== 0 && item.goods_id === relationData.goods_id || item.goods_name === relationData.goods_name
          );
          if (!isExist) {
            saveCollocationDataList.value.push(relationData);
          } else {
            uni.showToast({
              title: "已存在相同关联项",
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pages/collocation/collocation.vue:189", "保存关联数据失败:", error2);
          uni.showToast({
            title: "保存关联信息失败",
            icon: "none"
          });
        }
      };
      const handleRelationCancel = () => {
        formatAppLog("log", "at pages/collocation/collocation.vue:199", "用户取消选择");
      };
      const showRelationPicker = () => {
        showSelectTab.value = true;
      };
      function getGoodsInfo(id) {
        return new Promise((resolve, reject) => {
          uni.request({
            url: websiteUrl.value + "/goods?id=" + id,
            method: "GET",
            timeout: 5e3,
            success: (res) => {
              resolve(res.data);
            },
            fail: (err) => {
              formatAppLog("log", "at pages/collocation/collocation.vue:217", err);
              uni.showToast({
                title: "网络请求失败",
                icon: "none"
              });
              reject(err);
            }
          });
        });
      }
      function viewFullImage(index) {
        uni.previewImage({
          current: uploadList.value[index],
          urls: uploadList.value
        });
      }
      async function selectImage() {
        try {
          const remaining = 9 - uploadList.value.length;
          if (remaining <= 0) {
            uni.showToast({
              title: "最多只能上传9张图片",
              icon: "none"
            });
            return;
          }
          const imagePaths = await chooseImageList(remaining);
          for (const path of imagePaths) {
            const tokenData = await getQiniuToken();
            await uploadImageToQiniu(path, tokenData.token, tokenData.path);
            uploadList.value.push(image1Url + tokenData.path);
          }
          uni.showToast({
            title: `成功上传${imagePaths.length}张图片`,
            icon: "success"
          });
        } catch (error2) {
          formatAppLog("error", "at pages/collocation/collocation.vue:268", "上传出错:", error2);
          uni.showToast({
            title: "部分图片上传失败",
            icon: "none"
          });
        }
      }
      async function submitForm() {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        if (!title.value.trim()) {
          uni.showToast({
            title: "标题不能为空",
            icon: "none"
          });
          return;
        }
        if (uploadList.value.length === 0) {
          uni.showToast({
            title: "请至少上传一张图片",
            icon: "none"
          });
          return;
        }
        const postData = {
          title: title.value,
          content: content.value,
          image_url_list: uploadList.value,
          relations: saveCollocationDataList.value.map((item) => ({
            relation_goods_id: item.goods_id,
            relation_goods_name: item.goods_name,
            relation_brand_id: item.brand_id,
            relation_brand_name: item.brand_name,
            type: item.type,
            relation_origin: 1
            // 标识关联的是collocation
          }))
        };
        if (isEdit.value) {
          postData.collocation_id = parseInt(props.collocation_id);
        }
        try {
          const url = isEdit.value ? websiteUrl.value + "/with-state/update-collocation" : websiteUrl.value + "/with-state/add-collocation";
          const res = await uni.request({
            url,
            method: "POST",
            data: postData,
            header: {
              "Content-Type": "application/json",
              "Authorization": token
            },
            timeout: 8e3
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: isEdit.value ? "更新成功" : "提交成功"
            });
            setTimeout(() => uni.navigateBack(), 1500);
          } else {
            uni.showToast({
              title: res[1].data.msg || (isEdit.value ? "更新失败" : "提交失败"),
              icon: "none"
            });
          }
        } catch (err) {
          formatAppLog("log", "at pages/collocation/collocation.vue:351", err);
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        }
      }
      function getTypes() {
        uni.request({
          url: websiteUrl.value + "/goods-types",
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            if (res.data && res.data.data) {
              typeList.value = res.data.data;
            }
          },
          fail: (err) => {
            formatAppLog("log", "at pages/collocation/collocation.vue:371", err);
            uni.showToast({
              title: "获取商品类型失败",
              icon: "none"
            });
          }
        });
      }
      function deleteImage(index) {
        uploadList.value.splice(index, 1);
      }
      function deleteCollcation(index) {
        saveCollocationDataList.value.splice(index, 1);
      }
      const __returned__ = { props, isEdit, uploadList, goodsList, typeList, showSelectTab, saveCollocationDataList, title, content, fetchCollocationDetail, handleRelationConfirm, handleRelationCancel, showRelationPicker, getGoodsInfo, viewFullImage, selectImage, submitForm, getTypes, deleteImage, deleteCollcation, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, get chooseImageList() {
        return chooseImageList;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$13(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_relation_picker = resolveEasycom(vue.resolveDynamicComponent("relation-picker"), __easycom_1$3);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { style: { "width": "100%", "overflow": "hidden" } }, [
        vue.createElementVNode("scroll-view", {
          "scroll-x": "true",
          class: "upload_box",
          "ll-with-animation": "true",
          "show-scrollbar": false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.uploadList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: "upload_item",
                key: index
              }, [
                vue.createElementVNode("image", {
                  src: item,
                  class: "uploaded_image",
                  onClick: ($event) => $setup.viewFullImage(index),
                  mode: "aspectFill"
                }, null, 8, ["src", "onClick"]),
                vue.createElementVNode("image", {
                  src: _imports_2$5,
                  class: "close_icon",
                  onClick: ($event) => $setup.deleteImage(index)
                }, null, 8, ["onClick"])
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          $setup.uploadList.length < 9 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "uploadImageBox",
            style: { "background": "#f8f8f8" }
          }, [
            vue.createElementVNode("image", {
              src: _imports_0$7,
              class: "upload_img",
              onClick: $setup.selectImage,
              style: { "width": "50px", "height": "50px", "margin": "25px" }
            })
          ])) : vue.createCommentVNode("v-if", true)
        ])
      ]),
      vue.withDirectives(vue.createElementVNode(
        "input",
        {
          "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.title = $event),
          type: "text",
          placeholder: "请输入标题",
          style: { "padding": "10px", "margin": "20px 15px 5px 15px", "display": "block" }
        },
        null,
        512
        /* NEED_PATCH */
      ), [
        [vue.vModelText, $setup.title]
      ]),
      vue.createElementVNode("view", { class: "oneLine" }),
      vue.withDirectives(vue.createElementVNode(
        "textarea",
        {
          "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.content = $event),
          placeholder: "请输入正文",
          style: { "padding": "10px", "margin": "10px 15px 5px 15px", "display": "block", "line-height": "28px", "width": "calc(100% - 50px)" }
        },
        null,
        512
        /* NEED_PATCH */
      ), [
        [vue.vModelText, $setup.content]
      ]),
      vue.createElementVNode("view", { class: "oneLine" }),
      vue.createElementVNode("view", { class: "" }, [
        vue.createElementVNode("view", {
          class: "relation-trigger",
          onClick: $setup.showRelationPicker
        }, [
          vue.createElementVNode("text", { class: "placeholder" }, "点击关联娃物"),
          vue.createElementVNode("image", {
            src: _imports_1$5,
            class: "arrow-icon"
          })
        ])
      ]),
      vue.createElementVNode("view", { class: "publish-detail" }, [
        vue.createElementVNode("text", null, "* 分享您对于打扮BJD的想法✨。"),
        vue.createElementVNode("text", null, "* 不关联商品的搭配不会出现在广场中。"),
        vue.createElementVNode("text", null, "* 可以是BJD相关的贩售广告，但不可以滥用关联。")
      ]),
      vue.createElementVNode("view", { class: "saveCollocationDataList" }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.saveCollocationDataList, (item, index) => {
            return vue.openBlock(), vue.createElementBlock("view", { key: index }, [
              vue.createElementVNode("view", { class: "saveCollocationDataItem" }, [
                item.goods_image ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  src: item.goods_image,
                  mode: "aspectFill",
                  style: { "width": "70px", "height": "70px" }
                }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("text", {
                  key: 1,
                  class: "no-image"
                }, "?")),
                vue.createElementVNode(
                  "text",
                  {
                    class: "info-tap",
                    style: { "width": "calc(100% - 120px)", "display": "inline-block" }
                  },
                  vue.toDisplayString(item.type) + " " + vue.toDisplayString(item.brand_name) + " - " + vue.toDisplayString(item.goods_name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("image", {
                  src: _imports_2$5,
                  class: "close_icon",
                  onClick: ($event) => $setup.deleteCollcation(index)
                }, null, 8, ["onClick"])
              ])
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createVNode(_component_relation_picker, {
        visible: $setup.showSelectTab,
        "onUpdate:visible": _cache[2] || (_cache[2] = ($event) => $setup.showSelectTab = $event),
        typeList: $setup.typeList,
        goodsList: $setup.goodsList,
        onConfirm: $setup.handleRelationConfirm,
        onCancel: $setup.handleRelationCancel
      }, null, 8, ["visible", "typeList", "goodsList"]),
      vue.createElementVNode("view", { class: "footer" }, [
        vue.createElementVNode(
          "button",
          { onClick: $setup.submitForm },
          vue.toDisplayString($setup.isEdit ? "更新" : "提交"),
          1
          /* TEXT */
        )
      ])
    ]);
  }
  const PagesCollocationCollocation = /* @__PURE__ */ _export_sfc(_sfc_main$14, [["render", _sfc_render$13], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/collocation/collocation.vue"]]);
  const _sfc_main$13 = {
    __name: "collocation_share",
    props: {
      collocation_id: {
        type: String,
        default: 0
      },
      origin: {
        type: String,
        default: 0
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const detailData = vue.ref({
        title: "",
        content: "",
        image_url_list: [],
        collocation_relation_list: []
      });
      const authorInfo = vue.ref({
        userName: "",
        avatar: ""
      });
      const systemInfo2 = uni.getSystemInfoSync();
      const loading2 = vue.ref(true);
      const error2 = vue.ref(false);
      const errorMsg = vue.ref("");
      const props = __props;
      const pageId = vue.ref(0);
      const origin = vue.ref(0);
      let hasLike = vue.ref(false);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      let commentsPage = vue.ref(1);
      let replyForItem = vue.ref({});
      uni.setNavigationBarTitle({
        title: "搭配详情"
      });
      function jump2user(uid) {
        uni.navigateTo({
          url: "/pages/user_page/user_page?uid=" + uid
        });
      }
      const getGoodsInfo = (id) => {
        return new Promise((resolve, reject) => {
          if (!id || id === 0) {
            reject("无效的商品ID");
            return;
          }
          uni.request({
            url: `${websiteUrl.value}/goods?id=${id}`,
            method: "GET",
            timeout: 5e3,
            success: (res) => {
              if (res.data.status === "success") {
                resolve(res.data.data);
              } else {
                reject(res.data.msg || "获取商品信息失败");
              }
            },
            fail: (err) => {
              formatAppLog("error", "at pages/collocation_share/collocation_share.vue:214", err);
              uni.showToast({
                title: "网络请求失败",
                icon: "none"
              });
              reject(err);
            }
          });
        });
      };
      function viewFullImage(index) {
        formatAppLog("log", "at pages/collocation_share/collocation_share.vue:227", detailData);
        uni.previewImage({
          current: detailData.value.image_url_list[index],
          urls: detailData.value.image_url_list
        });
      }
      const fetchData = async (id, origin2) => {
        var _a;
        try {
          const res = await uni.request({
            url: websiteUrl.value + `/view-collocation?collocation_id=${id}&origin=${origin2}`
          });
          if (res.data.status !== "success") {
            handleError(res.data.msg || "数据加载失败");
            return;
          }
          const processedData = {
            ...res.data.data,
            image_url_list: Array.isArray(res.data.data.image_url_list) ? res.data.data.image_url_list : [res.data.data.image_urls],
            collocation_relation_list: await Promise.all(
              res.data.data.collocation_relation_list.map(async (item) => {
                try {
                  const goodsInfo = item.relation_goods_id > 0 ? await getGoodsInfo(item.relation_goods_id) : null;
                  return {
                    ...item,
                    goodsInfo,
                    imageLoaded: !!goodsInfo,
                    imageError: !goodsInfo
                  };
                } catch (error22) {
                  formatAppLog("error", "at pages/collocation_share/collocation_share.vue:266", "商品信息获取失败:", error22);
                  return {
                    ...item,
                    goodsInfo: null,
                    imageLoaded: false,
                    imageError: true
                  };
                }
              })
            )
          };
          detailData.value = processedData;
          if ((_a = res.data.data) == null ? void 0 : _a.uid) {
            await getAuthorInfo(res.data.data.uid);
          }
        } catch (error22) {
          handleError("数据加载失败");
        } finally {
          loading2.value = false;
        }
      };
      const navigateToRelated = (item) => {
        if (item.relation_goods_id > 0) {
          uni.navigateTo({
            url: `/pages/goods/goods?goods_id=${item.relation_goods_id}`
          });
        } else if (item.relation_brand_id > 0) {
          uni.navigateTo({
            url: `/pages/brand/brand?brand_id=${item.relation_brand_id}`
          });
        } else {
          uni.showToast({
            title: "暂无商品和品牌信息",
            icon: "none"
          });
        }
      };
      const getAuthorInfo = async (uid) => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/user-info?uid=${uid}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            authorInfo.value = res.data.data;
          } else {
            formatAppLog("error", "at pages/collocation_share/collocation_share.vue:325", "获取用户信息失败:", res.data.msg);
          }
        } catch (error22) {
          formatAppLog("error", "at pages/collocation_share/collocation_share.vue:328", "用户信息请求失败:", error22);
          uni.showToast({
            title: "作者信息加载失败",
            icon: "none"
          });
        }
      };
      const navigateToUser = (uid) => {
        if (uid) {
          uni.navigateTo({
            url: `/pages/user/profile?uid=${uid}`
          });
        }
      };
      const handleError = (msg) => {
        error2.value = true;
        errorMsg.value = msg;
        uni.showToast({
          title: msg,
          icon: "none"
        });
      };
      function likeFn() {
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        let type = origin.value == 1 ? 3 : 4;
        let requestData = {
          id: parseInt(detailData.value.id),
          type
        };
        let url = hasLike.value ? "/with-state/unlike" : "/with-state/add-like";
        uni.request({
          url: websiteUrl.value + url,
          method: "POST",
          header: {
            "Authorization": token
          },
          data: requestData,
          success: (res) => {
            formatAppLog("log", "at pages/collocation_share/collocation_share.vue:383", res.data);
            if (res.data.status == "success") {
              uni.showToast({
                title: "操作成功",
                icon: "success"
              });
              hasLike.value = !hasLike.value;
              getHasLike(detailData.value.id);
              fetchData(detailData.value.id, origin.value);
              return;
            } else {
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
              return;
            }
          },
          fail: (err) => {
            formatAppLog("log", "at pages/collocation_share/collocation_share.vue:402", err);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      }
      function getHasLike(id) {
        let token = uni.getStorageSync("token");
        if (token == "") {
          return;
        }
        let type = origin.value == 1 ? 3 : 4;
        uni.request({
          url: websiteUrl.value + "/with-state/hasLike?id=" + id + "&type=" + type,
          method: "POST",
          header: {
            "Authorization": token
          },
          success: (res) => {
            if (res.data.status == "success") {
              if (res.data.data.id > 0) {
                hasLike.value = true;
              } else {
                hasLike.value = false;
              }
            } else {
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
              return;
            }
          },
          fail: (err) => {
            formatAppLog("log", "at pages/collocation_share/collocation_share.vue:442", err);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      }
      function formatNumber(num) {
        if (num < 1e3) {
          return num.toString();
        } else {
          const kValue = Math.floor(num / 1e3);
          return `${kValue}k+`;
        }
      }
      const handleReplyComment = ({
        parent,
        target
      }) => {
        var _a;
        formatAppLog("log", "at pages/collocation_share/collocation_share.vue:466", "parent", parent);
        formatAppLog("log", "at pages/collocation_share/collocation_share.vue:467", "target", target);
        let item = parent;
        if (target != null) {
          item = target;
        }
        if (replyForItem.value.id == item.id) {
          replyForItem.value = {};
          return;
        }
        formatAppLog("log", "at pages/collocation_share/collocation_share.vue:478", "item", item);
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      const onShareAppMessage2 = () => ({
        title: "BJD娃圈你想知道的这里都有~",
        path: "/pages/news/news",
        success(res) {
          formatAppLog("log", "at pages/collocation_share/collocation_share.vue:488", "分享成功", res);
        },
        fail(err) {
          formatAppLog("log", "at pages/collocation_share/collocation_share.vue:491", "分享失败", err);
        },
        mp: {
          wxpath: "/pages/index/index.html"
        }
      });
      const handleCommentSubmit = (submitData) => {
        var _a, _b, _c;
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        const commentType = origin.value == 1 ? 3 : 6;
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: parseInt(pageId.value),
          type: commentType,
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          // 临时ID
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          // 临时楼层数
          // 匿名处理
          ...submitData.is_anonymous ? {
            avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
            username: "匿名用户",
            is_anonymous: 1
          } : {
            avatar: global$1.userInfo.avatar,
            username: global$1.userInfo.nickname,
            is_anonymous: 0
          },
          // 关联信息
          ...submitData.association_id && {
            association_id: submitData.association_id,
            association_type: submitData.association_type
          },
          // 图片信息
          ...submitData.image_url && {
            image_url: submitData.image_url
          },
          // 回复信息
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            // 处理被回复者的匿名状态
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id) {
          (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(tempComment);
        } else if (replyForItem.value.parent_id === 0) {
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.id
          });
        } else {
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.parent_id
          });
        }
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: {
            "Authorization": token
          },
          data: requestData,
          success: (res) => {
            var _a2, _b2;
            if (res.data.status == "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? {
                  avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
                  username: "匿名用户",
                  is_anonymous: 1
                } : {
                  avatar: global$1.userInfo.avatar,
                  username: global$1.userInfo.nickname,
                  is_anonymous: 0
                }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous) {
                finalComment.reply_username = "匿名用户";
              }
              (_a2 = commentListRef.value) == null ? void 0 : _a2.updateTempComment(tempComment.id, finalComment);
              uni.showToast({
                title: "评论成功",
                icon: "success"
              });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
            }
          },
          fail: (err) => {
            var _a2;
            (_a2 = commentListRef.value) == null ? void 0 : _a2.removeTempComment(tempComment.id);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      };
      function formatTimestamp(timestamp) {
        const date = new Date(timestamp * 1e3);
        const year = date.getFullYear();
        const month = (date.getMonth() + 1).toString().padStart(2, "0");
        const day = date.getDate().toString().padStart(2, "0");
        const hours = date.getHours().toString().padStart(2, "0");
        const minutes = date.getMinutes().toString().padStart(2, "0");
        date.getSeconds().toString().padStart(2, "0");
        return `${year}-${month}-${day} ${hours}:${minutes}`;
      }
      onShow(() => {
        formatAppLog("log", "at pages/collocation_share/collocation_share.vue:674", "注册键盘弹出事件");
        uni.onKeyboardHeightChange(keyboardHeightChangeHandler);
      });
      onLoad((options) => {
        try {
          if (!options.collocation_id) {
            error2.value = true;
            errorMsg.value = "缺少必要参数Id";
            return;
          }
          if (!options.origin) {
            error2.value = true;
            errorMsg.value = "缺少必要参数Origin";
            return;
          }
          pageId.value = options.collocation_id;
          origin.value = options.origin;
          fetchData(options.collocation_id, options.origin);
          asyncGetUserInfo().then((userInfo) => {
            formatAppLog("log", "at pages/collocation_share/collocation_share.vue:697", userInfo);
            getHasLike(options.collocation_id);
          });
        } catch (err) {
          formatAppLog("error", "at pages/collocation_share/collocation_share.vue:703", "onLoad Error:", err);
          uni.showToast({
            title: "加载失败",
            icon: "none"
          });
        }
      });
      const __returned__ = { detailData, authorInfo, systemInfo: systemInfo2, loading: loading2, error: error2, errorMsg, props, pageId, origin, get hasLike() {
        return hasLike;
      }, set hasLike(v2) {
        hasLike = v2;
      }, commentListRef, commentInputRef, get commentsPage() {
        return commentsPage;
      }, set commentsPage(v2) {
        commentsPage = v2;
      }, get replyForItem() {
        return replyForItem;
      }, set replyForItem(v2) {
        replyForItem = v2;
      }, jump2user, getGoodsInfo, viewFullImage, fetchData, navigateToRelated, getAuthorInfo, navigateToUser, handleError, likeFn, getHasLike, formatNumber, handleReplyComment, onShareAppMessage: onShareAppMessage2, handleCommentSubmit, formatTimestamp, ref: vue.ref, onMounted: vue.onMounted, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get onHide() {
        return onHide;
      }, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get getScene() {
        return getScene;
      }, get chooseImage() {
        return chooseImage;
      }, get jumpToCroper() {
        return jumpToCroper;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$12(_ctx, _cache, $props, $setup, $data, $options) {
    var _a;
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_report_button = resolveEasycom(vue.resolveDynamicComponent("report-button"), __easycom_1$9);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { style: { "position": "relative" } }, [
        vue.createElementVNode("view", {
          class: "heart",
          onClick: _cache[0] || (_cache[0] = ($event) => $setup.likeFn())
        }, [
          !$setup.hasLike ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
            key: 0,
            type: "heart",
            size: "28",
            color: "#ff4d4f"
          })) : (vue.openBlock(), vue.createBlock(_component_uni_icons, {
            key: 1,
            type: "heart-filled",
            size: "28",
            color: "#ff4d4f"
          })),
          vue.createElementVNode(
            "text",
            { class: "num" },
            vue.toDisplayString(((_a = $setup.detailData) == null ? void 0 : _a.like_count) ? $setup.formatNumber($setup.detailData.like_count) : 0),
            1
            /* TEXT */
          )
        ]),
        vue.createElementVNode("swiper", {
          class: "swiper-box",
          "indicator-dots": true,
          autoplay: false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.detailData.image_url_list, (img, index) => {
              return vue.openBlock(), vue.createElementBlock("swiper-item", { key: index }, [
                vue.createElementVNode("image", {
                  src: img,
                  mode: "aspectFill",
                  class: "swiper-image",
                  onClick: ($event) => $setup.viewFullImage(index)
                }, null, 8, ["src", "onClick"])
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ]),
      vue.createElementVNode("view", {
        class: "author-info",
        onClick: _cache[1] || (_cache[1] = ($event) => $setup.jump2user($setup.detailData.uid))
      }, [
        vue.createElementVNode("image", {
          class: "avatar",
          src: $setup.authorInfo.avatar,
          mode: "aspectFill"
        }, null, 8, ["src"]),
        vue.createElementVNode("view", { class: "user-meta" }, [
          vue.createElementVNode(
            "text",
            { class: "username" },
            vue.toDisplayString($setup.authorInfo.user_name || "未知用户"),
            1
            /* TEXT */
          ),
          vue.createElementVNode(
            "text",
            { class: "publish-time" },
            "发布于 " + vue.toDisplayString($setup.formatTimestamp($setup.detailData.created_at)),
            1
            /* TEXT */
          )
        ])
      ]),
      vue.createElementVNode("view", { class: "content-box" }, [
        vue.createElementVNode("view", { style: { "display": "flex", "justify-content": "space-between" } }, [
          vue.createElementVNode(
            "text",
            { class: "title" },
            vue.toDisplayString($setup.detailData.title),
            1
            /* TEXT */
          ),
          vue.createElementVNode("view", { class: "report-container" }, [
            vue.createVNode(_component_report_button, {
              "report-type": $setup.origin === 1 ? 2 : 1,
              "relation-id": parseInt($setup.pageId),
              "icon-type": "flag",
              "icon-color": "#999"
            }, null, 8, ["report-type", "relation-id"])
          ])
        ]),
        vue.createElementVNode(
          "text",
          { class: "content" },
          vue.toDisplayString($setup.detailData.content),
          1
          /* TEXT */
        )
      ]),
      vue.createElementVNode("view", { class: "goods-list" }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.detailData.collocation_relation_list, (item) => {
            var _a2, _b;
            return vue.openBlock(), vue.createElementBlock("view", {
              key: item.id,
              class: "goods-item",
              onClick: ($event) => $setup.navigateToRelated(item)
            }, [
              vue.createElementVNode("view", { class: "image-container" }, [
                item.relation_goods_id === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "placeholder-box"
                }, [
                  vue.createElementVNode("text", { style: { "color": "#fff", "font-size": "45px" } }, "?")
                ])) : (vue.openBlock(), vue.createElementBlock(
                  vue.Fragment,
                  { key: 1 },
                  [
                    item.imageLoaded ? (vue.openBlock(), vue.createElementBlock("image", {
                      key: 0,
                      src: ((_b = (_a2 = item.goodsInfo) == null ? void 0 : _a2.goods_images) == null ? void 0 : _b[0]) || "/static/goods-default.png",
                      mode: "aspectFill",
                      class: "goods-image"
                    }, null, 8, ["src"])) : item.imageError ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "error-box"
                    }, [
                      vue.createElementVNode("text", null, "图片加载失败")
                    ])) : (vue.openBlock(), vue.createElementBlock("view", {
                      key: 2,
                      class: "loading-box"
                    }, [
                      vue.createElementVNode("text", null, "加载中...")
                    ]))
                  ],
                  64
                  /* STABLE_FRAGMENT */
                ))
              ]),
              vue.createElementVNode("view", { class: "goods-info" }, [
                vue.createElementVNode(
                  "text",
                  { class: "brand" },
                  vue.toDisplayString(item.relation_brand_name) + " - " + vue.toDisplayString(item.relation_goods_name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("view", null, [
                  vue.createElementVNode(
                    "text",
                    { class: "category" },
                    vue.toDisplayString(item.type),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "price" },
                    " （" + vue.toDisplayString(item.goodsInfo && item.goodsInfo.total_amount ? item.goodsInfo.total_amount + item.goodsInfo.currency : "贩售价格未收录") + "）",
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "see font-alimamashuhei" }, " 去看看 → ")
              ])
            ], 8, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      $setup.origin > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        style: { "padding": "10px" }
      }, [
        vue.createVNode(_component_comment_list, {
          ref: "commentListRef",
          type: $setup.origin == 1 ? 3 : 6,
          "relation-id": parseInt($setup.pageId),
          onReply: $setup.handleReplyComment
        }, null, 8, ["type", "relation-id"])
      ])) : vue.createCommentVNode("v-if", true),
      $setup.detailData.title ? (vue.openBlock(), vue.createBlock(_component_comment_input, {
        key: 1,
        ref: "commentInputRef",
        "reply-info": $setup.replyForItem,
        "target-id": $setup.pageId,
        onSubmit: $setup.handleCommentSubmit,
        "onUpdate:replyInfo": _cache[2] || (_cache[2] = (val) => $setup.replyForItem = val)
      }, null, 8, ["reply-info", "target-id"])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { style: { "width": "100%", "height": "120rpx" } }),
      $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        class: "loading"
      }, "加载中...")) : vue.createCommentVNode("v-if", true),
      $setup.error ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 3,
          class: "error"
        },
        vue.toDisplayString($setup.errorMsg),
        1
        /* TEXT */
      )) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PagesCollocationShareCollocationShare = /* @__PURE__ */ _export_sfc(_sfc_main$13, [["render", _sfc_render$12], ["__scopeId", "data-v-38d96b75"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/collocation_share/collocation_share.vue"]]);
  const _sfc_main$12 = {
    __name: "user_page",
    props: ["uid"],
    setup(__props, { expose: __expose }) {
      __expose();
      const currentTab = vue.ref(0);
      const tabs = vue.ref(["搭配", "娃柜", "点评"]);
      const props = __props;
      const userInfo = vue.ref({
        avatar: "",
        user_name: "加载中..."
      });
      const is_blocked = vue.ref(false);
      const they_blocked = vue.ref(false);
      const collocations = vue.ref([]);
      const dolls = vue.ref([]);
      const reviews = vue.ref([]);
      const paginations = vue.ref([
        { page: 1, pageSize: 10, total: 0, loading: false, finished: false },
        // 搭配
        { page: 1, pageSize: 10, total: 0, loading: false, finished: false },
        // 娃柜
        { page: 1, pageSize: 10, total: 0, loading: false, finished: false }
        // 点评
      ]);
      vue.watch(currentTab, (newVal) => {
        const currentData = [collocations, dolls, reviews][newVal].value;
        if (currentData.length === 0) {
          loadData();
        }
      });
      const navigateTo = (url) => {
        uni.navigateTo({ url });
      };
      const loadData = async () => {
        if (is_blocked.value || they_blocked.value)
          return;
        const pg = paginations.value[currentTab.value];
        if (pg.loading || pg.finished)
          return;
        pg.loading = true;
        try {
          let url, listKey;
          switch (currentTab.value) {
            case 0:
              url = `/user-info/collection?user_id=${props.uid}&page=${pg.page}`;
              listKey = "List";
              break;
            case 1:
              url = `/user-info/show-case?user_id=${props.uid}&page=${pg.page}`;
              listKey = "List";
              break;
            case 2:
              url = `/user-info/posts?user_id=${props.uid}&page=${pg.page}`;
              listKey = "comment_list";
              break;
          }
          const res = await uni.request({ url: `${websiteUrl.value}${url}` });
          if (res.data.status === "success") {
            const data = res.data.data;
            const list = data[listKey] || [];
            const transformed = list.map((item) => {
              var _a, _b, _c;
              switch (currentTab.value) {
                case 0:
                  return {
                    id: item.id,
                    cover: ((_a = item.image_urls) == null ? void 0 : _a.split(",")[0]) || "",
                    images: ((_b = item.image_urls) == null ? void 0 : _b.split(",")) || [],
                    title: item.title,
                    time: item.created_at
                  };
                case 1:
                  return {
                    id: item.id,
                    cover: ((_c = item.image_urls) == null ? void 0 : _c.split(",")[0]) || "",
                    title: item.name,
                    desc: item.description,
                    time: item.created_at,
                    price: item.price || 0
                  };
                case 2:
                  return {
                    id: item.id,
                    product_thumb: item.target_image,
                    product_name: item.target_name,
                    content: item.comment,
                    create_time: formatTime(item.created_at),
                    target_id: item.relation_id
                  };
              }
            });
            const target = [collocations, dolls, reviews][currentTab.value];
            target.value = [...target.value, ...transformed];
            pg.total = Number(data.total || 0);
            pg.finished = target.value.length >= pg.total;
            pg.page++;
          }
        } catch (error2) {
          formatAppLog("log", "at pages/user_page/user_page.vue:213", error2);
          uni.showToast({ title: "数据加载失败", icon: "none" });
        } finally {
          pg.loading = false;
        }
      };
      const getAuthorInfo = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/user-info?uid=${props.uid}`
          });
          if (res.data.status === "success") {
            userInfo.value = res.data.data;
          }
        } catch (error2) {
          uni.showToast({ title: "用户信息加载失败", icon: "none" });
        }
      };
      const getBlockStatus = async () => {
        const token = uni.getStorageSync("token");
        if (!token) {
          formatAppLog("log", "at pages/user_page/user_page.vue:238", "未登录");
          return;
        }
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/blacklist/status`,
            method: "GET",
            data: { target_user_id: props.uid },
            header: { Authorization: token }
          });
          if (res.data.status === "success") {
            is_blocked.value = !!res.data.data.is_blocked;
            they_blocked.value = !!res.data.data.they_blocked;
          }
        } catch (error2) {
          formatAppLog("log", "at pages/user_page/user_page.vue:253", "获取黑名单状态失败", error2);
        }
      };
      const toggleBlock = async () => {
        try {
          const token = uni.getStorageSync("token");
          if (!token) {
            formatAppLog("log", "at pages/user_page/user_page.vue:262", "未登录");
            return;
          }
          const action = is_blocked.value ? "remove" : "add";
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/blacklist/${action}?target_user_id=${props.uid}`,
            method: "POST",
            header: { Authorization: token }
          });
          if (res.data.status === "success") {
            is_blocked.value = !is_blocked.value;
            uni.showToast({
              title: is_blocked.value ? "已加入黑名单" : "已移除黑名单",
              icon: "none"
            });
            if (is_blocked.value || they_blocked.value) {
              collocations.value = [];
              dolls.value = [];
              reviews.value = [];
            }
          } else {
            uni.showToast({ title: res.data.msg, icon: "none" });
          }
        } catch (error2) {
          formatAppLog("log", "at pages/user_page/user_page.vue:286", "操作失败", error2);
          uni.showToast({ title: "操作失败", icon: "none" });
        }
      };
      const startDM = async () => {
        var _a, _b, _c, _d;
        if (is_blocked.value) {
          return uni.showToast({ title: "你已拉黑对方，无法私信", icon: "none" });
        }
        if (they_blocked.value) {
          return uni.showToast({ title: "对方已拉黑你，无法私信", icon: "none" });
        }
        const token = uni.getStorageSync("token");
        if (!token) {
          return uni.showToast({ title: "请先登录", icon: "none" });
        }
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/im/start-session?peer_id=${props.uid}`,
            method: "POST",
            header: { Authorization: token }
          });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success") {
            const sid = (_c = (_b = res == null ? void 0 : res.data) == null ? void 0 : _b.data) == null ? void 0 : _c.session_id;
            if (!sid)
              throw new Error("no session_id");
            navigateToChat(sid);
          } else {
            uni.showToast({ title: ((_d = res == null ? void 0 : res.data) == null ? void 0 : _d.msg) || "发起私信失败", icon: "none" });
          }
        } catch (e2) {
          formatAppLog("log", "at pages/user_page/user_page.vue:317", "startDM error", e2);
          uni.showToast({ title: "发起私信失败", icon: "none" });
        }
      };
      const navigateToChat = (sessionId) => {
        const url = `/pkg-im/chat/chat?session_id=${encodeURIComponent(sessionId)}&peer_id=${props.uid}`;
        uni.navigateTo({ url });
      };
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        const y2 = date.getFullYear();
        const m2 = String(date.getMonth() + 1).padStart(2, "0");
        const d2 = String(date.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${d2}`;
      };
      uni.setNavigationBarTitle({ title: "用户主页" });
      onShow(() => {
        getAuthorInfo();
        getBlockStatus().then(() => {
          if (!is_blocked.value && !they_blocked.value) {
            loadData();
          }
        });
      });
      onReachBottom(() => {
        loadData();
      });
      const __returned__ = { currentTab, tabs, props, userInfo, is_blocked, they_blocked, collocations, dolls, reviews, paginations, navigateTo, loadData, getAuthorInfo, getBlockStatus, toggleBlock, startDM, navigateToChat, formatTime, ref: vue.ref, watch: vue.watch, get onShow() {
        return onShow;
      }, get onReachBottom() {
        return onReachBottom;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$11(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "page-container" }, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "header_container" }, [
        vue.createElementVNode("view", { class: "user-info-container" }, [
          vue.createElementVNode("view", { class: "user-info-box" }, [
            vue.createElementVNode("image", {
              class: "avatar",
              src: $setup.userInfo.avatar,
              mode: "aspectFill"
            }, null, 8, ["src"]),
            vue.createElementVNode(
              "text",
              { class: "username" },
              vue.toDisplayString($setup.userInfo.user_name),
              1
              /* TEXT */
            ),
            vue.createElementVNode("view", { class: "action-buttons-inline" }, [
              vue.createElementVNode("view", {
                class: "action-btn dm-btn",
                onClick: $setup.startDM
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "chat",
                  size: "16",
                  color: "#fff"
                }),
                vue.createElementVNode("text", null, "私信")
              ]),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["action-btn block-btn", { "blocked": $setup.is_blocked }]),
                  onClick: $setup.toggleBlock
                },
                [
                  vue.createVNode(_component_uni_icons, {
                    type: $setup.is_blocked ? "minus" : "minus",
                    size: "16",
                    color: "#fff"
                  }, null, 8, ["type"]),
                  vue.createElementVNode(
                    "text",
                    null,
                    vue.toDisplayString($setup.is_blocked ? "已拉黑" : "拉黑"),
                    1
                    /* TEXT */
                  )
                ],
                2
                /* CLASS */
              )
            ])
          ])
        ])
      ]),
      !$setup.is_blocked && !$setup.they_blocked ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "content"
      }, [
        vue.createElementVNode("view", { class: "tabs-container" }, [
          vue.createElementVNode("view", { class: "tabs" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.tabs, (tab, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: vue.normalizeClass(["tab-item font-alimamashuhei", { active: $setup.currentTab === index }]),
                  onClick: ($event) => $setup.currentTab = index
                }, vue.toDisplayString(tab), 11, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ]),
        $setup.currentTab === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "collocation-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.collocations, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "collocation-item item-image-container",
                onClick: ($event) => $setup.navigateTo(`/pages/collocation_share/collocation_share?collocation_id=${item.id}&origin=1`)
              }, [
                vue.createElementVNode("image", {
                  class: "item-image",
                  src: item.cover,
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode(
                  "text",
                  { class: "item-title" },
                  vue.toDisplayString(item.title),
                  1
                  /* TEXT */
                )
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : vue.createCommentVNode("v-if", true),
        $setup.currentTab === 1 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "doll-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.dolls, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "doll-item item-image-container",
                onClick: ($event) => $setup.navigateTo(`/pages/collocation_share/collocation_share?collocation_id=${item.id}&origin=2`)
              }, [
                vue.createElementVNode("image", {
                  class: "item-image",
                  src: item.cover,
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode(
                  "text",
                  { class: "item-title" },
                  vue.toDisplayString(item.title),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "item-price" },
                  "¥" + vue.toDisplayString(item.price),
                  1
                  /* TEXT */
                )
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : vue.createCommentVNode("v-if", true),
        $setup.currentTab === 2 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "review-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.reviews, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "review-item",
                onClick: ($event) => $setup.navigateTo(`/pages/goods/goods?goods_id=${item.target_id}`)
              }, [
                vue.createElementVNode("image", {
                  class: "product-thumb",
                  src: item.product_thumb,
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode("view", { class: "review-content" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "product-name" },
                    vue.toDisplayString(item.product_name),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "review-text" },
                    vue.toDisplayString(item.content),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "review-time" },
                    vue.toDisplayString(item.create_time),
                    1
                    /* TEXT */
                  )
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : vue.createCommentVNode("v-if", true)
      ])) : vue.createCommentVNode("v-if", true),
      $setup.is_blocked || $setup.they_blocked ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "empty-state"
      }, [
        vue.createVNode(_component_uni_icons, {
          type: "minus",
          size: "48",
          color: "#ccc"
        }),
        vue.createElementVNode("text", null, "你无法查看该用户的主页")
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PagesUserPageUserPage = /* @__PURE__ */ _export_sfc(_sfc_main$12, [["render", _sfc_render$11], ["__scopeId", "data-v-c3556478"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/user_page/user_page.vue"]]);
  const _sfc_main$11 = {
    __name: "message_list",
    setup(__props, { expose: __expose }) {
      __expose();
      const messageList = vue.ref([]);
      const unreadCount = vue.ref(0);
      const page = vue.ref(1);
      const isRefreshing = vue.ref(false);
      const hasMore = vue.ref(true);
      const loading2 = vue.ref(false);
      const swipeOptions = (item) => {
        return [
          {
            text: "删除",
            style: {
              backgroundColor: "#dd524d"
            },
            data: {
              type: "delete",
              id: item.id
            }
          },
          {
            text: item.is_read ? "已读" : "标记已读",
            style: {
              backgroundColor: "#6fd0d4"
            },
            data: {
              type: "read",
              id: item.id
            }
          }
        ];
      };
      vue.onMounted(async () => {
        await asyncGetUserInfo();
        fetchMessages();
        fetchUnreadCount();
        uni.setNavigationBarTitle({
          title: "消息"
        });
      });
      const fetchMessages = async (isLoadMore = false) => {
        try {
          loading2.value = true;
          const targetPage = isLoadMore ? page.value : 1;
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/user-messages?page=${targetPage}`,
            header: {
              Authorization: uni.getStorageSync("token")
            }
          });
          if (res.data.status === "success") {
            const newMessages = res.data.data.message_list;
            if (isLoadMore) {
              messageList.value = [...messageList.value, ...newMessages];
            } else {
              messageList.value = newMessages;
            }
            hasMore.value = newMessages.length >= 10;
            page.value = targetPage + (hasMore.value ? 1 : 0);
          }
        } catch (error2) {
          uni.showToast({
            title: "消息加载失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
          isRefreshing.value = false;
        }
      };
      const onRefresh = () => {
        if (loading2.value)
          return;
        isRefreshing.value = true;
        page.value = 1;
        hasMore.value = true;
        fetchMessages();
      };
      const loadMore = () => {
        formatAppLog("log", "at pages/message_list/message_list.vue:138", "触发加载更多", loading2.value, hasMore.value);
        if (loading2.value || !hasMore.value)
          return;
        fetchMessages(true);
      };
      const fetchUnreadCount = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/unread-message-count`,
            header: {
              Authorization: uni.getStorageSync("token")
            }
          });
          if (res.data.status === "success") {
            unreadCount.value = res.data.data.count;
          }
        } catch (error2) {
          uni.showToast({
            title: "未读数获取失败",
            icon: "none"
          });
        }
      };
      const handleSwipeClick = (e2) => {
        const {
          type,
          id
        } = e2.content.data;
        if (type === "delete") {
          showDeleteConfirm(id);
        } else if (type === "read") {
          markAsRead(id);
        }
      };
      const showDeleteConfirm = (messageId) => {
        uni.showModal({
          title: "删除确认",
          content: "确定要删除这条消息吗？",
          success: (res) => {
            if (res.confirm) {
              deleteMessage(messageId);
            }
          }
        });
      };
      const deleteMessage = async (messageId) => {
        try {
          await uni.request({
            url: `${websiteUrl.value}/with-state/delete-message`,
            method: "POST",
            header: {
              "Authorization": uni.getStorageSync("token"),
              "Content-Type": "application/json"
            },
            data: {
              message_id: messageId
            }
          });
          messageList.value = messageList.value.filter((item) => item.id !== messageId);
          fetchUnreadCount();
          uni.showToast({
            title: "删除成功"
          });
        } catch (error2) {
          uni.showToast({
            title: "删除失败",
            icon: "none"
          });
        }
      };
      const markAsRead = async (messageId) => {
        try {
          await uni.request({
            url: `${websiteUrl.value}/with-state/mark-message-read`,
            method: "POST",
            header: {
              "Authorization": uni.getStorageSync("token"),
              "Content-Type": "application/json"
            },
            data: {
              message_id: messageId
            }
          });
          const item = messageList.value.find((i2) => i2.id === messageId);
          if (item && !item.is_read) {
            item.is_read = 1;
            unreadCount.value--;
          }
        } catch (error2) {
          uni.showToast({
            title: "标记失败",
            icon: "none"
          });
        }
      };
      const gotoDetail = (id) => {
        uni.navigateTo({
          url: `/pages/message_info/message_info?message_id=${id}`
        });
      };
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        return `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
      };
      const __returned__ = { messageList, unreadCount, page, isRefreshing, hasMore, loading: loading2, swipeOptions, fetchMessages, onRefresh, loadMore, fetchUnreadCount, handleSwipeClick, showDeleteConfirm, deleteMessage, markAsRead, gotoDetail, formatTime, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$10(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_swipe_action_item = resolveEasycom(vue.resolveDynamicComponent("uni-swipe-action-item"), __easycom_1$b);
    const _component_uni_swipe_action = resolveEasycom(vue.resolveDynamicComponent("uni-swipe-action"), __easycom_2$6);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "message-list",
          "scroll-y": "",
          onScrolltolower: $setup.loadMore,
          "show-scrollbar": false
        },
        [
          vue.createVNode(_component_uni_swipe_action, null, {
            default: vue.withCtx(() => [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.messageList, (item) => {
                  return vue.openBlock(), vue.createBlock(_component_uni_swipe_action_item, {
                    key: item.id,
                    "right-options": $setup.swipeOptions(item),
                    threshold: 0.4,
                    onClick: $setup.handleSwipeClick
                  }, {
                    default: vue.withCtx(() => [
                      vue.createElementVNode("view", {
                        class: "message-item",
                        onClick: ($event) => $setup.gotoDetail(item.id)
                      }, [
                        vue.createElementVNode("image", {
                          class: "cover-image",
                          src: item.cover_image,
                          mode: "aspectFill"
                        }, null, 8, ["src"]),
                        vue.createElementVNode("view", { class: "content" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "title" },
                            vue.toDisplayString(item.title),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            {
                              class: vue.normalizeClass(["msg", { unread: !item.is_read }])
                            },
                            vue.toDisplayString(item.msg),
                            3
                            /* TEXT, CLASS */
                          )
                        ]),
                        vue.createElementVNode(
                          "text",
                          { class: "time" },
                          vue.toDisplayString($setup.formatTime(item.created_at)),
                          1
                          /* TEXT */
                        )
                      ], 8, ["onClick"])
                    ]),
                    _: 2
                    /* DYNAMIC */
                  }, 1032, ["right-options"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            _: 1
            /* STABLE */
          }),
          vue.createElementVNode("view", { class: "loading-wrapper" }, [
            $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 0,
              class: "loading-text"
            }, "加载中...")) : vue.createCommentVNode("v-if", true),
            !$setup.hasMore ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "loading-text"
            }, "没有更多数据了")) : vue.createCommentVNode("v-if", true)
          ])
        ],
        32
        /* NEED_HYDRATION */
      )
    ]);
  }
  const PagesMessageListMessageList = /* @__PURE__ */ _export_sfc(_sfc_main$11, [["render", _sfc_render$10], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/message_list/message_list.vue"]]);
  const en$1 = {
    "uni-load-more.contentdown": "Pull up to show more",
    "uni-load-more.contentrefresh": "loading...",
    "uni-load-more.contentnomore": "No more data"
  };
  const zhHans = {
    "uni-load-more.contentdown": "上拉显示更多",
    "uni-load-more.contentrefresh": "正在加载...",
    "uni-load-more.contentnomore": "没有更多数据了"
  };
  const zhHant = {
    "uni-load-more.contentdown": "上拉顯示更多",
    "uni-load-more.contentrefresh": "正在加載...",
    "uni-load-more.contentnomore": "沒有更多數據了"
  };
  const messages = {
    en: en$1,
    "zh-Hans": zhHans,
    "zh-Hant": zhHant
  };
  let platform;
  setTimeout(() => {
    platform = uni.getSystemInfoSync().platform;
  }, 16);
  const {
    t: t$1
  } = initVueI18n(messages);
  const _sfc_main$10 = {
    name: "UniLoadMore",
    emits: ["clickLoadMore"],
    props: {
      status: {
        // 上拉的状态：more-loading前；loading-loading中；noMore-没有更多了
        type: String,
        default: "more"
      },
      showIcon: {
        type: Boolean,
        default: true
      },
      iconType: {
        type: String,
        default: "auto"
      },
      iconSize: {
        type: Number,
        default: 24
      },
      color: {
        type: String,
        default: "#777777"
      },
      contentText: {
        type: Object,
        default() {
          return {
            contentdown: "",
            contentrefresh: "",
            contentnomore: ""
          };
        }
      },
      showText: {
        type: Boolean,
        default: true
      }
    },
    data() {
      return {
        webviewHide: false,
        platform,
        imgBase64: "data:image/png;base64,"
      };
    },
    computed: {
      iconSnowWidth() {
        return (Math.floor(this.iconSize / 24) || 1) * 2;
      },
      contentdownText() {
        return this.contentText.contentdown || t$1("uni-load-more.contentdown");
      },
      contentrefreshText() {
        return this.contentText.contentrefresh || t$1("uni-load-more.contentrefresh");
      },
      contentnomoreText() {
        return this.contentText.contentnomore || t$1("uni-load-more.contentnomore");
      }
    },
    mounted() {
      var pages2 = getCurrentPages();
      var page = pages2[pages2.length - 1];
      var currentWebview = page.$getAppWebview();
      currentWebview.addEventListener("hide", () => {
        this.webviewHide = true;
      });
      currentWebview.addEventListener("show", () => {
        this.webviewHide = false;
      });
    },
    methods: {
      onClick() {
        this.$emit("clickLoadMore", {
          detail: {
            status: this.status
          }
        });
      }
    }
  };
  function _sfc_render$$(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", {
      class: "uni-load-more",
      onClick: _cache[0] || (_cache[0] = (...args) => $options.onClick && $options.onClick(...args))
    }, [
      !$data.webviewHide && ($props.iconType === "circle" || $props.iconType === "auto" && $data.platform === "android") && $props.status === "loading" && $props.showIcon ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 0,
          style: vue.normalizeStyle({ width: $props.iconSize + "px", height: $props.iconSize + "px" }),
          class: "uni-load-more__img uni-load-more__img--android-MP"
        },
        [
          vue.createElementVNode(
            "view",
            {
              class: "uni-load-more__img-icon",
              style: vue.normalizeStyle({ borderTopColor: $props.color, borderTopWidth: $props.iconSize / 12 })
            },
            null,
            4
            /* STYLE */
          ),
          vue.createElementVNode(
            "view",
            {
              class: "uni-load-more__img-icon",
              style: vue.normalizeStyle({ borderTopColor: $props.color, borderTopWidth: $props.iconSize / 12 })
            },
            null,
            4
            /* STYLE */
          ),
          vue.createElementVNode(
            "view",
            {
              class: "uni-load-more__img-icon",
              style: vue.normalizeStyle({ borderTopColor: $props.color, borderTopWidth: $props.iconSize / 12 })
            },
            null,
            4
            /* STYLE */
          )
        ],
        4
        /* STYLE */
      )) : !$data.webviewHide && $props.status === "loading" && $props.showIcon ? (vue.openBlock(), vue.createElementBlock(
        "view",
        {
          key: 1,
          style: vue.normalizeStyle({ width: $props.iconSize + "px", height: $props.iconSize + "px" }),
          class: "uni-load-more__img uni-load-more__img--ios-H5"
        },
        [
          vue.createElementVNode("image", {
            src: $data.imgBase64,
            mode: "widthFix"
          }, null, 8, ["src"])
        ],
        4
        /* STYLE */
      )) : vue.createCommentVNode("v-if", true),
      $props.showText ? (vue.openBlock(), vue.createElementBlock(
        "text",
        {
          key: 2,
          class: "uni-load-more__text",
          style: vue.normalizeStyle({ color: $props.color })
        },
        vue.toDisplayString($props.status === "more" ? $options.contentdownText : $props.status === "loading" ? $options.contentrefreshText : $options.contentnomoreText),
        5
        /* TEXT, STYLE */
      )) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_3 = /* @__PURE__ */ _export_sfc(_sfc_main$10, [["render", _sfc_render$$], ["__scopeId", "data-v-9245e42c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-load-more/components/uni-load-more/uni-load-more.vue"]]);
  const _sfc_main$$ = {
    __name: "message_info",
    props: ["message_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const messageDetail = vue.ref(null);
      const loading2 = vue.ref(true);
      const error2 = vue.ref("");
      const messageId = vue.ref("");
      const fetchMessageDetail = async () => {
        try {
          loading2.value = true;
          error2.value = "";
          if (!messageId.value) {
            error2.value = "无效的消息ID";
            return;
          }
          const userInfo = await asyncGetUserInfo();
          if (!userInfo) {
            error2.value = "请先登录";
            return;
          }
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/message-detail`,
            method: "GET",
            data: { id: messageId.value },
            header: { Authorization: uni.getStorageSync("token") }
          });
          if (res.data.status === "success") {
            messageDetail.value = res.data.data;
            uni.$emit("message-status-update", {
              id: messageId.value,
              is_read: 1
            });
          } else {
            error2.value = res.data.msg || "获取详情失败";
          }
        } catch (err) {
          error2.value = "请求失败，请检查网络";
          formatAppLog("error", "at pages/message_info/message_info.vue:97", "详情请求失败:", err);
        } finally {
          loading2.value = false;
        }
      };
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        return date.toLocaleDateString("zh-CN", {
          year: "numeric",
          month: "2-digit",
          day: "2-digit",
          hour: "2-digit",
          minute: "2-digit"
        }).replace(/\//g, "-");
      };
      vue.onMounted(() => {
        uni.setNavigationBarTitle({
          title: "消息详情"
        });
        messageId.value = props.message_id;
        fetchMessageDetail();
      });
      const __returned__ = { props, messageDetail, loading: loading2, error: error2, messageId, fetchMessageDetail, formatTime, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$_(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "loading-wrapper"
      }, [
        vue.createVNode(_component_uni_load_more, { status: "loading" })
      ])) : $setup.error ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "error-wrapper"
      }, [
        vue.createVNode(_component_uni_icons, {
          type: "info",
          size: "24",
          color: "#ff4d4f"
        }),
        vue.createElementVNode(
          "text",
          { class: "error-text" },
          vue.toDisplayString($setup.error),
          1
          /* TEXT */
        ),
        vue.createElementVNode("button", {
          class: "retry-btn",
          onClick: $setup.fetchMessageDetail
        }, "重试")
      ])) : (vue.openBlock(), vue.createElementBlock("scroll-view", {
        key: 2,
        class: "content-wrapper",
        "scroll-y": ""
      }, [
        vue.createElementVNode("image", {
          class: "cover-image",
          src: $setup.messageDetail.cover_image,
          mode: "widthFix",
          "lazy-load": ""
        }, null, 8, ["src"]),
        vue.createElementVNode("view", { class: "main-content" }, [
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode(
              "text",
              { class: "title" },
              vue.toDisplayString($setup.messageDetail.title),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "text",
              { class: "time" },
              vue.toDisplayString($setup.formatTime($setup.messageDetail.created_at)),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "message-content" }, [
            vue.createElementVNode(
              "text",
              { class: "message-text" },
              vue.toDisplayString($setup.messageDetail.msg),
              1
              /* TEXT */
            )
          ])
        ])
      ]))
    ]);
  }
  const PagesMessageInfoMessageInfo = /* @__PURE__ */ _export_sfc(_sfc_main$$, [["render", _sfc_render$_], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/message_info/message_info.vue"]]);
  const _sfc_main$_ = {
    __name: "register",
    setup(__props, { expose: __expose }) {
      __expose();
      const formData = vue.ref({
        telephone: "",
        code: "",
        password: "",
        rePassword: ""
      });
      const errors = vue.ref({
        phone: "",
        code: "",
        password: "",
        rePassword: ""
      });
      const codeCountdown = vue.ref(0);
      const codeBtnDisabled = vue.computed(() => codeCountdown.value > 0);
      const codeBtnText = vue.computed(
        () => codeCountdown.value > 0 ? `${codeCountdown.value}秒后重发` : "获取验证码"
      );
      const loading2 = vue.ref(false);
      const validatePhone = () => {
        if (!formData.value.telephone) {
          errors.value.phone = "手机号不能为空";
          return false;
        }
        if (!/^1[3-9]\d{9}$/.test(formData.value.telephone)) {
          errors.value.phone = "手机号格式不正确";
          return false;
        }
        errors.value.phone = "";
        return true;
      };
      const sendSmsCode = async () => {
        if (!validatePhone())
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/send-sms-code`,
            method: "POST",
            data: {
              tel_phone: formData.value.telephone
            }
          });
          if (res.data.status === "success") {
            startCountdown();
            uni.showToast({
              title: "验证码已发送",
              icon: "none"
            });
          } else {
            uni.showToast({
              title: res.data.msg || "发送失败",
              icon: "none"
            });
          }
        } catch (err) {
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        }
      };
      const startCountdown = () => {
        codeCountdown.value = 60;
        const timer = setInterval(() => {
          if (codeCountdown.value <= 0) {
            clearInterval(timer);
            return;
          }
          codeCountdown.value--;
        }, 1e3);
      };
      const validatePassword = () => {
        if (formData.value.password.length < 6) {
          errors.value.password = "密码至少需要6位";
          return false;
        }
        errors.value.password = "";
        return true;
      };
      const validateRePassword = () => {
        if (formData.value.password !== formData.value.rePassword) {
          errors.value.rePassword = "两次密码输入不一致";
          return false;
        }
        errors.value.rePassword = "";
        return true;
      };
      const validateCode = () => {
        if (!formData.value.code) {
          errors.value.code = "验证码不能为空";
          return false;
        }
        if (formData.value.code.length !== 6) {
          errors.value.code = "验证码格式不正确";
          return false;
        }
        errors.value.code = "";
        return true;
      };
      const handleRegister = async () => {
        const validations = [
          validatePhone(),
          validateCode(),
          validatePassword(),
          validateRePassword()
        ];
        if (!validations.every((v2) => v2))
          return;
        try {
          loading2.value = true;
          const res = await uni.request({
            url: `${websiteUrl.value}/register`,
            method: "POST",
            data: {
              telephone: formData.value.telephone,
              code: formData.value.code,
              password: formData.value.password,
              re_password: formData.value.rePassword
            }
          });
          if (res.data.status === "success") {
            uni.setStorageSync("token", res.data.data.jwt_token);
            uni.showToast({
              title: "注册成功",
              icon: "success",
              complete: () => {
                setTimeout(() => {
                  uni.switchTab({
                    url: "/pages/mine/mine"
                  });
                }, 1500);
              }
            });
          } else {
            uni.showToast({
              title: res.data.msg || "注册失败",
              icon: "none"
            });
          }
        } catch (err) {
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const navigateToLogin = () => {
        uni.navigateTo({
          url: "/pages/login/login"
        });
      };
      const __returned__ = { formData, errors, codeCountdown, codeBtnDisabled, codeBtnText, loading: loading2, validatePhone, sendSmsCode, startCountdown, validatePassword, validateRePassword, validateCode, handleRegister, navigateToLogin, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$Z(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "container" }, [
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode("text", { class: "title" }, "手机号注册")
          ]),
          vue.createElementVNode("view", { class: "form-box" }, [
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.createElementVNode("text", { class: "prefix" }, "+86"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.formData.telephone = $event),
                    type: "number",
                    placeholder: "请输入手机号",
                    maxlength: "11",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validatePhone
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.telephone]
                ])
              ]),
              $setup.errors.phone ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.phone),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "code-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.formData.code = $event),
                    type: "number",
                    autocomplete: "new-password",
                    placeholder: "请输入验证码",
                    maxlength: "6",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validateCode
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.code]
                ]),
                vue.createElementVNode("button", {
                  class: vue.normalizeClass(["code-btn", { disabled: $setup.codeBtnDisabled }]),
                  disabled: $setup.codeBtnDisabled,
                  onClick: $setup.sendSmsCode
                }, vue.toDisplayString($setup.codeBtnText), 11, ["disabled"])
              ]),
              $setup.errors.code ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.code),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.formData.password = $event),
                    autocomplete: "new-password",
                    type: "password",
                    placeholder: "请输入密码（至少6位）",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validatePassword
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.password]
                ])
              ]),
              $setup.errors.password ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.password),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.formData.rePassword = $event),
                    type: "password",
                    placeholder: "请再次输入密码",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validateRePassword
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.rePassword]
                ])
              ]),
              $setup.errors.rePassword ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.rePassword),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("button", {
              class: vue.normalizeClass(["submit-btn", { loading: $setup.loading }]),
              disabled: $setup.loading,
              onClick: $setup.handleRegister
            }, vue.toDisplayString($setup.loading ? "注册中..." : "立即注册"), 11, ["disabled"]),
            vue.createElementVNode("view", { class: "bottom-tips" }, [
              vue.createTextVNode(" 已有账号？ "),
              vue.createElementVNode("text", {
                class: "link-text",
                onClick: $setup.navigateToLogin
              }, "去登录")
            ])
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesRegisterRegister = /* @__PURE__ */ _export_sfc(_sfc_main$_, [["render", _sfc_render$Z], ["__scopeId", "data-v-bac4a35d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/register/register.vue"]]);
  const _sfc_main$Z = {
    __name: "user_like",
    setup(__props, { expose: __expose }) {
      __expose();
      const categories = vue.ref([
        {
          type: 1,
          name: "商品"
        },
        {
          type: 2,
          name: "品牌"
        },
        {
          type: 3,
          name: "搭配"
        },
        {
          type: 4,
          name: "展示柜"
        }
      ]);
      const activeCategory = vue.ref(1);
      const currentList = vue.ref([]);
      const loading2 = vue.ref(false);
      const noMore = vue.ref(false);
      const pagination = vue.reactive({
        page: 1,
        page_size: 8,
        total: 0
      });
      vue.onMounted(async () => {
        await checkLogin();
        loadData();
      });
      const checkLogin = async () => {
        const userInfo = await asyncGetUserInfo();
        if (!userInfo) {
          uni.navigateTo({
            url: "/pages/login/login"
          });
        }
      };
      const switchCategory = (type) => {
        if (activeCategory.value === type)
          return;
        activeCategory.value = type;
        resetPagination();
        loadData();
      };
      const loadData = async () => {
        if (loading2.value || noMore.value)
          return;
        loading2.value = true;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/user-likes`,
            method: "GET",
            data: {
              type: activeCategory.value,
              page: pagination.page,
              page_size: pagination.page_size
            },
            header: {
              Authorization: uni.getStorageSync("token")
            }
          });
          if (res.data.status === "success") {
            const newData = res.data.data.list;
            currentList.value = [...currentList.value, ...newData];
            pagination.total = res.data.data.total;
            if (!res.data.data.list.length) {
              noMore.value = true;
            } else {
              noMore.value = false;
            }
            pagination.page += 1;
          }
        } catch (error2) {
          uni.showToast({
            title: "加载失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (!noMore.value) {
          loadData();
        } else {
          formatAppLog("log", "at pages/user_like/user_like.vue:203", "no more");
        }
      };
      const resetPagination = () => {
        pagination.page = 1;
        currentList.value = [];
        noMore.value = false;
      };
      const getFirstImage = (images) => {
        return (images == null ? void 0 : images.length) > 0 ? images[0] : ``;
      };
      const getFirstCollocationImage = (images) => {
        const url = (images == null ? void 0 : images.split(",")[0]) || "";
        return url ? url : ``;
      };
      const unfollowGoods = async (id) => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/user/unfollow`,
            method: "POST",
            data: {
              id
            },
            header: {
              Authorization: uni.getStorageSync("token")
            }
          });
          if (res.data.status === "success") {
            currentList.value = currentList.value.filter((item) => item.id !== id);
            uni.showToast({
              title: "已取消关注",
              icon: "success"
            });
          }
        } catch (error2) {
          uni.showToast({
            title: "操作失败",
            icon: "none"
          });
        }
      };
      const navigateToGoods = (id) => {
        uni.navigateTo({
          url: `/pages/goods/goods?goods_id=${id}`
        });
      };
      const navigateToBrand = (id) => {
        uni.navigateTo({
          url: `/pages/brand/brand?brand_id=${id}`
        });
      };
      const navigateToCollocation = (id) => {
        uni.navigateTo({
          url: `/pages/collocation/detail?id=${id}`
        });
      };
      const navigateToShowcase = (id) => {
        uni.navigateTo({
          url: `/pages/showcase/detail?id=${id}`
        });
      };
      uni.setNavigationBarTitle({
        title: "我的关注"
      });
      const __returned__ = { categories, activeCategory, currentList, loading: loading2, noMore, pagination, checkLogin, switchCategory, loadData, loadMore, resetPagination, getFirstImage, getFirstCollocationImage, unfollowGoods, navigateToGoods, navigateToBrand, navigateToCollocation, navigateToShowcase, ref: vue.ref, reactive: vue.reactive, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$Y(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "nav-bar" }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.categories, (item, index) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: index,
              class: vue.normalizeClass(["nav-item", { active: $setup.activeCategory === item.type }]),
              onClick: ($event) => $setup.switchCategory(item.type)
            }, vue.toDisplayString(item.name), 11, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "scroll-view",
          "scroll-y": "",
          onScrolltolower: $setup.loadMore,
          "show-scrollbar": false
        },
        [
          vue.createElementVNode("view", { class: "list-container" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.currentList, (item, index) => {
                return vue.openBlock(), vue.createElementBlock(
                  vue.Fragment,
                  {
                    key: item.id
                  },
                  [
                    $setup.activeCategory === 1 && item.goods ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "list-item goods-item",
                      onClick: ($event) => $setup.navigateToGoods(item.goods.id)
                    }, [
                      vue.createElementVNode("view", { class: "goods-left" }, [
                        vue.createElementVNode("image", {
                          class: "goods-thumb",
                          src: $setup.getFirstImage(item.goods.goods_images),
                          mode: "aspectFill"
                        }, null, 8, ["src"]),
                        vue.createElementVNode(
                          "view",
                          { class: "price-tag" },
                          "¥" + vue.toDisplayString(item.goods.total_amount),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "goods-right" }, [
                        vue.createElementVNode("view", { class: "goods-header" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "goods-title line-clamp-2" },
                            vue.toDisplayString(item.goods.name),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode("button", {
                            class: "unfollow-btn",
                            onClick: vue.withModifiers(($event) => $setup.unfollowGoods(item.id), ["stop"])
                          }, "取消关注", 8, ["onClick"])
                        ]),
                        vue.createElementVNode("view", { class: "goods-specs" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "spec-item" },
                            vue.toDisplayString(item.goods.brand_name),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "spec-item" },
                            vue.toDisplayString(item.goods.size),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "spec-item" },
                            vue.toDisplayString(item.goods.skin.split(" ")[0]),
                            1
                            /* TEXT */
                          )
                        ]),
                        vue.createElementVNode("view", { class: "goods-footer" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "goods-type" },
                            vue.toDisplayString(item.goods.type),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "goods-material" },
                            vue.toDisplayString(item.goods.doll_material),
                            1
                            /* TEXT */
                          )
                        ])
                      ])
                    ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                    $setup.activeCategory === 2 && item.brand ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "list-item brand-item",
                      onClick: ($event) => $setup.navigateToBrand(item.brand.id)
                    }, [
                      vue.createElementVNode("view", { class: "brand-content" }, [
                        vue.createElementVNode("image", {
                          class: "brand-logo",
                          src: item.brand.logo_image,
                          mode: "aspectFit"
                        }, null, 8, ["src"]),
                        vue.createElementVNode("view", { class: "brand-info" }, [
                          vue.createElementVNode("view", { class: "brand-header" }, [
                            vue.createElementVNode(
                              "text",
                              { class: "brand-name" },
                              vue.toDisplayString(item.brand.brand_name),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "brand-country" },
                              vue.toDisplayString(item.brand.country_name),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode(
                            "text",
                            { class: "brand-description line-clamp-2" },
                            vue.toDisplayString(item.brand.description),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode("view", { class: "brand-actions" }, [
                            vue.createElementVNode("button", {
                              class: "action-btn",
                              onClick: vue.withModifiers(($event) => _ctx.unfollowBrand(item.id), ["stop"])
                            }, "取消关注", 8, ["onClick"])
                          ])
                        ])
                      ])
                    ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                    $setup.activeCategory === 3 && item.collocation ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 2,
                      class: "list-item",
                      onClick: ($event) => $setup.navigateToCollocation(item.collocation.id)
                    }, [
                      vue.createElementVNode("image", {
                        class: "item-image",
                        src: $setup.getFirstCollocationImage(item.collocation.image_urls),
                        mode: "aspectFill"
                      }, null, 8, ["src"]),
                      vue.createElementVNode(
                        "text",
                        { class: "item-title" },
                        vue.toDisplayString(item.collocation.title),
                        1
                        /* TEXT */
                      )
                    ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                    $setup.activeCategory === 4 && item.showcase ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 3,
                      class: "list-item",
                      onClick: ($event) => $setup.navigateToShowcase(item.showcase.id)
                    }, [
                      vue.createElementVNode("image", {
                        class: "item-image",
                        src: $setup.getFirstImage(item.showcase.image_urls),
                        mode: "aspectFill"
                      }, null, 8, ["src"]),
                      vue.createElementVNode(
                        "text",
                        { class: "item-title" },
                        vue.toDisplayString(item.showcase.name),
                        1
                        /* TEXT */
                      )
                    ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
                  ],
                  64
                  /* STABLE_FRAGMENT */
                );
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            vue.createElementVNode("view", { class: "loading-status" }, [
              $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "loading-text"
              }, "加载中...")) : vue.createCommentVNode("v-if", true),
              $setup.noMore ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 1,
                class: "no-more-text"
              }, "没有更多了")) : vue.createCommentVNode("v-if", true)
            ])
          ])
        ],
        32
        /* NEED_HYDRATION */
      )
    ]);
  }
  const PagesUserLikeUserLike = /* @__PURE__ */ _export_sfc(_sfc_main$Z, [["render", _sfc_render$Y], ["__scopeId", "data-v-909fcc31"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/user_like/user_like.vue"]]);
  const _imports_0$6 = "/static/new-icon/findfind.gif";
  const _imports_1$3 = "/static/new-icon/sesee.png";
  const SIDE_PADDING_RPX = 20;
  const GUTTER_RPX = 12;
  const TABS_EXPANDED_RPX = 150;
  const TABS_COLLAPSED_RPX = 60;
  const FIND_PANEL_RPX = 260;
  const VIEW_PANEL_RPX = 160;
  const pageSize$4 = 10;
  const COLUMN_COUNT = 2;
  const _sfc_main$Y = {
    __name: "collocation_square",
    setup(__props, { expose: __expose }) {
      __expose();
      const NO_IMG = "data:image/svg+xml;utf8," + encodeURIComponent(
        `<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400">
      <rect width="100%" height="100%" fill="#e9ebef"/>
      <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle"
        fill="#9aa0a6" font-size="40" font-family="Arial">No Image</text>
    </svg>`
      );
      function firstGoodsImage(goods) {
        const arr = Array.isArray(goods == null ? void 0 : goods.goods_images) ? goods.goods_images : [];
        let url = (arr[0] || "").trim();
        if (!url)
          url = (goods == null ? void 0 : goods.goods_name_image) || (goods == null ? void 0 : goods.goods_brand_name_image) || "";
        if (url) {
          const low = url.toLowerCase();
          if (low.includes("/default") || low.endsWith("default.png") || low.includes("noimage"))
            url = "";
        }
        if (goods == null ? void 0 : goods.__imgBroken)
          url = "";
        return url || NO_IMG;
      }
      function onGoodsImgError(goods) {
        if (goods)
          goods.__imgBroken = true;
      }
      function onCardImgError(item) {
        if (!item)
          return;
        if (!Array.isArray(item.image_urls))
          item.image_urls = [];
        item.image_urls[0] = NO_IMG;
      }
      function onCardImgLoad() {
        layoutFunction(true);
      }
      const si = (global$1 == null ? void 0 : global$1.systemInfo) || uni.getSystemInfoSync();
      const px2rpx = (px) => si && si.windowWidth ? 750 / si.windowWidth * px : px;
      const rpx2px2 = (rpx) => si && si.windowWidth ? si.windowWidth / 750 * rpx : rpx;
      const windowHeightRpx = Math.round(px2rpx(si.windowHeight || 0));
      const statusBarRpx = Math.round(px2rpx(si.statusBarHeight || 0));
      const SIDE_PADDING_PX = rpx2px2(SIDE_PADDING_RPX);
      const GUTTER_PX = rpx2px2(GUTTER_RPX);
      const collapsePx = vue.ref(0);
      const MAX_COLLAPSE_PX = rpx2px2(TABS_EXPANDED_RPX - TABS_COLLAPSED_RPX);
      const progress = vue.computed(() => MAX_COLLAPSE_PX ? collapsePx.value / MAX_COLLAPSE_PX : 0);
      const tabsHeightRpx = vue.computed(
        () => Math.round(TABS_EXPANDED_RPX - (TABS_EXPANDED_RPX - TABS_COLLAPSED_RPX) * progress.value)
      );
      const auxFullRpx = vue.computed(() => currentTab.value === "find" ? FIND_PANEL_RPX : VIEW_PANEL_RPX);
      const auxHeightRpx = vue.computed(() => Math.round(auxFullRpx.value * (1 - progress.value)));
      const headerContentRpx = vue.computed(() => tabsHeightRpx.value + auxHeightRpx.value);
      const headerTotalRpx = vue.computed(() => statusBarRpx + headerContentRpx.value);
      const listHeightRpx = vue.computed(() => Math.max(0, windowHeightRpx - headerTotalRpx.value));
      const atTop = vue.ref(true);
      const onContentScroll = (e2) => {
        var _a;
        const t2 = Math.max(0, ((_a = e2 == null ? void 0 : e2.detail) == null ? void 0 : _a.scrollTop) || 0);
        atTop.value = t2 <= 0;
        collapsePx.value = Math.min(MAX_COLLAPSE_PX, t2);
      };
      let startY = 0;
      const pullingExpand = vue.ref(false);
      const onTouchStart = (e2) => {
        var _a, _b;
        startY = ((_b = (_a = e2 == null ? void 0 : e2.touches) == null ? void 0 : _a[0]) == null ? void 0 : _b.clientY) || 0;
        pullingExpand.value = false;
      };
      const onTouchMove = (e2) => {
        var _a, _b;
        const y2 = ((_b = (_a = e2 == null ? void 0 : e2.touches) == null ? void 0 : _a[0]) == null ? void 0 : _b.clientY) || 0;
        const dy = y2 - startY;
        if (atTop.value && progress.value > 0 && dy > 0) {
          pullingExpand.value = true;
          collapsePx.value = Math.max(0, collapsePx.value - dy);
          startY = y2;
        }
      };
      const onTouchEnd = () => {
        pullingExpand.value = false;
      };
      const enableRefresher = vue.computed(() => progress.value === 0);
      const currentTab = vue.ref("find");
      const findRefreshing = vue.ref(false);
      const onFindRefresh = async () => {
        if (findRefreshing.value)
          return;
        findRefreshing.value = true;
        try {
          await fetchRandomGoods(true);
        } finally {
          findRefreshing.value = false;
        }
      };
      const viewRefreshing = vue.ref(false);
      const onViewRefresh = async () => {
        if (viewRefreshing.value)
          return;
        viewRefreshing.value = true;
        try {
          await fetchCollocations(true);
        } finally {
          viewRefreshing.value = false;
        }
      };
      const collocationList = vue.ref([]);
      const loading2 = vue.ref(false);
      const noMore = vue.ref(false);
      const currentPage = vue.ref(1);
      const containerHeight = vue.ref(0);
      const cardWidth = vue.ref(0);
      const columnsHeight = vue.reactive([0, 0]);
      const instance = vue.getCurrentInstance();
      const miniProgram = false;
      const showPostMenu = vue.ref(false);
      const togglePostMenu = () => {
        showPostMenu.value = !showPostMenu.value;
      };
      const closePostMenu = () => {
        showPostMenu.value = false;
      };
      const filterGoods = vue.ref([]);
      const randomGoodsList = vue.ref([]);
      const goodsLoading = vue.ref(false);
      const goodsNoMore = vue.ref(false);
      const goodsCursor = vue.ref(-1);
      const selectedType = vue.ref("全部");
      const selectedSize = vue.ref("全部");
      const goodsTypes = vue.ref([]);
      const sizes = vue.ref([]);
      const goodsOptions = vue.ref([]);
      const handleTabSwitch = (tab) => {
        showPostMenu.value = false;
        closeFilter();
        if (currentTab.value === "view") {
          collocationList.value.forEach((item) => {
            delete item.position;
          });
          resetColumns();
        }
        switchTab(tab);
      };
      const switchTab = (tab) => {
        currentTab.value = tab;
        if (tab === "find") {
          if (goodsTypes.value.length === 0) {
            fetchGoodsTypes();
            fetchSizes();
          }
          if (randomGoodsList.value.length === 0) {
            fetchRandomGoods(true);
          }
        }
        if (tab === "view") {
          fetchCollocations(true);
        }
      };
      const { value: baseUrl } = websiteUrl;
      const fetchGoodsTypes = async () => {
        var _a;
        try {
          const res = await uni.request({ url: `${baseUrl}/goods-types`, method: "GET" });
          if (((_a = res.data) == null ? void 0 : _a.status) === "success")
            goodsTypes.value = ["全部", ...res.data.data || []];
        } catch (e2) {
          formatAppLog("error", "at pages/collocation_square/collocation_square.vue:443", e2);
        }
      };
      const fetchSizes = async () => {
        var _a;
        try {
          const res = await uni.request({ url: `${baseUrl}/sizes?show_type=hot`, method: "GET" });
          if (((_a = res.data) == null ? void 0 : _a.status) === "success")
            sizes.value = ["全部", ...Object.keys(res.data.data || {})];
        } catch (e2) {
          formatAppLog("error", "at pages/collocation_square/collocation_square.vue:449", e2);
        }
      };
      const selectType = (type) => {
        selectedType.value = type;
        fetchRandomGoods(true);
      };
      const selectSize = (size) => {
        selectedSize.value = size;
        fetchRandomGoods(true);
      };
      const fetchRandomGoods = async (reset = false) => {
        var _a;
        if (reset) {
          randomGoodsList.value = [];
          goodsCursor.value = -1;
          goodsNoMore.value = false;
        }
        if (goodsLoading.value || goodsNoMore.value)
          return;
        try {
          goodsLoading.value = true;
          const params = {
            cursor: goodsCursor.value,
            page_size: 10,
            type: selectedType.value === "全部" ? "" : selectedType.value,
            size: selectedSize.value === "全部" ? "" : selectedSize.value
          };
          const res = await uni.request({
            url: `${baseUrl}/random-list`,
            method: "POST",
            data: params,
            header: { "content-type": "application/json" }
          });
          if (((_a = res.data) == null ? void 0 : _a.status) === "success") {
            const data = res.data.data || {};
            const newItems = Array.isArray(data.goods_list) ? data.goods_list : [];
            randomGoodsList.value = reset ? newItems : [...randomGoodsList.value, ...newItems];
            goodsCursor.value = data.next_cursor;
            goodsNoMore.value = newItems.length < 10;
          }
        } catch (e2) {
          uni.showToast({ title: "加载失败", icon: "none" });
        } finally {
          goodsLoading.value = false;
        }
      };
      const loadMoreGoods = () => {
        fetchRandomGoods();
      };
      const cardStyle = (index) => {
        const item = collocationList.value[index];
        if (!(item == null ? void 0 : item.position))
          return {};
        return { left: `${item.position.left}px`, top: `${item.position.top}px`, width: `${cardWidth.value}px` };
      };
      const resetColumns = () => {
        for (let i2 = 0; i2 < COLUMN_COUNT; i2++)
          columnsHeight[i2] = 0;
      };
      const calculateLayout = (inst, { force = false } = {}) => {
        var _a;
        if (!inst || !((_a = collocationList.value) == null ? void 0 : _a.length))
          return;
        const query = uni.createSelectorQuery().in(inst.proxy);
        query.selectAll(".card").boundingClientRect((rects) => {
          if (!rects || !rects.length)
            return;
          const zeroCount = rects.filter((r2) => !r2 || !r2.height).length;
          if (zeroCount > 0 && !force) {
            setTimeout(() => calculateLayout(inst), 120);
            return;
          }
          resetColumns();
          rects.forEach((rect, i2) => {
            if (!rect)
              return;
            let colIdx = 0;
            for (let c2 = 1; c2 < COLUMN_COUNT; c2++) {
              if (columnsHeight[c2] < columnsHeight[colIdx])
                colIdx = c2;
            }
            const left = SIDE_PADDING_PX + colIdx * (cardWidth.value + GUTTER_PX);
            const top = columnsHeight[colIdx];
            columnsHeight[colIdx] = top + rect.height + GUTTER_PX;
            const item = collocationList.value[i2];
            if (item)
              item.position = { left, top };
          });
          containerHeight.value = Math.max(...columnsHeight);
        }).exec();
      };
      let layoutTimer = null;
      const layoutFunction = async (force = false) => {
        await vue.nextTick();
        if (layoutTimer) {
          clearTimeout(layoutTimer);
          layoutTimer = null;
        }
        calculateLayout(instance, { force });
        layoutTimer = setTimeout(() => calculateLayout(instance, { force }), 250);
        setTimeout(() => calculateLayout(instance, { force }), 600);
      };
      const fetchCollocations = async (reset = false) => {
        var _a;
        if (reset) {
          collocationList.value = [];
          currentPage.value = 1;
          noMore.value = false;
          loading2.value = false;
          resetColumns();
        }
        if (loading2.value || noMore.value) {
          layoutFunction();
          return;
        }
        try {
          loading2.value = true;
          const params = { page: currentPage.value, page_size: pageSize$4, filter_goods_id_list: filterGoods.value.map((g2) => g2.goods_id) };
          const res = await uni.request({
            url: `${baseUrl}/collocation-list`,
            method: "POST",
            data: params,
            header: { "content-type": "application/json" }
          });
          if (((_a = res.data) == null ? void 0 : _a.status) === "success") {
            const data = res.data.data || {};
            const newItems = Array.isArray(data.collocation_relation_list) ? data.collocation_relation_list : [];
            collocationList.value = reset ? newItems : [...collocationList.value, ...newItems];
            noMore.value = newItems.length < pageSize$4;
            currentPage.value++;
            if (reset && newItems.length === 0)
              uni.showToast({ title: "暂无相关搭配", icon: "none" });
            layoutFunction();
          }
        } catch (e2) {
          uni.showToast({ title: "加载失败", icon: "none" });
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (!noMore.value)
          fetchCollocations();
      };
      const filterPopupRef = vue.ref(null);
      const openFilter = () => {
        var _a;
        (_a = filterPopupRef.value) == null ? void 0 : _a.open("center");
      };
      const closeFilter = () => {
        var _a;
        (_a = filterPopupRef.value) == null ? void 0 : _a.close();
      };
      const showFilter = () => {
        openFilter();
      };
      const applyFilterAndOpen = () => {
        openFilter();
      };
      const handleGoodsSelect = (goods) => {
        if (!(goods == null ? void 0 : goods.id))
          return;
        const newGoods = { goods_id: goods.id, goods_name: `${goods.brand_name ? goods.brand_name + "·" : ""}${goods.name}` };
        if (!filterGoods.value.some((item) => item.goods_id === newGoods.goods_id)) {
          filterGoods.value = [...filterGoods.value, newGoods];
        }
      };
      const applyFilter = async () => {
        closeFilter();
        await fetchCollocations(true);
      };
      const removeGood = (goodsId, e2) => {
        var _a;
        (_a = e2 == null ? void 0 : e2.stopPropagation) == null ? void 0 : _a.call(e2);
        filterGoods.value = filterGoods.value.filter((g2) => g2.goods_id !== goodsId);
        noMore.value = false;
        fetchCollocations(true);
      };
      const resetFilter = () => {
        filterGoods.value = [];
      };
      const jumpGoods = (id) => {
        uni.navigateTo({ url: "/pages/goods/goods?goods_id=" + id });
      };
      const jump2collectionDetail = (id, origin) => {
        uni.navigateTo({ url: `/pages/collocation_share/collocation_share?collocation_id=${id}&origin=${origin}` });
      };
      const jumpToUserPage = (uid) => {
        uni.navigateTo({ url: `/pages/user_page/user_page?uid=${uid}` });
      };
      const getUserName = (name) => name && name.length > 10 ? `${name.toString().slice(-8)}...` : name;
      const goToCollocation = () => uni.showToast({ title: "TODO: 发搭配", icon: "none" });
      const goToShowcase = () => uni.showToast({ title: "TODO: 发展示柜", icon: "none" });
      vue.onMounted(() => {
        const containerWidthPx = si.windowWidth - SIDE_PADDING_PX * 2;
        cardWidth.value = (containerWidthPx - GUTTER_PX) / 2;
        fetchGoodsTypes();
        fetchSizes();
        fetchRandomGoods(true);
      });
      onLoad(() => {
      });
      const __returned__ = { NO_IMG, firstGoodsImage, onGoodsImgError, onCardImgError, onCardImgLoad, si, px2rpx, rpx2px: rpx2px2, windowHeightRpx, statusBarRpx, SIDE_PADDING_RPX, GUTTER_RPX, SIDE_PADDING_PX, GUTTER_PX, TABS_EXPANDED_RPX, TABS_COLLAPSED_RPX, FIND_PANEL_RPX, VIEW_PANEL_RPX, collapsePx, MAX_COLLAPSE_PX, progress, tabsHeightRpx, auxFullRpx, auxHeightRpx, headerContentRpx, headerTotalRpx, listHeightRpx, atTop, onContentScroll, get startY() {
        return startY;
      }, set startY(v2) {
        startY = v2;
      }, pullingExpand, onTouchStart, onTouchMove, onTouchEnd, enableRefresher, currentTab, findRefreshing, onFindRefresh, viewRefreshing, onViewRefresh, collocationList, loading: loading2, noMore, currentPage, pageSize: pageSize$4, containerHeight, cardWidth, columnsHeight, COLUMN_COUNT, instance, miniProgram, showPostMenu, togglePostMenu, closePostMenu, filterGoods, randomGoodsList, goodsLoading, goodsNoMore, goodsCursor, selectedType, selectedSize, goodsTypes, sizes, goodsOptions, handleTabSwitch, switchTab, baseUrl, fetchGoodsTypes, fetchSizes, selectType, selectSize, fetchRandomGoods, loadMoreGoods, cardStyle, resetColumns, calculateLayout, get layoutTimer() {
        return layoutTimer;
      }, set layoutTimer(v2) {
        layoutTimer = v2;
      }, layoutFunction, fetchCollocations, loadMore, filterPopupRef, openFilter, closeFilter, showFilter, applyFilterAndOpen, handleGoodsSelect, applyFilter, removeGood, resetFilter, jumpGoods, jump2collectionDetail, jumpToUserPage, getUserName, goToCollocation, goToShowcase, ref: vue.ref, reactive: vue.reactive, onMounted: vue.onMounted, nextTick: vue.nextTick, getCurrentInstance: vue.getCurrentInstance, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$X(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_goods_search = resolveEasycom(vue.resolveDynamicComponent("goods-search"), __easycom_3$2);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_transition = resolveEasycom(vue.resolveDynamicComponent("uni-transition"), __easycom_3$9);
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    const _component_loading_toast = resolveEasycom(vue.resolveDynamicComponent("loading-toast"), __easycom_3$8);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createElementVNode("meta", {
          name: "theme-color",
          content: "#def9ff"
        }),
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "page-root" }, [
          vue.createElementVNode(
            "view",
            {
              class: "header-fixed",
              style: vue.normalizeStyle({
                paddingTop: $setup.statusBarRpx + "rpx",
                boxShadow: `0 6rpx 18rpx rgba(0,0,0,${0.1 * $setup.progress})`
              })
            },
            [
              vue.createElementVNode(
                "view",
                {
                  class: "tabs-area",
                  style: vue.normalizeStyle({ height: $setup.tabsHeightRpx + "rpx" })
                },
                [
                  vue.createElementVNode("view", { class: "tabs-inner" }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: "layer image-layer",
                        style: vue.normalizeStyle({ opacity: 1 - $setup.progress })
                      },
                      [
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["tab", { active: $setup.currentTab === "find" }]),
                            onClick: _cache[0] || (_cache[0] = ($event) => $setup.handleTabSwitch("find"))
                          },
                          [
                            vue.createElementVNode("image", {
                              src: _imports_0$6,
                              class: "tab-image",
                              mode: "aspectFill"
                            })
                          ],
                          2
                          /* CLASS */
                        ),
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["tab", { active: $setup.currentTab === "view" }]),
                            onClick: _cache[1] || (_cache[1] = ($event) => $setup.handleTabSwitch("view"))
                          },
                          [
                            vue.createElementVNode("image", {
                              src: _imports_1$3,
                              class: "tab-image",
                              mode: "aspectFill"
                            })
                          ],
                          2
                          /* CLASS */
                        )
                      ],
                      4
                      /* STYLE */
                    ),
                    vue.createElementVNode(
                      "view",
                      {
                        class: "layer text-layer",
                        style: vue.normalizeStyle({ opacity: $setup.progress })
                      },
                      [
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["tab text", { active: $setup.currentTab === "find" }]),
                            onClick: _cache[2] || (_cache[2] = ($event) => $setup.handleTabSwitch("find"))
                          },
                          [
                            vue.createElementVNode("text", { class: "tab-text" }, "找找")
                          ],
                          2
                          /* CLASS */
                        ),
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["tab text", { active: $setup.currentTab === "view" }]),
                            onClick: _cache[3] || (_cache[3] = ($event) => $setup.handleTabSwitch("view"))
                          },
                          [
                            vue.createElementVNode("text", { class: "tab-text" }, "看看")
                          ],
                          2
                          /* CLASS */
                        )
                      ],
                      4
                      /* STYLE */
                    )
                  ])
                ],
                4
                /* STYLE */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: "aux-container",
                  style: vue.normalizeStyle({ height: $setup.auxHeightRpx + "rpx", opacity: 1 - $setup.progress })
                },
                [
                  vue.withDirectives(vue.createElementVNode(
                    "view",
                    { class: "category-container" },
                    [
                      vue.createElementVNode("view", {
                        class: "search_tab",
                        style: { "margin": "10rpx 0 40rpx 0" }
                      }, [
                        vue.createVNode(_component_goods_search, {
                          "hidden-icon": false,
                          width: "670rpx"
                        })
                      ]),
                      vue.createElementVNode("scroll-view", {
                        "scroll-x": "",
                        class: "category-scroll",
                        "show-scrollbar": false
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.goodsTypes, (type, index) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              class: vue.normalizeClass(["category-item", { active: $setup.selectedType === type }]),
                              key: index,
                              onClick: ($event) => $setup.selectType(type)
                            }, vue.toDisplayString(type), 11, ["onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ]),
                      vue.createElementVNode("scroll-view", {
                        "scroll-x": "",
                        class: "size-scroll",
                        "show-scrollbar": false
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.sizes, (size, index) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              class: vue.normalizeClass(["size-item", { active: $setup.selectedSize === size }]),
                              key: index,
                              onClick: ($event) => $setup.selectSize(size)
                            }, vue.toDisplayString(size), 11, ["onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ],
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vShow, $setup.currentTab === "find"]
                  ]),
                  vue.withDirectives(vue.createElementVNode(
                    "view",
                    { class: "filter-bar-container" },
                    [
                      vue.createElementVNode("view", { class: "filter-bar" }, [
                        vue.createElementVNode("view", {
                          class: "filter-tags",
                          onClick: $setup.showFilter
                        }, [
                          vue.createElementVNode("view", { class: "selected-goods" }, [
                            (vue.openBlock(true), vue.createElementBlock(
                              vue.Fragment,
                              null,
                              vue.renderList($setup.filterGoods, (goods) => {
                                return vue.openBlock(), vue.createElementBlock("view", {
                                  key: goods.goods_id,
                                  class: "tag",
                                  onClick: vue.withModifiers((e2) => $setup.removeGood(goods.goods_id, e2), ["stop"])
                                }, [
                                  vue.createTextVNode(
                                    vue.toDisplayString(goods.goods_name) + " ",
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode("text", { class: "remove" }, "×")
                                ], 8, ["onClick"]);
                              }),
                              128
                              /* KEYED_FRAGMENT */
                            ))
                          ]),
                          !$setup.filterGoods.length ? (vue.openBlock(), vue.createElementBlock("text", {
                            key: 0,
                            class: "tip"
                          }, "当前无筛选条件")) : vue.createCommentVNode("v-if", true)
                        ]),
                        vue.createElementVNode("button", {
                          class: "submit-btn",
                          onClick: vue.withModifiers($setup.applyFilterAndOpen, ["stop"])
                        }, "筛选")
                      ])
                    ],
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vShow, $setup.currentTab === "view"]
                  ])
                ],
                4
                /* STYLE */
              )
            ],
            4
            /* STYLE */
          ),
          vue.createElementVNode(
            "view",
            {
              class: "page-body",
              style: vue.normalizeStyle({ marginTop: $setup.headerTotalRpx + "rpx" })
            },
            [
              $setup.currentTab === "find" ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                vue.createElementVNode("scroll-view", {
                  class: "goods-list",
                  "scroll-y": "",
                  style: vue.normalizeStyle({ height: $setup.listHeightRpx + "rpx" }),
                  "refresher-enabled": $setup.enableRefresher,
                  "refresher-triggered": $setup.findRefreshing,
                  "refresher-default-style": "black",
                  "refresher-background": "#f5f5f5",
                  onRefresherrefresh: $setup.onFindRefresh,
                  onRefresherrestore: _cache[4] || (_cache[4] = () => {
                    $setup.findRefreshing = false;
                  }),
                  onRefresherabort: _cache[5] || (_cache[5] = () => {
                    $setup.findRefreshing = false;
                  }),
                  onScrolltolower: $setup.loadMoreGoods,
                  "show-scrollbar": false,
                  onScroll: $setup.onContentScroll,
                  onTouchstart: $setup.onTouchStart,
                  onTouchmove: $setup.onTouchMove,
                  onTouchend: $setup.onTouchEnd
                }, [
                  vue.createElementVNode("view", { class: "goods-container" }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList($setup.randomGoodsList, (goods, index) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          key: goods.id,
                          class: "goods-card",
                          onClick: ($event) => $setup.jumpGoods(goods.id)
                        }, [
                          vue.createElementVNode("image", {
                            src: $setup.firstGoodsImage(goods),
                            mode: "aspectFill",
                            class: "goods-image",
                            onError: ($event) => $setup.onGoodsImgError(goods)
                          }, null, 40, ["src", "onError"]),
                          vue.createElementVNode("view", { class: "goods-info" }, [
                            vue.createElementVNode(
                              "text",
                              { class: "goods-name" },
                              vue.toDisplayString(goods.name),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "goods-brand" },
                              vue.toDisplayString(goods.brand_name),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "goods-price" },
                              vue.toDisplayString(goods.total_amount) + " " + vue.toDisplayString(goods.currency),
                              1
                              /* TEXT */
                            )
                          ])
                        ], 8, ["onClick"]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ]),
                  vue.createElementVNode("view", { class: "loading-state" }, [
                    $setup.goodsLoading ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中...")) : vue.createCommentVNode("v-if", true),
                    $setup.goodsNoMore ? (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "no-more"
                    }, "没有更多了")) : vue.createCommentVNode("v-if", true)
                  ])
                ], 44, ["refresher-enabled", "refresher-triggered"])
              ])) : vue.createCommentVNode("v-if", true),
              $setup.currentTab === "view" ? (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
                vue.createElementVNode("view", {
                  class: "container",
                  style: { "height": "100vh" }
                }, [
                  vue.createElementVNode("scroll-view", {
                    class: "card-list",
                    "scroll-y": "",
                    style: vue.normalizeStyle({ height: $setup.listHeightRpx + "rpx" }),
                    "refresher-enabled": $setup.enableRefresher,
                    "refresher-triggered": $setup.viewRefreshing,
                    "refresher-default-style": "black",
                    "refresher-background": "#f5f5f5",
                    onRefresherrefresh: $setup.onViewRefresh,
                    onRefresherrestore: _cache[6] || (_cache[6] = () => {
                      $setup.viewRefreshing = false;
                    }),
                    onRefresherabort: _cache[7] || (_cache[7] = () => {
                      $setup.viewRefreshing = false;
                    }),
                    onScrolltolower: $setup.loadMore,
                    "show-scrollbar": false,
                    onScroll: $setup.onContentScroll,
                    onTouchstart: $setup.onTouchStart,
                    onTouchmove: $setup.onTouchMove,
                    onTouchend: $setup.onTouchEnd
                  }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: "cards-container",
                        style: vue.normalizeStyle({ height: $setup.containerHeight + "px" })
                      },
                      [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList($setup.collocationList, (item, index) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              key: item.collocation_id,
                              class: "card",
                              id: "card-" + index,
                              style: vue.normalizeStyle($setup.cardStyle(index)),
                              onClick: ($event) => $setup.jump2collectionDetail(item.collocation_id, item.origin)
                            }, [
                              vue.createElementVNode("view", {
                                class: "user-info",
                                onClick: vue.withModifiers(($event) => $setup.jumpToUserPage(item.uid), ["stop"])
                              }, [
                                vue.createElementVNode("image", {
                                  src: item.avatar || "/default_avatar.jpg",
                                  class: "avatar",
                                  mode: "aspectFill"
                                }, null, 8, ["src"]),
                                vue.createElementVNode("view", { class: "user-meta" }, [
                                  vue.createElementVNode(
                                    "text",
                                    { class: "username" },
                                    vue.toDisplayString($setup.getUserName(item.username)),
                                    1
                                    /* TEXT */
                                  ),
                                  vue.createElementVNode("view", { class: "like-count" }, [
                                    vue.createVNode(_component_uni_icons, {
                                      type: "hand-up",
                                      size: "18",
                                      color: "#5f85a3"
                                    }),
                                    vue.createElementVNode(
                                      "text",
                                      { style: { "margin": "0 5rpx", "color": "#5f85a3" } },
                                      vue.toDisplayString(item.like_count),
                                      1
                                      /* TEXT */
                                    )
                                  ])
                                ])
                              ], 8, ["onClick"]),
                              Array.isArray(item.image_urls) && item.image_urls.length > 0 ? (vue.openBlock(), vue.createElementBlock("image", {
                                key: 0,
                                src: item.image_urls[0] || $setup.NO_IMG,
                                mode: "aspectFill",
                                class: "card-image",
                                "lazy-load": "",
                                onError: ($event) => $setup.onCardImgError(item),
                                onLoad: $setup.onCardImgLoad
                              }, null, 40, ["src", "onError"])) : vue.createCommentVNode("v-if", true),
                              vue.createElementVNode("view", { class: "card-content" }, [
                                vue.createElementVNode(
                                  "text",
                                  { class: "title" },
                                  vue.toDisplayString(item.title),
                                  1
                                  /* TEXT */
                                ),
                                vue.createElementVNode(
                                  "text",
                                  { class: "desc" },
                                  vue.toDisplayString(item.content),
                                  1
                                  /* TEXT */
                                ),
                                vue.createElementVNode("view", { class: "goods-tags" }, [
                                  (vue.openBlock(true), vue.createElementBlock(
                                    vue.Fragment,
                                    null,
                                    vue.renderList(item.relation_list || [], (rel, idx) => {
                                      return vue.openBlock(), vue.createElementBlock("view", {
                                        class: "tag-box",
                                        key: idx
                                      }, [
                                        vue.createElementVNode(
                                          "text",
                                          { class: "goods-tag" },
                                          vue.toDisplayString(rel.relation_goods_name || "未命名商品"),
                                          1
                                          /* TEXT */
                                        )
                                      ]);
                                    }),
                                    128
                                    /* KEYED_FRAGMENT */
                                  ))
                                ])
                              ])
                            ], 12, ["id", "onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ],
                      4
                      /* STYLE */
                    ),
                    vue.createElementVNode("view", { class: "loading-state" }, [
                      $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中...")) : vue.createCommentVNode("v-if", true),
                      $setup.noMore ? (vue.openBlock(), vue.createElementBlock("text", {
                        key: 1,
                        class: "no-more"
                      }, "没有更多了")) : vue.createCommentVNode("v-if", true)
                    ])
                  ], 44, ["refresher-enabled", "refresher-triggered"]),
                  vue.createElementVNode("view", {
                    class: "floating-button",
                    onClick: $setup.togglePostMenu
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "plusempty",
                      size: "30",
                      color: "#fff"
                    })
                  ]),
                  vue.createVNode(_component_uni_transition, {
                    name: "fade",
                    mode: "out-in",
                    duration: 300
                  }, {
                    default: vue.withCtx(() => [
                      $setup.showPostMenu ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "post-menu-mask",
                        onClick: $setup.closePostMenu
                      })) : vue.createCommentVNode("v-if", true)
                    ]),
                    _: 1
                    /* STABLE */
                  }),
                  vue.createElementVNode(
                    "view",
                    {
                      class: vue.normalizeClass(["post-menu", { show: $setup.showPostMenu }])
                    },
                    [
                      vue.createElementVNode("view", {
                        class: "menu-item",
                        onClick: $setup.goToCollocation
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          style: { "margin-right": "10rpx" },
                          type: "color",
                          size: "24",
                          color: "#5f85a3"
                        }),
                        vue.createElementVNode("text", null, "发搭配分享")
                      ]),
                      vue.createElementVNode("view", {
                        class: "menu-item",
                        onClick: $setup.goToShowcase
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          style: { "margin-right": "10rpx" },
                          type: "compose",
                          size: "24",
                          color: "#5f85a3"
                        }),
                        vue.createElementVNode("text", null, "发展示柜")
                      ])
                    ],
                    2
                    /* CLASS */
                  ),
                  vue.createVNode(
                    _component_uni_popup,
                    {
                      ref: "filterPopupRef",
                      type: "center",
                      "mask-click": true
                    },
                    {
                      default: vue.withCtx(() => [
                        vue.createElementVNode("view", { class: "filter-modal" }, [
                          vue.createElementVNode(
                            "view",
                            {
                              class: "modal-header",
                              style: vue.normalizeStyle($setup.miniProgram ? "margin-top:60rpx" : "margin-top:0rpx")
                            },
                            [
                              vue.createElementVNode("text", { class: "title" }, "筛选看想要的娃物搭配吧！"),
                              vue.createElementVNode("text", {
                                class: "close",
                                onClick: $setup.closeFilter
                              }, "×")
                            ],
                            4
                            /* STYLE */
                          ),
                          vue.createElementVNode("view", { class: "modal-body" }, [
                            vue.createElementVNode("view", { class: "search_tab" }, [
                              vue.createVNode(_component_goods_search, {
                                mode: "fill",
                                onSelect: $setup.handleGoodsSelect,
                                background: "#f8f8f8",
                                width: "100%"
                              })
                            ]),
                            vue.createElementVNode("view", { class: "selected-goods" }, [
                              (vue.openBlock(true), vue.createElementBlock(
                                vue.Fragment,
                                null,
                                vue.renderList($setup.filterGoods, (goods) => {
                                  return vue.openBlock(), vue.createElementBlock("view", {
                                    key: goods.goods_id,
                                    class: "goods-tag",
                                    onClick: vue.withModifiers((e2) => $setup.removeGood(goods.goods_id, e2), ["stop"])
                                  }, [
                                    vue.createTextVNode(
                                      vue.toDisplayString(goods.goods_name) + " ",
                                      1
                                      /* TEXT */
                                    ),
                                    vue.createElementVNode("text", { class: "remove" }, "×")
                                  ], 8, ["onClick"]);
                                }),
                                128
                                /* KEYED_FRAGMENT */
                              ))
                            ])
                          ]),
                          vue.createElementVNode("view", { class: "action-btns" }, [
                            vue.createElementVNode("button", {
                              class: "btn reset",
                              onClick: $setup.resetFilter
                            }, "重置"),
                            vue.createElementVNode("button", {
                              class: "btn confirm",
                              onClick: $setup.applyFilter
                            }, "应用筛选")
                          ])
                        ])
                      ]),
                      _: 1
                      /* STABLE */
                    },
                    512
                    /* NEED_PATCH */
                  )
                ])
              ])) : vue.createCommentVNode("v-if", true)
            ],
            4
            /* STYLE */
          ),
          vue.createVNode(_component_loading_toast, {
            show: $setup.goodsLoading || $setup.loading
          }, null, 8, ["show"])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesCollocationSquareCollocationSquare = /* @__PURE__ */ _export_sfc(_sfc_main$Y, [["render", _sfc_render$X], ["__scopeId", "data-v-e981f8ca"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/collocation_square/collocation_square.vue"]]);
  const _sfc_main$X = {
    __name: "sale_news",
    setup(__props, { expose: __expose }) {
      __expose();
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      function goBack() {
        const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
        if (pages2.length > 1)
          uni.navigateBack({ delta: 1 });
        else
          uni.switchTab({ url: "/pages/index/index" });
      }
      const detailData = vue.ref({ title: "", content: "", image_list: [], created_at: 0 });
      const brand = vue.ref({ brand_name: "", logo: "" });
      const loading2 = vue.ref(true);
      const error2 = vue.ref(false);
      const errorMsg = vue.ref("");
      const pageId = vue.ref(0);
      const brandId = vue.ref(0);
      const hasLikeBrand = vue.ref(false);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      let commentsPage = vue.ref(1);
      let replyForItem = vue.ref({});
      const systemInfo2 = uni.getSystemInfoSync();
      const brandSaleInfo = vue.ref({
        presale: null,
        // waiting_goods[0]
        lastSale: null
        // latest_sale
      });
      const handleReplyComment = ({ parent, target }) => {
        var _a;
        let item = target ?? parent;
        if (replyForItem.value.id === item.id) {
          replyForItem.value = {};
          return;
        }
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      function viewFullImage(index) {
        uni.previewImage({
          current: detailData.value.image_list[index],
          urls: detailData.value.image_list
        });
      }
      uni.setNavigationBarTitle({ title: "图透详情" });
      function jump2brand(id) {
        uni.navigateTo({ url: "/pages/brand/brand?brand_id=" + id });
      }
      const onShareAppMessage2 = () => ({
        title: "BJD娃圈你想知道的这里都有~",
        path: "/pages/news/news",
        success(res) {
        },
        fail(err) {
        },
        mp: { wxpath: "/pages/index/index.html" }
      });
      const jump2user = (uid) => {
        uni.navigateTo({ url: "/pages/user_page/user_page?uid=" + uid });
      };
      const fetchNewsDetail = async (id) => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/sale-news-detail?id=${id}`,
            timeout: 5e3
          });
          if (res.data.status === "success") {
            detailData.value = { ...res.data.data, image_list: res.data.data.image_list || [] };
            brandId.value = res.data.data.brand_id;
            getBrandsInfo();
            fetchBrandSaleInfo();
          } else {
            handleError(res.data.msg || "数据加载失败");
          }
        } catch (err) {
          handleError("网络请求失败");
        } finally {
          loading2.value = false;
        }
      };
      const fetchBrandSaleInfo = () => {
        if (!brandId.value)
          return;
        uni.request({
          url: `${websiteUrl.value}/brand-goods-summary?brand_id=${brandId.value}`,
          method: "GET",
          timeout: 5e3,
          success: (res) => {
            if (res.data && res.data.status === "success" && res.data.data) {
              const data = res.data.data || {};
              brandSaleInfo.value.lastSale = data.latest_sale || null;
              if (Array.isArray(data.waiting_goods) && data.waiting_goods.length > 0) {
                brandSaleInfo.value.presale = data.waiting_goods[0];
              } else {
                brandSaleInfo.value.presale = null;
              }
            }
          },
          fail: () => {
          }
        });
      };
      const handleCommentSubmit = (submitData) => {
        var _a, _b, _c;
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: parseInt(pageId.value),
          type: 4,
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          ...submitData.is_anonymous ? {
            avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
            username: "匿名用户",
            is_anonymous: 1
          } : {
            avatar: global$1.userInfo.avatar,
            username: global$1.userInfo.nickname,
            is_anonymous: 0
          },
          ...submitData.association_id && { association_id: submitData.association_id, association_type: submitData.association_type },
          ...submitData.image_url && { image_url: submitData.image_url },
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id)
          (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(tempComment);
        else if (replyForItem.value.parent_id === 0)
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({ ...tempComment, parent_id: replyForItem.value.id });
        else
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({ ...tempComment, parent_id: replyForItem.value.parent_id });
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: { "Authorization": token },
          data: requestData,
          success: (res) => {
            var _a2, _b2;
            if (res.data.status == "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? {
                  avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
                  username: "匿名用户",
                  is_anonymous: 1
                } : {
                  avatar: global$1.userInfo.avatar,
                  username: global$1.userInfo.nickname,
                  is_anonymous: 0
                }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous)
                finalComment.reply_username = "匿名用户";
              (_a2 = commentListRef.value) == null ? void 0 : _a2.updateTempComment(tempComment.id, finalComment);
              uni.showToast({ title: "评论成功", icon: "success" });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => {
            var _a2;
            (_a2 = commentListRef.value) == null ? void 0 : _a2.removeTempComment(tempComment.id);
            uni.showToast({ title: "网络请求失败", icon: "none" });
          }
        });
      };
      function formatTimestamp(ts2) {
        if (!ts2)
          return "";
        const d2 = new Date(ts2 * 1e3);
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const da = String(d2.getDate()).padStart(2, "0");
        const h2 = String(d2.getHours()).padStart(2, "0");
        const mi = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${da} ${h2}:${mi}`;
      }
      function getGoodsName(item) {
        if (!item)
          return "";
        return item.goods_name || item.name || item.title || "";
      }
      function getGoodsTime(item) {
        if (!item)
          return "";
        if (item.waiting_sale)
          return "";
        if (item.sub_time && item.sub_time_end) {
          return `${formatTimestamp(item.sub_time)} ~ ${formatTimestamp(item.sub_time_end)}`;
        }
        const ts2 = item.sub_time || item.fin_time || item.create_time || item.created_at;
        return ts2 ? formatTimestamp(ts2) : "";
      }
      function getGoodsCover(item) {
        if (!item)
          return "";
        if (Array.isArray(item.goods_images) && item.goods_images.length > 0) {
          return item.goods_images[0];
        }
        return item.goods_image || item.cover_image || item.image || item.thumb || item.pic || "";
      }
      function getGoodsId(item) {
        if (!item)
          return 0;
        return item.goods_id || item.id || 0;
      }
      function goGoodsDetail(goodsId) {
        if (!goodsId)
          return;
        uni.navigateTo({
          url: `/pages/goods/goods?goods_id=${goodsId}`
        });
      }
      const getBrandsInfo = () => {
        uni.request({
          url: websiteUrl.value + "/brand-info?id=" + brandId.value,
          success: (res) => {
            if (res.data.status === "success") {
              brand.value = res.data.data;
              uni.setNavigationBarTitle({ title: res.data.data.brand_name });
              getHasLikeBrand();
            }
          },
          fail: () => handleError("品牌信息加载失败")
        });
      };
      const likeBrand = async () => {
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const url = `/with-state/${hasLikeBrand.value ? "unlike" : "add-like"}`;
        uni.request({
          url: websiteUrl.value + url,
          method: "POST",
          header: { Authorization: uni.getStorageSync("token") },
          data: { id: parseInt(brandId.value), type: 2 },
          success: (res) => {
            if (res.data.status === "success") {
              hasLikeBrand.value = !hasLikeBrand.value;
              uni.showToast({ title: hasLikeBrand.value ? "关注成功" : "已取消关注", icon: "none" });
            }
          }
        });
      };
      const getHasLikeBrand = () => {
        if (!global$1.isLogin)
          return;
        uni.request({
          url: `${websiteUrl.value}/with-state/hasLike?id=${brandId.value}&type=2`,
          method: "POST",
          header: { Authorization: uni.getStorageSync("token") },
          success: (res) => {
            var _a;
            hasLikeBrand.value = ((_a = res.data.data) == null ? void 0 : _a.id) > 0;
          }
        });
      };
      function handleError(msg) {
        error2.value = true;
        errorMsg.value = msg || "出错了";
      }
      onLoad((options) => {
        if (!options.id) {
          handleError("缺少必要参数");
          return;
        }
        pageId.value = options.id;
        fetchNewsDetail(options.id);
        asyncGetUserInfo().then(getHasLikeBrand);
      });
      const __returned__ = { scrollTop, goBack, detailData, brand, loading: loading2, error: error2, errorMsg, pageId, brandId, hasLikeBrand, commentListRef, commentInputRef, get commentsPage() {
        return commentsPage;
      }, set commentsPage(v2) {
        commentsPage = v2;
      }, get replyForItem() {
        return replyForItem;
      }, set replyForItem(v2) {
        replyForItem = v2;
      }, systemInfo: systemInfo2, brandSaleInfo, handleReplyComment, viewFullImage, jump2brand, onShareAppMessage: onShareAppMessage2, jump2user, fetchNewsDetail, fetchBrandSaleInfo, handleCommentSubmit, formatTimestamp, getGoodsName, getGoodsTime, getGoodsCover, getGoodsId, goGoodsDetail, getBrandsInfo, likeBrand, getHasLikeBrand, handleError, ref: vue.ref, get onLoad() {
        return onLoad;
      }, get onPageScroll() {
        return onPageScroll;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$W(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_zhouWei_navBar, {
          type: "transparentFixed",
          backState: 2e3,
          homeState: 2e3,
          fontColor: "#000",
          transparentFixedFontColor: "#000",
          scrollTop: $setup.scrollTop,
          titleCenter: true
        }, {
          left: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          transparentFixedLeft: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["scrollTop"]),
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "page" }, [
          vue.createElementVNode("meta", {
            name: "theme-color",
            content: "#F8F8F8"
          }),
          vue.createElementVNode("view", { style: { "position": "relative" } }, [
            vue.createElementVNode("swiper", {
              class: "swiper-box",
              "indicator-dots": true,
              autoplay: false
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.detailData.image_list, (img, index) => {
                  return vue.openBlock(), vue.createElementBlock("swiper-item", { key: index }, [
                    vue.createElementVNode("image", {
                      src: img,
                      mode: "aspectFill",
                      class: "swiper-image",
                      onClick: ($event) => $setup.viewFullImage(index)
                    }, null, 8, ["src", "onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ]),
          vue.createElementVNode("view", { class: "brand-info" }, [
            vue.createElementVNode("image", {
              class: "brand-logo",
              src: $setup.brand.logo_image,
              mode: "aspectFill",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.jump2brand($setup.brand.id))
            }, null, 8, ["src"]),
            vue.createElementVNode("view", {
              class: "brand-details",
              onClick: _cache[1] || (_cache[1] = ($event) => $setup.jump2brand($setup.brand.id))
            }, [
              vue.createElementVNode(
                "text",
                { class: "brand-name" },
                vue.toDisplayString($setup.brand.brand_name),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                { class: "publish-time" },
                vue.toDisplayString($setup.formatTimestamp($setup.detailData.created_at)),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode(
              "text",
              {
                class: "follow",
                onClick: $setup.likeBrand,
                style: vue.normalizeStyle({ background: $setup.hasLikeBrand ? "#ff6a6c" : "#65C3D6" })
              },
              vue.toDisplayString($setup.hasLikeBrand ? "已关注" : "+ 关注品牌"),
              5
              /* TEXT, STYLE */
            )
          ]),
          vue.createElementVNode("view", { class: "content-box" }, [
            vue.createElementVNode("view", null, [
              vue.createElementVNode(
                "text",
                { class: "title" },
                vue.toDisplayString($setup.detailData.title),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { style: { "margin": "20rpx 0rpx" } }, [
              vue.createElementVNode(
                "text",
                { class: "content" },
                vue.toDisplayString($setup.detailData.content),
                1
                /* TEXT */
              )
            ])
          ]),
          $setup.brandSaleInfo.presale || $setup.brandSaleInfo.lastSale ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "brand-sale-section"
          }, [
            $setup.brandSaleInfo.presale ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "sale-card",
              onClick: _cache[2] || (_cache[2] = ($event) => $setup.goGoodsDetail($setup.getGoodsId($setup.brandSaleInfo.presale)))
            }, [
              vue.createElementVNode("view", { class: "sale-card-body" }, [
                $setup.getGoodsCover($setup.brandSaleInfo.presale) ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  class: "sale-cover",
                  src: $setup.getGoodsCover($setup.brandSaleInfo.presale),
                  mode: "aspectFill"
                }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "sale-text" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "sale-title" },
                    vue.toDisplayString($setup.getGoodsName($setup.brandSaleInfo.presale)),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("text", { class: "sale-sub sale-sub-tag font-alimamashuhei" }, "预售商品"),
                  $setup.getGoodsTime($setup.brandSaleInfo.presale) ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "sale-sub"
                    },
                    " 预计开始：" + vue.toDisplayString($setup.getGoodsTime($setup.brandSaleInfo.presale)),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ]),
                vue.createVNode(_component_uni_icons, {
                  type: "arrow-right",
                  size: "18",
                  color: "#C0C4CC"
                })
              ])
            ])) : vue.createCommentVNode("v-if", true),
            $setup.brandSaleInfo.lastSale ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "sale-card",
              onClick: _cache[3] || (_cache[3] = ($event) => $setup.goGoodsDetail($setup.getGoodsId($setup.brandSaleInfo.lastSale)))
            }, [
              vue.createElementVNode("view", { class: "sale-card-body" }, [
                $setup.getGoodsCover($setup.brandSaleInfo.lastSale) ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  class: "sale-cover",
                  src: $setup.getGoodsCover($setup.brandSaleInfo.lastSale),
                  mode: "aspectFill"
                }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "sale-text" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "sale-title" },
                    vue.toDisplayString($setup.getGoodsName($setup.brandSaleInfo.lastSale)),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("text", { class: "sale-sub sale-sub-tag font-alimamashuhei" }, "上次贩售"),
                  $setup.getGoodsTime($setup.brandSaleInfo.lastSale) ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "sale-sub"
                    },
                    " 贩售时间：" + vue.toDisplayString($setup.getGoodsTime($setup.brandSaleInfo.lastSale)),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ]),
                vue.createVNode(_component_uni_icons, {
                  type: "arrow-right",
                  size: "18",
                  color: "#C0C4CC"
                })
              ])
            ])) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { style: { "padding": "10px" } }, [
            vue.createVNode(_component_comment_list, {
              ref: "commentListRef",
              type: 4,
              "relation-id": parseInt($setup.pageId),
              onReply: $setup.handleReplyComment
            }, null, 8, ["relation-id"])
          ]),
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "loading"
          }, "加载中...")) : vue.createCommentVNode("v-if", true),
          $setup.error ? (vue.openBlock(), vue.createElementBlock(
            "view",
            {
              key: 2,
              class: "error"
            },
            vue.toDisplayString($setup.errorMsg),
            1
            /* TEXT */
          )) : vue.createCommentVNode("v-if", true),
          vue.createVNode(_component_comment_input, {
            ref: "commentInputRef",
            "reply-info": $setup.replyForItem,
            "target-id": $setup.pageId,
            onSubmit: $setup.handleCommentSubmit,
            "onUpdate:replyInfo": _cache[4] || (_cache[4] = (val) => $setup.replyForItem = val)
          }, null, 8, ["reply-info", "target-id"]),
          vue.createElementVNode("view", { style: { "width": "100%", "height": "120rpx" } })
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSaleNewsSaleNews = /* @__PURE__ */ _export_sfc(_sfc_main$X, [["render", _sfc_render$W], ["__scopeId", "data-v-c9c53f1c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/sale_news/sale_news.vue"]]);
  const _sfc_main$W = {
    __name: "treehole_publish",
    setup(__props, { expose: __expose }) {
      __expose();
      const content = vue.ref("");
      const isAnonymous = vue.ref(0);
      const uploadList = vue.ref([]);
      const categories = vue.ref([]);
      const selectedCategory = vue.ref("");
      const selectedIndex = vue.ref(-1);
      const selectedCategoryLabel = vue.ref("");
      function viewFullImage(index) {
        uni.previewImage({
          current: uploadList.value[index],
          urls: uploadList.value
        });
      }
      function handleAnonymous(e2) {
        isAnonymous.value = e2.detail.value.length > 0 ? 1 : 0;
      }
      uni.setNavigationBarTitle({
        title: "投稿树洞"
      });
      function selectImage() {
        formatAppLog("log", "at pages/treehole_publish/treehole_publish.vue:109", "openSelect");
        chooseImage().then((res) => {
          getQiniuToken().then((tokenData) => {
            formatAppLog("log", "at pages/treehole_publish/treehole_publish.vue:112", tokenData);
            uploadImageToQiniu(res, tokenData.token, tokenData.path).then((uploadRes) => {
              if (uploadRes.statusCode != 200) {
                uni.showToast({
                  title: "上传失败",
                  icon: "none"
                });
              }
              formatAppLog("log", "at pages/treehole_publish/treehole_publish.vue:121", image1Url + tokenData.path);
              uploadList.value.push(image1Url + tokenData.path);
              uni.showToast({
                title: "上传成功",
                icon: "success"
              });
            });
          });
        });
      }
      const fetchCategories = async () => {
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/treehole-typelist",
            method: "GET"
          });
          if (res.data.status === "success") {
            categories.value = Object.entries(res.data.data).map(([value, label]) => ({
              value: Number(value),
              label
            }));
          }
        } catch (err) {
          formatAppLog("error", "at pages/treehole_publish/treehole_publish.vue:151", "获取分类失败:", err);
        }
      };
      function handleCategoryChange(e2) {
        const index = e2.detail.value;
        if (index >= 0 && categories.value[index]) {
          selectedIndex.value = index;
          selectedCategory.value = categories.value[index].value;
          selectedCategoryLabel.value = categories.value[index].label;
        }
      }
      const canSubmit = vue.computed(() => {
        return content.value.trim().length > 0 && selectedCategory.value !== null;
      });
      async function handleSubmit() {
        if (!canSubmit.value)
          return;
        if (!selectedCategory.value) {
          uni.showToast({
            title: "请选择分类",
            icon: "none"
          });
          return;
        }
        try {
          uni.showLoading({
            title: "提交中..."
          });
          const token = uni.getStorageSync("token");
          if (!token) {
            uni.showToast({
              title: "请先登录",
              icon: "none"
            });
            return;
          }
          const postData = {
            content: content.value.trim(),
            images: uploadList.value,
            is_anonymous: isAnonymous.value,
            category_id: selectedCategory.value
          };
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/add-submission",
            method: "POST",
            data: postData,
            header: {
              "Content-Type": "application/json",
              "Authorization": token
            }
          });
          uni.hideLoading();
          if (res.data.status == "success") {
            uni.showToast({
              title: "已收到您的投稿",
              icon: "success"
            });
            setTimeout(() => {
              content.value = "";
              uploadList.value = [];
              isAnonymous.value = 0;
              uni.navigateBack();
            }, 1500);
          } else {
            uni.showToast({
              title: res.data.msg || "提交失败",
              icon: "none"
            });
          }
        } catch (err) {
          uni.hideLoading();
          formatAppLog("error", "at pages/treehole_publish/treehole_publish.vue:237", "提交失败:", err);
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        }
      }
      vue.onMounted(() => {
        fetchCategories();
      });
      const __returned__ = { content, isAnonymous, uploadList, categories, selectedCategory, selectedIndex, selectedCategoryLabel, viewFullImage, handleAnonymous, selectImage, fetchCategories, handleCategoryChange, canSubmit, handleSubmit, onMounted: vue.onMounted, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get chooseImage() {
        return chooseImage;
      }, get jumpToCroper() {
        return jumpToCroper;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$V(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { title: "发布树洞" }, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "publish-box" }, [
        vue.createElementVNode("picker", {
          class: "category-picker",
          range: $setup.categories,
          "range-key": "label",
          onChange: $setup.handleCategoryChange,
          value: $setup.selectedIndex
        }, [
          vue.createElementVNode("view", { class: "picker-content" }, [
            vue.createElementVNode(
              "text",
              {
                class: vue.normalizeClass(["picker-text", $setup.selectedCategory ? "" : "placeholder"])
              },
              vue.toDisplayString($setup.selectedCategoryLabel || "请选择分类（必选）"),
              3
              /* TEXT, CLASS */
            ),
            vue.createVNode(_component_uni_icons, {
              type: "right",
              size: "22",
              color: "#888"
            })
          ])
        ], 40, ["range", "value"]),
        vue.createElementVNode("scroll-view", {
          "scroll-x": "true",
          class: "upload_box",
          "ll-with-animation": "true",
          "show-scrollbar": false
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.uploadList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: "upload_item",
                key: index
              }, [
                vue.createElementVNode("image", {
                  src: item,
                  class: "uploaded_image",
                  onClick: $setup.viewFullImage,
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode("image", {
                  src: _imports_2$5,
                  class: "close_icon",
                  onClick: ($event) => _ctx.deleteImage(index)
                }, null, 8, ["onClick"])
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          vue.createElementVNode("view", {
            class: "uploadImageBox",
            style: { "background": "#f8f8f8" }
          }, [
            vue.createElementVNode("image", {
              src: _imports_0$7,
              class: "upload_img",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.selectImage(_ctx.index)),
              style: { "width": "50px", "height": "50px", "margin": "25px" }
            })
          ])
        ]),
        vue.withDirectives(vue.createElementVNode(
          "textarea",
          {
            "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.content = $event),
            placeholder: "写下你想说的话...",
            class: "textarea",
            maxlength: "500"
          },
          null,
          512
          /* NEED_PATCH */
        ), [
          [vue.vModelText, $setup.content]
        ]),
        vue.createElementVNode("view", { class: "anonymous" }, [
          vue.createElementVNode(
            "checkbox-group",
            { onChange: $setup.handleAnonymous },
            [
              vue.createElementVNode("label", null, [
                vue.createElementVNode("checkbox", {
                  checked: $setup.isAnonymous === 1,
                  color: "#4cbbd0"
                }, null, 8, ["checked"]),
                vue.createElementVNode("text", null, "匿名发布")
              ])
            ],
            32
            /* NEED_HYDRATION */
          )
        ]),
        vue.createElementVNode("view", { class: "publish-detail" }, [
          vue.createElementVNode("text", null, "* 目前树洞为非审稿发布，只接受娃圈相关内容。"),
          vue.createElementVNode("text", null, "* 如果勾选匿名发布，他人将无法通过稿件进入您的主页。如果稿件遭到过多点踩或投诉，我们可能会隐藏这封稿件。")
        ]),
        vue.createElementVNode(
          "button",
          {
            class: vue.normalizeClass(["submit-btn", { disabled: !$setup.canSubmit }]),
            onClick: $setup.handleSubmit
          },
          " 提交 ",
          2
          /* CLASS */
        )
      ])
    ]);
  }
  const PagesTreeholePublishTreeholePublish = /* @__PURE__ */ _export_sfc(_sfc_main$W, [["render", _sfc_render$V], ["__scopeId", "data-v-07f0d6e5"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/treehole_publish/treehole_publish.vue"]]);
  const _sfc_main$V = {
    __name: "treehole_detail",
    props: {
      id: {
        type: String,
        required: true
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const loading2 = vue.ref(true);
      const error2 = vue.ref(false);
      const errorMsg = vue.ref("");
      const pageId = vue.ref(0);
      const detailData = vue.ref(null);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      let commentsPage = vue.ref(1);
      let replyForItem = vue.ref({});
      const selectedAssociation = vue.ref({});
      const hasLiked = vue.ref(false);
      const props = __props;
      const handleReplyComment = ({ parent, target }) => {
        var _a;
        const item = target || parent;
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      const handleCommentSubmit = (submitData) => {
        var _a, _b, _c;
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: parseInt(pageId.value),
          type: 5,
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          ...submitData.is_anonymous ? { avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg", username: "匿名用户", is_anonymous: 1 } : { avatar: global$1.userInfo.avatar, username: global$1.userInfo.nickname, is_anonymous: 0 },
          ...submitData.association_id && { association_id: submitData.association_id, association_type: submitData.association_type },
          ...submitData.image_url && { image_url: submitData.image_url },
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id) {
          (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(tempComment);
        } else if (replyForItem.value.parent_id === 0) {
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({ ...tempComment, parent_id: replyForItem.value.id });
        } else {
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({ ...tempComment, parent_id: replyForItem.value.parent_id });
        }
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: { Authorization: token },
          data: requestData,
          success: (res) => {
            var _a2, _b2;
            if (res.data.status == "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? { avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg", username: "匿名用户", is_anonymous: 1 } : { avatar: global$1.userInfo.avatar, username: global$1.userInfo.nickname, is_anonymous: 0 }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous)
                finalComment.reply_username = "匿名用户";
              (_a2 = commentListRef.value) == null ? void 0 : _a2.updateTempComment(tempComment.id, finalComment);
              uni.showToast({ title: "评论成功", icon: "success" });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => {
            var _a2;
            (_a2 = commentListRef.value) == null ? void 0 : _a2.removeTempComment(tempComment.id);
            uni.showToast({ title: "网络请求失败", icon: "none" });
          }
        });
      };
      function copyUrl(item) {
        const url = "http://m.dogdogdoll.com/#/pages/treehole_detail/treehole_detail?id=" + item.id;
        uni.setClipboardData({
          data: url,
          success: function() {
            uni.showToast({ title: "复制链接成功", icon: "none" });
          }
        });
      }
      const handleLike = () => {
        if (!global$1.isLogin) {
          uni.showModal({
            title: "提示",
            content: "需要登录后才能点赞",
            success: (res) => {
              if (res.confirm)
                uni.navigateTo({ url: "/pages/login/login" });
            }
          });
          return;
        }
        const token = uni.getStorageSync("token");
        const url = websiteUrl.value + (hasLiked.value ? "/with-state/unlike" : "/with-state/add-like");
        uni.request({
          url,
          method: "POST",
          header: { Authorization: token },
          data: { id: parseInt(props.id), type: 5 },
          success: (res) => {
            if (res.data.status === "success") {
              hasLiked.value = !hasLiked.value;
              detailData.value.like_count += hasLiked.value ? 1 : -1;
            }
          }
        });
      };
      function getHasLike() {
        let token = uni.getStorageSync("token");
        if (!token)
          return;
        uni.request({
          url: websiteUrl.value + "/with-state/hasLike?id=" + props.id + "&type=5",
          method: "POST",
          header: { Authorization: token },
          success: (res) => {
            if (res.data.status == "success") {
              hasLiked.value = res.data.data.id > 0;
            } else {
              uni.showToast({ title: res.data.msg, icon: "none" });
            }
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      const displayImages = vue.computed(() => {
        var _a, _b;
        return ((_b = (_a = detailData.value) == null ? void 0 : _a.images) == null ? void 0 : _b.slice(0, 9)) || [];
      });
      const remainingCount = vue.computed(() => {
        var _a, _b;
        return (((_b = (_a = detailData.value) == null ? void 0 : _a.images) == null ? void 0 : _b.length) || 0) - 9;
      });
      const showOverlay = vue.computed(() => {
        var _a, _b;
        return (((_b = (_a = detailData.value) == null ? void 0 : _a.images) == null ? void 0 : _b.length) || 0) > 9;
      });
      uni.setNavigationBarTitle({ title: "投稿详情" });
      const layoutClass = vue.computed(() => {
        const count = displayImages.value.length;
        if (count === 1)
          return "single";
        if (count === 2)
          return "double";
        return "multi";
      });
      function formatTime(timestamp) {
        const date = new Date(timestamp * 1e3);
        return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")} ${String(
          date.getHours()
        ).padStart(2, "0")}:${String(date.getMinutes()).padStart(2, "0")}`;
      }
      function previewImage(index) {
        uni.previewImage({ current: index, urls: detailData.value.images });
      }
      vue.onMounted(async () => {
        pageId.value = props.id;
        try {
          const res = await uni.request({ url: `${websiteUrl.value}/treehole-detail?id=${props.id}` });
          if (res.data.status === "success")
            detailData.value = res.data.data;
        } catch (e2) {
          uni.showToast({ title: "加载失败", icon: "none" });
        }
        asyncGetUserInfo();
        getHasLike();
      });
      const __returned__ = { loading: loading2, error: error2, errorMsg, pageId, detailData, commentListRef, commentInputRef, get commentsPage() {
        return commentsPage;
      }, set commentsPage(v2) {
        commentsPage = v2;
      }, get replyForItem() {
        return replyForItem;
      }, set replyForItem(v2) {
        replyForItem = v2;
      }, selectedAssociation, hasLiked, props, handleReplyComment, handleCommentSubmit, copyUrl, handleLike, getHasLike, displayImages, remainingCount, showOverlay, layoutClass, formatTime, previewImage, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$U(_ctx, _cache, $props, $setup, $data, $options) {
    var _a, _b;
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_report_button = resolveEasycom(vue.resolveDynamicComponent("report-button"), __easycom_1$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        $setup.detailData ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "container"
        }, [
          vue.createElementVNode("meta", {
            name: "theme-color",
            content: "#F8F8F8"
          }),
          vue.createVNode(_component_view_logs),
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode("view", { class: "author-info card" }, [
              vue.createElementVNode("image", {
                src: $setup.detailData.avatar || "/static/noname.png",
                class: "avatar",
                mode: "aspectFill"
              }, null, 8, ["src"]),
              vue.createElementVNode("view", { class: "author-meta" }, [
                vue.createElementVNode(
                  "text",
                  { class: "author-name" },
                  vue.toDisplayString($setup.detailData.author_name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "view",
                  { class: "time chip" },
                  vue.toDisplayString($setup.formatTime($setup.detailData.created_at)),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "report-wrap" }, [
                vue.createVNode(_component_report_button, {
                  "report-type": "3",
                  "relation-id": parseInt($setup.props.id),
                  "button-text": "举报",
                  "icon-type": "flag",
                  "icon-size": "24",
                  "icon-color": "#666"
                }, null, 8, ["relation-id"])
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "post-card card" }, [
            vue.createElementVNode(
              "view",
              { class: "post-section post-content" },
              vue.toDisplayString($setup.detailData.content),
              1
              /* TEXT */
            ),
            (((_a = $setup.detailData.images) == null ? void 0 : _a.length) || 0) > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "divider"
            })) : vue.createCommentVNode("v-if", true),
            (((_b = $setup.detailData.images) == null ? void 0 : _b.length) || 0) > 0 ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 1,
                class: vue.normalizeClass(["post-section image-container", $setup.layoutClass])
              },
              [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.displayImages, (img, index) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: index,
                      class: "image-wrapper",
                      "hover-class": "press",
                      "hover-stay-time": "80"
                    }, [
                      vue.createElementVNode("image", {
                        src: img,
                        mode: "aspectFill",
                        class: "image-item",
                        onClick: ($event) => $setup.previewImage(index)
                      }, null, 8, ["src", "onClick"]),
                      $setup.showOverlay && index === 8 ? (vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: 0,
                          class: "image-overlay"
                        },
                        "+" + vue.toDisplayString($setup.remainingCount),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ],
              2
              /* CLASS */
            )) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "divider" }),
            vue.createElementVNode("view", { class: "post-section action-bar" }, [
              vue.createElementVNode("view", { class: "action-group" }, [
                vue.createElementVNode("view", {
                  class: "action-item",
                  onClick: $setup.handleLike,
                  "hover-class": "press",
                  "hover-stay-time": "80"
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: $setup.hasLiked ? "hand-up-filled" : "hand-up",
                    size: "24",
                    color: $setup.hasLiked ? "var(--brand)" : "#666"
                  }, null, 8, ["type", "color"]),
                  vue.createElementVNode(
                    "text",
                    {
                      class: vue.normalizeClass(["action-text", { active: $setup.hasLiked }])
                    },
                    vue.toDisplayString($setup.detailData.like_count || 0),
                    3
                    /* TEXT, CLASS */
                  )
                ]),
                vue.createElementVNode("view", {
                  class: "action-item",
                  onClick: _cache[0] || (_cache[0] = ($event) => $setup.copyUrl($setup.detailData)),
                  "hover-class": "press",
                  "hover-stay-time": "80"
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "redo",
                    size: "24",
                    color: "#666"
                  }),
                  vue.createElementVNode("text", { class: "action-text" }, "分享")
                ])
              ]),
              $setup.detailData.approve_time > 0 ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "time-text"
                },
                "审核于 " + vue.toDisplayString($setup.formatTime($setup.detailData.approve_time)),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ])
          ])
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "loading"
        }, "加载中...")),
        vue.createElementVNode("view", { class: "comments card" }, [
          vue.createVNode(_component_comment_list, {
            ref: "commentListRef",
            type: 5,
            "relation-id": parseInt($setup.props.id),
            onReply: $setup.handleReplyComment
          }, null, 8, ["relation-id"])
        ]),
        vue.createVNode(_component_comment_input, {
          ref: "commentInputRef",
          "reply-info": $setup.replyForItem,
          "target-id": $setup.pageId,
          onSubmit: $setup.handleCommentSubmit,
          "onUpdate:replyInfo": _cache[1] || (_cache[1] = (val) => $setup.replyForItem = val)
        }, null, 8, ["reply-info", "target-id"]),
        vue.createElementVNode("view", { style: { "width": "100%", "height": "120rpx" } })
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesTreeholeDetailTreeholeDetail = /* @__PURE__ */ _export_sfc(_sfc_main$V, [["render", _sfc_render$U], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/treehole_detail/treehole_detail.vue"]]);
  const _sfc_main$U = {
    __name: "reset_password",
    setup(__props, { expose: __expose }) {
      __expose();
      const formData = vue.ref({
        telephone: "",
        code: "",
        newPassword: "",
        reNewPassword: ""
      });
      const errors = vue.ref({
        phone: "",
        code: "",
        newPassword: "",
        reNewPassword: ""
      });
      const codeCountdown = vue.ref(0);
      const codeBtnDisabled = vue.computed(() => codeCountdown.value > 0);
      const codeBtnText = vue.computed(
        () => codeCountdown.value > 0 ? `${codeCountdown.value}秒后重发` : "获取验证码"
      );
      const loading2 = vue.ref(false);
      const validatePhone = () => {
        if (!formData.value.telephone) {
          errors.value.phone = "手机号不能为空";
          return false;
        }
        if (!/^1[3-9]\d{9}$/.test(formData.value.telephone)) {
          errors.value.phone = "手机号格式不正确";
          return false;
        }
        errors.value.phone = "";
        return true;
      };
      const sendSmsCode = async () => {
        if (!validatePhone())
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/send-sms-code`,
            // 根据实际接口调整
            method: "POST",
            data: {
              tel_phone: formData.value.telephone
            }
          });
          if (res.data.status === "success") {
            startCountdown();
            uni.showToast({
              title: "验证码已发送",
              icon: "none"
            });
          } else {
            uni.showToast({
              title: res.data.msg || "发送失败",
              icon: "none"
            });
          }
        } catch (err) {
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        }
      };
      const validateNewPassword = () => {
        if (formData.value.newPassword.length < 6) {
          errors.value.newPassword = "密码至少需要6位";
          return false;
        }
        errors.value.newPassword = "";
        return true;
      };
      const validateReNewPassword = () => {
        if (formData.value.newPassword !== formData.value.reNewPassword) {
          errors.value.reNewPassword = "两次密码输入不一致";
          return false;
        }
        errors.value.reNewPassword = "";
        return true;
      };
      const validateCode = () => {
        if (!formData.value.code) {
          errors.value.code = "验证码不能为空";
          return false;
        }
        if (formData.value.code.length !== 6) {
          errors.value.code = "验证码格式不正确";
          return false;
        }
        errors.value.code = "";
        return true;
      };
      const handleResetPassword = async () => {
        const validations = [
          validatePhone(),
          validateCode(),
          validateNewPassword(),
          validateReNewPassword()
        ];
        if (!validations.every((v2) => v2))
          return;
        try {
          loading2.value = true;
          const res = await uni.request({
            url: `${websiteUrl.value}/reset-password`,
            // 修改为重置密码接口
            method: "POST",
            data: {
              telephone: formData.value.telephone,
              code: formData.value.code,
              password: formData.value.newPassword,
              re_password: formData.value.reNewPassword
            }
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "密码重置成功",
              icon: "success",
              complete: () => {
                setTimeout(() => {
                  uni.navigateBack();
                }, 1500);
              }
            });
          } else {
            uni.showToast({
              title: res.data.msg || "重置失败",
              icon: "none"
            });
          }
        } catch (err) {
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const startCountdown = () => {
        codeCountdown.value = 60;
        const timer = setInterval(() => {
          if (codeCountdown.value <= 0) {
            clearInterval(timer);
            return;
          }
          codeCountdown.value--;
        }, 1e3);
      };
      const navigateToLogin = () => {
        uni.navigateBack();
      };
      const __returned__ = { formData, errors, codeCountdown, codeBtnDisabled, codeBtnText, loading: loading2, validatePhone, sendSmsCode, validateNewPassword, validateReNewPassword, validateCode, handleResetPassword, startCountdown, navigateToLogin, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$T(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "container" }, [
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode("text", { class: "title" }, "重置密码")
          ]),
          vue.createElementVNode("view", { class: "form-box" }, [
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.createElementVNode("text", { class: "prefix" }, "+86"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.formData.telephone = $event),
                    type: "number",
                    placeholder: "请输入手机号",
                    maxlength: "11",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validatePhone
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.telephone]
                ])
              ]),
              $setup.errors.phone ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.phone),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "code-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.formData.code = $event),
                    type: "number",
                    placeholder: "请输入验证码",
                    maxlength: "6",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validateCode
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.code]
                ]),
                vue.createElementVNode("button", {
                  class: vue.normalizeClass(["code-btn", { disabled: $setup.codeBtnDisabled }]),
                  disabled: $setup.codeBtnDisabled,
                  onClick: $setup.sendSmsCode
                }, vue.toDisplayString($setup.codeBtnText), 11, ["disabled"])
              ]),
              $setup.errors.code ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.code),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.formData.newPassword = $event),
                    type: "password",
                    placeholder: "请输入新密码（至少6位）",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validateNewPassword
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.newPassword]
                ])
              ]),
              $setup.errors.newPassword ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.newPassword),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "input-item" }, [
              vue.createElementVNode("view", { class: "input-wrapper" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.formData.reNewPassword = $event),
                    type: "password",
                    placeholder: "请再次输入新密码",
                    class: "input",
                    "placeholder-class": "placeholder",
                    onBlur: $setup.validateReNewPassword
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.formData.reNewPassword]
                ])
              ]),
              $setup.errors.reNewPassword ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "error-text"
                },
                vue.toDisplayString($setup.errors.reNewPassword),
                1
                /* TEXT */
              )) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("button", {
              class: vue.normalizeClass(["submit-btn", { loading: $setup.loading }]),
              disabled: $setup.loading,
              onClick: $setup.handleResetPassword
            }, vue.toDisplayString($setup.loading ? "提交中..." : "重置密码"), 11, ["disabled"]),
            vue.createElementVNode("view", { class: "bottom-tips" }, [
              vue.createElementVNode("text", {
                class: "link-text",
                onClick: $setup.navigateToLogin
              }, "返回登录")
            ])
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesResetPasswordResetPassword = /* @__PURE__ */ _export_sfc(_sfc_main$U, [["render", _sfc_render$T], ["__scopeId", "data-v-7140db85"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/reset_password/reset_password.vue"]]);
  const _sfc_main$T = {
    __name: "username",
    setup(__props, { expose: __expose }) {
      __expose();
      uni.setNavigationBarTitle({
        title: "修改用户名"
      });
      const username = vue.ref(global$1.userInfo.username || "");
      const isLoading = vue.ref(false);
      const isFormValid = vue.computed(() => {
        return username.value.length >= 2 && username.value !== global$1.userInfo.username;
      });
      async function updateUsername() {
        if (!isFormValid.value)
          return;
        try {
          isLoading.value = true;
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/update-profile",
            method: "POST",
            header: {
              "Authorization": uni.getStorageSync("token")
            },
            data: {
              username: username.value
            }
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "更新成功",
              icon: "success"
            });
            await getUserInfo();
            setTimeout(() => uni.navigateBack(), 1500);
          } else {
            uni.showToast({
              title: res.data.msg || "更新失败",
              icon: "none"
            });
          }
        } catch (error2) {
          uni.showToast({
            title: "请求失败，请稍后重试",
            icon: "none"
          });
        } finally {
          isLoading.value = false;
        }
      }
      const __returned__ = { username, isLoading, isFormValid, updateUsername, ref: vue.ref, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get getUserInfo() {
        return getUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$S(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "password-container" }, [
          vue.createElementVNode("view", { class: "form-wrapper" }, [
            vue.createElementVNode("view", { class: "input-group" }, [
              vue.createElementVNode("view", { class: "input-item" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    class: "input",
                    type: "text",
                    placeholder: " ",
                    "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.username = $event),
                    maxlength: "20"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.username]
                ]),
                vue.createElementVNode("label", { class: "floating-label" }, "用户名"),
                vue.createVNode(_component_uni_icons, {
                  class: "input-icon",
                  type: "person",
                  size: "18",
                  color: "#999"
                })
              ])
            ]),
            vue.createElementVNode("button", {
              class: vue.normalizeClass(["submit-btn", { active: $setup.isFormValid }]),
              onClick: $setup.updateUsername,
              disabled: !$setup.isFormValid
            }, [
              vue.createTextVNode(
                vue.toDisplayString($setup.isLoading ? "提交中..." : "更新用户名") + " ",
                1
                /* TEXT */
              ),
              $setup.isLoading ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "loading"
              })) : vue.createCommentVNode("v-if", true)
            ], 10, ["disabled"])
          ]),
          vue.createElementVNode("view", { class: "security-tips" }, [
            vue.createVNode(_component_uni_icons, {
              type: "info",
              size: "16",
              color: "#666"
            }),
            vue.createElementVNode("text", null, "用户名2-20个字符，支持中英文、数字和下划线")
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSettingUsernameUsername = /* @__PURE__ */ _export_sfc(_sfc_main$T, [["render", _sfc_render$S], ["__scopeId", "data-v-ce7f77cb"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/setting/username/username.vue"]]);
  const _sfc_main$S = {
    __name: "my_comment",
    setup(__props, { expose: __expose }) {
      __expose();
      const commentList = vue.ref([]);
      const currentPage = vue.ref(1);
      const pageSize2 = vue.ref(10);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const noMore = vue.ref(false);
      const commentTypes = {
        1: {
          name: "品牌",
          class: "brand"
        },
        2: {
          name: "商品",
          class: "goods"
        },
        3: {
          name: "搭配",
          class: "collocation"
        },
        4: {
          name: "资讯",
          class: "news"
        },
        5: {
          name: "树洞",
          class: "treehole"
        },
        6: {
          name: "展示柜",
          class: "collocation"
        },
        7: {
          name: "文章",
          class: "collocation"
        }
      };
      vue.onMounted(() => {
        loadComments();
      });
      const loadComments = async (isLoadMore = false) => {
        if (loading2.value || noMore.value)
          return;
        loading2.value = true;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/my-comment`,
            method: "GET",
            header: {
              "Authorization": uni.getStorageSync("token")
            },
            data: {
              page: currentPage.value,
              page_size: pageSize2.value
            }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            commentList.value = isLoadMore ? [...commentList.value, ...data.comment_list] : data.comment_list;
            total.value = data.total;
            currentPage.value++;
            noMore.value = commentList.value.length >= total.value;
          }
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (!noMore.value)
          loadComments(true);
      };
      const navigateToTarget = (item) => {
        let url = "";
        const id = item.relation_id;
        switch (item.type) {
          case 1:
            url = `/pages/brand/brand?brand_id=${id}`;
            break;
          case 2:
            url = `/pages/goods/goods?goods_id=${id}`;
            break;
          case 3:
            url = `/pages/collocation_share/collocation_share?collocation_id=${id}&origin=1`;
            break;
          case 4:
            url = `/pages/sale_news/sale_news?id=${id}&brand_id=0`;
            break;
          case 5:
            url = `/pages/treehole_detail/treehole_detail?id=${id}`;
            break;
          case 6:
            url = `/pages/collocation_share/collocation_share?collocation_id=${id}&origin=2`;
            break;
          default:
            uni.showToast({
              title: "未知类型",
              icon: "none"
            });
            return;
        }
        if (url) {
          uni.navigateTo({
            url
          });
        }
      };
      const getTypeName = (type) => {
        var _a;
        return ((_a = commentTypes[type]) == null ? void 0 : _a.name) || "未知";
      };
      const getTypeClass = (type) => {
        var _a;
        return ((_a = commentTypes[type]) == null ? void 0 : _a.class) || "";
      };
      const truncate = (text, length) => {
        return text.length > length ? text.substring(0, length) + "..." : text;
      };
      function formatTime(timestamp) {
        const date = new Date(timestamp * 1e3);
        const year = date.getFullYear();
        const month = (date.getMonth() + 1).toString().padStart(2, "0");
        const day = date.getDate().toString().padStart(2, "0");
        const hours = date.getHours().toString().padStart(2, "0");
        const minutes = date.getMinutes().toString().padStart(2, "0");
        const seconds = date.getSeconds().toString().padStart(2, "0");
        return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
      }
      uni.setNavigationBarTitle({
        title: "我的评论"
      });
      const __returned__ = { commentList, currentPage, pageSize: pageSize2, total, loading: loading2, noMore, commentTypes, loadComments, loadMore, navigateToTarget, getTypeName, getTypeClass, truncate, formatTime, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$R(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock("view", { class: "page-container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "comment-list",
          "scroll-y": "",
          onScrolltolower: $setup.loadMore,
          "show-scrollbar": false
        },
        [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.commentList, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "comment-item",
                onClick: ($event) => $setup.navigateToTarget(item)
              }, [
                vue.createElementVNode("view", { class: "user-info" }, [
                  vue.createElementVNode("image", {
                    class: "avatar",
                    src: item.avatar || "/static/default-avatar.png",
                    mode: "aspectFill"
                  }, null, 8, ["src"]),
                  vue.createElementVNode("view", { class: "user-meta" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "username" },
                      vue.toDisplayString(item.username),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "time" },
                      vue.toDisplayString($setup.formatTime(item.created_at)),
                      1
                      /* TEXT */
                    )
                  ])
                ]),
                vue.createElementVNode("view", { class: "content-box" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "content" },
                    vue.toDisplayString(item.comment),
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", {
                  class: "target-box",
                  onClick: ($event) => $setup.navigateToTarget(item)
                }, [
                  item.target_image ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    class: "target-image",
                    src: item.target_image,
                    mode: "aspectFill"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode("view", { class: "target-info" }, [
                    vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["type-tag", $setup.getTypeClass(item.type)])
                      },
                      vue.toDisplayString($setup.getTypeName(item.type)),
                      3
                      /* TEXT, CLASS */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "target-name" },
                      vue.toDisplayString(item.target_name),
                      1
                      /* TEXT */
                    )
                  ])
                ], 8, ["onClick"])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          vue.createElementVNode("view", { class: "loading-status" }, [
            $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 0,
              class: "loading-text"
            }, "加载中...")) : vue.createCommentVNode("v-if", true),
            $setup.noMore ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "no-more-text"
            }, "没有更多了")) : vue.createCommentVNode("v-if", true)
          ])
        ],
        32
        /* NEED_HYDRATION */
      )
    ]);
  }
  const PagesMyCommentMyComment = /* @__PURE__ */ _export_sfc(_sfc_main$S, [["render", _sfc_render$R], ["__scopeId", "data-v-7fd39f53"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/my_comment/my_comment.vue"]]);
  const _imports_0$5 = "/static/images/loading.gif";
  const _sfc_main$R = {
    __name: "my_collocation",
    setup(__props, { expose: __expose }) {
      __expose();
      const list = vue.ref([]);
      const page = vue.ref(1);
      const pageSize2 = vue.ref(10);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const noMore = vue.ref(false);
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        return `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
      };
      const getList = async () => {
        if (noMore.value || loading2.value)
          return;
        loading2.value = true;
        try {
          const userInfo = await asyncGetUserInfo();
          if (!userInfo)
            return;
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/my-collocation`,
            method: "GET",
            header: {
              Authorization: uni.getStorageSync("token")
            },
            data: {
              page: page.value,
              page_size: pageSize2.value
            }
          });
          if (res.data.status === "success") {
            list.value = [...list.value, ...res.data.data.collocation_relation_list];
            total.value = res.data.data.total;
            noMore.value = list.value.length >= total.value;
            page.value++;
          }
        } catch (error2) {
          uni.showToast({
            title: "加载失败",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (!noMore.value)
          getList();
      };
      uni.setNavigationBarTitle({
        title: "我的搭配"
      });
      vue.onMounted(() => {
        getList();
      });
      const __returned__ = { list, page, pageSize: pageSize2, total, loading: loading2, noMore, formatTime, getList, loadMore, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$Q(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "scroll-content",
          "scroll-y": "",
          onScrolltolower: $setup.loadMore,
          "show-scrollbar": false
        },
        [
          $setup.list.length === 0 && !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty-tip"
          }, " 暂无发布的搭配，快去创建吧~ ")) : vue.createCommentVNode("v-if", true),
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.list, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: index,
                class: "collocation-card"
              }, [
                vue.createElementVNode("view", { class: "card-header" }, [
                  vue.createElementVNode("image", {
                    class: "avatar",
                    src: item.avatar || `${$setup.image1Url}/default-avatar.png`,
                    mode: "aspectFill"
                  }, null, 8, ["src"]),
                  vue.createElementVNode(
                    "text",
                    { class: "time" },
                    vue.toDisplayString($setup.formatTime(item.created_at)),
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "image-container" }, [
                  item.image_urls.length > 1 ? (vue.openBlock(), vue.createElementBlock("swiper", {
                    key: 0,
                    class: "swiper-box",
                    autoplay: false,
                    circular: true
                  }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList(item.image_urls, (img, imgIndex) => {
                        return vue.openBlock(), vue.createElementBlock("swiper-item", { key: imgIndex }, [
                          vue.createElementVNode("image", {
                            class: "swiper-image",
                            src: img,
                            mode: "aspectFill",
                            "lazy-load": "",
                            style: { aspectRatio: "3/4" }
                          }, null, 8, ["src"])
                        ]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])) : (vue.openBlock(), vue.createElementBlock("image", {
                    key: 1,
                    class: "single-image",
                    src: item.image_urls[0],
                    mode: "aspectFill",
                    "lazy-load": "",
                    style: { aspectRatio: "3/4" }
                  }, null, 8, ["src"]))
                ]),
                vue.createElementVNode("view", { class: "content-box" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "title" },
                    vue.toDisplayString(item.title),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "content" },
                    vue.toDisplayString(item.content),
                    1
                    /* TEXT */
                  )
                ]),
                item.relation_list.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "relation-box"
                }, [
                  vue.createElementVNode("text", { class: "relation-label" }, "搭配包含："),
                  vue.createElementVNode("scroll-view", {
                    "scroll-x": "",
                    class: "tag-scroll"
                  }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList(item.relation_list, (rel, relIndex) => {
                        return vue.openBlock(), vue.createElementBlock(
                          "view",
                          {
                            key: relIndex,
                            class: "relation-tag"
                          },
                          vue.toDisplayString(rel.type) + "·" + vue.toDisplayString(rel.relation_goods_name || rel.relation_brand_name),
                          1
                          /* TEXT */
                        );
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])
                ])) : vue.createCommentVNode("v-if", true)
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "loading-box"
          }, [
            vue.createElementVNode("image", {
              src: _imports_0$5,
              class: "loading-icon"
            }),
            vue.createElementVNode("text", null, "加载中...")
          ])) : vue.createCommentVNode("v-if", true),
          $setup.noMore ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "no-more"
          }, "没有更多了～")) : vue.createCommentVNode("v-if", true)
        ],
        32
        /* NEED_HYDRATION */
      )
    ]);
  }
  const PagesMyCollocationMyCollocation = /* @__PURE__ */ _export_sfc(_sfc_main$R, [["render", _sfc_render$Q], ["__scopeId", "data-v-c07e3656"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/my_collocation/my_collocation.vue"]]);
  const block0 = (Comp) => {
    (Comp.$wxs || (Comp.$wxs = [])).push("handler");
    (Comp.$wxsModules || (Comp.$wxsModules = {}))["handler"] = "2f992f8c";
  };
  const _sfc_main$Q = {
    name: "node",
    options: {},
    data() {
      return {
        ctrl: {}
      };
    },
    props: {
      name: String,
      attrs: {
        type: Object,
        default() {
          return {};
        }
      },
      childs: Array,
      opts: Array
    },
    components: {},
    mounted() {
      this.$nextTick(() => {
        for (this.root = this.$parent; this.root.$options.name !== "mp-html"; this.root = this.root.$parent)
          ;
      });
      if (this.opts[0]) {
        let i2;
        for (i2 = this.childs.length; i2--; ) {
          if (this.childs[i2].name === "img")
            break;
        }
        if (i2 !== -1) {
          this.observer = uni.createIntersectionObserver(this).relativeToViewport({
            top: 500,
            bottom: 500
          });
          this.observer.observe("._img", (res) => {
            if (res.intersectionRatio) {
              this.$set(this.ctrl, "load", 1);
              this.observer.disconnect();
            }
          });
        }
      }
    },
    beforeDestroy() {
      if (this.observer) {
        this.observer.disconnect();
      }
    },
    methods: {
      /**
       * @description 播放视频事件
       * @param {Event} e
       */
      play(e2) {
        const i2 = e2.currentTarget.dataset.i;
        const node2 = this.childs[i2];
        this.root.$emit("play", {
          source: node2.name,
          attrs: {
            ...node2.attrs,
            src: node2.src[this.ctrl[i2] || 0]
          }
        });
      },
      /**
       * @description 图片点击事件
       * @param {Event} e
       */
      imgTap(e2) {
        const node2 = this.childs[e2.currentTarget.dataset.i];
        if (node2.a) {
          this.linkTap(node2.a);
          return;
        }
        if (node2.attrs.ignore)
          return;
        node2.attrs.src = node2.attrs.src || node2.attrs["data-src"];
        this.root.$emit("imgtap", node2.attrs);
        if (this.root.previewImg) {
          uni.previewImage({
            current: parseInt(node2.attrs.i),
            urls: this.root.imgList
          });
        }
      },
      /**
       * @description 图片长按
       */
      imgLongTap(e2) {
        const attrs = this.childs[e2.currentTarget.dataset.i].attrs;
        if (this.opts[3] && !attrs.ignore) {
          uni.showActionSheet({
            itemList: ["保存图片"],
            success: () => {
              const save = (path) => {
                uni.saveImageToPhotosAlbum({
                  filePath: path,
                  success() {
                    uni.showToast({
                      title: "保存成功"
                    });
                  }
                });
              };
              if (this.root.imgList[attrs.i].startsWith("http")) {
                uni.downloadFile({
                  url: this.root.imgList[attrs.i],
                  success: (res) => save(res.tempFilePath)
                });
              } else {
                save(this.root.imgList[attrs.i]);
              }
            }
          });
        }
      },
      /**
       * @description 图片加载完成事件
       * @param {Event} e
       */
      imgLoad(e2) {
        const i2 = e2.currentTarget.dataset.i;
        if (!this.childs[i2].w) {
          this.$set(this.ctrl, i2, e2.detail.width);
        } else if (this.opts[1] && !this.ctrl[i2] || this.ctrl[i2] === -1) {
          this.$set(this.ctrl, i2, 1);
        }
        this.checkReady();
      },
      /**
       * @description 检查是否所有图片加载完毕
       */
      checkReady() {
        if (this.root && !this.root.lazyLoad) {
          this.root._unloadimgs -= 1;
          if (!this.root._unloadimgs) {
            setTimeout(() => {
              this.root.getRect().then((rect) => {
                this.root.$emit("ready", rect);
              }).catch(() => {
                this.root.$emit("ready", {});
              });
            }, 350);
          }
        }
      },
      /**
       * @description 链接点击事件
       * @param {Event} e
       */
      linkTap(e2) {
        const node2 = e2.currentTarget ? this.childs[e2.currentTarget.dataset.i] : {};
        const attrs = node2.attrs || e2;
        const href = attrs.href;
        this.root.$emit("linktap", Object.assign({
          innerText: this.root.getText(node2.children || [])
          // 链接内的文本内容
        }, attrs));
        if (href) {
          if (href[0] === "#") {
            this.root.navigateTo(href.substring(1)).catch(() => {
            });
          } else if (href.split("?")[0].includes("://")) {
            if (this.root.copyLink) {
              plus.runtime.openWeb(href);
            }
          } else {
            uni.navigateTo({
              url: href,
              fail() {
                uni.switchTab({
                  url: href,
                  fail() {
                  }
                });
              }
            });
          }
        }
      },
      /**
       * @description 错误事件
       * @param {Event} e
       */
      mediaError(e2) {
        const i2 = e2.currentTarget.dataset.i;
        const node2 = this.childs[i2];
        if (node2.name === "video" || node2.name === "audio") {
          let index = (this.ctrl[i2] || 0) + 1;
          if (index > node2.src.length) {
            index = 0;
          }
          if (index < node2.src.length) {
            this.$set(this.ctrl, i2, index);
            return;
          }
        } else if (node2.name === "img") {
          if (this.opts[2]) {
            this.$set(this.ctrl, i2, -1);
          }
          this.checkReady();
        }
        if (this.root) {
          this.root.$emit("error", {
            source: node2.name,
            attrs: node2.attrs,
            errMsg: e2.detail.errMsg
          });
        }
      }
    }
  };
  function _sfc_render$P(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_node = vue.resolveComponent("node", true);
    return vue.openBlock(), vue.createElementBlock("view", {
      id: $props.attrs.id,
      class: vue.normalizeClass("_block _" + $props.name + " " + $props.attrs.class),
      style: vue.normalizeStyle($props.attrs.style)
    }, [
      (vue.openBlock(true), vue.createElementBlock(
        vue.Fragment,
        null,
        vue.renderList($props.childs, (n2, i2) => {
          return vue.openBlock(), vue.createElementBlock(
            vue.Fragment,
            { key: i2 },
            [
              n2.name === "img" && !n2.t && ($props.opts[1] && !$data.ctrl[i2] || $data.ctrl[i2] < 0) ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 0,
                class: "_img",
                style: vue.normalizeStyle(n2.attrs.style),
                src: $data.ctrl[i2] < 0 ? $props.opts[2] : $props.opts[1],
                mode: "widthFix"
              }, null, 12, ["src"])) : vue.createCommentVNode("v-if", true),
              n2.name === "img" && n2.t ? (vue.openBlock(), vue.createElementBlock("rich-text", {
                key: 1,
                style: vue.normalizeStyle("display:" + n2.t),
                nodes: [{ attrs: { style: n2.attrs.style || "", src: n2.attrs.src }, name: "img" }],
                "data-i": i2,
                onClick: _cache[0] || (_cache[0] = vue.withModifiers((...args) => $options.imgTap && $options.imgTap(...args), ["stop"]))
              }, null, 12, ["nodes", "data-i"])) : n2.name === "img" ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 2,
                id: n2.attrs.id,
                class: vue.normalizeClass("_img " + n2.attrs.class),
                style: vue.normalizeStyle(($data.ctrl[i2] === -1 ? "display:none;" : "") + "width:" + ($data.ctrl[i2] || 1) + "px;" + n2.attrs.style),
                src: n2.attrs.src || ($data.ctrl.load ? n2.attrs["data-src"] : ""),
                mode: !n2.h ? "widthFix" : !n2.w ? "heightFix" : n2.m || "",
                "data-i": i2,
                onLoad: _cache[1] || (_cache[1] = (...args) => $options.imgLoad && $options.imgLoad(...args)),
                onError: _cache[2] || (_cache[2] = (...args) => $options.mediaError && $options.mediaError(...args)),
                onClick: _cache[3] || (_cache[3] = vue.withModifiers((...args) => $options.imgTap && $options.imgTap(...args), ["stop"])),
                onLongpress: _cache[4] || (_cache[4] = (...args) => $options.imgLongTap && $options.imgLongTap(...args))
              }, null, 46, ["id", "src", "mode", "data-i"])) : n2.text ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 3,
                  decode: ""
                },
                vue.toDisplayString(n2.text),
                1
                /* TEXT */
              )) : n2.name === "br" ? (vue.openBlock(), vue.createElementBlock("text", { key: 4 }, "\\n")) : n2.name === "a" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 5,
                id: n2.attrs.id,
                class: vue.normalizeClass((n2.attrs.href ? "_a " : "") + n2.attrs.class),
                "hover-class": "_hover",
                style: vue.normalizeStyle("display:inline;" + n2.attrs.style),
                "data-i": i2,
                onClick: _cache[5] || (_cache[5] = vue.withModifiers((...args) => $options.linkTap && $options.linkTap(...args), ["stop"]))
              }, [
                vue.createVNode(_component_node, {
                  name: "span",
                  childs: n2.children,
                  opts: $props.opts,
                  style: { "display": "inherit" }
                }, null, 8, ["childs", "opts"])
              ], 14, ["id", "data-i"])) : n2.html ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 6,
                id: n2.attrs.id,
                class: vue.normalizeClass("_video " + n2.attrs.class),
                style: vue.normalizeStyle(n2.attrs.style),
                innerHTML: n2.html,
                "data-i": i2,
                onVplay: _cache[6] || (_cache[6] = vue.withModifiers((...args) => $options.play && $options.play(...args), ["stop"]))
              }, null, 46, ["id", "innerHTML", "data-i"])) : n2.name === "iframe" ? (vue.openBlock(), vue.createElementBlock("iframe", {
                key: 7,
                style: vue.normalizeStyle(n2.attrs.style),
                allowfullscreen: n2.attrs.allowfullscreen,
                frameborder: n2.attrs.frameborder,
                src: n2.attrs.src
              }, null, 12, ["allowfullscreen", "frameborder", "src"])) : n2.name === "embed" ? (vue.openBlock(), vue.createElementBlock("embed", {
                key: 8,
                style: vue.normalizeStyle(n2.attrs.style),
                src: n2.attrs.src
              }, null, 12, ["src"])) : n2.name === "table" && n2.c || n2.name === "li" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 9,
                id: n2.attrs.id,
                class: vue.normalizeClass("_" + n2.name + " " + n2.attrs.class),
                style: vue.normalizeStyle(n2.attrs.style)
              }, [
                n2.name === "li" ? (vue.openBlock(), vue.createBlock(_component_node, {
                  key: 0,
                  childs: n2.children,
                  opts: $props.opts
                }, null, 8, ["childs", "opts"])) : (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  { key: 1 },
                  vue.renderList(n2.children, (tbody, x) => {
                    return vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: x,
                        class: vue.normalizeClass("_" + tbody.name + " " + tbody.attrs.class),
                        style: vue.normalizeStyle(tbody.attrs.style)
                      },
                      [
                        tbody.name === "td" || tbody.name === "th" ? (vue.openBlock(), vue.createBlock(_component_node, {
                          key: 0,
                          childs: tbody.children,
                          opts: $props.opts
                        }, null, 8, ["childs", "opts"])) : (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          { key: 1 },
                          vue.renderList(tbody.children, (tr2, y2) => {
                            return vue.openBlock(), vue.createElementBlock(
                              vue.Fragment,
                              { key: y2 },
                              [
                                tr2.name === "td" || tr2.name === "th" ? (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 0,
                                    class: vue.normalizeClass("_" + tr2.name + " " + tr2.attrs.class),
                                    style: vue.normalizeStyle(tr2.attrs.style)
                                  },
                                  [
                                    vue.createVNode(_component_node, {
                                      childs: tr2.children,
                                      opts: $props.opts
                                    }, null, 8, ["childs", "opts"])
                                  ],
                                  6
                                  /* CLASS, STYLE */
                                )) : (vue.openBlock(), vue.createElementBlock(
                                  "view",
                                  {
                                    key: 1,
                                    class: vue.normalizeClass("_" + tr2.name + " " + tr2.attrs.class),
                                    style: vue.normalizeStyle(tr2.attrs.style)
                                  },
                                  [
                                    (vue.openBlock(true), vue.createElementBlock(
                                      vue.Fragment,
                                      null,
                                      vue.renderList(tr2.children, (td, z2) => {
                                        return vue.openBlock(), vue.createElementBlock(
                                          "view",
                                          {
                                            key: z2,
                                            class: vue.normalizeClass("_" + td.name + " " + td.attrs.class),
                                            style: vue.normalizeStyle(td.attrs.style)
                                          },
                                          [
                                            vue.createVNode(_component_node, {
                                              childs: td.children,
                                              opts: $props.opts
                                            }, null, 8, ["childs", "opts"])
                                          ],
                                          6
                                          /* CLASS, STYLE */
                                        );
                                      }),
                                      128
                                      /* KEYED_FRAGMENT */
                                    ))
                                  ],
                                  6
                                  /* CLASS, STYLE */
                                ))
                              ],
                              64
                              /* STABLE_FRAGMENT */
                            );
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ],
                      6
                      /* CLASS, STYLE */
                    );
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ], 14, ["id"])) : !n2.c ? (vue.openBlock(), vue.createElementBlock("rich-text", {
                key: 10,
                id: n2.attrs.id,
                style: vue.normalizeStyle("display:inline;" + n2.f),
                preview: false,
                selectable: $props.opts[4],
                "user-select": $props.opts[4],
                nodes: [n2]
              }, null, 12, ["id", "selectable", "user-select", "nodes"])) : n2.c === 2 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 11,
                id: n2.attrs.id,
                class: vue.normalizeClass("_block _" + n2.name + " " + n2.attrs.class),
                style: vue.normalizeStyle(n2.f + ";" + n2.attrs.style)
              }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList(n2.children, (n22, j2) => {
                    return vue.openBlock(), vue.createBlock(_component_node, {
                      key: j2,
                      style: vue.normalizeStyle(n22.f),
                      name: n22.name,
                      attrs: n22.attrs,
                      childs: n22.children,
                      opts: $props.opts
                    }, null, 8, ["style", "name", "attrs", "childs", "opts"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ], 14, ["id"])) : (vue.openBlock(), vue.createBlock(_component_node, {
                key: 12,
                style: vue.normalizeStyle(n2.f),
                name: n2.name,
                attrs: n2.attrs,
                childs: n2.children,
                opts: $props.opts
              }, null, 8, ["style", "name", "attrs", "childs", "opts"]))
            ],
            64
            /* STABLE_FRAGMENT */
          );
        }),
        128
        /* KEYED_FRAGMENT */
      ))
    ], 14, ["id"]);
  }
  if (typeof block0 === "function")
    block0(_sfc_main$Q);
  const node = /* @__PURE__ */ _export_sfc(_sfc_main$Q, [["render", _sfc_render$P], ["__scopeId", "data-v-8845ff2f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/mp-html/components/mp-html/node/node.vue"]]);
  const config = {
    // 信任的标签（保持标签名不变）
    trustTags: makeMap("a,abbr,ad,audio,b,blockquote,br,code,col,colgroup,dd,del,dl,dt,div,em,fieldset,h1,h2,h3,h4,h5,h6,hr,i,img,ins,label,legend,li,ol,p,q,ruby,rt,source,span,strong,sub,sup,table,tbody,td,tfoot,th,thead,tr,title,ul,video"),
    // 块级标签（转为 div，其他的非信任标签转为 span）
    blockTags: makeMap("address,article,aside,body,caption,center,cite,footer,header,html,nav,pre,section"),
    // 行内标签
    inlineTags: makeMap("abbr,b,big,code,del,em,i,ins,label,q,small,span,strong,sub,sup"),
    // 要移除的标签
    ignoreTags: makeMap("area,base,canvas,embed,frame,head,iframe,input,link,map,meta,param,rp,script,source,style,textarea,title,track,wbr"),
    // 自闭合的标签
    voidTags: makeMap("area,base,br,col,circle,ellipse,embed,frame,hr,img,input,line,link,meta,param,path,polygon,rect,source,track,use,wbr"),
    // html 实体
    entities: {
      lt: "<",
      gt: ">",
      quot: '"',
      apos: "'",
      ensp: " ",
      emsp: " ",
      nbsp: " ",
      semi: ";",
      ndash: "–",
      mdash: "—",
      middot: "·",
      lsquo: "‘",
      rsquo: "’",
      ldquo: "“",
      rdquo: "”",
      bull: "•",
      hellip: "…",
      larr: "←",
      uarr: "↑",
      rarr: "→",
      darr: "↓"
    },
    // 默认的标签样式
    tagStyle: {
      address: "font-style:italic",
      big: "display:inline;font-size:1.2em",
      caption: "display:table-caption;text-align:center",
      center: "text-align:center",
      cite: "font-style:italic",
      dd: "margin-left:40px",
      mark: "background-color:yellow",
      pre: "font-family:monospace;white-space:pre",
      s: "text-decoration:line-through",
      small: "display:inline;font-size:0.8em",
      strike: "text-decoration:line-through",
      u: "text-decoration:underline"
    },
    // svg 大小写对照表
    svgDict: {
      animatetransform: "animateTransform",
      lineargradient: "linearGradient",
      viewbox: "viewBox",
      attributename: "attributeName",
      repeatcount: "repeatCount",
      repeatdur: "repeatDur",
      foreignobject: "foreignObject"
    }
  };
  const tagSelector = {};
  let windowWidth;
  const systemInfo = uni.getSystemInfoSync();
  windowWidth = systemInfo.windowWidth;
  const blankChar = makeMap(" ,\r,\n,	,\f");
  let idIndex = 0;
  config.ignoreTags.iframe = void 0;
  config.trustTags.iframe = true;
  config.ignoreTags.embed = void 0;
  config.trustTags.embed = true;
  function makeMap(str) {
    const map = /* @__PURE__ */ Object.create(null);
    const list = str.split(",");
    for (let i2 = list.length; i2--; ) {
      map[list[i2]] = true;
    }
    return map;
  }
  function decodeEntity(str, amp) {
    let i2 = str.indexOf("&");
    while (i2 !== -1) {
      const j2 = str.indexOf(";", i2 + 3);
      let code;
      if (j2 === -1)
        break;
      if (str[i2 + 1] === "#") {
        code = parseInt((str[i2 + 2] === "x" ? "0" : "") + str.substring(i2 + 2, j2));
        if (!isNaN(code)) {
          str = str.substr(0, i2) + String.fromCharCode(code) + str.substr(j2 + 1);
        }
      } else {
        code = str.substring(i2 + 1, j2);
        if (config.entities[code] || code === "amp" && amp) {
          str = str.substr(0, i2) + (config.entities[code] || "&") + str.substr(j2 + 1);
        }
      }
      i2 = str.indexOf("&", i2 + 1);
    }
    return str;
  }
  function mergeNodes(nodes) {
    let i2 = nodes.length - 1;
    for (let j2 = i2; j2 >= -1; j2--) {
      if (j2 === -1 || nodes[j2].c || !nodes[j2].name || nodes[j2].name !== "div" && nodes[j2].name !== "p" && nodes[j2].name[0] !== "h" || (nodes[j2].attrs.style || "").includes("inline")) {
        if (i2 - j2 >= 5) {
          nodes.splice(j2 + 1, i2 - j2, {
            name: "div",
            attrs: {},
            children: nodes.slice(j2 + 1, i2 + 1)
          });
        }
        i2 = j2 - 1;
      }
    }
  }
  function Parser(vm) {
    this.options = vm || {};
    this.tagStyle = Object.assign({}, config.tagStyle, this.options.tagStyle);
    this.imgList = vm.imgList || [];
    this.imgList._unloadimgs = 0;
    this.plugins = vm.plugins || [];
    this.attrs = /* @__PURE__ */ Object.create(null);
    this.stack = [];
    this.nodes = [];
    this.pre = (this.options.containerStyle || "").includes("white-space") && this.options.containerStyle.includes("pre") ? 2 : 0;
  }
  Parser.prototype.parse = function(content) {
    for (let i2 = this.plugins.length; i2--; ) {
      if (this.plugins[i2].onUpdate) {
        content = this.plugins[i2].onUpdate(content, config) || content;
      }
    }
    new Lexer(this).parse(content);
    while (this.stack.length) {
      this.popNode();
    }
    if (this.nodes.length > 50) {
      mergeNodes(this.nodes);
    }
    return this.nodes;
  };
  Parser.prototype.expose = function() {
    for (let i2 = this.stack.length; i2--; ) {
      const item = this.stack[i2];
      if (item.c || item.name === "a" || item.name === "video" || item.name === "audio")
        return;
      item.c = 1;
    }
  };
  Parser.prototype.hook = function(node2) {
    for (let i2 = this.plugins.length; i2--; ) {
      if (this.plugins[i2].onParse && this.plugins[i2].onParse(node2, this) === false) {
        return false;
      }
    }
    return true;
  };
  Parser.prototype.getUrl = function(url) {
    const domain = this.options.domain;
    if (url[0] === "/") {
      if (url[1] === "/") {
        url = (domain ? domain.split("://")[0] : "http") + ":" + url;
      } else if (domain) {
        url = domain + url;
      } else {
        url = plus.io.convertLocalFileSystemURL(url);
      }
    } else if (!url.includes("data:") && !url.includes("://")) {
      if (domain) {
        url = domain + "/" + url;
      } else {
        url = plus.io.convertLocalFileSystemURL(url);
      }
    }
    return url;
  };
  Parser.prototype.parseStyle = function(node2) {
    const attrs = node2.attrs;
    const list = (this.tagStyle[node2.name] || "").split(";").concat((attrs.style || "").split(";"));
    const styleObj = {};
    let tmp = "";
    if (attrs.id && !this.xml) {
      if (this.options.useAnchor) {
        this.expose();
      } else if (node2.name !== "img" && node2.name !== "a" && node2.name !== "video" && node2.name !== "audio") {
        attrs.id = void 0;
      }
    }
    if (attrs.width) {
      styleObj.width = parseFloat(attrs.width) + (attrs.width.includes("%") ? "%" : "px");
      attrs.width = void 0;
    }
    if (attrs.height) {
      styleObj.height = parseFloat(attrs.height) + (attrs.height.includes("%") ? "%" : "px");
      attrs.height = void 0;
    }
    for (let i2 = 0, len = list.length; i2 < len; i2++) {
      const info = list[i2].split(":");
      if (info.length < 2)
        continue;
      const key = info.shift().trim().toLowerCase();
      let value = info.join(":").trim();
      if (value[0] === "-" && value.lastIndexOf("-") > 0 || value.includes("safe")) {
        tmp += `;${key}:${value}`;
      } else if (!styleObj[key] || value.includes("import") || !styleObj[key].includes("import")) {
        if (value.includes("url")) {
          let j2 = value.indexOf("(") + 1;
          if (j2) {
            while (value[j2] === '"' || value[j2] === "'" || blankChar[value[j2]]) {
              j2++;
            }
            value = value.substr(0, j2) + this.getUrl(value.substr(j2));
          }
        } else if (value.includes("rpx")) {
          value = value.replace(/[0-9.]+\s*rpx/g, ($2) => parseFloat($2) * windowWidth / 750 + "px");
        }
        styleObj[key] = value;
      }
    }
    node2.attrs.style = tmp;
    return styleObj;
  };
  Parser.prototype.onTagName = function(name) {
    this.tagName = this.xml ? name : name.toLowerCase();
    if (this.tagName === "svg") {
      this.xml = (this.xml || 0) + 1;
      config.ignoreTags.style = void 0;
    }
  };
  Parser.prototype.onAttrName = function(name) {
    name = this.xml ? name : name.toLowerCase();
    if (name.includes("?") || name.includes(";")) {
      this.attrName = void 0;
      return;
    }
    if (name.substr(0, 5) === "data-") {
      if (name === "data-src" && !this.attrs.src) {
        this.attrName = "src";
      } else if (this.tagName === "img" || this.tagName === "a") {
        this.attrName = name;
      } else {
        this.attrName = void 0;
      }
    } else {
      this.attrName = name;
      this.attrs[name] = "T";
    }
  };
  Parser.prototype.onAttrVal = function(val) {
    const name = this.attrName || "";
    if (name === "style" || name === "href") {
      this.attrs[name] = decodeEntity(val, true);
    } else if (name.includes("src")) {
      this.attrs[name] = this.getUrl(decodeEntity(val, true));
    } else if (name) {
      this.attrs[name] = val;
    }
  };
  Parser.prototype.onOpenTag = function(selfClose) {
    const node2 = /* @__PURE__ */ Object.create(null);
    node2.name = this.tagName;
    node2.attrs = this.attrs;
    if (this.options.nodes.length) {
      node2.type = "node";
    }
    this.attrs = /* @__PURE__ */ Object.create(null);
    const attrs = node2.attrs;
    const parent = this.stack[this.stack.length - 1];
    const siblings = parent ? parent.children : this.nodes;
    const close = this.xml ? selfClose : config.voidTags[node2.name];
    if (tagSelector[node2.name]) {
      attrs.class = tagSelector[node2.name] + (attrs.class ? " " + attrs.class : "");
    }
    if (node2.name === "embed") {
      this.expose();
    }
    if (node2.name === "video" || node2.name === "audio") {
      if (node2.name === "video" && !attrs.id) {
        attrs.id = "v" + idIndex++;
      }
      if (!attrs.controls && !attrs.autoplay) {
        attrs.controls = "T";
      }
      node2.src = [];
      if (attrs.src) {
        node2.src.push(attrs.src);
        attrs.src = void 0;
      }
      this.expose();
    }
    if (close) {
      if (!this.hook(node2) || config.ignoreTags[node2.name]) {
        if (node2.name === "base" && !this.options.domain) {
          this.options.domain = attrs.href;
        } else if (node2.name === "source" && parent && (parent.name === "video" || parent.name === "audio") && attrs.src) {
          parent.src.push(attrs.src);
        }
        return;
      }
      const styleObj = this.parseStyle(node2);
      if (node2.name === "img") {
        if (attrs.src) {
          if (attrs.src.includes("webp")) {
            node2.webp = "T";
          }
          if (attrs.src.includes("data:") && this.options.previewImg !== "all" && !attrs["original-src"]) {
            attrs.ignore = "T";
          }
          if (!attrs.ignore || node2.webp || attrs.src.includes("cloud://")) {
            for (let i2 = this.stack.length; i2--; ) {
              const item = this.stack[i2];
              if (item.name === "a") {
                node2.a = item.attrs;
              }
              if (item.name === "table" && !node2.webp && !attrs.src.includes("cloud://")) {
                if (!styleObj.display || styleObj.display.includes("inline")) {
                  node2.t = "inline-block";
                } else {
                  node2.t = styleObj.display;
                }
                styleObj.display = void 0;
              }
              item.c = 1;
            }
            attrs.i = this.imgList.length.toString();
            let src = attrs["original-src"] || attrs.src;
            this.imgList.push(src);
            if (!node2.t) {
              this.imgList._unloadimgs += 1;
            }
            if (this.options.lazyLoad) {
              attrs["data-src"] = attrs.src;
              attrs.src = void 0;
            }
          }
        }
        if (styleObj.display === "inline") {
          styleObj.display = "";
        }
        if (attrs.ignore) {
          styleObj["max-width"] = styleObj["max-width"] || "100%";
          attrs.style += ";-webkit-touch-callout:none";
        }
        if (parseInt(styleObj.width) > windowWidth) {
          styleObj.height = void 0;
        }
        if (!isNaN(parseInt(styleObj.width))) {
          node2.w = "T";
        }
        if (!isNaN(parseInt(styleObj.height)) && (!styleObj.height.includes("%") || parent && (parent.attrs.style || "").includes("height"))) {
          node2.h = "T";
        }
        if (node2.w && node2.h && styleObj["object-fit"]) {
          if (styleObj["object-fit"] === "contain") {
            node2.m = "aspectFit";
          } else if (styleObj["object-fit"] === "cover") {
            node2.m = "aspectFill";
          }
        }
      } else if (node2.name === "svg") {
        siblings.push(node2);
        this.stack.push(node2);
        this.popNode();
        return;
      }
      for (const key in styleObj) {
        if (styleObj[key]) {
          attrs.style += `;${key}:${styleObj[key].replace(" !important", "")}`;
        }
      }
      attrs.style = attrs.style.substr(1) || void 0;
    } else {
      if ((node2.name === "pre" || (attrs.style || "").includes("white-space") && attrs.style.includes("pre")) && this.pre !== 2) {
        this.pre = node2.pre = 1;
      }
      node2.children = [];
      this.stack.push(node2);
    }
    siblings.push(node2);
  };
  Parser.prototype.onCloseTag = function(name) {
    name = this.xml ? name : name.toLowerCase();
    let i2;
    for (i2 = this.stack.length; i2--; ) {
      if (this.stack[i2].name === name)
        break;
    }
    if (i2 !== -1) {
      while (this.stack.length > i2) {
        this.popNode();
      }
    } else if (name === "p" || name === "br") {
      const siblings = this.stack.length ? this.stack[this.stack.length - 1].children : this.nodes;
      siblings.push({
        name,
        attrs: {
          class: tagSelector[name] || "",
          style: this.tagStyle[name] || ""
        }
      });
    }
  };
  Parser.prototype.popNode = function() {
    const node2 = this.stack.pop();
    let attrs = node2.attrs;
    const children = node2.children;
    const parent = this.stack[this.stack.length - 1];
    const siblings = parent ? parent.children : this.nodes;
    if (!this.hook(node2) || config.ignoreTags[node2.name]) {
      if (node2.name === "title" && children.length && children[0].type === "text" && this.options.setTitle) {
        uni.setNavigationBarTitle({
          title: children[0].text
        });
      }
      siblings.pop();
      return;
    }
    if (node2.pre && this.pre !== 2) {
      this.pre = node2.pre = void 0;
      for (let i2 = this.stack.length; i2--; ) {
        if (this.stack[i2].pre) {
          this.pre = 1;
        }
      }
    }
    const styleObj = {};
    if (node2.name === "svg") {
      if (this.xml > 1) {
        this.xml--;
        return;
      }
      let src = "";
      const style = attrs.style;
      attrs.style = "";
      attrs.xmlns = "http://www.w3.org/2000/svg";
      (function traversal(node3) {
        if (node3.type === "text") {
          src += node3.text;
          return;
        }
        const name = config.svgDict[node3.name] || node3.name;
        if (name === "foreignObject") {
          for (const child of node3.children || []) {
            if (child.attrs && !child.attrs.xmlns) {
              child.attrs.xmlns = "http://www.w3.org/1999/xhtml";
              break;
            }
          }
        }
        src += "<" + name;
        for (const item in node3.attrs) {
          const val = node3.attrs[item];
          if (val) {
            src += ` ${config.svgDict[item] || item}="${val.replace(/"/g, "")}"`;
          }
        }
        if (!node3.children) {
          src += "/>";
        } else {
          src += ">";
          for (let i2 = 0; i2 < node3.children.length; i2++) {
            traversal(node3.children[i2]);
          }
          src += "</" + name + ">";
        }
      })(node2);
      node2.name = "img";
      node2.attrs = {
        src: "data:image/svg+xml;utf8," + src.replace(/#/g, "%23"),
        style,
        ignore: "T"
      };
      node2.children = void 0;
      this.xml = false;
      config.ignoreTags.style = true;
      return;
    }
    if (attrs.align) {
      if (node2.name === "table") {
        if (attrs.align === "center") {
          styleObj["margin-inline-start"] = styleObj["margin-inline-end"] = "auto";
        } else {
          styleObj.float = attrs.align;
        }
      } else {
        styleObj["text-align"] = attrs.align;
      }
      attrs.align = void 0;
    }
    if (attrs.dir) {
      styleObj.direction = attrs.dir;
      attrs.dir = void 0;
    }
    if (node2.name === "font") {
      if (attrs.color) {
        styleObj.color = attrs.color;
        attrs.color = void 0;
      }
      if (attrs.face) {
        styleObj["font-family"] = attrs.face;
        attrs.face = void 0;
      }
      if (attrs.size) {
        let size = parseInt(attrs.size);
        if (!isNaN(size)) {
          if (size < 1) {
            size = 1;
          } else if (size > 7) {
            size = 7;
          }
          styleObj["font-size"] = ["x-small", "small", "medium", "large", "x-large", "xx-large", "xxx-large"][size - 1];
        }
        attrs.size = void 0;
      }
    }
    if ((attrs.class || "").includes("align-center")) {
      styleObj["text-align"] = "center";
    }
    Object.assign(styleObj, this.parseStyle(node2));
    if (node2.name !== "table" && parseInt(styleObj.width) > windowWidth) {
      styleObj["max-width"] = "100%";
      styleObj["box-sizing"] = "border-box";
    }
    if (config.blockTags[node2.name]) {
      node2.name = "div";
    } else if (!config.trustTags[node2.name] && !this.xml) {
      node2.name = "span";
    }
    if (node2.name === "a" || node2.name === "ad" || node2.name === "iframe") {
      this.expose();
    } else if (node2.name === "video") {
      if ((styleObj.height || "").includes("auto")) {
        styleObj.height = void 0;
      }
      let str = '<video style="width:100%;height:100%"';
      for (const item in attrs) {
        if (attrs[item]) {
          str += " " + item + '="' + attrs[item] + '"';
        }
      }
      if (this.options.pauseVideo) {
        str += ` onplay="this.dispatchEvent(new CustomEvent('vplay',{bubbles:!0}));for(var e=document.getElementsByTagName('video'),t=0;t<e.length;t++)e[t]!=this&&e[t].pause()"`;
      }
      str += ">";
      for (let i2 = 0; i2 < node2.src.length; i2++) {
        str += '<source src="' + node2.src[i2] + '">';
      }
      str += "</video>";
      node2.html = str;
    } else if ((node2.name === "ul" || node2.name === "ol") && node2.c) {
      const types = {
        a: "lower-alpha",
        A: "upper-alpha",
        i: "lower-roman",
        I: "upper-roman"
      };
      if (types[attrs.type]) {
        attrs.style += ";list-style-type:" + types[attrs.type];
        attrs.type = void 0;
      }
      for (let i2 = children.length; i2--; ) {
        if (children[i2].name === "li") {
          children[i2].c = 1;
        }
      }
    } else if (node2.name === "table") {
      let padding = parseFloat(attrs.cellpadding);
      let spacing = parseFloat(attrs.cellspacing);
      const border = parseFloat(attrs.border);
      const bordercolor = styleObj["border-color"];
      const borderstyle = styleObj["border-style"];
      if (node2.c) {
        if (isNaN(padding)) {
          padding = 2;
        }
        if (isNaN(spacing)) {
          spacing = 2;
        }
      }
      if (border) {
        attrs.style += `;border:${border}px ${borderstyle || "solid"} ${bordercolor || "gray"}`;
      }
      if (node2.flag && node2.c) {
        styleObj.display = "grid";
        if (styleObj["border-collapse"] === "collapse") {
          styleObj["border-collapse"] = void 0;
          spacing = 0;
        }
        if (spacing) {
          styleObj["grid-gap"] = spacing + "px";
          styleObj.padding = spacing + "px";
        } else if (border) {
          attrs.style += ";border-left:0;border-top:0";
        }
        const width = [];
        const trList = [];
        const cells = [];
        const map = {};
        (function traversal(nodes) {
          for (let i2 = 0; i2 < nodes.length; i2++) {
            if (nodes[i2].name === "tr") {
              trList.push(nodes[i2]);
            } else if (nodes[i2].name === "colgroup") {
              let colI = 1;
              for (const col of nodes[i2].children || []) {
                if (col.name === "col") {
                  const style = col.attrs.style || "";
                  const start = style.indexOf("width") ? style.indexOf(";width") : 0;
                  if (start !== -1) {
                    let end = style.indexOf(";", start + 6);
                    if (end === -1) {
                      end = style.length;
                    }
                    width[colI] = style.substring(start ? start + 7 : 6, end);
                  }
                  colI += 1;
                }
              }
            } else {
              traversal(nodes[i2].children || []);
            }
          }
        })(children);
        for (let row = 1; row <= trList.length; row++) {
          let col = 1;
          for (let j2 = 0; j2 < trList[row - 1].children.length; j2++) {
            const td = trList[row - 1].children[j2];
            if (td.name === "td" || td.name === "th") {
              while (map[row + "." + col]) {
                col++;
              }
              let style = td.attrs.style || "";
              let start = style.indexOf("width") ? style.indexOf(";width") : 0;
              if (start !== -1) {
                let end = style.indexOf(";", start + 6);
                if (end === -1) {
                  end = style.length;
                }
                if (!td.attrs.colspan) {
                  width[col] = style.substring(start ? start + 7 : 6, end);
                }
                style = style.substr(0, start) + style.substr(end);
              }
              style += ";display:flex";
              start = style.indexOf("vertical-align");
              if (start !== -1) {
                const val = style.substr(start + 15, 10);
                if (val.includes("middle")) {
                  style += ";align-items:center";
                } else if (val.includes("bottom")) {
                  style += ";align-items:flex-end";
                }
              } else {
                style += ";align-items:center";
              }
              start = style.indexOf("text-align");
              if (start !== -1) {
                const val = style.substr(start + 11, 10);
                if (val.includes("center")) {
                  style += ";justify-content: center";
                } else if (val.includes("right")) {
                  style += ";justify-content: right";
                }
              }
              style = (border ? `;border:${border}px ${borderstyle || "solid"} ${bordercolor || "gray"}` + (spacing ? "" : ";border-right:0;border-bottom:0") : "") + (padding ? `;padding:${padding}px` : "") + ";" + style;
              if (td.attrs.colspan) {
                style += `;grid-column-start:${col};grid-column-end:${col + parseInt(td.attrs.colspan)}`;
                if (!td.attrs.rowspan) {
                  style += `;grid-row-start:${row};grid-row-end:${row + 1}`;
                }
                col += parseInt(td.attrs.colspan) - 1;
              }
              if (td.attrs.rowspan) {
                style += `;grid-row-start:${row};grid-row-end:${row + parseInt(td.attrs.rowspan)}`;
                if (!td.attrs.colspan) {
                  style += `;grid-column-start:${col};grid-column-end:${col + 1}`;
                }
                for (let rowspan = 1; rowspan < td.attrs.rowspan; rowspan++) {
                  for (let colspan = 0; colspan < (td.attrs.colspan || 1); colspan++) {
                    map[row + rowspan + "." + (col - colspan)] = 1;
                  }
                }
              }
              if (style) {
                td.attrs.style = style;
              }
              cells.push(td);
              col++;
            }
          }
          if (row === 1) {
            let temp = "";
            for (let i2 = 1; i2 < col; i2++) {
              temp += (width[i2] ? width[i2] : "auto") + " ";
            }
            styleObj["grid-template-columns"] = temp;
          }
        }
        node2.children = cells;
      } else {
        if (node2.c) {
          styleObj.display = "table";
        }
        if (!isNaN(spacing)) {
          styleObj["border-spacing"] = spacing + "px";
        }
        if (border || padding) {
          (function traversal(nodes) {
            for (let i2 = 0; i2 < nodes.length; i2++) {
              const td = nodes[i2];
              if (td.name === "th" || td.name === "td") {
                if (border) {
                  td.attrs.style = `border:${border}px ${borderstyle || "solid"} ${bordercolor || "gray"};${td.attrs.style || ""}`;
                }
                if (padding) {
                  td.attrs.style = `padding:${padding}px;${td.attrs.style || ""}`;
                }
              } else if (td.children) {
                traversal(td.children);
              }
            }
          })(children);
        }
      }
      if (this.options.scrollTable && !(attrs.style || "").includes("inline")) {
        const table = Object.assign({}, node2);
        node2.name = "div";
        node2.attrs = {
          style: "overflow:auto"
        };
        node2.children = [table];
        attrs = table.attrs;
      }
    } else if ((node2.name === "tbody" || node2.name === "tr") && node2.flag && node2.c) {
      node2.flag = void 0;
      (function traversal(nodes) {
        for (let i2 = 0; i2 < nodes.length; i2++) {
          if (nodes[i2].name === "td") {
            for (const style of ["color", "background", "background-color"]) {
              if (styleObj[style]) {
                nodes[i2].attrs.style = style + ":" + styleObj[style] + ";" + (nodes[i2].attrs.style || "");
              }
            }
          } else {
            traversal(nodes[i2].children || []);
          }
        }
      })(children);
    } else if ((node2.name === "td" || node2.name === "th") && (attrs.colspan || attrs.rowspan)) {
      for (let i2 = this.stack.length; i2--; ) {
        if (this.stack[i2].name === "table" || this.stack[i2].name === "tbody" || this.stack[i2].name === "tr") {
          this.stack[i2].flag = 1;
        }
      }
    } else if (node2.name === "ruby") {
      node2.name = "span";
      for (let i2 = 0; i2 < children.length - 1; i2++) {
        if (children[i2].type === "text" && children[i2 + 1].name === "rt") {
          children[i2] = {
            name: "div",
            attrs: {
              style: "display:inline-block;text-align:center"
            },
            children: [{
              name: "div",
              attrs: {
                style: "font-size:50%;" + (children[i2 + 1].attrs.style || "")
              },
              children: children[i2 + 1].children
            }, children[i2]]
          };
          children.splice(i2 + 1, 1);
        }
      }
    } else if (node2.c) {
      (function traversal(node3) {
        node3.c = 2;
        for (let i2 = node3.children.length; i2--; ) {
          const child = node3.children[i2];
          if (child.name && (config.inlineTags[child.name] || (child.attrs.style || "").includes("inline") && child.children) && !child.c) {
            traversal(child);
          }
          if (!child.c || child.name === "table") {
            node3.c = 1;
          }
        }
      })(node2);
    }
    if ((styleObj.display || "").includes("flex") && !node2.c) {
      for (let i2 = children.length; i2--; ) {
        const item = children[i2];
        if (item.f) {
          item.attrs.style = (item.attrs.style || "") + item.f;
          item.f = void 0;
        }
      }
    }
    const flex = parent && ((parent.attrs.style || "").includes("flex") || (parent.attrs.style || "").includes("grid")) && !node2.c;
    if (flex) {
      node2.f = ";max-width:100%";
    }
    if (children.length >= 50 && node2.c && !(styleObj.display || "").includes("flex")) {
      mergeNodes(children);
    }
    for (const key in styleObj) {
      if (styleObj[key]) {
        const val = `;${key}:${styleObj[key].replace(" !important", "")}`;
        if (flex && (key.includes("flex") && key !== "flex-direction" || key === "align-self" || key.includes("grid") || styleObj[key][0] === "-" || key.includes("width") && val.includes("%"))) {
          node2.f += val;
          if (key === "width") {
            attrs.style += ";width:100%";
          }
        } else {
          attrs.style += val;
        }
      }
    }
    attrs.style = attrs.style.substr(1) || void 0;
  };
  Parser.prototype.onText = function(text) {
    if (!this.pre) {
      let trim = "";
      let flag;
      for (let i2 = 0, len = text.length; i2 < len; i2++) {
        if (!blankChar[text[i2]]) {
          trim += text[i2];
        } else {
          if (trim[trim.length - 1] !== " ") {
            trim += " ";
          }
          if (text[i2] === "\n" && !flag) {
            flag = true;
          }
        }
      }
      if (trim === " ") {
        if (flag)
          return;
        else {
          const parent = this.stack[this.stack.length - 1];
          if (parent && parent.name[0] === "t")
            return;
        }
      }
      text = trim;
    }
    const node2 = /* @__PURE__ */ Object.create(null);
    node2.type = "text";
    node2.text = decodeEntity(text);
    if (this.hook(node2)) {
      const siblings = this.stack.length ? this.stack[this.stack.length - 1].children : this.nodes;
      siblings.push(node2);
    }
  };
  function Lexer(handler) {
    this.handler = handler;
  }
  Lexer.prototype.parse = function(content) {
    this.content = content || "";
    this.i = 0;
    this.start = 0;
    this.state = this.text;
    for (let len = this.content.length; this.i !== -1 && this.i < len; ) {
      this.state();
    }
  };
  Lexer.prototype.checkClose = function(method) {
    const selfClose = this.content[this.i] === "/";
    if (this.content[this.i] === ">" || selfClose && this.content[this.i + 1] === ">") {
      if (method) {
        this.handler[method](this.content.substring(this.start, this.i));
      }
      this.i += selfClose ? 2 : 1;
      this.start = this.i;
      this.handler.onOpenTag(selfClose);
      if (this.handler.tagName === "script") {
        this.i = this.content.indexOf("</", this.i);
        if (this.i !== -1) {
          this.i += 2;
          this.start = this.i;
        }
        this.state = this.endTag;
      } else {
        this.state = this.text;
      }
      return true;
    }
    return false;
  };
  Lexer.prototype.text = function() {
    this.i = this.content.indexOf("<", this.i);
    if (this.i === -1) {
      if (this.start < this.content.length) {
        this.handler.onText(this.content.substring(this.start, this.content.length));
      }
      return;
    }
    const c2 = this.content[this.i + 1];
    if (c2 >= "a" && c2 <= "z" || c2 >= "A" && c2 <= "Z") {
      if (this.start !== this.i) {
        this.handler.onText(this.content.substring(this.start, this.i));
      }
      this.start = ++this.i;
      this.state = this.tagName;
    } else if (c2 === "/" || c2 === "!" || c2 === "?") {
      if (this.start !== this.i) {
        this.handler.onText(this.content.substring(this.start, this.i));
      }
      const next = this.content[this.i + 2];
      if (c2 === "/" && (next >= "a" && next <= "z" || next >= "A" && next <= "Z")) {
        this.i += 2;
        this.start = this.i;
        this.state = this.endTag;
        return;
      }
      let end = "-->";
      if (c2 !== "!" || this.content[this.i + 2] !== "-" || this.content[this.i + 3] !== "-") {
        end = ">";
      }
      this.i = this.content.indexOf(end, this.i);
      if (this.i !== -1) {
        this.i += end.length;
        this.start = this.i;
      }
    } else {
      this.i++;
    }
  };
  Lexer.prototype.tagName = function() {
    if (blankChar[this.content[this.i]]) {
      this.handler.onTagName(this.content.substring(this.start, this.i));
      while (blankChar[this.content[++this.i]])
        ;
      if (this.i < this.content.length && !this.checkClose()) {
        this.start = this.i;
        this.state = this.attrName;
      }
    } else if (!this.checkClose("onTagName")) {
      this.i++;
    }
  };
  Lexer.prototype.attrName = function() {
    let c2 = this.content[this.i];
    if (blankChar[c2] || c2 === "=") {
      this.handler.onAttrName(this.content.substring(this.start, this.i));
      let needVal = c2 === "=";
      const len = this.content.length;
      while (++this.i < len) {
        c2 = this.content[this.i];
        if (!blankChar[c2]) {
          if (this.checkClose())
            return;
          if (needVal) {
            this.start = this.i;
            this.state = this.attrVal;
            return;
          }
          if (this.content[this.i] === "=") {
            needVal = true;
          } else {
            this.start = this.i;
            this.state = this.attrName;
            return;
          }
        }
      }
    } else if (!this.checkClose("onAttrName")) {
      this.i++;
    }
  };
  Lexer.prototype.attrVal = function() {
    const c2 = this.content[this.i];
    const len = this.content.length;
    if (c2 === '"' || c2 === "'") {
      this.start = ++this.i;
      this.i = this.content.indexOf(c2, this.i);
      if (this.i === -1)
        return;
      this.handler.onAttrVal(this.content.substring(this.start, this.i));
    } else {
      for (; this.i < len; this.i++) {
        if (blankChar[this.content[this.i]]) {
          this.handler.onAttrVal(this.content.substring(this.start, this.i));
          break;
        } else if (this.checkClose("onAttrVal"))
          return;
      }
    }
    while (blankChar[this.content[++this.i]])
      ;
    if (this.i < len && !this.checkClose()) {
      this.start = this.i;
      this.state = this.attrName;
    }
  };
  Lexer.prototype.endTag = function() {
    const c2 = this.content[this.i];
    if (blankChar[c2] || c2 === ">" || c2 === "/") {
      this.handler.onCloseTag(this.content.substring(this.start, this.i));
      if (c2 !== ">") {
        this.i = this.content.indexOf(">", this.i);
        if (this.i === -1)
          return;
      }
      this.start = ++this.i;
      this.state = this.text;
    } else {
      this.i++;
    }
  };
  const plugins = [];
  const _sfc_main$P = {
    name: "mp-html",
    data() {
      return {
        nodes: []
      };
    },
    props: {
      containerStyle: {
        type: String,
        default: ""
      },
      content: {
        type: String,
        default: ""
      },
      copyLink: {
        type: [Boolean, String],
        default: true
      },
      domain: String,
      errorImg: {
        type: String,
        default: ""
      },
      lazyLoad: {
        type: [Boolean, String],
        default: false
      },
      loadingImg: {
        type: String,
        default: ""
      },
      pauseVideo: {
        type: [Boolean, String],
        default: true
      },
      previewImg: {
        type: [Boolean, String],
        default: true
      },
      scrollTable: [Boolean, String],
      selectable: [Boolean, String],
      setTitle: {
        type: [Boolean, String],
        default: true
      },
      showImgMenu: {
        type: [Boolean, String],
        default: true
      },
      tagStyle: Object,
      useAnchor: [Boolean, Number]
    },
    emits: ["load", "ready", "imgtap", "linktap", "play", "error"],
    components: {
      node
    },
    watch: {
      content(content) {
        this.setContent(content);
      }
    },
    created() {
      this.plugins = [];
      for (let i2 = plugins.length; i2--; ) {
        this.plugins.push(new plugins[i2](this));
      }
    },
    mounted() {
      if (this.content && !this.nodes.length) {
        this.setContent(this.content);
      }
    },
    beforeDestroy() {
      this._hook("onDetached");
    },
    methods: {
      /**
       * @description 将锚点跳转的范围限定在一个 scroll-view 内
       * @param {Object} page scroll-view 所在页面的示例
       * @param {String} selector scroll-view 的选择器
       * @param {String} scrollTop scroll-view scroll-top 属性绑定的变量名
       */
      in(page, selector, scrollTop) {
        if (page && selector && scrollTop) {
          this._in = {
            page,
            selector,
            scrollTop
          };
        }
      },
      /**
       * @description 锚点跳转
       * @param {String} id 要跳转的锚点 id
       * @param {Number} offset 跳转位置的偏移量
       * @returns {Promise}
       */
      navigateTo(id, offset) {
        return new Promise((resolve, reject) => {
          if (!this.useAnchor) {
            reject(Error("Anchor is disabled"));
            return;
          }
          offset = offset || parseInt(this.useAnchor) || 0;
          let deep = " ";
          const selector = uni.createSelectorQuery().in(this._in ? this._in.page : this).select((this._in ? this._in.selector : "._root") + (id ? `${deep}#${id}` : "")).boundingClientRect();
          if (this._in) {
            selector.select(this._in.selector).scrollOffset().select(this._in.selector).boundingClientRect();
          } else {
            selector.selectViewport().scrollOffset();
          }
          selector.exec((res) => {
            if (!res[0]) {
              reject(Error("Label not found"));
              return;
            }
            const scrollTop = res[1].scrollTop + res[0].top - (res[2] ? res[2].top : 0) + offset;
            if (this._in) {
              this._in.page[this._in.scrollTop] = scrollTop;
            } else {
              uni.pageScrollTo({
                scrollTop,
                duration: 300
              });
            }
            resolve();
          });
        });
      },
      /**
       * @description 获取文本内容
       * @return {String}
       */
      getText(nodes) {
        let text = "";
        (function traversal(nodes2) {
          for (let i2 = 0; i2 < nodes2.length; i2++) {
            const node2 = nodes2[i2];
            if (node2.type === "text") {
              text += node2.text.replace(/&amp;/g, "&");
            } else if (node2.name === "br") {
              text += "\n";
            } else {
              const isBlock = node2.name === "p" || node2.name === "div" || node2.name === "tr" || node2.name === "li" || node2.name[0] === "h" && node2.name[1] > "0" && node2.name[1] < "7";
              if (isBlock && text && text[text.length - 1] !== "\n") {
                text += "\n";
              }
              if (node2.children) {
                traversal(node2.children);
              }
              if (isBlock && text[text.length - 1] !== "\n") {
                text += "\n";
              } else if (node2.name === "td" || node2.name === "th") {
                text += "	";
              }
            }
          }
        })(nodes || this.nodes);
        return text;
      },
      /**
       * @description 获取内容大小和位置
       * @return {Promise}
       */
      getRect() {
        return new Promise((resolve, reject) => {
          uni.createSelectorQuery().in(this).select("#_root").boundingClientRect().exec((res) => res[0] ? resolve(res[0]) : reject(Error("Root label not found")));
        });
      },
      /**
       * @description 暂停播放媒体
       */
      pauseMedia() {
        for (let i2 = (this._videos || []).length; i2--; ) {
          this._videos[i2].pause();
        }
        const command = 'for(var e=document.getElementsByTagName("video"),i=e.length;i--;)e[i].pause()';
        let page = this.$parent;
        while (!page.$scope)
          page = page.$parent;
        page.$scope.$getAppWebview().evalJS(command);
      },
      /**
       * @description 设置媒体播放速率
       * @param {Number} rate 播放速率
       */
      setPlaybackRate(rate) {
        this.playbackRate = rate;
        for (let i2 = (this._videos || []).length; i2--; ) {
          this._videos[i2].playbackRate(rate);
        }
        const command = 'for(var e=document.getElementsByTagName("video"),i=e.length;i--;)e[i].playbackRate=' + rate;
        let page = this.$parent;
        while (!page.$scope)
          page = page.$parent;
        page.$scope.$getAppWebview().evalJS(command);
      },
      /**
       * @description 设置内容
       * @param {String} content html 内容
       * @param {Boolean} append 是否在尾部追加
       */
      setContent(content, append) {
        if (!append || !this.imgList) {
          this.imgList = [];
        }
        const nodes = new Parser(this).parse(content);
        this.$set(this, "nodes", append ? (this.nodes || []).concat(nodes) : nodes);
        this._videos = [];
        this.$nextTick(() => {
          this._hook("onLoad");
          this.$emit("load");
        });
        if (this.lazyLoad || this.imgList._unloadimgs < this.imgList.length / 2) {
          let height = 0;
          const callback = (rect) => {
            if (!rect || !rect.height)
              rect = {};
            if (rect.height === height) {
              this.$emit("ready", rect);
            } else {
              height = rect.height;
              setTimeout(() => {
                this.getRect().then(callback).catch(callback);
              }, 350);
            }
          };
          this.getRect().then(callback).catch(callback);
        } else {
          if (!this.imgList._unloadimgs) {
            this.getRect().then((rect) => {
              this.$emit("ready", rect);
            }).catch(() => {
              this.$emit("ready", {});
            });
          }
        }
      },
      /**
       * @description 调用插件钩子函数
       */
      _hook(name) {
        for (let i2 = plugins.length; i2--; ) {
          if (this.plugins[i2][name]) {
            this.plugins[i2][name]();
          }
        }
      }
    }
  };
  function _sfc_render$O(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_node = vue.resolveComponent("node");
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        id: "_root",
        class: vue.normalizeClass(($props.selectable ? "_select " : "") + "_root"),
        style: vue.normalizeStyle($props.containerStyle)
      },
      [
        !$data.nodes[0] ? vue.renderSlot(_ctx.$slots, "default", { key: 0 }, void 0, true) : (vue.openBlock(), vue.createBlock(_component_node, {
          key: 1,
          childs: $data.nodes,
          opts: [$props.lazyLoad, $props.loadingImg, $props.errorImg, $props.showImgMenu, $props.selectable],
          name: "span"
        }, null, 8, ["childs", "opts"]))
      ],
      6
      /* CLASS, STYLE */
    );
  }
  const __easycom_1$2 = /* @__PURE__ */ _export_sfc(_sfc_main$P, [["render", _sfc_render$O], ["__scopeId", "data-v-a290f043"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/mp-html/components/mp-html/mp-html.vue"]]);
  const _sfc_main$O = {
    __name: "article_detail",
    setup(__props, { expose: __expose }) {
      __expose();
      const article = vue.ref({
        title: "",
        content: "",
        banner: "",
        created_at: 0
      });
      const currentPage = vue.ref(1);
      const loading2 = vue.ref(false);
      const noMore = vue.ref(false);
      const replyComment = vue.ref(null);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      const replyForItem = vue.ref({});
      const handleCommentSubmit = async ({ content, replyInfo, origin }) => {
        var _a, _b;
        if (!await checkLogin(true))
          return;
        const requestData = {
          content,
          origin,
          target_id: parseInt(article.value.id),
          type: 7,
          ...replyInfo.id && {
            reply_id: replyInfo.id,
            reply_for: replyInfo.comment,
            reply_user_id: replyInfo.user_id,
            parent_id: replyInfo.parent_id > 0 ? replyInfo.parent_id : replyInfo.id
          }
        };
        try {
          const { data } = await uni.request({
            url: `${websiteUrl.value}/with-state/add-comment`,
            method: "POST",
            header: { Authorization: uni.getStorageSync("token") },
            data: requestData
          });
          if (data.status === "success") {
            if (data.data.parent_id === 0) {
              (_a = commentListRef.value) == null ? void 0 : _a.addNewComment(data.data);
            } else {
              (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment(data.data);
            }
            uni.showToast({ title: "评论成功" });
          }
        } catch (error2) {
          uni.showToast({ title: "评论失败", icon: "none" });
        }
      };
      const handleReplyComment = ({ parent, target }) => {
        var _a;
        const item = target || parent;
        replyForItem.value = item;
        (_a = commentInputRef.value) == null ? void 0 : _a.focusInput();
      };
      onLoad(async (options) => {
        if (!options.id)
          return uni.navigateBack();
        asyncGetUserInfo();
        await fetchArticle(options.id);
      });
      const fetchArticle = async (id) => {
        try {
          const { data } = await uni.request({
            url: `${websiteUrl.value}/article-detail?id=${id}`
          });
          if (data.status === "success") {
            article.value = data.data;
            uni.setNavigationBarTitle({ title: data.data.title });
          }
        } catch (error2) {
          uni.showToast({ title: "加载失败", icon: "none" });
        }
      };
      const checkLogin = async (showToast = false) => {
        if (global$1.isLogin)
          return true;
        if (showToast) {
          uni.showToast({ title: "请先登录", icon: "none" });
        }
        return false;
      };
      const formatTime = (timestamp) => {
        const date = new Date(timestamp * 1e3);
        return `${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}`;
      };
      const __returned__ = { article, currentPage, loading: loading2, noMore, replyComment, commentListRef, commentInputRef, replyForItem, handleCommentSubmit, handleReplyComment, fetchArticle, checkLogin, formatTime, ref: vue.ref, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$N(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_mp_html = resolveEasycom(vue.resolveDynamicComponent("mp-html"), __easycom_1$2);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock("view", { class: "article-container" }, [
      vue.createVNode(_component_view_logs),
      $setup.article.banner ? (vue.openBlock(), vue.createElementBlock("image", {
        key: 0,
        src: $setup.article.banner,
        mode: "widthFix",
        class: "banner"
      }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "content-wrapper" }, [
        vue.createElementVNode(
          "text",
          { class: "title" },
          vue.toDisplayString($setup.article.title),
          1
          /* TEXT */
        ),
        vue.createVNode(_component_mp_html, {
          content: $setup.article.content,
          class: "content-html"
        }, null, 8, ["content"]),
        $setup.article.id > 0 ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
          vue.createElementVNode("view", null, [
            vue.createVNode(_component_comment_list, {
              ref: "commentListRef",
              type: 7,
              "relation-id": parseInt($setup.article.id),
              onReply: $setup.handleReplyComment
            }, null, 8, ["relation-id"])
          ]),
          vue.createVNode(_component_comment_input, {
            ref: "commentInputRef",
            "reply-info": $setup.replyForItem,
            "target-id": $setup.article.id.toString(),
            onSubmit: $setup.handleCommentSubmit,
            "onUpdate:replyInfo": _cache[0] || (_cache[0] = (val) => $setup.replyForItem = val)
          }, null, 8, ["reply-info", "target-id"])
        ])) : vue.createCommentVNode("v-if", true)
      ])
    ]);
  }
  const PagesArticleDetailArticleDetail = /* @__PURE__ */ _export_sfc(_sfc_main$O, [["render", _sfc_render$N], ["__scopeId", "data-v-55f34774"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/article_detail/article_detail.vue"]]);
  const _sfc_main$N = {
    props: {
      // 下拉刷新阈值（触发刷新的高度）
      threshold: {
        type: Number,
        default: 150
      },
      // 滚动区域高度
      scrollHeight: {
        type: String,
        default: "100vh"
      }
    },
    data() {
      return {
        triggered: false,
        // 是否触发刷新
        // 下拉时显示的静态图片
        pullDownGif: "https://images1.fantuanpu.com/home/f4a2f2a997e45b87bfce23edc794d9a6.gif",
        // 刷新中显示的动态GIF
        refreshingGif: "https://images1.fantuanpu.com/home/f4a2f2a997e45b87bfce23edc794d9a6.gif",
        refreshText: "下拉刷新"
        // 提示文字
      };
    },
    methods: {
      // 触发刷新
      onRefresh() {
        this.triggered = true;
        this.refreshText = "正在刷新...";
        this.$emit("refresh");
      },
      // 刷新完成
      finishRefresh() {
        this.triggered = false;
        this.refreshText = "刷新完成";
        setTimeout(() => {
          this.refreshText = "下拉刷新";
        }, 800);
      },
      // 下拉状态恢复
      onRestore() {
        this.refreshText = "下拉刷新";
      },
      // 刷新中断
      onAbort() {
        this.refreshText = "刷新取消";
      }
    }
  };
  function _sfc_render$M(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode("scroll-view", {
        "scroll-y": "",
        "refresher-default-style": "none",
        "refresher-enabled": true,
        "refresher-triggered": $data.triggered,
        "refresher-threshold": $props.threshold,
        "refresher-background": "#f8f8f8",
        onRefresherrefresh: _cache[0] || (_cache[0] = (...args) => $options.onRefresh && $options.onRefresh(...args)),
        onRefresherrestore: _cache[1] || (_cache[1] = (...args) => $options.onRestore && $options.onRestore(...args)),
        onRefresherabort: _cache[2] || (_cache[2] = (...args) => $options.onAbort && $options.onAbort(...args)),
        style: vue.normalizeStyle({ height: $props.scrollHeight }),
        class: "scroll-view"
      }, [
        vue.createElementVNode(
          "view",
          {
            slot: "refresher",
            class: "refresh-container",
            style: vue.normalizeStyle({ height: `${$props.threshold}px` })
          },
          [
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["refresh-content", { refreshing: $data.triggered }])
              },
              [
                vue.createElementVNode("image", {
                  class: "refresh-icon",
                  src: $data.triggered ? $data.refreshingGif : $data.pullDownGif,
                  mode: "aspectFit"
                }, null, 8, ["src"]),
                vue.createElementVNode(
                  "text",
                  { class: "refresh-text" },
                  vue.toDisplayString($data.refreshText),
                  1
                  /* TEXT */
                )
              ],
              2
              /* CLASS */
            )
          ],
          4
          /* STYLE */
        ),
        vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
      ], 44, ["refresher-triggered", "refresher-threshold"])
    ]);
  }
  const __easycom_0$2 = /* @__PURE__ */ _export_sfc(_sfc_main$N, [["render", _sfc_render$M], ["__scopeId", "data-v-a113cad1"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/comment-loadding/comment-loadding.vue"]]);
  const _sfc_main$M = {
    data() {
      return {
        statusBarHeight: 0,
        dataList: [
          {
            id: 1,
            name: "张三",
            content: "今天天气真不错，适合户外运动！",
            time: "10:30",
            avatar: "https://randomuser.me/api/portraits/men/1.jpg"
          },
          {
            id: 2,
            name: "李四",
            content: "刚刚完成了一个重要的项目，庆祝一下！",
            time: "09:15",
            avatar: "https://randomuser.me/api/portraits/women/2.jpg"
          },
          {
            id: 3,
            name: "王五",
            content: "学习新技能真的很有成就感，继续加油！",
            time: "昨天 18:20",
            avatar: "https://randomuser.me/api/portraits/men/3.jpg"
          },
          {
            id: 4,
            name: "赵六",
            content: "分享一张今天拍的美景照片，大自然真神奇！",
            time: "昨天 16:45",
            avatar: "https://randomuser.me/api/portraits/women/4.jpg"
          },
          {
            id: 5,
            name: "钱七",
            content: "尝试了新的菜谱，味道超级棒！",
            time: "昨天 14:30",
            avatar: "https://randomuser.me/api/portraits/men/5.jpg"
          }
        ]
      };
    },
    onLoad() {
      const systemInfo2 = uni.getSystemInfoSync();
      this.statusBarHeight = systemInfo2.statusBarHeight;
    },
    methods: {
      handleRefresh() {
        setTimeout(() => {
          const newItem = {
            id: this.dataList.length + 1,
            name: `新用户${this.dataList.length + 1}`,
            content: `这是新刷出的内容 #${this.dataList.length + 1}`,
            time: "刚刚",
            avatar: `https://randomuser.me/api/portraits/${this.dataList.length % 2 === 0 ? "men" : "women"}/${this.dataList.length + 1}.jpg`
          };
          this.dataList.unshift(newItem);
          this.$refs.refresh.finishRefresh();
        }, 1500);
      }
    }
  };
  function _sfc_render$L(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_comment_loadding = resolveEasycom(vue.resolveDynamicComponent("comment-loadding"), __easycom_0$2);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_comment_loadding, {
        ref: "refresh",
        onRefresh: $options.handleRefresh,
        scrollHeight: `calc(100vh - ${$data.statusBarHeight}px)`
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "content" }, [
            vue.createElementVNode("view", { class: "header" }, [
              vue.createElementVNode("text", { class: "title" }, "自定义下拉刷新演示")
            ]),
            vue.createElementVNode("view", { class: "card-list" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($data.dataList, (item, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: "card"
                  }, [
                    vue.createElementVNode("view", { class: "card-header" }, [
                      vue.createElementVNode("image", {
                        class: "avatar",
                        src: item.avatar,
                        mode: "aspectFill"
                      }, null, 8, ["src"]),
                      vue.createElementVNode(
                        "text",
                        { class: "name" },
                        vue.toDisplayString(item.name),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode(
                      "text",
                      { class: "card-content" },
                      vue.toDisplayString(item.content),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "card-time" },
                      vue.toDisplayString(item.time),
                      1
                      /* TEXT */
                    )
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["onRefresh", "scrollHeight"])
    ]);
  }
  const PagesLoaddingLoadding = /* @__PURE__ */ _export_sfc(_sfc_main$M, [["render", _sfc_render$L], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/loadding/loadding.vue"]]);
  const _sfc_main$L = {
    __name: "private",
    setup(__props, { expose: __expose }) {
      __expose();
      uni.setNavigationBarTitle({
        title: "隐私政策"
      });
      const __returned__ = {};
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$K(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "container" }, [
        vue.createElementVNode("view", { class: "content" }, [
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-info-circle" }),
              vue.createTextVNode(" 引言")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "本应用尊重并保护所有使用服务用户的个人隐私权。"),
            vue.createElementVNode("text", { class: "policy-text" }, "为了给您提供更准确、更有个性化的服务，本应用会按照本隐私权政策的规定使用和披露您的个人信息。"),
            vue.createElementVNode("text", { class: "policy-text" }, "但本应用将以高度的勤勉、审慎义务对待这些信息。"),
            vue.createElementVNode("text", { class: "policy-text" }, "除本隐私权政策另有规定外，在未征得您事先许可的情况下，本应用不会将这些信息对外披露或向第三方提供。"),
            vue.createElementVNode("text", { class: "policy-text" }, "本应用会不时更新本隐私权政策。"),
            vue.createElementVNode("text", { class: "policy-text" }, "您在同意本应用服务使用协议之时，即视为您已经同意本隐私权政策全部内容。"),
            vue.createElementVNode("text", { class: "policy-text" }, "本隐私权政策属于本应用服务使用协议不可分割的一部分。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-globe" }),
              vue.createTextVNode(" 1. 适用范围")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "(a) 在您注册本应用帐号时，您根据本应用要求提供的个人注册信息；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b) 在您使用本应用网络服务，或访问本应用平台网页时，本应用自动接收并记录的您的浏览器和计算机上的信息，包括但不限于您的IP地址、浏览器的类型、使用的语言、访问日期和时间、软硬件特征信息及您需求的网页记录等数据；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(c) 本应用通过合法途径从商业伙伴处取得的用户个人数据。"),
            vue.createElementVNode("text", { class: "policy-text" }, "您了解并同意，以下信息不适用本隐私权政策："),
            vue.createElementVNode("text", { class: "policy-text" }, "(a) 您在使用本应用平台提供的搜索服务时输入的关键字信息；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b) 本应用收集到的您在本应用发布的有关信息数据，包括但不限于参与活动、成交信息及评价详情；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(c) 违反法律规定或违反本应用规则行为及本应用已对您采取的措施。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-cogs" }),
              vue.createTextVNode(" 2. 信息使用")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "(a)本应用不会向任何无关第三方提供、出售、出租、分享或交易您的个人信息，除非事先得到您的许可，或该第三方和本应用（含本应用关联公司）单独或共同为您提供服务，且在该服务结束后，其将被禁止访问包括其以前能够访问的所有这些资料。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b) 本应用亦不允许任何第三方以任何手段收集、编辑、出售或者无偿传播您的个人信息。任何本应用平台用户如从事上述活动，一经发现，本应用有权立即终止与该用户的服务协议。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(c) 为服务用户的目的，本应用可能通过使用您的个人信息，向您提供您感兴趣的信息，包括但不限于向您发出产品和服务信息，或者与本应用合作伙伴共享信息以便他们向您发送有关其产品和服务的信息（后者需要您的事先同意）。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-shield-alt" }),
              vue.createTextVNode(" 3. 信息披露")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "在如下情况下，本应用将依据您的个人意愿或法律的规定全部或部分的披露您的个人信息："),
            vue.createElementVNode("text", { class: "policy-text" }, "(a) 经您事先同意，向第三方披露；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b)为提供您所要求的产品和服务，而必须和第三方分享您的个人信息；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(c) 根据法律的有关规定，或者行政或司法机构的要求，向第三方或者行政、司法机构披露；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(d) 如您出现违反中国有关法律、法规或者本应用服务协议或相关规则的情况，需要向第三方披露；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(e) 如您是适格的知识产权投诉人并已提起投诉，应被投诉人要求，向被投诉人披露，以便双方处理可能的权利纠纷；"),
            vue.createElementVNode("text", { class: "policy-text" }, "(f) 在本应用平台上创建的某一交易中，如交易任何一方履行或部分履行了交易义务并提出信息披露请求的，本应用有权决定向该用户提供其交易对方的联络方式等必要信息，以促成交易的完成或纠纷的解决。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(g) 其它本应用根据法律、法规或者网站政策认为合适的披露。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-database" }),
              vue.createTextVNode(" 4. 信息存储和交换")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "本应用收集的有关您的信息和资料将保存在本应用及（或）其关联公司的服务器上，这些信息和资料可能传送至您所在国家、地区或本应用收集信息和资料所在地的境外并在境外被访问、存储和展示。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-cookie" }),
              vue.createTextVNode(" 5. Cookie的使用")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "(a) 在您未拒绝接受cookies的情况下，本应用会在您的计算机上设定或取用cookies ，以便您能登录或使用依赖于cookies的本应用平台服务或功能。本应用使用cookies可为您提供更加周到的个性化服务，包括推广服务。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b) 您有权选择接受或拒绝接受cookies。您可以通过修改浏览器设置的方式拒绝接受cookies。但如果您选择拒绝接受cookies，则您可能无法登录或使用依赖于cookies的本应用网络服务或功能。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(c) 通过本应用所设cookies所取得的有关信息，将适用本政策。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-lock" }),
              vue.createTextVNode(" 6. 信息安全")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "(a) 本应用帐号均有安全保护功能，请妥善保管您的用户名及密码信息。本应用将通过对用户密码进行加密等安全措施确保您的信息不丢失，不被滥用和变造。尽管有前述安全措施，但同时也请您注意在信息网络上不存在“完善的安全措施”。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b) 在使用本应用网络服务进行网上交易时，您不可避免的要向交易对方或潜在的交易对方披露自己的个人信息，如联络方式或者邮政地址。请您妥善保护自己的个人信息，仅在必要的情形下向他人提供。如您发现自己的个人信息泄密，尤其是本应用用户名及密码发生泄露，请您立即联络本应用客服，以便本应用采取相应措施。")
          ]),
          vue.createElementVNode("view", { class: "section" }, [
            vue.createElementVNode("h2", { class: "section-title" }, [
              vue.createElementVNode("i", { class: "fas fa-sync-alt" }),
              vue.createTextVNode(" 7. 本隐私政策的更改")
            ]),
            vue.createElementVNode("text", { class: "policy-text" }, "(a)如果决定更改隐私政策，我们会在本政策中、本公司网站中以及我们认为适当的位置发布这些更改，以便您了解我们如何收集、使用您的个人信息，哪些人可以访问这些信息，以及在什么情况下我们会透露这些信息。"),
            vue.createElementVNode("text", { class: "policy-text" }, "(b)本公司保留随时修改本政策的权利，因此请经常查看。如对本政策作出重大更改，本公司会通过网站通知的形式告知。")
          ]),
          vue.createElementVNode("view", { class: "last-update" }, [
            vue.createElementVNode("text", { class: "policy-text" }, [
              vue.createElementVNode("i", { class: "fas fa-exclamation-circle" }),
              vue.createTextVNode(" 重要提示：请您妥善保护自己的个人信息，仅在必要的情形下向他人提供。如您发现自己的个人信息泄密，尤其是本应用用户名及密码发生泄露，请您立即联络本应用客服，以便本应用采取相应措施。")
            ])
          ])
        ])
      ])
    ]);
  }
  const PagesPrivatePrivate = /* @__PURE__ */ _export_sfc(_sfc_main$L, [["render", _sfc_render$K], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/private/private.vue"]]);
  const _sfc_main$K = {
    __name: "permission",
    setup(__props, { expose: __expose }) {
      __expose();
      vue.onMounted(() => {
        uni.setNavigationBarTitle({
          title: "用户协议"
        });
      });
      const __returned__ = { onMounted: vue.onMounted };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$J(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "container" }, [
          vue.createElementVNode("view", { class: "content" }, [
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "数据保护条款"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "严格禁止任何未经授权的的人工转载、复制或传播平台内容")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "未经授权禁止使用爬虫工具获取平台数据")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "所有用户生成内容均受版权保护，未经授权不得使用")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "我们部署了反爬虫系统，实时监控数据访问行为")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "我们保留对侵权行为采取法律手段的权利")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "用户行为规范与内容管理"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, " 您在使用本应用过程中所发布的任何文字、图片、音视频、评论等内容，均需遵守相关法律法规及本协议的约定，不得包含以下内容（包括但不限于）： 淫秽、暴力、威胁、骚扰、辱骂、恐吓、恶意中伤他人； 散布谣言、虚假信息、诈骗、钓鱼链接； 仇恨言论、歧视性言论或可能引发社会不安的内容； 其他违反法律法规、社会公序良俗或平台管理规范的内容。 ")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "我们对不良内容和滥用行为采取“零容忍”政策，包括但不限于恶意骚扰、辱骂他人、发送垃圾内容、冒充他人等。一经发现，我们有权立即删除相关内容，并封禁发布者账号，且无需另行通知。")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "用户可通过本应用提供的“举报”功能对不良信息进行举报，我们将在 24 小时内处理并视情况采取禁言、删除、封号等措施。")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "相机权限"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于扫描二维码、拍摄照片与上传图片")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 仅在您主动使用拍照功能时申请")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 不会在后台访问您的相机")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "位置权限"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于查找附近的娃店")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 仅在您使用基于位置的服务时申请")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 您可以随时在系统设置中关闭此权限")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "通知权限"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于接受BJD娃圈关注品牌的上新通知")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 您可以自定义接收的通知类型")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 可以在系统设置中随时关闭通知")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "存储权限"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于保存照片和下载内容")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于缓存应用数据以提升加载速度")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 不会访问您设备上的其他文件")
              ])
            ]),
            vue.createElementVNode("view", { class: "section" }, [
              vue.createElementVNode("text", { class: "section-title" }, "设备信息"),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于适配不同设备，提供更好的兼容性")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 用于统计分析，改进产品质量")
              ]),
              vue.createElementVNode("view", { class: "permission-item" }, [
                vue.createElementVNode("text", { class: "permission-text" }, "• 收集的信息包括设备型号、操作系统版本等")
              ])
            ])
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesPermissionPermission = /* @__PURE__ */ _export_sfc(_sfc_main$K, [["render", _sfc_render$J], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/permission/permission.vue"]]);
  const _sfc_main$J = {
    __name: "drag-image-test",
    setup(__props, { expose: __expose }) {
      __expose();
      const list = vue.ref([]);
      function getAccountBookData(type) {
        formatAppLog("log", "at pages/drag-image-test/drag-image-test.vue:38", global$1);
        if (!global$1.isLogin) {
          return;
        }
        let token = uni.getStorageSync("token");
        let url = websiteUrl.value + "/with-state/account-book";
        if (type && type !== "全部") {
          url = websiteUrl.value + "/with-state/account-book?type=" + type;
        }
        uni.request({
          url,
          method: "GET",
          header: {
            "Authorization": token
          },
          success: (res) => {
            list.value = res.data.data.account_books;
            formatAppLog("log", "at pages/drag-image-test/drag-image-test.vue:58", list.value);
          },
          fail: (err) => {
            formatAppLog("log", "at pages/drag-image-test/drag-image-test.vue:61", err);
          }
        });
      }
      asyncGetUserInfo().then((userInfo) => {
        getAccountBookData();
      });
      const __returned__ = { list, getAccountBookData, ref: vue.ref, computed: vue.computed, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get wechatSignLogin() {
        return wechatSignLogin;
      }, get getUserInfo() {
        return getUserInfo;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$I(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_shmily_drag_image = resolveEasycom(vue.resolveDynamicComponent("shmily-drag-image"), __easycom_3$6);
    return vue.openBlock(), vue.createElementBlock("view", { style: { "min-height": "100vh", "width": "100vw" } }, [
      $setup.list.length > 0 ? (vue.openBlock(), vue.createBlock(_component_shmily_drag_image, {
        key: 0,
        modelValue: $setup.list,
        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.list = $event),
        "border-radius": "20"
      }, null, 8, ["modelValue"])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PagesDragImageTestDragImageTest = /* @__PURE__ */ _export_sfc(_sfc_main$J, [["render", _sfc_render$I], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/drag-image-test/drag-image-test.vue"]]);
  const _imports_0$4 = "/static/images/empty.png";
  const _sfc_main$I = {
    __name: "artist_info",
    setup(__props, { expose: __expose }) {
      __expose();
      const artistInfo = vue.ref({
        artist_id: 0,
        brand_id: 0,
        brand_name: "",
        description: "",
        logo_image: "",
        highlight_images: [],
        status_of_artist: 0,
        status_of_artist_text: "",
        status_of_hairstylist: 0,
        status_of_hairstylist_text: "",
        base_price_of_artist: 0,
        base_price_of_hairstylist: 0,
        // 兼容可能的后端字段
        contact: "",
        contact_of_artist: "",
        contact_of_hairstylist: ""
      });
      const descExpanded = vue.ref(false);
      const worksList = vue.ref([]);
      const activeMode = vue.ref("works");
      const activePlanTab = vue.ref("artist");
      const currentPage = vue.ref(1);
      const pageSize2 = vue.ref(20);
      const hasMore = vue.ref(true);
      const brandId = vue.ref(0);
      const orderPlans = vue.ref([]);
      const plansPage = vue.ref(1);
      const plansPageSize = vue.ref(5);
      const hasMorePlans = vue.ref(true);
      const getUrlParams = (options) => {
        brandId.value = (options == null ? void 0 : options.brand_id) || 0;
      };
      const getTagColor = (tag) => {
        var _a;
        const colors = ["#FFEEF2", "#E8F5FF", "#F0FFF0", "#FFF8E8", "#F5F0FF", "#E8FFF8", "#FFF0F5", "#F0F8FF"];
        const index = ((_a = tag == null ? void 0 : tag.toString()) == null ? void 0 : _a.charCodeAt(0)) % colors.length || 0;
        return colors[index];
      };
      function jump2brand(id) {
        uni.navigateTo({ url: "/pages/brand/brand?brand_id=" + id });
      }
      const fetchArtistInfo = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/info?brand_id=${brandId.value}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            artistInfo.value = res.data.data || {};
          } else {
            uni.showToast({ title: "获取创作者信息失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "网络错误", icon: "none" });
        }
      };
      const fetchWorksList = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/bjd-faceup?brand_id=${brandId.value}&page=${currentPage.value}&size=${pageSize2.value}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            const newList = res.data.data.list || [];
            worksList.value = currentPage.value === 1 ? newList : [...worksList.value, ...newList];
            hasMore.value = worksList.value.length < (res.data.data.total || 0);
          } else {
            uni.showToast({ title: "获取作品列表失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "网络错误", icon: "none" });
        }
      };
      const fetchOrderPlans = async () => {
        try {
          const artistType = activePlanTab.value === "artist" ? 1 : 2;
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/order-plans?brand_id=${brandId.value}&artist_type=${artistType}&page=${plansPage.value}&size=${plansPageSize.value}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            const newPlans = res.data.data || [];
            orderPlans.value = plansPage.value === 1 ? newPlans : [...orderPlans.value, ...newPlans];
            hasMorePlans.value = newPlans.length === plansPageSize.value;
          } else {
            uni.showToast({ title: "获取开单记录失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "网络错误", icon: "none" });
        }
      };
      const copyContactInfo = () => {
        const byTab = activePlanTab.value === "artist" ? artistInfo.value.contact_of_artist || "" : artistInfo.value.contact_of_hairstylist || "";
        const contactInfo = byTab || artistInfo.value.contact || "";
        if (!contactInfo) {
          uni.showToast({ title: "暂无联系信息", icon: "none" });
          return;
        }
        uni.setClipboardData({
          data: contactInfo,
          success: () => uni.showToast({ title: "已复制联系信息", icon: "success" })
        });
      };
      const previewImages = (images = [], currentIndex = 0) => {
        if (!(images == null ? void 0 : images.length))
          return;
        uni.previewImage({
          current: currentIndex,
          urls: images,
          indicator: "number",
          loop: true
        });
      };
      const getOrderTypeText = (type) => {
        const types = { 1: "长期接单", 2: "限时手速", 3: "限时抽选", 4: "限时黑箱卡", 9: "关闭投递" };
        return types[type] || "未知类型";
      };
      const switchMode = (mode) => {
        activeMode.value = mode;
        if (mode === "plans") {
          plansPage.value = 1;
          fetchOrderPlans();
        }
      };
      const switchPlanTab = (tab) => {
        activePlanTab.value = tab;
        if (activeMode.value === "works") {
          currentPage.value = 1;
          fetchWorksList();
        } else {
          plansPage.value = 1;
          fetchOrderPlans();
        }
      };
      const formatDate = (timestamp) => {
        if (!timestamp)
          return "";
        const d2 = new Date(timestamp);
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        const h2 = String(d2.getHours()).padStart(2, "0");
        const mm = String(d2.getMinutes()).padStart(2, "0");
        const s2 = String(d2.getSeconds()).padStart(2, "0");
        return `${y2}-${m2}-${day} ${h2}:${mm}:${s2}`;
      };
      const navigateToGoods = (goodsId) => {
        uni.navigateTo({ url: `/pages/goods/goods?goods_id=${goodsId || 1576}` });
      };
      const navigateToFaceup = (id) => {
        uni.navigateTo({ url: `/pages/artwork/artwork?id=${id}` });
      };
      onLoad((options) => {
        uni.setNavigationBarTitle({ title: "妆师毛娘主页" });
        getUrlParams(options);
        fetchArtistInfo();
        fetchWorksList();
      });
      const __returned__ = { artistInfo, descExpanded, worksList, activeMode, activePlanTab, currentPage, pageSize: pageSize2, hasMore, brandId, orderPlans, plansPage, plansPageSize, hasMorePlans, getUrlParams, getTagColor, jump2brand, fetchArtistInfo, fetchWorksList, fetchOrderPlans, copyContactInfo, previewImages, getOrderTypeText, switchMode, switchPlanTab, formatDate, navigateToGoods, navigateToFaceup, ref: vue.ref, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$H(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "artist-detail" }, [
          vue.createElementVNode("view", { class: "artist-header" }, [
            vue.createElementVNode("view", { class: "avatar-container" }, [
              vue.createElementVNode("image", {
                class: "avatar",
                src: $setup.artistInfo.logo_image,
                mode: "aspectFill",
                onClick: _cache[0] || (_cache[0] = ($event) => $setup.jump2brand($setup.artistInfo.brand_id))
              }, null, 8, ["src"])
            ]),
            vue.createElementVNode("view", { class: "info-container" }, [
              vue.createElementVNode("view", { class: "name-container" }, [
                vue.createElementVNode(
                  "text",
                  {
                    class: "brand-name",
                    onClick: _cache[1] || (_cache[1] = ($event) => $setup.jump2brand($setup.artistInfo.brand_id))
                  },
                  vue.toDisplayString($setup.artistInfo.brand_name),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode(
                "text",
                {
                  class: vue.normalizeClass(["description", { clamp: !$setup.descExpanded }]),
                  onClick: _cache[2] || (_cache[2] = ($event) => $setup.descExpanded = !$setup.descExpanded)
                },
                vue.toDisplayString($setup.artistInfo.description),
                3
                /* TEXT, CLASS */
              ),
              vue.createElementVNode("view", { class: "status-container" }, [
                $setup.activePlanTab === "artist" ? (vue.openBlock(), vue.createElementBlock(
                  "text",
                  {
                    key: 0,
                    class: "status"
                  },
                  "妆师接单方式: " + vue.toDisplayString($setup.artistInfo.status_of_artist_text),
                  1
                  /* TEXT */
                )) : (vue.openBlock(), vue.createElementBlock(
                  "text",
                  {
                    key: 1,
                    class: "status"
                  },
                  "毛娘接单方式: " + vue.toDisplayString($setup.artistInfo.status_of_hairstylist_text),
                  1
                  /* TEXT */
                ))
              ]),
              vue.createElementVNode("view", { class: "contact-container" }, [
                vue.createElementVNode(
                  "text",
                  { class: "contact-info" },
                  "联系方式：" + vue.toDisplayString($setup.artistInfo.contact ? $setup.artistInfo.contact : "尚未设置"),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("text", {
                  class: "copy-tip",
                  onClick: $setup.copyContactInfo
                }, "复制")
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "main-tabs" }, [
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["tab-item", { active: $setup.activeMode === "works" }]),
                onClick: _cache[3] || (_cache[3] = ($event) => $setup.switchMode("works"))
              },
              "作品展示",
              2
              /* CLASS */
            ),
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["tab-item", { active: $setup.activeMode === "plans" }]),
                onClick: _cache[4] || (_cache[4] = ($event) => $setup.switchMode("plans"))
              },
              "开单记录",
              2
              /* CLASS */
            )
          ]),
          $setup.activeMode === "works" ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
            vue.createElementVNode("view", { class: "showcase-header" }, [
              vue.createElementVNode("text", { class: "showcase-title" }, "置顶作品"),
              vue.createElementVNode("view", { class: "showcase-divider" })
            ]),
            vue.createElementVNode("scroll-view", {
              class: "highlight-scroll",
              "scroll-x": ""
            }, [
              vue.createElementVNode("view", { class: "highlight-container" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.artistInfo.highlight_images, (img, index) => {
                    return vue.openBlock(), vue.createElementBlock("image", {
                      key: index,
                      src: img,
                      mode: "aspectFill",
                      class: "highlight-img",
                      onClick: ($event) => $setup.previewImages($setup.artistInfo.highlight_images, index)
                    }, null, 8, ["src", "onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ]),
            vue.createElementVNode("view", { class: "showcase-header" }, [
              vue.createElementVNode("text", { class: "showcase-title" }, "作品展示"),
              vue.createElementVNode("view", { class: "showcase-divider" })
            ]),
            vue.createElementVNode("view", { class: "tabs" }, [
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["tab-item", { active: $setup.activePlanTab === "artist" }]),
                  onClick: _cache[5] || (_cache[5] = ($event) => $setup.switchPlanTab("artist"))
                },
                "妆师作品",
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["tab-item", { active: $setup.activePlanTab === "hairstylist" }]),
                  onClick: _cache[6] || (_cache[6] = ($event) => $setup.switchPlanTab("hairstylist"))
                },
                "毛娘作品",
                2
                /* CLASS */
              )
            ]),
            $setup.activePlanTab === "artist" ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "works-list"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.worksList, (work, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "work-item",
                    key: work.id,
                    onClick: ($event) => $setup.navigateToFaceup(work.id)
                  }, [
                    vue.createElementVNode("image", {
                      class: "work-cover",
                      src: work.image_urls && work.image_urls[0],
                      mode: "aspectFill",
                      onClick: vue.withModifiers(($event) => $setup.previewImages(work.image_urls || [], 0), ["stop"])
                    }, null, 8, ["src", "onClick"]),
                    vue.createElementVNode("view", { class: "work-info" }, [
                      vue.createElementVNode("view", { class: "work-header" }, [
                        vue.createElementVNode("text", {
                          class: "work-head",
                          onClick: vue.withModifiers(($event) => $setup.navigateToGoods(work.goods_id), ["stop"])
                        }, vue.toDisplayString(work.head_name), 9, ["onClick"]),
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["sex-tag", work.sex === "男" ? "male" : "female"])
                          },
                          vue.toDisplayString(work.sex),
                          3
                          /* TEXT, CLASS */
                        )
                      ]),
                      work.style_tags && work.style_tags.length ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "style-tags"
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(work.style_tags, (tag, idx) => {
                            return vue.openBlock(), vue.createElementBlock(
                              "view",
                              {
                                class: "style-tag",
                                key: idx,
                                style: vue.normalizeStyle({ backgroundColor: $setup.getTagColor(tag) })
                              },
                              vue.toDisplayString(tag),
                              5
                              /* TEXT, STYLE */
                            );
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])) : vue.createCommentVNode("v-if", true)
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "no-works"
            }, [
              vue.createElementVNode("image", {
                class: "empty-icon",
                src: _imports_0$4,
                mode: "aspectFit"
              }),
              vue.createElementVNode("text", { class: "empty-text" }, "暂无作品")
            ])),
            $setup.activePlanTab === "artist" && $setup.hasMore ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 2,
              class: "load-more"
            }, [
              vue.createVNode(_component_uni_load_more, { status: "loading" })
            ])) : $setup.activePlanTab === "artist" && $setup.worksList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 3,
              class: "no-more"
            }, [
              vue.createElementVNode("text", null, "— 已经到底了 —")
            ])) : vue.createCommentVNode("v-if", true)
          ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
            vue.createElementVNode("view", { class: "showcase-header" }, [
              vue.createElementVNode("text", { class: "showcase-title" }, "开单记录"),
              vue.createElementVNode("view", { class: "showcase-divider" })
            ]),
            vue.createElementVNode("view", { class: "tabs" }, [
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["tab-item", { active: $setup.activePlanTab === "artist" }]),
                  onClick: _cache[7] || (_cache[7] = ($event) => $setup.switchPlanTab("artist"))
                },
                "妆师开单",
                2
                /* CLASS */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["tab-item", { active: $setup.activePlanTab === "hairstylist" }]),
                  onClick: _cache[8] || (_cache[8] = ($event) => $setup.switchPlanTab("hairstylist"))
                },
                "毛娘开单",
                2
                /* CLASS */
              )
            ]),
            vue.createElementVNode("view", { class: "plans-list" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.orderPlans, (plan, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: plan.id,
                    class: "plan-item"
                  }, [
                    vue.createElementVNode("view", { class: "plan-images" }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList(plan.images, (img, imgIndex) => {
                          return vue.openBlock(), vue.createElementBlock("image", {
                            key: imgIndex,
                            src: img,
                            mode: "aspectFill",
                            class: "plan-image",
                            onClick: ($event) => $setup.previewImages(plan.images, imgIndex)
                          }, null, 8, ["src", "onClick"]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ]),
                    vue.createElementVNode("view", { class: "plan-info" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "info-item" },
                        "接单类型: " + vue.toDisplayString($setup.getOrderTypeText(plan.order_type)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "info-item" },
                        "名额: " + vue.toDisplayString(plan.max_participants) + "人/每人" + vue.toDisplayString(plan.max_submissions_per_user) + "单",
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("text", { class: "info-item" }, [
                        vue.createTextVNode(
                          vue.toDisplayString($setup.formatDate(plan.open_time * 1e3)) + " 至 " + vue.toDisplayString($setup.formatDate(plan.close_time * 1e3)) + " ",
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "view",
                          {
                            class: vue.normalizeClass(["plan-status", plan.is_active ? "active" : "inactive"])
                          },
                          vue.toDisplayString(plan.is_active ? "进行中" : "已结束"),
                          3
                          /* TEXT, CLASS */
                        )
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "tiers-section" }, [
                      vue.createElementVNode("text", { class: "section-title" }, "档位选择"),
                      vue.createElementVNode("view", { class: "tiers-container" }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(plan.tiers, (tier, tierIndex) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              key: tierIndex,
                              class: "tier-item"
                            }, [
                              vue.createElementVNode(
                                "text",
                                { class: "tier-title" },
                                vue.toDisplayString(tier.title),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "tier-price" },
                                "¥" + vue.toDisplayString(tier.price),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "tier-desc" },
                                vue.toDisplayString(tier.description),
                                1
                                /* TEXT */
                              )
                            ]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ]),
                    plan.addons && plan.addons.length ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "addons-section"
                    }, [
                      vue.createElementVNode("text", { class: "section-title" }, "加购服务"),
                      vue.createElementVNode("view", { class: "addons-container" }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(plan.addons, (addon, addonIndex) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              key: addonIndex,
                              class: "addon-item"
                            }, [
                              vue.createElementVNode(
                                "text",
                                { class: "addon-title" },
                                vue.toDisplayString(addon.title),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "addon-price" },
                                "+¥" + vue.toDisplayString(addon.price),
                                1
                                /* TEXT */
                              ),
                              vue.createElementVNode(
                                "text",
                                { class: "addon-desc" },
                                vue.toDisplayString(addon.description),
                                1
                                /* TEXT */
                              )
                            ]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ])) : vue.createCommentVNode("v-if", true)
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            $setup.hasMorePlans ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "load-more"
            }, [
              vue.createVNode(_component_uni_load_more, { status: "loading" })
            ])) : $setup.orderPlans.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "no-more"
            }, [
              vue.createElementVNode("text", null, "— 没有更多开单记录了 —")
            ])) : (vue.openBlock(), vue.createElementBlock("view", {
              key: 2,
              class: "no-works"
            }, [
              vue.createElementVNode("image", {
                class: "empty-icon",
                src: _imports_0$4,
                mode: "aspectFit"
              }),
              vue.createElementVNode("text", { class: "empty-text" }, "暂无开单记录")
            ]))
          ]))
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesArtistInfoArtistInfo = /* @__PURE__ */ _export_sfc(_sfc_main$I, [["render", _sfc_render$H], ["__scopeId", "data-v-d20a8a4d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/artist_info/artist_info.vue"]]);
  const _imports_0$3 = "/static/artist/left.png";
  const _sfc_main$H = {
    __name: "artwork",
    setup(__props, { expose: __expose }) {
      var _a;
      __expose();
      const detailData = vue.ref({
        id: 0,
        title: "",
        head_name: "",
        faceup_tag_content: [],
        sex: 0,
        created_at: 0,
        brand_id: 0,
        art_type: 1,
        goods_id: 0,
        images: [],
        like_count: 0
      });
      const goodsInfo = vue.ref(null);
      const brandInfo = vue.ref(null);
      const loading2 = vue.ref(true);
      const error2 = vue.ref(false);
      const errorMsg = vue.ref("");
      const hasLike = vue.ref(false);
      const pageId = vue.ref(0);
      const commentType = vue.ref(8);
      const commentListRef = vue.ref(null);
      const commentInputRef = vue.ref(null);
      const replyForItem = vue.ref({});
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      const goBack = () => {
        const pages2 = getCurrentPages();
        if (pages2.length > 1) {
          uni.navigateBack({ delta: 1 });
        } else {
          uni.switchTab({ url: "/pages/index/index" });
        }
      };
      const onShare = () => {
        const url = `/pages/faceup/detail?id=${pageId.value}`;
        uni.setClipboardData({
          data: url,
          success: () => uni.showToast({ title: "链接已复制", icon: "none" })
        });
      };
      onShareAppMessage(() => {
        var _a2, _b;
        return {
          title: detailData.value.title || (((_a2 = brandInfo.value) == null ? void 0 : _a2.brand_name) ? `${brandInfo.value.brand_name} · 作品` : "BJD作品"),
          path: `/pages/faceup/detail?id=${pageId.value}`,
          imageUrl: ((_b = detailData.value.images) == null ? void 0 : _b[0]) || ""
        };
      });
      onShareTimeline == null ? void 0 : onShareTimeline(() => {
        var _a2;
        return {
          title: detailData.value.title || "BJD作品",
          imageUrl: ((_a2 = detailData.value.images) == null ? void 0 : _a2[0]) || ""
        };
      });
      const statusBarHeight = ((_a = uni.getSystemInfoSync()) == null ? void 0 : _a.statusBarHeight) || 0;
      const heartTopPx = `${statusBarHeight + 12}px`;
      function formatNumber(num) {
        if (!num)
          return "0";
        if (num < 1e3)
          return num.toString();
        return `${Math.floor(num / 1e3)}k+`;
      }
      function getSexText(sex) {
        const sexMap = {
          0: "未知",
          1: "男",
          2: "女"
        };
        return sexMap[sex] || "未知";
      }
      function getArtTypeText(type) {
        const typeMap = {
          1: "妆师",
          2: "毛娘"
        };
        return typeMap[type] || "未知类型";
      }
      function formatTimestamp(timestamp) {
        if (!timestamp)
          return "";
        const date = new Date(timestamp * 1e3);
        return `${date.getFullYear()}-${(date.getMonth() + 1).toString().padStart(2, "0")}-${date.getDate().toString().padStart(2, "0")}`;
      }
      function viewFullImage(index) {
        uni.previewImage({
          current: detailData.value.images[index],
          urls: detailData.value.images
        });
      }
      const fetchData = async (id) => {
        try {
          loading2.value = true;
          const res = await uni.request({
            url: websiteUrl.value + `/faceup/detail?id=${id}`
          });
          if (res.data.status !== "success") {
            handleError(res.data.msg || "数据加载失败");
            return;
          }
          detailData.value = res.data.data;
          if (!Array.isArray(detailData.value.images)) {
            detailData.value.images = detailData.value.face_up_image_urls ? detailData.value.face_up_image_urls.split(",") : [];
          }
          if (detailData.value.goods_id) {
            fetchGoodsInfo(detailData.value.goods_id);
          }
          if (detailData.value.brand_id) {
            fetchBrandInfo(detailData.value.brand_id);
          }
        } catch (err) {
          formatAppLog("error", "at pages/artwork/artwork.vue:294", "获取作品详情失败:", err);
          handleError("数据加载失败");
        } finally {
          loading2.value = false;
        }
      };
      const fetchGoodsInfo = async (goodsId) => {
        try {
          const res = await uni.request({
            url: websiteUrl.value + `/goods?id=${goodsId}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            goodsInfo.value = res.data.data;
          }
        } catch (e2) {
          formatAppLog("error", "at pages/artwork/artwork.vue:313", "获取商品信息失败:", e2);
        }
      };
      const fetchBrandInfo = async (brandId) => {
        try {
          const res = await uni.request({
            url: websiteUrl.value + `/brand-info?id=${brandId}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            brandInfo.value = res.data.data;
          }
        } catch (e2) {
          formatAppLog("error", "at pages/artwork/artwork.vue:329", "获取品牌信息失败:", e2);
        }
      };
      const handleError = (msg) => {
        error2.value = true;
        errorMsg.value = msg;
        uni.showToast({
          title: msg,
          icon: "none"
        });
      };
      function navigateToGoods(goodsId) {
        uni.navigateTo({
          url: `/pages/goods/goods?goods_id=${goodsId}`
        });
      }
      function navigateToBrand(brandId) {
        uni.navigateTo({
          url: `/pages/brand/brand?brand_id=${brandId}`
        });
      }
      function likeFn() {
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        const requestData = {
          id: parseInt(pageId.value),
          type: 7
          // 妆师作品点赞类型为7
        };
        let url = hasLike.value ? "/with-state/unlike" : "/with-state/add-like";
        uni.request({
          url: websiteUrl.value + url,
          method: "POST",
          header: {
            "Authorization": token
          },
          data: requestData,
          success: (res) => {
            if (res.data.status == "success") {
              uni.showToast({
                title: hasLike.value ? "取消点赞" : "点赞成功",
                icon: "success"
              });
              hasLike.value = !hasLike.value;
              if (hasLike.value) {
                detailData.value.like_count += 1;
              } else {
                detailData.value.like_count -= 1;
              }
            } else {
              uni.showToast({
                title: res.data.msg || "操作失败",
                icon: "none"
              });
            }
          },
          fail: (err) => {
            formatAppLog("log", "at pages/artwork/artwork.vue:404", err);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      }
      function getHasLike() {
        let token = uni.getStorageSync("token");
        if (!token) {
          return;
        }
        uni.request({
          url: websiteUrl.value + "/with-state/hasLike?id=" + pageId.value + "&type=7",
          // 类型7
          method: "POST",
          header: {
            "Authorization": token
          },
          success: (res) => {
            if (res.data.status == "success") {
              hasLike.value = res.data.data.id > 0;
            }
          }
        });
      }
      const handleReplyComment = ({
        parent,
        target
      }) => {
        var _a2;
        let item = parent;
        if (target != null) {
          item = target;
        }
        if (replyForItem.value.id == item.id) {
          replyForItem.value = {};
          return;
        }
        replyForItem.value = item;
        (_a2 = commentInputRef.value) == null ? void 0 : _a2.focusInput();
      };
      const handleCommentSubmit = (submitData) => {
        var _a2, _b, _c;
        let token = uni.getStorageSync("token");
        if (!global$1.isLogin) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        const requestData = {
          content: submitData.content,
          origin: submitData.origin,
          target_id: parseInt(pageId.value),
          type: 8,
          // 妆师作品评论类型为8
          image_url: submitData.image_url || "",
          association_id: submitData.association_id || 0,
          association_type: submitData.association_type || 0,
          is_anonymous: submitData.is_anonymous || 0,
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id
          }
        };
        const tempComment = {
          id: Date.now(),
          content: submitData.content,
          created_at: Math.floor(Date.now() / 1e3),
          like_count: 0,
          reply_count: 0,
          is_liked: false,
          floor: 0,
          ...submitData.is_anonymous ? {
            avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
            username: "匿名用户",
            is_anonymous: 1
          } : {
            avatar: global$1.userInfo.avatar,
            username: global$1.userInfo.nickname,
            is_anonymous: 0
          },
          ...submitData.image_url && {
            image_url: submitData.image_url
          },
          ...replyForItem.value.id && {
            reply_id: replyForItem.value.id,
            reply_for: replyForItem.value.comment,
            reply_uid: replyForItem.value.user_id,
            parent_id: replyForItem.value.parent_id > 0 ? replyForItem.value.parent_id : replyForItem.value.id,
            reply_username: replyForItem.value.is_anonymous ? "匿名用户" : replyForItem.value.username
          }
        };
        if (!replyForItem.value.id) {
          (_a2 = commentListRef.value) == null ? void 0 : _a2.addNewComment(tempComment);
        } else if (replyForItem.value.parent_id === 0) {
          (_b = commentListRef.value) == null ? void 0 : _b.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.id
          });
        } else {
          (_c = commentListRef.value) == null ? void 0 : _c.addReplyComment({
            ...tempComment,
            parent_id: replyForItem.value.parent_id
          });
        }
        uni.request({
          url: websiteUrl.value + "/with-state/add-comment",
          method: "POST",
          header: {
            "Authorization": token
          },
          data: requestData,
          success: (res) => {
            var _a3, _b2;
            if (res.data.status == "success") {
              const newComment = res.data.data;
              const finalComment = {
                ...newComment,
                ...submitData.is_anonymous ? {
                  avatar: "https://images1.fantuanpu.com/home/default_avatar.jpg",
                  username: "匿名用户",
                  is_anonymous: 1
                } : {
                  avatar: global$1.userInfo.avatar,
                  username: global$1.userInfo.nickname,
                  is_anonymous: 0
                }
              };
              if (newComment.reply_uid && replyForItem.value.is_anonymous) {
                finalComment.reply_username = "匿名用户";
              }
              (_a3 = commentListRef.value) == null ? void 0 : _a3.updateTempComment(tempComment.id, finalComment);
              uni.showToast({
                title: "评论成功",
                icon: "success"
              });
            } else {
              (_b2 = commentListRef.value) == null ? void 0 : _b2.removeTempComment(tempComment.id);
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
            }
          },
          fail: (err) => {
            var _a3;
            (_a3 = commentListRef.value) == null ? void 0 : _a3.removeTempComment(tempComment.id);
            uni.showToast({
              title: "网络请求失败",
              icon: "none"
            });
          }
        });
      };
      onLoad((options) => {
        if (!options.id) {
          handleError("缺少必要参数");
          return;
        }
        pageId.value = options.id;
        fetchData(options.id);
        asyncGetUserInfo().then(() => {
          getHasLike();
        });
      });
      const __returned__ = { detailData, goodsInfo, brandInfo, loading: loading2, error: error2, errorMsg, hasLike, pageId, commentType, commentListRef, commentInputRef, replyForItem, scrollTop, goBack, onShare, statusBarHeight, heartTopPx, formatNumber, getSexText, getArtTypeText, formatTimestamp, viewFullImage, fetchData, fetchGoodsInfo, fetchBrandInfo, handleError, navigateToGoods, navigateToBrand, likeFn, getHasLike, handleReplyComment, handleCommentSubmit, ref: vue.ref, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get global() {
        return global$1;
      }, get onLoad() {
        return onLoad;
      }, get onPageScroll() {
        return onPageScroll;
      }, get onShareAppMessage() {
        return onShareAppMessage;
      }, get onShareTimeline() {
        return onShareTimeline;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$G(_ctx, _cache, $props, $setup, $data, $options) {
    var _a, _b;
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_comment_list = resolveEasycom(vue.resolveDynamicComponent("comment-list"), __easycom_3$3);
    const _component_comment_input = resolveEasycom(vue.resolveDynamicComponent("comment-input"), __easycom_4$1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createVNode(_component_zhouWei_navBar, {
          type: "transparentFixed",
          backState: 2e3,
          homeState: 2e3,
          scrollTop: $setup.scrollTop,
          fontColor: "#000",
          transparentFixedFontColor: "#000",
          titleCenter: true
        }, {
          left: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-left" }, [
              vue.createElementVNode("view", {
                class: "nav-back-pill",
                onClick: $setup.goBack,
                "aria-label": "返回"
              }, [
                vue.createElementVNode("image", {
                  src: _imports_0$3,
                  mode: "aspectFill",
                  style: { "width": "18rpx", "height": "18rpx" }
                })
              ]),
              vue.createElementVNode("text", {
                class: "nav-back-text font-title",
                onClick: $setup.goBack
              }, "返回")
            ])
          ]),
          transparentFixedLeft: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-left" }, [
              vue.createElementVNode("view", {
                class: "nav-back-pill",
                onClick: $setup.goBack,
                "aria-label": "返回"
              }, [
                vue.createElementVNode("image", {
                  src: _imports_0$3,
                  mode: "aspectFill",
                  style: { "width": "18rpx", "height": "18rpx" }
                })
              ]),
              vue.createElementVNode("text", {
                class: "nav-back-text font-title",
                onClick: $setup.goBack
              }, "返回")
            ])
          ]),
          right: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-right" }, [
              vue.createElementVNode("view", {
                class: "nav-share-pill",
                onClick: $setup.onShare,
                "aria-label": "分享"
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "redo",
                  size: "22",
                  color: "#000"
                })
              ])
            ])
          ]),
          transparentFixedRight: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-right" }, [
              vue.createElementVNode("view", {
                class: "nav-share-pill",
                onClick: $setup.onShare,
                "aria-label": "分享"
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "redo",
                  size: "22",
                  color: "#000"
                })
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["scrollTop"]),
        vue.createElementVNode("view", null, [
          vue.createElementVNode("view", { style: { "position": "relative" } }, [
            vue.createElementVNode("view", {
              class: "heart",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.likeFn())
            }, [
              !$setup.hasLike ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                key: 0,
                type: "heart",
                size: "28",
                color: "#ff4d4f"
              })) : (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                key: 1,
                type: "heart-filled",
                size: "28",
                color: "#ff4d4f"
              })),
              vue.createElementVNode(
                "text",
                { class: "num" },
                vue.toDisplayString(((_a = $setup.detailData) == null ? void 0 : _a.like_count) ? $setup.formatNumber($setup.detailData.like_count) : 0),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("swiper", {
              class: "swiper-box",
              "indicator-dots": true,
              autoplay: false
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.detailData.images, (img, index) => {
                  return vue.openBlock(), vue.createElementBlock("swiper-item", { key: index }, [
                    vue.createElementVNode("image", {
                      src: img,
                      mode: "aspectFill",
                      class: "swiper-image",
                      onClick: ($event) => $setup.viewFullImage(index)
                    }, null, 8, ["src", "onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ]),
          $setup.brandInfo ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "author-info",
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.navigateToBrand($setup.detailData.brand_id))
          }, [
            vue.createElementVNode("image", {
              src: $setup.brandInfo.logo_image,
              mode: "aspectFill",
              class: "author-avatar"
            }, null, 8, ["src"]),
            vue.createElementVNode("view", { class: "author-details" }, [
              vue.createElementVNode(
                "text",
                { class: "author-name" },
                vue.toDisplayString($setup.brandInfo.brand_name),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                { class: "publish-time" },
                "发布于 " + vue.toDisplayString($setup.formatTimestamp($setup.detailData.created_at)),
                1
                /* TEXT */
              )
            ])
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { class: "content-box" }, [
            vue.createElementVNode(
              "text",
              { class: "title" },
              vue.toDisplayString($setup.detailData.title),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "text",
              { class: "head-name" },
              "头名: " + vue.toDisplayString($setup.detailData.head_name),
              1
              /* TEXT */
            ),
            vue.createElementVNode("view", { class: "meta-info" }, [
              vue.createElementVNode(
                "text",
                { class: "meta-item" },
                "性别: " + vue.toDisplayString($setup.getSexText($setup.detailData.sex)),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                { class: "meta-item" },
                "类型: " + vue.toDisplayString($setup.getArtTypeText($setup.detailData.art_type)),
                1
                /* TEXT */
              )
            ]),
            $setup.detailData.faceup_tag_content && $setup.detailData.faceup_tag_content.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "tags-container"
            }, [
              vue.createElementVNode("text", { class: "tags-title" }, "风格:"),
              vue.createElementVNode("view", { class: "tags-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.detailData.faceup_tag_content, (tag, index) => {
                    return vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: index,
                        class: "tag-item"
                      },
                      vue.toDisplayString(tag),
                      1
                      /* TEXT */
                    );
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          $setup.detailData.goods_id && $setup.goodsInfo ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "goods-section"
          }, [
            vue.createElementVNode("text", { class: "section-title" }, "关联商品"),
            vue.createElementVNode("view", {
              class: "goods-item",
              onClick: _cache[2] || (_cache[2] = ($event) => $setup.navigateToGoods($setup.detailData.goods_id))
            }, [
              vue.createElementVNode("view", { class: "image-container" }, [
                vue.createElementVNode("image", {
                  src: ((_b = $setup.goodsInfo.goods_images) == null ? void 0 : _b[0]) || "/static/goods-default.png",
                  mode: "aspectFill",
                  class: "goods-image"
                }, null, 8, ["src"])
              ]),
              vue.createElementVNode("view", { class: "goods-info" }, [
                vue.createElementVNode(
                  "text",
                  { class: "goods-name" },
                  vue.toDisplayString($setup.goodsInfo.name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "goods-brand" },
                  vue.toDisplayString($setup.goodsInfo.brand_name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("view", { class: "see" }, "去看看 →")
              ])
            ])
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { style: { "padding": "10px" } }, [
            vue.createVNode(_component_comment_list, {
              ref: "commentListRef",
              type: $setup.commentType,
              "relation-id": parseInt($setup.pageId),
              onReply: $setup.handleReplyComment
            }, null, 8, ["type", "relation-id"])
          ]),
          vue.createVNode(_component_comment_input, {
            ref: "commentInputRef",
            "reply-info": $setup.replyForItem,
            "target-id": $setup.pageId,
            onSubmit: $setup.handleCommentSubmit,
            "onUpdate:replyInfo": _cache[3] || (_cache[3] = (val) => $setup.replyForItem = val)
          }, null, 8, ["reply-info", "target-id"]),
          vue.createElementVNode("view", { style: { "width": "100%", "height": "120rpx" } }),
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "loading"
          }, "加载中...")) : vue.createCommentVNode("v-if", true),
          $setup.error ? (vue.openBlock(), vue.createElementBlock(
            "view",
            {
              key: 3,
              class: "error"
            },
            vue.toDisplayString($setup.errorMsg),
            1
            /* TEXT */
          )) : vue.createCommentVNode("v-if", true)
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesArtworkArtwork = /* @__PURE__ */ _export_sfc(_sfc_main$H, [["render", _sfc_render$G], ["__scopeId", "data-v-0583fe19"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/artwork/artwork.vue"]]);
  const pageSize$3 = 12;
  const defaultImg = "https://images1.fantuanpu.com/box/admin/ee5e6c79b2bee5efb4b53ab89e8d9f62";
  const _sfc_main$G = {
    __name: "recent-feed",
    setup(__props, { expose: __expose }) {
      __expose();
      const hours = vue.ref(168);
      const items = vue.ref([]);
      const page = vue.ref(1);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const refreshing = vue.ref(false);
      const noMore = vue.ref(false);
      const lastUpdated = vue.ref(0);
      const showToTop = vue.ref(false);
      const safeTop = vue.ref(0);
      async function fetchFeed(reset = false) {
        var _a;
        if (loading2.value)
          return;
        if (reset) {
          page.value = 1;
          noMore.value = false;
          items.value = [];
        }
        loading2.value = true;
        try {
          const url = `${websiteUrl.value}/ops-feed/recent?hours=${hours.value}&page=${page.value}&page_size=${pageSize$3}`;
          const res = await uni.request({ url, method: "GET" });
          const data = ((_a = res.data) == null ? void 0 : _a.data) || {};
          const list = data.list || [];
          items.value = page.value === 1 ? list : [...items.value, ...list];
          total.value = data.total || 0;
          noMore.value = items.value.length >= total.value || list.length < pageSize$3;
          if (!noMore.value)
            page.value += 1;
          lastUpdated.value = Date.now();
        } catch (e2) {
          uni.showToast({ title: "加载失败", icon: "none" });
        } finally {
          loading2.value = false;
          refreshing.value = false;
        }
      }
      function onRefresh() {
        if (!refreshing.value) {
          refreshing.value = true;
          fetchFeed(true);
        }
      }
      function loadMore() {
        if (!loading2.value && !noMore.value)
          fetchFeed();
      }
      function manualRefresh() {
        if (!loading2.value)
          onRefresh();
      }
      const lastUpdatedText = vue.computed(() => {
        if (!lastUpdated.value)
          return "刚刚";
        const diff = Math.floor((Date.now() - lastUpdated.value) / 1e3);
        if (diff < 10)
          return "已更新";
        if (diff < 60)
          return `${diff}s前`;
        const m2 = Math.floor(diff / 60);
        if (m2 < 60)
          return `${m2}分钟前`;
        const h2 = Math.floor(m2 / 60);
        return `${h2}小时前`;
      });
      function formatTime(tsSec) {
        const d2 = new Date((tsSec || 0) * 1e3);
        if (isNaN(d2.getTime()))
          return "";
        const y2 = d2.getFullYear(), mm = String(d2.getMonth() + 1).padStart(2, "0"), dd = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0"), mi = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${mm}-${dd} ${hh}:${mi}`;
      }
      function badgeStyle(item) {
        const act = Number(item.action);
        if (act === 1)
          return { text: "新增", icon: "plus", bg: "linear-gradient(135deg,#ff9d6c,#ff6b8b)" };
        if (act === 3)
          return { text: "删除", icon: "trash", bg: "linear-gradient(135deg,#ff8e8e,#ff5e7d)" };
        const t2 = Number(item.op_type);
        const bg = t2 === 1 ? "linear-gradient(135deg,#6bc5f8,#9c6bff)" : t2 === 3 ? "linear-gradient(135deg,#ffd166,#ff9d6c)" : "linear-gradient(135deg,#6bc5f8,#9c6bff)";
        return { text: "更新", icon: "compose", bg };
      }
      function openGoods(item) {
        if (+item.goods_id > 0)
          uni.navigateTo({ url: `/pages/goods/goods?goods_id=${item.goods_id}` });
      }
      function openBrand(item) {
        if (+item.brand_id > 0)
          uni.navigateTo({ url: `/pages/brand/brand?brand_id=${item.brand_id}` });
      }
      function handleOpen(item) {
        +item.goods_id > 0 ? openGoods(item) : +item.brand_id > 0 && openBrand(item);
      }
      function openActions(item) {
        uni.showActionSheet({
          itemList: ["查看商品", "查看品牌"],
          success: (r2) => {
            r2.tapIndex === 0 ? openGoods(item) : openBrand(item);
          }
        });
      }
      function scrollToTop() {
        uni.pageScrollTo({ scrollTop: 0, duration: 200 });
      }
      vue.onMounted(() => {
        var _a, _b;
        try {
          const wi = uni.getWindowInfo && uni.getWindowInfo();
          safeTop.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.top) ?? (wi == null ? void 0 : wi.statusBarHeight) ?? 0;
          if (!safeTop.value) {
            const si = uni.getSystemInfoSync();
            safeTop.value = ((_b = si == null ? void 0 : si.safeAreaInsets) == null ? void 0 : _b.top) ?? (si == null ? void 0 : si.statusBarHeight) ?? 0;
          }
        } catch {
          safeTop.value = 20;
        }
        fetchFeed(true);
      });
      uni.setNavigationBarTitle({ title: "上新动态" });
      const __returned__ = { hours, items, page, pageSize: pageSize$3, total, loading: loading2, refreshing, noMore, lastUpdated, showToTop, defaultImg, safeTop, fetchFeed, onRefresh, loadMore, manualRefresh, lastUpdatedText, formatTime, badgeStyle, openGoods, openBrand, handleOpen, openActions, scrollToTop, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$F(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: "page",
        style: vue.normalizeStyle({ "--safe-top": $setup.safeTop + "px" })
      },
      [
        vue.createElementVNode("meta", {
          name: "theme-color",
          content: "#f8f9ff"
        }),
        vue.createElementVNode("view", { class: "pastel-bg" }),
        vue.createElementVNode("view", { class: "top-tools" }),
        vue.createElementVNode("scroll-view", {
          class: "list",
          "scroll-y": "",
          "refresher-enabled": true,
          "refresher-triggered": $setup.refreshing,
          "refresher-background": "#f8f9ff",
          "refresher-default-style": "black",
          onRefresherrefresh: $setup.onRefresh,
          onRefresherrestore: _cache[0] || (_cache[0] = () => $setup.refreshing = false),
          onRefresherabort: _cache[1] || (_cache[1] = () => $setup.refreshing = false),
          onScrolltolower: $setup.loadMore,
          "show-scrollbar": false
        }, [
          $setup.loading && $setup.items.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "skeleton-wrap"
          }, [
            (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList(6, (n2) => {
                return vue.createElementVNode("view", {
                  key: "sk-" + n2,
                  class: "card skeleton"
                }, [
                  vue.createElementVNode("view", { class: "left" }, [
                    vue.createElementVNode("view", { class: "img sk" })
                  ]),
                  vue.createElementVNode("view", { class: "right" }, [
                    vue.createElementVNode("view", {
                      class: "line sk",
                      style: { "width": "60%" }
                    }),
                    vue.createElementVNode("view", {
                      class: "line sk",
                      style: { "width": "80%" }
                    }),
                    vue.createElementVNode("view", { class: "chip-row" }, [
                      vue.createElementVNode("view", {
                        class: "chip sk",
                        style: { "width": "120rpx" }
                      }),
                      vue.createElementVNode("view", {
                        class: "chip sk",
                        style: { "width": "160rpx" }
                      })
                    ]),
                    vue.createElementVNode("view", {
                      class: "time sk",
                      style: { "width": "180rpx" }
                    })
                  ])
                ]);
              }),
              64
              /* STABLE_FRAGMENT */
            ))
          ])) : !$setup.loading && $setup.items.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              class: "empty-img",
              src: "https://images1.fantuanpu.com/box/admin/4b0fae8e4f074a1f9d9f5b3d3da8e9c3"
            }),
            vue.createElementVNode("text", { class: "empty-title" }, "暂时还没有动态"),
            vue.createElementVNode("text", { class: "empty-sub" }, "已固定为最近 7 天")
          ])) : (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "cards"
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.items, (item) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "card",
                  onClick: ($event) => $setup.handleOpen(item)
                }, [
                  vue.createElementVNode("view", { class: "left" }, [
                    vue.createElementVNode("image", {
                      class: "img",
                      src: item.image_url || $setup.defaultImg,
                      mode: "aspectFill",
                      "lazy-load": ""
                    }, null, 8, ["src"]),
                    vue.createElementVNode(
                      "view",
                      {
                        class: "badge",
                        style: vue.normalizeStyle({ background: $setup.badgeStyle(item).bg })
                      },
                      [
                        vue.createVNode(_component_uni_icons, {
                          type: $setup.badgeStyle(item).icon,
                          size: "14",
                          color: "#fff"
                        }, null, 8, ["type"]),
                        vue.createElementVNode(
                          "text",
                          null,
                          vue.toDisplayString($setup.badgeStyle(item).text),
                          1
                          /* TEXT */
                        )
                      ],
                      4
                      /* STYLE */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "right" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "summary" },
                      vue.toDisplayString(item.summary),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("view", { class: "chip-row" }, [
                      item.brand_name ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "chip chip-brand",
                        onClick: vue.withModifiers(($event) => $setup.openBrand(item), ["stop"])
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "shop",
                          size: "16",
                          color: "#ff6b8b"
                        }),
                        vue.createElementVNode(
                          "text",
                          null,
                          vue.toDisplayString(item.brand_name),
                          1
                          /* TEXT */
                        )
                      ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true),
                      item.goods_name ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "chip chip-goods",
                        onClick: vue.withModifiers(($event) => $setup.openGoods(item), ["stop"])
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "gift",
                          size: "16",
                          color: "#9c6bff"
                        }),
                        vue.createElementVNode(
                          "text",
                          null,
                          vue.toDisplayString(item.goods_name),
                          1
                          /* TEXT */
                        )
                      ], 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
                    ]),
                    vue.createElementVNode("view", { class: "meta-row" }, [
                      vue.createElementVNode("view", { class: "time" }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "time",
                          size: "16",
                          color: "#ff9d6c"
                        }),
                        vue.createElementVNode(
                          "text",
                          { class: "time-text" },
                          vue.toDisplayString(item.time_ago || $setup.formatTime(item.created_at)),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", {
                        class: "more",
                        onClick: vue.withModifiers(($event) => $setup.openActions(item), ["stop"])
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "more",
                          size: "18",
                          color: "#ff9d6c"
                        })
                      ], 8, ["onClick"])
                    ])
                  ])
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            vue.createElementVNode("view", { class: "loading-state" }, [
              $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "loading-spinner"
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "spinner-cycle",
                  size: "24",
                  color: "#ff6b8b"
                }),
                vue.createElementVNode("text", null, "加载中...")
              ])) : $setup.noMore ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 1,
                class: "no-more"
              }, "没有更多了")) : vue.createCommentVNode("v-if", true)
            ])
          ]))
        ], 40, ["refresher-triggered"]),
        $setup.showToTop ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "to-top",
          onClick: $setup.scrollToTop
        }, [
          vue.createVNode(_component_uni_icons, {
            type: "arrow-up",
            size: "20",
            color: "#fff"
          })
        ])) : vue.createCommentVNode("v-if", true)
      ],
      4
      /* STYLE */
    );
  }
  const PagesRecentFeedRecentFeed = /* @__PURE__ */ _export_sfc(_sfc_main$G, [["render", _sfc_render$F], ["__scopeId", "data-v-931bc155"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/recent-feed/recent-feed.vue"]]);
  const _sfc_main$F = {
    name: "UniStatusBar",
    data() {
      return {
        statusBarHeight: uni.getSystemInfoSync().statusBarHeight + "px"
      };
    }
  };
  function _sfc_render$E(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        style: vue.normalizeStyle({ height: $data.statusBarHeight }),
        class: "uni-status-bar"
      },
      [
        vue.renderSlot(_ctx.$slots, "default", {}, void 0, true)
      ],
      4
      /* STYLE */
    );
  }
  const statusBar = /* @__PURE__ */ _export_sfc(_sfc_main$F, [["render", _sfc_render$E], ["__scopeId", "data-v-7920e3e0"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-nav-bar/components/uni-nav-bar/uni-status-bar.vue"]]);
  const getVal = (val) => typeof val === "number" ? val + "px" : val;
  const _sfc_main$E = {
    name: "UniNavBar",
    components: {
      statusBar
    },
    emits: ["clickLeft", "clickRight", "clickTitle"],
    props: {
      dark: {
        type: Boolean,
        default: false
      },
      title: {
        type: String,
        default: ""
      },
      leftText: {
        type: String,
        default: ""
      },
      rightText: {
        type: String,
        default: ""
      },
      leftIcon: {
        type: String,
        default: ""
      },
      rightIcon: {
        type: String,
        default: ""
      },
      fixed: {
        type: [Boolean, String],
        default: false
      },
      color: {
        type: String,
        default: ""
      },
      backgroundColor: {
        type: String,
        default: ""
      },
      statusBar: {
        type: [Boolean, String],
        default: false
      },
      shadow: {
        type: [Boolean, String],
        default: false
      },
      border: {
        type: [Boolean, String],
        default: true
      },
      height: {
        type: [Number, String],
        default: 44
      },
      leftWidth: {
        type: [Number, String],
        default: 60
      },
      rightWidth: {
        type: [Number, String],
        default: 60
      },
      showMenuButtonWidth: {
        type: Boolean,
        default: false
      },
      stat: {
        type: [Boolean, String],
        default: ""
      }
    },
    data() {
      return {
        navWidth: "auto"
      };
    },
    computed: {
      themeBgColor() {
        if (this.dark) {
          if (this.backgroundColor) {
            return this.backgroundColor;
          } else {
            return this.dark ? "#333" : "#FFF";
          }
        }
        return this.backgroundColor || "#FFF";
      },
      themeColor() {
        if (this.dark) {
          if (this.color) {
            return this.color;
          } else {
            return this.dark ? "#fff" : "#333";
          }
        }
        return this.color || "#333";
      },
      navbarHeight() {
        if (this.fixed && this.height === 0) {
          return getVal(44);
        }
        return getVal(this.height);
      },
      leftIconWidth() {
        return getVal(this.leftWidth);
      },
      rightIconWidth() {
        return getVal(this.rightWidth);
      }
    },
    created() {
    },
    mounted() {
      if (uni.report && this.stat && this.title !== "") {
        uni.report("title", this.title);
      }
    },
    methods: {
      onClickLeft() {
        this.$emit("clickLeft");
      },
      onClickRight() {
        this.$emit("clickRight");
      },
      onClickTitle() {
        this.$emit("clickTitle");
      }
    }
  };
  function _sfc_render$D(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_status_bar = vue.resolveComponent("status-bar");
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: vue.normalizeClass(["uni-navbar", { "uni-dark": $props.dark, "uni-nvue-fixed": $props.fixed }])
      },
      [
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["uni-navbar__content", { "uni-navbar--fixed": $props.fixed, "uni-navbar--shadow": $props.shadow, "uni-navbar--border": $props.border }]),
            style: vue.normalizeStyle({ "background-color": $options.themeBgColor })
          },
          [
            $props.statusBar ? (vue.openBlock(), vue.createBlock(_component_status_bar, { key: 0 })) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode(
              "view",
              {
                style: vue.normalizeStyle({ color: $options.themeColor, backgroundColor: $options.themeBgColor, height: $options.navbarHeight, width: $props.showMenuButtonWidth ? $data.navWidth + "px" : "100%" }),
                class: "uni-navbar__header"
              },
              [
                vue.createElementVNode(
                  "view",
                  {
                    onClick: _cache[0] || (_cache[0] = (...args) => $options.onClickLeft && $options.onClickLeft(...args)),
                    class: "uni-navbar__header-btns uni-navbar__header-btns-left",
                    style: vue.normalizeStyle({ width: $options.leftIconWidth })
                  },
                  [
                    vue.renderSlot(_ctx.$slots, "left", {}, () => [
                      $props.leftIcon.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "uni-navbar__content_view"
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          color: $options.themeColor,
                          type: $props.leftIcon,
                          size: "20"
                        }, null, 8, ["color", "type"])
                      ])) : vue.createCommentVNode("v-if", true),
                      $props.leftText.length ? (vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: 1,
                          class: vue.normalizeClass([{ "uni-navbar-btn-icon-left": !$props.leftIcon.length > 0 }, "uni-navbar-btn-text"])
                        },
                        [
                          vue.createElementVNode(
                            "text",
                            {
                              style: vue.normalizeStyle({ color: $options.themeColor, fontSize: "12px" })
                            },
                            vue.toDisplayString($props.leftText),
                            5
                            /* TEXT, STYLE */
                          )
                        ],
                        2
                        /* CLASS */
                      )) : vue.createCommentVNode("v-if", true)
                    ], true)
                  ],
                  4
                  /* STYLE */
                ),
                vue.createElementVNode("view", {
                  class: "uni-navbar__header-container",
                  onClick: _cache[1] || (_cache[1] = (...args) => $options.onClickTitle && $options.onClickTitle(...args))
                }, [
                  vue.renderSlot(_ctx.$slots, "default", {}, () => [
                    $props.title.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "uni-navbar__header-container-inner"
                    }, [
                      vue.createElementVNode(
                        "text",
                        {
                          class: "uni-nav-bar-text uni-ellipsis-1",
                          style: vue.normalizeStyle({ color: $options.themeColor })
                        },
                        vue.toDisplayString($props.title),
                        5
                        /* TEXT, STYLE */
                      )
                    ])) : vue.createCommentVNode("v-if", true)
                  ], true)
                ]),
                vue.createElementVNode(
                  "view",
                  {
                    onClick: _cache[2] || (_cache[2] = (...args) => $options.onClickRight && $options.onClickRight(...args)),
                    class: "uni-navbar__header-btns uni-navbar__header-btns-right",
                    style: vue.normalizeStyle({ width: $options.rightIconWidth })
                  },
                  [
                    vue.renderSlot(_ctx.$slots, "right", {}, () => [
                      $props.rightIcon.length ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                        vue.createVNode(_component_uni_icons, {
                          color: $options.themeColor,
                          type: $props.rightIcon,
                          size: "22"
                        }, null, 8, ["color", "type"])
                      ])) : vue.createCommentVNode("v-if", true),
                      $props.rightText.length && !$props.rightIcon.length ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "uni-navbar-btn-text"
                      }, [
                        vue.createElementVNode(
                          "text",
                          {
                            class: "uni-nav-bar-right-text",
                            style: vue.normalizeStyle({ color: $options.themeColor })
                          },
                          vue.toDisplayString($props.rightText),
                          5
                          /* TEXT, STYLE */
                        )
                      ])) : vue.createCommentVNode("v-if", true)
                    ], true)
                  ],
                  4
                  /* STYLE */
                )
              ],
              4
              /* STYLE */
            )
          ],
          6
          /* CLASS, STYLE */
        ),
        $props.fixed ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "uni-navbar__placeholder"
        }, [
          $props.statusBar ? (vue.openBlock(), vue.createBlock(_component_status_bar, { key: 0 })) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode(
            "view",
            {
              class: "uni-navbar__placeholder-view",
              style: vue.normalizeStyle({ height: $options.navbarHeight })
            },
            null,
            4
            /* STYLE */
          )
        ])) : vue.createCommentVNode("v-if", true)
      ],
      2
      /* CLASS */
    );
  }
  const __easycom_1$1 = /* @__PURE__ */ _export_sfc(_sfc_main$E, [["render", _sfc_render$D], ["__scopeId", "data-v-26544265"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-nav-bar/components/uni-nav-bar/uni-nav-bar.vue"]]);
  const NAV_BAR_BODY = 44;
  const MAX_HISTORY = 50;
  const _sfc_main$D = {
    __name: "search",
    setup(__props, { expose: __expose }) {
      var _a;
      __expose();
      const TAB_MAP = {
        1: { key: 1, label: "娃物", type: "goods", index: "" },
        2: { key: 2, label: "店铺", type: "brand", index: "" },
        3: { key: 3, label: "妆师", type: "artist", index: "bjd_artist" },
        4: { key: 4, label: "毛娘", type: "hair", index: "hairstylist" }
      };
      const statusBar2 = ((_a = uni.getSystemInfoSync()) == null ? void 0 : _a.statusBarHeight) || 0;
      const navHeight = statusBar2 + NAV_BAR_BODY;
      const routeMode = vue.ref("jump");
      const orderedKeys = vue.ref([1, 2, 3, 4]);
      const tabs = vue.computed(
        () => orderedKeys.value.map((k) => TAB_MAP[k]).filter(Boolean)
      );
      const activeKey = vue.ref(orderedKeys.value[0]);
      const activeType = vue.computed(() => TAB_MAP[activeKey.value].type);
      const activeLabel = vue.computed(() => TAB_MAP[activeKey.value].label);
      const activeIndexParam = vue.computed(() => TAB_MAP[activeKey.value].index);
      const kw = vue.ref("");
      const results = vue.ref([]);
      const showResults = vue.ref(false);
      const inputFocused = vue.ref(false);
      let reqLock = false;
      const placeholder = vue.computed(
        () => activeType.value === "goods" ? "搜索娃物..." : `搜索${activeLabel.value}（可拼音/别名）`
      );
      const chipsRef = vue.ref(null);
      const collapsed = vue.ref(true);
      const needMask = vue.ref(false);
      const needFoldBtn = vue.ref(false);
      const historyList = vue.ref([]);
      const STORAGE_KEY = vue.computed(() => `search_history_${activeKey.value}`);
      function loadHistory() {
        try {
          const raw = uni.getStorageSync(STORAGE_KEY.value);
          historyList.value = Array.isArray(raw) ? raw.slice(0, MAX_HISTORY) : [];
        } catch {
          historyList.value = [];
        }
        vue.nextTick(measureFold);
      }
      function saveHistory() {
        try {
          uni.setStorageSync(
            STORAGE_KEY.value,
            historyList.value.slice(0, MAX_HISTORY)
          );
        } catch {
        }
      }
      function pushHistory(name, payload) {
        const key = String((payload == null ? void 0 : payload.id) || name || "");
        historyList.value = [
          { key, name, payload },
          ...historyList.value.filter((x) => x.key !== key)
        ].slice(0, MAX_HISTORY);
        saveHistory();
        vue.nextTick(measureFold);
      }
      function clearAll() {
        historyList.value = [];
        saveHistory();
        vue.nextTick(measureFold);
      }
      function removeOne(i2) {
        historyList.value.splice(i2, 1);
        saveHistory();
        vue.nextTick(measureFold);
      }
      function tapHistory(w2) {
        kw.value = w2.name || "";
        if (routeMode.value === "return") {
          choose(w2.payload || { id: w2.key, name: w2.name });
          return;
        }
        inputFocused.value = true;
        onInput();
      }
      const rpx2px2 = (r2) => (uni.getSystemInfoSync().windowWidth || 375) / 750 * r2;
      async function measureFold() {
        await vue.nextTick();
        uni.createSelectorQuery().select(".chips").boundingClientRect((rect) => {
          const fiveLines = rpx2px2(220);
          needFoldBtn.value = !!rect && rect.height > fiveLines;
          needMask.value = collapsed.value && needFoldBtn.value;
        }).exec();
      }
      function toggleFold() {
        collapsed.value = !collapsed.value;
        needMask.value = collapsed.value && needFoldBtn.value;
      }
      function onFocus() {
        inputFocused.value = true;
        if (results.value.length)
          showResults.value = true;
      }
      function clearInput() {
        kw.value = "";
        results.value = [];
        showResults.value = false;
      }
      function closeResults() {
        showResults.value = false;
      }
      async function onInput() {
        const q2 = kw.value.trim();
        if (!q2) {
          results.value = [];
          showResults.value = false;
          return;
        }
        showResults.value = true;
        if (reqLock)
          return;
        reqLock = true;
        try {
          if (activeType.value === "goods") {
            await fetchGoods(q2);
          } else {
            await fetchIndex(q2, activeIndexParam.value);
          }
        } finally {
          reqLock = false;
        }
      }
      async function onSubmit() {
        if (results.value[0])
          choose(results.value[0]);
      }
      async function fetchGoods(q2) {
        var _a2;
        const url = `${websiteUrl.value}/search-goods?search=${encodeURIComponent(
          q2
        )}`;
        const res = await uni.request({ url, method: "GET" });
        results.value = ((_a2 = res.data) == null ? void 0 : _a2.status) === "success" ? res.data.data || [] : [];
      }
      async function fetchIndex(q2, indexVal) {
        var _a2;
        let url = `${websiteUrl.value}/search-brand?search=${encodeURIComponent(
          q2
        )}`;
        if (indexVal)
          url += `&index=${indexVal}`;
        const res = await uni.request({ url, method: "GET" });
        results.value = ((_a2 = res.data) == null ? void 0 : _a2.status) === "success" ? res.data.data || [] : [];
      }
      function choose(item) {
        pushHistory(item.name, {
          id: item.id,
          name: item.name,
          brand_name: item.brand_name
        });
        if (routeMode.value === "jump") {
          if (activeType.value === "goods") {
            uni.navigateTo({
              url: `/pages/goods/goods?goods_id=${item.id}`
            });
          } else {
            uni.navigateTo({
              url: `/pages/brand/brand?brand_id=${item.id}`
            });
          }
        } else {
          const payload = {
            tabKey: activeKey.value,
            tabLabel: activeLabel.value,
            id: item.id,
            name: item.name,
            brand_name: item.brand_name || ""
          };
          const eventChannel = getOpenerEventChannel == null ? void 0 : getOpenerEventChannel();
          if (eventChannel && eventChannel.emit) {
            eventChannel.emit("search-selected", payload);
          } else {
            uni.$emit("search-selected", payload);
          }
          uni.navigateBack();
        }
        results.value = [];
        showResults.value = false;
      }
      function switchTab(k) {
        if (activeKey.value === k)
          return;
        activeKey.value = k;
        kw.value = "";
        results.value = [];
        showResults.value = false;
        collapsed.value = true;
        loadHistory();
      }
      function goBack() {
        uni.navigateBack({ delta: 1 });
      }
      onLoad((opt) => {
        const m2 = ((opt == null ? void 0 : opt.mode) || "").toLowerCase();
        if (m2 === "return")
          routeMode.value = "return";
        const raw = opt == null ? void 0 : opt.tabs;
        if (typeof raw === "string" && raw.trim()) {
          const keys = raw.split(",").map((s2) => parseInt(s2.trim(), 10)).filter((k) => TAB_MAP[k]);
          if (keys.length)
            orderedKeys.value = keys;
        }
        activeKey.value = orderedKeys.value[0] || 1;
        loadHistory();
      });
      vue.onMounted(() => {
        vue.nextTick(measureFold);
      });
      const __returned__ = { TAB_MAP, statusBar: statusBar2, NAV_BAR_BODY, navHeight, routeMode, orderedKeys, tabs, activeKey, activeType, activeLabel, activeIndexParam, kw, results, showResults, inputFocused, get reqLock() {
        return reqLock;
      }, set reqLock(v2) {
        reqLock = v2;
      }, placeholder, chipsRef, MAX_HISTORY, collapsed, needMask, needFoldBtn, historyList, STORAGE_KEY, loadHistory, saveHistory, pushHistory, clearAll, removeOne, tapHistory, rpx2px: rpx2px2, measureFold, toggleFold, onFocus, clearInput, closeResults, onInput, onSubmit, fetchGoods, fetchIndex, choose, switchTab, goBack, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, nextTick: vue.nextTick, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$C(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_nav_bar = resolveEasycom(vue.resolveDynamicComponent("uni-nav-bar"), __easycom_1$1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_uni_nav_bar, {
          fixed: "",
          statusBar: true,
          border: false,
          backgroundColor: "#def9ff",
          color: "#111"
        }, {
          left: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-left",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#111"
              })
            ])
          ]),
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-center" }, [
              vue.createElementVNode("view", { class: "nav-tabs" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.tabs, (t2) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: t2.key,
                      class: vue.normalizeClass(["nav-tab", { active: t2.key === $setup.activeKey }]),
                      onClick: ($event) => $setup.switchTab(t2.key)
                    }, [
                      vue.createElementVNode(
                        "text",
                        { class: "nav-tab-text" },
                        vue.toDisplayString(t2.label),
                        1
                        /* TEXT */
                      )
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        }),
        vue.createElementVNode("view", { class: "page" }, [
          vue.createElementVNode("view", { class: "sticky-search" }, [
            vue.createElementVNode("view", { class: "search-box" }, [
              vue.createElementVNode("image", {
                class: "icon",
                src: _imports_0$g
              }),
              vue.withDirectives(vue.createElementVNode("input", {
                class: "input",
                placeholder: $setup.placeholder,
                "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.kw = $event),
                "confirm-type": "search",
                focus: $setup.inputFocused,
                onInput: $setup.onInput,
                onConfirm: $setup.onSubmit,
                onFocus: $setup.onFocus
              }, null, 40, ["placeholder", "focus"]), [
                [vue.vModelText, $setup.kw]
              ]),
              $setup.kw ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 0,
                class: "icon",
                src: _imports_2$5,
                onClick: $setup.clearInput
              })) : vue.createCommentVNode("v-if", true)
            ]),
            $setup.showResults ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "mask",
              onClick: $setup.closeResults
            })) : vue.createCommentVNode("v-if", true),
            $setup.results.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
              key: 1,
              class: "results",
              style: { width: "100%" },
              "scroll-y": ""
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.results, (item) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "result",
                    key: item.id,
                    onClick: ($event) => $setup.choose(item)
                  }, [
                    $setup.activeType === "goods" && item.brand_name ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        class: "tag"
                      },
                      vue.toDisplayString(item.brand_name),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode(
                      "text",
                      { class: "name" },
                      vue.toDisplayString(item.name),
                      1
                      /* TEXT */
                    )
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              )),
              vue.createElementVNode("view", {
                class: "result close",
                onClick: $setup.closeResults
              }, "关闭联想")
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          $setup.historyList.length ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "history"
          }, [
            vue.createElementVNode("view", { class: "history-head" }, [
              vue.createElementVNode("text", { class: "title" }, "历史搜索"),
              vue.createElementVNode("text", {
                class: "clear",
                onClick: $setup.clearAll
              }, "清空")
            ]),
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["chips", { collapsed: $setup.collapsed }]),
                ref: "chipsRef"
              },
              [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.historyList, (w2, i2) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      class: "chip",
                      key: w2.key,
                      onClick: ($event) => $setup.tapHistory(w2),
                      onLongpress: ($event) => $setup.removeOne(i2)
                    }, vue.toDisplayString(w2.name), 41, ["onClick", "onLongpress"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                )),
                $setup.needMask ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "chips-mask"
                })) : vue.createCommentVNode("v-if", true)
              ],
              2
              /* CLASS */
            ),
            $setup.needFoldBtn ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "fold"
            }, [
              vue.createElementVNode(
                "text",
                {
                  class: "btn",
                  onClick: $setup.toggleFold
                },
                vue.toDisplayString($setup.collapsed ? "展开" : "收起"),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { style: { "height": "40rpx" } })
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PagesSearchSearch = /* @__PURE__ */ _export_sfc(_sfc_main$D, [["render", _sfc_render$C], ["__scopeId", "data-v-c10c040c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/search/search.vue"]]);
  const _sfc_main$C = {
    __name: "common-share",
    emits: ["click"],
    setup(__props, { expose: __expose }) {
      __expose();
      const __returned__ = {};
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$B(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", {
      class: "nav-right",
      onClick: _cache[0] || (_cache[0] = ($event) => _ctx.$emit("click"))
    }, [
      vue.renderSlot(_ctx.$slots, "default", {}, () => [
        vue.createElementVNode("view", { class: "nav-share-pill" }, [
          vue.createVNode(_component_uni_icons, {
            type: "redo",
            size: "22",
            color: "#000"
          })
        ])
      ], true)
    ]);
  }
  const __easycom_0$1 = /* @__PURE__ */ _export_sfc(_sfc_main$C, [["render", _sfc_render$B], ["__scopeId", "data-v-ef5e67d9"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-share/common-share.vue"]]);
  const _sfc_main$B = {
    __name: "common-placeholder",
    setup(__props, { expose: __expose }) {
      __expose();
      const headerPadPx = vue.computed(() => toPx(getHeaderPlaceholderHeight()));
      const __returned__ = { headerPadPx, ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick, getCurrentInstance: vue.getCurrentInstance, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get getStatusBarHeight() {
        return getStatusBarHeight;
      }, get getHeaderPlaceholderHeight() {
        return getHeaderPlaceholderHeight;
      }, get toPx() {
        return toPx;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$A(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock(
      "view",
      {
        class: "nav-placeholder",
        style: vue.normalizeStyle({ height: $setup.headerPadPx })
      },
      null,
      4
      /* STYLE */
    );
  }
  const __easycom_2$2 = /* @__PURE__ */ _export_sfc(_sfc_main$B, [["render", _sfc_render$A], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-placeholder/common-placeholder.vue"]]);
  function getAppScene() {
    {
      const systemInfo2 = uni.getSystemInfoSync();
      if (systemInfo2.osName === "ios")
        return 2;
      if (systemInfo2.osName === "android")
        return 3;
    }
    return 1;
  }
  const toH5Path = (path, useHash = USE_HASH_ROUTER) => {
    if (!path)
      return "/#/";
    if (/^https?:\/\//i.test(path))
      return path;
    const clean = "/" + String(path).replace(/^\/+/, "");
    return useHash ? "/#" + clean : clean;
  };
  function useCrossShare(payloadFactory) {
    const scene = vue.ref(getAppScene());
    const shareBtnVisible = vue.ref(scene.value !== 4);
    const providers = vue.ref([]);
    if (scene.value === 2 || scene.value === 3) {
      uni.getProvider({
        service: "share",
        success: (res) => {
          providers.value = res.provider || [];
        },
        fail: () => {
          providers.value = [];
        }
      });
    }
    const setupMpShare = () => {
      if (scene.value === 4) {
        onShareAppMessage(() => {
          const { title = "", imageUrl = "", path = "/" } = payloadFactory() || {};
          return { title, imageUrl, path };
        });
        onShareTimeline(() => {
          const { title = "", imageUrl = "", path = "/" } = payloadFactory() || {};
          const query = path.includes("?") ? path.split("?")[1] : "";
          return { title, imageUrl, query };
        });
      }
    };
    const getHref = () => {
      const p2 = payloadFactory() || {};
      const domain = (p2.h5Base || SHARE_H5_BASE || scene.value === 1 && typeof location !== "undefined" && location.origin || "").replace(/\/$/, "");
      const h5Path = toH5Path(p2.path, p2.useHashRouter);
      return /^https?:\/\//i.test(p2.path) ? p2.path : domain + h5Path;
    };
    const shareClick = () => {
      const p2 = payloadFactory() || {};
      const href = getHref();
      if (scene.value === 2 || scene.value === 3) {
        const items = [];
        const map = [];
        if (providers.value.includes("weixin")) {
          items.push("微信好友", "朋友圈");
          map.push(
            { provider: "weixin", scene: "WXSceneSession" },
            { provider: "weixin", scene: "WXSceneTimeline" }
          );
        }
        if (providers.value.includes("qq")) {
          items.push("QQ");
          map.push({ provider: "qq", scene: "" });
        }
        if (!items.length) {
          uni.setClipboardData({
            data: href,
            success: () => uni.showToast({ title: "链接已复制" })
          });
          return;
        }
        uni.showActionSheet({
          itemList: items,
          success: ({ tapIndex }) => {
            const opt = map[tapIndex];
            if (!opt)
              return;
            uni.share({
              provider: opt.provider,
              scene: opt.scene,
              type: 0,
              // 图文链接
              href,
              // 点击后打开的链接（建议给 H5 页面）
              title: p2.title || "",
              summary: p2.summary || "",
              imageUrl: p2.imageUrl || "",
              success: () => uni.showToast({ title: "已分享" }),
              fail: (e2) => uni.showToast({ title: (e2 == null ? void 0 : e2.errMsg) || "分享失败", icon: "none" })
            });
          }
        });
        return;
      }
      if (scene.value === 1) {
        if (typeof navigator !== "undefined" && navigator.share) {
          navigator.share({ title: p2.title || "", text: p2.summary || "", url: href }).catch(() => {
          });
        } else {
          uni.setClipboardData({
            data: href,
            success: () => uni.showToast({ title: "链接已复制" })
          });
        }
        return;
      }
      uni.showToast({ title: "请使用右上角“转发”", icon: "none" });
    };
    return { shareBtnVisible, setupMpShare, shareClick, getHref };
  }
  const _imports_1$2 = "/static/artist/iconify-uil_shop.png";
  const _imports_2$2 = "/static/artist/wig.png";
  const _imports_3 = "/static/artist/pen.png";
  const defaultAvatar$1 = "https://images1.fantuanpu.com/brand-avatar/default";
  const _sfc_main$A = {
    __name: "bjd_faceup_artist",
    setup(__props, { expose: __expose }) {
      __expose();
      const brandId = vue.ref(0);
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => scrollTop.value = e2.scrollTop || 0);
      const goBack = () => uni.navigateBack({
        delta: 1
      });
      const pageId = vue.ref("1");
      const detail = vue.ref({
        title: "超好看的作品",
        images: ["https://images1.fantuanpu.com/artist/demo-cover.jpg"]
      });
      const {
        setupMpShare,
        shareBtnVisible,
        shareClick
      } = useCrossShare(() => {
        var _a;
        return {
          title: `BJD妆师 · ${detail.value.title || "精选"}`,
          summary: "看看这个作品，超好看～",
          imageUrl: (_a = detail.value.images) == null ? void 0 : _a[0],
          path: `/pages/faceup/detail?id=${pageId.value}`
          // H5 域名（可不传，默认 location.origin）
          // h5Base: 'https://m.example.com'
        };
      });
      setupMpShare();
      const info = vue.reactive({
        brand_id: 0,
        brand_name: "",
        description: "",
        logo_image: "",
        last_login_time: 0,
        artist_stats: null
      });
      const lastLoginText = vue.computed(() => {
        const ts2 = Number(info.last_login_time || 0);
        if (!ts2)
          return "未入驻";
        return timeAgo(ts2 * 1e3);
      });
      const avgCycleDaysText = vue.computed(() => {
        var _a;
        const d2 = Number(((_a = info.artist_stats) == null ? void 0 : _a.avg_cycle_days) || 0);
        return d2 ? `${toFixed2(d2)}天` : "—";
      });
      const punctualityRateText = vue.computed(() => {
        var _a;
        const r2 = Number(((_a = info.artist_stats) == null ? void 0 : _a.punctuality_rate) || 0);
        return `${Math.round(r2 * 100)}%`;
      });
      const onShare = () => {
        uni.showToast({
          title: "请使用系统分享或右上角菜单",
          icon: "none"
        });
      };
      const listTab = vue.ref("images");
      const switchListTab = async (tab) => {
        listTab.value = tab;
        if (tab === "orders" && orderPlans.value.length === 0) {
          await fetchOrderPlans();
        }
      };
      const faceups = vue.ref([]);
      const page = vue.ref(1);
      const size = vue.ref(30);
      const hasMore = vue.ref(true);
      const loadingMore = vue.ref(false);
      function mapFaceup(list = []) {
        return list.map((it2) => {
          const first = it2.image_urls && it2.image_urls[0] || "";
          return first ? {
            id: it2.id,
            previewUrl: first
          } : null;
        }).filter(Boolean);
      }
      const monthBusyList = vue.ref([]);
      const MONTH_NAMES = ["一月", "二月", "三月", "四月", "五月", "六月", "七月", "八月", "九月", "十月", "十一月", "十二月"];
      function toFixed2(v2) {
        const n2 = parseFloat(v2);
        return Number.isNaN(n2) ? 0 : Math.round(n2 * 100) / 100;
      }
      function timeAgo(ms2) {
        const diff = Date.now() - ms2;
        if (diff < 0)
          return "刚刚";
        const s2 = Math.floor(diff / 1e3);
        if (s2 < 60)
          return `${s2} 秒前`;
        const m2 = Math.floor(s2 / 60);
        if (m2 < 60)
          return `${m2} 分钟前`;
        const h2 = Math.floor(m2 / 60);
        if (h2 < 24)
          return `${h2} 小时前`;
        const d2 = Math.floor(h2 / 24);
        if (d2 < 30)
          return `${d2} 天前`;
        const mo = Math.floor(d2 / 30);
        if (mo < 12)
          return `${mo} 月前`;
        return `${Math.floor(mo / 12)} 年前`;
      }
      function formatTime(ts2) {
        if (!ts2)
          return "--";
        const d2 = new Date(ts2 * 1e3);
        const mm = String(d2.getMonth() + 1).padStart(2, "0");
        const dd = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0");
        const mi = String(d2.getMinutes()).padStart(2, "0");
        return `${mm}/${dd} ${hh}:${mi}`;
      }
      async function fetchInfo() {
        var _a, _b;
        const res = await uni.request({
          url: `${websiteUrl.value}/brand-artist/info`,
          method: "GET",
          data: {
            brand_id: brandId.value
          }
          // 必带
        });
        if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
          uni.showToast({
            title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败",
            icon: "none"
          });
          return;
        }
        const d2 = res.data.data || {};
        info.brand_id = d2.brand_id || 0;
        info.brand_name = d2.brand_name || "";
        info.description = d2.description || "";
        info.logo_image = d2.logo_image || "";
        info.last_login_time = Number(d2.last_login_time || 0);
        info.artist_stats = d2.artist_stats || null;
      }
      async function fetchFaceups() {
        var _a, _b, _c, _d, _e2, _f;
        if (!hasMore.value || loadingMore.value)
          return;
        loadingMore.value = true;
        const res = await uni.request({
          url: `${websiteUrl.value}/brand-artist/bjd-faceup`,
          method: "GET",
          data: {
            brand_id: brandId.value,
            page: page.value,
            size: size.value
          }
          // 必带
        });
        loadingMore.value = false;
        if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
          uni.showToast({
            title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败",
            icon: "none"
          });
          return;
        }
        const list = mapFaceup(((_d = (_c = res.data) == null ? void 0 : _c.data) == null ? void 0 : _d.list) || []);
        faceups.value = [...faceups.value, ...list];
        const total = Number(((_f = (_e2 = res.data) == null ? void 0 : _e2.data) == null ? void 0 : _f.total) || faceups.value.length);
        hasMore.value = faceups.value.length < total;
        if (hasMore.value)
          page.value += 1;
      }
      async function fetchMonthlyBusy() {
        var _a, _b, _c;
        const now = /* @__PURE__ */ new Date();
        const startMonth = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}`;
        const months = 6;
        const res = await uni.request({
          url: `${websiteUrl.value}/brand-artist/monthly-busy`,
          method: "GET",
          data: {
            brand_id: brandId.value,
            start_month: startMonth,
            months
          }
        });
        let rows = [];
        if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() === "success") {
          rows = ((_c = (_b = res.data) == null ? void 0 : _b.data) == null ? void 0 : _c.list) || [];
        }
        if (!rows.length) {
          for (let i2 = 0; i2 < months; i2++) {
            const d2 = new Date(now.getFullYear(), now.getMonth() + i2 + 1, 0);
            rows.push({
              year: d2.getFullYear(),
              month: d2.getMonth() + 1,
              busy_days: 0,
              total_days: d2.getDate()
            });
          }
        }
        monthBusyList.value = rows.map((r2, idx) => {
          const isCurrent = idx === 0;
          const label = isCurrent ? "本月" : MONTH_NAMES[(Number(r2.month) - 1 + 12) % 12];
          return {
            key: `${r2.year}-${r2.month}`,
            label,
            isCurrent,
            busy_days: Number(r2.busy_days || 0),
            total_days: Number(r2.total_days || 30)
          };
        });
      }
      const orderPlans = vue.ref([]);
      const orderPage = vue.ref(1);
      const orderSize = vue.ref(5);
      const orderHasMore = vue.ref(true);
      const orderLoadingMore = vue.ref(false);
      const hasOrderDot = vue.ref(false);
      function statusText(p2) {
        const now = Math.floor(Date.now() / 1e3);
        if (now >= p2.open_time && now <= p2.close_time)
          return "进行中";
        if (now < p2.open_time)
          return "即将开始";
        return "已结束";
      }
      function statusClass(p2) {
        const t2 = statusText(p2);
        return {
          ongoing: t2 === "进行中",
          upcoming: t2 === "即将开始",
          ended: t2 === "已结束"
        };
      }
      function tierRange(p2) {
        const prices = (p2.tiers || []).map((t2) => Number(t2.price || 0)).filter((n2) => !Number.isNaN(n2));
        if (!prices.length)
          return "未设置档位";
        const min = Math.min(...prices);
        const max = Math.max(...prices);
        return `¥${min}${min !== max ? `~${max}` : ""} · ${prices.length}档`;
      }
      async function fetchOrderPlans() {
        var _a, _b, _c;
        if (!orderHasMore.value || orderLoadingMore.value)
          return;
        orderLoadingMore.value = true;
        const res = await uni.request({
          url: `${websiteUrl.value}/brand-artist/order-plans`,
          method: "GET",
          data: {
            brand_id: brandId.value,
            artist_type: 1,
            page: orderPage.value,
            size: orderSize.value
          }
        });
        orderLoadingMore.value = false;
        if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
          uni.showToast({
            title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败",
            icon: "none"
          });
          return;
        }
        const list = ((_c = res.data) == null ? void 0 : _c.data) || [];
        orderPlans.value = [...orderPlans.value, ...list];
        const now = Math.floor(Date.now() / 1e3);
        const soon = 7 * 24 * 3600;
        hasOrderDot.value = orderPlans.value.some((p2) => now >= p2.open_time && now <= p2.close_time || p2.open_time > now && p2.open_time - now <= soon);
        if (list.length < orderSize.value) {
          orderHasMore.value = false;
        } else {
          orderPage.value += 1;
        }
      }
      function openOrder(id) {
        uni.navigateTo({
          url: `/pkg-creator/creator_order/plane_detail/plane_detail?id=${id}`
        });
      }
      function preview(url) {
        const urls = faceups.value.map((i2) => i2.previewUrl);
        const current = urls.indexOf(url);
        uni.previewImage({
          urls,
          current: current >= 0 ? current : 0
        });
      }
      function goRole(type) {
        const map = {
          shop: "/pages/shop/index",
          hair: "/pages/hair/index",
          artist: "/pages/artist/index"
        };
        uni.navigateTo({
          url: map[type] || "/"
        });
      }
      onLoad((query) => {
        const bid = Number((query == null ? void 0 : query.brand_id) || 0);
        if (!bid) {
          uni.showToast({
            title: "缺少 brand_id",
            icon: "none"
          });
          return;
        }
        brandId.value = bid;
      });
      vue.onMounted(async () => {
        if (!brandId.value)
          return;
        uni.showLoading({
          title: "加载中..."
        });
        try {
          await fetchInfo();
          await Promise.all([fetchMonthlyBusy(), fetchFaceups(), fetchOrderPlans()]);
        } finally {
          uni.hideLoading();
        }
      });
      const navigateToFaceup = (id) => {
        uni.navigateTo({
          url: `/pages/artwork/artwork?id=${id}`
        });
      };
      onReachBottom(() => {
        if (listTab.value === "images")
          fetchFaceups();
        else
          fetchOrderPlans();
      });
      const __returned__ = { brandId, scrollTop, goBack, pageId, detail, setupMpShare, shareBtnVisible, shareClick, info, defaultAvatar: defaultAvatar$1, lastLoginText, avgCycleDaysText, punctualityRateText, onShare, listTab, switchListTab, faceups, page, size, hasMore, loadingMore, mapFaceup, monthBusyList, MONTH_NAMES, toFixed2, timeAgo, formatTime, fetchInfo, fetchFaceups, fetchMonthlyBusy, orderPlans, orderPage, orderSize, orderHasMore, orderLoadingMore, hasOrderDot, statusText, statusClass, tierRange, fetchOrderPlans, openOrder, preview, goRole, navigateToFaceup, ref: vue.ref, reactive: vue.reactive, computed: vue.computed, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get onReachBottom() {
        return onReachBottom;
      }, get onPageScroll() {
        return onPageScroll;
      }, get websiteUrl() {
        return websiteUrl;
      }, get useCrossShare() {
        return useCrossShare;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$z(_ctx, _cache, $props, $setup, $data, $options) {
    var _a;
    const _component_common_share = resolveEasycom(vue.resolveDynamicComponent("common-share"), __easycom_0$1);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_common_placeholder = resolveEasycom(vue.resolveDynamicComponent("common-placeholder"), __easycom_2$2);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "artist-page" }, [
      vue.createElementVNode("view", { class: "head-container" }, [
        vue.createVNode(_component_zhouWei_navBar, {
          type: "transparentFixed",
          backState: 2e3,
          homeState: 2e3,
          scrollTop: $setup.scrollTop,
          fontColor: "#000",
          transparentFixedFontColor: "#000",
          titleCenter: true
        }, {
          left: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-left" }, [
              vue.createElementVNode("view", {
                class: "nav-back-pill",
                onClick: $setup.goBack,
                "aria-label": "返回"
              }, [
                vue.createElementVNode("image", {
                  src: _imports_0$3,
                  mode: "aspectFill",
                  style: { "width": "14rpx", "height": "14rpx", "position": "relative", "top": "3rpx" }
                })
              ]),
              vue.createElementVNode("text", {
                class: "nav-back-text font-title",
                onClick: $setup.goBack
              }, "返回")
            ])
          ]),
          right: vue.withCtx(() => [
            $setup.shareBtnVisible ? (vue.openBlock(), vue.createBlock(_component_common_share, {
              key: 0,
              onClick: $setup.shareClick
            }, null, 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
          ]),
          transparentFixedLeft: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-left" }, [
              vue.createElementVNode("view", {
                class: "nav-back-pill",
                onClick: $setup.goBack,
                "aria-label": "返回"
              }, [
                vue.createElementVNode("image", {
                  src: _imports_0$3,
                  mode: "aspectFill",
                  style: { "width": "14rpx", "height": "14rpx", "position": "relative", "top": "3rpx" }
                })
              ]),
              vue.createElementVNode("text", {
                class: "nav-back-text font-title",
                onClick: $setup.goBack
              }, "BJD妆师")
            ])
          ]),
          transparentFixedRight: vue.withCtx(() => [
            $setup.shareBtnVisible ? (vue.openBlock(), vue.createBlock(_component_common_share, {
              key: 0,
              onClick: $setup.shareClick
            }, null, 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
          ]),
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "nav-center" }, [
              vue.createElementVNode("image", {
                class: "nav-center-avatar",
                src: $setup.info.logo_image || $setup.defaultAvatar,
                mode: "aspectFill"
              }, null, 8, ["src"]),
              vue.createElementVNode(
                "text",
                { class: "nav-center-name font-alimamashuhei" },
                vue.toDisplayString($setup.info.brand_name || "—"),
                1
                /* TEXT */
              )
            ])
          ]),
          transparentFixed: vue.withCtx(() => []),
          _: 1
          /* STABLE */
        }, 8, ["scrollTop"]),
        vue.createVNode(_component_common_placeholder),
        vue.createElementVNode("view", { class: "header" }, [
          vue.createElementVNode("view", { class: "avatar-wrap" }, [
            vue.createElementVNode("image", {
              class: "avatar",
              src: $setup.info.logo_image || $setup.defaultAvatar,
              mode: "aspectFill"
            }, null, 8, ["src"])
          ]),
          vue.createElementVNode("view", { class: "info-card" }, [
            vue.createElementVNode("view", { class: "row-1" }, [
              vue.createElementVNode(
                "text",
                { class: "brand-name font-alimamashuhei" },
                vue.toDisplayString($setup.info.brand_name || "—"),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "text",
                { class: "last-time" },
                vue.toDisplayString($setup.lastLoginText),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "row-2" }, [
              vue.createElementVNode("view", { class: "stat" }, [
                vue.createElementVNode("text", { class: "label font-title" }, "完成单数"),
                vue.createElementVNode(
                  "text",
                  { class: "val" },
                  vue.toDisplayString(((_a = $setup.info.artist_stats) == null ? void 0 : _a.finished_count) ?? 0),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "stat" }, [
                vue.createElementVNode("text", { class: "label font-title" }, "平均工期"),
                vue.createElementVNode(
                  "text",
                  { class: "val" },
                  vue.toDisplayString($setup.avgCycleDaysText),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "stat" }, [
                vue.createElementVNode("text", { class: "label font-title" }, "准时率"),
                vue.createElementVNode(
                  "text",
                  { class: "val" },
                  vue.toDisplayString($setup.punctualityRateText),
                  1
                  /* TEXT */
                )
              ])
            ]),
            $setup.info.description ? (vue.openBlock(), vue.createElementBlock(
              "view",
              {
                key: 0,
                class: "desc"
              },
              vue.toDisplayString($setup.info.description),
              1
              /* TEXT */
            )) : vue.createCommentVNode("v-if", true)
          ])
        ]),
        vue.createElementVNode("view", { class: "calendar-card" }, [
          vue.createElementVNode("view", { class: "cal-title font-title" }, "档期"),
          vue.createElementVNode("scroll-view", {
            "scroll-x": "",
            class: "month-row-scroll",
            "show-scrollbar": false
          }, [
            vue.createElementVNode("view", { class: "month-row" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.monthBusyList, (m2) => {
                  return vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: m2.key,
                      class: vue.normalizeClass(["month-pill", { current: m2.isCurrent }])
                    },
                    [
                      vue.createElementVNode(
                        "view",
                        { class: "m1 font-title" },
                        vue.toDisplayString(m2.label),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "view",
                        { class: "m2 font-title" },
                        vue.toDisplayString(m2.busy_days) + "/" + vue.toDisplayString(m2.total_days),
                        1
                        /* TEXT */
                      )
                    ],
                    2
                    /* CLASS */
                  );
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ])
        ])
      ]),
      vue.createElementVNode("view", { class: "identity-card" }, [
        vue.createElementVNode("view", { class: "id-title font-title" }, "其它身份"),
        vue.createElementVNode("view", { class: "id-row" }, [
          vue.createElementVNode("view", {
            class: "id-pill",
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.goRole("shop"))
          }, [
            vue.createElementVNode("image", {
              class: "id-img",
              src: _imports_1$2
            }),
            vue.createElementVNode("text", { class: "font-title" }, "贩售")
          ]),
          vue.createElementVNode("view", {
            class: "id-pill",
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.goRole("hair"))
          }, [
            vue.createElementVNode("image", {
              class: "id-img",
              src: _imports_2$2
            }),
            vue.createElementVNode("text", { class: "font-title" }, "手改毛")
          ]),
          vue.createElementVNode("view", {
            class: "id-pill",
            onClick: _cache[2] || (_cache[2] = ($event) => $setup.goRole("artist"))
          }, [
            vue.createElementVNode("image", {
              class: "id-img",
              src: _imports_3
            }),
            vue.createElementVNode("text", { class: "font-title" }, "妆师")
          ])
        ])
      ]),
      vue.createElementVNode("view", { style: { "margin": "20rpx" } }, [
        vue.createElementVNode("view", { style: { "margin-left": "01rpx" } }, [
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["tab-btn", "font-title", { active: $setup.listTab === "images" }]),
              onClick: _cache[3] || (_cache[3] = ($event) => $setup.switchListTab("images"))
            },
            [
              vue.createElementVNode("text", { class: "label" }, "展示图片"),
              vue.createElementVNode("view", { class: "ink-ellipse" })
            ],
            2
            /* CLASS */
          ),
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["tab-btn", "font-title", { active: $setup.listTab === "orders" }]),
              onClick: _cache[4] || (_cache[4] = ($event) => $setup.switchListTab("orders"))
            },
            [
              $setup.hasOrderDot ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "red-dot"
              })) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("text", { class: "label" }, "开单记录"),
              vue.createElementVNode("view", { class: "ink-ellipse" })
            ],
            2
            /* CLASS */
          )
        ]),
        $setup.listTab === "images" ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "grid"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.faceups, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: item.id,
                class: "grid-item",
                onClick: ($event) => $setup.navigateToFaceup(item.id)
              }, [
                vue.createElementVNode("image", {
                  class: "grid-img",
                  src: item.previewUrl,
                  mode: "aspectFill"
                }, null, 8, ["src"])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          $setup.loadingMore ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "grid-tip"
          }, [
            vue.createVNode(_component_uni_load_more, { status: "loading" })
          ])) : !$setup.hasMore && $setup.faceups.length ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "grid-tip"
          }, "— 已到底 —")) : vue.createCommentVNode("v-if", true)
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "order-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.orderPlans, (p2) => {
              var _a2;
              return vue.openBlock(), vue.createElementBlock("view", {
                key: p2.id,
                class: "order-card",
                onClick: ($event) => $setup.openOrder(p2.id)
              }, [
                vue.createElementVNode("image", {
                  class: "order-cover",
                  src: ((_a2 = p2.images) == null ? void 0 : _a2[0]) || $setup.info.logo_image,
                  mode: "aspectFill"
                }, null, 8, ["src"]),
                vue.createElementVNode("view", { class: "order-body" }, [
                  vue.createElementVNode(
                    "view",
                    { class: "order-title font-title" },
                    vue.toDisplayString(p2.brand_name) + " · 开单",
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("view", { class: "order-meta" }, [
                    vue.createElementVNode(
                      "text",
                      {
                        class: vue.normalizeClass(["chip", $setup.statusClass(p2)])
                      },
                      vue.toDisplayString($setup.statusText(p2)),
                      3
                      /* TEXT, CLASS */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "time" },
                      "开：" + vue.toDisplayString($setup.formatTime(p2.open_time)),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "time" },
                      "截：" + vue.toDisplayString($setup.formatTime(p2.close_time)),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode(
                    "view",
                    { class: "tier-line" },
                    vue.toDisplayString($setup.tierRange(p2)),
                    1
                    /* TEXT */
                  )
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          )),
          $setup.orderLoadingMore ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "grid-tip"
          }, [
            vue.createVNode(_component_uni_load_more, { status: "loading" })
          ])) : !$setup.orderHasMore && $setup.orderPlans.length ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "grid-tip"
          }, "— 没有更多记录 —")) : !$setup.orderPlans.length && !$setup.orderLoadingMore ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "grid-tip"
          }, "暂无记录")) : vue.createCommentVNode("v-if", true)
        ]))
      ])
    ]);
  }
  const PagesArtistInfoBjdFaceupArtist = /* @__PURE__ */ _export_sfc(_sfc_main$A, [["render", _sfc_render$z], ["__scopeId", "data-v-bf18f30f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pages/artist_info/bjd_faceup_artist.vue"]]);
  const defaultAvatar = "/static/avatar-default.png";
  const _sfc_main$z = {
    __name: "message_list",
    setup(__props, { expose: __expose }) {
      var _a;
      const sessions = vue.reactive([]);
      const page = vue.ref(1);
      const pageSize2 = vue.ref(20);
      const noMore = vue.ref(false);
      const loading2 = vue.ref(true);
      const loadingMore = vue.ref(false);
      const loadingText = vue.ref("加载中…");
      const headerPlaceholder = vue.ref(getHeaderPlaceholderHeight() + "px");
      const footerPlaceholder = vue.ref(getFooterPlaceholderHeight() + "px");
      const selfId = vue.ref(0);
      try {
        const u2 = uni.getStorageSync("userInfo") || {};
        selfId.value = Number((u2 == null ? void 0 : u2.id) || (u2 == null ? void 0 : u2.Id) || 0) || 0;
      } catch (_2) {
      }
      function pickSessionsPayload(res) {
        var _a2;
        if (!res)
          return { list: [], total: 0 };
        if (Array.isArray(res))
          return { list: res, total: res.length };
        if (Array.isArray(res == null ? void 0 : res.list))
          return { list: res.list, total: Number((res == null ? void 0 : res.total) || res.list.length || 0) };
        if (Array.isArray((_a2 = res == null ? void 0 : res.data) == null ? void 0 : _a2.sessions)) {
          return {
            list: res.data.sessions,
            total: Number(res.data.total || res.data.sessions.length || 0),
            page: Number(res.data.page || 1),
            page_size: Number(res.data.page_size || 0),
            self_uid: Number(res.data.self_uid || 0)
          };
        }
        return { list: [], total: 0 };
      }
      async function fetchList(reset = false) {
        if (reset) {
          page.value = 1;
          noMore.value = false;
          sessions.splice(0, sessions.length);
        }
        if (noMore.value)
          return;
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          loading2.value = false;
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        if (page.value === 1)
          loading2.value = true;
        else
          loadingMore.value = true;
        try {
          const res = await listSessions({ page: page.value, page_size: pageSize2.value });
          const { list } = pickSessionsPayload(res);
          list.forEach((item) => upsertSession(item));
          noMore.value = list.length < pageSize2.value;
          page.value += 1;
        } catch (e2) {
          formatAppLog("error", "at pkg-im/message_list/message_list.vue:120", "fetch sessions error", e2);
          uni.showToast({ title: "加载失败", icon: "none" });
        } finally {
          loading2.value = false;
          loadingMore.value = false;
        }
      }
      function findSessionIndex({ sid, peer_id }) {
        const sidStr = sid === void 0 || sid === null ? "" : String(sid);
        let idx = -1;
        if (sidStr)
          idx = sessions.findIndex((x) => String(x.session_id) === sidStr);
        if (idx < 0 && peer_id)
          idx = sessions.findIndex((x) => Number(x.peer_id || 0) === Number(peer_id));
        return idx;
      }
      async function fetchUserSnapshot(uid) {
        var _a2, _b;
        if (!uid)
          return { name: "用户", avatar: "" };
        try {
          const res = await uni.request({ url: `${websiteUrl.value}/user-info?uid=${uid}`, method: "GET", timeout: 5e3 });
          const d2 = (_a2 = res == null ? void 0 : res.data) == null ? void 0 : _a2.data;
          if (((_b = res == null ? void 0 : res.data) == null ? void 0 : _b.status) === "success" && d2) {
            return { name: (d2 == null ? void 0 : d2.username) || (d2 == null ? void 0 : d2.user_name) || "用户", avatar: (d2 == null ? void 0 : d2.avatar) || "" };
          }
        } catch (_2) {
        }
        return { name: "用户", avatar: "" };
      }
      function upsertSession(s2) {
        const sid = s2.session_id === void 0 || s2.session_id === null ? "" : String(s2.session_id);
        if (!sid && !s2.peer_id)
          return;
        const norm = {
          session_id: sid || String(s2.id || ""),
          // 统一存字符串
          peer_id: Number(s2.peer_id || 0),
          name: s2.name || s2.peer_name || s2.username || "用户",
          avatar: s2.avatar || s2.peer_avatar || "",
          last_msg_preview: s2.last_msg_preview || s2.preview || s2.last_msg || "",
          last_msg_time: Number(s2.last_msg_time || 0),
          unread: Number(s2.unread || s2.unread_count || 0)
        };
        const i2 = findSessionIndex({ sid: norm.session_id, peer_id: norm.peer_id });
        if (i2 >= 0) {
          const old = sessions[i2];
          const merged = { ...old, ...norm };
          sessions.splice(i2, 1);
          sessions.unshift(merged);
        } else {
          sessions.unshift(norm);
        }
      }
      async function handleIMEvent(evt) {
        if (!evt || evt.type !== "im.event")
          return;
        const ev = (evt.event || "").toLowerCase();
        const data = evt.data || {};
        if (ev === "message") {
          const sid = data.session_id === void 0 || data.session_id === null ? "" : String(data.session_id);
          const msg = data.message || {};
          const from = Number(msg.sender_id || 0);
          const preview = previewFromContent(msg.content);
          const msgTime = Number(msg.msg_time || Date.now());
          const targetPeer = from && from !== selfId.value ? from : Number(data.peer_id || 0);
          let idx = findSessionIndex({ sid, peer_id: targetPeer });
          if (idx < 0 && targetPeer) {
            const snap = await fetchUserSnapshot(targetPeer);
            upsertSession({
              session_id: sid || `dm-${Math.min(selfId.value, targetPeer)}-${Math.max(selfId.value, targetPeer)}`,
              peer_id: targetPeer,
              name: snap.name,
              avatar: snap.avatar,
              last_msg: preview,
              last_msg_preview: preview,
              last_msg_time: msgTime,
              unread: from && from !== selfId.value ? 1 : 0
            });
            return;
          }
          if (idx >= 0) {
            const row = sessions[idx];
            const next = {
              ...row,
              last_msg_preview: preview,
              last_msg_time: msgTime,
              unread: from && from !== selfId.value ? Math.min(999, Number(row.unread || 0) + 1) : row.unread
            };
            sessions.splice(idx, 1);
            sessions.unshift(next);
          }
        }
        if (ev === "read") {
          const sid = data.session_id === void 0 || data.session_id === null ? "" : String(data.session_id);
          const reader = Number(data.user_id || data.reader_id || data.uid || 0);
          if (!sid || !reader)
            return;
          if (reader === selfId.value) {
            return;
          }
        }
      }
      function previewFromContent(content) {
        var _a2;
        if (!content || !content.type)
          return "";
        const t2 = content.type;
        if (t2 === "text")
          return content.text || "";
        if (t2 === "emoji")
          return "[表情]";
        if (t2 === "image")
          return "[图片]";
        if (t2 === "other")
          return ((_a2 = content == null ? void 0 : content.other) == null ? void 0 : _a2.biz) ? `[${content.other.biz}]` : "[其它]";
        return "";
      }
      function fmtTime(ms2) {
        const n2 = Number(ms2 || 0);
        if (!n2)
          return "";
        const d2 = new Date(n2);
        const now0 = /* @__PURE__ */ new Date();
        now0.setHours(0, 0, 0, 0);
        if (n2 >= now0.getTime()) {
          return `${pad(d2.getHours())}:${pad(d2.getMinutes())}`;
        }
        return `${pad(d2.getMonth() + 1)}-${pad(d2.getDate())}`;
      }
      function pad(n2) {
        return n2 < 10 ? "0" + n2 : "" + n2;
      }
      function goChat(s2) {
        uni.navigateTo({
          url: `/pkg-im/chat/chat?session_id=${encodeURIComponent(String(s2.session_id))}&peer_id=${Number(s2.peer_id || 0)}`
        });
      }
      vue.onMounted(async () => {
        connectIM();
        onIMEvent(handleIMEvent);
      });
      onShow(async () => {
        uni.setNavigationBarTitle({
          title: "聊天列表"
        });
        await fetchList(true);
      });
      vue.onUnmounted(() => {
      });
      (_a = uni.stopPullDownRefresh) == null ? void 0 : _a.call(uni);
      __expose({ fetchList });
      const __returned__ = { sessions, page, pageSize: pageSize2, noMore, loading: loading2, loadingMore, loadingText, defaultAvatar, headerPlaceholder, footerPlaceholder, selfId, pickSessionsPayload, fetchList, findSessionIndex, fetchUserSnapshot, upsertSession, handleIMEvent, previewFromContent, fmtTime, pad, goChat, ref: vue.ref, reactive: vue.reactive, onMounted: vue.onMounted, onUnmounted: vue.onUnmounted, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get getHeaderPlaceholderHeight() {
        return getHeaderPlaceholderHeight;
      }, get getFooterPlaceholderHeight() {
        return getFooterPlaceholderHeight;
      }, get listSessions() {
        return listSessions;
      }, get connectIM() {
        return connectIM;
      }, get closeIM() {
        return closeIM;
      }, get onIMEvent() {
        return onIMEvent;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$y(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "page" }, [
      vue.createElementVNode(
        "view",
        {
          style: vue.normalizeStyle({ height: $setup.headerPlaceholder })
        },
        null,
        4
        /* STYLE */
      ),
      !$setup.loading && $setup.sessions.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "empty"
      }, [
        vue.createElementVNode("text", { class: "empty-tips" }, "还没有消息")
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "list"
      }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.sessions, (s2, idx) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              key: s2.session_id + ":" + (s2.peer_id || idx),
              class: "cell",
              onClick: ($event) => $setup.goChat(s2)
            }, [
              vue.createElementVNode("image", {
                class: "avatar",
                src: s2.avatar || $setup.defaultAvatar,
                mode: "aspectFill"
              }, null, 8, ["src"]),
              vue.createElementVNode("view", { class: "meta" }, [
                vue.createElementVNode("view", { class: "row head" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "name" },
                    vue.toDisplayString(s2.name || "陌生人"),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "text",
                    { class: "time" },
                    vue.toDisplayString($setup.fmtTime(s2.last_msg_time)),
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "row msg" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "preview one-line" },
                    vue.toDisplayString(s2.last_msg_preview || "…"),
                    1
                    /* TEXT */
                  ),
                  s2.unread > 0 ? (vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: 0,
                      class: "badge"
                    },
                    vue.toDisplayString(s2.unread > 99 ? "99+" : s2.unread),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ])
            ], 8, ["onClick"]);
          }),
          128
          /* KEYED_FRAGMENT */
        )),
        $setup.loadingMore ? (vue.openBlock(), vue.createElementBlock(
          "view",
          {
            key: 0,
            class: "loading-more"
          },
          vue.toDisplayString($setup.loadingText),
          1
          /* TEXT */
        )) : $setup.noMore && $setup.sessions.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "loading-more"
        }, "已经到底啦~")) : vue.createCommentVNode("v-if", true)
      ])),
      vue.createElementVNode(
        "view",
        {
          style: vue.normalizeStyle({ height: $setup.footerPlaceholder })
        },
        null,
        4
        /* STYLE */
      )
    ]);
  }
  const PkgImMessageListMessageList = /* @__PURE__ */ _export_sfc(_sfc_main$z, [["render", _sfc_render$y], ["__scopeId", "data-v-4557a12d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-im/message_list/message_list.vue"]]);
  const pageSize$2 = 20;
  const bottomAnchorId = "bottom-anchor";
  const _sfc_main$y = {
    __name: "chat",
    setup(__props, { expose: __expose }) {
      __expose();
      const peerId = vue.ref(0);
      const sessionKey = vue.ref("");
      const numericSid = vue.ref(0);
      const selfUid = vue.ref(0);
      const selfInfo = vue.ref({ id: 0, avatar: "" });
      const peerInfo = vue.ref({ user_name: "", avatar: "" });
      const onlineText = vue.ref("");
      const messages2 = vue.ref([]);
      const page = vue.ref(1);
      const hasMore = vue.ref(true);
      const loadingMore = vue.ref(false);
      const scrollIntoId = vue.ref("");
      const draft = vue.ref("");
      const showEmoji = vue.ref(false);
      const emojis = ["😀", "😁", "😂", "🤣", "😊", "😍", "😘", "😎", "😡", "👍", "👏", "🎉"];
      const isBlocked = vue.ref(false);
      const myReadPts = vue.ref(0);
      const peerReadPts = vue.ref(0);
      let offIM = null;
      const hasInitOnce = vue.ref(false);
      const windowTopPxRaw = vue.ref(0);
      const footerSafeRaw = vue.ref(0);
      const headerOffsetPx = vue.computed(() => toPx(windowTopPxRaw.value));
      const footerSafePx = vue.computed(() => toPx(footerSafeRaw.value + 20));
      const bodyBottomOffset = vue.computed(() => "104rpx");
      function initSelfUidFromStorage() {
        try {
          const u2 = uni.getStorageSync("userInfo") || {};
          const id = Number((u2 == null ? void 0 : u2.id) || (u2 == null ? void 0 : u2.Id) || 0);
          if (id)
            selfUid.value = id;
          else {
            const legacy = Number(uni.getStorageSync("uid") || 0);
            if (legacy)
              selfUid.value = legacy;
          }
        } catch (_2) {
        }
      }
      function pickReaderId(d2) {
        return Number(
          (d2 == null ? void 0 : d2.reader_id) ?? (d2 == null ? void 0 : d2.reader) ?? (d2 == null ? void 0 : d2.user_id) ?? (d2 == null ? void 0 : d2.uid) ?? (d2 == null ? void 0 : d2.from_uid) ?? 0
        );
      }
      function applyPeerReadPts(newPts) {
        const n2 = Number(newPts || 0);
        if (n2 <= 0 || n2 <= peerReadPts.value)
          return;
        peerReadPts.value = n2;
      }
      onLoad((query) => {
        peerId.value = Number(query.peer_id || 0);
        initSelfUidFromStorage();
        windowTopPxRaw.value = getWindowTop();
        footerSafeRaw.value = getFooterPlaceholderHeight();
        formatAppLog("log", "at pkg-im/chat/chat.vue:269", "footerSafe:", footerSafeRaw.value);
      });
      onShow(async () => {
        var _a, _b, _c;
        if (!hasInitOnce.value) {
          await Promise.all([fetchPeerInfo(), fetchSelfInfo()]);
          connectIM();
          if (offIM) {
            offIM = null;
          }
          offIM = onIMEvent(handleIMEvent);
          const token = uni.getStorageSync("token");
          try {
            const res = await uni.request({
              url: `${websiteUrl.value}/with-state/im/start-session?peer_id=${encodeURIComponent(peerId.value)}`,
              method: "POST",
              header: token ? { Authorization: token } : {}
            });
            if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success" && ((_c = (_b = res.data) == null ? void 0 : _b.data) == null ? void 0 : _c.session_id)) {
              const data = res.data.data;
              sessionKey.value = String(data.session_id);
              const nId = Number((data == null ? void 0 : data.numeric_id) || 0);
              if (nId > 0)
                numericSid.value = nId;
              const peerPtsFromStart = Number(
                (data == null ? void 0 : data.peer_read_pts) ?? (data == null ? void 0 : data.peerReadPts) ?? (data == null ? void 0 : data.read_pts_peer) ?? 0
              );
              if (peerPtsFromStart > 0)
                applyPeerReadPts(peerPtsFromStart);
            } else {
              formatAppLog("warn", "at pkg-im/chat/chat.vue:298", "[CHAT] start-session failed", res == null ? void 0 : res.data);
              return;
            }
          } catch (e2) {
            formatAppLog("error", "at pkg-im/chat/chat.vue:302", "[CHAT] start-session error", e2);
            return;
          }
          page.value = 1;
          messages2.value = [];
          hasMore.value = true;
          await loadHistory();
          markReadToBottom();
          setActiveSession(numericSid.value > 0 ? numericSid.value : sessionKey.value);
          hasInitOnce.value = true;
          return;
        }
        connectIM();
        if (offIM) {
          offIM = null;
        }
        offIM = onIMEvent(handleIMEvent);
        const key = numericSid.value > 0 ? numericSid.value : sessionKey.value;
        if (key)
          setActiveSession(key);
      });
      onHide(() => {
        if (offIM) {
          offIM = null;
        }
      });
      onUnload(() => {
        if (offIM) {
          offIM = null;
        }
        leaveActiveSession();
      });
      function leaveActiveSession() {
        const key = numericSid.value > 0 ? numericSid.value : sessionKey.value;
        if (key)
          clearActiveSession(key);
      }
      async function fetchPeerInfo() {
        var _a;
        try {
          const res = await uni.request({ url: `${websiteUrl.value}/user-info?uid=${peerId.value}` });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success")
            peerInfo.value = res.data.data || {};
        } catch (_2) {
        }
      }
      async function fetchSelfInfo() {
        var _a, _b;
        try {
          const token = uni.getStorageSync("token");
          if (!token)
            return;
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/mine`,
            method: "GET",
            header: { Authorization: token }
          });
          if (((_a = res == null ? void 0 : res.data) == null ? void 0 : _a.status) === "success") {
            selfInfo.value = res.data.data || {};
            const id = Number(((_b = selfInfo.value) == null ? void 0 : _b.id) || 0);
            if (id)
              selfUid.value = id;
          }
        } catch (_2) {
        }
      }
      async function loadHistory() {
        var _a, _b, _c, _d, _e2, _f;
        if (!hasMore.value || loadingMore.value)
          return;
        loadingMore.value = true;
        let keepAnchor = "";
        if (messages2.value.length > 0) {
          const top = messages2.value[0];
          keepAnchor = "msg-" + (top.local_key || top.id || 0);
        }
        try {
          if (numericSid.value > 0) {
            const socket = getWS();
            if (socket && socket.readyState === 1) {
              let afterPTS = 0;
              if (messages2.value.length > 0) {
                afterPTS = Math.min(...messages2.value.map((m2) => Number(m2.pts || 0))) - 1;
                if (afterPTS < 0)
                  afterPTS = 0;
              }
              const reqId = "sync_" + Date.now();
              const pkt = {
                type: "im.req",
                action: "sync",
                req_id: reqId,
                data: { session_id: Number(numericSid.value), after_pts: Number(afterPTS), limit: pageSize$2 }
              };
              const resp = await waitWsResponseOnce(socket, "sync", reqId, 8e3, () => {
                socket.send(JSON.stringify(pkt));
              });
              if ((resp == null ? void 0 : resp.status) === "success") {
                const arr = (((_a = resp.data) == null ? void 0 : _a.messages) || []).map(wsToUiMessage);
                messages2.value = [...arr, ...messages2.value];
                hasMore.value = !!((_b = resp.data) == null ? void 0 : _b.has_more);
                page.value += 1;
                const peerPtsFromSync = Number(
                  ((_c = resp == null ? void 0 : resp.data) == null ? void 0 : _c.peer_read_pts) ?? ((_d = resp == null ? void 0 : resp.data) == null ? void 0 : _d.peerReadPts) ?? ((_e2 = resp == null ? void 0 : resp.data) == null ? void 0 : _e2.read_pts_peer) ?? 0
                );
                if (peerPtsFromSync > 0)
                  applyPeerReadPts(peerPtsFromSync);
                await vue.nextTick();
                if (page.value === 2) {
                  scrollToBottomSoon();
                } else if (keepAnchor) {
                  scrollIntoId.value = keepAnchor;
                  setTimeout(() => {
                    scrollIntoId.value = "";
                  }, 50);
                }
                return;
              }
            }
          }
          if (!sessionKey.value)
            return;
          const token = uni.getStorageSync("token") || "";
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/im/history`,
            method: "GET",
            data: { session_id: sessionKey.value, page: page.value, page_size: pageSize$2 },
            header: token ? { Authorization: token } : {}
          });
          if (((_f = res == null ? void 0 : res.data) == null ? void 0 : _f.status) === "success") {
            const d2 = res.data.data || {};
            const list = (d2.list || []).map(httpToUiMessage);
            messages2.value = [...list, ...messages2.value];
            const total = Number(d2.total || 0);
            hasMore.value = total > 0 ? messages2.value.length < total : list.length === pageSize$2;
            page.value += 1;
            const peerPtsFromHttp = Number(
              (d2 == null ? void 0 : d2.peer_read_pts) ?? (d2 == null ? void 0 : d2.peerReadPts) ?? (d2 == null ? void 0 : d2.peer_read) ?? (d2 == null ? void 0 : d2.read_pts_peer) ?? 0
            );
            if (peerPtsFromHttp > 0)
              applyPeerReadPts(peerPtsFromHttp);
            await vue.nextTick();
            if (page.value === 2) {
              scrollToBottomSoon();
            } else if (keepAnchor) {
              scrollIntoId.value = keepAnchor;
              setTimeout(() => {
                scrollIntoId.value = "";
              }, 50);
            }
          }
        } catch (e2) {
          formatAppLog("error", "at pkg-im/chat/chat.vue:447", "[CHAT] loadHistory error", e2);
        } finally {
          loadingMore.value = false;
        }
      }
      function loadMore() {
        if (hasMore.value && !loadingMore.value)
          loadHistory();
      }
      function sendText() {
        if (!draft.value)
          return;
        const text = draft.value;
        draft.value = "";
        sendPayload({ kind: "text", text });
      }
      function sendEmoji(e2) {
        showEmoji.value = false;
        sendPayload({ kind: "emoji", emoji: e2 });
      }
      function toggleEmoji() {
        showEmoji.value = !showEmoji.value;
      }
      async function pickAndSendImage() {
        var _a;
        try {
          const chooseRes = await uni.chooseImage({ count: 1, sizeType: ["compressed"] });
          const filePath = chooseRes.tempFilePaths[0];
          const tkRes = await uni.request({
            url: `${websiteUrl.value}/with-state/qiniu-token`,
            method: "POST",
            header: authHeader()
          });
          if (((_a = tkRes == null ? void 0 : tkRes.data) == null ? void 0 : _a.status) !== "success") {
            return uni.showToast({ title: "上传凭证失败", icon: "none" });
          }
          const { upload_url, token, key, domain } = tkRes.data.data;
          const upRes = await uni.uploadFile({
            url: upload_url,
            filePath,
            name: "file",
            formData: { token, key }
          });
          const body = JSON.parse(upRes.data || "{}");
          const url = (domain ? domain.replace(/\/$/, "") + "/" : "") + (body.key || key);
          sendPayload({ kind: "image", url });
        } catch (e2) {
        }
      }
      function sendSampleOther() {
        sendPayload({
          kind: "other",
          sub_type: "order",
          payload: { order_id: "D1234567", price: 199, title: "示例订单" }
        });
      }
      function sendPayload(msgPart) {
        const socket = getWS();
        if (!socket || socket.readyState !== 1) {
          uni.showToast({ title: "连接未就绪", icon: "none" });
          return;
        }
        const client_mid = genClientMID();
        const local = buildLocalMsg({ ...msgPart, client_mid });
        messages2.value.push(local);
        scrollToBottomSoon();
        const pkt = {
          type: "im.req",
          action: "send",
          req_id: client_mid,
          data: {
            peer_id: Number(peerId.value),
            client_msg_id: client_mid,
            content: toWsContent(msgPart)
          }
        };
        waitWsResponseOnce(socket, "send", client_mid, 8e3, () => {
          socket.send(JSON.stringify(pkt));
        }).then((resp) => {
          if (!resp || resp.status !== "success") {
            setMessageStatusByLocalKey(local.local_key, "failed");
            return;
          }
          const d2 = resp.data || {};
          updateMessageByLocalKey(local.local_key, (old) => ({
            ...old,
            id: Number(d2.message_id || old.id || 0),
            pts: Number(d2.pts || old.pts || 0),
            ts: d2.msg_time ? Math.floor(Number(d2.msg_time) / 1e3) : old.ts,
            status: "sent"
          }));
          if (Number(d2.session_id || 0) > 0 && numericSid.value === 0) {
            numericSid.value = Number(d2.session_id);
          }
          const peerPtsFromAck = Number(
            (d2 == null ? void 0 : d2.peer_read_pts) ?? (d2 == null ? void 0 : d2.peerReadPts) ?? (d2 == null ? void 0 : d2.read_pts_peer) ?? 0
          );
          if (peerPtsFromAck > 0)
            applyPeerReadPts(peerPtsFromAck);
        }).catch(() => {
          setMessageStatusByLocalKey(local.local_key, "failed");
        });
      }
      function updateMessageByLocalKey(localKey, updater) {
        const idx = messages2.value.findIndex((x) => x.local_key === localKey);
        if (idx >= 0) {
          const next = typeof updater === "function" ? updater(messages2.value[idx]) : updater;
          messages2.value.splice(idx, 1, next);
        }
      }
      function setMessageStatusByLocalKey(localKey, status) {
        updateMessageByLocalKey(localKey, (old) => ({ ...old, status }));
      }
      function markReadToBottom() {
        if (numericSid.value <= 0)
          return;
        const maxPts = messages2.value.reduce(
          (mx, m2) => Math.max(mx, Number(m2.pts || 0)),
          0
        );
        if (maxPts <= myReadPts.value)
          return;
        myReadPts.value = maxPts;
        const socket = getWS();
        if (!socket || socket.readyState !== 1)
          return;
        const reqId = "read_" + Date.now();
        const pkt = {
          type: "im.req",
          action: "read_ack",
          req_id: reqId,
          data: { session_id: Number(numericSid.value), read_pts: Number(myReadPts.value) }
        };
        socket.send(JSON.stringify(pkt));
      }
      function handleIMEvent(payload) {
        if (!payload || typeof payload !== "object")
          return;
        if (payload.type === "im.event" && payload.event === "message") {
          const d2 = payload.data || {};
          if (Number(d2.session_id || 0) > 0 && numericSid.value === 0) {
            numericSid.value = Number(d2.session_id);
          }
          if (typeof d2.session_id === "string" && d2.session_id !== sessionKey.value)
            return;
          if (numericSid.value > 0 && Number(d2.session_id || 0) !== Number(numericSid.value)) {
            return;
          }
          const ui = wsToUiMessage(d2.message || {});
          const isSelf = ui.from_uid === selfUid.value;
          if (isSelf) {
            let idx = ui.pts > 0 ? messages2.value.findIndex(
              (x) => x.from_uid === selfUid.value && Number(x.pts || 0) === ui.pts
            ) : -1;
            if (idx < 0) {
              const sig = buildSig(ui);
              idx = messages2.value.findIndex(
                (x) => x.from_uid === selfUid.value && x.status === "sending" && x.sig && x.sig === sig && Math.abs(Number(x.ts) - Number(ui.ts)) <= 5
              );
            }
            if (idx >= 0) {
              const old = messages2.value[idx];
              const merged = {
                ...old,
                id: ui.id || old.id,
                pts: ui.pts || old.pts,
                ts: ui.ts || old.ts,
                status: "sent"
              };
              messages2.value.splice(idx, 1, merged);
              return;
            }
          }
          messages2.value.push(ui);
          scrollToBottomSoon();
          markReadToBottom();
          return;
        }
        if (payload.type === "im.event" && payload.event === "read") {
          const d2 = payload.data || {};
          if (numericSid.value > 0 && Number(d2.session_id || 0) !== Number(numericSid.value)) {
            return;
          }
          const reader = pickReaderId(d2);
          if (reader && reader === selfUid.value)
            return;
          if (reader && Number(peerId.value || 0) > 0 && reader !== Number(peerId.value)) {
            return;
          }
          const peerPts = Number(
            (d2 == null ? void 0 : d2.read_pts) ?? (d2 == null ? void 0 : d2.peer_read_pts) ?? (d2 == null ? void 0 : d2.readPts) ?? 0
          );
          if (peerPts > 0)
            applyPeerReadPts(peerPts);
        }
      }
      function waitWsResponseOnce(socket, action, reqId, timeout = 8e3, sender) {
        return new Promise((resolve) => {
          let timer = null;
          const off = onIMEvent((payload) => {
            if (!payload || payload.type !== "im.res")
              return;
            if (payload.action !== action)
              return;
            if (payload.req_id !== reqId)
              return;
            clearTimeout(timer);
            off();
            resolve(payload);
          });
          timer = setTimeout(() => {
            off();
            resolve(null);
          }, timeout);
          sender && sender();
        });
      }
      function toWsContent(part) {
        if (part.kind === "text") {
          return { type: "text", text: part.text };
        }
        if (part.kind === "emoji") {
          return { type: "emoji", emoji: part.emoji };
        }
        if (part.kind === "image") {
          return { type: "image", images: [{ url: part.url }] };
        }
        if (part.kind === "other") {
          return {
            type: "other",
            other: {
              biz: part.sub_type || "other",
              ...part.payload || {}
            }
          };
        }
        return { type: "text", text: "[未知类型]" };
      }
      function buildSig(m2) {
        var _a, _b, _c, _d;
        const kind = m2.kind || ((_a = m2.content) == null ? void 0 : _a.type) || "";
        if (kind === "text")
          return `t|${(m2.text || "").slice(0, 200)}`;
        if (kind === "emoji")
          return `e|${m2.emoji || ""}`;
        if (kind === "image")
          return `i|${m2.url || (((_c = (_b = m2.images) == null ? void 0 : _b[0]) == null ? void 0 : _c.url) || "")}`;
        if (kind === "other") {
          const payload = m2.payload || ((_d = m2.content) == null ? void 0 : _d.other) || {};
          return `o|${m2.sub_type || payload.biz || "other"}|${JSON.stringify(payload)}`;
        }
        return `u|`;
      }
      function buildLocalMsg(part) {
        const now = Date.now();
        const base = {
          local_key: "local-" + now + "-" + Math.random().toString(36).slice(2),
          id: 0,
          pts: 0,
          session_id: sessionKey.value || "",
          from_uid: selfUid.value,
          to_uid: Number(peerId.value),
          ts: Math.floor(now / 1e3),
          status: "sending",
          client_mid: part.client_mid
        };
        let ui;
        if (part.kind === "text") {
          ui = { ...base, kind: "text", text: part.text };
        } else if (part.kind === "emoji") {
          ui = { ...base, kind: "emoji", emoji: part.emoji };
        } else if (part.kind === "image") {
          ui = { ...base, kind: "image", url: part.url };
        } else {
          const payload = part.payload || {};
          ui = {
            ...base,
            kind: "other",
            sub_type: part.sub_type,
            payload,
            card: payload.card || null
          };
        }
        ui.sig = buildSig(ui);
        return ui;
      }
      function httpToUiMessage(n2) {
        const payload = n2.payload || {};
        const ui = {
          id: Number(n2.id || 0),
          pts: Number(n2.pts || 0),
          session_id: sessionKey.value,
          from_uid: Number(n2.from_uid || 0),
          to_uid: Number(n2.to_uid || 0),
          kind: n2.kind || "text",
          text: n2.text,
          emoji: n2.emoji,
          url: n2.url,
          sub_type: n2.sub_type,
          payload,
          card: payload.card || null,
          ts: Number(n2.ts || Math.floor(Date.now() / 1e3)),
          status: "sent",
          client_mid: n2.client_mid
        };
        ui.sig = buildSig(ui);
        return ui;
      }
      function wsToUiMessage(m2) {
        var _a;
        const base = {
          id: Number(m2.id || 0),
          pts: Number(m2.pts || 0),
          session_id: numericSid.value > 0 ? Number(numericSid.value) : sessionKey.value,
          from_uid: Number(m2.sender_id || 0),
          to_uid: Number(m2.sender_id || 0) === selfUid.value ? Number(peerId.value) : selfUid.value,
          ts: Math.floor((m2.msg_time || Date.now()) / 1e3),
          status: "sent",
          client_mid: m2.client_msg_id
        };
        const c2 = m2.content || {};
        let ui;
        switch (c2.type) {
          case "text":
            ui = { ...base, kind: "text", text: c2.text };
            break;
          case "emoji":
            ui = { ...base, kind: "emoji", emoji: c2.emoji };
            break;
          case "image": {
            const url = Array.isArray(c2.images) && ((_a = c2.images[0]) == null ? void 0 : _a.url) ? c2.images[0].url : "";
            ui = { ...base, kind: "image", url };
            break;
          }
          case "other": {
            const other = c2.other || {};
            ui = {
              ...base,
              kind: "other",
              sub_type: other.biz || "other",
              payload: other,
              card: other.card || null
            };
            break;
          }
          default:
            ui = { ...base, kind: "text", text: "[未知类型]" };
        }
        ui.sig = buildSig(ui);
        return ui;
      }
      function statusText(m2) {
        if (!m2 || m2.from_uid !== selfUid.value)
          return "";
        if (m2.status === "failed")
          return "发送失败";
        if (Number(m2.pts || 0) > 0 && Number(peerReadPts.value || 0) >= Number(m2.pts)) {
          return "已读";
        }
        if (m2.status === "sending")
          return "发送中…";
        return "已发送";
      }
      function fmtTime(ts2) {
        const d2 = new Date(Number(ts2) * 1e3);
        const hh = String(d2.getHours()).padStart(2, "0");
        const m2 = String(d2.getMinutes()).padStart(2, "0");
        return `${hh}:${m2}`;
      }
      function briefOther(m2) {
        var _a, _b, _c, _d, _e2, _f;
        try {
          const card = m2.card || ((_a = m2.payload) == null ? void 0 : _a.card);
          if (card) {
            if (card.description)
              return card.description;
            if (card.title)
              return card.title;
            if (card.card_type) {
              return `[${mapCardTypeLabel(card.card_type)}]`;
            }
            return "[卡片消息]";
          }
          if (m2.sub_type === "order") {
            return `订单：${((_b = m2.payload) == null ? void 0 : _b.title) || ""} ￥${((_c = m2.payload) == null ? void 0 : _c.price) ?? ""}`;
          }
          if (m2.sub_type === "transfer") {
            return `转账：￥${((_d = m2.payload) == null ? void 0 : _d.amount) ?? ""}`;
          }
          if (m2.sub_type === "goods") {
            return `商品：${((_e2 = m2.payload) == null ? void 0 : _e2.name) || ""} ￥${((_f = m2.payload) == null ? void 0 : _f.price) ?? ""}`;
          }
        } catch (_2) {
        }
        return "[点击查看]";
      }
      function safeText(t2) {
        return (t2 || "").replace(/\n/g, "<br/>");
      }
      function previewImage(url) {
        uni.previewImage({ urls: [url] });
      }
      function scrollToBottomSoon() {
        vue.nextTick(() => {
          scrollIntoId.value = bottomAnchorId;
          setTimeout(() => {
            scrollIntoId.value = "";
          }, 50);
        });
      }
      function goBack() {
        uni.navigateBack();
      }
      function authHeader() {
        const token = uni.getStorageSync("token") || "";
        return token ? { Authorization: token } : {};
      }
      function genClientMID() {
        return "m_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
      }
      function mapCardTypeLabel(cardType) {
        switch (cardType) {
          case "artist_order_step_request":
            return "节点确认请求";
          case "artist_order_step_approve":
            return "节点已确认";
          case "artist_order_step_reject":
            return "节点已驳回";
          case "artist_order_change_price":
            return "价格调整";
          case "artist_order_change_item":
            return "投递内容更新";
          default:
            return "系统通知";
        }
      }
      function pickCard(m2) {
        var _a;
        return (m2 == null ? void 0 : m2.card) || ((_a = m2 == null ? void 0 : m2.payload) == null ? void 0 : _a.card) || null;
      }
      function cardTag(m2) {
        const card = pickCard(m2) || {};
        return mapCardTypeLabel(card.card_type || "");
      }
      function cardTitle(m2) {
        const card = pickCard(m2) || {};
        return card.title || "消息通知";
      }
      function cardDescription(m2) {
        const card = pickCard(m2) || {};
        return card.description || "";
      }
      function cardImage(m2) {
        const card = pickCard(m2) || {};
        const url = card.image_url || card.imageUrl || card.img_url || "";
        return typeof url === "string" ? url : "";
      }
      function cardActionText(m2) {
        const card = pickCard(m2) || {};
        return card.action_text || "查看详情";
      }
      function handleCardClick(m2) {
        const card = pickCard(m2);
        if (!card)
          return;
        if (card.app_page) {
          try {
            uni.navigateTo({ url: card.app_page });
            return;
          } catch (e2) {
          }
        }
        if (card.h5_url) {
          try {
            uni.setClipboardData({
              data: card.h5_url,
              success: () => {
                uni.showToast({
                  title: "链接已复制，可在浏览器打开",
                  icon: "none"
                });
              }
            });
            return;
          } catch (_2) {
          }
        }
        uni.showToast({
          title: card.action_text || "暂不支持的卡片操作",
          icon: "none"
        });
      }
      const __returned__ = { peerId, sessionKey, numericSid, selfUid, selfInfo, peerInfo, onlineText, messages: messages2, page, pageSize: pageSize$2, hasMore, loadingMore, bottomAnchorId, scrollIntoId, draft, showEmoji, emojis, isBlocked, myReadPts, peerReadPts, get offIM() {
        return offIM;
      }, set offIM(v2) {
        offIM = v2;
      }, hasInitOnce, windowTopPxRaw, footerSafeRaw, headerOffsetPx, footerSafePx, bodyBottomOffset, initSelfUidFromStorage, pickReaderId, applyPeerReadPts, leaveActiveSession, fetchPeerInfo, fetchSelfInfo, loadHistory, loadMore, sendText, sendEmoji, toggleEmoji, pickAndSendImage, sendSampleOther, sendPayload, updateMessageByLocalKey, setMessageStatusByLocalKey, markReadToBottom, handleIMEvent, waitWsResponseOnce, toWsContent, buildSig, buildLocalMsg, httpToUiMessage, wsToUiMessage, statusText, fmtTime, briefOther, safeText, previewImage, scrollToBottomSoon, goBack, authHeader, genClientMID, mapCardTypeLabel, pickCard, cardTag, cardTitle, cardDescription, cardImage, cardActionText, handleCardClick, ref: vue.ref, nextTick: vue.nextTick, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get onUnload() {
        return onUnload;
      }, get onShow() {
        return onShow;
      }, get onHide() {
        return onHide;
      }, get websiteUrl() {
        return websiteUrl;
      }, get getWindowTop() {
        return getWindowTop;
      }, get getFooterPlaceholderHeight() {
        return getFooterPlaceholderHeight;
      }, get toPx() {
        return toPx;
      }, get connectIM() {
        return connectIM;
      }, get onIMEvent() {
        return onIMEvent;
      }, get getWS() {
        return getWS;
      }, get setActiveSession() {
        return setActiveSession;
      }, get clearActiveSession() {
        return clearActiveSession;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$x(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_loading_jump_text = resolveEasycom(vue.resolveDynamicComponent("loading-jump-text"), LoadingJumpText);
    return vue.openBlock(), vue.createElementBlock("view", { class: "chat-page" }, [
      vue.createVNode(_component_zhouWei_navBar, {
        type: "fixed",
        backState: 1e3,
        homeState: 2e3,
        bgColor: "rgba(255,255,255,0.8)",
        fontColor: "#000000",
        shadow: false
      }, {
        left: vue.withCtx(() => [
          vue.createElementVNode("view", {
            class: "nav-back-pill nav-back-pill--offset",
            onClick: $setup.goBack,
            "aria-label": "返回"
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "left",
              size: "22",
              color: "#000"
            })
          ])
        ]),
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "nav-center" }, [
            vue.createElementVNode(
              "text",
              { class: "nav-title-ellipsis" },
              vue.toDisplayString($setup.peerInfo.user_name || "私信"),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "text",
              { class: "nav-sub" },
              vue.toDisplayString($setup.onlineText),
              1
              /* TEXT */
            )
          ])
        ]),
        _: 1
        /* STABLE */
      }),
      vue.createElementVNode("scroll-view", {
        class: "chat-body",
        "scroll-y": true,
        "scroll-with-animation": true,
        "scroll-into-view": $setup.scrollIntoId,
        "upper-threshold": 100,
        onScrolltoupper: $setup.loadMore,
        style: vue.normalizeStyle({ top: $setup.headerOffsetPx, bottom: $setup.bodyBottomOffset })
      }, [
        $setup.hasMore && !$setup.loadingMore ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "load-more",
          onClick: $setup.loadMore
        }, "下拉加载更多")) : vue.createCommentVNode("v-if", true),
        $setup.loadingMore ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "load-more"
        }, [
          vue.createVNode(_component_loading_jump_text)
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "msg-list" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.messages, (m2, idx) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: m2.local_key || m2.id || idx,
                class: vue.normalizeClass(["msg-item", { self: m2.from_uid === $setup.selfUid }]),
                id: "msg-" + (m2.local_key || m2.id || idx)
              }, [
                vue.createElementVNode("image", {
                  class: "avatar",
                  src: m2.from_uid === $setup.selfUid ? $setup.selfInfo.avatar : $setup.peerInfo.avatar
                }, null, 8, ["src"]),
                vue.createElementVNode("view", { class: "content" }, [
                  vue.createElementVNode("view", { class: "bubble" }, [
                    m2.kind === "text" ? (vue.openBlock(), vue.createElementBlock("rich-text", {
                      key: 0,
                      nodes: $setup.safeText(m2.text)
                    }, null, 8, ["nodes"])) : m2.kind === "emoji" ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 1,
                        class: "emoji"
                      },
                      vue.toDisplayString(m2.emoji),
                      1
                      /* TEXT */
                    )) : m2.kind === "image" ? (vue.openBlock(), vue.createElementBlock("image", {
                      key: 2,
                      class: "img-msg",
                      src: m2.url,
                      mode: "widthFix",
                      onClick: ($event) => $setup.previewImage(m2.url)
                    }, null, 8, ["src", "onClick"])) : m2.kind === "other" && (m2.card || m2.payload && m2.payload.card) ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 3,
                      class: "msg-card",
                      onClick: ($event) => $setup.handleCardClick(m2)
                    }, [
                      vue.createElementVNode("view", { class: "msg-card-header" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "msg-card-tag" },
                          vue.toDisplayString($setup.cardTag(m2)),
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          { class: "msg-card-title" },
                          vue.toDisplayString($setup.cardTitle(m2)),
                          1
                          /* TEXT */
                        )
                      ]),
                      $setup.cardImage(m2) ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "msg-card-image-wrap"
                      }, [
                        vue.createElementVNode("image", {
                          class: "msg-card-image",
                          src: $setup.cardImage(m2),
                          mode: "aspectFill",
                          onClick: vue.withModifiers(($event) => $setup.previewImage($setup.cardImage(m2)), ["stop"])
                        }, null, 8, ["src", "onClick"])
                      ])) : vue.createCommentVNode("v-if", true),
                      $setup.cardDescription(m2) ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "msg-card-body"
                      }, [
                        vue.createElementVNode(
                          "text",
                          { class: "msg-card-desc" },
                          vue.toDisplayString($setup.cardDescription(m2)),
                          1
                          /* TEXT */
                        )
                      ])) : vue.createCommentVNode("v-if", true),
                      vue.createElementVNode("view", { class: "msg-card-footer" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "msg-card-action font-alimamashuhei" },
                          vue.toDisplayString($setup.cardActionText(m2)),
                          1
                          /* TEXT */
                        )
                      ])
                    ], 8, ["onClick"])) : m2.kind === "other" ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 4,
                      class: "other-card"
                    }, [
                      vue.createElementVNode(
                        "text",
                        { class: "title" },
                        "[" + vue.toDisplayString(m2.sub_type || "其它") + "]",
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "desc" },
                        vue.toDisplayString($setup.briefOther(m2)),
                        1
                        /* TEXT */
                      )
                    ])) : (vue.openBlock(), vue.createElementBlock("view", {
                      key: 5,
                      class: "other-card"
                    }, [
                      vue.createElementVNode("text", { class: "title" }, "[未知]"),
                      vue.createElementVNode("text", { class: "desc" }, "[暂不支持的消息类型]")
                    ]))
                  ]),
                  vue.createElementVNode("view", { class: "meta font-alimamashuhei" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "time" },
                      vue.toDisplayString($setup.fmtTime(m2.ts)),
                      1
                      /* TEXT */
                    ),
                    m2.from_uid === $setup.selfUid ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "status"
                      },
                      vue.toDisplayString($setup.statusText(m2)),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ])
                ])
              ], 10, ["id"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ]),
        vue.createElementVNode("view", {
          id: $setup.bottomAnchorId,
          class: "bottom-anchor"
        })
      ], 44, ["scroll-into-view"]),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["chat-inputbar", { "with-emoji": $setup.showEmoji }])
        },
        [
          vue.createElementVNode("view", { class: "tools" }, [
            vue.createVNode(_component_uni_icons, {
              type: "image",
              size: "24",
              color: "#666",
              onClick: $setup.pickAndSendImage
            }),
            vue.createVNode(_component_uni_icons, {
              type: "smile",
              size: "24",
              color: "#666",
              onClick: $setup.toggleEmoji
            }),
            vue.createVNode(_component_uni_icons, {
              type: "paperplane",
              size: "24",
              color: "#666",
              onClick: $setup.sendSampleOther
            })
          ]),
          vue.withDirectives(vue.createElementVNode("input", {
            class: "text-input",
            type: "text",
            "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.draft = $event),
            "confirm-type": "send",
            disabled: $setup.isBlocked,
            onConfirm: $setup.sendText,
            onFocus: $setup.scrollToBottomSoon
          }, null, 40, ["disabled"]), [
            [
              vue.vModelText,
              $setup.draft,
              void 0,
              { trim: true }
            ]
          ]),
          $setup.showEmoji ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "emoji-panel"
          }, [
            vue.createElementVNode("view", { class: "emoji-row" }, [
              (vue.openBlock(), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.emojis, (e2) => {
                  return vue.createElementVNode("text", {
                    key: e2,
                    class: "emoji-item",
                    onClick: ($event) => $setup.sendEmoji(e2)
                  }, vue.toDisplayString(e2), 9, ["onClick"]);
                }),
                64
                /* STABLE_FRAGMENT */
              ))
            ])
          ])) : vue.createCommentVNode("v-if", true)
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode(
        "view",
        {
          class: "chat-safe-bottom",
          style: vue.normalizeStyle({ height: $setup.footerSafePx })
        },
        null,
        4
        /* STYLE */
      )
    ]);
  }
  const PkgImChatChat = /* @__PURE__ */ _export_sfc(_sfc_main$y, [["render", _sfc_render$x], ["__scopeId", "data-v-19791113"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-im/chat/chat.vue"]]);
  const pages = [
    {
      path: "pages/index/index",
      style: {
        navigationBarTitleText: "首页",
        navigationStyle: "custom",
        enablePullDownRefresh: false
      }
    },
    {
      path: "pages/homepage/homepage",
      style: {
        navigationBarTitleText: "home",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/stock/stock",
      style: {
        navigationBarTitleText: "我的娃柜",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/mine/mine",
      style: {
        navigationBarTitleText: "个人信息",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/watch_demo/watch_demo",
      style: {
        navigationBarTitleText: "watch_demo"
      }
    },
    {
      path: "pages/goods/goods",
      style: {
        navigationBarTitleText: "商品页面",
        "app-plus": {
          softinputmode: "adjustResize"
        },
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/brand/brand",
      style: {
        navigationBarTitleText: "品牌页面"
      }
    },
    {
      path: "pages/calendar/calendar",
      style: {
        navigationBarTitleText: "上新",
        navigationStyle: "custom",
        enablePullDownRefresh: true
      }
    },
    {
      path: "pages/summary/summary",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom",
        enablePullDownRefresh: true
      }
    },
    {
      path: "pages/pop_croper/pop_croper",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/setting/setting",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/setting/password/password",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/setting/tel_phone/tel_phone",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/collocation/collocation",
      style: {
        navigationBarTitleText: "分享搭配"
      }
    },
    {
      path: "pages/collocation_share/collocation_share",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/user_page/user_page",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/message_list/message_list",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/message_info/message_info",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/register/register",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/user_like/user_like",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/collocation_square/collocation_square",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/sale_news/sale_news",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/treehole_publish/treehole_publish",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/treehole_detail/treehole_detail",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/reset_password/reset_password",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/setting/username/username",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/my_comment/my_comment",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/my_collocation/my_collocation",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/article_detail/article_detail",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/loadding/loadding",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/private/private",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/permission/permission",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/drag-image-test/drag-image-test",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/artist_info/artist_info",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/artwork/artwork",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/recent-feed/recent-feed",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pages/search/search",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    },
    {
      path: "pages/artist_info/bjd_faceup_artist",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    },
    {
      path: "pkg-im/message_list/message_list",
      style: {
        navigationBarTitleText: ""
      }
    },
    {
      path: "pkg-im/chat/chat",
      style: {
        navigationBarTitleText: "",
        navigationStyle: "custom"
      }
    }
  ];
  const subPackages = [
    {
      root: "pkg-stock",
      pages: [
        {
          path: "stock/account_book_form/account_book_form"
        },
        {
          path: "stock/showcase_form/showcase_form"
        },
        {
          path: "stock/bill_form/bill_form"
        },
        {
          path: "account_book_preview/account_book_preview",
          style: {
            navigationStyle: "custom"
          }
        },
        {
          path: "stock-myitems-search/stock-myitems-search",
          style: {
            navigationStyle: "custom"
          }
        }
      ]
    },
    {
      root: "pkg-creator",
      pages: [
        {
          path: "creator_base/creator_base"
        },
        {
          path: "creator_base/brand_edit/brand_edit"
        },
        {
          path: "creator_base/goods_list/goods_list"
        },
        {
          path: "creator_base/goods_editor/goods_editor"
        },
        {
          path: "creator_base/collocation_list/collocation_list"
        },
        {
          path: "creator_base/artist_info/artist_info"
        },
        {
          path: "creator_base/artist_setting/artist_setting"
        },
        {
          path: "creator_base/set_showcase/set_showcase"
        },
        {
          path: "creator_base/faceup_editor/faceup_editor"
        },
        {
          path: "creator_base/order_plane/order_plane"
        },
        {
          path: "creator_base/news/news"
        },
        {
          path: "creator_base/set_hair_showcase/set_hair_showcase"
        },
        {
          path: "creator_base/order_plane/form-artist"
        },
        {
          path: "creator_base/order_plane/form-wig"
        },
        {
          path: "creator_order/plane_detail/plane_detail",
          style: {
            navigationStyle: "custom"
          }
        },
        {
          path: "creator_order/my_submissions/my_submissions",
          style: {
            navigationBarTitleText: ""
          }
        },
        {
          path: "creator_order/submission_detail/submission_detail",
          style: {
            navigationBarTitleText: ""
          }
        },
        {
          path: "creator_order/submission_item_form/submission_item_form",
          style: {
            navigationBarTitleText: ""
          }
        },
        {
          path: "creator_order/received-list/received-list",
          style: {
            navigationBarTitleText: ""
          }
        },
        {
          path: "creator_order/received-detail/received-detail",
          style: {
            navigationBarTitleText: ""
          }
        }
      ]
    },
    {
      root: "pkg-common",
      pages: [
        {
          path: "brand-pick/brand-pick",
          style: {
            navigationBarTitleText: "请选择品牌"
          }
        },
        {
          path: "goods-pick/goods-pick",
          style: {
            navigationBarTitleText: "请选择商品"
          }
        }
      ]
    }
  ];
  const preloadRule = {
    "pages/stock/stock": {
      packages: [
        "pkg-stock"
      ],
      network: "all"
    },
    "pages/mine/mine": {
      packages: [
        "pkg-creator"
      ],
      network: "all"
    }
  };
  const globalStyle = {
    navigationBarTextStyle: "black",
    navigationBarTitleText: "",
    navigationBarBackgroundColor: "#F8F8F8",
    backgroundColor: "#F8F8F8",
    rpxCalcMaxDeviceWidth: 750,
    rpxCalcBaseDeviceWidth: 375,
    rpxCalcIncludeWidth: 750
  };
  const tabBar = {
    selectedColor: "#7cc1d2",
    backgroundColor: "#fefefe",
    borderStyle: "white",
    list: [
      {
        pagePath: "pages/index/index",
        text: "百科",
        iconPath: "static/new-icon/tab1.png",
        selectedIconPath: "static/new-icon/tab1.png"
      },
      {
        pagePath: "pages/collocation_square/collocation_square",
        text: "广场",
        iconPath: "static/new-icon/tab4.png",
        selectedIconPath: "static/new-icon/tab4.png"
      },
      {
        pagePath: "pages/summary/summary",
        text: "上新",
        iconPath: "static/new-icon/tab2.png",
        selectedIconPath: "static/new-icon/tab2.png"
      },
      {
        pagePath: "pages/stock/stock",
        text: "娃柜",
        iconPath: "static/new-icon/tab3.png",
        selectedIconPath: "static/new-icon/tab3.png"
      },
      {
        pagePath: "pages/mine/mine",
        text: "我的",
        iconPath: "static/new-icon/tab5.png",
        selectedIconPath: "static/new-icon/tab5.png"
      }
    ]
  };
  const uniIdRouter = {};
  const e = {
    pages,
    subPackages,
    preloadRule,
    globalStyle,
    tabBar,
    uniIdRouter,
    "app-plus": {
      popGesture: "close"
    },
    "mp-weixin": {
      disableSwipeBack: false
    }
  };
  var define_process_env_UNI_SECURE_NETWORK_CONFIG_default = [];
  function t(e2) {
    return e2 && e2.__esModule && Object.prototype.hasOwnProperty.call(e2, "default") ? e2.default : e2;
  }
  function n(e2, t2, n2) {
    return e2(n2 = { path: t2, exports: {}, require: function(e3, t3) {
      return function() {
        throw new Error("Dynamic requires are not currently supported by @rollup/plugin-commonjs");
      }(null == t3 && n2.path);
    } }, n2.exports), n2.exports;
  }
  var s = n(function(e2, t2) {
    var n2;
    e2.exports = (n2 = n2 || function(e3, t3) {
      var n3 = Object.create || /* @__PURE__ */ function() {
        function e4() {
        }
        return function(t4) {
          var n4;
          return e4.prototype = t4, n4 = new e4(), e4.prototype = null, n4;
        };
      }(), s2 = {}, r2 = s2.lib = {}, i2 = r2.Base = { extend: function(e4) {
        var t4 = n3(this);
        return e4 && t4.mixIn(e4), t4.hasOwnProperty("init") && this.init !== t4.init || (t4.init = function() {
          t4.$super.init.apply(this, arguments);
        }), t4.init.prototype = t4, t4.$super = this, t4;
      }, create: function() {
        var e4 = this.extend();
        return e4.init.apply(e4, arguments), e4;
      }, init: function() {
      }, mixIn: function(e4) {
        for (var t4 in e4)
          e4.hasOwnProperty(t4) && (this[t4] = e4[t4]);
        e4.hasOwnProperty("toString") && (this.toString = e4.toString);
      }, clone: function() {
        return this.init.prototype.extend(this);
      } }, o2 = r2.WordArray = i2.extend({ init: function(e4, n4) {
        e4 = this.words = e4 || [], this.sigBytes = n4 != t3 ? n4 : 4 * e4.length;
      }, toString: function(e4) {
        return (e4 || c2).stringify(this);
      }, concat: function(e4) {
        var t4 = this.words, n4 = e4.words, s3 = this.sigBytes, r3 = e4.sigBytes;
        if (this.clamp(), s3 % 4)
          for (var i3 = 0; i3 < r3; i3++) {
            var o3 = n4[i3 >>> 2] >>> 24 - i3 % 4 * 8 & 255;
            t4[s3 + i3 >>> 2] |= o3 << 24 - (s3 + i3) % 4 * 8;
          }
        else
          for (i3 = 0; i3 < r3; i3 += 4)
            t4[s3 + i3 >>> 2] = n4[i3 >>> 2];
        return this.sigBytes += r3, this;
      }, clamp: function() {
        var t4 = this.words, n4 = this.sigBytes;
        t4[n4 >>> 2] &= 4294967295 << 32 - n4 % 4 * 8, t4.length = e3.ceil(n4 / 4);
      }, clone: function() {
        var e4 = i2.clone.call(this);
        return e4.words = this.words.slice(0), e4;
      }, random: function(t4) {
        for (var n4, s3 = [], r3 = function(t5) {
          var n5 = 987654321, s4 = 4294967295;
          return function() {
            var r4 = ((n5 = 36969 * (65535 & n5) + (n5 >> 16) & s4) << 16) + (t5 = 18e3 * (65535 & t5) + (t5 >> 16) & s4) & s4;
            return r4 /= 4294967296, (r4 += 0.5) * (e3.random() > 0.5 ? 1 : -1);
          };
        }, i3 = 0; i3 < t4; i3 += 4) {
          var a3 = r3(4294967296 * (n4 || e3.random()));
          n4 = 987654071 * a3(), s3.push(4294967296 * a3() | 0);
        }
        return new o2.init(s3, t4);
      } }), a2 = s2.enc = {}, c2 = a2.Hex = { stringify: function(e4) {
        for (var t4 = e4.words, n4 = e4.sigBytes, s3 = [], r3 = 0; r3 < n4; r3++) {
          var i3 = t4[r3 >>> 2] >>> 24 - r3 % 4 * 8 & 255;
          s3.push((i3 >>> 4).toString(16)), s3.push((15 & i3).toString(16));
        }
        return s3.join("");
      }, parse: function(e4) {
        for (var t4 = e4.length, n4 = [], s3 = 0; s3 < t4; s3 += 2)
          n4[s3 >>> 3] |= parseInt(e4.substr(s3, 2), 16) << 24 - s3 % 8 * 4;
        return new o2.init(n4, t4 / 2);
      } }, u2 = a2.Latin1 = { stringify: function(e4) {
        for (var t4 = e4.words, n4 = e4.sigBytes, s3 = [], r3 = 0; r3 < n4; r3++) {
          var i3 = t4[r3 >>> 2] >>> 24 - r3 % 4 * 8 & 255;
          s3.push(String.fromCharCode(i3));
        }
        return s3.join("");
      }, parse: function(e4) {
        for (var t4 = e4.length, n4 = [], s3 = 0; s3 < t4; s3++)
          n4[s3 >>> 2] |= (255 & e4.charCodeAt(s3)) << 24 - s3 % 4 * 8;
        return new o2.init(n4, t4);
      } }, h2 = a2.Utf8 = { stringify: function(e4) {
        try {
          return decodeURIComponent(escape(u2.stringify(e4)));
        } catch (e5) {
          throw new Error("Malformed UTF-8 data");
        }
      }, parse: function(e4) {
        return u2.parse(unescape(encodeURIComponent(e4)));
      } }, l2 = r2.BufferedBlockAlgorithm = i2.extend({ reset: function() {
        this._data = new o2.init(), this._nDataBytes = 0;
      }, _append: function(e4) {
        "string" == typeof e4 && (e4 = h2.parse(e4)), this._data.concat(e4), this._nDataBytes += e4.sigBytes;
      }, _process: function(t4) {
        var n4 = this._data, s3 = n4.words, r3 = n4.sigBytes, i3 = this.blockSize, a3 = r3 / (4 * i3), c3 = (a3 = t4 ? e3.ceil(a3) : e3.max((0 | a3) - this._minBufferSize, 0)) * i3, u3 = e3.min(4 * c3, r3);
        if (c3) {
          for (var h3 = 0; h3 < c3; h3 += i3)
            this._doProcessBlock(s3, h3);
          var l3 = s3.splice(0, c3);
          n4.sigBytes -= u3;
        }
        return new o2.init(l3, u3);
      }, clone: function() {
        var e4 = i2.clone.call(this);
        return e4._data = this._data.clone(), e4;
      }, _minBufferSize: 0 });
      r2.Hasher = l2.extend({ cfg: i2.extend(), init: function(e4) {
        this.cfg = this.cfg.extend(e4), this.reset();
      }, reset: function() {
        l2.reset.call(this), this._doReset();
      }, update: function(e4) {
        return this._append(e4), this._process(), this;
      }, finalize: function(e4) {
        return e4 && this._append(e4), this._doFinalize();
      }, blockSize: 16, _createHelper: function(e4) {
        return function(t4, n4) {
          return new e4.init(n4).finalize(t4);
        };
      }, _createHmacHelper: function(e4) {
        return function(t4, n4) {
          return new d2.HMAC.init(e4, n4).finalize(t4);
        };
      } });
      var d2 = s2.algo = {};
      return s2;
    }(Math), n2);
  }), r = s, i = (n(function(e2, t2) {
    var n2;
    e2.exports = (n2 = r, function(e3) {
      var t3 = n2, s2 = t3.lib, r2 = s2.WordArray, i2 = s2.Hasher, o2 = t3.algo, a2 = [];
      !function() {
        for (var t4 = 0; t4 < 64; t4++)
          a2[t4] = 4294967296 * e3.abs(e3.sin(t4 + 1)) | 0;
      }();
      var c2 = o2.MD5 = i2.extend({ _doReset: function() {
        this._hash = new r2.init([1732584193, 4023233417, 2562383102, 271733878]);
      }, _doProcessBlock: function(e4, t4) {
        for (var n3 = 0; n3 < 16; n3++) {
          var s3 = t4 + n3, r3 = e4[s3];
          e4[s3] = 16711935 & (r3 << 8 | r3 >>> 24) | 4278255360 & (r3 << 24 | r3 >>> 8);
        }
        var i3 = this._hash.words, o3 = e4[t4 + 0], c3 = e4[t4 + 1], p2 = e4[t4 + 2], f2 = e4[t4 + 3], g2 = e4[t4 + 4], m2 = e4[t4 + 5], y2 = e4[t4 + 6], _2 = e4[t4 + 7], w2 = e4[t4 + 8], I2 = e4[t4 + 9], v2 = e4[t4 + 10], S2 = e4[t4 + 11], T2 = e4[t4 + 12], b2 = e4[t4 + 13], E2 = e4[t4 + 14], k2 = e4[t4 + 15], A2 = i3[0], P2 = i3[1], C2 = i3[2], O2 = i3[3];
        A2 = u2(A2, P2, C2, O2, o3, 7, a2[0]), O2 = u2(O2, A2, P2, C2, c3, 12, a2[1]), C2 = u2(C2, O2, A2, P2, p2, 17, a2[2]), P2 = u2(P2, C2, O2, A2, f2, 22, a2[3]), A2 = u2(A2, P2, C2, O2, g2, 7, a2[4]), O2 = u2(O2, A2, P2, C2, m2, 12, a2[5]), C2 = u2(C2, O2, A2, P2, y2, 17, a2[6]), P2 = u2(P2, C2, O2, A2, _2, 22, a2[7]), A2 = u2(A2, P2, C2, O2, w2, 7, a2[8]), O2 = u2(O2, A2, P2, C2, I2, 12, a2[9]), C2 = u2(C2, O2, A2, P2, v2, 17, a2[10]), P2 = u2(P2, C2, O2, A2, S2, 22, a2[11]), A2 = u2(A2, P2, C2, O2, T2, 7, a2[12]), O2 = u2(O2, A2, P2, C2, b2, 12, a2[13]), C2 = u2(C2, O2, A2, P2, E2, 17, a2[14]), A2 = h2(A2, P2 = u2(P2, C2, O2, A2, k2, 22, a2[15]), C2, O2, c3, 5, a2[16]), O2 = h2(O2, A2, P2, C2, y2, 9, a2[17]), C2 = h2(C2, O2, A2, P2, S2, 14, a2[18]), P2 = h2(P2, C2, O2, A2, o3, 20, a2[19]), A2 = h2(A2, P2, C2, O2, m2, 5, a2[20]), O2 = h2(O2, A2, P2, C2, v2, 9, a2[21]), C2 = h2(C2, O2, A2, P2, k2, 14, a2[22]), P2 = h2(P2, C2, O2, A2, g2, 20, a2[23]), A2 = h2(A2, P2, C2, O2, I2, 5, a2[24]), O2 = h2(O2, A2, P2, C2, E2, 9, a2[25]), C2 = h2(C2, O2, A2, P2, f2, 14, a2[26]), P2 = h2(P2, C2, O2, A2, w2, 20, a2[27]), A2 = h2(A2, P2, C2, O2, b2, 5, a2[28]), O2 = h2(O2, A2, P2, C2, p2, 9, a2[29]), C2 = h2(C2, O2, A2, P2, _2, 14, a2[30]), A2 = l2(A2, P2 = h2(P2, C2, O2, A2, T2, 20, a2[31]), C2, O2, m2, 4, a2[32]), O2 = l2(O2, A2, P2, C2, w2, 11, a2[33]), C2 = l2(C2, O2, A2, P2, S2, 16, a2[34]), P2 = l2(P2, C2, O2, A2, E2, 23, a2[35]), A2 = l2(A2, P2, C2, O2, c3, 4, a2[36]), O2 = l2(O2, A2, P2, C2, g2, 11, a2[37]), C2 = l2(C2, O2, A2, P2, _2, 16, a2[38]), P2 = l2(P2, C2, O2, A2, v2, 23, a2[39]), A2 = l2(A2, P2, C2, O2, b2, 4, a2[40]), O2 = l2(O2, A2, P2, C2, o3, 11, a2[41]), C2 = l2(C2, O2, A2, P2, f2, 16, a2[42]), P2 = l2(P2, C2, O2, A2, y2, 23, a2[43]), A2 = l2(A2, P2, C2, O2, I2, 4, a2[44]), O2 = l2(O2, A2, P2, C2, T2, 11, a2[45]), C2 = l2(C2, O2, A2, P2, k2, 16, a2[46]), A2 = d2(A2, P2 = l2(P2, C2, O2, A2, p2, 23, a2[47]), C2, O2, o3, 6, a2[48]), O2 = d2(O2, A2, P2, C2, _2, 10, a2[49]), C2 = d2(C2, O2, A2, P2, E2, 15, a2[50]), P2 = d2(P2, C2, O2, A2, m2, 21, a2[51]), A2 = d2(A2, P2, C2, O2, T2, 6, a2[52]), O2 = d2(O2, A2, P2, C2, f2, 10, a2[53]), C2 = d2(C2, O2, A2, P2, v2, 15, a2[54]), P2 = d2(P2, C2, O2, A2, c3, 21, a2[55]), A2 = d2(A2, P2, C2, O2, w2, 6, a2[56]), O2 = d2(O2, A2, P2, C2, k2, 10, a2[57]), C2 = d2(C2, O2, A2, P2, y2, 15, a2[58]), P2 = d2(P2, C2, O2, A2, b2, 21, a2[59]), A2 = d2(A2, P2, C2, O2, g2, 6, a2[60]), O2 = d2(O2, A2, P2, C2, S2, 10, a2[61]), C2 = d2(C2, O2, A2, P2, p2, 15, a2[62]), P2 = d2(P2, C2, O2, A2, I2, 21, a2[63]), i3[0] = i3[0] + A2 | 0, i3[1] = i3[1] + P2 | 0, i3[2] = i3[2] + C2 | 0, i3[3] = i3[3] + O2 | 0;
      }, _doFinalize: function() {
        var t4 = this._data, n3 = t4.words, s3 = 8 * this._nDataBytes, r3 = 8 * t4.sigBytes;
        n3[r3 >>> 5] |= 128 << 24 - r3 % 32;
        var i3 = e3.floor(s3 / 4294967296), o3 = s3;
        n3[15 + (r3 + 64 >>> 9 << 4)] = 16711935 & (i3 << 8 | i3 >>> 24) | 4278255360 & (i3 << 24 | i3 >>> 8), n3[14 + (r3 + 64 >>> 9 << 4)] = 16711935 & (o3 << 8 | o3 >>> 24) | 4278255360 & (o3 << 24 | o3 >>> 8), t4.sigBytes = 4 * (n3.length + 1), this._process();
        for (var a3 = this._hash, c3 = a3.words, u3 = 0; u3 < 4; u3++) {
          var h3 = c3[u3];
          c3[u3] = 16711935 & (h3 << 8 | h3 >>> 24) | 4278255360 & (h3 << 24 | h3 >>> 8);
        }
        return a3;
      }, clone: function() {
        var e4 = i2.clone.call(this);
        return e4._hash = this._hash.clone(), e4;
      } });
      function u2(e4, t4, n3, s3, r3, i3, o3) {
        var a3 = e4 + (t4 & n3 | ~t4 & s3) + r3 + o3;
        return (a3 << i3 | a3 >>> 32 - i3) + t4;
      }
      function h2(e4, t4, n3, s3, r3, i3, o3) {
        var a3 = e4 + (t4 & s3 | n3 & ~s3) + r3 + o3;
        return (a3 << i3 | a3 >>> 32 - i3) + t4;
      }
      function l2(e4, t4, n3, s3, r3, i3, o3) {
        var a3 = e4 + (t4 ^ n3 ^ s3) + r3 + o3;
        return (a3 << i3 | a3 >>> 32 - i3) + t4;
      }
      function d2(e4, t4, n3, s3, r3, i3, o3) {
        var a3 = e4 + (n3 ^ (t4 | ~s3)) + r3 + o3;
        return (a3 << i3 | a3 >>> 32 - i3) + t4;
      }
      t3.MD5 = i2._createHelper(c2), t3.HmacMD5 = i2._createHmacHelper(c2);
    }(Math), n2.MD5);
  }), n(function(e2, t2) {
    var n2;
    e2.exports = (n2 = r, void function() {
      var e3 = n2, t3 = e3.lib.Base, s2 = e3.enc.Utf8;
      e3.algo.HMAC = t3.extend({ init: function(e4, t4) {
        e4 = this._hasher = new e4.init(), "string" == typeof t4 && (t4 = s2.parse(t4));
        var n3 = e4.blockSize, r2 = 4 * n3;
        t4.sigBytes > r2 && (t4 = e4.finalize(t4)), t4.clamp();
        for (var i2 = this._oKey = t4.clone(), o2 = this._iKey = t4.clone(), a2 = i2.words, c2 = o2.words, u2 = 0; u2 < n3; u2++)
          a2[u2] ^= 1549556828, c2[u2] ^= 909522486;
        i2.sigBytes = o2.sigBytes = r2, this.reset();
      }, reset: function() {
        var e4 = this._hasher;
        e4.reset(), e4.update(this._iKey);
      }, update: function(e4) {
        return this._hasher.update(e4), this;
      }, finalize: function(e4) {
        var t4 = this._hasher, n3 = t4.finalize(e4);
        return t4.reset(), t4.finalize(this._oKey.clone().concat(n3));
      } });
    }());
  }), n(function(e2, t2) {
    e2.exports = r.HmacMD5;
  })), o = n(function(e2, t2) {
    e2.exports = r.enc.Utf8;
  }), a = n(function(e2, t2) {
    var n2;
    e2.exports = (n2 = r, function() {
      var e3 = n2, t3 = e3.lib.WordArray;
      function s2(e4, n3, s3) {
        for (var r2 = [], i2 = 0, o2 = 0; o2 < n3; o2++)
          if (o2 % 4) {
            var a2 = s3[e4.charCodeAt(o2 - 1)] << o2 % 4 * 2, c2 = s3[e4.charCodeAt(o2)] >>> 6 - o2 % 4 * 2;
            r2[i2 >>> 2] |= (a2 | c2) << 24 - i2 % 4 * 8, i2++;
          }
        return t3.create(r2, i2);
      }
      e3.enc.Base64 = { stringify: function(e4) {
        var t4 = e4.words, n3 = e4.sigBytes, s3 = this._map;
        e4.clamp();
        for (var r2 = [], i2 = 0; i2 < n3; i2 += 3)
          for (var o2 = (t4[i2 >>> 2] >>> 24 - i2 % 4 * 8 & 255) << 16 | (t4[i2 + 1 >>> 2] >>> 24 - (i2 + 1) % 4 * 8 & 255) << 8 | t4[i2 + 2 >>> 2] >>> 24 - (i2 + 2) % 4 * 8 & 255, a2 = 0; a2 < 4 && i2 + 0.75 * a2 < n3; a2++)
            r2.push(s3.charAt(o2 >>> 6 * (3 - a2) & 63));
        var c2 = s3.charAt(64);
        if (c2)
          for (; r2.length % 4; )
            r2.push(c2);
        return r2.join("");
      }, parse: function(e4) {
        var t4 = e4.length, n3 = this._map, r2 = this._reverseMap;
        if (!r2) {
          r2 = this._reverseMap = [];
          for (var i2 = 0; i2 < n3.length; i2++)
            r2[n3.charCodeAt(i2)] = i2;
        }
        var o2 = n3.charAt(64);
        if (o2) {
          var a2 = e4.indexOf(o2);
          -1 !== a2 && (t4 = a2);
        }
        return s2(e4, t4, r2);
      }, _map: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=" };
    }(), n2.enc.Base64);
  });
  const c = "uni_id_token", u = "uni_id_token_expired", h = "uniIdToken", l = { DEFAULT: "FUNCTION", FUNCTION: "FUNCTION", OBJECT: "OBJECT", CLIENT_DB: "CLIENT_DB" }, d = "pending", p = "fulfilled", f = "rejected";
  function g(e2) {
    return Object.prototype.toString.call(e2).slice(8, -1).toLowerCase();
  }
  function m(e2) {
    return "object" === g(e2);
  }
  function y(e2) {
    return "function" == typeof e2;
  }
  function _(e2) {
    return function() {
      try {
        return e2.apply(e2, arguments);
      } catch (e3) {
        console.error(e3);
      }
    };
  }
  const w = "REJECTED", I = "NOT_PENDING";
  class v {
    constructor({ createPromise: e2, retryRule: t2 = w } = {}) {
      this.createPromise = e2, this.status = null, this.promise = null, this.retryRule = t2;
    }
    get needRetry() {
      if (!this.status)
        return true;
      switch (this.retryRule) {
        case w:
          return this.status === f;
        case I:
          return this.status !== d;
      }
    }
    exec() {
      return this.needRetry ? (this.status = d, this.promise = this.createPromise().then((e2) => (this.status = p, Promise.resolve(e2)), (e2) => (this.status = f, Promise.reject(e2))), this.promise) : this.promise;
    }
  }
  class S {
    constructor() {
      this._callback = {};
    }
    addListener(e2, t2) {
      this._callback[e2] || (this._callback[e2] = []), this._callback[e2].push(t2);
    }
    on(e2, t2) {
      return this.addListener(e2, t2);
    }
    removeListener(e2, t2) {
      if (!t2)
        throw new Error('The "listener" argument must be of type function. Received undefined');
      const n2 = this._callback[e2];
      if (!n2)
        return;
      const s2 = function(e3, t3) {
        for (let n3 = e3.length - 1; n3 >= 0; n3--)
          if (e3[n3] === t3)
            return n3;
        return -1;
      }(n2, t2);
      n2.splice(s2, 1);
    }
    off(e2, t2) {
      return this.removeListener(e2, t2);
    }
    removeAllListener(e2) {
      delete this._callback[e2];
    }
    emit(e2, ...t2) {
      const n2 = this._callback[e2];
      if (n2)
        for (let e3 = 0; e3 < n2.length; e3++)
          n2[e3](...t2);
    }
  }
  function T(e2) {
    return e2 && "string" == typeof e2 ? JSON.parse(e2) : e2;
  }
  const b = true, E = "app", A = T(define_process_env_UNI_SECURE_NETWORK_CONFIG_default), P = E, C = T(""), O = T("[]") || [];
  let N = "";
  try {
    N = "__UNI__5D7AAEE";
  } catch (e2) {
  }
  let R, L = {};
  function U(e2, t2 = {}) {
    var n2, s2;
    return n2 = L, s2 = e2, Object.prototype.hasOwnProperty.call(n2, s2) || (L[e2] = t2), L[e2];
  }
  function D() {
    return R || (R = function() {
      if ("undefined" != typeof globalThis)
        return globalThis;
      if ("undefined" != typeof self)
        return self;
      if ("undefined" != typeof window)
        return window;
      function e2() {
        return this;
      }
      return void 0 !== e2() ? e2() : new Function("return this")();
    }(), R);
  }
  L = uni._globalUniCloudObj ? uni._globalUniCloudObj : uni._globalUniCloudObj = {};
  const M = ["invoke", "success", "fail", "complete"], q = U("_globalUniCloudInterceptor");
  function F(e2, t2) {
    q[e2] || (q[e2] = {}), m(t2) && Object.keys(t2).forEach((n2) => {
      M.indexOf(n2) > -1 && function(e3, t3, n3) {
        let s2 = q[e3][t3];
        s2 || (s2 = q[e3][t3] = []), -1 === s2.indexOf(n3) && y(n3) && s2.push(n3);
      }(e2, n2, t2[n2]);
    });
  }
  function K(e2, t2) {
    q[e2] || (q[e2] = {}), m(t2) ? Object.keys(t2).forEach((n2) => {
      M.indexOf(n2) > -1 && function(e3, t3, n3) {
        const s2 = q[e3][t3];
        if (!s2)
          return;
        const r2 = s2.indexOf(n3);
        r2 > -1 && s2.splice(r2, 1);
      }(e2, n2, t2[n2]);
    }) : delete q[e2];
  }
  function j(e2, t2) {
    return e2 && 0 !== e2.length ? e2.reduce((e3, n2) => e3.then(() => n2(t2)), Promise.resolve()) : Promise.resolve();
  }
  function $(e2, t2) {
    return q[e2] && q[e2][t2] || [];
  }
  function B(e2) {
    F("callObject", e2);
  }
  const W = U("_globalUniCloudListener"), H = { RESPONSE: "response", NEED_LOGIN: "needLogin", REFRESH_TOKEN: "refreshToken" }, J = { CLIENT_DB: "clientdb", CLOUD_FUNCTION: "cloudfunction", CLOUD_OBJECT: "cloudobject" };
  function z(e2) {
    return W[e2] || (W[e2] = []), W[e2];
  }
  function V(e2, t2) {
    const n2 = z(e2);
    n2.includes(t2) || n2.push(t2);
  }
  function G(e2, t2) {
    const n2 = z(e2), s2 = n2.indexOf(t2);
    -1 !== s2 && n2.splice(s2, 1);
  }
  function Y(e2, t2) {
    const n2 = z(e2);
    for (let e3 = 0; e3 < n2.length; e3++) {
      (0, n2[e3])(t2);
    }
  }
  let Q, X = false;
  function Z() {
    return Q || (Q = new Promise((e2) => {
      X && e2(), function t2() {
        if ("function" == typeof getCurrentPages) {
          const t3 = getCurrentPages();
          t3 && t3[0] && (X = true, e2());
        }
        X || setTimeout(() => {
          t2();
        }, 30);
      }();
    }), Q);
  }
  function ee(e2) {
    const t2 = {};
    for (const n2 in e2) {
      const s2 = e2[n2];
      y(s2) && (t2[n2] = _(s2));
    }
    return t2;
  }
  class te extends Error {
    constructor(e2) {
      const t2 = e2.message || e2.errMsg || "unknown system error";
      super(t2), this.errMsg = t2, this.code = this.errCode = e2.code || e2.errCode || "SYSTEM_ERROR", this.errSubject = this.subject = e2.subject || e2.errSubject, this.cause = e2.cause, this.requestId = e2.requestId;
    }
    toJson(e2 = 0) {
      if (!(e2 >= 10))
        return e2++, { errCode: this.errCode, errMsg: this.errMsg, errSubject: this.errSubject, cause: this.cause && this.cause.toJson ? this.cause.toJson(e2) : this.cause };
    }
  }
  var ne = { request: (e2) => uni.request(e2), uploadFile: (e2) => uni.uploadFile(e2), setStorageSync: (e2, t2) => uni.setStorageSync(e2, t2), getStorageSync: (e2) => uni.getStorageSync(e2), removeStorageSync: (e2) => uni.removeStorageSync(e2), clearStorageSync: () => uni.clearStorageSync(), connectSocket: (e2) => uni.connectSocket(e2) };
  function se(e2) {
    return e2 && se(e2.__v_raw) || e2;
  }
  function re() {
    return { token: ne.getStorageSync(c) || ne.getStorageSync(h), tokenExpired: ne.getStorageSync(u) };
  }
  function ie({ token: e2, tokenExpired: t2 } = {}) {
    e2 && ne.setStorageSync(c, e2), t2 && ne.setStorageSync(u, t2);
  }
  let oe, ae;
  function ce() {
    return oe || (oe = uni.getSystemInfoSync()), oe;
  }
  function ue() {
    let e2, t2;
    try {
      if (uni.getLaunchOptionsSync) {
        if (uni.getLaunchOptionsSync.toString().indexOf("not yet implemented") > -1)
          return;
        const { scene: n2, channel: s2 } = uni.getLaunchOptionsSync();
        e2 = s2, t2 = n2;
      }
    } catch (e3) {
    }
    return { channel: e2, scene: t2 };
  }
  let he = {};
  function le() {
    const e2 = uni.getLocale && uni.getLocale() || "en";
    if (ae)
      return { ...he, ...ae, locale: e2, LOCALE: e2 };
    const t2 = ce(), { deviceId: n2, osName: s2, uniPlatform: r2, appId: i2 } = t2, o2 = ["appId", "appLanguage", "appName", "appVersion", "appVersionCode", "appWgtVersion", "browserName", "browserVersion", "deviceBrand", "deviceId", "deviceModel", "deviceType", "osName", "osVersion", "romName", "romVersion", "ua", "hostName", "hostVersion", "uniPlatform", "uniRuntimeVersion", "uniRuntimeVersionCode", "uniCompilerVersion", "uniCompilerVersionCode"];
    for (const e3 in t2)
      Object.hasOwnProperty.call(t2, e3) && -1 === o2.indexOf(e3) && delete t2[e3];
    return ae = { PLATFORM: r2, OS: s2, APPID: i2, DEVICEID: n2, ...ue(), ...t2 }, { ...he, ...ae, locale: e2, LOCALE: e2 };
  }
  var de = { sign: function(e2, t2) {
    let n2 = "";
    return Object.keys(e2).sort().forEach(function(t3) {
      e2[t3] && (n2 = n2 + "&" + t3 + "=" + e2[t3]);
    }), n2 = n2.slice(1), i(n2, t2).toString();
  }, wrappedRequest: function(e2, t2) {
    return new Promise((n2, s2) => {
      t2(Object.assign(e2, { complete(e3) {
        e3 || (e3 = {});
        const t3 = e3.data && e3.data.header && e3.data.header["x-serverless-request-id"] || e3.header && e3.header["request-id"];
        if (!e3.statusCode || e3.statusCode >= 400) {
          const n3 = e3.data && e3.data.error && e3.data.error.code || "SYS_ERR", r3 = e3.data && e3.data.error && e3.data.error.message || e3.errMsg || "request:fail";
          return s2(new te({ code: n3, message: r3, requestId: t3 }));
        }
        const r2 = e3.data;
        if (r2.error)
          return s2(new te({ code: r2.error.code, message: r2.error.message, requestId: t3 }));
        r2.result = r2.data, r2.requestId = t3, delete r2.data, n2(r2);
      } }));
    });
  }, toBase64: function(e2) {
    return a.stringify(o.parse(e2));
  } };
  var pe = class {
    constructor(e2) {
      ["spaceId", "clientSecret"].forEach((t2) => {
        if (!Object.prototype.hasOwnProperty.call(e2, t2))
          throw new Error(`${t2} required`);
      }), this.config = Object.assign({}, { endpoint: 0 === e2.spaceId.indexOf("mp-") ? "https://api.next.bspapp.com" : "https://api.bspapp.com" }, e2), this.config.provider = "aliyun", this.config.requestUrl = this.config.endpoint + "/client", this.config.envType = this.config.envType || "public", this.config.accessTokenKey = "access_token_" + this.config.spaceId, this.adapter = ne, this._getAccessTokenPromiseHub = new v({ createPromise: () => this.requestAuth(this.setupRequest({ method: "serverless.auth.user.anonymousAuthorize", params: "{}" }, "auth")).then((e3) => {
        if (!e3.result || !e3.result.accessToken)
          throw new te({ code: "AUTH_FAILED", message: "获取accessToken失败" });
        this.setAccessToken(e3.result.accessToken);
      }), retryRule: I });
    }
    get hasAccessToken() {
      return !!this.accessToken;
    }
    setAccessToken(e2) {
      this.accessToken = e2;
    }
    requestWrapped(e2) {
      return de.wrappedRequest(e2, this.adapter.request);
    }
    requestAuth(e2) {
      return this.requestWrapped(e2);
    }
    request(e2, t2) {
      return Promise.resolve().then(() => this.hasAccessToken ? t2 ? this.requestWrapped(e2) : this.requestWrapped(e2).catch((t3) => new Promise((e3, n2) => {
        !t3 || "GATEWAY_INVALID_TOKEN" !== t3.code && "InvalidParameter.InvalidToken" !== t3.code ? n2(t3) : e3();
      }).then(() => this.getAccessToken()).then(() => {
        const t4 = this.rebuildRequest(e2);
        return this.request(t4, true);
      })) : this.getAccessToken().then(() => {
        const t3 = this.rebuildRequest(e2);
        return this.request(t3, true);
      }));
    }
    rebuildRequest(e2) {
      const t2 = Object.assign({}, e2);
      return t2.data.token = this.accessToken, t2.header["x-basement-token"] = this.accessToken, t2.header["x-serverless-sign"] = de.sign(t2.data, this.config.clientSecret), t2;
    }
    setupRequest(e2, t2) {
      const n2 = Object.assign({}, e2, { spaceId: this.config.spaceId, timestamp: Date.now() }), s2 = { "Content-Type": "application/json" };
      return "auth" !== t2 && (n2.token = this.accessToken, s2["x-basement-token"] = this.accessToken), s2["x-serverless-sign"] = de.sign(n2, this.config.clientSecret), { url: this.config.requestUrl, method: "POST", data: n2, dataType: "json", header: s2 };
    }
    getAccessToken() {
      return this._getAccessTokenPromiseHub.exec();
    }
    async authorize() {
      await this.getAccessToken();
    }
    callFunction(e2) {
      const t2 = { method: "serverless.function.runtime.invoke", params: JSON.stringify({ functionTarget: e2.name, functionArgs: e2.data || {} }) };
      return this.request({ ...this.setupRequest(t2), timeout: e2.timeout });
    }
    getOSSUploadOptionsFromPath(e2) {
      const t2 = { method: "serverless.file.resource.generateProximalSign", params: JSON.stringify(e2) };
      return this.request(this.setupRequest(t2));
    }
    uploadFileToOSS({ url: e2, formData: t2, name: n2, filePath: s2, fileType: r2, onUploadProgress: i2 }) {
      return new Promise((o2, a2) => {
        const c2 = this.adapter.uploadFile({ url: e2, formData: t2, name: n2, filePath: s2, fileType: r2, header: { "X-OSS-server-side-encrpytion": "AES256" }, success(e3) {
          e3 && e3.statusCode < 400 ? o2(e3) : a2(new te({ code: "UPLOAD_FAILED", message: "文件上传失败" }));
        }, fail(e3) {
          a2(new te({ code: e3.code || "UPLOAD_FAILED", message: e3.message || e3.errMsg || "文件上传失败" }));
        } });
        "function" == typeof i2 && c2 && "function" == typeof c2.onProgressUpdate && c2.onProgressUpdate((e3) => {
          i2({ loaded: e3.totalBytesSent, total: e3.totalBytesExpectedToSend });
        });
      });
    }
    reportOSSUpload(e2) {
      const t2 = { method: "serverless.file.resource.report", params: JSON.stringify(e2) };
      return this.request(this.setupRequest(t2));
    }
    async uploadFile({ filePath: e2, cloudPath: t2, fileType: n2 = "image", cloudPathAsRealPath: s2 = false, onUploadProgress: r2, config: i2 }) {
      if ("string" !== g(t2))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath必须为字符串类型" });
      if (!(t2 = t2.trim()))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath不可为空" });
      if (/:\/\//.test(t2))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath不合法" });
      const o2 = i2 && i2.envType || this.config.envType;
      if (s2 && ("/" !== t2[0] && (t2 = "/" + t2), t2.indexOf("\\") > -1))
        throw new te({ code: "INVALID_PARAM", message: "使用cloudPath作为路径时，cloudPath不可包含“\\”" });
      const a2 = (await this.getOSSUploadOptionsFromPath({ env: o2, filename: s2 ? t2.split("/").pop() : t2, fileId: s2 ? t2 : void 0 })).result, c2 = "https://" + a2.cdnDomain + "/" + a2.ossPath, { securityToken: u2, accessKeyId: h2, signature: l2, host: d2, ossPath: p2, id: f2, policy: m2, ossCallbackUrl: y2 } = a2, _2 = { "Cache-Control": "max-age=2592000", "Content-Disposition": "attachment", OSSAccessKeyId: h2, Signature: l2, host: d2, id: f2, key: p2, policy: m2, success_action_status: 200 };
      if (u2 && (_2["x-oss-security-token"] = u2), y2) {
        const e3 = JSON.stringify({ callbackUrl: y2, callbackBody: JSON.stringify({ fileId: f2, spaceId: this.config.spaceId }), callbackBodyType: "application/json" });
        _2.callback = de.toBase64(e3);
      }
      const w2 = { url: "https://" + a2.host, formData: _2, fileName: "file", name: "file", filePath: e2, fileType: n2 };
      if (await this.uploadFileToOSS(Object.assign({}, w2, { onUploadProgress: r2 })), y2)
        return { success: true, filePath: e2, fileID: c2 };
      if ((await this.reportOSSUpload({ id: f2 })).success)
        return { success: true, filePath: e2, fileID: c2 };
      throw new te({ code: "UPLOAD_FAILED", message: "文件上传失败" });
    }
    getTempFileURL({ fileList: e2 } = {}) {
      return new Promise((t2, n2) => {
        Array.isArray(e2) && 0 !== e2.length || n2(new te({ code: "INVALID_PARAM", message: "fileList的元素必须是非空的字符串" })), this.getFileInfo({ fileList: e2 }).then((n3) => {
          t2({ fileList: e2.map((e3, t3) => {
            const s2 = n3.fileList[t3];
            return { fileID: e3, tempFileURL: s2 && s2.url || e3 };
          }) });
        });
      });
    }
    async getFileInfo({ fileList: e2 } = {}) {
      if (!Array.isArray(e2) || 0 === e2.length)
        throw new te({ code: "INVALID_PARAM", message: "fileList的元素必须是非空的字符串" });
      const t2 = { method: "serverless.file.resource.info", params: JSON.stringify({ id: e2.map((e3) => e3.split("?")[0]).join(",") }) };
      return { fileList: (await this.request(this.setupRequest(t2))).result };
    }
  };
  var fe = { init(e2) {
    const t2 = new pe(e2), n2 = { signInAnonymously: function() {
      return t2.authorize();
    }, getLoginState: function() {
      return Promise.resolve(false);
    } };
    return t2.auth = function() {
      return n2;
    }, t2.customAuth = t2.auth, t2;
  } };
  const ge = "undefined" != typeof location && "http:" === location.protocol ? "http:" : "https:";
  var me;
  !function(e2) {
    e2.local = "local", e2.none = "none", e2.session = "session";
  }(me || (me = {}));
  var ye = function() {
  }, _e = n(function(e2, t2) {
    var n2;
    e2.exports = (n2 = r, function(e3) {
      var t3 = n2, s2 = t3.lib, r2 = s2.WordArray, i2 = s2.Hasher, o2 = t3.algo, a2 = [], c2 = [];
      !function() {
        function t4(t5) {
          for (var n4 = e3.sqrt(t5), s4 = 2; s4 <= n4; s4++)
            if (!(t5 % s4))
              return false;
          return true;
        }
        function n3(e4) {
          return 4294967296 * (e4 - (0 | e4)) | 0;
        }
        for (var s3 = 2, r3 = 0; r3 < 64; )
          t4(s3) && (r3 < 8 && (a2[r3] = n3(e3.pow(s3, 0.5))), c2[r3] = n3(e3.pow(s3, 1 / 3)), r3++), s3++;
      }();
      var u2 = [], h2 = o2.SHA256 = i2.extend({ _doReset: function() {
        this._hash = new r2.init(a2.slice(0));
      }, _doProcessBlock: function(e4, t4) {
        for (var n3 = this._hash.words, s3 = n3[0], r3 = n3[1], i3 = n3[2], o3 = n3[3], a3 = n3[4], h3 = n3[5], l2 = n3[6], d2 = n3[7], p2 = 0; p2 < 64; p2++) {
          if (p2 < 16)
            u2[p2] = 0 | e4[t4 + p2];
          else {
            var f2 = u2[p2 - 15], g2 = (f2 << 25 | f2 >>> 7) ^ (f2 << 14 | f2 >>> 18) ^ f2 >>> 3, m2 = u2[p2 - 2], y2 = (m2 << 15 | m2 >>> 17) ^ (m2 << 13 | m2 >>> 19) ^ m2 >>> 10;
            u2[p2] = g2 + u2[p2 - 7] + y2 + u2[p2 - 16];
          }
          var _2 = s3 & r3 ^ s3 & i3 ^ r3 & i3, w2 = (s3 << 30 | s3 >>> 2) ^ (s3 << 19 | s3 >>> 13) ^ (s3 << 10 | s3 >>> 22), I2 = d2 + ((a3 << 26 | a3 >>> 6) ^ (a3 << 21 | a3 >>> 11) ^ (a3 << 7 | a3 >>> 25)) + (a3 & h3 ^ ~a3 & l2) + c2[p2] + u2[p2];
          d2 = l2, l2 = h3, h3 = a3, a3 = o3 + I2 | 0, o3 = i3, i3 = r3, r3 = s3, s3 = I2 + (w2 + _2) | 0;
        }
        n3[0] = n3[0] + s3 | 0, n3[1] = n3[1] + r3 | 0, n3[2] = n3[2] + i3 | 0, n3[3] = n3[3] + o3 | 0, n3[4] = n3[4] + a3 | 0, n3[5] = n3[5] + h3 | 0, n3[6] = n3[6] + l2 | 0, n3[7] = n3[7] + d2 | 0;
      }, _doFinalize: function() {
        var t4 = this._data, n3 = t4.words, s3 = 8 * this._nDataBytes, r3 = 8 * t4.sigBytes;
        return n3[r3 >>> 5] |= 128 << 24 - r3 % 32, n3[14 + (r3 + 64 >>> 9 << 4)] = e3.floor(s3 / 4294967296), n3[15 + (r3 + 64 >>> 9 << 4)] = s3, t4.sigBytes = 4 * n3.length, this._process(), this._hash;
      }, clone: function() {
        var e4 = i2.clone.call(this);
        return e4._hash = this._hash.clone(), e4;
      } });
      t3.SHA256 = i2._createHelper(h2), t3.HmacSHA256 = i2._createHmacHelper(h2);
    }(Math), n2.SHA256);
  }), we = _e, Ie = n(function(e2, t2) {
    e2.exports = r.HmacSHA256;
  });
  const ve = () => {
    let e2;
    if (!Promise) {
      e2 = () => {
      }, e2.promise = {};
      const t3 = () => {
        throw new te({ message: 'Your Node runtime does support ES6 Promises. Set "global.Promise" to your preferred implementation of promises.' });
      };
      return Object.defineProperty(e2.promise, "then", { get: t3 }), Object.defineProperty(e2.promise, "catch", { get: t3 }), e2;
    }
    const t2 = new Promise((t3, n2) => {
      e2 = (e3, s2) => e3 ? n2(e3) : t3(s2);
    });
    return e2.promise = t2, e2;
  };
  function Se(e2) {
    return void 0 === e2;
  }
  function Te(e2) {
    return "[object Null]" === Object.prototype.toString.call(e2);
  }
  function be(e2 = "") {
    return e2.replace(/([\s\S]+)\s+(请前往云开发AI小助手查看问题：.*)/, "$1");
  }
  function Ee(e2 = 32) {
    const t2 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let n2 = "";
    for (let s2 = 0; s2 < e2; s2++)
      n2 += t2.charAt(Math.floor(62 * Math.random()));
    return n2;
  }
  var ke;
  function Ae(e2) {
    const t2 = (n2 = e2, "[object Array]" === Object.prototype.toString.call(n2) ? e2 : [e2]);
    var n2;
    for (const e3 of t2) {
      const { isMatch: t3, genAdapter: n3, runtime: s2 } = e3;
      if (t3())
        return { adapter: n3(), runtime: s2 };
    }
  }
  !function(e2) {
    e2.WEB = "web", e2.WX_MP = "wx_mp";
  }(ke || (ke = {}));
  const Pe = { adapter: null, runtime: void 0 }, Ce = ["anonymousUuidKey"];
  class Oe extends ye {
    constructor() {
      super(), Pe.adapter.root.tcbObject || (Pe.adapter.root.tcbObject = {});
    }
    setItem(e2, t2) {
      Pe.adapter.root.tcbObject[e2] = t2;
    }
    getItem(e2) {
      return Pe.adapter.root.tcbObject[e2];
    }
    removeItem(e2) {
      delete Pe.adapter.root.tcbObject[e2];
    }
    clear() {
      delete Pe.adapter.root.tcbObject;
    }
  }
  function xe(e2, t2) {
    switch (e2) {
      case "local":
        return t2.localStorage || new Oe();
      case "none":
        return new Oe();
      default:
        return t2.sessionStorage || new Oe();
    }
  }
  class Ne {
    constructor(e2) {
      if (!this._storage) {
        this._persistence = Pe.adapter.primaryStorage || e2.persistence, this._storage = xe(this._persistence, Pe.adapter);
        const t2 = `access_token_${e2.env}`, n2 = `access_token_expire_${e2.env}`, s2 = `refresh_token_${e2.env}`, r2 = `anonymous_uuid_${e2.env}`, i2 = `login_type_${e2.env}`, o2 = "device_id", a2 = `token_type_${e2.env}`, c2 = `user_info_${e2.env}`;
        this.keys = { accessTokenKey: t2, accessTokenExpireKey: n2, refreshTokenKey: s2, anonymousUuidKey: r2, loginTypeKey: i2, userInfoKey: c2, deviceIdKey: o2, tokenTypeKey: a2 };
      }
    }
    updatePersistence(e2) {
      if (e2 === this._persistence)
        return;
      const t2 = "local" === this._persistence;
      this._persistence = e2;
      const n2 = xe(e2, Pe.adapter);
      for (const e3 in this.keys) {
        const s2 = this.keys[e3];
        if (t2 && Ce.includes(e3))
          continue;
        const r2 = this._storage.getItem(s2);
        Se(r2) || Te(r2) || (n2.setItem(s2, r2), this._storage.removeItem(s2));
      }
      this._storage = n2;
    }
    setStore(e2, t2, n2) {
      if (!this._storage)
        return;
      const s2 = { version: n2 || "localCachev1", content: t2 }, r2 = JSON.stringify(s2);
      try {
        this._storage.setItem(e2, r2);
      } catch (e3) {
        throw e3;
      }
    }
    getStore(e2, t2) {
      try {
        if (!this._storage)
          return;
      } catch (e3) {
        return "";
      }
      t2 = t2 || "localCachev1";
      const n2 = this._storage.getItem(e2);
      if (!n2)
        return "";
      if (n2.indexOf(t2) >= 0) {
        return JSON.parse(n2).content;
      }
      return "";
    }
    removeStore(e2) {
      this._storage.removeItem(e2);
    }
  }
  const Re = {}, Le = {};
  function Ue(e2) {
    return Re[e2];
  }
  class De {
    constructor(e2, t2) {
      this.data = t2 || null, this.name = e2;
    }
  }
  class Me extends De {
    constructor(e2, t2) {
      super("error", { error: e2, data: t2 }), this.error = e2;
    }
  }
  const qe = new class {
    constructor() {
      this._listeners = {};
    }
    on(e2, t2) {
      return function(e3, t3, n2) {
        n2[e3] = n2[e3] || [], n2[e3].push(t3);
      }(e2, t2, this._listeners), this;
    }
    off(e2, t2) {
      return function(e3, t3, n2) {
        if (n2 && n2[e3]) {
          const s2 = n2[e3].indexOf(t3);
          -1 !== s2 && n2[e3].splice(s2, 1);
        }
      }(e2, t2, this._listeners), this;
    }
    fire(e2, t2) {
      if (e2 instanceof Me)
        return console.error(e2.error), this;
      const n2 = "string" == typeof e2 ? new De(e2, t2 || {}) : e2;
      const s2 = n2.name;
      if (this._listens(s2)) {
        n2.target = this;
        const e3 = this._listeners[s2] ? [...this._listeners[s2]] : [];
        for (const t3 of e3)
          t3.call(this, n2);
      }
      return this;
    }
    _listens(e2) {
      return this._listeners[e2] && this._listeners[e2].length > 0;
    }
  }();
  function Fe(e2, t2) {
    qe.on(e2, t2);
  }
  function Ke(e2, t2 = {}) {
    qe.fire(e2, t2);
  }
  function je(e2, t2) {
    qe.off(e2, t2);
  }
  const $e = "loginStateChanged", Be = "loginStateExpire", We = "loginTypeChanged", He = "anonymousConverted", Je = "refreshAccessToken";
  var ze;
  !function(e2) {
    e2.ANONYMOUS = "ANONYMOUS", e2.WECHAT = "WECHAT", e2.WECHAT_PUBLIC = "WECHAT-PUBLIC", e2.WECHAT_OPEN = "WECHAT-OPEN", e2.CUSTOM = "CUSTOM", e2.EMAIL = "EMAIL", e2.USERNAME = "USERNAME", e2.NULL = "NULL";
  }(ze || (ze = {}));
  class Ve {
    constructor() {
      this._fnPromiseMap = /* @__PURE__ */ new Map();
    }
    async run(e2, t2) {
      let n2 = this._fnPromiseMap.get(e2);
      return n2 || (n2 = new Promise(async (n3, s2) => {
        try {
          await this._runIdlePromise();
          const e3 = t2();
          n3(await e3);
        } catch (e3) {
          s2(e3);
        } finally {
          this._fnPromiseMap.delete(e2);
        }
      }), this._fnPromiseMap.set(e2, n2)), n2;
    }
    _runIdlePromise() {
      return Promise.resolve();
    }
  }
  class Ge {
    constructor(e2) {
      this._singlePromise = new Ve(), this._cache = Ue(e2.env), this._baseURL = `https://${e2.env}.ap-shanghai.tcb-api.tencentcloudapi.com`, this._reqClass = new Pe.adapter.reqClass({ timeout: e2.timeout, timeoutMsg: `请求在${e2.timeout / 1e3}s内未完成，已中断`, restrictedMethods: ["post"] });
    }
    _getDeviceId() {
      if (this._deviceID)
        return this._deviceID;
      const { deviceIdKey: e2 } = this._cache.keys;
      let t2 = this._cache.getStore(e2);
      return "string" == typeof t2 && t2.length >= 16 && t2.length <= 48 || (t2 = Ee(), this._cache.setStore(e2, t2)), this._deviceID = t2, t2;
    }
    async _request(e2, t2, n2 = {}) {
      const s2 = { "x-request-id": Ee(), "x-device-id": this._getDeviceId() };
      if (n2.withAccessToken) {
        const { tokenTypeKey: e3 } = this._cache.keys, t3 = await this.getAccessToken(), n3 = this._cache.getStore(e3);
        s2.authorization = `${n3} ${t3}`;
      }
      return this._reqClass["get" === n2.method ? "get" : "post"]({ url: `${this._baseURL}${e2}`, data: t2, headers: s2 });
    }
    async _fetchAccessToken() {
      const { loginTypeKey: e2, accessTokenKey: t2, accessTokenExpireKey: n2, tokenTypeKey: s2 } = this._cache.keys, r2 = this._cache.getStore(e2);
      if (r2 && r2 !== ze.ANONYMOUS)
        throw new te({ code: "INVALID_OPERATION", message: "非匿名登录不支持刷新 access token" });
      const i2 = await this._singlePromise.run("fetchAccessToken", async () => (await this._request("/auth/v1/signin/anonymously", {}, { method: "post" })).data), { access_token: o2, expires_in: a2, token_type: c2 } = i2;
      return this._cache.setStore(s2, c2), this._cache.setStore(t2, o2), this._cache.setStore(n2, Date.now() + 1e3 * a2), o2;
    }
    isAccessTokenExpired(e2, t2) {
      let n2 = true;
      return e2 && t2 && (n2 = t2 < Date.now()), n2;
    }
    async getAccessToken() {
      const { accessTokenKey: e2, accessTokenExpireKey: t2 } = this._cache.keys, n2 = this._cache.getStore(e2), s2 = this._cache.getStore(t2);
      return this.isAccessTokenExpired(n2, s2) ? this._fetchAccessToken() : n2;
    }
    async refreshAccessToken() {
      const { accessTokenKey: e2, accessTokenExpireKey: t2, loginTypeKey: n2 } = this._cache.keys;
      return this._cache.removeStore(e2), this._cache.removeStore(t2), this._cache.setStore(n2, ze.ANONYMOUS), this.getAccessToken();
    }
    async getUserInfo() {
      return this._singlePromise.run("getUserInfo", async () => (await this._request("/auth/v1/user/me", {}, { withAccessToken: true, method: "get" })).data);
    }
  }
  const Ye = ["auth.getJwt", "auth.logout", "auth.signInWithTicket", "auth.signInAnonymously", "auth.signIn", "auth.fetchAccessTokenWithRefreshToken", "auth.signUpWithEmailAndPassword", "auth.activateEndUserMail", "auth.sendPasswordResetEmail", "auth.resetPasswordWithToken", "auth.isUsernameRegistered"], Qe = { "X-SDK-Version": "1.3.5" };
  function Xe(e2, t2, n2) {
    const s2 = e2[t2];
    e2[t2] = function(t3) {
      const r2 = {}, i2 = {};
      n2.forEach((n3) => {
        const { data: s3, headers: o3 } = n3.call(e2, t3);
        Object.assign(r2, s3), Object.assign(i2, o3);
      });
      const o2 = t3.data;
      return o2 && (() => {
        var e3;
        if (e3 = o2, "[object FormData]" !== Object.prototype.toString.call(e3))
          t3.data = { ...o2, ...r2 };
        else
          for (const e4 in r2)
            o2.append(e4, r2[e4]);
      })(), t3.headers = { ...t3.headers || {}, ...i2 }, s2.call(e2, t3);
    };
  }
  function Ze() {
    const e2 = Math.random().toString(16).slice(2);
    return { data: { seqId: e2 }, headers: { ...Qe, "x-seqid": e2 } };
  }
  class et {
    constructor(e2 = {}) {
      var t2;
      this.config = e2, this._reqClass = new Pe.adapter.reqClass({ timeout: this.config.timeout, timeoutMsg: `请求在${this.config.timeout / 1e3}s内未完成，已中断`, restrictedMethods: ["post"] }), this._cache = Ue(this.config.env), this._localCache = (t2 = this.config.env, Le[t2]), this.oauth = new Ge(this.config), Xe(this._reqClass, "post", [Ze]), Xe(this._reqClass, "upload", [Ze]), Xe(this._reqClass, "download", [Ze]);
    }
    async post(e2) {
      return await this._reqClass.post(e2);
    }
    async upload(e2) {
      return await this._reqClass.upload(e2);
    }
    async download(e2) {
      return await this._reqClass.download(e2);
    }
    async refreshAccessToken() {
      let e2, t2;
      this._refreshAccessTokenPromise || (this._refreshAccessTokenPromise = this._refreshAccessToken());
      try {
        e2 = await this._refreshAccessTokenPromise;
      } catch (e3) {
        t2 = e3;
      }
      if (this._refreshAccessTokenPromise = null, this._shouldRefreshAccessTokenHook = null, t2)
        throw t2;
      return e2;
    }
    async _refreshAccessToken() {
      const { accessTokenKey: e2, accessTokenExpireKey: t2, refreshTokenKey: n2, loginTypeKey: s2, anonymousUuidKey: r2 } = this._cache.keys;
      this._cache.removeStore(e2), this._cache.removeStore(t2);
      let i2 = this._cache.getStore(n2);
      if (!i2)
        throw new te({ message: "未登录CloudBase" });
      const o2 = { refresh_token: i2 }, a2 = await this.request("auth.fetchAccessTokenWithRefreshToken", o2);
      if (a2.data.code) {
        const { code: e3 } = a2.data;
        if ("SIGN_PARAM_INVALID" === e3 || "REFRESH_TOKEN_EXPIRED" === e3 || "INVALID_REFRESH_TOKEN" === e3) {
          if (this._cache.getStore(s2) === ze.ANONYMOUS && "INVALID_REFRESH_TOKEN" === e3) {
            const e4 = this._cache.getStore(r2), t3 = this._cache.getStore(n2), s3 = await this.send("auth.signInAnonymously", { anonymous_uuid: e4, refresh_token: t3 });
            return this.setRefreshToken(s3.refresh_token), this._refreshAccessToken();
          }
          Ke(Be), this._cache.removeStore(n2);
        }
        throw new te({ code: a2.data.code, message: `刷新access token失败：${a2.data.code}` });
      }
      if (a2.data.access_token)
        return Ke(Je), this._cache.setStore(e2, a2.data.access_token), this._cache.setStore(t2, a2.data.access_token_expire + Date.now()), { accessToken: a2.data.access_token, accessTokenExpire: a2.data.access_token_expire };
      a2.data.refresh_token && (this._cache.removeStore(n2), this._cache.setStore(n2, a2.data.refresh_token), this._refreshAccessToken());
    }
    async getAccessToken() {
      const { accessTokenKey: e2, accessTokenExpireKey: t2, refreshTokenKey: n2 } = this._cache.keys;
      if (!this._cache.getStore(n2))
        throw new te({ message: "refresh token不存在，登录状态异常" });
      let s2 = this._cache.getStore(e2), r2 = this._cache.getStore(t2), i2 = true;
      return this._shouldRefreshAccessTokenHook && !await this._shouldRefreshAccessTokenHook(s2, r2) && (i2 = false), (!s2 || !r2 || r2 < Date.now()) && i2 ? this.refreshAccessToken() : { accessToken: s2, accessTokenExpire: r2 };
    }
    async request(e2, t2, n2) {
      const s2 = `x-tcb-trace_${this.config.env}`;
      let r2 = "application/x-www-form-urlencoded";
      const i2 = { action: e2, env: this.config.env, dataVersion: "2019-08-16", ...t2 };
      let o2;
      if (-1 === Ye.indexOf(e2) && (this._cache.keys, i2.access_token = await this.oauth.getAccessToken()), "storage.uploadFile" === e2) {
        o2 = new FormData();
        for (let e3 in o2)
          o2.hasOwnProperty(e3) && void 0 !== o2[e3] && o2.append(e3, i2[e3]);
        r2 = "multipart/form-data";
      } else {
        r2 = "application/json", o2 = {};
        for (let e3 in i2)
          void 0 !== i2[e3] && (o2[e3] = i2[e3]);
      }
      let a2 = { headers: { "content-type": r2 } };
      n2 && n2.timeout && (a2.timeout = n2.timeout), n2 && n2.onUploadProgress && (a2.onUploadProgress = n2.onUploadProgress);
      const c2 = this._localCache.getStore(s2);
      c2 && (a2.headers["X-TCB-Trace"] = c2);
      const { parse: u2, inQuery: h2, search: l2 } = t2;
      let d2 = { env: this.config.env };
      u2 && (d2.parse = true), h2 && (d2 = { ...h2, ...d2 });
      let p2 = function(e3, t3, n3 = {}) {
        const s3 = /\?/.test(t3);
        let r3 = "";
        for (let e4 in n3)
          "" === r3 ? !s3 && (t3 += "?") : r3 += "&", r3 += `${e4}=${encodeURIComponent(n3[e4])}`;
        return /^http(s)?\:\/\//.test(t3 += r3) ? t3 : `${e3}${t3}`;
      }(ge, "//tcb-api.tencentcloudapi.com/web", d2);
      l2 && (p2 += l2);
      const f2 = await this.post({ url: p2, data: o2, ...a2 }), g2 = f2.header && f2.header["x-tcb-trace"];
      if (g2 && this._localCache.setStore(s2, g2), 200 !== Number(f2.status) && 200 !== Number(f2.statusCode) || !f2.data)
        throw new te({ code: "NETWORK_ERROR", message: "network request error" });
      return f2;
    }
    async send(e2, t2 = {}, n2 = {}) {
      const s2 = await this.request(e2, t2, { ...n2, onUploadProgress: t2.onUploadProgress });
      if (("ACCESS_TOKEN_DISABLED" === s2.data.code || "ACCESS_TOKEN_EXPIRED" === s2.data.code) && -1 === Ye.indexOf(e2)) {
        await this.oauth.refreshAccessToken();
        const s3 = await this.request(e2, t2, { ...n2, onUploadProgress: t2.onUploadProgress });
        if (s3.data.code)
          throw new te({ code: s3.data.code, message: be(s3.data.message) });
        return s3.data;
      }
      if (s2.data.code)
        throw new te({ code: s2.data.code, message: be(s2.data.message) });
      return s2.data;
    }
    setRefreshToken(e2) {
      const { accessTokenKey: t2, accessTokenExpireKey: n2, refreshTokenKey: s2 } = this._cache.keys;
      this._cache.removeStore(t2), this._cache.removeStore(n2), this._cache.setStore(s2, e2);
    }
  }
  const tt = {};
  function nt(e2) {
    return tt[e2];
  }
  class st {
    constructor(e2) {
      this.config = e2, this._cache = Ue(e2.env), this._request = nt(e2.env);
    }
    setRefreshToken(e2) {
      const { accessTokenKey: t2, accessTokenExpireKey: n2, refreshTokenKey: s2 } = this._cache.keys;
      this._cache.removeStore(t2), this._cache.removeStore(n2), this._cache.setStore(s2, e2);
    }
    setAccessToken(e2, t2) {
      const { accessTokenKey: n2, accessTokenExpireKey: s2 } = this._cache.keys;
      this._cache.setStore(n2, e2), this._cache.setStore(s2, t2);
    }
    async refreshUserInfo() {
      const { data: e2 } = await this._request.send("auth.getUserInfo", {});
      return this.setLocalUserInfo(e2), e2;
    }
    setLocalUserInfo(e2) {
      const { userInfoKey: t2 } = this._cache.keys;
      this._cache.setStore(t2, e2);
    }
  }
  class rt {
    constructor(e2) {
      if (!e2)
        throw new te({ code: "PARAM_ERROR", message: "envId is not defined" });
      this._envId = e2, this._cache = Ue(this._envId), this._request = nt(this._envId), this.setUserInfo();
    }
    linkWithTicket(e2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "ticket must be string" });
      return this._request.send("auth.linkWithTicket", { ticket: e2 });
    }
    linkWithRedirect(e2) {
      e2.signInWithRedirect();
    }
    updatePassword(e2, t2) {
      return this._request.send("auth.updatePassword", { oldPassword: t2, newPassword: e2 });
    }
    updateEmail(e2) {
      return this._request.send("auth.updateEmail", { newEmail: e2 });
    }
    updateUsername(e2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "username must be a string" });
      return this._request.send("auth.updateUsername", { username: e2 });
    }
    async getLinkedUidList() {
      const { data: e2 } = await this._request.send("auth.getLinkedUidList", {});
      let t2 = false;
      const { users: n2 } = e2;
      return n2.forEach((e3) => {
        e3.wxOpenId && e3.wxPublicId && (t2 = true);
      }), { users: n2, hasPrimaryUid: t2 };
    }
    setPrimaryUid(e2) {
      return this._request.send("auth.setPrimaryUid", { uid: e2 });
    }
    unlink(e2) {
      return this._request.send("auth.unlink", { platform: e2 });
    }
    async update(e2) {
      const { nickName: t2, gender: n2, avatarUrl: s2, province: r2, country: i2, city: o2 } = e2, { data: a2 } = await this._request.send("auth.updateUserInfo", { nickName: t2, gender: n2, avatarUrl: s2, province: r2, country: i2, city: o2 });
      this.setLocalUserInfo(a2);
    }
    async refresh() {
      const e2 = await this._request.oauth.getUserInfo();
      return this.setLocalUserInfo(e2), e2;
    }
    setUserInfo() {
      const { userInfoKey: e2 } = this._cache.keys, t2 = this._cache.getStore(e2);
      ["uid", "loginType", "openid", "wxOpenId", "wxPublicId", "unionId", "qqMiniOpenId", "email", "hasPassword", "customUserId", "nickName", "gender", "avatarUrl"].forEach((e3) => {
        this[e3] = t2[e3];
      }), this.location = { country: t2.country, province: t2.province, city: t2.city };
    }
    setLocalUserInfo(e2) {
      const { userInfoKey: t2 } = this._cache.keys;
      this._cache.setStore(t2, e2), this.setUserInfo();
    }
  }
  class it {
    constructor(e2) {
      if (!e2)
        throw new te({ code: "PARAM_ERROR", message: "envId is not defined" });
      this._cache = Ue(e2);
      const { refreshTokenKey: t2, accessTokenKey: n2, accessTokenExpireKey: s2 } = this._cache.keys, r2 = this._cache.getStore(t2), i2 = this._cache.getStore(n2), o2 = this._cache.getStore(s2);
      this.credential = { refreshToken: r2, accessToken: i2, accessTokenExpire: o2 }, this.user = new rt(e2);
    }
    get isAnonymousAuth() {
      return this.loginType === ze.ANONYMOUS;
    }
    get isCustomAuth() {
      return this.loginType === ze.CUSTOM;
    }
    get isWeixinAuth() {
      return this.loginType === ze.WECHAT || this.loginType === ze.WECHAT_OPEN || this.loginType === ze.WECHAT_PUBLIC;
    }
    get loginType() {
      return this._cache.getStore(this._cache.keys.loginTypeKey);
    }
  }
  class ot extends st {
    async signIn() {
      this._cache.updatePersistence("local"), await this._request.oauth.getAccessToken(), Ke($e), Ke(We, { env: this.config.env, loginType: ze.ANONYMOUS, persistence: "local" });
      const e2 = new it(this.config.env);
      return await e2.user.refresh(), e2;
    }
    async linkAndRetrieveDataWithTicket(e2) {
      const { anonymousUuidKey: t2, refreshTokenKey: n2 } = this._cache.keys, s2 = this._cache.getStore(t2), r2 = this._cache.getStore(n2), i2 = await this._request.send("auth.linkAndRetrieveDataWithTicket", { anonymous_uuid: s2, refresh_token: r2, ticket: e2 });
      if (i2.refresh_token)
        return this._clearAnonymousUUID(), this.setRefreshToken(i2.refresh_token), await this._request.refreshAccessToken(), Ke(He, { env: this.config.env }), Ke(We, { loginType: ze.CUSTOM, persistence: "local" }), { credential: { refreshToken: i2.refresh_token } };
      throw new te({ message: "匿名转化失败" });
    }
    _setAnonymousUUID(e2) {
      const { anonymousUuidKey: t2, loginTypeKey: n2 } = this._cache.keys;
      this._cache.removeStore(t2), this._cache.setStore(t2, e2), this._cache.setStore(n2, ze.ANONYMOUS);
    }
    _clearAnonymousUUID() {
      this._cache.removeStore(this._cache.keys.anonymousUuidKey);
    }
  }
  class at extends st {
    async signIn(e2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "ticket must be a string" });
      const { refreshTokenKey: t2 } = this._cache.keys, n2 = await this._request.send("auth.signInWithTicket", { ticket: e2, refresh_token: this._cache.getStore(t2) || "" });
      if (n2.refresh_token)
        return this.setRefreshToken(n2.refresh_token), await this._request.refreshAccessToken(), Ke($e), Ke(We, { env: this.config.env, loginType: ze.CUSTOM, persistence: this.config.persistence }), await this.refreshUserInfo(), new it(this.config.env);
      throw new te({ message: "自定义登录失败" });
    }
  }
  class ct extends st {
    async signIn(e2, t2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "email must be a string" });
      const { refreshTokenKey: n2 } = this._cache.keys, s2 = await this._request.send("auth.signIn", { loginType: "EMAIL", email: e2, password: t2, refresh_token: this._cache.getStore(n2) || "" }), { refresh_token: r2, access_token: i2, access_token_expire: o2 } = s2;
      if (r2)
        return this.setRefreshToken(r2), i2 && o2 ? this.setAccessToken(i2, o2) : await this._request.refreshAccessToken(), await this.refreshUserInfo(), Ke($e), Ke(We, { env: this.config.env, loginType: ze.EMAIL, persistence: this.config.persistence }), new it(this.config.env);
      throw s2.code ? new te({ code: s2.code, message: `邮箱登录失败: ${s2.message}` }) : new te({ message: "邮箱登录失败" });
    }
    async activate(e2) {
      return this._request.send("auth.activateEndUserMail", { token: e2 });
    }
    async resetPasswordWithToken(e2, t2) {
      return this._request.send("auth.resetPasswordWithToken", { token: e2, newPassword: t2 });
    }
  }
  class ut extends st {
    async signIn(e2, t2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "username must be a string" });
      "string" != typeof t2 && (t2 = "", console.warn("password is empty"));
      const { refreshTokenKey: n2 } = this._cache.keys, s2 = await this._request.send("auth.signIn", { loginType: ze.USERNAME, username: e2, password: t2, refresh_token: this._cache.getStore(n2) || "" }), { refresh_token: r2, access_token_expire: i2, access_token: o2 } = s2;
      if (r2)
        return this.setRefreshToken(r2), o2 && i2 ? this.setAccessToken(o2, i2) : await this._request.refreshAccessToken(), await this.refreshUserInfo(), Ke($e), Ke(We, { env: this.config.env, loginType: ze.USERNAME, persistence: this.config.persistence }), new it(this.config.env);
      throw s2.code ? new te({ code: s2.code, message: `用户名密码登录失败: ${s2.message}` }) : new te({ message: "用户名密码登录失败" });
    }
  }
  class ht {
    constructor(e2) {
      this.config = e2, this._cache = Ue(e2.env), this._request = nt(e2.env), this._onAnonymousConverted = this._onAnonymousConverted.bind(this), this._onLoginTypeChanged = this._onLoginTypeChanged.bind(this), Fe(We, this._onLoginTypeChanged);
    }
    get currentUser() {
      const e2 = this.hasLoginState();
      return e2 && e2.user || null;
    }
    get loginType() {
      return this._cache.getStore(this._cache.keys.loginTypeKey);
    }
    anonymousAuthProvider() {
      return new ot(this.config);
    }
    customAuthProvider() {
      return new at(this.config);
    }
    emailAuthProvider() {
      return new ct(this.config);
    }
    usernameAuthProvider() {
      return new ut(this.config);
    }
    async signInAnonymously() {
      return new ot(this.config).signIn();
    }
    async signInWithEmailAndPassword(e2, t2) {
      return new ct(this.config).signIn(e2, t2);
    }
    signInWithUsernameAndPassword(e2, t2) {
      return new ut(this.config).signIn(e2, t2);
    }
    async linkAndRetrieveDataWithTicket(e2) {
      this._anonymousAuthProvider || (this._anonymousAuthProvider = new ot(this.config)), Fe(He, this._onAnonymousConverted);
      return await this._anonymousAuthProvider.linkAndRetrieveDataWithTicket(e2);
    }
    async signOut() {
      if (this.loginType === ze.ANONYMOUS)
        throw new te({ message: "匿名用户不支持登出操作" });
      const { refreshTokenKey: e2, accessTokenKey: t2, accessTokenExpireKey: n2 } = this._cache.keys, s2 = this._cache.getStore(e2);
      if (!s2)
        return;
      const r2 = await this._request.send("auth.logout", { refresh_token: s2 });
      return this._cache.removeStore(e2), this._cache.removeStore(t2), this._cache.removeStore(n2), Ke($e), Ke(We, { env: this.config.env, loginType: ze.NULL, persistence: this.config.persistence }), r2;
    }
    async signUpWithEmailAndPassword(e2, t2) {
      return this._request.send("auth.signUpWithEmailAndPassword", { email: e2, password: t2 });
    }
    async sendPasswordResetEmail(e2) {
      return this._request.send("auth.sendPasswordResetEmail", { email: e2 });
    }
    onLoginStateChanged(e2) {
      Fe($e, () => {
        const t3 = this.hasLoginState();
        e2.call(this, t3);
      });
      const t2 = this.hasLoginState();
      e2.call(this, t2);
    }
    onLoginStateExpired(e2) {
      Fe(Be, e2.bind(this));
    }
    onAccessTokenRefreshed(e2) {
      Fe(Je, e2.bind(this));
    }
    onAnonymousConverted(e2) {
      Fe(He, e2.bind(this));
    }
    onLoginTypeChanged(e2) {
      Fe(We, () => {
        const t2 = this.hasLoginState();
        e2.call(this, t2);
      });
    }
    async getAccessToken() {
      return { accessToken: (await this._request.getAccessToken()).accessToken, env: this.config.env };
    }
    hasLoginState() {
      const { accessTokenKey: e2, accessTokenExpireKey: t2 } = this._cache.keys, n2 = this._cache.getStore(e2), s2 = this._cache.getStore(t2);
      return this._request.oauth.isAccessTokenExpired(n2, s2) ? null : new it(this.config.env);
    }
    async isUsernameRegistered(e2) {
      if ("string" != typeof e2)
        throw new te({ code: "PARAM_ERROR", message: "username must be a string" });
      const { data: t2 } = await this._request.send("auth.isUsernameRegistered", { username: e2 });
      return t2 && t2.isRegistered;
    }
    getLoginState() {
      return Promise.resolve(this.hasLoginState());
    }
    async signInWithTicket(e2) {
      return new at(this.config).signIn(e2);
    }
    shouldRefreshAccessToken(e2) {
      this._request._shouldRefreshAccessTokenHook = e2.bind(this);
    }
    getUserInfo() {
      return this._request.send("auth.getUserInfo", {}).then((e2) => e2.code ? e2 : { ...e2.data, requestId: e2.seqId });
    }
    getAuthHeader() {
      const { refreshTokenKey: e2, accessTokenKey: t2 } = this._cache.keys, n2 = this._cache.getStore(e2);
      return { "x-cloudbase-credentials": this._cache.getStore(t2) + "/@@/" + n2 };
    }
    _onAnonymousConverted(e2) {
      const { env: t2 } = e2.data;
      t2 === this.config.env && this._cache.updatePersistence(this.config.persistence);
    }
    _onLoginTypeChanged(e2) {
      const { loginType: t2, persistence: n2, env: s2 } = e2.data;
      s2 === this.config.env && (this._cache.updatePersistence(n2), this._cache.setStore(this._cache.keys.loginTypeKey, t2));
    }
  }
  const lt = function(e2, t2) {
    t2 = t2 || ve();
    const n2 = nt(this.config.env), { cloudPath: s2, filePath: r2, onUploadProgress: i2, fileType: o2 = "image" } = e2;
    return n2.send("storage.getUploadMetadata", { path: s2 }).then((e3) => {
      const { data: { url: a2, authorization: c2, token: u2, fileId: h2, cosFileId: l2 }, requestId: d2 } = e3, p2 = { key: s2, signature: c2, "x-cos-meta-fileid": l2, success_action_status: "201", "x-cos-security-token": u2 };
      n2.upload({ url: a2, data: p2, file: r2, name: s2, fileType: o2, onUploadProgress: i2 }).then((e4) => {
        201 === e4.statusCode ? t2(null, { fileID: h2, requestId: d2 }) : t2(new te({ code: "STORAGE_REQUEST_FAIL", message: `STORAGE_REQUEST_FAIL: ${e4.data}` }));
      }).catch((e4) => {
        t2(e4);
      });
    }).catch((e3) => {
      t2(e3);
    }), t2.promise;
  }, dt = function(e2, t2) {
    t2 = t2 || ve();
    const n2 = nt(this.config.env), { cloudPath: s2 } = e2;
    return n2.send("storage.getUploadMetadata", { path: s2 }).then((e3) => {
      t2(null, e3);
    }).catch((e3) => {
      t2(e3);
    }), t2.promise;
  }, pt = function({ fileList: e2 }, t2) {
    if (t2 = t2 || ve(), !e2 || !Array.isArray(e2))
      return { code: "INVALID_PARAM", message: "fileList必须是非空的数组" };
    for (let t3 of e2)
      if (!t3 || "string" != typeof t3)
        return { code: "INVALID_PARAM", message: "fileList的元素必须是非空的字符串" };
    const n2 = { fileid_list: e2 };
    return nt(this.config.env).send("storage.batchDeleteFile", n2).then((e3) => {
      e3.code ? t2(null, e3) : t2(null, { fileList: e3.data.delete_list, requestId: e3.requestId });
    }).catch((e3) => {
      t2(e3);
    }), t2.promise;
  }, ft = function({ fileList: e2 }, t2) {
    t2 = t2 || ve(), e2 && Array.isArray(e2) || t2(null, { code: "INVALID_PARAM", message: "fileList必须是非空的数组" });
    let n2 = [];
    for (let s3 of e2)
      "object" == typeof s3 ? (s3.hasOwnProperty("fileID") && s3.hasOwnProperty("maxAge") || t2(null, { code: "INVALID_PARAM", message: "fileList的元素必须是包含fileID和maxAge的对象" }), n2.push({ fileid: s3.fileID, max_age: s3.maxAge })) : "string" == typeof s3 ? n2.push({ fileid: s3 }) : t2(null, { code: "INVALID_PARAM", message: "fileList的元素必须是字符串" });
    const s2 = { file_list: n2 };
    return nt(this.config.env).send("storage.batchGetDownloadUrl", s2).then((e3) => {
      e3.code ? t2(null, e3) : t2(null, { fileList: e3.data.download_list, requestId: e3.requestId });
    }).catch((e3) => {
      t2(e3);
    }), t2.promise;
  }, gt = async function({ fileID: e2 }, t2) {
    const n2 = (await ft.call(this, { fileList: [{ fileID: e2, maxAge: 600 }] })).fileList[0];
    if ("SUCCESS" !== n2.code)
      return t2 ? t2(n2) : new Promise((e3) => {
        e3(n2);
      });
    const s2 = nt(this.config.env);
    let r2 = n2.download_url;
    if (r2 = encodeURI(r2), !t2)
      return s2.download({ url: r2 });
    t2(await s2.download({ url: r2 }));
  }, mt = function({ name: e2, data: t2, query: n2, parse: s2, search: r2, timeout: i2 }, o2) {
    const a2 = o2 || ve();
    let c2;
    try {
      c2 = t2 ? JSON.stringify(t2) : "";
    } catch (e3) {
      return Promise.reject(e3);
    }
    if (!e2)
      return Promise.reject(new te({ code: "PARAM_ERROR", message: "函数名不能为空" }));
    const u2 = { inQuery: n2, parse: s2, search: r2, function_name: e2, request_data: c2 };
    return nt(this.config.env).send("functions.invokeFunction", u2, { timeout: i2 }).then((e3) => {
      if (e3.code)
        a2(null, e3);
      else {
        let t3 = e3.data.response_data;
        if (s2)
          a2(null, { result: t3, requestId: e3.requestId });
        else
          try {
            t3 = JSON.parse(e3.data.response_data), a2(null, { result: t3, requestId: e3.requestId });
          } catch (e4) {
            a2(new te({ message: "response data must be json" }));
          }
      }
      return a2.promise;
    }).catch((e3) => {
      a2(e3);
    }), a2.promise;
  }, yt = { timeout: 15e3, persistence: "session" }, _t = 6e5, wt = {};
  class It {
    constructor(e2) {
      this.config = e2 || this.config, this.authObj = void 0;
    }
    init(e2) {
      switch (Pe.adapter || (this.requestClient = new Pe.adapter.reqClass({ timeout: e2.timeout || 5e3, timeoutMsg: `请求在${(e2.timeout || 5e3) / 1e3}s内未完成，已中断` })), this.config = { ...yt, ...e2 }, true) {
        case this.config.timeout > _t:
          console.warn("timeout大于可配置上限[10分钟]，已重置为上限数值"), this.config.timeout = _t;
          break;
        case this.config.timeout < 100:
          console.warn("timeout小于可配置下限[100ms]，已重置为下限数值"), this.config.timeout = 100;
      }
      return new It(this.config);
    }
    auth({ persistence: e2 } = {}) {
      if (this.authObj)
        return this.authObj;
      const t2 = e2 || Pe.adapter.primaryStorage || yt.persistence;
      var n2;
      return t2 !== this.config.persistence && (this.config.persistence = t2), function(e3) {
        const { env: t3 } = e3;
        Re[t3] = new Ne(e3), Le[t3] = new Ne({ ...e3, persistence: "local" });
      }(this.config), n2 = this.config, tt[n2.env] = new et(n2), this.authObj = new ht(this.config), this.authObj;
    }
    on(e2, t2) {
      return Fe.apply(this, [e2, t2]);
    }
    off(e2, t2) {
      return je.apply(this, [e2, t2]);
    }
    callFunction(e2, t2) {
      return mt.apply(this, [e2, t2]);
    }
    deleteFile(e2, t2) {
      return pt.apply(this, [e2, t2]);
    }
    getTempFileURL(e2, t2) {
      return ft.apply(this, [e2, t2]);
    }
    downloadFile(e2, t2) {
      return gt.apply(this, [e2, t2]);
    }
    uploadFile(e2, t2) {
      return lt.apply(this, [e2, t2]);
    }
    getUploadMetadata(e2, t2) {
      return dt.apply(this, [e2, t2]);
    }
    registerExtension(e2) {
      wt[e2.name] = e2;
    }
    async invokeExtension(e2, t2) {
      const n2 = wt[e2];
      if (!n2)
        throw new te({ message: `扩展${e2} 必须先注册` });
      return await n2.invoke(t2, this);
    }
    useAdapters(e2) {
      const { adapter: t2, runtime: n2 } = Ae(e2) || {};
      t2 && (Pe.adapter = t2), n2 && (Pe.runtime = n2);
    }
  }
  var vt = new It();
  function St(e2, t2, n2) {
    void 0 === n2 && (n2 = {});
    var s2 = /\?/.test(t2), r2 = "";
    for (var i2 in n2)
      "" === r2 ? !s2 && (t2 += "?") : r2 += "&", r2 += i2 + "=" + encodeURIComponent(n2[i2]);
    return /^http(s)?:\/\//.test(t2 += r2) ? t2 : "" + e2 + t2;
  }
  class Tt {
    get(e2) {
      const { url: t2, data: n2, headers: s2, timeout: r2 } = e2;
      return new Promise((e3, i2) => {
        ne.request({ url: St("https:", t2), data: n2, method: "GET", header: s2, timeout: r2, success(t3) {
          e3(t3);
        }, fail(e4) {
          i2(e4);
        } });
      });
    }
    post(e2) {
      const { url: t2, data: n2, headers: s2, timeout: r2 } = e2;
      return new Promise((e3, i2) => {
        ne.request({ url: St("https:", t2), data: n2, method: "POST", header: s2, timeout: r2, success(t3) {
          e3(t3);
        }, fail(e4) {
          i2(e4);
        } });
      });
    }
    upload(e2) {
      return new Promise((t2, n2) => {
        const { url: s2, file: r2, data: i2, headers: o2, fileType: a2 } = e2, c2 = ne.uploadFile({ url: St("https:", s2), name: "file", formData: Object.assign({}, i2), filePath: r2, fileType: a2, header: o2, success(e3) {
          const n3 = { statusCode: e3.statusCode, data: e3.data || {} };
          200 === e3.statusCode && i2.success_action_status && (n3.statusCode = parseInt(i2.success_action_status, 10)), t2(n3);
        }, fail(e3) {
          n2(new Error(e3.errMsg || "uploadFile:fail"));
        } });
        "function" == typeof e2.onUploadProgress && c2 && "function" == typeof c2.onProgressUpdate && c2.onProgressUpdate((t3) => {
          e2.onUploadProgress({ loaded: t3.totalBytesSent, total: t3.totalBytesExpectedToSend });
        });
      });
    }
  }
  const bt = { setItem(e2, t2) {
    ne.setStorageSync(e2, t2);
  }, getItem: (e2) => ne.getStorageSync(e2), removeItem(e2) {
    ne.removeStorageSync(e2);
  }, clear() {
    ne.clearStorageSync();
  } };
  var Et = { genAdapter: function() {
    return { root: {}, reqClass: Tt, localStorage: bt, primaryStorage: "local" };
  }, isMatch: function() {
    return true;
  }, runtime: "uni_app" };
  vt.useAdapters(Et);
  const kt = vt, At = kt.init;
  kt.init = function(e2) {
    e2.env = e2.spaceId;
    const t2 = At.call(this, e2);
    t2.config.provider = "tencent", t2.config.spaceId = e2.spaceId;
    const n2 = t2.auth;
    return t2.auth = function(e3) {
      const t3 = n2.call(this, e3);
      return ["linkAndRetrieveDataWithTicket", "signInAnonymously", "signOut", "getAccessToken", "getLoginState", "signInWithTicket", "getUserInfo"].forEach((e4) => {
        var n3;
        t3[e4] = (n3 = t3[e4], function(e5) {
          e5 = e5 || {};
          const { success: t4, fail: s2, complete: r2 } = ee(e5);
          if (!(t4 || s2 || r2))
            return n3.call(this, e5);
          n3.call(this, e5).then((e6) => {
            t4 && t4(e6), r2 && r2(e6);
          }, (e6) => {
            s2 && s2(e6), r2 && r2(e6);
          });
        }).bind(t3);
      }), t3;
    }, t2.customAuth = t2.auth, t2;
  };
  var Pt = kt;
  async function Ct(e2, t2) {
    const n2 = `http://${e2}:${t2}/system/ping`;
    try {
      const e3 = await (s2 = { url: n2, timeout: 500 }, new Promise((e4, t3) => {
        ne.request({ ...s2, success(t4) {
          e4(t4);
        }, fail(e5) {
          t3(e5);
        } });
      }));
      return !(!e3.data || 0 !== e3.data.code);
    } catch (e3) {
      return false;
    }
    var s2;
  }
  async function Ot(e2, t2) {
    let n2;
    for (let s2 = 0; s2 < e2.length; s2++) {
      const r2 = e2[s2];
      if (await Ct(r2, t2)) {
        n2 = r2;
        break;
      }
    }
    return { address: n2, port: t2 };
  }
  const xt = { "serverless.file.resource.generateProximalSign": "storage/generate-proximal-sign", "serverless.file.resource.report": "storage/report", "serverless.file.resource.delete": "storage/delete", "serverless.file.resource.getTempFileURL": "storage/get-temp-file-url" };
  var Nt = class {
    constructor(e2) {
      if (["spaceId", "clientSecret"].forEach((t2) => {
        if (!Object.prototype.hasOwnProperty.call(e2, t2))
          throw new Error(`${t2} required`);
      }), !e2.endpoint)
        throw new Error("集群空间未配置ApiEndpoint，配置后需要重新关联服务空间后生效");
      this.config = Object.assign({}, e2), this.config.provider = "dcloud", this.config.requestUrl = this.config.endpoint + "/client", this.config.envType = this.config.envType || "public", this.adapter = ne;
    }
    async request(e2, t2 = true) {
      const n2 = t2;
      return e2 = n2 ? await this.setupLocalRequest(e2) : this.setupRequest(e2), Promise.resolve().then(() => n2 ? this.requestLocal(e2) : de.wrappedRequest(e2, this.adapter.request));
    }
    requestLocal(e2) {
      return new Promise((t2, n2) => {
        this.adapter.request(Object.assign(e2, { complete(e3) {
          if (e3 || (e3 = {}), !e3.statusCode || e3.statusCode >= 400) {
            const t3 = e3.data && e3.data.code || "SYS_ERR", s2 = e3.data && e3.data.message || "request:fail";
            return n2(new te({ code: t3, message: s2 }));
          }
          t2({ success: true, result: e3.data });
        } }));
      });
    }
    setupRequest(e2) {
      const t2 = Object.assign({}, e2, { spaceId: this.config.spaceId, timestamp: Date.now() }), n2 = { "Content-Type": "application/json" };
      n2["x-serverless-sign"] = de.sign(t2, this.config.clientSecret);
      const s2 = le();
      n2["x-client-info"] = encodeURIComponent(JSON.stringify(s2));
      const { token: r2 } = re();
      return n2["x-client-token"] = r2, { url: this.config.requestUrl, method: "POST", data: t2, dataType: "json", header: JSON.parse(JSON.stringify(n2)) };
    }
    async setupLocalRequest(e2) {
      const t2 = le(), { token: n2 } = re(), s2 = Object.assign({}, e2, { spaceId: this.config.spaceId, timestamp: Date.now(), clientInfo: t2, token: n2 }), { address: r2, servePort: i2 } = this.__dev__ && this.__dev__.debugInfo || {}, { address: o2 } = await Ot(r2, i2);
      return { url: `http://${o2}:${i2}/${xt[e2.method]}`, method: "POST", data: s2, dataType: "json", header: JSON.parse(JSON.stringify({ "Content-Type": "application/json" })) };
    }
    callFunction(e2) {
      const t2 = { method: "serverless.function.runtime.invoke", params: JSON.stringify({ functionTarget: e2.name, functionArgs: e2.data || {} }) };
      return this.request(t2, false);
    }
    getUploadFileOptions(e2) {
      const t2 = { method: "serverless.file.resource.generateProximalSign", params: JSON.stringify(e2) };
      return this.request(t2);
    }
    reportUploadFile(e2) {
      const t2 = { method: "serverless.file.resource.report", params: JSON.stringify(e2) };
      return this.request(t2);
    }
    uploadFile({ filePath: e2, cloudPath: t2, fileType: n2 = "image", onUploadProgress: s2 }) {
      if (!t2)
        throw new te({ code: "CLOUDPATH_REQUIRED", message: "cloudPath不可为空" });
      let r2;
      return this.getUploadFileOptions({ cloudPath: t2 }).then((t3) => {
        const { url: i2, formData: o2, name: a2 } = t3.result;
        return r2 = t3.result.fileUrl, new Promise((t4, r3) => {
          const c2 = this.adapter.uploadFile({ url: i2, formData: o2, name: a2, filePath: e2, fileType: n2, success(e3) {
            e3 && e3.statusCode < 400 ? t4(e3) : r3(new te({ code: "UPLOAD_FAILED", message: "文件上传失败" }));
          }, fail(e3) {
            r3(new te({ code: e3.code || "UPLOAD_FAILED", message: e3.message || e3.errMsg || "文件上传失败" }));
          } });
          "function" == typeof s2 && c2 && "function" == typeof c2.onProgressUpdate && c2.onProgressUpdate((e3) => {
            s2({ loaded: e3.totalBytesSent, total: e3.totalBytesExpectedToSend });
          });
        });
      }).then(() => this.reportUploadFile({ cloudPath: t2 })).then((t3) => new Promise((n3, s3) => {
        t3.success ? n3({ success: true, filePath: e2, fileID: r2 }) : s3(new te({ code: "UPLOAD_FAILED", message: "文件上传失败" }));
      }));
    }
    deleteFile({ fileList: e2 }) {
      const t2 = { method: "serverless.file.resource.delete", params: JSON.stringify({ fileList: e2 }) };
      return this.request(t2).then((e3) => {
        if (e3.success)
          return e3.result;
        throw new te({ code: "DELETE_FILE_FAILED", message: "删除文件失败" });
      });
    }
    getTempFileURL({ fileList: e2, maxAge: t2 } = {}) {
      if (!Array.isArray(e2) || 0 === e2.length)
        throw new te({ code: "INVALID_PARAM", message: "fileList的元素必须是非空的字符串" });
      const n2 = { method: "serverless.file.resource.getTempFileURL", params: JSON.stringify({ fileList: e2, maxAge: t2 }) };
      return this.request(n2).then((e3) => {
        if (e3.success)
          return { fileList: e3.result.fileList.map((e4) => ({ fileID: e4.fileID, tempFileURL: e4.tempFileURL })) };
        throw new te({ code: "GET_TEMP_FILE_URL_FAILED", message: "获取临时文件链接失败" });
      });
    }
  };
  var Rt = { init(e2) {
    const t2 = new Nt(e2), n2 = { signInAnonymously: function() {
      return Promise.resolve();
    }, getLoginState: function() {
      return Promise.resolve(false);
    } };
    return t2.auth = function() {
      return n2;
    }, t2.customAuth = t2.auth, t2;
  } }, Lt = n(function(e2, t2) {
    e2.exports = r.enc.Hex;
  });
  function Ut() {
    return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(e2) {
      var t2 = 16 * Math.random() | 0;
      return ("x" === e2 ? t2 : 3 & t2 | 8).toString(16);
    });
  }
  function Dt(e2 = "", t2 = {}) {
    const { data: n2, functionName: s2, method: r2, headers: i2, signHeaderKeys: o2 = [], config: a2 } = t2, c2 = String(Date.now()), u2 = Ut(), h2 = Object.assign({}, i2, { "x-from-app-id": a2.spaceAppId, "x-from-env-id": a2.spaceId, "x-to-env-id": a2.spaceId, "x-from-instance-id": c2, "x-from-function-name": s2, "x-client-timestamp": c2, "x-alipay-source": "client", "x-request-id": u2, "x-alipay-callid": u2, "x-trace-id": u2 }), l2 = ["x-from-app-id", "x-from-env-id", "x-to-env-id", "x-from-instance-id", "x-from-function-name", "x-client-timestamp"].concat(o2), [d2 = "", p2 = ""] = e2.split("?") || [], f2 = function(e3) {
      const t3 = "HMAC-SHA256", n3 = e3.signedHeaders.join(";"), s3 = e3.signedHeaders.map((t4) => `${t4.toLowerCase()}:${e3.headers[t4]}
`).join(""), r3 = we(e3.body).toString(Lt), i3 = `${e3.method.toUpperCase()}
${e3.path}
${e3.query}
${s3}
${n3}
${r3}
`, o3 = we(i3).toString(Lt), a3 = `${t3}
${e3.timestamp}
${o3}
`, c3 = Ie(a3, e3.secretKey).toString(Lt);
      return `${t3} Credential=${e3.secretId}, SignedHeaders=${n3}, Signature=${c3}`;
    }({ path: d2, query: p2, method: r2, headers: h2, timestamp: c2, body: JSON.stringify(n2), secretId: a2.accessKey, secretKey: a2.secretKey, signedHeaders: l2.sort() });
    return { url: `${a2.endpoint}${e2}`, headers: Object.assign({}, h2, { Authorization: f2 }) };
  }
  function Mt({ url: e2, data: t2, method: n2 = "POST", headers: s2 = {}, timeout: r2 }) {
    return new Promise((i2, o2) => {
      ne.request({ url: e2, method: n2, data: "object" == typeof t2 ? JSON.stringify(t2) : t2, header: s2, dataType: "json", timeout: r2, complete: (e3 = {}) => {
        const t3 = s2["x-trace-id"] || "";
        if (!e3.statusCode || e3.statusCode >= 400) {
          const { message: n3, errMsg: s3, trace_id: r3 } = e3.data || {};
          return o2(new te({ code: "SYS_ERR", message: n3 || s3 || "request:fail", requestId: r3 || t3 }));
        }
        i2({ status: e3.statusCode, data: e3.data, headers: e3.header, requestId: t3 });
      } });
    });
  }
  function qt(e2, t2) {
    const { path: n2, data: s2, method: r2 = "GET" } = e2, { url: i2, headers: o2 } = Dt(n2, { functionName: "", data: s2, method: r2, headers: { "x-alipay-cloud-mode": "oss", "x-data-api-type": "oss", "x-expire-timestamp": String(Date.now() + 6e4) }, signHeaderKeys: ["x-data-api-type", "x-expire-timestamp"], config: t2 });
    return Mt({ url: i2, data: s2, method: r2, headers: o2 }).then((e3) => {
      const t3 = e3.data || {};
      if (!t3.success)
        throw new te({ code: e3.errCode, message: e3.errMsg, requestId: e3.requestId });
      return t3.data || {};
    }).catch((e3) => {
      throw new te({ code: e3.errCode, message: e3.errMsg, requestId: e3.requestId });
    });
  }
  function Ft(e2 = "") {
    const t2 = e2.trim().replace(/^cloud:\/\//, ""), n2 = t2.indexOf("/");
    if (n2 <= 0)
      throw new te({ code: "INVALID_PARAM", message: "fileID不合法" });
    const s2 = t2.substring(0, n2), r2 = t2.substring(n2 + 1);
    return s2 !== this.config.spaceId && console.warn("file ".concat(e2, " does not belong to env ").concat(this.config.spaceId)), r2;
  }
  function Kt(e2 = "") {
    return "cloud://".concat(this.config.spaceId, "/").concat(e2.replace(/^\/+/, ""));
  }
  class jt {
    constructor(e2) {
      this.config = e2;
    }
    signedURL(e2, t2 = {}) {
      const n2 = `/ws/function/${e2}`, s2 = this.config.wsEndpoint.replace(/^ws(s)?:\/\//, ""), r2 = Object.assign({}, t2, { accessKeyId: this.config.accessKey, signatureNonce: Ut(), timestamp: "" + Date.now() }), i2 = [n2, ["accessKeyId", "authorization", "signatureNonce", "timestamp"].sort().map(function(e3) {
        return r2[e3] ? "".concat(e3, "=").concat(r2[e3]) : null;
      }).filter(Boolean).join("&"), `host:${s2}`].join("\n"), o2 = ["HMAC-SHA256", we(i2).toString(Lt)].join("\n"), a2 = Ie(o2, this.config.secretKey).toString(Lt), c2 = Object.keys(r2).map((e3) => `${e3}=${encodeURIComponent(r2[e3])}`).join("&");
      return `${this.config.wsEndpoint}${n2}?${c2}&signature=${a2}`;
    }
  }
  var $t = class {
    constructor(e2) {
      if (["spaceId", "spaceAppId", "accessKey", "secretKey"].forEach((t2) => {
        if (!Object.prototype.hasOwnProperty.call(e2, t2))
          throw new Error(`${t2} required`);
      }), e2.endpoint) {
        if ("string" != typeof e2.endpoint)
          throw new Error("endpoint must be string");
        if (!/^https:\/\//.test(e2.endpoint))
          throw new Error("endpoint must start with https://");
        e2.endpoint = e2.endpoint.replace(/\/$/, "");
      }
      this.config = Object.assign({}, e2, { endpoint: e2.endpoint || `https://${e2.spaceId}.api-hz.cloudbasefunction.cn`, wsEndpoint: e2.wsEndpoint || `wss://${e2.spaceId}.api-hz.cloudbasefunction.cn` }), this._websocket = new jt(this.config);
    }
    callFunction(e2) {
      return function(e3, t2) {
        const { name: n2, data: s2, async: r2 = false, timeout: i2 } = e3, o2 = "POST", a2 = { "x-to-function-name": n2 };
        r2 && (a2["x-function-invoke-type"] = "async");
        const { url: c2, headers: u2 } = Dt("/functions/invokeFunction", { functionName: n2, data: s2, method: o2, headers: a2, signHeaderKeys: ["x-to-function-name"], config: t2 });
        return Mt({ url: c2, data: s2, method: o2, headers: u2, timeout: i2 }).then((e4) => {
          let t3 = 0;
          if (r2) {
            const n3 = e4.data || {};
            t3 = "200" === n3.errCode ? 0 : n3.errCode, e4.data = n3.data || {}, e4.errMsg = n3.errMsg;
          }
          if (0 !== t3)
            throw new te({ code: t3, message: e4.errMsg, requestId: e4.requestId });
          return { errCode: t3, success: 0 === t3, requestId: e4.requestId, result: e4.data };
        }).catch((e4) => {
          throw new te({ code: e4.errCode, message: e4.errMsg, requestId: e4.requestId });
        });
      }(e2, this.config);
    }
    uploadFileToOSS({ url: e2, filePath: t2, fileType: n2, formData: s2, onUploadProgress: r2 }) {
      return new Promise((i2, o2) => {
        const a2 = ne.uploadFile({ url: e2, filePath: t2, fileType: n2, formData: s2, name: "file", success(e3) {
          e3 && e3.statusCode < 400 ? i2(e3) : o2(new te({ code: "UPLOAD_FAILED", message: "文件上传失败" }));
        }, fail(e3) {
          o2(new te({ code: e3.code || "UPLOAD_FAILED", message: e3.message || e3.errMsg || "文件上传失败" }));
        } });
        "function" == typeof r2 && a2 && "function" == typeof a2.onProgressUpdate && a2.onProgressUpdate((e3) => {
          r2({ loaded: e3.totalBytesSent, total: e3.totalBytesExpectedToSend });
        });
      });
    }
    async uploadFile({ filePath: e2, cloudPath: t2 = "", fileType: n2 = "image", onUploadProgress: s2 }) {
      if ("string" !== g(t2))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath必须为字符串类型" });
      if (!(t2 = t2.trim()))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath不可为空" });
      if (/:\/\//.test(t2))
        throw new te({ code: "INVALID_PARAM", message: "cloudPath不合法" });
      const r2 = await qt({ path: "/".concat(t2.replace(/^\//, ""), "?post_url") }, this.config), { file_id: i2, upload_url: o2, form_data: a2 } = r2, c2 = a2 && a2.reduce((e3, t3) => (e3[t3.key] = t3.value, e3), {});
      return this.uploadFileToOSS({ url: o2, filePath: e2, fileType: n2, formData: c2, onUploadProgress: s2 }).then(() => ({ fileID: i2 }));
    }
    async getTempFileURL({ fileList: e2 }) {
      return new Promise((t2, n2) => {
        (!e2 || e2.length < 0) && t2({ code: "INVALID_PARAM", message: "fileList不能为空数组" }), e2.length > 50 && t2({ code: "INVALID_PARAM", message: "fileList数组长度不能超过50" });
        const s2 = [];
        for (const n3 of e2) {
          let e3;
          "string" !== g(n3) && t2({ code: "INVALID_PARAM", message: "fileList的元素必须是非空的字符串" });
          try {
            e3 = Ft.call(this, n3);
          } catch (t3) {
            console.warn(t3.errCode, t3.errMsg), e3 = n3;
          }
          s2.push({ file_id: e3, expire: 600 });
        }
        qt({ path: "/?download_url", data: { file_list: s2 }, method: "POST" }, this.config).then((e3) => {
          const { file_list: n3 = [] } = e3;
          t2({ fileList: n3.map((e4) => ({ fileID: Kt.call(this, e4.file_id), tempFileURL: e4.download_url })) });
        }).catch((e3) => n2(e3));
      });
    }
    async connectWebSocket(e2) {
      const { name: t2, query: n2 } = e2;
      return ne.connectSocket({ url: this._websocket.signedURL(t2, n2), complete: () => {
      } });
    }
  };
  var Bt = { init: (e2) => {
    e2.provider = "alipay";
    const t2 = new $t(e2);
    return t2.auth = function() {
      return { signInAnonymously: function() {
        return Promise.resolve();
      }, getLoginState: function() {
        return Promise.resolve(true);
      } };
    }, t2;
  } };
  function Wt({ data: e2 }) {
    let t2;
    t2 = le();
    const n2 = JSON.parse(JSON.stringify(e2 || {}));
    if (Object.assign(n2, { clientInfo: t2 }), !n2.uniIdToken) {
      const { token: e3 } = re();
      e3 && (n2.uniIdToken = e3);
    }
    return n2;
  }
  async function Ht(e2 = {}) {
    await this.__dev__.initLocalNetwork();
    const { localAddress: t2, localPort: n2 } = this.__dev__, s2 = { aliyun: "aliyun", tencent: "tcb", alipay: "alipay", dcloud: "dcloud" }[this.config.provider], r2 = this.config.spaceId, i2 = `http://${t2}:${n2}/system/check-function`, o2 = `http://${t2}:${n2}/cloudfunctions/${e2.name}`;
    return new Promise((t3, n3) => {
      ne.request({ method: "POST", url: i2, data: { name: e2.name, platform: P, provider: s2, spaceId: r2 }, timeout: 3e3, success(e3) {
        t3(e3);
      }, fail() {
        t3({ data: { code: "NETWORK_ERROR", message: "连接本地调试服务失败，请检查客户端是否和主机在同一局域网下，自动切换为已部署的云函数。" } });
      } });
    }).then(({ data: e3 } = {}) => {
      const { code: t3, message: n3 } = e3 || {};
      return { code: 0 === t3 ? 0 : t3 || "SYS_ERR", message: n3 || "SYS_ERR" };
    }).then(({ code: t3, message: n3 }) => {
      if (0 !== t3) {
        switch (t3) {
          case "MODULE_ENCRYPTED":
            console.error(`此云函数（${e2.name}）依赖加密公共模块不可本地调试，自动切换为云端已部署的云函数`);
            break;
          case "FUNCTION_ENCRYPTED":
            console.error(`此云函数（${e2.name}）已加密不可本地调试，自动切换为云端已部署的云函数`);
            break;
          case "ACTION_ENCRYPTED":
            console.error(n3 || "需要访问加密的uni-clientDB-action，自动切换为云端环境");
            break;
          case "NETWORK_ERROR":
            console.error(n3 || "连接本地调试服务失败，请检查客户端是否和主机在同一局域网下");
            break;
          case "SWITCH_TO_CLOUD":
            break;
          default: {
            const e3 = `检测本地调试服务出现错误：${n3}，请检查网络环境或重启客户端再试`;
            throw console.error(e3), new Error(e3);
          }
        }
        return this._callCloudFunction(e2);
      }
      return new Promise((t4, n4) => {
        const r3 = Wt.call(this, { data: e2.data });
        ne.request({ method: "POST", url: o2, data: { provider: s2, platform: P, param: r3 }, timeout: e2.timeout, success: ({ statusCode: e3, data: s3 } = {}) => !e3 || e3 >= 400 ? n4(new te({ code: s3.code || "SYS_ERR", message: s3.message || "request:fail" })) : t4({ result: s3 }), fail(e3) {
          n4(new te({ code: e3.code || e3.errCode || "SYS_ERR", message: e3.message || e3.errMsg || "request:fail" }));
        } });
      });
    });
  }
  const Jt = [{ rule: /fc_function_not_found|FUNCTION_NOT_FOUND/, content: "，云函数[{functionName}]在云端不存在，请检查此云函数名称是否正确以及该云函数是否已上传到服务空间", mode: "append" }];
  var zt = /[\\^$.*+?()[\]{}|]/g, Vt = RegExp(zt.source);
  function Gt(e2, t2, n2) {
    return e2.replace(new RegExp((s2 = t2) && Vt.test(s2) ? s2.replace(zt, "\\$&") : s2, "g"), n2);
    var s2;
  }
  const Yt = { NONE: "none", REQUEST: "request", RESPONSE: "response", BOTH: "both" }, Qt = "_globalUniCloudStatus", Xt = "_globalUniCloudSecureNetworkCache__{spaceId}", Zt = "uni-secure-network", en = { SYSTEM_ERROR: { code: 2e4, message: "System error" }, APP_INFO_INVALID: { code: 20101, message: "Invalid client" }, GET_ENCRYPT_KEY_FAILED: { code: 20102, message: "Get encrypt key failed" } };
  function nn(e2) {
    const { errSubject: t2, subject: n2, errCode: s2, errMsg: r2, code: i2, message: o2, cause: a2 } = e2 || {};
    return new te({ subject: t2 || n2 || Zt, code: s2 || i2 || en.SYSTEM_ERROR.code, message: r2 || o2, cause: a2 });
  }
  let Kn;
  function Hn({ secretType: e2 } = {}) {
    return e2 === Yt.REQUEST || e2 === Yt.RESPONSE || e2 === Yt.BOTH;
  }
  function Jn({ name: e2, data: t2 = {} } = {}) {
    return "DCloud-clientDB" === e2 && "encryption" === t2.redirectTo && "getAppClientKey" === t2.action;
  }
  function zn({ provider: e2, spaceId: t2, functionName: n2 } = {}) {
    const { appId: s2, uniPlatform: r2, osName: i2 } = ce();
    let o2 = r2;
    "app" === r2 && (o2 = i2);
    const a2 = function({ provider: e3, spaceId: t3 } = {}) {
      const n3 = A;
      if (!n3)
        return {};
      e3 = /* @__PURE__ */ function(e4) {
        return "tencent" === e4 ? "tcb" : e4;
      }(e3);
      const s3 = n3.find((n4) => n4.provider === e3 && n4.spaceId === t3);
      return s3 && s3.config;
    }({ provider: e2, spaceId: t2 });
    if (!a2 || !a2.accessControl || !a2.accessControl.enable)
      return false;
    const c2 = a2.accessControl.function || {}, u2 = Object.keys(c2);
    if (0 === u2.length)
      return true;
    const h2 = function(e3, t3) {
      let n3, s3, r3;
      for (let i3 = 0; i3 < e3.length; i3++) {
        const o3 = e3[i3];
        o3 !== t3 ? "*" !== o3 ? o3.split(",").map((e4) => e4.trim()).indexOf(t3) > -1 && (s3 = o3) : r3 = o3 : n3 = o3;
      }
      return n3 || s3 || r3;
    }(u2, n2);
    if (!h2)
      return false;
    if ((c2[h2] || []).find((e3 = {}) => e3.appId === s2 && (e3.platform || "").toLowerCase() === o2.toLowerCase()))
      return true;
    throw console.error(`此应用[appId: ${s2}, platform: ${o2}]不在云端配置的允许访问的应用列表内，参考：https://uniapp.dcloud.net.cn/uniCloud/secure-network.html#verify-client`), nn(en.APP_INFO_INVALID);
  }
  function Vn({ functionName: e2, result: t2, logPvd: n2 }) {
    if (this.__dev__.debugLog && t2 && t2.requestId) {
      const s2 = JSON.stringify({ spaceId: this.config.spaceId, functionName: e2, requestId: t2.requestId });
      console.log(`[${n2}-request]${s2}[/${n2}-request]`);
    }
  }
  function Gn(e2) {
    const t2 = e2.callFunction, n2 = function(n3) {
      const s2 = n3.name;
      n3.data = Wt.call(e2, { data: n3.data });
      const r2 = { aliyun: "aliyun", tencent: "tcb", tcb: "tcb", alipay: "alipay", dcloud: "dcloud" }[this.config.provider], i2 = Hn(n3), o2 = Jn(n3), a2 = i2 || o2;
      return t2.call(this, n3).then((e3) => (e3.errCode = 0, !a2 && Vn.call(this, { functionName: s2, result: e3, logPvd: r2 }), Promise.resolve(e3)), (e3) => (!a2 && Vn.call(this, { functionName: s2, result: e3, logPvd: r2 }), e3 && e3.message && (e3.message = function({ message: e4 = "", extraInfo: t3 = {}, formatter: n4 = [] } = {}) {
        for (let s3 = 0; s3 < n4.length; s3++) {
          const { rule: r3, content: i3, mode: o3 } = n4[s3], a3 = e4.match(r3);
          if (!a3)
            continue;
          let c2 = i3;
          for (let e5 = 1; e5 < a3.length; e5++)
            c2 = Gt(c2, `{$${e5}}`, a3[e5]);
          for (const e5 in t3)
            c2 = Gt(c2, `{${e5}}`, t3[e5]);
          return "replace" === o3 ? c2 : e4 + c2;
        }
        return e4;
      }({ message: `[${n3.name}]: ${e3.message}`, formatter: Jt, extraInfo: { functionName: s2 } })), Promise.reject(e3)));
    };
    e2.callFunction = function(t3) {
      const { provider: s2, spaceId: r2 } = e2.config, i2 = t3.name;
      let o2, a2;
      if (t3.data = t3.data || {}, e2.__dev__.debugInfo && !e2.__dev__.debugInfo.forceRemote && O ? (e2._callCloudFunction || (e2._callCloudFunction = n2, e2._callLocalFunction = Ht), o2 = Ht) : o2 = n2, o2 = o2.bind(e2), Jn(t3))
        a2 = n2.call(e2, t3);
      else if (Hn(t3)) {
        a2 = new Kn({ secretType: t3.secretType, uniCloudIns: e2 }).wrapEncryptDataCallFunction(n2.bind(e2))(t3);
      } else if (zn({ provider: s2, spaceId: r2, functionName: i2 })) {
        a2 = new Kn({ secretType: t3.secretType, uniCloudIns: e2 }).wrapVerifyClientCallFunction(n2.bind(e2))(t3);
      } else
        a2 = o2(t3);
      return Object.defineProperty(a2, "result", { get: () => (console.warn("当前返回结果为Promise类型，不可直接访问其result属性，详情请参考：https://uniapp.dcloud.net.cn/uniCloud/faq?id=promise"), {}) }), a2.then((e3) => e3);
    };
  }
  Kn = class {
    constructor() {
      throw nn({ message: `Platform ${P} is not enabled, please check whether secure network module is enabled in your manifest.json` });
    }
  };
  const Yn = Symbol("CLIENT_DB_INTERNAL");
  function Qn(e2, t2) {
    return e2.then = "DoNotReturnProxyWithAFunctionNamedThen", e2._internalType = Yn, e2.inspect = null, e2.__v_raw = void 0, new Proxy(e2, { get(e3, n2, s2) {
      if ("_uniClient" === n2)
        return null;
      if ("symbol" == typeof n2)
        return e3[n2];
      if (n2 in e3 || "string" != typeof n2) {
        const t3 = e3[n2];
        return "function" == typeof t3 ? t3.bind(e3) : t3;
      }
      return t2.get(e3, n2, s2);
    } });
  }
  function Xn(e2) {
    return { on: (t2, n2) => {
      e2[t2] = e2[t2] || [], e2[t2].indexOf(n2) > -1 || e2[t2].push(n2);
    }, off: (t2, n2) => {
      e2[t2] = e2[t2] || [];
      const s2 = e2[t2].indexOf(n2);
      -1 !== s2 && e2[t2].splice(s2, 1);
    } };
  }
  const Zn = ["db.Geo", "db.command", "command.aggregate"];
  function es(e2, t2) {
    return Zn.indexOf(`${e2}.${t2}`) > -1;
  }
  function ts(e2) {
    switch (g(e2 = se(e2))) {
      case "array":
        return e2.map((e3) => ts(e3));
      case "object":
        return e2._internalType === Yn || Object.keys(e2).forEach((t2) => {
          e2[t2] = ts(e2[t2]);
        }), e2;
      case "regexp":
        return { $regexp: { source: e2.source, flags: e2.flags } };
      case "date":
        return { $date: e2.toISOString() };
      default:
        return e2;
    }
  }
  function ns(e2) {
    return e2 && e2.content && e2.content.$method;
  }
  class ss {
    constructor(e2, t2, n2) {
      this.content = e2, this.prevStage = t2 || null, this.udb = null, this._database = n2;
    }
    toJSON() {
      let e2 = this;
      const t2 = [e2.content];
      for (; e2.prevStage; )
        e2 = e2.prevStage, t2.push(e2.content);
      return { $db: t2.reverse().map((e3) => ({ $method: e3.$method, $param: ts(e3.$param) })) };
    }
    toString() {
      return JSON.stringify(this.toJSON());
    }
    getAction() {
      const e2 = this.toJSON().$db.find((e3) => "action" === e3.$method);
      return e2 && e2.$param && e2.$param[0];
    }
    getCommand() {
      return { $db: this.toJSON().$db.filter((e2) => "action" !== e2.$method) };
    }
    get isAggregate() {
      let e2 = this;
      for (; e2; ) {
        const t2 = ns(e2), n2 = ns(e2.prevStage);
        if ("aggregate" === t2 && "collection" === n2 || "pipeline" === t2)
          return true;
        e2 = e2.prevStage;
      }
      return false;
    }
    get isCommand() {
      let e2 = this;
      for (; e2; ) {
        if ("command" === ns(e2))
          return true;
        e2 = e2.prevStage;
      }
      return false;
    }
    get isAggregateCommand() {
      let e2 = this;
      for (; e2; ) {
        const t2 = ns(e2), n2 = ns(e2.prevStage);
        if ("aggregate" === t2 && "command" === n2)
          return true;
        e2 = e2.prevStage;
      }
      return false;
    }
    getNextStageFn(e2) {
      const t2 = this;
      return function() {
        return rs({ $method: e2, $param: ts(Array.from(arguments)) }, t2, t2._database);
      };
    }
    get count() {
      return this.isAggregate ? this.getNextStageFn("count") : function() {
        return this._send("count", Array.from(arguments));
      };
    }
    get remove() {
      return this.isCommand ? this.getNextStageFn("remove") : function() {
        return this._send("remove", Array.from(arguments));
      };
    }
    get() {
      return this._send("get", Array.from(arguments));
    }
    get add() {
      return this.isCommand ? this.getNextStageFn("add") : function() {
        return this._send("add", Array.from(arguments));
      };
    }
    update() {
      return this._send("update", Array.from(arguments));
    }
    end() {
      return this._send("end", Array.from(arguments));
    }
    get set() {
      return this.isCommand ? this.getNextStageFn("set") : function() {
        throw new Error("JQL禁止使用set方法");
      };
    }
    _send(e2, t2) {
      const n2 = this.getAction(), s2 = this.getCommand();
      if (s2.$db.push({ $method: e2, $param: ts(t2) }), b) {
        const e3 = s2.$db.find((e4) => "collection" === e4.$method), t3 = e3 && e3.$param;
        t3 && 1 === t3.length && "string" == typeof e3.$param[0] && e3.$param[0].indexOf(",") > -1 && console.warn("检测到使用JQL语法联表查询时，未使用getTemp先过滤主表数据，在主表数据量大的情况下可能会查询缓慢。\n- 如何优化请参考此文档：https://uniapp.dcloud.net.cn/uniCloud/jql?id=lookup-with-temp \n- 如果主表数据量很小请忽略此信息，项目发行时不会出现此提示。");
      }
      return this._database._callCloudFunction({ action: n2, command: s2 });
    }
  }
  function rs(e2, t2, n2) {
    return Qn(new ss(e2, t2, n2), { get(e3, t3) {
      let s2 = "db";
      return e3 && e3.content && (s2 = e3.content.$method), es(s2, t3) ? rs({ $method: t3 }, e3, n2) : function() {
        return rs({ $method: t3, $param: ts(Array.from(arguments)) }, e3, n2);
      };
    } });
  }
  function is({ path: e2, method: t2 }) {
    return class {
      constructor() {
        this.param = Array.from(arguments);
      }
      toJSON() {
        return { $newDb: [...e2.map((e3) => ({ $method: e3 })), { $method: t2, $param: this.param }] };
      }
      toString() {
        return JSON.stringify(this.toJSON());
      }
    };
  }
  class os {
    constructor({ uniClient: e2 = {}, isJQL: t2 = false } = {}) {
      this._uniClient = e2, this._authCallBacks = {}, this._dbCallBacks = {}, e2._isDefault && (this._dbCallBacks = U("_globalUniCloudDatabaseCallback")), t2 || (this.auth = Xn(this._authCallBacks)), this._isJQL = t2, Object.assign(this, Xn(this._dbCallBacks)), this.env = Qn({}, { get: (e3, t3) => ({ $env: t3 }) }), this.Geo = Qn({}, { get: (e3, t3) => is({ path: ["Geo"], method: t3 }) }), this.serverDate = is({ path: [], method: "serverDate" }), this.RegExp = is({ path: [], method: "RegExp" });
    }
    getCloudEnv(e2) {
      if ("string" != typeof e2 || !e2.trim())
        throw new Error("getCloudEnv参数错误");
      return { $env: e2.replace("$cloudEnv_", "") };
    }
    _callback(e2, t2) {
      const n2 = this._dbCallBacks;
      n2[e2] && n2[e2].forEach((e3) => {
        e3(...t2);
      });
    }
    _callbackAuth(e2, t2) {
      const n2 = this._authCallBacks;
      n2[e2] && n2[e2].forEach((e3) => {
        e3(...t2);
      });
    }
    multiSend() {
      const e2 = Array.from(arguments), t2 = e2.map((e3) => {
        const t3 = e3.getAction(), n2 = e3.getCommand();
        if ("getTemp" !== n2.$db[n2.$db.length - 1].$method)
          throw new Error("multiSend只支持子命令内使用getTemp");
        return { action: t3, command: n2 };
      });
      return this._callCloudFunction({ multiCommand: t2, queryList: e2 });
    }
  }
  function as(e2, t2 = {}) {
    return Qn(new e2(t2), { get: (e3, t3) => es("db", t3) ? rs({ $method: t3 }, null, e3) : function() {
      return rs({ $method: t3, $param: ts(Array.from(arguments)) }, null, e3);
    } });
  }
  class cs extends os {
    _parseResult(e2) {
      return this._isJQL ? e2.result : e2;
    }
    _callCloudFunction({ action: e2, command: t2, multiCommand: n2, queryList: s2 }) {
      function r2(e3, t3) {
        if (n2 && s2)
          for (let n3 = 0; n3 < s2.length; n3++) {
            const r3 = s2[n3];
            r3.udb && "function" == typeof r3.udb.setResult && (t3 ? r3.udb.setResult(t3) : r3.udb.setResult(e3.result.dataList[n3]));
          }
      }
      const i2 = this, o2 = this._isJQL ? "databaseForJQL" : "database";
      function a2(e3) {
        return i2._callback("error", [e3]), j($(o2, "fail"), e3).then(() => j($(o2, "complete"), e3)).then(() => (r2(null, e3), Y(H.RESPONSE, { type: J.CLIENT_DB, content: e3 }), Promise.reject(e3)));
      }
      const c2 = j($(o2, "invoke")), u2 = this._uniClient;
      return c2.then(() => u2.callFunction({ name: "DCloud-clientDB", type: l.CLIENT_DB, data: { action: e2, command: t2, multiCommand: n2 } })).then((e3) => {
        const { code: t3, message: n3, token: s3, tokenExpired: c3, systemInfo: u3 = [] } = e3.result;
        if (u3)
          for (let e4 = 0; e4 < u3.length; e4++) {
            const { level: t4, message: n4, detail: s4 } = u3[e4];
            let r3 = "[System Info]" + n4;
            s4 && (r3 = `${r3}
详细信息：${s4}`), (console["warn" === t4 ? "error" : t4] || console.log)(r3);
          }
        if (t3) {
          return a2(new te({ code: t3, message: n3, requestId: e3.requestId }));
        }
        e3.result.errCode = e3.result.errCode || e3.result.code, e3.result.errMsg = e3.result.errMsg || e3.result.message, s3 && c3 && (ie({ token: s3, tokenExpired: c3 }), this._callbackAuth("refreshToken", [{ token: s3, tokenExpired: c3 }]), this._callback("refreshToken", [{ token: s3, tokenExpired: c3 }]), Y(H.REFRESH_TOKEN, { token: s3, tokenExpired: c3 }));
        const h2 = [{ prop: "affectedDocs", tips: "affectedDocs不再推荐使用，请使用inserted/deleted/updated/data.length替代" }, { prop: "code", tips: "code不再推荐使用，请使用errCode替代" }, { prop: "message", tips: "message不再推荐使用，请使用errMsg替代" }];
        for (let t4 = 0; t4 < h2.length; t4++) {
          const { prop: n4, tips: s4 } = h2[t4];
          if (n4 in e3.result) {
            const t5 = e3.result[n4];
            Object.defineProperty(e3.result, n4, { get: () => (console.warn(s4), t5) });
          }
        }
        return function(e4) {
          return j($(o2, "success"), e4).then(() => j($(o2, "complete"), e4)).then(() => {
            r2(e4, null);
            const t4 = i2._parseResult(e4);
            return Y(H.RESPONSE, { type: J.CLIENT_DB, content: t4 }), Promise.resolve(t4);
          });
        }(e3);
      }, (e3) => {
        /fc_function_not_found|FUNCTION_NOT_FOUND/g.test(e3.message) && console.warn("clientDB未初始化，请在web控制台保存一次schema以开启clientDB");
        return a2(new te({ code: e3.code || "SYSTEM_ERROR", message: e3.message, requestId: e3.requestId }));
      });
    }
  }
  const us = "token无效，跳转登录页面", hs = "token过期，跳转登录页面", ls = { TOKEN_INVALID_TOKEN_EXPIRED: hs, TOKEN_INVALID_INVALID_CLIENTID: us, TOKEN_INVALID: us, TOKEN_INVALID_WRONG_TOKEN: us, TOKEN_INVALID_ANONYMOUS_USER: us }, ds = { "uni-id-token-expired": hs, "uni-id-check-token-failed": us, "uni-id-token-not-exist": us, "uni-id-check-device-feature-failed": us }, ps = { ...ls, ...ds, default: "用户未登录或登录状态过期，自动跳转登录页面" };
  function fs(e2, t2) {
    let n2 = "";
    return n2 = e2 ? `${e2}/${t2}` : t2, n2.replace(/^\//, "");
  }
  function gs(e2 = [], t2 = "") {
    const n2 = [], s2 = [];
    return e2.forEach((e3) => {
      true === e3.needLogin ? n2.push(fs(t2, e3.path)) : false === e3.needLogin && s2.push(fs(t2, e3.path));
    }), { needLoginPage: n2, notNeedLoginPage: s2 };
  }
  function ms(e2) {
    return e2.split("?")[0].replace(/^\//, "");
  }
  function ys() {
    return function(e2) {
      let t2 = e2 && e2.$page && e2.$page.fullPath;
      return t2 ? ("/" !== t2.charAt(0) && (t2 = "/" + t2), t2) : "";
    }(function() {
      const e2 = getCurrentPages();
      return e2[e2.length - 1];
    }());
  }
  function _s() {
    return ms(ys());
  }
  function ws(e2 = "", t2 = {}) {
    if (!e2)
      return false;
    if (!(t2 && t2.list && t2.list.length))
      return false;
    const n2 = t2.list, s2 = ms(e2);
    return n2.some((e3) => e3.pagePath === s2);
  }
  const Is = !!e.uniIdRouter;
  const { loginPage: vs, routerNeedLogin: Ss, resToLogin: Ts, needLoginPage: bs, notNeedLoginPage: Es, loginPageInTabBar: ks } = function({ pages: t2 = [], subPackages: n2 = [], uniIdRouter: s2 = {}, tabBar: r2 = {} } = e) {
    const { loginPage: i2, needLogin: o2 = [], resToLogin: a2 = true } = s2, { needLoginPage: c2, notNeedLoginPage: u2 } = gs(t2), { needLoginPage: h2, notNeedLoginPage: l2 } = function(e2 = []) {
      const t3 = [], n3 = [];
      return e2.forEach((e3) => {
        const { root: s3, pages: r3 = [] } = e3, { needLoginPage: i3, notNeedLoginPage: o3 } = gs(r3, s3);
        t3.push(...i3), n3.push(...o3);
      }), { needLoginPage: t3, notNeedLoginPage: n3 };
    }(n2);
    return { loginPage: i2, routerNeedLogin: o2, resToLogin: a2, needLoginPage: [...c2, ...h2], notNeedLoginPage: [...u2, ...l2], loginPageInTabBar: ws(i2, r2) };
  }();
  if (bs.indexOf(vs) > -1)
    throw new Error(`Login page [${vs}] should not be "needLogin", please check your pages.json`);
  function As(e2) {
    const t2 = _s();
    if ("/" === e2.charAt(0))
      return e2;
    const [n2, s2] = e2.split("?"), r2 = n2.replace(/^\//, "").split("/"), i2 = t2.split("/");
    i2.pop();
    for (let e3 = 0; e3 < r2.length; e3++) {
      const t3 = r2[e3];
      ".." === t3 ? i2.pop() : "." !== t3 && i2.push(t3);
    }
    return "" === i2[0] && i2.shift(), "/" + i2.join("/") + (s2 ? "?" + s2 : "");
  }
  function Ps(e2) {
    const t2 = ms(As(e2));
    return !(Es.indexOf(t2) > -1) && (bs.indexOf(t2) > -1 || Ss.some((t3) => function(e3, t4) {
      return new RegExp(t4).test(e3);
    }(e2, t3)));
  }
  function Cs({ redirect: e2 }) {
    const t2 = ms(e2), n2 = ms(vs);
    return _s() !== n2 && t2 !== n2;
  }
  function Os({ api: e2, redirect: t2 } = {}) {
    if (!t2 || !Cs({ redirect: t2 }))
      return;
    const n2 = function(e3, t3) {
      return "/" !== e3.charAt(0) && (e3 = "/" + e3), t3 ? e3.indexOf("?") > -1 ? e3 + `&uniIdRedirectUrl=${encodeURIComponent(t3)}` : e3 + `?uniIdRedirectUrl=${encodeURIComponent(t3)}` : e3;
    }(vs, t2);
    ks ? "navigateTo" !== e2 && "redirectTo" !== e2 || (e2 = "switchTab") : "switchTab" === e2 && (e2 = "navigateTo");
    const s2 = { navigateTo: uni.navigateTo, redirectTo: uni.redirectTo, switchTab: uni.switchTab, reLaunch: uni.reLaunch };
    setTimeout(() => {
      s2[e2]({ url: n2 });
    }, 0);
  }
  function xs({ url: e2 } = {}) {
    const t2 = { abortLoginPageJump: false, autoToLoginPage: false }, n2 = function() {
      const { token: e3, tokenExpired: t3 } = re();
      let n3;
      if (e3) {
        if (t3 < Date.now()) {
          const e4 = "uni-id-token-expired";
          n3 = { errCode: e4, errMsg: ps[e4] };
        }
      } else {
        const e4 = "uni-id-check-token-failed";
        n3 = { errCode: e4, errMsg: ps[e4] };
      }
      return n3;
    }();
    if (Ps(e2) && n2) {
      n2.uniIdRedirectUrl = e2;
      if (z(H.NEED_LOGIN).length > 0)
        return setTimeout(() => {
          Y(H.NEED_LOGIN, n2);
        }, 0), t2.abortLoginPageJump = true, t2;
      t2.autoToLoginPage = true;
    }
    return t2;
  }
  function Ns() {
    !function() {
      const e3 = ys(), { abortLoginPageJump: t2, autoToLoginPage: n2 } = xs({ url: e3 });
      t2 || n2 && Os({ api: "redirectTo", redirect: e3 });
    }();
    const e2 = ["navigateTo", "redirectTo", "reLaunch", "switchTab"];
    for (let t2 = 0; t2 < e2.length; t2++) {
      const n2 = e2[t2];
      uni.addInterceptor(n2, { invoke(e3) {
        const { abortLoginPageJump: t3, autoToLoginPage: s2 } = xs({ url: e3.url });
        return t3 ? e3 : s2 ? (Os({ api: n2, redirect: As(e3.url) }), false) : e3;
      } });
    }
  }
  function Rs() {
    this.onResponse((e2) => {
      const { type: t2, content: n2 } = e2;
      let s2 = false;
      switch (t2) {
        case "cloudobject":
          s2 = function(e3) {
            if ("object" != typeof e3)
              return false;
            const { errCode: t3 } = e3 || {};
            return t3 in ps;
          }(n2);
          break;
        case "clientdb":
          s2 = function(e3) {
            if ("object" != typeof e3)
              return false;
            const { errCode: t3 } = e3 || {};
            return t3 in ls;
          }(n2);
      }
      s2 && function(e3 = {}) {
        const t3 = z(H.NEED_LOGIN);
        Z().then(() => {
          const n3 = ys();
          if (n3 && Cs({ redirect: n3 }))
            return t3.length > 0 ? Y(H.NEED_LOGIN, Object.assign({ uniIdRedirectUrl: n3 }, e3)) : void (vs && Os({ api: "navigateTo", redirect: n3 }));
        });
      }(n2);
    });
  }
  function Ls(e2) {
    !function(e3) {
      e3.onResponse = function(e4) {
        V(H.RESPONSE, e4);
      }, e3.offResponse = function(e4) {
        G(H.RESPONSE, e4);
      };
    }(e2), function(e3) {
      e3.onNeedLogin = function(e4) {
        V(H.NEED_LOGIN, e4);
      }, e3.offNeedLogin = function(e4) {
        G(H.NEED_LOGIN, e4);
      }, Is && (U(Qt).needLoginInit || (U(Qt).needLoginInit = true, Z().then(() => {
        Ns.call(e3);
      }), Ts && Rs.call(e3)));
    }(e2), function(e3) {
      e3.onRefreshToken = function(e4) {
        V(H.REFRESH_TOKEN, e4);
      }, e3.offRefreshToken = function(e4) {
        G(H.REFRESH_TOKEN, e4);
      };
    }(e2);
  }
  let Us;
  const Ds = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=", Ms = /^(?:[A-Za-z\d+/]{4})*?(?:[A-Za-z\d+/]{2}(?:==)?|[A-Za-z\d+/]{3}=?)?$/;
  function qs() {
    const e2 = re().token || "", t2 = e2.split(".");
    if (!e2 || 3 !== t2.length)
      return { uid: null, role: [], permission: [], tokenExpired: 0 };
    let n2;
    try {
      n2 = JSON.parse((s2 = t2[1], decodeURIComponent(Us(s2).split("").map(function(e3) {
        return "%" + ("00" + e3.charCodeAt(0).toString(16)).slice(-2);
      }).join(""))));
    } catch (e3) {
      throw new Error("获取当前用户信息出错，详细错误信息为：" + e3.message);
    }
    var s2;
    return n2.tokenExpired = 1e3 * n2.exp, delete n2.exp, delete n2.iat, n2;
  }
  Us = "function" != typeof atob ? function(e2) {
    if (e2 = String(e2).replace(/[\t\n\f\r ]+/g, ""), !Ms.test(e2))
      throw new Error("Failed to execute 'atob' on 'Window': The string to be decoded is not correctly encoded.");
    var t2;
    e2 += "==".slice(2 - (3 & e2.length));
    for (var n2, s2, r2 = "", i2 = 0; i2 < e2.length; )
      t2 = Ds.indexOf(e2.charAt(i2++)) << 18 | Ds.indexOf(e2.charAt(i2++)) << 12 | (n2 = Ds.indexOf(e2.charAt(i2++))) << 6 | (s2 = Ds.indexOf(e2.charAt(i2++))), r2 += 64 === n2 ? String.fromCharCode(t2 >> 16 & 255) : 64 === s2 ? String.fromCharCode(t2 >> 16 & 255, t2 >> 8 & 255) : String.fromCharCode(t2 >> 16 & 255, t2 >> 8 & 255, 255 & t2);
    return r2;
  } : atob;
  var Fs = n(function(e2, t2) {
    Object.defineProperty(t2, "__esModule", { value: true });
    const n2 = "chooseAndUploadFile:ok", s2 = "chooseAndUploadFile:fail";
    function r2(e3, t3) {
      return e3.tempFiles.forEach((e4, n3) => {
        e4.name || (e4.name = e4.path.substring(e4.path.lastIndexOf("/") + 1)), t3 && (e4.fileType = t3), e4.cloudPath = Date.now() + "_" + n3 + e4.name.substring(e4.name.lastIndexOf("."));
      }), e3.tempFilePaths || (e3.tempFilePaths = e3.tempFiles.map((e4) => e4.path)), e3;
    }
    function i2(e3, t3, { onChooseFile: s3, onUploadProgress: r3 }) {
      return t3.then((e4) => {
        if (s3) {
          const t4 = s3(e4);
          if (void 0 !== t4)
            return Promise.resolve(t4).then((t5) => void 0 === t5 ? e4 : t5);
        }
        return e4;
      }).then((t4) => false === t4 ? { errMsg: n2, tempFilePaths: [], tempFiles: [] } : function(e4, t5, s4 = 5, r4) {
        (t5 = Object.assign({}, t5)).errMsg = n2;
        const i3 = t5.tempFiles, o2 = i3.length;
        let a2 = 0;
        return new Promise((n3) => {
          for (; a2 < s4; )
            c2();
          function c2() {
            const s5 = a2++;
            if (s5 >= o2)
              return void (!i3.find((e5) => !e5.url && !e5.errMsg) && n3(t5));
            const u2 = i3[s5];
            e4.uploadFile({ provider: u2.provider, filePath: u2.path, cloudPath: u2.cloudPath, fileType: u2.fileType, cloudPathAsRealPath: u2.cloudPathAsRealPath, onUploadProgress(e5) {
              e5.index = s5, e5.tempFile = u2, e5.tempFilePath = u2.path, r4 && r4(e5);
            } }).then((e5) => {
              u2.url = e5.fileID, s5 < o2 && c2();
            }).catch((e5) => {
              u2.errMsg = e5.errMsg || e5.message, s5 < o2 && c2();
            });
          }
        });
      }(e3, t4, 5, r3));
    }
    t2.initChooseAndUploadFile = function(e3) {
      return function(t3 = { type: "all" }) {
        return "image" === t3.type ? i2(e3, function(e4) {
          const { count: t4, sizeType: n3, sourceType: i3 = ["album", "camera"], extension: o2 } = e4;
          return new Promise((e5, a2) => {
            uni.chooseImage({ count: t4, sizeType: n3, sourceType: i3, extension: o2, success(t5) {
              e5(r2(t5, "image"));
            }, fail(e6) {
              a2({ errMsg: e6.errMsg.replace("chooseImage:fail", s2) });
            } });
          });
        }(t3), t3) : "video" === t3.type ? i2(e3, function(e4) {
          const { camera: t4, compressed: n3, maxDuration: i3, sourceType: o2 = ["album", "camera"], extension: a2 } = e4;
          return new Promise((e5, c2) => {
            uni.chooseVideo({ camera: t4, compressed: n3, maxDuration: i3, sourceType: o2, extension: a2, success(t5) {
              const { tempFilePath: n4, duration: s3, size: i4, height: o3, width: a3 } = t5;
              e5(r2({ errMsg: "chooseVideo:ok", tempFilePaths: [n4], tempFiles: [{ name: t5.tempFile && t5.tempFile.name || "", path: n4, size: i4, type: t5.tempFile && t5.tempFile.type || "", width: a3, height: o3, duration: s3, fileType: "video", cloudPath: "" }] }, "video"));
            }, fail(e6) {
              c2({ errMsg: e6.errMsg.replace("chooseVideo:fail", s2) });
            } });
          });
        }(t3), t3) : i2(e3, function(e4) {
          const { count: t4, extension: n3 } = e4;
          return new Promise((e5, i3) => {
            let o2 = uni.chooseFile;
            if ("undefined" != typeof wx && "function" == typeof wx.chooseMessageFile && (o2 = wx.chooseMessageFile), "function" != typeof o2)
              return i3({ errMsg: s2 + " 请指定 type 类型，该平台仅支持选择 image 或 video。" });
            o2({ type: "all", count: t4, extension: n3, success(t5) {
              e5(r2(t5));
            }, fail(e6) {
              i3({ errMsg: e6.errMsg.replace("chooseFile:fail", s2) });
            } });
          });
        }(t3), t3);
      };
    };
  }), Ks = t(Fs);
  const js = { auto: "auto", onready: "onready", manual: "manual" };
  function $s(e2) {
    return { props: { localdata: { type: Array, default: () => [] }, options: { type: [Object, Array], default: () => ({}) }, spaceInfo: { type: Object, default: () => ({}) }, collection: { type: [String, Array], default: "" }, action: { type: String, default: "" }, field: { type: String, default: "" }, orderby: { type: String, default: "" }, where: { type: [String, Object], default: "" }, pageData: { type: String, default: "add" }, pageCurrent: { type: Number, default: 1 }, pageSize: { type: Number, default: 20 }, getcount: { type: [Boolean, String], default: false }, gettree: { type: [Boolean, String], default: false }, gettreepath: { type: [Boolean, String], default: false }, startwith: { type: String, default: "" }, limitlevel: { type: Number, default: 10 }, groupby: { type: String, default: "" }, groupField: { type: String, default: "" }, distinct: { type: [Boolean, String], default: false }, foreignKey: { type: String, default: "" }, loadtime: { type: String, default: "auto" }, manual: { type: Boolean, default: false } }, data: () => ({ mixinDatacomLoading: false, mixinDatacomHasMore: false, mixinDatacomResData: [], mixinDatacomErrorMessage: "", mixinDatacomPage: {}, mixinDatacomError: null }), created() {
      this.mixinDatacomPage = { current: this.pageCurrent, size: this.pageSize, count: 0 }, this.$watch(() => {
        var e3 = [];
        return ["pageCurrent", "pageSize", "localdata", "collection", "action", "field", "orderby", "where", "getont", "getcount", "gettree", "groupby", "groupField", "distinct"].forEach((t2) => {
          e3.push(this[t2]);
        }), e3;
      }, (e3, t2) => {
        if (this.loadtime === js.manual)
          return;
        let n2 = false;
        const s2 = [];
        for (let r2 = 2; r2 < e3.length; r2++)
          e3[r2] !== t2[r2] && (s2.push(e3[r2]), n2 = true);
        e3[0] !== t2[0] && (this.mixinDatacomPage.current = this.pageCurrent), this.mixinDatacomPage.size = this.pageSize, this.onMixinDatacomPropsChange(n2, s2);
      });
    }, methods: { onMixinDatacomPropsChange(e3, t2) {
    }, mixinDatacomEasyGet({ getone: e3 = false, success: t2, fail: n2 } = {}) {
      this.mixinDatacomLoading || (this.mixinDatacomLoading = true, this.mixinDatacomErrorMessage = "", this.mixinDatacomError = null, this.mixinDatacomGet().then((n3) => {
        this.mixinDatacomLoading = false;
        const { data: s2, count: r2 } = n3.result;
        this.getcount && (this.mixinDatacomPage.count = r2), this.mixinDatacomHasMore = s2.length < this.pageSize;
        const i2 = e3 ? s2.length ? s2[0] : void 0 : s2;
        this.mixinDatacomResData = i2, t2 && t2(i2);
      }).catch((e4) => {
        this.mixinDatacomLoading = false, this.mixinDatacomErrorMessage = e4, this.mixinDatacomError = e4, n2 && n2(e4);
      }));
    }, mixinDatacomGet(t2 = {}) {
      let n2;
      t2 = t2 || {}, n2 = "undefined" != typeof __uniX && __uniX ? e2.databaseForJQL(this.spaceInfo) : e2.database(this.spaceInfo);
      const s2 = t2.action || this.action;
      s2 && (n2 = n2.action(s2));
      const r2 = t2.collection || this.collection;
      n2 = Array.isArray(r2) ? n2.collection(...r2) : n2.collection(r2);
      const i2 = t2.where || this.where;
      i2 && Object.keys(i2).length && (n2 = n2.where(i2));
      const o2 = t2.field || this.field;
      o2 && (n2 = n2.field(o2));
      const a2 = t2.foreignKey || this.foreignKey;
      a2 && (n2 = n2.foreignKey(a2));
      const c2 = t2.groupby || this.groupby;
      c2 && (n2 = n2.groupBy(c2));
      const u2 = t2.groupField || this.groupField;
      u2 && (n2 = n2.groupField(u2));
      true === (void 0 !== t2.distinct ? t2.distinct : this.distinct) && (n2 = n2.distinct());
      const h2 = t2.orderby || this.orderby;
      h2 && (n2 = n2.orderBy(h2));
      const l2 = void 0 !== t2.pageCurrent ? t2.pageCurrent : this.mixinDatacomPage.current, d2 = void 0 !== t2.pageSize ? t2.pageSize : this.mixinDatacomPage.size, p2 = void 0 !== t2.getcount ? t2.getcount : this.getcount, f2 = void 0 !== t2.gettree ? t2.gettree : this.gettree, g2 = void 0 !== t2.gettreepath ? t2.gettreepath : this.gettreepath, m2 = { getCount: p2 }, y2 = { limitLevel: void 0 !== t2.limitlevel ? t2.limitlevel : this.limitlevel, startWith: void 0 !== t2.startwith ? t2.startwith : this.startwith };
      return f2 && (m2.getTree = y2), g2 && (m2.getTreePath = y2), n2 = n2.skip(d2 * (l2 - 1)).limit(d2).get(m2), n2;
    } } };
  }
  function Bs(e2) {
    return function(t2, n2 = {}) {
      n2 = function(e3, t3 = {}) {
        return e3.customUI = t3.customUI || e3.customUI, e3.parseSystemError = t3.parseSystemError || e3.parseSystemError, Object.assign(e3.loadingOptions, t3.loadingOptions), Object.assign(e3.errorOptions, t3.errorOptions), "object" == typeof t3.secretMethods && (e3.secretMethods = t3.secretMethods), e3;
      }({ customUI: false, loadingOptions: { title: "加载中...", mask: true }, errorOptions: { type: "modal", retry: false } }, n2);
      const { customUI: s2, loadingOptions: r2, errorOptions: i2, parseSystemError: o2 } = n2, a2 = !s2;
      return new Proxy({}, { get(s3, c2) {
        switch (c2) {
          case "toString":
            return "[object UniCloudObject]";
          case "toJSON":
            return {};
        }
        return function({ fn: e3, interceptorName: t3, getCallbackArgs: n3 } = {}) {
          return async function(...s4) {
            const r3 = n3 ? n3({ params: s4 }) : {};
            let i3, o3;
            try {
              return await j($(t3, "invoke"), { ...r3 }), i3 = await e3(...s4), await j($(t3, "success"), { ...r3, result: i3 }), i3;
            } catch (e4) {
              throw o3 = e4, await j($(t3, "fail"), { ...r3, error: o3 }), o3;
            } finally {
              await j($(t3, "complete"), o3 ? { ...r3, error: o3 } : { ...r3, result: i3 });
            }
          };
        }({ fn: async function s4(...u2) {
          let h2;
          a2 && uni.showLoading({ title: r2.title, mask: r2.mask });
          const d2 = { name: t2, type: l.OBJECT, data: { method: c2, params: u2 } };
          "object" == typeof n2.secretMethods && function(e3, t3) {
            const n3 = t3.data.method, s5 = e3.secretMethods || {}, r3 = s5[n3] || s5["*"];
            r3 && (t3.secretType = r3);
          }(n2, d2);
          let p2 = false;
          try {
            h2 = await e2.callFunction(d2);
          } catch (e3) {
            p2 = true, h2 = { result: new te(e3) };
          }
          const { errSubject: f2, errCode: g2, errMsg: m2, newToken: y2 } = h2.result || {};
          if (a2 && uni.hideLoading(), y2 && y2.token && y2.tokenExpired && (ie(y2), Y(H.REFRESH_TOKEN, { ...y2 })), g2) {
            let e3 = m2;
            if (p2 && o2) {
              e3 = (await o2({ objectName: t2, methodName: c2, params: u2, errSubject: f2, errCode: g2, errMsg: m2 })).errMsg || m2;
            }
            if (a2)
              if ("toast" === i2.type)
                uni.showToast({ title: e3, icon: "none" });
              else {
                if ("modal" !== i2.type)
                  throw new Error(`Invalid errorOptions.type: ${i2.type}`);
                {
                  const { confirm: t3 } = await async function({ title: e4, content: t4, showCancel: n4, cancelText: s5, confirmText: r3 } = {}) {
                    return new Promise((i3, o3) => {
                      uni.showModal({ title: e4, content: t4, showCancel: n4, cancelText: s5, confirmText: r3, success(e5) {
                        i3(e5);
                      }, fail() {
                        i3({ confirm: false, cancel: true });
                      } });
                    });
                  }({ title: "提示", content: e3, showCancel: i2.retry, cancelText: "取消", confirmText: i2.retry ? "重试" : "确定" });
                  if (i2.retry && t3)
                    return s4(...u2);
                }
              }
            const n3 = new te({ subject: f2, code: g2, message: m2, requestId: h2.requestId });
            throw n3.detail = h2.result, Y(H.RESPONSE, { type: J.CLOUD_OBJECT, content: n3 }), n3;
          }
          return Y(H.RESPONSE, { type: J.CLOUD_OBJECT, content: h2.result }), h2.result;
        }, interceptorName: "callObject", getCallbackArgs: function({ params: e3 } = {}) {
          return { objectName: t2, methodName: c2, params: e3 };
        } });
      } });
    };
  }
  function Ws(e2) {
    return U(Xt.replace("{spaceId}", e2.config.spaceId));
  }
  async function Hs({ openid: e2, callLoginByWeixin: t2 = false } = {}) {
    Ws(this);
    throw new Error(`[SecureNetwork] API \`initSecureNetworkByWeixin\` is not supported on platform \`${P}\``);
  }
  async function Js(e2) {
    const t2 = Ws(this);
    return t2.initPromise || (t2.initPromise = Hs.call(this, e2).then((e3) => e3).catch((e3) => {
      throw delete t2.initPromise, e3;
    })), t2.initPromise;
  }
  function zs(e2) {
    return function({ openid: t2, callLoginByWeixin: n2 = false } = {}) {
      return Js.call(e2, { openid: t2, callLoginByWeixin: n2 });
    };
  }
  function Vs(e2) {
    !function(e3) {
      he = e3;
    }(e2);
  }
  function Gs(e2) {
    const n2 = { getAppBaseInfo: uni.getSystemInfo, getPushClientId: uni.getPushClientId };
    return function(s2) {
      return new Promise((r2, i2) => {
        n2[e2]({ ...s2, success(e3) {
          r2(e3);
        }, fail(e3) {
          i2(e3);
        } });
      });
    };
  }
  class Ys extends S {
    constructor() {
      super(), this._uniPushMessageCallback = this._receivePushMessage.bind(this), this._currentMessageId = -1, this._payloadQueue = [];
    }
    init() {
      return Promise.all([Gs("getAppBaseInfo")(), Gs("getPushClientId")()]).then(([{ appId: e2 } = {}, { cid: t2 } = {}] = []) => {
        if (!e2)
          throw new Error("Invalid appId, please check the manifest.json file");
        if (!t2)
          throw new Error("Invalid push client id");
        this._appId = e2, this._pushClientId = t2, this._seqId = Date.now() + "-" + Math.floor(9e5 * Math.random() + 1e5), this.emit("open"), this._initMessageListener();
      }, (e2) => {
        throw this.emit("error", e2), this.close(), e2;
      });
    }
    async open() {
      return this.init();
    }
    _isUniCloudSSE(e2) {
      if ("receive" !== e2.type)
        return false;
      const t2 = e2 && e2.data && e2.data.payload;
      return !(!t2 || "UNI_CLOUD_SSE" !== t2.channel || t2.seqId !== this._seqId);
    }
    _receivePushMessage(e2) {
      if (!this._isUniCloudSSE(e2))
        return;
      const t2 = e2 && e2.data && e2.data.payload, { action: n2, messageId: s2, message: r2 } = t2;
      this._payloadQueue.push({ action: n2, messageId: s2, message: r2 }), this._consumMessage();
    }
    _consumMessage() {
      for (; ; ) {
        const e2 = this._payloadQueue.find((e3) => e3.messageId === this._currentMessageId + 1);
        if (!e2)
          break;
        this._currentMessageId++, this._parseMessagePayload(e2);
      }
    }
    _parseMessagePayload(e2) {
      const { action: t2, messageId: n2, message: s2 } = e2;
      "end" === t2 ? this._end({ messageId: n2, message: s2 }) : "message" === t2 && this._appendMessage({ messageId: n2, message: s2 });
    }
    _appendMessage({ messageId: e2, message: t2 } = {}) {
      this.emit("message", t2);
    }
    _end({ messageId: e2, message: t2 } = {}) {
      this.emit("end", t2), this.close();
    }
    _initMessageListener() {
      uni.onPushMessage(this._uniPushMessageCallback);
    }
    _destroy() {
      uni.offPushMessage(this._uniPushMessageCallback);
    }
    toJSON() {
      return { appId: this._appId, pushClientId: this._pushClientId, seqId: this._seqId };
    }
    close() {
      this._destroy(), this.emit("close");
    }
  }
  async function Qs(e2) {
    {
      const { osName: e3, osVersion: t3 } = ce();
      "ios" === e3 && function(e4) {
        if (!e4 || "string" != typeof e4)
          return 0;
        const t4 = e4.match(/^(\d+)./);
        return t4 && t4[1] ? parseInt(t4[1]) : 0;
      }(t3) >= 14 && console.warn("iOS 14及以上版本连接uniCloud本地调试服务需要允许客户端查找并连接到本地网络上的设备（仅开发期间需要，发行后不需要）");
    }
    const t2 = e2.__dev__;
    if (!t2.debugInfo)
      return;
    const { address: n2, servePort: s2 } = t2.debugInfo, { address: r2 } = await Ot(n2, s2);
    if (r2)
      return t2.localAddress = r2, void (t2.localPort = s2);
    const i2 = console["error"];
    let o2 = "";
    if ("remote" === t2.debugInfo.initialLaunchType ? (t2.debugInfo.forceRemote = true, o2 = "当前客户端和HBuilderX不在同一局域网下（或其他网络原因无法连接HBuilderX），uniCloud本地调试服务不对当前客户端生效。\n- 如果不使用uniCloud本地调试服务，请直接忽略此信息。\n- 如需使用uniCloud本地调试服务，请将客户端与主机连接到同一局域网下并重新运行到客户端。") : o2 = "无法连接uniCloud本地调试服务，请检查当前客户端是否与主机在同一局域网下。\n- 如需使用uniCloud本地调试服务，请将客户端与主机连接到同一局域网下并重新运行到客户端。", o2 += "\n- 如果在HBuilderX开启的状态下切换过网络环境，请重启HBuilderX后再试\n- 检查系统防火墙是否拦截了HBuilderX自带的nodejs\n- 检查是否错误的使用拦截器修改uni.request方法的参数", 0 === P.indexOf("mp-") && (o2 += "\n- 小程序中如何使用uniCloud，请参考：https://uniapp.dcloud.net.cn/uniCloud/publish.html#useinmp"), !t2.debugInfo.forceRemote)
      throw new Error(o2);
    i2(o2);
  }
  function Xs(e2) {
    e2._initPromiseHub || (e2._initPromiseHub = new v({ createPromise: function() {
      let t2 = Promise.resolve();
      var n2;
      n2 = 1, t2 = new Promise((e3) => {
        setTimeout(() => {
          e3();
        }, n2);
      });
      const s2 = e2.auth();
      return t2.then(() => s2.getLoginState()).then((e3) => e3 ? Promise.resolve() : s2.signInAnonymously());
    } }));
  }
  const Zs = { tcb: Pt, tencent: Pt, aliyun: fe, private: Rt, dcloud: Rt, alipay: Bt };
  let er = new class {
    init(e2) {
      let t2 = {};
      const n2 = Zs[e2.provider];
      if (!n2)
        throw new Error("未提供正确的provider参数");
      t2 = n2.init(e2), function(e3) {
        const t3 = {};
        e3.__dev__ = t3, t3.debugLog = "app" === P;
        const n3 = C;
        n3 && !n3.code && (t3.debugInfo = n3);
        const s2 = new v({ createPromise: function() {
          return Qs(e3);
        } });
        t3.initLocalNetwork = function() {
          return s2.exec();
        };
      }(t2), Xs(t2), Gn(t2), function(e3) {
        const t3 = e3.uploadFile;
        e3.uploadFile = function(e4) {
          return t3.call(this, e4);
        };
      }(t2), function(e3) {
        e3.database = function(t3) {
          if (t3 && Object.keys(t3).length > 0)
            return e3.init(t3).database();
          if (this._database)
            return this._database;
          const n3 = as(cs, { uniClient: e3 });
          return this._database = n3, n3;
        }, e3.databaseForJQL = function(t3) {
          if (t3 && Object.keys(t3).length > 0)
            return e3.init(t3).databaseForJQL();
          if (this._databaseForJQL)
            return this._databaseForJQL;
          const n3 = as(cs, { uniClient: e3, isJQL: true });
          return this._databaseForJQL = n3, n3;
        };
      }(t2), function(e3) {
        e3.getCurrentUserInfo = qs, e3.chooseAndUploadFile = Ks.initChooseAndUploadFile(e3), Object.assign(e3, { get mixinDatacom() {
          return $s(e3);
        } }), e3.SSEChannel = Ys, e3.initSecureNetworkByWeixin = zs(e3), e3.setCustomClientInfo = Vs, e3.importObject = Bs(e3);
      }(t2);
      return ["callFunction", "uploadFile", "deleteFile", "getTempFileURL", "downloadFile", "chooseAndUploadFile"].forEach((e3) => {
        if (!t2[e3])
          return;
        const n3 = t2[e3];
        t2[e3] = function() {
          return n3.apply(t2, Array.from(arguments));
        }, t2[e3] = (/* @__PURE__ */ function(e4, t3) {
          return function(n4) {
            let s2 = false;
            if ("callFunction" === t3) {
              const e5 = n4 && n4.type || l.DEFAULT;
              s2 = e5 !== l.DEFAULT;
            }
            const r2 = "callFunction" === t3 && !s2, i2 = this._initPromiseHub.exec();
            n4 = n4 || {};
            const { success: o2, fail: a2, complete: c2 } = ee(n4), u2 = i2.then(() => s2 ? Promise.resolve() : j($(t3, "invoke"), n4)).then(() => e4.call(this, n4)).then((e5) => s2 ? Promise.resolve(e5) : j($(t3, "success"), e5).then(() => j($(t3, "complete"), e5)).then(() => (r2 && Y(H.RESPONSE, { type: J.CLOUD_FUNCTION, content: e5 }), Promise.resolve(e5))), (e5) => s2 ? Promise.reject(e5) : j($(t3, "fail"), e5).then(() => j($(t3, "complete"), e5)).then(() => (Y(H.RESPONSE, { type: J.CLOUD_FUNCTION, content: e5 }), Promise.reject(e5))));
            if (!(o2 || a2 || c2))
              return u2;
            u2.then((e5) => {
              o2 && o2(e5), c2 && c2(e5), r2 && Y(H.RESPONSE, { type: J.CLOUD_FUNCTION, content: e5 });
            }, (e5) => {
              a2 && a2(e5), c2 && c2(e5), r2 && Y(H.RESPONSE, { type: J.CLOUD_FUNCTION, content: e5 });
            });
          };
        }(t2[e3], e3)).bind(t2);
      }), t2.init = this.init, t2;
    }
  }();
  (() => {
    const e2 = O;
    let t2 = {};
    if (e2 && 1 === e2.length)
      t2 = e2[0], er = er.init(t2), er._isDefault = true;
    else {
      const t3 = ["auth", "callFunction", "uploadFile", "deleteFile", "getTempFileURL", "downloadFile"], n2 = ["database", "getCurrentUserInfo", "importObject"];
      let s2;
      s2 = e2 && e2.length > 0 ? "应用有多个服务空间，请通过uniCloud.init方法指定要使用的服务空间" : "应用未关联服务空间，请在uniCloud目录右键关联服务空间", [...t3, ...n2].forEach((e3) => {
        er[e3] = function() {
          if (console.error(s2), -1 === n2.indexOf(e3))
            return Promise.reject(new te({ code: "SYS_ERR", message: s2 }));
          console.error(s2);
        };
      });
    }
    if (Object.assign(er, { get mixinDatacom() {
      return $s(er);
    } }), Ls(er), er.addInterceptor = F, er.removeInterceptor = K, er.interceptObject = B, uni.__uniCloud = er, "app" === P) {
      const e3 = D();
      e3.uniCloud = er, e3.UniCloudError = te;
    }
  })();
  var tr = er;
  const dataPicker = {
    props: {
      localdata: {
        type: [Array, Object],
        default() {
          return [];
        }
      },
      spaceInfo: {
        type: Object,
        default() {
          return {};
        }
      },
      collection: {
        type: String,
        default: ""
      },
      action: {
        type: String,
        default: ""
      },
      field: {
        type: String,
        default: ""
      },
      orderby: {
        type: String,
        default: ""
      },
      where: {
        type: [String, Object],
        default: ""
      },
      pageData: {
        type: String,
        default: "add"
      },
      pageCurrent: {
        type: Number,
        default: 1
      },
      pageSize: {
        type: Number,
        default: 500
      },
      getcount: {
        type: [Boolean, String],
        default: false
      },
      getone: {
        type: [Boolean, String],
        default: false
      },
      gettree: {
        type: [Boolean, String],
        default: false
      },
      manual: {
        type: Boolean,
        default: false
      },
      value: {
        type: [Array, String, Number],
        default() {
          return [];
        }
      },
      modelValue: {
        type: [Array, String, Number],
        default() {
          return [];
        }
      },
      preload: {
        type: Boolean,
        default: false
      },
      stepSearh: {
        type: Boolean,
        default: true
      },
      selfField: {
        type: String,
        default: ""
      },
      parentField: {
        type: String,
        default: ""
      },
      multiple: {
        type: Boolean,
        default: false
      },
      map: {
        type: Object,
        default() {
          return {
            text: "text",
            value: "value"
          };
        }
      }
    },
    data() {
      return {
        loading: false,
        errorMessage: "",
        loadMore: {
          contentdown: "",
          contentrefresh: "",
          contentnomore: ""
        },
        dataList: [],
        selected: [],
        selectedIndex: 0,
        page: {
          current: this.pageCurrent,
          size: this.pageSize,
          count: 0
        }
      };
    },
    computed: {
      isLocalData() {
        return !this.collection.length;
      },
      isCloudData() {
        return this.collection.length > 0;
      },
      isCloudDataList() {
        return this.isCloudData && (!this.parentField && !this.selfField);
      },
      isCloudDataTree() {
        return this.isCloudData && this.parentField && this.selfField;
      },
      dataValue() {
        let isModelValue = Array.isArray(this.modelValue) ? this.modelValue.length > 0 : this.modelValue !== null || this.modelValue !== void 0;
        return isModelValue ? this.modelValue : this.value;
      },
      hasValue() {
        if (typeof this.dataValue === "number") {
          return true;
        }
        return this.dataValue != null && this.dataValue.length > 0;
      }
    },
    created() {
      this.$watch(() => {
        var al = [];
        [
          "pageCurrent",
          "pageSize",
          "spaceInfo",
          "value",
          "modelValue",
          "localdata",
          "collection",
          "action",
          "field",
          "orderby",
          "where",
          "getont",
          "getcount",
          "gettree"
        ].forEach((key) => {
          al.push(this[key]);
        });
        return al;
      }, (newValue, oldValue) => {
        for (let i2 = 2; i2 < newValue.length; i2++) {
          if (newValue[i2] != oldValue[i2]) {
            break;
          }
        }
        if (newValue[0] != oldValue[0]) {
          this.page.current = this.pageCurrent;
        }
        this.page.size = this.pageSize;
        this.onPropsChange();
      });
      this._treeData = [];
    },
    methods: {
      onPropsChange() {
        this._treeData = [];
      },
      // 填充 pickview 数据
      async loadData() {
        if (this.isLocalData) {
          this.loadLocalData();
        } else if (this.isCloudDataList) {
          this.loadCloudDataList();
        } else if (this.isCloudDataTree) {
          this.loadCloudDataTree();
        }
      },
      // 加载本地数据
      async loadLocalData() {
        this._treeData = [];
        this._extractTree(this.localdata, this._treeData);
        let inputValue = this.dataValue;
        if (inputValue === void 0) {
          return;
        }
        if (Array.isArray(inputValue)) {
          inputValue = inputValue[inputValue.length - 1];
          if (typeof inputValue === "object" && inputValue[this.map.value]) {
            inputValue = inputValue[this.map.value];
          }
        }
        this.selected = this._findNodePath(inputValue, this.localdata);
      },
      // 加载 Cloud 数据 (单列)
      async loadCloudDataList() {
        if (this.loading) {
          return;
        }
        this.loading = true;
        try {
          let response = await this.getCommand();
          let responseData = response.result.data;
          this._treeData = responseData;
          this._updateBindData();
          this._updateSelected();
          this.onDataChange();
        } catch (e2) {
          this.errorMessage = e2;
        } finally {
          this.loading = false;
        }
      },
      // 加载 Cloud 数据 (树形)
      async loadCloudDataTree() {
        if (this.loading) {
          return;
        }
        this.loading = true;
        try {
          let commandOptions = {
            field: this._cloudDataPostField(),
            where: this._cloudDataTreeWhere()
          };
          if (this.gettree) {
            commandOptions.startwith = `${this.selfField}=='${this.dataValue}'`;
          }
          let response = await this.getCommand(commandOptions);
          let responseData = response.result.data;
          this._treeData = responseData;
          this._updateBindData();
          this._updateSelected();
          this.onDataChange();
        } catch (e2) {
          this.errorMessage = e2;
        } finally {
          this.loading = false;
        }
      },
      // 加载 Cloud 数据 (节点)
      async loadCloudDataNode(callback) {
        if (this.loading) {
          return;
        }
        this.loading = true;
        try {
          let commandOptions = {
            field: this._cloudDataPostField(),
            where: this._cloudDataNodeWhere()
          };
          let response = await this.getCommand(commandOptions);
          let responseData = response.result.data;
          callback(responseData);
        } catch (e2) {
          this.errorMessage = e2;
        } finally {
          this.loading = false;
        }
      },
      // 回显 Cloud 数据
      getCloudDataValue() {
        if (this.isCloudDataList) {
          return this.getCloudDataListValue();
        }
        if (this.isCloudDataTree) {
          return this.getCloudDataTreeValue();
        }
      },
      // 回显 Cloud 数据 (单列)
      getCloudDataListValue() {
        let where = [];
        let whereField = this._getForeignKeyByField();
        if (whereField) {
          where.push(`${whereField} == '${this.dataValue}'`);
        }
        where = where.join(" || ");
        if (this.where) {
          where = `(${this.where}) && (${where})`;
        }
        return this.getCommand({
          field: this._cloudDataPostField(),
          where
        }).then((res) => {
          this.selected = res.result.data;
          return res.result.data;
        });
      },
      // 回显 Cloud 数据 (树形)
      getCloudDataTreeValue() {
        return this.getCommand({
          field: this._cloudDataPostField(),
          getTreePath: {
            startWith: `${this.selfField}=='${this.dataValue}'`
          }
        }).then((res) => {
          let treePath = [];
          this._extractTreePath(res.result.data, treePath);
          this.selected = treePath;
          return treePath;
        });
      },
      getCommand(options = {}) {
        let db = tr.database(this.spaceInfo);
        const action = options.action || this.action;
        if (action) {
          db = db.action(action);
        }
        const collection = options.collection || this.collection;
        db = db.collection(collection);
        const where = options.where || this.where;
        if (!(!where || !Object.keys(where).length)) {
          db = db.where(where);
        }
        const field = options.field || this.field;
        if (field) {
          db = db.field(field);
        }
        const orderby = options.orderby || this.orderby;
        if (orderby) {
          db = db.orderBy(orderby);
        }
        const current = options.pageCurrent !== void 0 ? options.pageCurrent : this.page.current;
        const size = options.pageSize !== void 0 ? options.pageSize : this.page.size;
        const getCount = options.getcount !== void 0 ? options.getcount : this.getcount;
        const getTree = options.gettree !== void 0 ? options.gettree : this.gettree;
        const getOptions = {
          getCount,
          getTree
        };
        if (options.getTreePath) {
          getOptions.getTreePath = options.getTreePath;
        }
        db = db.skip(size * (current - 1)).limit(size).get(getOptions);
        return db;
      },
      _cloudDataPostField() {
        let fields = [this.field];
        if (this.parentField) {
          fields.push(`${this.parentField} as parent_value`);
        }
        return fields.join(",");
      },
      _cloudDataTreeWhere() {
        let result = [];
        let selected = this.selected;
        let parentField = this.parentField;
        if (parentField) {
          result.push(`${parentField} == null || ${parentField} == ""`);
        }
        if (selected.length) {
          for (var i2 = 0; i2 < selected.length - 1; i2++) {
            result.push(`${parentField} == '${selected[i2].value}'`);
          }
        }
        let where = [];
        if (this.where) {
          where.push(`(${this.where})`);
        }
        if (result.length) {
          where.push(`(${result.join(" || ")})`);
        }
        return where.join(" && ");
      },
      _cloudDataNodeWhere() {
        let where = [];
        let selected = this.selected;
        if (selected.length) {
          where.push(`${this.parentField} == '${selected[selected.length - 1].value}'`);
        }
        where = where.join(" || ");
        if (this.where) {
          return `(${this.where}) && (${where})`;
        }
        return where;
      },
      _getWhereByForeignKey() {
        let result = [];
        let whereField = this._getForeignKeyByField();
        if (whereField) {
          result.push(`${whereField} == '${this.dataValue}'`);
        }
        if (this.where) {
          return `(${this.where}) && (${result.join(" || ")})`;
        }
        return result.join(" || ");
      },
      _getForeignKeyByField() {
        let fields = this.field.split(",");
        let whereField = null;
        for (let i2 = 0; i2 < fields.length; i2++) {
          const items = fields[i2].split("as");
          if (items.length < 2) {
            continue;
          }
          if (items[1].trim() === "value") {
            whereField = items[0].trim();
            break;
          }
        }
        return whereField;
      },
      _updateBindData(node2) {
        const {
          dataList,
          hasNodes
        } = this._filterData(this._treeData, this.selected);
        let isleaf = this._stepSearh === false && !hasNodes;
        if (node2) {
          node2.isleaf = isleaf;
        }
        this.dataList = dataList;
        this.selectedIndex = dataList.length - 1;
        if (!isleaf && this.selected.length < dataList.length) {
          this.selected.push({
            value: null,
            text: "请选择"
          });
        }
        return {
          isleaf,
          hasNodes
        };
      },
      _updateSelected() {
        let dl = this.dataList;
        let sl = this.selected;
        let textField = this.map.text;
        let valueField = this.map.value;
        for (let i2 = 0; i2 < sl.length; i2++) {
          let value = sl[i2].value;
          let dl2 = dl[i2];
          for (let j2 = 0; j2 < dl2.length; j2++) {
            let item2 = dl2[j2];
            if (item2[valueField] === value) {
              sl[i2].text = item2[textField];
              break;
            }
          }
        }
      },
      _filterData(data, paths) {
        let dataList = [];
        let hasNodes = true;
        dataList.push(data.filter((item) => {
          return item.parent_value === null || item.parent_value === void 0 || item.parent_value === "";
        }));
        for (let i2 = 0; i2 < paths.length; i2++) {
          let value = paths[i2].value;
          let nodes = data.filter((item) => {
            return item.parent_value === value;
          });
          if (nodes.length) {
            dataList.push(nodes);
          } else {
            hasNodes = false;
          }
        }
        return {
          dataList,
          hasNodes
        };
      },
      _extractTree(nodes, result, parent_value) {
        let valueField = this.map.value;
        for (let i2 = 0; i2 < nodes.length; i2++) {
          let node2 = nodes[i2];
          let child = {};
          for (let key in node2) {
            if (key !== "children") {
              child[key] = node2[key];
            }
          }
          if (parent_value !== null && parent_value !== void 0 && parent_value !== "") {
            child.parent_value = parent_value;
          }
          result.push(child);
          let children = node2.children;
          if (children) {
            this._extractTree(children, result, node2[valueField]);
          }
        }
      },
      _extractTreePath(nodes, result) {
        for (let i2 = 0; i2 < nodes.length; i2++) {
          let node2 = nodes[i2];
          let child = {};
          for (let key in node2) {
            if (key !== "children") {
              child[key] = node2[key];
            }
          }
          result.push(child);
          let children = node2.children;
          if (children) {
            this._extractTreePath(children, result);
          }
        }
      },
      _findNodePath(key, nodes, path = []) {
        let textField = this.map.text;
        let valueField = this.map.value;
        for (let i2 = 0; i2 < nodes.length; i2++) {
          let node2 = nodes[i2];
          let children = node2.children;
          let text = node2[textField];
          let value = node2[valueField];
          path.push({
            value,
            text
          });
          if (value === key) {
            return path;
          }
          if (children) {
            const p2 = this._findNodePath(key, children, path);
            if (p2.length) {
              return p2;
            }
          }
          path.pop();
        }
        return [];
      }
    }
  };
  const _sfc_main$x = {
    name: "UniDataPickerView",
    emits: ["nodeclick", "change", "datachange", "update:modelValue"],
    mixins: [dataPicker],
    props: {
      managedMode: {
        type: Boolean,
        default: false
      },
      ellipsis: {
        type: Boolean,
        default: true
      }
    },
    created() {
      if (!this.managedMode) {
        this.$nextTick(() => {
          this.loadData();
        });
      }
    },
    methods: {
      onPropsChange() {
        this._treeData = [];
        this.selectedIndex = 0;
        this.$nextTick(() => {
          this.loadData();
        });
      },
      handleSelect(index) {
        this.selectedIndex = index;
      },
      handleNodeClick(item, i2, j2) {
        if (item.disable) {
          return;
        }
        const node2 = this.dataList[i2][j2];
        const text = node2[this.map.text];
        const value = node2[this.map.value];
        if (i2 < this.selected.length - 1) {
          this.selected.splice(i2, this.selected.length - i2);
          this.selected.push({
            text,
            value
          });
        } else if (i2 === this.selected.length - 1) {
          this.selected.splice(i2, 1, {
            text,
            value
          });
        }
        if (node2.isleaf) {
          this.onSelectedChange(node2, node2.isleaf);
          return;
        }
        const {
          isleaf,
          hasNodes
        } = this._updateBindData();
        if (this.isLocalData) {
          this.onSelectedChange(node2, !hasNodes || isleaf);
        } else if (this.isCloudDataList) {
          this.onSelectedChange(node2, true);
        } else if (this.isCloudDataTree) {
          if (isleaf) {
            this.onSelectedChange(node2, node2.isleaf);
          } else if (!hasNodes) {
            this.loadCloudDataNode((data) => {
              if (!data.length) {
                node2.isleaf = true;
              } else {
                this._treeData.push(...data);
                this._updateBindData(node2);
              }
              this.onSelectedChange(node2, node2.isleaf);
            });
          }
        }
      },
      updateData(data) {
        this._treeData = data.treeData;
        this.selected = data.selected;
        if (!this._treeData.length) {
          this.loadData();
        } else {
          this._updateBindData();
        }
      },
      onDataChange() {
        this.$emit("datachange");
      },
      onSelectedChange(node2, isleaf) {
        if (isleaf) {
          this._dispatchEvent();
        }
        if (node2) {
          this.$emit("nodeclick", node2);
        }
      },
      _dispatchEvent() {
        this.$emit("change", this.selected.slice(0));
      }
    }
  };
  function _sfc_render$w(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "uni-data-pickerview" }, [
      !_ctx.isCloudDataList ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
        key: 0,
        class: "selected-area",
        "scroll-x": "true"
      }, [
        vue.createElementVNode("view", { class: "selected-list" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList(_ctx.selected, (item, index) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: vue.normalizeClass(["selected-item", {
                  "selected-item-active": index == _ctx.selectedIndex
                }]),
                key: index,
                onClick: ($event) => $options.handleSelect(index)
              }, [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString(item.text || ""),
                  1
                  /* TEXT */
                )
              ], 10, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "tab-c" }, [
        vue.createElementVNode("scroll-view", {
          class: "list",
          "scroll-y": true
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList(_ctx.dataList[_ctx.selectedIndex], (item, j2) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: vue.normalizeClass(["item", { "is-disabled": !!item.disable }]),
                key: j2,
                onClick: ($event) => $options.handleNodeClick(item, _ctx.selectedIndex, j2)
              }, [
                vue.createElementVNode(
                  "text",
                  { class: "item-text" },
                  vue.toDisplayString(item[_ctx.map.text]),
                  1
                  /* TEXT */
                ),
                _ctx.selected.length > _ctx.selectedIndex && item[_ctx.map.value] == _ctx.selected[_ctx.selectedIndex].value ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "check"
                })) : vue.createCommentVNode("v-if", true)
              ], 10, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ]),
        _ctx.loading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "loading-cover"
        }, [
          vue.createVNode(_component_uni_load_more, {
            class: "load-more",
            contentText: _ctx.loadMore,
            status: "loading"
          }, null, 8, ["contentText"])
        ])) : vue.createCommentVNode("v-if", true),
        _ctx.errorMessage ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "error-message"
        }, [
          vue.createElementVNode(
            "text",
            { class: "error-text" },
            vue.toDisplayString(_ctx.errorMessage),
            1
            /* TEXT */
          )
        ])) : vue.createCommentVNode("v-if", true)
      ])
    ]);
  }
  const DataPickerView = /* @__PURE__ */ _export_sfc(_sfc_main$x, [["render", _sfc_render$w], ["__scopeId", "data-v-91ec6a82"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-data-picker/components/uni-data-pickerview/uni-data-pickerview.vue"]]);
  const _sfc_main$w = {
    name: "UniDataPicker",
    emits: ["popupopened", "popupclosed", "nodeclick", "input", "change", "update:modelValue", "inputclick"],
    mixins: [dataPicker],
    components: {
      DataPickerView
    },
    props: {
      options: {
        type: [Object, Array],
        default() {
          return {};
        }
      },
      popupTitle: {
        type: String,
        default: "请选择"
      },
      placeholder: {
        type: String,
        default: "请选择"
      },
      heightMobile: {
        type: String,
        default: ""
      },
      readonly: {
        type: Boolean,
        default: false
      },
      clearIcon: {
        type: Boolean,
        default: true
      },
      border: {
        type: Boolean,
        default: true
      },
      split: {
        type: String,
        default: "/"
      },
      ellipsis: {
        type: Boolean,
        default: true
      }
    },
    data() {
      return {
        isOpened: false,
        inputSelected: []
      };
    },
    created() {
      this.$nextTick(() => {
        this.load();
      });
    },
    watch: {
      localdata: {
        handler() {
          this.load();
        },
        deep: true
      }
    },
    methods: {
      clear() {
        this._dispatchEvent([]);
      },
      onPropsChange() {
        this._treeData = [];
        this.selectedIndex = 0;
        this.load();
      },
      load() {
        if (this.readonly) {
          this._processReadonly(this.localdata, this.dataValue);
          return;
        }
        if (this.isLocalData) {
          this.loadData();
          this.inputSelected = this.selected.slice(0);
        } else if (this.isCloudDataList || this.isCloudDataTree) {
          this.loading = true;
          this.getCloudDataValue().then((res) => {
            this.loading = false;
            this.inputSelected = res;
          }).catch((err) => {
            this.loading = false;
            this.errorMessage = err;
          });
        }
      },
      show() {
        this.isOpened = true;
        setTimeout(() => {
          this.$refs.pickerView.updateData({
            treeData: this._treeData,
            selected: this.selected,
            selectedIndex: this.selectedIndex
          });
        }, 200);
        this.$emit("popupopened");
      },
      hide() {
        this.isOpened = false;
        this.$emit("popupclosed");
      },
      handleInput() {
        if (this.readonly) {
          this.$emit("inputclick");
          return;
        }
        this.show();
      },
      handleClose(e2) {
        this.hide();
      },
      onnodeclick(e2) {
        this.$emit("nodeclick", e2);
      },
      ondatachange(e2) {
        this._treeData = this.$refs.pickerView._treeData;
      },
      onchange(e2) {
        this.hide();
        this.$nextTick(() => {
          this.inputSelected = e2;
        });
        this._dispatchEvent(e2);
      },
      _processReadonly(dataList, value) {
        var isTree = dataList.findIndex((item2) => {
          return item2.children;
        });
        if (isTree > -1) {
          let inputValue;
          if (Array.isArray(value)) {
            inputValue = value[value.length - 1];
            if (typeof inputValue === "object" && inputValue.value) {
              inputValue = inputValue.value;
            }
          } else {
            inputValue = value;
          }
          this.inputSelected = this._findNodePath(inputValue, this.localdata);
          return;
        }
        if (!this.hasValue) {
          this.inputSelected = [];
          return;
        }
        let result = [];
        if (Array.isArray(value)) {
          for (let i2 = 0; i2 < value.length; i2++) {
            var val = value[i2];
            var item = dataList.find((v2) => {
              return v2.value == val;
            });
            if (item) {
              result.push(item);
            }
          }
        } else {
          let item2 = dataList.find((v2) => {
            return v2.value == value;
          });
          if (item2) {
            result.push(item2);
          }
        }
        if (result.length) {
          this.inputSelected = result;
        }
      },
      _filterForArray(data, valueArray) {
        var result = [];
        for (let i2 = 0; i2 < valueArray.length; i2++) {
          var value = valueArray[i2];
          var found = data.find((item) => {
            return item.value == value;
          });
          if (found) {
            result.push(found);
          }
        }
        return result;
      },
      _dispatchEvent(selected) {
        let item = {};
        if (selected.length) {
          var value = new Array(selected.length);
          for (var i2 = 0; i2 < selected.length; i2++) {
            value[i2] = selected[i2].value;
          }
          item = selected[selected.length - 1];
        } else {
          item.value = "";
        }
        if (this.formItem) {
          this.formItem.setValue(item.value);
        }
        this.$emit("input", item.value);
        this.$emit("update:modelValue", item.value);
        this.$emit("change", {
          detail: {
            value: selected
          }
        });
      }
    }
  };
  function _sfc_render$v(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_data_picker_view = vue.resolveComponent("data-picker-view");
    return vue.openBlock(), vue.createElementBlock("view", { class: "uni-data-tree" }, [
      vue.createElementVNode("view", {
        class: "uni-data-tree-input",
        onClick: _cache[1] || (_cache[1] = (...args) => $options.handleInput && $options.handleInput(...args))
      }, [
        vue.renderSlot(_ctx.$slots, "default", {
          options: $props.options,
          data: $data.inputSelected,
          error: _ctx.errorMessage
        }, () => [
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["input-value", { "input-value-border": $props.border }])
            },
            [
              _ctx.errorMessage ? (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 0,
                  class: "selected-area error-text"
                },
                vue.toDisplayString(_ctx.errorMessage),
                1
                /* TEXT */
              )) : _ctx.loading && !$data.isOpened ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "selected-area"
              }, [
                vue.createVNode(_component_uni_load_more, {
                  class: "load-more",
                  contentText: _ctx.loadMore,
                  status: "loading"
                }, null, 8, ["contentText"])
              ])) : $data.inputSelected.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
                key: 2,
                class: "selected-area",
                "scroll-x": "true"
              }, [
                vue.createElementVNode("view", { class: "selected-list" }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($data.inputSelected, (item, index) => {
                      return vue.openBlock(), vue.createElementBlock("view", {
                        class: "selected-item",
                        key: index
                      }, [
                        vue.createElementVNode(
                          "text",
                          { class: "text-color" },
                          vue.toDisplayString(item.text),
                          1
                          /* TEXT */
                        ),
                        index < $data.inputSelected.length - 1 ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 0,
                            class: "input-split-line"
                          },
                          vue.toDisplayString($props.split),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])
              ])) : (vue.openBlock(), vue.createElementBlock(
                "text",
                {
                  key: 3,
                  class: "selected-area placeholder"
                },
                vue.toDisplayString($props.placeholder),
                1
                /* TEXT */
              )),
              $props.clearIcon && !$props.readonly && $data.inputSelected.length ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 4,
                class: "icon-clear",
                onClick: _cache[0] || (_cache[0] = vue.withModifiers((...args) => $options.clear && $options.clear(...args), ["stop"]))
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "clear",
                  color: "#c0c4cc",
                  size: "24"
                })
              ])) : vue.createCommentVNode("v-if", true),
              (!$props.clearIcon || !$data.inputSelected.length) && !$props.readonly ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 5,
                class: "arrow-area"
              }, [
                vue.createElementVNode("view", { class: "input-arrow" })
              ])) : vue.createCommentVNode("v-if", true)
            ],
            2
            /* CLASS */
          )
        ], true)
      ]),
      $data.isOpened ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "uni-data-tree-cover",
        onClick: _cache[2] || (_cache[2] = (...args) => $options.handleClose && $options.handleClose(...args))
      })) : vue.createCommentVNode("v-if", true),
      $data.isOpened ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "uni-data-tree-dialog"
      }, [
        vue.createElementVNode("view", { class: "uni-popper__arrow" }),
        vue.createElementVNode("view", { class: "dialog-caption" }, [
          vue.createElementVNode("view", { class: "title-area" }, [
            vue.createElementVNode(
              "text",
              { class: "dialog-title" },
              vue.toDisplayString($props.popupTitle),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", {
            class: "dialog-close",
            onClick: _cache[3] || (_cache[3] = (...args) => $options.handleClose && $options.handleClose(...args))
          }, [
            vue.createElementVNode("view", {
              class: "dialog-close-plus",
              "data-id": "close"
            }),
            vue.createElementVNode("view", {
              class: "dialog-close-plus dialog-close-rotate",
              "data-id": "close"
            })
          ])
        ]),
        vue.createVNode(_component_data_picker_view, {
          class: "picker-view",
          ref: "pickerView",
          modelValue: _ctx.dataValue,
          "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => _ctx.dataValue = $event),
          localdata: _ctx.localdata,
          preload: _ctx.preload,
          collection: _ctx.collection,
          field: _ctx.field,
          orderby: _ctx.orderby,
          where: _ctx.where,
          "step-searh": _ctx.stepSearh,
          "self-field": _ctx.selfField,
          "parent-field": _ctx.parentField,
          "managed-mode": true,
          map: _ctx.map,
          ellipsis: $props.ellipsis,
          onChange: $options.onchange,
          onDatachange: $options.ondatachange,
          onNodeclick: $options.onnodeclick
        }, null, 8, ["modelValue", "localdata", "preload", "collection", "field", "orderby", "where", "step-searh", "self-field", "parent-field", "map", "ellipsis", "onChange", "onDatachange", "onNodeclick"])
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const __easycom_4 = /* @__PURE__ */ _export_sfc(_sfc_main$w, [["render", _sfc_render$v], ["__scopeId", "data-v-2653531e"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-data-picker/components/uni-data-picker/uni-data-picker.vue"]]);
  const PICKER_HEIGHT = 420;
  const INDICATOR_PX$1 = 64;
  const _sfc_main$v = {
    __name: "common-date-picker",
    props: {
      show: { type: Boolean, default: false },
      // v-model:show
      modelValue: { type: [String, Number, Date], default: "" },
      // v-model => 'YYYY-MM-DD'
      title: { type: String, default: "选择日期" },
      minDate: { type: [String, Number, Date], default: "2000-01-01" },
      maxDate: { type: [String, Number, Date], default: "2035-12-31" },
      themeColor: { type: String, default: "#FFD84D" }
      // 按钮主色（淡黄）
    },
    emits: ["update:modelValue", "update:show", "confirm", "cancel"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const popup = vue.ref(null);
      const mountedInPopup = vue.ref(false);
      const safeBottom = vue.ref(0);
      const indicatorStyle2 = `height:${INDICATOR_PX$1}px;`;
      const years = vue.ref([]);
      const months = vue.ref([]);
      const days = vue.ref([]);
      const selY = vue.ref(2024);
      const selM = vue.ref(9);
      const selD = vue.ref(1);
      const pickerIndex = vue.computed(() => {
        return [
          Math.max(0, years.value.indexOf(selY.value)),
          Math.max(0, months.value.indexOf(selM.value)),
          Math.max(0, days.value.indexOf(selD.value))
        ];
      });
      function toDate(v2) {
        if (v2 instanceof Date)
          return isNaN(v2.getTime()) ? /* @__PURE__ */ new Date() : v2;
        if (typeof v2 === "number") {
          const d2 = new Date(v2);
          return isNaN(d2.getTime()) ? /* @__PURE__ */ new Date() : d2;
        }
        if (!v2)
          return /* @__PURE__ */ new Date();
        const m2 = String(v2).match(/(\d{4})\D?(\d{1,2})\D?(\d{1,2})/);
        if (m2) {
          const d2 = new Date(+m2[1], +m2[2] - 1, +m2[3]);
          return isNaN(d2.getTime()) ? /* @__PURE__ */ new Date() : d2;
        }
        return /* @__PURE__ */ new Date();
      }
      const fmt = (n2) => n2 < 10 ? "0" + n2 : "" + n2;
      function clamp(date, min, max) {
        const t2 = toDate(date).getTime(), mi = toDate(min).getTime(), ma = toDate(max).getTime();
        return new Date(Math.min(Math.max(t2, mi), ma));
      }
      function lastDay(y2, m2) {
        return new Date(y2, m2, 0).getDate();
      }
      const minYMD = vue.computed(() => {
        const d2 = toDate(props.minDate);
        return { y: d2.getFullYear(), m: d2.getMonth() + 1, d: d2.getDate() };
      });
      const maxYMD = vue.computed(() => {
        const d2 = toDate(props.maxDate);
        return { y: d2.getFullYear(), m: d2.getMonth() + 1, d: d2.getDate() };
      });
      function buildYears() {
        const arr = [];
        for (let y2 = minYMD.value.y; y2 <= maxYMD.value.y; y2++)
          arr.push(y2);
        years.value = arr;
      }
      function buildMonths() {
        const arr = [];
        const minM = selY.value === minYMD.value.y ? minYMD.value.m : 1;
        const maxM = selY.value === maxYMD.value.y ? maxYMD.value.m : 12;
        for (let m2 = 1; m2 <= 12; m2++)
          if (m2 >= minM && m2 <= maxM)
            arr.push(m2);
        months.value = arr;
        if (!arr.includes(selM.value))
          selM.value = arr[0];
      }
      function buildDays() {
        const dim = lastDay(selY.value, selM.value);
        const minD = selY.value === minYMD.value.y && selM.value === minYMD.value.m ? minYMD.value.d : 1;
        const maxD = selY.value === maxYMD.value.y && selM.value === maxYMD.value.m ? maxYMD.value.d : dim;
        const arr = [];
        for (let d2 = minD; d2 <= maxD; d2++)
          arr.push(d2);
        days.value = arr;
        if (!arr.includes(selD.value))
          selD.value = arr[0];
      }
      function onPickerChange(e2) {
        const [yi, mi, di] = e2.detail.value;
        selY.value = years.value[yi] || selY.value;
        selM.value = months.value[mi] || selM.value;
        buildMonths();
        buildDays();
        selD.value = days.value[di] || selD.value;
      }
      function onCancel() {
        emit("cancel");
        emit("update:show", false);
      }
      function onConfirm() {
        const dt2 = clamp(new Date(selY.value, selM.value - 1, selD.value), props.minDate, props.maxDate);
        const out = `${dt2.getFullYear()}-${fmt(dt2.getMonth() + 1)}-${fmt(dt2.getDate())}`;
        emit("update:modelValue", out);
        emit("confirm", out);
        emit("update:show", false);
      }
      function initByModel() {
        const base = clamp(props.modelValue || /* @__PURE__ */ new Date(), props.minDate, props.maxDate);
        selY.value = base.getFullYear();
        selM.value = base.getMonth() + 1;
        selD.value = base.getDate();
        buildYears();
        buildMonths();
        buildDays();
      }
      function onPopupChange(e2) {
        if (e2.show) {
          vue.nextTick(() => {
            mountedInPopup.value = true;
          });
        } else {
          mountedInPopup.value = false;
          emit("update:show", false);
        }
      }
      vue.watch(() => props.show, async (val) => {
        var _a, _b, _c, _d, _e2;
        if (val) {
          try {
            const wi = uni.getWindowInfo && uni.getWindowInfo();
            safeBottom.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.bottom) || 0;
          } catch (e2) {
            safeBottom.value = 0;
          }
          initByModel();
          await vue.nextTick();
          (_c = (_b = popup.value) == null ? void 0 : _b.open) == null ? void 0 : _c.call(_b);
        } else {
          (_e2 = (_d = popup.value) == null ? void 0 : _d.close) == null ? void 0 : _e2.call(_d);
        }
      }, { immediate: true });
      const __returned__ = { props, emit, popup, mountedInPopup, safeBottom, PICKER_HEIGHT, INDICATOR_PX: INDICATOR_PX$1, indicatorStyle: indicatorStyle2, years, months, days, selY, selM, selD, pickerIndex, toDate, fmt, clamp, lastDay, minYMD, maxYMD, buildYears, buildMonths, buildDays, onPickerChange, onCancel, onConfirm, initByModel, onPopupChange, ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$u(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createBlock(
      _component_uni_popup,
      {
        ref: "popup",
        type: "bottom",
        "mask-click": true,
        onChange: $setup.onPopupChange
      },
      {
        default: vue.withCtx(() => [
          vue.createElementVNode(
            "view",
            {
              class: "sheet",
              style: vue.normalizeStyle({ paddingBottom: $setup.safeBottom + "px" })
            },
            [
              vue.createElementVNode("view", { class: "sheet-header" }, [
                vue.createElementVNode("text", {
                  class: "btn-cancel",
                  onClick: $setup.onCancel
                }, "取消"),
                vue.createElementVNode(
                  "text",
                  { class: "sheet-title" },
                  vue.toDisplayString($props.title),
                  1
                  /* TEXT */
                )
              ]),
              $setup.mountedInPopup ? (vue.openBlock(), vue.createElementBlock("picker-view", {
                key: 0,
                class: "picker",
                "indicator-style": $setup.indicatorStyle,
                value: $setup.pickerIndex,
                onChange: $setup.onPickerChange
              }, [
                vue.createElementVNode("picker-view-column", null, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.years, (y2) => {
                      return vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: y2,
                          class: "picker-item"
                        },
                        vue.toDisplayString(y2) + "年",
                        1
                        /* TEXT */
                      );
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ]),
                vue.createElementVNode("picker-view-column", null, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.months, (m2) => {
                      return vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: m2,
                          class: "picker-item"
                        },
                        vue.toDisplayString(m2) + "月",
                        1
                        /* TEXT */
                      );
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ]),
                vue.createElementVNode("picker-view-column", null, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.days, (d2) => {
                      return vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: d2,
                          class: "picker-item"
                        },
                        vue.toDisplayString(d2) + "日",
                        1
                        /* TEXT */
                      );
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])
              ], 40, ["value"])) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("view", { class: "sheet-footer" }, [
                vue.createElementVNode("button", {
                  class: "btn-confirm",
                  onClick: $setup.onConfirm
                }, "选择")
              ])
            ],
            4
            /* STYLE */
          )
        ]),
        _: 1
        /* STABLE */
      },
      512
      /* NEED_PATCH */
    );
  }
  const __easycom_1 = /* @__PURE__ */ _export_sfc(_sfc_main$v, [["render", _sfc_render$u], ["__scopeId", "data-v-7b21b08e"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-date-picker/common-date-picker.vue"]]);
  const _imports_0$2 = "/static/info-circle.png";
  const _sfc_main$u = {
    __name: "account_book_form",
    props: ["account_book_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const isEdit = props.account_book_id ? true : false;
      const showBuyPicker = vue.ref(false);
      const showArrivalPicker = vue.ref(false);
      const showFinalPicker = vue.ref(false);
      const typeModalVisible = vue.ref(false);
      const customTypes = vue.ref([]);
      const defaultTypes = [];
      const typeOptions = vue.computed(() => [...defaultTypes, ...customTypes.value.map((t2) => t2.name)]);
      const selectedType = vue.ref(0);
      const updateSelectedType = (e2) => {
        selectedType.value = e2.detail.value;
      };
      const onTypeMgrVisibleChange = (visible) => {
        typeModalVisible.value = visible;
        if (!visible)
          getAccountTypes();
      };
      const onTypesUpdated = (list) => {
        customTypes.value = list || [];
        if (selectedType.value >= typeOptions.value.length)
          selectedType.value = 0;
      };
      const count = vue.ref(1);
      const imageList = vue.ref([]);
      let name = vue.ref("");
      let price = vue.ref("");
      const form = vue.ref({ isRemind: false, finalPrice: 0, finalTime: "" });
      const PAYMENT_OPTIONS = [
        { value: 1, label: "已全款" },
        { value: 2, label: "已付定金" },
        { value: 3, label: "未购买" }
      ];
      const paymentStatus = vue.ref(1);
      const showPayPopup = vue.ref(false);
      const paymentLabel = vue.computed(() => {
        var _a;
        return ((_a = PAYMENT_OPTIONS.find((o2) => o2.value === paymentStatus.value)) == null ? void 0 : _a.label) || "已全款";
      });
      const openPayPopup = () => {
        showPayPopup.value = !showPayPopup.value;
      };
      const choosePayment = (v2) => {
        paymentStatus.value = v2;
        showPayPopup.value = false;
      };
      const showMoreInfo = vue.ref(false);
      const sizeOptions = vue.ref([]);
      const selectedSizePath = vue.ref([]);
      const moreInfo = vue.ref({
        sizeDetail: "",
        color: "",
        remark: "",
        buyDate: "",
        position: "",
        shopName: "",
        headCircumference: "",
        shoulderWidth: "",
        makeupArtist: "",
        arrivalDate: "",
        additionalValue: ""
      });
      const fetchSizes = async () => {
        try {
          const res = await uni.request({ url: websiteUrl.value + "/sizes", method: "GET" });
          if (res.data.status === "success") {
            const sizesData = res.data.data;
            const formatted = [];
            for (const [category, items] of Object.entries(sizesData)) {
              formatted.push({ text: category, value: category, children: items.map((it2) => ({ text: it2, value: it2 })) });
            }
            sizeOptions.value = formatted;
          }
        } catch (e2) {
          uni.showToast({ title: "获取尺寸数据失败", icon: "none" });
        }
      };
      const onSizeChange = (e2) => {
        const nodes = e2.detail.value;
        if (nodes.length === 2) {
          selectedSizePath.value = [nodes[0].value, nodes[1].value];
          moreInfo.value.sizeDetail = nodes[1].value;
        } else {
          selectedSizePath.value = [];
          moreInfo.value.sizeDetail = "";
        }
      };
      const toggleMoreInfo = () => {
        showMoreInfo.value = !showMoreInfo.value;
      };
      const getAccountTypes = async () => {
        const token = uni.getStorageSync("token");
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/account-types",
            method: "GET",
            header: { "Authorization": token }
          });
          customTypes.value = res.data.data || [];
        } catch (err) {
          formatAppLog("error", "at pkg-stock/stock/account_book_form/account_book_form.vue:339", "获取分类失败:", err);
        }
      };
      const addNewType = async () => {
      };
      const deleteType = async () => {
      };
      const toggleRemind = () => {
        form.value.isRemind = !form.value.isRemind;
        if (!form.value.isRemind) {
          form.value.finalPrice = 0;
          form.value.finalTime = "";
        }
      };
      function onPriceInput(e2) {
        var _a;
        let s2 = String(((_a = e2 == null ? void 0 : e2.detail) == null ? void 0 : _a.value) ?? "");
        s2 = s2.replace(/[^0-9.]/g, "");
        const firstDot = s2.indexOf(".");
        if (firstDot !== -1) {
          s2 = s2.slice(0, firstDot + 1) + s2.slice(firstDot + 1).replace(/\./g, "");
          const parts = s2.split(".");
          s2 = parts[0] + (parts[1] !== void 0 ? "." + parts[1].slice(0, 1) : "");
        }
        if (s2.startsWith("."))
          s2 = "0" + s2;
        price.value = s2;
      }
      function onPriceBlur() {
        if (typeof price.value === "string" && price.value.endsWith(".")) {
          price.value = price.value.slice(0, -1);
        }
      }
      function formatPriceDisplay(val) {
        const n2 = Number(val);
        if (!isFinite(n2))
          return "";
        if (Math.floor(n2) === n2)
          return String(Math.trunc(n2));
        const trimmed = Math.sign(n2) * Math.floor(Math.abs(n2) * 10) / 10;
        return String(trimmed);
      }
      function normalizePriceForSubmit(s2) {
        if (!s2)
          return 0;
        const n2 = parseFloat(s2);
        if (!isFinite(n2))
          return 0;
        const trimmed = Math.sign(n2) * Math.floor(Math.abs(n2) * 10) / 10;
        return Number(String(trimmed));
      }
      function getAccountBookById(id) {
        let token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/account-book-detail",
          method: "GET",
          header: { "Authorization": token },
          data: { id },
          success: (res) => {
            const d2 = res.data.data;
            name.value = d2.name;
            price.value = formatPriceDisplay(d2.price);
            count.value = d2.count || 1;
            if (d2.image_url)
              imageList.value = d2.image_url.split(",");
            form.value = { isRemind: d2.is_remind, finalPrice: d2.final_price, finalTime: d2.final_time };
            const typeName = d2.type;
            const idx = typeOptions.value.findIndex((opt) => opt === typeName);
            selectedType.value = idx !== -1 ? idx : 0;
            moreInfo.value = {
              sizeDetail: d2.size_detail || "",
              color: d2.color || "",
              remark: d2.remark || "",
              buyDate: d2.buy_date ? new Date(d2.buy_date).toISOString().split("T")[0] : "",
              position: d2.position || "",
              shopName: d2.shop_name || "",
              headCircumference: d2.head_circumference || "",
              shoulderWidth: d2.shoulder_width || "",
              makeupArtist: d2.makeup_artist || "",
              arrivalDate: d2.arrival_date ? new Date(d2.arrival_date).toISOString().split("T")[0] : "",
              additionalValue: d2.additional_value || ""
            };
            if (d2.size) {
              selectedSizePath.value = [d2.size, d2.size_detail || ""];
              moreInfo.value.sizeDetail = d2.size_detail || "";
            }
            paymentStatus.value = d2.payment_status || 1;
          }
        });
      }
      const handleGoodsSelect = async (goods) => {
        var _a;
        try {
          const res = await uni.request({ url: websiteUrl.value + `/goods?id=${goods.id}`, method: "GET" });
          if (res.data.status === "success") {
            const detail = res.data.data;
            name.value = detail.name;
            const sub = Number(detail.sub_amount) || 0;
            const fin = Number(detail.fin_amount) || 0;
            price.value = formatPriceDisplay(sub + fin);
            if ((_a = detail.goods_images) == null ? void 0 : _a[0])
              imageList.value = [detail.goods_images[0]];
          }
        } catch (e2) {
          uni.showToast({ title: "获取商品信息失败", icon: "none" });
        }
      };
      function handleDelete() {
        uni.showModal({
          title: "提示",
          content: "确定删除该账本吗？",
          success: (res) => {
            if (!res.confirm)
              return;
            const id = Number(props.account_book_id);
            if (isNaN(id) || id <= 0) {
              uni.showToast({ title: "参数错误", icon: "none" });
              return;
            }
            uni.request({
              url: websiteUrl.value + "/with-state/delete-account-book",
              method: "POST",
              header: { "Authorization": uni.getStorageSync("token"), "Content-Type": "application/json" },
              data: { id },
              success: (r2) => {
                if (r2.data.status === "success") {
                  uni.showToast({ title: "删除成功", icon: "success" });
                  setTimeout(() => uni.navigateBack(), 500);
                } else {
                  uni.showToast({ title: r2.data.msg || "删除失败", icon: "none" });
                }
              },
              fail: () => uni.showToast({ title: "网络错误", icon: "none" })
            });
          }
        });
      }
      async function selectImage() {
        uni.chooseImage({
          count: 9,
          success: async (res) => {
            const files = res.tempFilePaths;
            for (const filePath of files) {
              try {
                const tokenData = await getQiniuToken();
                const uploadRes = await uploadImageToQiniu(filePath, tokenData.token, tokenData.path);
                if (uploadRes.qiniuRes.statusCode === 200)
                  imageList.value.push(image1Url + tokenData.path);
              } catch (e2) {
              }
            }
            uni.showToast({ title: `上传了${files.length}张图片`, icon: "success" });
          }
        });
      }
      function removeImage(index, event) {
        var _a;
        (_a = event == null ? void 0 : event.stopPropagation) == null ? void 0 : _a.call(event);
        uni.showModal({
          title: "删除图片",
          content: "确定删除这张图片吗？",
          success: (res) => {
            if (res.confirm)
              imageList.value.splice(index, 1);
          }
        });
      }
      function viewFullImage(index) {
        uni.previewImage({ current: index, urls: imageList.value });
      }
      function validateForm() {
        if (count.value <= 0) {
          uni.showToast({ title: "个数必须大于0", icon: "none" });
          return false;
        }
        if (form.value.isRemind && (!form.value.finalPrice || form.value.finalPrice <= 0)) {
          uni.showToast({ title: "请输入正确的尾款金额", icon: "none" });
          return false;
        }
        return true;
      }
      function addAccountBook() {
        const data = buildPostData();
        uni.request({
          url: websiteUrl.value + "/with-state/add-account-book",
          method: "POST",
          header: { "Authorization": uni.getStorageSync("token") },
          data,
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "提交成功", icon: "success" });
              setTimeout(() => uni.navigateBack(), 500);
            } else {
              uni.showToast({ title: res.data.msg || "提交失败", icon: "none" });
            }
          }
        });
      }
      function updateAccountBook() {
        const data = buildPostData();
        data.id = parseInt(props.account_book_id, 10);
        uni.request({
          url: websiteUrl.value + "/with-state/update-account-book",
          method: "POST",
          header: { "Authorization": uni.getStorageSync("token") },
          data,
          success: (res) => {
            if (res.data.status === "success") {
              uni.showToast({ title: "提交成功", icon: "success" });
              setTimeout(() => uni.navigateBack(), 500);
            } else {
              uni.showToast({ title: res.data.msg || "提交失败", icon: "none" });
            }
          }
        });
      }
      function buildPostData() {
        return {
          name: name.value,
          // 改为保留最多 1 位小数的 number
          price: normalizePriceForSubmit(price.value),
          count: parseInt(count.value, 10),
          type: typeOptions.value[selectedType.value],
          image_url: imageList.value.join(","),
          payment_status: paymentStatus.value,
          is_remind: form.value.isRemind,
          final_price: parseInt(form.value.finalPrice || 0, 10),
          final_time: form.value.finalTime,
          size: selectedSizePath.value[0] || "",
          size_detail: moreInfo.value.sizeDetail || "",
          color: moreInfo.value.color,
          remark: moreInfo.value.remark,
          buy_date: moreInfo.value.buyDate,
          position: moreInfo.value.position,
          shop_name: moreInfo.value.shopName,
          head_circumference: moreInfo.value.headCircumference,
          shoulder_width: moreInfo.value.shoulderWidth,
          makeup_artist: moreInfo.value.makeupArtist,
          arrival_date: moreInfo.value.arrivalDate,
          additional_value: moreInfo.value.additionalValue
        };
      }
      function postSubmit() {
        if (!validateForm())
          return;
        isEdit ? updateAccountBook() : addAccountBook();
      }
      onShow(async () => {
        await asyncGetUserInfo();
        await getAccountTypes();
        await fetchSizes();
        if (isEdit) {
          await getAccountBookById(props.account_book_id);
          uni.setNavigationBarTitle({ title: "编辑账本" });
        } else {
          uni.setNavigationBarTitle({ title: "新增账本" });
        }
      });
      onLoad(async (options) => {
        if (options.goods_id) {
          try {
            const goodsInfo = await getGoodsInfo(options.goods_id);
            fillFormWithGoodsInfo(goodsInfo);
          } catch (e2) {
            uni.showToast({ title: "获取商品信息失败", icon: "none" });
          }
        }
      });
      const getGoodsInfo = (id) => new Promise((resolve, reject) => {
        uni.request({
          url: websiteUrl.value + "/goods?id=" + id,
          method: "GET",
          timeout: 5e3,
          success: (res) => res.data.status === "success" ? resolve(res.data.data) : reject(new Error(res.data.msg || "获取商品信息失败")),
          fail: reject
        });
      });
      const fillFormWithGoodsInfo = (g2) => {
        var _a;
        name.value = g2.name;
        const totalPrice = g2.total_amount ? Number(g2.total_amount) : (Number(g2.sub_amount) || 0) + (Number(g2.fin_amount) || 0);
        price.value = formatPriceDisplay(totalPrice);
        if ((_a = g2.goods_images) == null ? void 0 : _a.length)
          imageList.value = [g2.goods_images[0]];
        if (g2.size) {
          selectedSizePath.value = [g2.size, g2.size_detail || ""];
          moreInfo.value.sizeDetail = g2.size_detail || "";
        }
      };
      const __returned__ = { props, isEdit, showBuyPicker, showArrivalPicker, showFinalPicker, typeModalVisible, customTypes, defaultTypes, typeOptions, selectedType, updateSelectedType, onTypeMgrVisibleChange, onTypesUpdated, count, imageList, get name() {
        return name;
      }, set name(v2) {
        name = v2;
      }, get price() {
        return price;
      }, set price(v2) {
        price = v2;
      }, form, PAYMENT_OPTIONS, paymentStatus, showPayPopup, paymentLabel, openPayPopup, choosePayment, showMoreInfo, sizeOptions, selectedSizePath, moreInfo, fetchSizes, onSizeChange, toggleMoreInfo, getAccountTypes, addNewType, deleteType, toggleRemind, onPriceInput, onPriceBlur, formatPriceDisplay, normalizePriceForSubmit, getAccountBookById, handleGoodsSelect, handleDelete, selectImage, removeImage, viewFullImage, validateForm, addAccountBook, updateAccountBook, buildPostData, postSubmit, getGoodsInfo, fillFormWithGoodsInfo, ref: vue.ref, computed: vue.computed, get onShow() {
        return onShow;
      }, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$t(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_item_category_manager = resolveEasycom(vue.resolveDynamicComponent("item-category-manager"), __easycom_1$d);
    const _component_goods_search = resolveEasycom(vue.resolveDynamicComponent("goods-search"), __easycom_3$2);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_data_picker = resolveEasycom(vue.resolveDynamicComponent("uni-data-picker"), __easycom_4);
    const _component_common_date_picker = resolveEasycom(vue.resolveDynamicComponent("common-date-picker"), __easycom_1);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", {
          class: "container",
          onClick: _cache[27] || (_cache[27] = ($event) => $setup.showPayPopup = false)
        }, [
          vue.createElementVNode("meta", {
            name: "theme-color",
            content: "#F8F8F8"
          }),
          vue.createVNode(_component_item_category_manager, {
            modelValue: $setup.typeModalVisible,
            "onUpdate:modelValue": [
              _cache[0] || (_cache[0] = ($event) => $setup.typeModalVisible = $event),
              $setup.onTypeMgrVisibleChange
            ],
            list: $setup.customTypes,
            onUpdated: $setup.onTypesUpdated
          }, null, 8, ["modelValue", "list"]),
          vue.createElementVNode("view", { class: "form-card" }, [
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "分类"),
              vue.createElementVNode("view", { class: "type-selector" }, [
                vue.createElementVNode("picker", {
                  mode: "selector",
                  class: "form-input",
                  value: $setup.selectedType,
                  range: $setup.typeOptions,
                  onChange: $setup.updateSelectedType
                }, [
                  vue.createElementVNode(
                    "view",
                    { class: "picker-content" },
                    vue.toDisplayString($setup.typeOptions[$setup.selectedType] || "请选择分类"),
                    1
                    /* TEXT */
                  )
                ], 40, ["value", "range"]),
                vue.createElementVNode("text", {
                  class: "manage-type",
                  onClick: _cache[1] || (_cache[1] = ($event) => $setup.typeModalVisible = true)
                }, "管理分类")
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "名称"),
              vue.createElementVNode("view", {
                class: "form-input",
                style: { "padding": "0" }
              }, [
                vue.createElementVNode("view", { class: "custom-search-container" }, [
                  vue.createVNode(_component_goods_search, {
                    "font-size": "24rpx",
                    mode: "fill",
                    onSelect: $setup.handleGoodsSelect,
                    modelValue: $setup.name,
                    "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.name = $event),
                    background: "#fff",
                    width: "620rpx",
                    "show-icon": false,
                    class: "custom-search"
                  }, null, 8, ["modelValue"])
                ])
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "价值(一位小数)"),
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "form-input",
                  type: "text",
                  inputmode: "decimal",
                  placeholder: "请输入价值",
                  "placeholder-class": "placeholder-style",
                  "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.price = $event),
                  onInput: $setup.onPriceInput,
                  onBlur: $setup.onPriceBlur
                },
                null,
                544
                /* NEED_HYDRATION, NEED_PATCH */
              ), [
                [vue.vModelText, $setup.price]
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "付款状态"),
              vue.createElementVNode("view", {
                class: "form-input",
                onClick: vue.withModifiers($setup.openPayPopup, ["stop"])
              }, [
                vue.createElementVNode(
                  "view",
                  { class: "picker-content" },
                  vue.toDisplayString($setup.paymentLabel),
                  1
                  /* TEXT */
                ),
                vue.createVNode(_component_uni_icons, {
                  type: "right",
                  size: "20",
                  class: "input-icon"
                })
              ]),
              $setup.showPayPopup ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "dropdown-popup",
                onClick: _cache[4] || (_cache[4] = vue.withModifiers(() => {
                }, ["stop"]))
              }, [
                (vue.openBlock(), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.PAYMENT_OPTIONS, (opt) => {
                    return vue.createElementVNode("view", {
                      key: opt.value,
                      class: vue.normalizeClass(["option-item", { active: $setup.paymentStatus === opt.value }]),
                      onClick: ($event) => $setup.choosePayment(opt.value)
                    }, [
                      vue.createElementVNode(
                        "text",
                        null,
                        vue.toDisplayString(opt.label),
                        1
                        /* TEXT */
                      ),
                      $setup.paymentStatus === opt.value ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                        key: 0,
                        type: "checkmarkempty",
                        size: "18"
                      })) : vue.createCommentVNode("v-if", true)
                    ], 10, ["onClick"]);
                  }),
                  64
                  /* STABLE_FRAGMENT */
                ))
              ])) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "个数"),
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "form-input",
                  type: "number",
                  placeholder: "请输入个数",
                  "placeholder-class": "placeholder-style",
                  "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.count = $event)
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.count]
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "物品图片"),
              vue.createElementVNode("view", { class: "upload-wrapper" }, [
                vue.createElementVNode("view", { class: "image-grid" }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.imageList, (img, index) => {
                      return vue.openBlock(), vue.createElementBlock("view", {
                        key: index,
                        class: "preview-image"
                      }, [
                        vue.createElementVNode("image", {
                          mode: "aspectFill",
                          src: img,
                          class: "image-preview",
                          onClick: ($event) => $setup.viewFullImage(index)
                        }, null, 8, ["src", "onClick"]),
                        vue.createElementVNode("view", { class: "image-actions" }, [
                          vue.createVNode(_component_uni_icons, {
                            type: "close",
                            size: "22",
                            color: "#fff",
                            onClick: (e2) => $setup.removeImage(index, e2),
                            class: "delete-icon"
                          }, null, 8, ["onClick"])
                        ])
                      ]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  )),
                  vue.createElementVNode("view", {
                    class: "add-image-box",
                    onClick: $setup.selectImage
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "plusempty",
                      size: "40",
                      color: "#ccc"
                    }),
                    vue.createElementVNode("text", { class: "add-text" }, "添加图片")
                  ])
                ])
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "尺寸"),
              vue.createVNode(_component_uni_data_picker, {
                placeholder: "请选择尺寸",
                localdata: $setup.sizeOptions,
                value: $setup.selectedSizePath,
                onChange: $setup.onSizeChange,
                class: "size-picker"
              }, null, 8, ["localdata", "value"])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("label", {
                class: "remind-label",
                onClick: _cache[6] || (_cache[6] = ($event) => $setup.toggleMoreInfo())
              }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["arrow-icon", { "arrow-down": $setup.showMoreInfo }])
                  },
                  null,
                  2
                  /* CLASS */
                ),
                vue.createElementVNode("text", null, "更多信息")
              ])
            ]),
            $setup.showMoreInfo ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "more-info-form"
            }, [
              vue.createElementVNode("view", { class: "form-item size_detail" }, [
                vue.createElementVNode("text", { class: "form-label" }, "尺寸详情"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[7] || (_cache[7] = ($event) => $setup.moreInfo.sizeDetail = $event),
                    placeholder: "请输入尺寸详情",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.sizeDetail]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "颜色"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[8] || (_cache[8] = ($event) => $setup.moreInfo.color = $event),
                    placeholder: "请输入颜色",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.color]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "店名"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[9] || (_cache[9] = ($event) => $setup.moreInfo.shopName = $event),
                    placeholder: "请输入店名",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.shopName]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "头围"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[10] || (_cache[10] = ($event) => $setup.moreInfo.headCircumference = $event),
                    placeholder: "请输入头围(cm)",
                    class: "form-input",
                    type: "digit"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.headCircumference]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "肩宽"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[11] || (_cache[11] = ($event) => $setup.moreInfo.shoulderWidth = $event),
                    placeholder: "请输入肩宽(cm)",
                    class: "form-input",
                    type: "digit"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.shoulderWidth]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "妆师"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[12] || (_cache[12] = ($event) => $setup.moreInfo.makeupArtist = $event),
                    placeholder: "请输入妆师",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.makeupArtist]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "备注"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[13] || (_cache[13] = ($event) => $setup.moreInfo.remark = $event),
                    placeholder: "请输入备注",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.remark]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "购入时间"),
                vue.createElementVNode("view", {
                  class: "date-picker-wrapper",
                  onClick: _cache[14] || (_cache[14] = ($event) => $setup.showBuyPicker = true)
                }, [
                  vue.createElementVNode(
                    "view",
                    { class: "picker-content" },
                    vue.toDisplayString($setup.moreInfo.buyDate || "选择购入日期"),
                    1
                    /* TEXT */
                  )
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "到家日期"),
                vue.createElementVNode("view", {
                  class: "date-picker-wrapper",
                  onClick: _cache[15] || (_cache[15] = ($event) => $setup.showArrivalPicker = true)
                }, [
                  vue.createElementVNode(
                    "view",
                    { class: "picker-content" },
                    vue.toDisplayString($setup.moreInfo.arrivalDate || "选择到家日期"),
                    1
                    /* TEXT */
                  )
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "附加值（妆费或h了多少入）"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[16] || (_cache[16] = ($event) => $setup.moreInfo.additionalValue = $event),
                    placeholder: "请输入附加值",
                    class: "form-input",
                    type: "digit"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.additionalValue]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "存放位置"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[17] || (_cache[17] = ($event) => $setup.moreInfo.position = $event),
                    placeholder: "请输入存放位置",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.moreInfo.position]
                ])
              ])
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("label", {
                class: "remind-label",
                onClick: _cache[18] || (_cache[18] = ($event) => $setup.toggleRemind())
              }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["arrow-icon", { "arrow-down": $setup.form.isRemind }])
                  },
                  null,
                  2
                  /* CLASS */
                ),
                vue.createElementVNode("text", null, "需要补款提醒"),
                $setup.form.finalPrice > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "remind-dot"
                })) : vue.createCommentVNode("v-if", true)
              ])
            ]),
            $setup.form.isRemind ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "remind-form"
            }, [
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "尾款金额"),
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    type: "number",
                    "onUpdate:modelValue": _cache[19] || (_cache[19] = ($event) => $setup.form.finalPrice = $event),
                    placeholder: "请输入待补款金额",
                    class: "form-input"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.form.finalPrice]
                ])
              ]),
              vue.createElementVNode("view", { class: "form-item" }, [
                vue.createElementVNode("text", { class: "form-label" }, "补款日期"),
                vue.createElementVNode("view", {
                  class: "date-picker-wrapper",
                  onClick: _cache[20] || (_cache[20] = ($event) => $setup.showFinalPicker = true)
                }, [
                  vue.createElementVNode(
                    "view",
                    { class: "picker-content" },
                    vue.toDisplayString($setup.form.finalTime || "选择截止日期"),
                    1
                    /* TEXT */
                  )
                ])
              ])
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "info-text" }, [
              vue.createElementVNode("image", {
                src: _imports_0$2,
                mode: "aspectFill"
              }),
              vue.createElementVNode("text", null, "仅用于记录您所购买过的物品，其他人不会看到")
            ]),
            vue.createElementVNode("view", { class: "button-group" }, [
              $setup.isEdit ? (vue.openBlock(), vue.createElementBlock("button", {
                key: 0,
                class: "delete-button",
                onClick: $setup.handleDelete
              }, "删除账本")) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode(
                "button",
                {
                  class: "submit-button",
                  onClick: $setup.postSubmit
                },
                "记录" + vue.toDisplayString($setup.isEdit ? "修改" : "新增"),
                1
                /* TEXT */
              )
            ])
          ]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showBuyPicker,
            "onUpdate:show": _cache[21] || (_cache[21] = ($event) => $setup.showBuyPicker = $event),
            modelValue: $setup.moreInfo.buyDate,
            "onUpdate:modelValue": _cache[22] || (_cache[22] = ($event) => $setup.moreInfo.buyDate = $event),
            title: "选择购入日期",
            "min-date": "2000-01-01",
            "max-date": "2035-12-31"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showArrivalPicker,
            "onUpdate:show": _cache[23] || (_cache[23] = ($event) => $setup.showArrivalPicker = $event),
            modelValue: $setup.moreInfo.arrivalDate,
            "onUpdate:modelValue": _cache[24] || (_cache[24] = ($event) => $setup.moreInfo.arrivalDate = $event),
            title: "选择宠物到家日期",
            "min-date": "2000-01-01",
            "max-date": "2035-12-31"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showFinalPicker,
            "onUpdate:show": _cache[25] || (_cache[25] = ($event) => $setup.showFinalPicker = $event),
            modelValue: $setup.form.finalTime,
            "onUpdate:modelValue": _cache[26] || (_cache[26] = ($event) => $setup.form.finalTime = $event),
            title: "选择补款截止日期",
            "min-date": "2000-01-01",
            "max-date": "2035-12-31"
          }, null, 8, ["show", "modelValue"])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgStockStockAccountBookFormAccountBookForm = /* @__PURE__ */ _export_sfc(_sfc_main$u, [["render", _sfc_render$t], ["__scopeId", "data-v-427d55e2"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-stock/stock/account_book_form/account_book_form.vue"]]);
  const _sfc_main$t = {
    __name: "showcase_form",
    props: ["showcase_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const uploadList = vue.ref([]);
      const hash = (s2) => String(s2).split("").reduce((a2, c2) => (a2 << 5) - a2 + c2.charCodeAt(0) | 0, 0);
      const dragItems = vue.computed(
        () => uploadList.value.map((url) => ({
          id: `img_${hash(url)}`,
          image_url: url
        }))
      );
      function onDragSort(sortedIds) {
        const idToUrl = new Map(dragItems.value.map((it2) => [it2.id, it2.image_url]));
        uploadList.value = sortedIds.map((id) => idToUrl.get(id)).filter(Boolean);
      }
      function onDragUpdate(newItems) {
        uploadList.value = newItems.map((it2) => it2.image_url);
      }
      function onDragDelete(id) {
        const idx = dragItems.value.findIndex((it2) => it2.id === id);
        if (idx >= 0) {
          const list = [...uploadList.value];
          list.splice(idx, 1);
          uploadList.value = list;
        }
      }
      function onDragItemClick(item) {
        const idx = dragItems.value.findIndex((x) => x.id === item.id);
        if (idx >= 0)
          viewFullImage(idx);
      }
      const tabs = [
        { key: "basic", label: "基本" },
        { key: "persona", label: "人设" },
        { key: "relation", label: "关联" }
      ];
      const activeTab = vue.ref("basic");
      const underlineStyle = vue.ref("");
      const instance = vue.getCurrentInstance();
      function updateUnderline() {
        vue.nextTick(() => {
          const q2 = uni.createSelectorQuery().in(instance);
          q2.select(`#tab-${activeTab.value}`).boundingClientRect().select("#tabs-underline").boundingClientRect().exec((res) => {
            const tabRect = res == null ? void 0 : res[0], wrapRect = res == null ? void 0 : res[1];
            if (tabRect && wrapRect) {
              const left = tabRect.left - wrapRect.left;
              const width = tabRect.width;
              underlineStyle.value = `left:${left}px;width:${width}px;`;
            }
          });
        });
      }
      function switchTab(key) {
        activeTab.value = key;
        updateUnderline();
      }
      vue.onMounted(updateUnderline);
      vue.watch(activeTab, updateUnderline);
      const display = vue.ref(-1);
      const isEditable = vue.computed(() => [-1, 1, 3].includes(display.value));
      const props = __props;
      const showDelete = vue.computed(() => Number(props.showcase_id) > 0);
      const name = vue.ref("");
      const description = vue.ref("");
      const birthDate = vue.ref("");
      const showBirthSheet = vue.ref(false);
      function onBirthConfirm(val) {
        birthDate.value = val;
      }
      const gender = vue.ref("");
      const genderOptions = [
        { value: "male", label: "男" },
        { value: "female", label: "女" },
        { value: "other", label: "其他" }
      ];
      const genderIndex = vue.computed(() => {
        const idx = genderOptions.findIndex((g2) => g2.value === gender.value);
        return idx >= 0 ? idx : -1;
      });
      const onGenderChange = (e2) => {
        const idx = Number(e2.detail.value);
        if (idx >= 0 && idx < genderOptions.length)
          gender.value = genderOptions[idx].value;
      };
      const personality = vue.ref("");
      const makeupArtist = vue.ref("");
      const makeupArtistBrandId = vue.ref(0);
      const saveCollocationDataList = vue.ref([]);
      function toGenderInt(v2) {
        if (!v2)
          return void 0;
        switch (v2) {
          case "male":
            return 1;
          case "female":
            return 2;
          case "other":
            return 3;
          default:
            return 0;
        }
      }
      function toGenderStr(v2) {
        if (v2 === 1)
          return "male";
        if (v2 === 2)
          return "female";
        if (v2 === 3)
          return "other";
        return "";
      }
      function dateToUnixSec(d2) {
        if (!d2)
          return void 0;
        const t2 = new Date(String(d2).replace(/-/g, "/"));
        if (isNaN(t2))
          return void 0;
        return Math.floor(t2.getTime() / 1e3);
      }
      function unixSecToDate(s2) {
        if (s2 === null || s2 === void 0)
          return "";
        const sec = Number(s2);
        const ms2 = sec > 2147483647 ? sec : sec * 1e3;
        const d2 = new Date(ms2);
        if (isNaN(d2))
          return "";
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        return `${y2}-${m2}-${day}`;
      }
      function onArtistSelect(id, name2) {
        if (!isEditable.value)
          return;
        if (id && Number(id) > 0) {
          makeupArtistBrandId.value = Number(id);
          makeupArtist.value = name2 || "";
        } else {
          makeupArtist.value = name2 || "";
        }
      }
      function onArtistClose(currentInput) {
        if (!isEditable.value)
          return;
        makeupArtistBrandId.value = 0;
        makeupArtist.value = (currentInput || "").trim();
      }
      function showRelationPicker() {
        if (!isEditable.value)
          return;
        uni.$once("associate:done", (payload) => {
          const brand = (payload == null ? void 0 : payload.brand) || {};
          const goods = (payload == null ? void 0 : payload.goods) || {};
          const relationData = {
            goods_id: Number(goods.id) || 0,
            goods_name: goods.name || "",
            goods_image: goods.cover || goods.image || "",
            brand_id: Number(brand.id) || 0,
            brand_name: brand.brand_name || brand.name || "",
            type: goods.type || ""
          };
          const isExist = saveCollocationDataList.value.some(
            (item) => relationData.goods_id && item.goods_id === relationData.goods_id || !!relationData.goods_name && item.goods_name === relationData.goods_name
          );
          if (!relationData.goods_name && !relationData.goods_id) {
            uni.showToast({ title: "未选择有效的娃物", icon: "none" });
            return;
          }
          if (isExist) {
            uni.showToast({ title: "已存在相同关联项", icon: "none" });
          } else {
            saveCollocationDataList.value.push(relationData);
            uni.showToast({ title: "已添加", icon: "success" });
          }
        });
        uni.navigateTo({
          url: "/pkg-common/brand-pick/brand-pick",
          // 可选：预选最近一次关联的品牌/商品（若你希望带入）
          success(res) {
            const last = saveCollocationDataList.value.slice(-1)[0];
            if (last && last.brand_id) {
              res.eventChannel.emit("associate:init", {
                preselect: {
                  brand: { id: last.brand_id, brand_name: last.brand_name },
                  goods: last.goods_id ? { id: last.goods_id, name: last.goods_name } : null
                }
              });
            }
          }
        });
      }
      async function getShowCaseInfo() {
        var _a;
        if (!props.showcase_id)
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/showcase-detail?id=${props.showcase_id}`,
            method: "GET",
            header: { "Authorization": uni.getStorageSync("token") }
          });
          const data = ((_a = res.data) == null ? void 0 : _a.data) || {};
          name.value = data.name || "";
          description.value = data.description || "";
          display.value = data.display;
          uploadList.value = data.image_urls ? data.image_urls.split(",").filter(Boolean) : [];
          if (Array.isArray(data.relations)) {
            saveCollocationDataList.value = data.relations.map((r2) => ({
              goods_id: r2.relation_goods_id,
              goods_name: r2.relation_goods_name,
              brand_id: r2.relation_brand_id,
              brand_name: r2.relation_brand_name,
              type: r2.type,
              goods_image: r2.relation_goods_image
            }));
          }
          birthDate.value = data.birth_at !== void 0 && data.birth_at !== null ? unixSecToDate(data.birth_at) : "";
          gender.value = toGenderStr(data.gender);
          personality.value = data.personality || "";
          makeupArtist.value = data.makeup_artist || "";
          makeupArtistBrandId.value = data.makeup_artist_id ? Number(data.makeup_artist_id) : 0;
          updateUnderline();
        } catch {
          uni.showToast({ title: "加载失败", icon: "none" });
        }
      }
      function viewFullImage(index) {
        uni.previewImage({ current: uploadList.value[index], urls: uploadList.value });
      }
      async function selectImage() {
        try {
          const imagePaths = await chooseImageList(9);
          for (const path of imagePaths) {
            const tokenData = await getQiniuToken();
            await uploadImageToQiniu(path, tokenData.token, tokenData.path);
            uploadList.value.push(image1Url + tokenData.path);
          }
          uni.showToast({ title: `成功上传${imagePaths.length}张图片`, icon: "success" });
        } catch {
          uni.showToast({ title: "部分图片上传失败", icon: "none" });
        }
      }
      async function handleDelete() {
        uni.showModal({
          title: "确认删除",
          content: "确定要删除这个展示吗？",
          success: async (res) => {
            if (res.confirm) {
              try {
                const resp = await uni.request({
                  url: `${websiteUrl.value}/with-state/delete-showcase?id=${props.showcase_id}`,
                  method: "POST",
                  header: { "Authorization": uni.getStorageSync("token") }
                });
                if ((resp.data || {}).status === "success") {
                  uni.showToast({ title: "删除成功" });
                  setTimeout(() => uni.navigateBack(), 800);
                } else {
                  uni.showToast({ title: (resp.data || {}).msg || "删除失败", icon: "none" });
                }
              } catch {
                uni.showToast({ title: "删除失败", icon: "none" });
              }
            }
          }
        });
      }
      async function submitForm() {
        const token = uni.getStorageSync("token");
        if (!token)
          return uni.showToast({ title: "请先登录", icon: "none" });
        if (!name.value.trim())
          return uni.showToast({ title: "名字不能为空", icon: "none" });
        if (!description.value.trim())
          return uni.showToast({ title: "描述不能为空", icon: "none" });
        if (uploadList.value.length === 0)
          return uni.showToast({ title: "请至少上传一张图片", icon: "none" });
        const genderVal = toGenderInt(gender.value);
        const birthAtVal = dateToUnixSec(birthDate.value);
        let postData = {
          name: name.value,
          description: description.value,
          image_urls: uploadList.value.join(","),
          // 按拖拽顺序
          origin: getScene(),
          relations: saveCollocationDataList.value.map((item) => ({
            relation_goods_id: item.goods_id,
            relation_goods_name: item.goods_name,
            relation_brand_id: item.brand_id,
            relation_brand_name: item.brand_name,
            type: item.type,
            relation_origin: 2
          })),
          personality: personality.value || "",
          makeup_artist: makeupArtist.value || "",
          makeup_artist_id: Number(makeupArtistBrandId.value) || 0
        };
        if (typeof genderVal === "number")
          postData.gender = genderVal;
        if (birthAtVal !== void 0)
          postData.birth_at = birthAtVal;
        try {
          let url = `${websiteUrl.value}/with-state/add-showcase`;
          if (props.showcase_id) {
            url = `${websiteUrl.value}/with-state/update-showcase`;
            postData = { ...postData, id: parseInt(props.showcase_id, 10) };
          }
          const res = await uni.request({
            url,
            method: "POST",
            data: postData,
            header: { "Content-Type": "application/json", "Authorization": token }
          });
          const resp = res.data || {};
          const ok = resp.status === "success" || resp.code === "success" || resp.code === 0 || resp.success === true;
          if (ok) {
            uni.showToast({ title: "提交成功" });
            setTimeout(() => uni.navigateBack(), 1e3);
          } else {
            uni.showToast({ title: resp.msg || "提交失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "提交失败", icon: "none" });
        }
      }
      function getTypes() {
        uni.request({
          url: websiteUrl.value + "/goods-types",
          method: "GET",
          timeout: 5e3,
          success: (res) => {
          },
          fail: () => uni.showToast({ title: "网络请求失败", icon: "none" })
        });
      }
      function deleteCollcation(index) {
        saveCollocationDataList.value.splice(index, 1);
      }
      function clearArtist() {
        if (!isEditable.value)
          return;
        makeupArtist.value = "";
        makeupArtistBrandId.value = 0;
      }
      uni.setNavigationBarTitle({ title: "私养展示" });
      onLoad(async (options) => {
        var _a;
        if (options.goods_id && options.goods_name && options.brand_id && options.brand_name && options.type) {
          let goodsImage = "";
          try {
            const res = await uni.request({ url: websiteUrl.value + "/goods?id=" + parseInt(options.goods_id), method: "GET" });
            const gi = (res.data || {}).data || {};
            goodsImage = Array.isArray(gi.goods_images) ? gi.goods_images[0] : ((_a = gi.goods_images) == null ? void 0 : _a[0]) || "";
          } catch {
          }
          saveCollocationDataList.value.push({
            brand_id: parseInt(options.brand_id, 10),
            goods_id: parseInt(options.goods_id, 10),
            brand_name: options.brand_name,
            goods_name: options.goods_name,
            goods_image: goodsImage,
            type: options.type
          });
        }
        getTypes();
        getShowCaseInfo();
      });
      const __returned__ = { uploadList, hash, dragItems, onDragSort, onDragUpdate, onDragDelete, onDragItemClick, tabs, activeTab, underlineStyle, instance, updateUnderline, switchTab, display, isEditable, props, showDelete, name, description, birthDate, showBirthSheet, onBirthConfirm, gender, genderOptions, genderIndex, onGenderChange, personality, makeupArtist, makeupArtistBrandId, saveCollocationDataList, toGenderInt, toGenderStr, dateToUnixSec, unixSecToDate, onArtistSelect, onArtistClose, showRelationPicker, getShowCaseInfo, viewFullImage, selectImage, handleDelete, submitForm, getTypes, deleteCollcation, clearArtist, ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick, getCurrentInstance: vue.getCurrentInstance, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get image1Url() {
        return image1Url;
      }, get getScene() {
        return getScene;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, get chooseImageList() {
        return chooseImageList;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$s(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_shmily_drag_image = resolveEasycom(vue.resolveDynamicComponent("shmily-drag-image"), __easycom_3$6);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_search = resolveEasycom(vue.resolveDynamicComponent("common-search"), __easycom_2$a);
    const _component_common_date_picker = resolveEasycom(vue.resolveDynamicComponent("common-date-picker"), __easycom_1);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createElementVNode("meta", {
        name: "theme-color",
        content: "#F8F8F8"
      }),
      vue.createVNode(_component_view_logs),
      !$setup.isEditable ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "edit-tip"
      }, [
        vue.createElementVNode("text", null, "当前状态不可编辑")
      ])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "image-block" }, [
        vue.createVNode(_component_shmily_drag_image, {
          "model-value": $setup.dragItems,
          keyName: "image_url",
          showDelete: $setup.isEditable,
          showItemInfo: false,
          cols: 3,
          imageWidth: 0,
          padding: 12,
          scale: 1.06,
          opacity: 0.88,
          customClick: true,
          "onUpdate:modelValue": $setup.onDragUpdate,
          onSortChange: $setup.onDragSort,
          onDelete: $setup.onDragDelete,
          onItemClick: $setup.onDragItemClick
        }, null, 8, ["model-value", "showDelete"]),
        $setup.isEditable ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "add-row"
        }, [
          vue.createElementVNode("button", {
            class: "add-btn",
            onClick: $setup.selectImage
          }, [
            vue.createElementVNode("image", {
              src: _imports_0$7,
              class: "add-icon"
            }),
            vue.createElementVNode("text", null, "添加图片")
          ])
        ])) : vue.createCommentVNode("v-if", true)
      ]),
      vue.createElementVNode("view", {
        class: "tabs-underline",
        id: "tabs-underline"
      }, [
        (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.tabs, (t2) => {
            return vue.createElementVNode("view", {
              key: t2.key,
              class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === t2.key }]),
              id: `tab-${t2.key}`,
              onClick: ($event) => $setup.switchTab(t2.key)
            }, vue.toDisplayString(t2.label), 11, ["id", "onClick"]);
          }),
          64
          /* STABLE_FRAGMENT */
        )),
        vue.createElementVNode(
          "view",
          {
            class: "underline",
            style: vue.normalizeStyle($setup.underlineStyle)
          },
          null,
          4
          /* STYLE */
        )
      ]),
      vue.withDirectives(vue.createElementVNode(
        "view",
        null,
        [
          vue.withDirectives(vue.createElementVNode("input", {
            "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.name = $event),
            type: "text",
            disabled: !$setup.isEditable,
            placeholder: "请输入标题",
            class: "field-input"
          }, null, 8, ["disabled"]), [
            [vue.vModelText, $setup.name]
          ]),
          vue.withDirectives(vue.createElementVNode("textarea", {
            "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.description = $event),
            disabled: !$setup.isEditable,
            placeholder: "请输入内容",
            class: "desc-area"
          }, null, 8, ["disabled"]), [
            [vue.vModelText, $setup.description]
          ]),
          vue.createElementVNode("view", { class: "publish-detail" }, [
            vue.createElementVNode("text", null, "* 展示您的宝宝们。🩵"),
            vue.createElementVNode("text", null, "* 不关联商品的展示柜不会出现在广场中。")
          ])
        ],
        512
        /* NEED_PATCH */
      ), [
        [vue.vShow, $setup.activeTab === "basic"]
      ]),
      vue.withDirectives(vue.createElementVNode(
        "view",
        null,
        [
          vue.createElementVNode("view", { class: "card-block" }, [
            vue.createElementVNode("view", { class: "form-row equal" }, [
              vue.createElementVNode("text", { class: "label" }, "出生日期"),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["cell", { disabled: !$setup.isEditable }]),
                  onClick: _cache[2] || (_cache[2] = ($event) => $setup.isEditable && ($setup.showBirthSheet = true))
                },
                [
                  vue.createElementVNode(
                    "text",
                    {
                      class: vue.normalizeClass(["cell-value", { placeholder: !$setup.birthDate }])
                    },
                    vue.toDisplayString($setup.birthDate || "请选择出生日期"),
                    3
                    /* TEXT, CLASS */
                  ),
                  vue.createVNode(_component_uni_icons, {
                    type: "right",
                    size: "16",
                    color: "#bbb"
                  })
                ],
                2
                /* CLASS */
              )
            ]),
            vue.createElementVNode("view", { class: "form-row equal" }, [
              vue.createElementVNode("text", { class: "label" }, "性别"),
              vue.createElementVNode("picker", {
                mode: "selector",
                range: $setup.genderOptions,
                "range-key": "label",
                value: $setup.genderIndex,
                disabled: !$setup.isEditable,
                onChange: $setup.onGenderChange,
                class: "cell"
              }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["cell-value", { placeholder: $setup.genderIndex === -1 }])
                  },
                  vue.toDisplayString($setup.genderIndex === -1 ? "请选择性别" : $setup.genderOptions[$setup.genderIndex].label),
                  3
                  /* TEXT, CLASS */
                )
              ], 40, ["value", "disabled"])
            ]),
            vue.createElementVNode("view", { class: "form-row equal" }, [
              vue.createElementVNode("text", { class: "label" }, "性格"),
              vue.withDirectives(vue.createElementVNode("input", {
                "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.personality = $event),
                disabled: !$setup.isEditable,
                placeholder: "设定中的性格",
                class: "cell-input",
                type: "text",
                maxlength: "50"
              }, null, 8, ["disabled"]), [
                [vue.vModelText, $setup.personality]
              ])
            ])
          ])
        ],
        512
        /* NEED_PATCH */
      ), [
        [vue.vShow, $setup.activeTab === "persona"]
      ]),
      vue.withDirectives(vue.createElementVNode(
        "view",
        null,
        [
          vue.createElementVNode("view", { class: "card-block" }, [
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["form-row artist-row", { disabled: !$setup.isEditable }]),
                style: { "border-bottom": "none" }
              },
              [
                vue.createElementVNode("text", { class: "label" }, "妆师"),
                vue.createElementVNode("view", { style: { "flex": "1" } }, [
                  $setup.isEditable ? (vue.openBlock(), vue.createBlock(_component_common_search, {
                    key: 0,
                    mode: "fill",
                    showIndexSelector: false,
                    defaultIndex: "bjd_artist",
                    background: "#f8f8f8",
                    hideHintText: true,
                    onSelect: $setup.onArtistSelect,
                    onCloseAssociate: $setup.onArtistClose,
                    class: "artist-search",
                    keywords: $setup.makeupArtist
                  }, null, 8, ["keywords"])) : vue.createCommentVNode("v-if", true),
                  $setup.isEditable && ($setup.makeupArtist || $setup.makeupArtistBrandId) ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 1,
                    class: "selected-artist"
                  }, [
                    vue.createElementVNode("text", { class: "prefix" }, "已选择："),
                    vue.createElementVNode(
                      "text",
                      { class: "name" },
                      vue.toDisplayString($setup.makeupArtist || "（未命名）"),
                      1
                      /* TEXT */
                    ),
                    $setup.makeupArtistBrandId ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "id"
                      },
                      "（ID：" + vue.toDisplayString($setup.makeupArtistBrandId) + "）",
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("text", {
                      class: "clear",
                      onClick: $setup.clearArtist
                    }, "清除")
                  ])) : !$setup.isEditable ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 2,
                    class: "readonly-row"
                  }, [
                    vue.createElementVNode(
                      "text",
                      { class: "readonly-name" },
                      vue.toDisplayString($setup.makeupArtist || "（未填写）"),
                      1
                      /* TEXT */
                    )
                  ])) : vue.createCommentVNode("v-if", true)
                ])
              ],
              2
              /* CLASS */
            )
          ]),
          vue.createElementVNode("view", null, [
            $setup.isEditable ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "relation-trigger",
              onClick: $setup.showRelationPicker
            }, [
              vue.createElementVNode("text", { class: "placeholder" }, "点击关联娃物"),
              vue.createElementVNode("image", {
                src: _imports_1$5,
                class: "arrow-icon"
              })
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          vue.createElementVNode("view", { class: "saveCollocationDataList" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.saveCollocationDataList, (item, index) => {
                return vue.openBlock(), vue.createElementBlock("view", { key: index }, [
                  vue.createElementVNode("view", { class: "saveCollocationDataItem" }, [
                    item.goods_image ? (vue.openBlock(), vue.createElementBlock("image", {
                      key: 0,
                      src: item.goods_image,
                      mode: "aspectFill",
                      style: { "width": "70px", "height": "70px" }
                    }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "no-image"
                    }, "?")),
                    vue.createElementVNode(
                      "text",
                      {
                        class: "info-tap",
                        style: { "width": "calc(100% - 120px)", "display": "inline-block" }
                      },
                      vue.toDisplayString(item.type) + " " + vue.toDisplayString(item.brand_name) + " - " + vue.toDisplayString(item.goods_name),
                      1
                      /* TEXT */
                    ),
                    $setup.isEditable ? (vue.openBlock(), vue.createElementBlock("image", {
                      key: 2,
                      src: _imports_2$5,
                      class: "close_icon",
                      onClick: ($event) => $setup.deleteCollcation(index)
                    }, null, 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ],
        512
        /* NEED_PATCH */
      ), [
        [vue.vShow, $setup.activeTab === "relation"]
      ]),
      vue.createVNode(_component_common_date_picker, {
        show: $setup.showBirthSheet,
        "onUpdate:show": _cache[4] || (_cache[4] = ($event) => $setup.showBirthSheet = $event),
        modelValue: $setup.birthDate,
        "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.birthDate = $event),
        title: "选择出生日期",
        minDate: "1970-01-01",
        maxDate: "2035-12-31",
        onConfirm: $setup.onBirthConfirm
      }, null, 8, ["show", "modelValue"]),
      vue.createElementVNode("view", { class: "footer" }, [
        $setup.showDelete ? (vue.openBlock(), vue.createElementBlock("text", {
          key: 0,
          class: "delete-link",
          onClick: $setup.handleDelete
        }, "删除")) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("button", {
          class: "publish-btn",
          onClick: $setup.submitForm
        }, "发表")
      ])
    ]);
  }
  const PkgStockStockShowcaseFormShowcaseForm = /* @__PURE__ */ _export_sfc(_sfc_main$t, [["render", _sfc_render$s], ["__scopeId", "data-v-b75c3767"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-stock/stock/showcase_form/showcase_form.vue"]]);
  const _sfc_main$s = {
    __name: "bill_form",
    props: ["bill_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const routeParams = vue.ref({});
      const isEdit = props.bill_id ? true : false;
      const name = vue.ref("");
      const price = vue.ref("");
      const date = vue.ref("");
      const status = vue.ref(0);
      const statusList = ["未补款", "已补款"];
      onLoad((options) => {
        routeParams.value = options;
        if (options.name) {
          name.value = decodeURIComponent(options.name);
        }
        if (options.amount) {
          price.value = options.amount;
        }
      });
      function getBillById(id) {
        let token = uni.getStorageSync("token");
        uni.request({
          url: websiteUrl.value + "/with-state/tail-bill-detail",
          method: "GET",
          header: { "Authorization": token },
          data: { id },
          success: (res) => {
            name.value = res.data.data.name;
            price.value = res.data.data.price;
            date.value = res.data.data.ymd;
            status.value = res.data.data.status;
            formattedDate.value = formatDate(res.data.data.ymd);
          },
          fail: (err) => {
            formatAppLog("log", "at pkg-stock/stock/bill_form/bill_form.vue:141", err);
          }
        });
      }
      const formattedDate = vue.ref("");
      function updateDate(e2) {
        const selectedDate = e2.detail.value;
        date.value = selectedDate;
        formattedDate.value = formatDate(selectedDate);
      }
      function handleDelete() {
        uni.showModal({
          title: "提示",
          content: "确定删除该账单吗？",
          success: (res) => {
            if (res.confirm) {
              uni.request({
                url: websiteUrl.value + "/with-state/delete-tail-bill?id=" + parseInt(props.bill_id, 10),
                method: "POST",
                header: { "Authorization": uni.getStorageSync("token") },
                success: (res2) => {
                  if (res2.data.status == "success") {
                    uni.showToast({
                      title: "删除成功",
                      icon: "success"
                    });
                    setTimeout(() => {
                      uni.navigateBack();
                    }, 500);
                  } else {
                    uni.showToast({
                      title: res2.data.msg,
                      icon: "none"
                    });
                  }
                }
              });
            }
          }
        });
      }
      function formatDate(dateStr) {
        let date2 = new Date(dateStr);
        const year = date2.getFullYear();
        const month = (date2.getMonth() + 1).toString().padStart(2, "0");
        const day = date2.getDate().toString().padStart(2, "0");
        return `${year}-${month}-${day}`;
      }
      function updateStatus(e2) {
        status.value = e2.detail.value;
      }
      vue.onMounted(() => {
        if (isEdit) {
          getBillById(props.bill_id);
          uni.setNavigationBarTitle({
            title: "编辑账单"
          });
        } else {
          uni.setNavigationBarTitle({
            title: "新增账单"
          });
        }
      });
      function postSubmit() {
        if (isEdit) {
          updateBill();
        } else {
          addBill();
        }
      }
      function addBill() {
        let postData = {
          name: name.value,
          price: parseFloat(price.value),
          ymd: date.value,
          // 原始日期
          status: status.value
        };
        uni.request({
          url: websiteUrl.value + "/with-state/add-tail-bill",
          method: "POST",
          header: {
            "Authorization": uni.getStorageSync("token")
          },
          data: postData,
          success: (res) => {
            if (res.data.status == "success") {
              uni.showToast({
                title: "提交成功",
                icon: "success"
              });
              setTimeout(() => {
                uni.navigateBack();
              }, 500);
            } else {
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
            }
          }
        });
      }
      function updateBill() {
        let postData = {
          id: parseInt(props.bill_id, 10),
          name: name.value,
          price: parseFloat(price.value),
          ymd: date.value,
          // 原始日期
          status: status.value
        };
        uni.request({
          url: websiteUrl.value + "/with-state/update-tail-bill",
          method: "POST",
          header: {
            "Authorization": uni.getStorageSync("token")
          },
          data: postData,
          success: (res) => {
            if (res.data.status == "success") {
              uni.showToast({
                title: "提交成功",
                icon: "success"
              });
              setTimeout(() => {
                uni.navigateBack();
              }, 500);
            } else {
              uni.showToast({
                title: res.data.msg,
                icon: "none"
              });
            }
          }
        });
      }
      const __returned__ = { props, routeParams, isEdit, name, price, date, status, statusList, getBillById, formattedDate, updateDate, handleDelete, formatDate, updateStatus, postSubmit, addBill, updateBill, ref: vue.ref, onMounted: vue.onMounted, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get onLoad() {
        return onLoad;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$r(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode("view", { class: "container" }, [
          vue.createElementVNode("meta", {
            name: "theme-color",
            content: "#F8F8F8"
          }),
          vue.createElementVNode("view", { class: "form-card" }, [
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "账单名称"),
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "form-input",
                  type: "text",
                  placeholder: "请输入名称",
                  "placeholder-class": "placeholder-style",
                  "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.name = $event)
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.name]
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "账单金额"),
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "form-input",
                  type: "digit",
                  placeholder: "请输入金额",
                  "placeholder-class": "placeholder-style",
                  "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.price = $event)
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.price]
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "账单日期"),
              vue.createElementVNode("view", { style: { "padding": "0px 10px", "border": "1px solid #e6e6e6", "border-radius": "10px" } }, [
                vue.createElementVNode("picker", {
                  mode: "date",
                  value: $setup.formattedDate,
                  onChange: $setup.updateDate
                }, [
                  vue.createElementVNode(
                    "view",
                    {
                      class: "picker-content",
                      style: { "color": "#2c2c2c", "font-size": "26rpx" }
                    },
                    vue.toDisplayString($setup.formattedDate || "请选择日期"),
                    1
                    /* TEXT */
                  )
                ], 40, ["value"])
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "form-label" }, "账单状态"),
              vue.createElementVNode("view", { style: { "padding": "0px 10px", "border": "1px solid #e6e6e6", "border-radius": "10px" } }, [
                vue.createElementVNode("picker", {
                  mode: "selector",
                  value: $setup.status,
                  range: $setup.statusList,
                  onChange: $setup.updateStatus
                }, [
                  vue.createElementVNode(
                    "view",
                    { class: "picker-content" },
                    vue.toDisplayString($setup.statusList[$setup.status] || "请选择状态"),
                    1
                    /* TEXT */
                  )
                ], 40, ["value"])
              ])
            ]),
            vue.createElementVNode("view", { style: { "overflow": "hidden" } }, [
              vue.createElementVNode("image", {
                src: _imports_0$2,
                mode: "aspectFill",
                style: { "width": "28rpx", "height": "28rpx", "margin-right": "10rpx", "position": "relative", "top": "3rpx" }
              }),
              vue.createElementVNode("text", { style: { "color": "#888" } }, "仅用于您的记账，其他人不会看到")
            ])
          ]),
          vue.createElementVNode("view", { class: "button-group" }, [
            vue.createElementVNode(
              "button",
              {
                class: "submit-button",
                onClick: $setup.postSubmit
              },
              " 记录" + vue.toDisplayString($setup.isEdit ? "修改" : "新增"),
              1
              /* TEXT */
            ),
            $setup.isEdit ? (vue.openBlock(), vue.createElementBlock("button", {
              key: 0,
              class: "delete-button",
              onClick: $setup.handleDelete
            }, " 删除账单 ")) : vue.createCommentVNode("v-if", true)
          ])
        ])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgStockStockBillFormBillForm = /* @__PURE__ */ _export_sfc(_sfc_main$s, [["render", _sfc_render$r], ["__scopeId", "data-v-65514f70"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-stock/stock/bill_form/bill_form.vue"]]);
  const _sfc_main$r = {
    __name: "account_book_preview",
    props: ["account_book_id"],
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const scrollTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      function goBack() {
        const pages2 = (getCurrentPages == null ? void 0 : getCurrentPages()) || [];
        if (pages2.length > 1)
          uni.navigateBack({ delta: 1 });
        else
          uni.switchTab({ url: "/pages/index/index" });
      }
      const loadingSuccess = vue.ref(false);
      const detail = vue.ref({});
      const swiperHeight = vue.ref(500);
      const imageHeights = vue.ref([]);
      const screenWidth = vue.ref(0);
      const STORAGE_KEY = `account_book_heights_${props.account_book_id}`;
      const handleImageLoad = (event, index) => {
        if (!screenWidth.value)
          return;
        const { width: originWidth, height: originHeight } = event.detail;
        const renderHeight = originHeight / originWidth * screenWidth.value;
        imageHeights.value[index] = renderHeight;
        const currentHeights = imageHeights.value.filter((h2) => h2);
        if (currentHeights.length > 0) {
          const maxHeight = Math.max(...currentHeights);
          if (maxHeight > swiperHeight.value) {
            swiperHeight.value = maxHeight;
            uni.setStorageSync(STORAGE_KEY, { heights: imageHeights.value, screenWidth: screenWidth.value });
          }
        }
      };
      const imageList = vue.computed(() => {
        if (!detail.value.image_url)
          return [];
        return detail.value.image_url.split(",").map((u2) => u2.trim());
      });
      const fetchDetail = async (id) => {
        const token = uni.getStorageSync("token");
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/account-book-detail?id=${id}`,
            method: "GET",
            header: { Authorization: token }
          });
          if (res.data.status === "success") {
            detail.value = res.data.data;
            setTimeout(() => {
              loadingSuccess.value = true;
              uni.setNavigationBarTitle({ title: detail.value.name || "账本详情" });
            }, 600);
          } else {
            uni.showToast({ title: res.data.msg || "获取详情失败", icon: "none" });
          }
        } catch (e2) {
          uni.showToast({ title: "网络请求失败", icon: "none" });
        }
      };
      const navigateToEdit = () => {
        uni.navigateTo({ url: `/pkg-stock/stock/account_book_form/account_book_form?account_book_id=${props.account_book_id}` });
      };
      onShow(() => {
        const systemInfo2 = uni.getSystemInfoSync();
        const currentScreenWidth = systemInfo2.windowWidth;
        screenWidth.value = currentScreenWidth;
        const storedData = uni.getStorageSync(STORAGE_KEY);
        swiperHeight.value = 300;
        imageHeights.value = [];
        if (storedData) {
          const isScreenSizeChanged = storedData.screenWidth !== currentScreenWidth;
          if (!isScreenSizeChanged && storedData.heights.length > 0) {
            imageHeights.value = storedData.heights;
            const maxStoredHeight = Math.max(...storedData.heights.filter((h2) => h2));
            swiperHeight.value = Math.max(maxStoredHeight, 300);
          }
        }
        asyncGetUserInfo().then(() => fetchDetail(props.account_book_id));
      });
      const payLabel = vue.computed(() => {
        var _a;
        const v2 = (_a = detail.value) == null ? void 0 : _a.payment_status;
        if (v2 === 2)
          return "已付定金";
        if (v2 === 3)
          return "未购买";
        return "已全款";
      });
      const payClass = vue.computed(() => {
        var _a;
        const v2 = (_a = detail.value) == null ? void 0 : _a.payment_status;
        if (v2 === 2)
          return "is-deposit";
        if (v2 === 3)
          return "is-unbought";
        return "is-full";
      });
      const __returned__ = { props, scrollTop, goBack, loadingSuccess, detail, swiperHeight, imageHeights, screenWidth, STORAGE_KEY, handleImageLoad, imageList, fetchDetail, navigateToEdit, payLabel, payClass, ref: vue.ref, computed: vue.computed, get onShow() {
        return onShow;
      }, get onPageScroll() {
        return onPageScroll;
      }, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$q(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_loading_toast = resolveEasycom(vue.resolveDynamicComponent("loading-toast"), __easycom_3$8);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createVNode(_component_zhouWei_navBar, {
          type: "transparentFixed",
          backState: 2e3,
          homeState: 2e3,
          fontColor: "#000",
          transparentFixedFontColor: "#000",
          scrollTop: $setup.scrollTop
        }, {
          left: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          transparentFixedLeft: vue.withCtx(() => [
            vue.createElementVNode("view", {
              class: "nav-back-pill",
              onClick: $setup.goBack,
              "aria-label": "返回"
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "left",
                size: "22",
                color: "#000"
              })
            ])
          ]),
          _: 1
          /* STABLE */
        }, 8, ["scrollTop"]),
        $setup.loadingSuccess ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "detail-container"
        }, [
          vue.createElementVNode("view", { class: "header-image-container" }, [
            vue.createElementVNode(
              "swiper",
              {
                class: "swiper",
                autoplay: true,
                interval: 3e3,
                circular: true,
                "indicator-dots": "",
                style: vue.normalizeStyle({ height: $setup.swiperHeight + "px" })
              },
              [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.imageList, (img, index) => {
                    return vue.openBlock(), vue.createElementBlock("swiper-item", { key: index }, [
                      vue.createElementVNode("view", { class: "image-container" }, [
                        vue.createElementVNode("image", {
                          src: img,
                          mode: "widthFix",
                          class: "header-image",
                          onLoad: ($event) => $setup.handleImageLoad($event, index)
                        }, null, 40, ["src", "onLoad"])
                      ])
                    ]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ],
              4
              /* STYLE */
            ),
            vue.createElementVNode("view", { class: "image-overlay" })
          ]),
          vue.createElementVNode("view", { class: "title-container" }, [
            vue.createElementVNode(
              "text",
              { class: "title" },
              vue.toDisplayString($setup.detail.name),
              1
              /* TEXT */
            ),
            vue.createElementVNode("view", { class: "tag-row" }, [
              vue.createElementVNode(
                "view",
                { class: "category-tag" },
                vue.toDisplayString($setup.detail.type),
                1
                /* TEXT */
              ),
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["pay-badge", $setup.payClass])
                },
                vue.toDisplayString($setup.payLabel),
                3
                /* TEXT, CLASS */
              )
            ])
          ]),
          vue.createElementVNode("view", { class: "info-section" }, [
            vue.createElementVNode("view", { class: "section-title" }, [
              vue.createVNode(_component_uni_icons, {
                type: "list",
                size: "16",
                color: "#5db7ff"
              }),
              vue.createElementVNode("text", null, "详细信息")
            ]),
            vue.createElementVNode("view", { class: "info-container" }, [
              $setup.detail.count ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "number",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "数量")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.count),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.price || $setup.detail.final_price > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "money",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "价格")
                ]),
                vue.createElementVNode("view", { class: "info-value" }, [
                  $setup.detail.final_price > 0 ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "highlight"
                    },
                    "总价: " + vue.toDisplayString($setup.detail.price) + "元",
                    1
                    /* TEXT */
                  )) : (vue.openBlock(), vue.createElementBlock(
                    "text",
                    { key: 1 },
                    vue.toDisplayString($setup.detail.price) + "元",
                    1
                    /* TEXT */
                  ))
                ])
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.final_price > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 2,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "money",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "尾款金额")
                ]),
                vue.createElementVNode("view", { class: "info-value" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "highlight" },
                    vue.toDisplayString($setup.detail.final_price) + "元",
                    1
                    /* TEXT */
                  ),
                  $setup.detail.final_time ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "time-tag"
                    },
                    "（" + vue.toDisplayString($setup.detail.final_time) + "）",
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.shop_name ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 3,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "shop",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "店铺")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.shop_name),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.size || $setup.detail.size_detail ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 4,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "compose",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "尺寸")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.size) + " " + vue.toDisplayString($setup.detail.size_detail),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.head_circumference ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 5,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "circle",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "头围")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.head_circumference),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.shoulder_width ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 6,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "arrowright",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "肩宽")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.shoulder_width),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.makeup_artist ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 7,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "person",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "妆师")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.makeup_artist),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.color ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 8,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "color",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "颜色")
                ]),
                vue.createElementVNode("view", { class: "info-value" }, [
                  vue.createTextVNode(
                    vue.toDisplayString($setup.detail.color) + " ",
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode(
                    "view",
                    {
                      class: "color-preview",
                      style: vue.normalizeStyle({ backgroundColor: $setup.detail.color })
                    },
                    null,
                    4
                    /* STYLE */
                  )
                ])
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          $setup.detail.buy_date || $setup.detail.arrival_date ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "info-section"
          }, [
            vue.createElementVNode("view", { class: "section-title" }, [
              vue.createVNode(_component_uni_icons, {
                type: "calendar",
                size: "16",
                color: "#5db7ff"
              }),
              vue.createElementVNode("text", null, "时间信息")
            ]),
            vue.createElementVNode("view", { class: "info-container" }, [
              $setup.detail.buy_date ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "cart",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "购入时间")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.buy_date),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.arrival_date ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "home",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "到家日期")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.arrival_date),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ])) : vue.createCommentVNode("v-if", true),
          $setup.detail.position ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "info-section"
          }, [
            vue.createElementVNode("view", { class: "section-title" }, [
              vue.createVNode(_component_uni_icons, {
                type: "location",
                size: "16",
                color: "#5db7ff"
              }),
              vue.createElementVNode("text", null, "位置信息")
            ]),
            vue.createElementVNode("view", { class: "info-container" }, [
              vue.createElementVNode("view", { class: "info-line" }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "map",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "存放位置")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.position),
                  1
                  /* TEXT */
                )
              ])
            ])
          ])) : vue.createCommentVNode("v-if", true),
          $setup.detail.additional_value || $setup.detail.remark ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "info-section"
          }, [
            vue.createElementVNode("view", { class: "section-title" }, [
              vue.createVNode(_component_uni_icons, {
                type: "info",
                size: "16",
                color: "#5db7ff"
              }),
              vue.createElementVNode("text", null, "附加信息")
            ]),
            vue.createElementVNode("view", { class: "info-container" }, [
              $setup.detail.additional_value ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "plus",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "附加值")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.additional_value),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true),
              $setup.detail.remark ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "info-line"
              }, [
                vue.createElementVNode("view", { class: "info-label" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "chat",
                    size: "14",
                    color: "#5db7ff"
                  }),
                  vue.createElementVNode("text", null, "备注")
                ]),
                vue.createElementVNode(
                  "view",
                  { class: "info-value" },
                  vue.toDisplayString($setup.detail.remark),
                  1
                  /* TEXT */
                )
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ])) : vue.createCommentVNode("v-if", true)
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", {
          class: "edit-button-container",
          onClick: $setup.navigateToEdit
        }, [
          vue.createElementVNode("view", { class: "edit-button" }, [
            vue.createVNode(_component_uni_icons, {
              type: "compose",
              size: "22",
              color: "#fff"
            })
          ])
        ]),
        vue.createVNode(_component_loading_toast, {
          show: !$setup.loadingSuccess
        }, null, 8, ["show"])
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgStockAccountBookPreviewAccountBookPreview = /* @__PURE__ */ _export_sfc(_sfc_main$r, [["render", _sfc_render$q], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-stock/account_book_preview/account_book_preview.vue"]]);
  const _sfc_main$q = {
    __name: "stock-myitems-search",
    setup(__props, { expose: __expose }) {
      var _a;
      __expose();
      const safeTop = vue.ref(0);
      try {
        const wi = uni.getWindowInfo && uni.getWindowInfo() || uni.getSystemInfoSync();
        safeTop.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.top) ?? (wi == null ? void 0 : wi.statusBarHeight) ?? 0;
      } catch {
        safeTop.value = 20;
      }
      const safePadPx = `${Math.max(0, safeTop.value)}px`;
      const baseList = vue.ref([]);
      const isPickMode = vue.ref(false);
      const isMP = vue.ref(false);
      const capsulePad = vue.ref(0);
      onLoad((options) => {
        if (options && (options.pickMode === "1" || options.pickMode === "true")) {
          isPickMode.value = true;
        }
        try {
          const ec = uni.getOpenerEventChannel && uni.getOpenerEventChannel();
          ec && ec.on && ec.on("initList", (data) => {
            baseList.value = Array.isArray(data == null ? void 0 : data.list) ? data.list : [];
          });
        } catch {
        }
        if (!baseList.value.length) {
          try {
            const cached = uni.getStorageSync("__stockList");
            if (Array.isArray(cached))
              baseList.value = cached;
            else if (typeof cached === "string" && cached)
              baseList.value = JSON.parse(cached);
          } catch {
          }
        }
        if (!baseList.value.length) {
          try {
            const fromState = history.state && history.state.__stockList;
            if (Array.isArray(fromState))
              baseList.value = fromState;
          } catch {
          }
        }
        if (!baseList.value.length) {
          try {
            const cached = localStorage.getItem("__stockList");
            if (cached)
              baseList.value = JSON.parse(cached);
          } catch {
          }
        }
      });
      const q2 = vue.ref("");
      function matchOne(val, k) {
        if (!val)
          return false;
        return String(val).toLowerCase().includes(k);
      }
      function matches(it2, k) {
        if (!k)
          return true;
        const s2 = k.toLowerCase();
        return matchOne(it2.name, s2) || matchOne(it2.title, s2) || matchOne(it2.remark, s2) || matchOne(it2.brand_name, s2) || matchOne(it2.type, s2) || matchOne(it2.size, s2) || matchOne(it2.size_detail, s2) || matchOne(it2.tags, s2) || matchOne(it2.goods_name, s2);
      }
      const filtered = vue.computed(() => {
        const key = q2.value.trim();
        if (!key)
          return baseList.value;
        return baseList.value.filter((it2) => matches(it2, key));
      });
      function getItemTitle(it2) {
        return it2.name || it2.title || it2.goods_name || "未命名物品";
      }
      function getItemSub(it2) {
        const brand = it2.brand_name ? `@${it2.brand_name}` : "";
        const size = [it2.size, it2.size_detail].filter(Boolean).join(" / ");
        const type = it2.type || "";
        return [type, size, brand].filter(Boolean).join(" · ");
      }
      const NO_IMG = "data:image/svg+xml;utf8," + encodeURIComponent(
        `<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400">
      <rect width="100%" height="100%" fill="#e9ebef"/>
      <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle"
        fill="#9aa0a6" font-size="40" font-family="Arial">No Image</text>
    </svg>`
      );
      function rawImg(it2) {
        var _a2, _b;
        if (it2.image)
          return it2.image;
        if (it2.image_url)
          return it2.image_url;
        if (it2.cover)
          return it2.cover;
        if (Array.isArray(it2.images) && it2.images.length) {
          return ((_a2 = it2.images[0]) == null ? void 0 : _a2.url) || ((_b = it2.images[0]) == null ? void 0 : _b.image_url) || it2.images[0];
        }
        return "";
      }
      function normalizeFirstImage(s2) {
        if (!s2)
          return "";
        const first = String(s2).split(",")[0].trim();
        if (!first)
          return "";
        const low = first.toLowerCase();
        if (low.includes("/default") || low.endsWith("default.png") || low.includes("noimage"))
          return "";
        return first;
      }
      function getDisplayImg(it2) {
        if (it2.__imgBroken)
          return NO_IMG;
        const src = normalizeFirstImage(rawImg(it2));
        return src || NO_IMG;
      }
      function onImgError(it2) {
        it2.__imgBroken = true;
      }
      function openPreview(it2) {
        const id = it2.id || it2.account_book_id || it2.AccountBookId || it2.book_id;
        if (!id) {
          uni.showToast({ title: "缺少ID", icon: "none" });
          return;
        }
        if (isPickMode.value) {
          const payload = {
            id,
            account_book_id: id,
            name: getItemTitle(it2),
            title: getItemTitle(it2),
            image: getDisplayImg(it2)
          };
          try {
            const ec = uni.getOpenerEventChannel && uni.getOpenerEventChannel();
            if (ec && ec.emit) {
              ec.emit("pickMyItemFromStock", payload);
            } else {
              uni.setStorageSync("__pickedMyItemFromStock", payload);
            }
          } catch (e2) {
            uni.setStorageSync("__pickedMyItemFromStock", payload);
          }
          uni.navigateBack({ delta: 1 });
          return;
        }
        const url = `/pkg-stock/account_book_preview/account_book_preview?account_book_id=${id}`;
        {
          uni.navigateTo({ url });
        }
      }
      function goBack() {
        try {
          const pages2 = getCurrentPages && getCurrentPages();
          if (pages2 && pages2.length > 1) {
            uni.navigateBack({ delta: 1 });
            return;
          }
        } catch {
        }
      }
      const noop = () => {
      };
      const __returned__ = { safeTop, safePadPx, baseList, isPickMode, isMP, capsulePad, q: q2, matchOne, matches, filtered, getItemTitle, getItemSub, NO_IMG, rawImg, normalizeFirstImage, getDisplayImg, onImgError, openPreview, goBack, noop, ref: vue.ref, computed: vue.computed, get onLoad() {
        return onLoad;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$p(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode(
          "view",
          {
            class: "wrap",
            style: vue.normalizeStyle({ "--safe-top": $setup.safeTop + "px" })
          },
          [
            vue.createElementVNode(
              "view",
              {
                class: "search-bar",
                style: vue.normalizeStyle({ paddingTop: $setup.safePadPx })
              },
              [
                vue.createElementVNode("view", { class: "bar" }, [
                  vue.createElementVNode("view", {
                    class: "back",
                    onClick: $setup.goBack
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "left",
                      size: "20",
                      color: "#666"
                    })
                  ]),
                  vue.createElementVNode("view", { class: "ipt-wrap" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "search",
                      size: "18",
                      color: "#999"
                    }),
                    vue.withDirectives(vue.createElementVNode(
                      "input",
                      {
                        class: "ipt",
                        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.q = $event),
                        type: "text",
                        placeholder: "搜索名称/备注/品牌/尺寸等",
                        focus: true,
                        "confirm-type": "search",
                        onConfirm: $setup.noop
                      },
                      null,
                      544
                      /* NEED_HYDRATION, NEED_PATCH */
                    ), [
                      [
                        vue.vModelText,
                        $setup.q,
                        void 0,
                        { trim: true }
                      ]
                    ])
                  ]),
                  vue.createElementVNode("view", {
                    class: "cancel font-alimamashuhei",
                    onClick: $setup.goBack
                  }, "取消"),
                  $setup.isMP ? (vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: 0,
                      class: "capsule-ph",
                      style: vue.normalizeStyle({ width: $setup.capsulePad + "px" })
                    },
                    null,
                    4
                    /* STYLE */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ],
              4
              /* STYLE */
            ),
            vue.createElementVNode("view", { class: "grid-list" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.filtered, (it2) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "grid-card",
                    key: it2.id || it2.__key || JSON.stringify(it2),
                    onClick: ($event) => $setup.openPreview(it2)
                  }, [
                    vue.createElementVNode("view", { class: "thumb" }, [
                      vue.createElementVNode("view", { class: "thumb-inner" }, [
                        vue.createElementVNode("image", {
                          class: "thumb-img",
                          src: $setup.getDisplayImg(it2),
                          mode: "aspectFill",
                          onError: ($event) => $setup.onImgError(it2)
                        }, null, 40, ["src", "onError"])
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "grid-info" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "grid-title ellipsis2 font-alimamashuhei" },
                        vue.toDisplayString($setup.getItemTitle(it2)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "grid-sub ellipsis1 font-title" },
                        vue.toDisplayString($setup.getItemSub(it2)),
                        1
                        /* TEXT */
                      )
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              )),
              $setup.filtered.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "empty"
              }, [
                vue.createElementVNode("image", {
                  class: "empty-icon",
                  src: _imports_0$d
                }),
                vue.createElementVNode("text", { class: "empty-title font-title" }, "没有匹配的物品"),
                vue.createElementVNode("text", { class: "empty-tip" }, "换个关键词试试～")
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ],
          4
          /* STYLE */
        )
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgStockStockMyitemsSearchStockMyitemsSearch = /* @__PURE__ */ _export_sfc(_sfc_main$q, [["render", _sfc_render$p], ["__scopeId", "data-v-023a4835"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-stock/stock-myitems-search/stock-myitems-search.vue"]]);
  const _sfc_main$p = {
    __name: "creator_base",
    setup(__props, { expose: __expose }) {
      __expose();
      const isShop = vue.ref(false);
      const isArtist2 = vue.ref(false);
      const isHairstylist2 = vue.ref(false);
      const brandData = vue.ref({
        brand_name: "加载中...",
        description: "",
        country_name: "",
        nickname_list: "",
        logo_image: "",
        type: "",
        goods_count: 0,
        views: 0,
        vote_number: 0,
        score: 0,
        is_brand: 0,
        is_bjd_artist: 0,
        is_bjd_hairstylist: 0
      });
      const brandName = vue.ref("");
      const selectedBrandId = vue.ref(0);
      const userInfo = vue.ref({});
      const hasBrand = vue.computed(() => {
        return userInfo.value.brand_id > 0;
      });
      const settleInfo = vue.computed(() => {
        if (!brandName.value)
          return "";
        let isBrand = selectedBrandId.value > 0 ? selectedBrandId.value : "尚未收录";
        return `我的UID是${userInfo.value.id}，我的品牌/名字是${brandName.value}，品牌ID是${isBrand}`;
      });
      let brandId = vue.ref(0);
      let updateInProgress = vue.ref(false);
      const loadBrandData = async () => {
        try {
          const user = await asyncGetUserInfo();
          userInfo.value = user;
          if (!user || !user.brand_id) {
            brandData.value.brand_id = 0;
            return;
          }
          brandId.value = user.brand_id;
          uni.request({
            url: `${websiteUrl.value}/brand-info?id=${user.brand_id}`,
            method: "GET",
            success: (res) => {
              if (res.data.status === "success") {
                brandData.value = res.data.data;
                isShop.value = res.data.data.is_brand === 1;
                isArtist2.value = res.data.data.is_bjd_artist === 1;
                isHairstylist2.value = res.data.data.is_bjd_hairstylist === 1;
              } else {
                uni.showToast({
                  title: res.data.msg || "获取品牌信息失败",
                  icon: "none"
                });
              }
            },
            fail: (err) => {
              formatAppLog("error", "at pkg-creator/creator_base/creator_base.vue:243", "请求失败:", err);
              uni.showToast({
                title: "网络请求失败",
                icon: "none"
              });
            }
          });
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/creator_base.vue:251", "加载品牌数据出错:", error2);
          uni.showToast({
            title: "加载数据出错",
            icon: "none"
          });
        }
      };
      const updateBrandIdentity = async (field, value) => {
        if (updateInProgress.value)
          return;
        updateInProgress.value = true;
        uni.showLoading({
          title: "更新中...",
          mask: true
        });
        try {
          const token = uni.getStorageSync("token");
          if (!token) {
            uni.showToast({
              title: "请先登录",
              icon: "none"
            });
            return;
          }
          const data = {};
          data[field] = value ? 1 : 0;
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/update-brand-identity`,
            method: "POST",
            header: {
              "Authorization": token,
              "Content-Type": "application/json"
            },
            data
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "更新成功",
              icon: "success"
            });
            brandData.value[field] = value ? 1 : 0;
          } else {
            uni.showToast({
              title: res.data.msg || "更新失败",
              icon: "none"
            });
            if (field === "is_brand")
              isShop.value = !value;
            if (field === "is_bjd_artist")
              isArtist2.value = !value;
            if (field === "is_bjd_hairstylist")
              isHairstylist2.value = !value;
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/creator_base.vue:309", "更新失败:", error2);
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        } finally {
          updateInProgress.value = false;
          uni.hideLoading();
        }
      };
      const toggleShop = (e2) => {
        const newValue = e2.detail.value;
        updateBrandIdentity("is_brand", newValue);
      };
      const toggleArtist = (e2) => {
        const newValue = e2.detail.value;
        updateBrandIdentity("is_bjd_artist", newValue);
      };
      const toggleHairstylist = (e2) => {
        const newValue = e2.detail.value;
        updateBrandIdentity("is_bjd_hairstylist", newValue);
      };
      const onCloseAssociate = (searchTerm) => {
        formatAppLog("log", "at pkg-creator/creator_base/creator_base.vue:338", searchTerm);
        brandName.value = searchTerm;
        selectedBrandId.value = 0;
      };
      const onBrandSelect = (id, name) => {
        selectedBrandId.value = id;
        brandName.value = name;
      };
      const copyContact = () => {
        uni.setClipboardData({
          data: "dogdogdoll",
          success: () => {
            uni.showToast({
              title: "已复制联系方式",
              icon: "success"
            });
          }
        });
      };
      const copySettleInfo = () => {
        if (!settleInfo.value) {
          uni.showToast({
            title: "请先选择品牌",
            icon: "none"
          });
          return;
        }
        uni.setClipboardData({
          data: settleInfo.value,
          success: () => {
            uni.showToast({
              title: "已复制入驻信息",
              icon: "success"
            });
          }
        });
      };
      const openLink = (url) => {
        if (!url)
          return;
        {
          window.open(url, "_blank");
        }
      };
      const navigateTo = (type) => {
        switch (type) {
          case "info-setting":
            uni.navigateTo({
              url: "/pkg-creator/creator_base/brand_edit/brand_edit"
            });
            break;
          case "goods-manage":
            uni.navigateTo({
              url: "/pkg-creator/creator_base/goods_list/goods_list"
            });
            break;
          case "image-manage":
            uni.navigateTo({
              url: "/pkg-creator/creator_base/news/news"
            });
            break;
          case "artist_info":
            uni.navigateTo({
              url: "/pkg-creator/creator_base/artist_setting/artist_setting"
            });
            break;
        }
      };
      vue.onMounted(() => {
        uni.setNavigationBarTitle({
          title: "我的品牌"
        });
        loadBrandData();
      });
      const __returned__ = { isShop, isArtist: isArtist2, isHairstylist: isHairstylist2, brandData, brandName, selectedBrandId, userInfo, hasBrand, settleInfo, get brandId() {
        return brandId;
      }, set brandId(v2) {
        brandId = v2;
      }, get updateInProgress() {
        return updateInProgress;
      }, set updateInProgress(v2) {
        updateInProgress = v2;
      }, loadBrandData, updateBrandIdentity, toggleShop, toggleArtist, toggleHairstylist, onCloseAssociate, onBrandSelect, copyContact, copySettleInfo, openLink, navigateTo, ref: vue.ref, onMounted: vue.onMounted, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$o(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_search = resolveEasycom(vue.resolveDynamicComponent("common-search"), __easycom_2$a);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      $setup.hasBrand ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
        vue.createElementVNode("view", { class: "header" }, [
          vue.createElementVNode("image", {
            class: "logo",
            src: $setup.brandData.logo_image,
            mode: "aspectFit"
          }, null, 8, ["src"]),
          vue.createElementVNode("view", { class: "brand-info" }, [
            vue.createElementVNode(
              "text",
              { class: "brand-name" },
              vue.toDisplayString($setup.brandData.brand_name),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "text",
              { class: "brand-type" },
              vue.toDisplayString($setup.brandData.type) + " · " + vue.toDisplayString($setup.brandData.country_name),
              1
              /* TEXT */
            )
          ])
        ]),
        vue.createElementVNode("view", { class: "card" }, [
          vue.createElementVNode("view", { class: "card-header" }, [
            vue.createElementVNode("text", { class: "title" }, "品牌信息"),
            vue.createElementVNode("view", { class: "score-box" }, [
              vue.createVNode(_component_uni_icons, {
                type: "star-filled",
                size: "16",
                color: "#FFC107"
              }),
              vue.createElementVNode(
                "text",
                { class: "score" },
                vue.toDisplayString($setup.brandData.score),
                1
                /* TEXT */
              )
            ])
          ]),
          vue.createElementVNode("view", { class: "info-item" }, [
            vue.createElementVNode("text", { class: "label" }, "名称："),
            vue.createElementVNode(
              "text",
              { class: "value" },
              vue.toDisplayString($setup.brandData.brand_name),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "info-item" }, [
            vue.createElementVNode("text", { class: "label" }, "简介："),
            vue.createElementVNode(
              "text",
              { class: "value" },
              vue.toDisplayString($setup.brandData.description),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "info-item" }, [
            vue.createElementVNode("text", { class: "label" }, "国籍："),
            vue.createElementVNode(
              "text",
              { class: "value" },
              vue.toDisplayString($setup.brandData.country_name),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "info-item" }, [
            vue.createElementVNode("text", { class: "label" }, "别名："),
            vue.createElementVNode(
              "text",
              { class: "value" },
              vue.toDisplayString($setup.brandData.nickname_list),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "info-item" }, [
            vue.createElementVNode("text", { class: "label" }, "身份："),
            vue.createElementVNode(
              "text",
              { class: "value" },
              vue.toDisplayString($setup.brandData.type),
              1
              /* TEXT */
            )
          ])
        ]),
        vue.createElementVNode("view", { class: "identity-section" }, [
          vue.createElementVNode("view", { class: "identity-item" }, [
            vue.createElementVNode("view", { class: "identity-title" }, "店铺身份"),
            vue.createElementVNode("switch", {
              checked: $setup.isShop,
              onChange: $setup.toggleShop,
              color: "#8fecff"
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "identity-status" },
              vue.toDisplayString($setup.isShop ? "我是店铺" : "不是店铺"),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "identity-item" }, [
            vue.createElementVNode("view", { class: "identity-title" }, "妆师身份"),
            vue.createElementVNode("switch", {
              checked: $setup.isArtist,
              onChange: $setup.toggleArtist,
              color: "#8fecff"
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "identity-status" },
              vue.toDisplayString($setup.isArtist ? "我是妆师" : "不是妆师"),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "identity-item" }, [
            vue.createElementVNode("view", { class: "identity-title" }, "毛娘身份"),
            vue.createElementVNode("switch", {
              checked: $setup.isHairstylist,
              onChange: $setup.toggleHairstylist,
              color: "#8fecff"
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "identity-status" },
              vue.toDisplayString($setup.isHairstylist ? "我是毛娘" : "不是毛娘"),
              1
              /* TEXT */
            )
          ])
        ]),
        vue.createElementVNode("view", { class: "action-buttons" }, [
          vue.createElementVNode("view", {
            class: "btn info-setting",
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.navigateTo("info-setting"))
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "contact",
              size: "24",
              color: "#006a57"
            }),
            vue.createElementVNode("text", null, "信息设置")
          ]),
          vue.createElementVNode("view", {
            class: "btn goods-manage",
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.navigateTo("goods-manage"))
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "shop",
              size: "24",
              color: "#dfd7c2"
            }),
            vue.createElementVNode("text", null, "商品管理")
          ]),
          vue.createElementVNode("view", {
            class: "btn image-manage",
            onClick: _cache[2] || (_cache[2] = ($event) => $setup.navigateTo("image-manage"))
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "image",
              size: "24",
              color: "#fff6e1"
            }),
            vue.createElementVNode("text", null, "文章管理")
          ]),
          vue.createElementVNode("view", {
            class: "btn info-artist",
            onClick: _cache[3] || (_cache[3] = ($event) => $setup.navigateTo("artist_info"))
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "contact",
              size: "24",
              color: "#c7e1fa"
            }),
            vue.createElementVNode("text", null, "妆师/毛娘")
          ])
        ]),
        vue.createElementVNode("view", { class: "info-section" }, [
          vue.createElementVNode("text", { class: "info-title" }, "功能说明"),
          vue.createElementVNode("text", { class: "info-text" }, "开启对应身份开关后，您的品牌名称将会出现在相关的搜索结果中，方便更多用户找到您。"),
          vue.createElementVNode("text", { class: "info-text" }, "您可以在「信息设置」中完善头像、品牌介绍及贩售渠道（仅限贩售类身份）。"),
          vue.createElementVNode("text", { class: "info-text" }, "「商品管理」用于创建和编辑您发布的商品信息。"),
          vue.createElementVNode("text", { class: "info-text" }, "「文章管理」用于发布图透、成品展示或宣传内容。"),
          vue.createElementVNode("text", { class: "info-text" }, "「妆师/毛娘」页面可编辑定制类服务（如妆面、手改毛）的接单信息。若为量产类手改毛或成品妆头，请在「商品管理」中创建；")
        ])
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "settle-container"
      }, [
        vue.createElementVNode("view", { class: "description" }, [
          vue.createElementVNode("text", { class: "desc-text" }, " 如果您是娃圈贩售方、BJD妆师或毛娘，并希望管理和展示自己的品牌信息，欢迎申请入驻。通过认证后，您可以发布商品词条、图透、妆图等相关内容。除特殊情况外，我们的官方账号会在小红书和微博每日定期汇总并发布当日的贩售信息；妆师与毛娘的开单信息也将提前进行集中同步。 "),
          vue.createElementVNode("text", { class: "desc-text" }, " 入驻认证流程如下： "),
          vue.createElementVNode("text", { class: "desc-text" }, " 1：添加官方客服微信 dogdogdoll，在下方输入或搜索自己的名称。复制并发送给客服微信 dogdogdoll； "),
          vue.createElementVNode("text", { class: "desc-text" }, " 2：使用您在小红书或微博的官方账号向我们的官方账号发送一条验证消息，即可完成认证； "),
          vue.createElementVNode("text", { class: "desc-text" }, " 入驻及代宣传服务均为无偿。后续的使用体验与意见，您可直接通过该微信号反馈给我们。 "),
          vue.createElementVNode("text", { class: "desc-text" }, " 此外，我们计划于明年推出线上交易平台服务，若您对此感兴趣，也欢迎提出宝贵建议。 ")
        ]),
        vue.createElementVNode("view", { class: "contact-section" }, [
          vue.createElementVNode("text", { class: "contact-label" }, "联系方式"),
          vue.createElementVNode("view", { class: "contact-content" }, [
            vue.createElementVNode("text", null, "dogdogdoll"),
            vue.createElementVNode("button", {
              class: "copy-btn",
              onClick: $setup.copyContact
            }, "点击复制")
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "form-group" }, [
            vue.createElementVNode("text", { class: "form-label" }, "品牌名称"),
            vue.createVNode(_component_common_search, {
              mode: "fill",
              onSelect: $setup.onBrandSelect,
              placeholder: "搜索并选择您的品牌/名字",
              "show-index-selector": "",
              onCloseAssociate: $setup.onCloseAssociate
            }),
            $setup.selectedBrandId ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 0,
                class: "brand-id"
              },
              "品牌ID: " + vue.toDisplayString($setup.selectedBrandId),
              1
              /* TEXT */
            )) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("text", { class: "desc-text" }, " 您的信息可能已经被录入，可以在尝试这里搜索您的品牌名称。需要在搜索框左边勾选【店铺】【毛娘】【妆师】身份后，才能搜索到您的名字。 ")
          ]),
          vue.createElementVNode("view", { class: "form-group" }, [
            vue.createElementVNode("text", { class: "form-label" }, "入驻信息"),
            vue.createElementVNode("textarea", {
              class: "info-textarea",
              value: $setup.settleInfo,
              disabled: "",
              placeholder: "请先选择品牌",
              "auto-height": ""
            }, null, 8, ["value"]),
            vue.createElementVNode("button", {
              class: "copy-btn",
              onClick: $setup.copySettleInfo
            }, "复制信息")
          ])
        ])
      ]))
    ]);
  }
  const PkgCreatorCreatorBaseCreatorBase = /* @__PURE__ */ _export_sfc(_sfc_main$p, [["render", _sfc_render$o], ["__scopeId", "data-v-cb774e31"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/creator_base.vue"]]);
  const _sfc_main$o = {
    __name: "brand_edit",
    setup(__props, { expose: __expose }) {
      __expose();
      const formData = vue.reactive({
        id: 0,
        brand_name: "",
        nickname_list: "",
        country_name: "",
        type: "",
        website_url: "",
        description: "",
        logo_image: ""
        // 新增LOGO字段
      });
      const brandData = vue.ref({});
      const typeOptions = vue.ref(["公司", "个人作者"]);
      const loading2 = vue.ref(false);
      const isFormValid = vue.computed(() => {
        var _a, _b, _c;
        return ((_a = formData.brand_name) == null ? void 0 : _a.trim()) !== "" && ((_b = formData.type) == null ? void 0 : _b.trim()) !== "" && ((_c = formData.website_url) == null ? void 0 : _c.trim()) !== "";
      });
      const onTypeChange = (e2) => {
        formData.type = typeOptions.value[e2.detail.value];
      };
      const fetchBrandInfo = async () => {
        try {
          loading2.value = true;
          const userInfo = await asyncGetUserInfo();
          formatAppLog("log", "at pkg-creator/creator_base/brand_edit/brand_edit.vue:155", userInfo);
          if (!userInfo || !userInfo.brand_id) {
            uni.showToast({
              title: "您尚未管理任何品牌",
              icon: "none"
            });
            return;
          }
          const brandId = userInfo == null ? void 0 : userInfo.brand_id;
          if (!brandId) {
            uni.showToast({
              title: "用户未关联品牌信息",
              icon: "none"
            });
            return;
          }
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-info?id=${brandId}`,
            method: "GET",
            header: {
              "Authorization": uni.getStorageSync("token")
            }
          });
          if (res.data.status === "success") {
            brandData.value = res.data.data;
            Object.assign(formData, {
              id: brandData.value.id,
              brand_name: brandData.value.brand_name || "",
              nickname_list: brandData.value.nickname_list || "",
              country_name: brandData.value.country_name || "",
              type: brandData.value.type || "",
              website_url: brandData.value.website_url || "",
              description: brandData.value.description || "",
              logo_image: brandData.value.logo_image || ""
            });
          } else {
            uni.showToast({
              title: "获取品牌信息失败: " + (res.data.msg || "未知错误"),
              icon: "none"
            });
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/brand_edit/brand_edit.vue:203", "获取品牌信息失败:", err);
          uni.showToast({
            title: "获取品牌信息失败，请重试",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const changeLogo = async () => {
        try {
          const tempFilePath = await chooseImage();
          uni.showLoading({
            title: "上传中...",
            mask: true
          });
          const qiniuTokenData = await getQiniuToken();
          if (!qiniuTokenData || !qiniuTokenData.path || !qiniuTokenData.token) {
            throw new Error("获取上传凭证失败: 缺少必要字段");
          }
          const fileName = qiniuTokenData.path;
          const result = await uploadImageToQiniu(
            tempFilePath,
            qiniuTokenData.token,
            fileName
          );
          if (result && result.imageUrl) {
            formData.logo_image = result.imageUrl;
            uni.showToast({
              title: "上传成功,保存生效",
              icon: "success"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/brand_edit/brand_edit.vue:249", "上传LOGO失败:", error2);
          uni.showToast({
            title: `上传LOGO失败: ${error2.message || error2}`,
            icon: "none",
            duration: 3e3
          });
        } finally {
          uni.hideLoading();
        }
      };
      const submitForm = async () => {
        if (!isFormValid.value)
          return;
        try {
          loading2.value = true;
          const submitData = {
            id: formData.id,
            brand_name: formData.brand_name,
            nickname_list: formData.nickname_list,
            country_name: formData.country_name,
            type: formData.type,
            website_url: formData.website_url,
            description: formData.description,
            logo_image: formData.logo_image
            // 新增LOGO字段
          };
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/update-brand`,
            method: "POST",
            header: {
              "Content-Type": "application/json",
              "Authorization": uni.getStorageSync("token")
            },
            data: submitData
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "已更新",
              icon: "success"
            });
            fetchBrandInfo();
          } else {
            uni.showToast({
              title: "更新失败: " + (res.data.msg || "未知错误"),
              icon: "none"
            });
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/brand_edit/brand_edit.vue:303", "提交失败:", err);
          uni.showToast({
            title: "提交失败，请重试",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      vue.onMounted(() => {
        uni.setNavigationBarTitle({
          title: "编辑品牌信息"
        });
        fetchBrandInfo();
      });
      const __returned__ = { formData, brandData, typeOptions, loading: loading2, isFormValid, onTypeChange, fetchBrandInfo, changeLogo, submitForm, ref: vue.ref, reactive: vue.reactive, onMounted: vue.onMounted, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      }, get chooseImage() {
        return chooseImage;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$n(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "brand-edit-container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "form-wrapper" }, [
        vue.createElementVNode("view", { class: "logo-editor" }, [
          vue.createElementVNode("view", { class: "preview-title" }, "品牌LOGO"),
          vue.createElementVNode("view", { class: "logo-preview" }, [
            vue.createElementVNode("image", {
              src: $setup.formData.logo_image || $setup.brandData.logo_image,
              mode: "aspectFit",
              class: "logo-image"
            }, null, 8, ["src"]),
            vue.createElementVNode("button", {
              class: "change-logo-btn",
              onClick: $setup.changeLogo
            }, "更换LOGO")
          ])
        ]),
        vue.createElementVNode("view", { class: "input-item" }, [
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              type: "text",
              placeholder: " ",
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.formData.brand_name = $event),
              maxlength: "20"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.brand_name]
          ]),
          vue.createElementVNode("label", { class: "floating-label" }, "品牌名称"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "compose",
            size: "18",
            color: "#999"
          })
        ]),
        $setup.formData.brand_name && $setup.brandData.brand_name_image ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "brand-preview"
        }, [
          vue.createElementVNode("view", { class: "preview-title" }, "效果预览"),
          vue.createElementVNode("view", { class: "preview-content" }, [
            vue.createElementVNode("image", {
              src: $setup.brandData.brand_name_image,
              mode: "widthFix",
              class: "preview-image"
            }, null, 8, ["src"])
          ])
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "hint-text" }, [
          vue.createElementVNode("text", null, "展示给用户的名称，尽量不要使用emoji，符号表情，我的程序会用艺术字体生成您品牌名称的粗体文字，含有特殊字符可能会导致渲染为【口】。"),
          vue.createElementVNode("text", null, "您的名称在程序中会进行中英文分词，以便用户更快速的搜索到您，字符顺序不对也可以兼容，例如：这些搜索都可以搜索到娃圈狗狗助手 WaQuanGouGou/wqgg/娃狗手/手狗 , 用户输入的命中词数决定了在搜索框中的排序权重。"),
          vue.createElementVNode("text", null, "但需要注意的是过长的名字并不会换行展示，而是压缩在一行里。")
        ]),
        vue.createElementVNode("view", { class: "input-item" }, [
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              type: "text",
              placeholder: " ",
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.formData.nickname_list = $event)
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.nickname_list]
          ]),
          vue.createElementVNode("label", { class: "floating-label" }, "品牌别名（多个用逗号分隔）"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "flag",
            size: "18",
            color: "#999"
          })
        ]),
        vue.createElementVNode("view", { class: "hint-text" }, [
          vue.createElementVNode("text", null, "您的其它称呼，我们也对别名进行了分词，用户同样可以通过别名查找到您，但别名的命中词权重低于名称，您可以在搜索框中实验搜索效果;"),
          vue.createElementVNode("text", null, "如果您的别称在圈内十分出名，我们建议放在名称后面。例如：娃圈狗狗助手（眠猫）")
        ]),
        vue.createElementVNode("view", { class: "input-item" }, [
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              type: "text",
              placeholder: " ",
              "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.formData.country_name = $event)
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.country_name]
          ]),
          vue.createElementVNode("label", { class: "floating-label" }, "国籍"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "location",
            size: "18",
            color: "#999"
          })
        ]),
        vue.createElementVNode("view", { class: "input-item" }, [
          vue.createElementVNode("picker", {
            value: $setup.typeOptions.indexOf($setup.formData.type),
            range: $setup.typeOptions,
            onChange: $setup.onTypeChange
          }, [
            vue.createElementVNode("view", { class: "picker-view" }, [
              vue.createElementVNode(
                "text",
                { class: "picker-value" },
                vue.toDisplayString($setup.formData.type || "请选择身份类型"),
                1
                /* TEXT */
              ),
              vue.createVNode(_component_uni_icons, {
                class: "picker-icon",
                type: "arrowdown",
                size: "16",
                color: "#999"
              })
            ])
          ], 40, ["value", "range"]),
          vue.createElementVNode("label", {
            class: "floating-label",
            style: { "top": "0px", "font-size": "14px" }
          }, "身份类型"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "person",
            size: "18",
            color: "#999"
          })
        ]),
        vue.createElementVNode("view", { class: "hint-text" }, [
          vue.createElementVNode("text", null, "向他人展示您的身份，可选公司或个人作者，个人作者意为个人自制。")
        ]),
        vue.createElementVNode("view", { class: "input-item" }, [
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              type: "text",
              placeholder: " ",
              "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.formData.website_url = $event)
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.website_url]
          ]),
          vue.createElementVNode("label", { class: "floating-label" }, "贩售渠道（URL）"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "link",
            size: "18",
            color: "#999"
          })
        ]),
        vue.createElementVNode("view", { class: "hint-text" }, [
          vue.createElementVNode("text", null, "淘宝店URL、微店URL等，请确认是一个能打开的地址。")
        ]),
        vue.createElementVNode("view", { class: "input-item textarea-item" }, [
          vue.withDirectives(vue.createElementVNode(
            "textarea",
            {
              class: "textarea",
              placeholder: " ",
              "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.formData.description = $event),
              maxlength: "200"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.description]
          ]),
          vue.createElementVNode("label", {
            class: "floating-label",
            style: { "top": "60rpx" }
          }, "品牌描述"),
          vue.createVNode(_component_uni_icons, {
            class: "input-icon",
            type: "chat",
            size: "18",
            color: "#999"
          })
        ]),
        vue.createElementVNode("button", {
          class: vue.normalizeClass(["submit-btn", { active: $setup.isFormValid }]),
          onClick: $setup.submitForm,
          disabled: !$setup.isFormValid || $setup.loading
        }, " 更新品牌信息 ", 10, ["disabled"]),
        $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "loading-overlay"
        }, [
          vue.createVNode(_component_uni_icons, {
            type: "spinner-cycle",
            size: "32",
            color: "#fff",
            class: "loading-icon"
          })
        ])) : vue.createCommentVNode("v-if", true)
      ])
    ]);
  }
  const PkgCreatorCreatorBaseBrandEditBrandEdit = /* @__PURE__ */ _export_sfc(_sfc_main$o, [["render", _sfc_render$n], ["__scopeId", "data-v-8504f3a4"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/brand_edit/brand_edit.vue"]]);
  const _sfc_main$n = {
    __name: "goods_list",
    setup(__props, { expose: __expose }) {
      __expose();
      const goodsList = vue.ref([]);
      const loading2 = vue.ref(true);
      const loadStatus2 = vue.ref("more");
      const error2 = vue.ref(null);
      const pageInfo = vue.ref(null);
      const currentPage = vue.ref(1);
      const typeCount = vue.ref({});
      const currentType = vue.ref("all");
      const allCount = vue.ref(0);
      const salesPopup = vue.ref(null);
      const salesList = vue.ref([]);
      const currentGoodsId = vue.ref(0);
      const showSaleForm = vue.ref(false);
      const saleTypeIndex = vue.ref(0);
      const currencyIndex = vue.ref(0);
      const saleTypeMap = {
        "限量预定": "限量预定",
        "限时预定": "限时预定",
        "限时限量预定": "限时限量预定",
        "限量现货": "限量现货",
        "现货": "现货"
      };
      const saleTypeOptions = ["限量预定", "限时预定", "限时限量预定", "限量现货", "现货"];
      const currencies = vue.ref([
        { value: "CNY", text: "人民币 (CNY)" },
        { value: "USD", text: "美元 (USD)" },
        { value: "JPY", text: "日元 (JPY)" },
        { value: "KRW", text: "韩元 (KRW)" }
      ]);
      const currencyOptions = vue.computed(() => currencies.value.map((i2) => i2.text));
      const subDate = vue.ref("");
      const subTime = vue.ref("");
      const endDate = vue.ref("");
      const endTime = vue.ref("");
      const currentSale = vue.ref({
        id: void 0,
        goods_id: 0,
        sale_type: "",
        sub_amount: "",
        fin_amount: "",
        sub_time: void 0,
        // 秒
        sub_time_end: void 0,
        // 秒
        currency: "CNY"
      });
      const fetchTypeCount = async () => {
        try {
          const token = uni.getStorageSync("token");
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-goods-count`,
            method: "GET",
            header: { Authorization: token },
            data: {
              brand_id: 1
              // TODO: 替换为真实品牌ID
            }
          });
          if (res.data.status === "success") {
            typeCount.value = res.data.data || {};
            let total = 0;
            Object.keys(typeCount.value).forEach((k) => total += Number(typeCount.value[k] || 0));
            allCount.value = total;
          } else {
            uni.showToast({ title: res.data.msg || "获取分类数量失败", icon: "none" });
          }
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_list/goods_list.vue:308", e2);
          uni.showToast({ title: "获取分类数量失败", icon: "none" });
        }
      };
      const jump2postGoods = () => {
        uni.navigateTo({ url: `/pkg-creator/creator_base/goods_editor/goods_editor` });
      };
      const previewGoods = (goodsId) => {
        uni.navigateTo({ url: `/pages/goods/goods?goods_id=${goodsId}` });
      };
      const editGoods = (goodsId) => {
        uni.navigateTo({ url: `/pkg-creator/creator_base/goods_editor/goods_editor?goods_id=${goodsId}` });
      };
      const openCollocation = (goodsId) => {
        uni.navigateTo({ url: `/pkg-creator/creator_base/collocation_list/collocation_list?goods_id=${goodsId}` });
      };
      const changeType = (type) => {
        currentType.value = type;
        currentPage.value = 1;
        goodsList.value = [];
        fetchGoodsList(1);
      };
      const fetchGoodsList = async (page = 1) => {
        try {
          if (page > 1)
            loadStatus2.value = "loading";
          else
            loading2.value = true;
          const token = uni.getStorageSync("token");
          if (!token) {
            error2.value = "未登录，请先登录";
            loading2.value = false;
            loadStatus2.value = "more";
            return;
          }
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-goods-list`,
            method: "GET",
            header: { Authorization: token },
            data: {
              page,
              page_size: 10,
              type: currentType.value === "all" ? "" : currentType.value
            }
          });
          const resp = res.data;
          if (resp.status === "success") {
            const newGoods = resp.data.goods_list || [];
            currentPage.value = page;
            goodsList.value = page === 1 ? newGoods : goodsList.value.concat(newGoods);
            pageInfo.value = {
              page_index: resp.data.page_index,
              page_size: resp.data.page_size,
              total: resp.data.total
            };
            const totalPages = Math.ceil(pageInfo.value.total / pageInfo.value.page_size);
            loadStatus2.value = pageInfo.value.page_index < totalPages ? "more" : "noMore";
          } else {
            error2.value = resp.msg || "获取商品列表失败";
            uni.showToast({ title: error2.value, icon: "none" });
            loadStatus2.value = "more";
          }
        } catch (e2) {
          error2.value = "网络请求失败";
          formatAppLog("error", "at pkg-creator/creator_base/goods_list/goods_list.vue:381", "获取商品列表失败:", e2);
          uni.showToast({ title: "网络请求失败", icon: "none" });
          loadStatus2.value = "more";
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (loadStatus2.value === "more")
          fetchGoodsList(currentPage.value + 1);
      };
      const openSalesPlan = async (goodsId) => {
        currentGoodsId.value = goodsId;
        await fetchSalesList(goodsId);
        salesPopup.value.open();
      };
      const closeSalesPopup = () => {
        salesPopup.value.close();
        resetSalesForm();
      };
      const fetchSalesList = async (goodsId) => {
        try {
          const token = uni.getStorageSync("token");
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/goods-sales`,
            method: "GET",
            header: { Authorization: token },
            data: { goods_id: goodsId }
          });
          if (res.data.status === "success") {
            salesList.value = res.data.data || [];
          } else {
            salesList.value = [];
            uni.showToast({ title: res.data.msg || "获取贩售计划失败", icon: "none" });
          }
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_list/goods_list.vue:420", "获取贩售计划失败:", e2);
          salesList.value = [];
          uni.showToast({ title: "获取贩售计划失败", icon: "none" });
        }
      };
      const addSale = () => {
        currentSale.value = {
          id: void 0,
          goods_id: currentGoodsId.value,
          sale_type: "",
          sub_amount: "",
          fin_amount: "",
          sub_time: void 0,
          sub_time_end: void 0,
          currency: "CNY"
        };
        subDate.value = "";
        subTime.value = "";
        endDate.value = "";
        endTime.value = "";
        showSaleForm.value = true;
      };
      const editSale = (sale) => {
        currentSale.value = {
          id: sale.id,
          goods_id: sale.goods_id,
          sale_type: sale.sale_type || "",
          sub_amount: sale.sub_amount ?? "",
          fin_amount: sale.fin_amount ?? "",
          sub_time: sale.sub_time,
          sub_time_end: sale.sub_time_end,
          currency: sale.currency || "CNY"
        };
        if (sale.sub_time && sale.sub_time > 0) {
          subDate.value = formatDateString(sale.sub_time);
          subTime.value = formatHourMinute(sale.sub_time);
        } else {
          subDate.value = "";
          subTime.value = "";
        }
        if (sale.sub_time_end && sale.sub_time_end > 0) {
          endDate.value = formatDateString(sale.sub_time_end);
          endTime.value = formatHourMinute(sale.sub_time_end);
        } else {
          endDate.value = "";
          endTime.value = "";
        }
        saleTypeIndex.value = Math.max(0, saleTypeOptions.indexOf(currentSale.value.sale_type));
        currencyIndex.value = Math.max(0, currencies.value.findIndex((c2) => c2.value === currentSale.value.currency));
        showSaleForm.value = true;
      };
      const deleteSale = async (saleId) => {
        uni.showModal({
          title: "删除确认",
          content: "确定要删除这条贩售计划吗？",
          success: async (res) => {
            if (res.confirm) {
              try {
                const token = uni.getStorageSync("token");
                const resp = await uni.request({
                  url: `${websiteUrl.value}/brand-manager/sale`,
                  method: "DELETE",
                  header: { Authorization: token },
                  data: { id: saleId }
                });
                if (resp.data.status === "success") {
                  uni.showToast({ title: "删除成功", icon: "success" });
                  await fetchSalesList(currentGoodsId.value);
                } else {
                  uni.showToast({ title: resp.data.msg || "删除失败", icon: "none" });
                }
              } catch (e2) {
                formatAppLog("error", "at pkg-creator/creator_base/goods_list/goods_list.vue:504", "删除贩售计划失败:", e2);
                uni.showToast({ title: "删除失败", icon: "none" });
              }
            }
          }
        });
      };
      const changeSaleType = (e2) => {
        const index = Number(e2.detail.value);
        saleTypeIndex.value = index;
        currentSale.value.sale_type = saleTypeOptions[index];
      };
      const changeCurrency = (e2) => {
        const index = Number(e2.detail.value);
        currencyIndex.value = index;
        currentSale.value.currency = currencies.value[index].value;
      };
      const onSubDateChange = (e2) => {
        subDate.value = e2.detail.value;
      };
      const onSubTimeChange = (e2) => {
        subTime.value = e2.detail.value;
      };
      const onEndDateChange = (e2) => {
        endDate.value = e2.detail.value;
      };
      const onEndTimeChange = (e2) => {
        endTime.value = e2.detail.value;
      };
      const saveSale = async () => {
        try {
          if (!currentSale.value.sale_type)
            return uni.showToast({ title: "请选择贩售模式", icon: "none" });
          if (currentSale.value.sub_amount === "" || isNaN(Number(currentSale.value.sub_amount))) {
            return uni.showToast({ title: "请输入定金金额", icon: "none" });
          }
          if (currentSale.value.fin_amount === "" || isNaN(Number(currentSale.value.fin_amount))) {
            return uni.showToast({ title: "请输入尾款金额", icon: "none" });
          }
          if (!currentSale.value.currency)
            return uni.showToast({ title: "请选择币种", icon: "none" });
          if (!subDate.value || !subTime.value) {
            return uni.showToast({ title: "请选择完整的开始时间", icon: "none" });
          }
          if (!endDate.value || !endTime.value) {
            return uni.showToast({ title: "请选择完整的预定/购买截止时间", icon: "none" });
          }
          const startSec = Math.floor((/* @__PURE__ */ new Date(`${subDate.value} ${subTime.value}`)).getTime() / 1e3);
          const endSec = Math.floor((/* @__PURE__ */ new Date(`${endDate.value} ${endTime.value}`)).getTime() / 1e3);
          const saleData = {
            ...currentSale.value.id ? { id: currentSale.value.id } : {},
            goods_id: Number(currentGoodsId.value),
            sale_type: currentSale.value.sale_type,
            sub_amount: Number(currentSale.value.sub_amount),
            fin_amount: Number(currentSale.value.fin_amount),
            currency: currentSale.value.currency,
            sub_time: Number(startSec),
            sub_time_end: Number(endSec)
          };
          const token = uni.getStorageSync("token");
          const isUpdate = !!currentSale.value.id;
          const url = isUpdate ? `${websiteUrl.value}/brand-manager/update-sales` : `${websiteUrl.value}/brand-manager/create-sales`;
          const resp = await uni.request({
            url,
            method: "POST",
            header: { Authorization: token, "Content-Type": "application/json" },
            data: saleData
          });
          if (resp.data.status === "success") {
            uni.showToast({ title: "保存成功", icon: "success" });
            await fetchSalesList(currentGoodsId.value);
            resetSalesForm();
          } else {
            uni.showToast({ title: resp.data.msg || "保存失败", icon: "none" });
          }
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_list/goods_list.vue:588", "保存贩售计划失败:", e2);
          uni.showToast({ title: "保存失败", icon: "none" });
        }
      };
      const resetSalesForm = () => {
        currentSale.value = {
          id: void 0,
          goods_id: 0,
          sale_type: "",
          sub_amount: "",
          fin_amount: "",
          sub_time: void 0,
          sub_time_end: void 0,
          currency: "CNY"
        };
        subDate.value = "";
        subTime.value = "";
        endDate.value = "";
        endTime.value = "";
        showSaleForm.value = false;
        saleTypeIndex.value = 0;
        currencyIndex.value = 0;
      };
      const formatTimeOnly = (date) => `${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}`;
      const formatHourMinute = (timestamp) => {
        if (!timestamp)
          return "";
        const d2 = new Date(timestamp * 1e3);
        return formatTimeOnly(d2);
      };
      const formatDateString = (timestamp) => {
        if (!timestamp)
          return "";
        const d2 = new Date(timestamp * 1e3);
        return `${d2.getFullYear()}-${(d2.getMonth() + 1).toString().padStart(2, "0")}-${d2.getDate().toString().padStart(2, "0")}`;
      };
      onShow(async () => {
        uni.setNavigationBarTitle({ title: "作品管理" });
        const userInfo = await asyncGetUserInfo();
        if (!userInfo) {
          uni.showToast({ title: "请先登录", icon: "none" });
          setTimeout(() => {
            uni.reLaunch({ url: "/pages/login/login" });
          }, 1500);
          return;
        }
        await fetchTypeCount();
        fetchGoodsList(1);
      });
      const __returned__ = { goodsList, loading: loading2, loadStatus: loadStatus2, error: error2, pageInfo, currentPage, typeCount, currentType, allCount, salesPopup, salesList, currentGoodsId, showSaleForm, saleTypeIndex, currencyIndex, saleTypeMap, saleTypeOptions, currencies, currencyOptions, subDate, subTime, endDate, endTime, currentSale, fetchTypeCount, jump2postGoods, previewGoods, editGoods, openCollocation, changeType, fetchGoodsList, loadMore, openSalesPlan, closeSalesPopup, fetchSalesList, addSale, editSale, deleteSale, changeSaleType, changeCurrency, onSubDateChange, onSubTimeChange, onEndDateChange, onEndTimeChange, saveSale, resetSalesForm, formatTimeOnly, formatHourMinute, formatDateString, ref: vue.ref, computed: vue.computed, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$m(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("scroll-view", {
        "scroll-x": "true",
        class: "category-scroll",
        "scroll-with-animation": true
      }, [
        vue.createElementVNode("view", { class: "category-list" }, [
          vue.createElementVNode(
            "view",
            {
              class: vue.normalizeClass(["category-item", { active: $setup.currentType === "all" }]),
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.changeType("all"))
            },
            [
              vue.createElementVNode("text", null, "全部"),
              vue.createElementVNode(
                "text",
                { class: "count" },
                "(" + vue.toDisplayString($setup.allCount || 0) + ")",
                1
                /* TEXT */
              )
            ],
            2
            /* CLASS */
          ),
          _ctx.e_type !== "all" ? (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            { key: 0 },
            vue.renderList($setup.typeCount, (count, e_type) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: e_type,
                class: vue.normalizeClass(["category-item", { active: $setup.currentType === e_type }]),
                onClick: ($event) => $setup.changeType(e_type)
              }, [
                vue.createElementVNode(
                  "text",
                  null,
                  vue.toDisplayString(e_type),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "count" },
                  "(" + vue.toDisplayString(count) + ")",
                  1
                  /* TEXT */
                )
              ], 10, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          )) : vue.createCommentVNode("v-if", true)
        ])
      ]),
      $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "loading"
      }, "加载中...")) : $setup.goodsList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "empty"
      }, "暂无商品")) : (vue.openBlock(), vue.createElementBlock(
        "scroll-view",
        {
          key: 2,
          "scroll-y": "true",
          class: "scroll-view",
          onScrolltolower: $setup.loadMore
        },
        [
          vue.createElementVNode("view", { class: "goods-list" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.goodsList, (item, index) => {
                return vue.openBlock(), vue.createElementBlock(
                  "view",
                  {
                    key: item.id,
                    class: vue.normalizeClass(["goods-item", { "last-item": index === $setup.goodsList.length - 1 }])
                  },
                  [
                    vue.createElementVNode("view", { class: "goods-main" }, [
                      vue.createElementVNode("view", { class: "image-container" }, [
                        vue.createElementVNode("image", {
                          class: "goods-image",
                          src: item.goods_images && item.goods_images[0] || "/static/default-goods.png",
                          mode: "aspectFill"
                        }, null, 8, ["src"])
                      ]),
                      vue.createElementVNode("view", { class: "goods-info" }, [
                        vue.createElementVNode("view", { class: "title-row" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "goods-name" },
                            vue.toDisplayString(item.name),
                            1
                            /* TEXT */
                          ),
                          item.is_hot ? (vue.openBlock(), vue.createElementBlock("view", {
                            key: 0,
                            class: "tag"
                          }, "热销")) : vue.createCommentVNode("v-if", true)
                        ]),
                        vue.createElementVNode("view", { class: "goods-details" }, [
                          vue.createElementVNode("view", { class: "detail-item" }, [
                            vue.createElementVNode("text", { class: "detail-label" }, "尺寸:"),
                            vue.createElementVNode(
                              "text",
                              { class: "detail-value" },
                              vue.toDisplayString(item.size || "无"),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "detail-item" }, [
                            vue.createElementVNode("text", { class: "detail-label" }, "肤色:"),
                            vue.createElementVNode(
                              "text",
                              { class: "detail-value" },
                              vue.toDisplayString(item.skin || "无"),
                              1
                              /* TEXT */
                            )
                          ])
                        ]),
                        vue.createElementVNode("view", { class: "price-container" }, [
                          vue.createElementVNode(
                            "text",
                            { class: "goods-price" },
                            "¥" + vue.toDisplayString(item.total_amount),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "goods-currency" },
                            vue.toDisplayString(item.currency),
                            1
                            /* TEXT */
                          )
                        ])
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "action-buttons-single-row" }, [
                      vue.createElementVNode("button", {
                        class: "btn preview",
                        onClick: ($event) => $setup.previewGoods(item.id)
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "eye",
                          size: "14",
                          color: "#3a3a3a"
                        }),
                        vue.createTextVNode(" 预览 ")
                      ], 8, ["onClick"]),
                      vue.createElementVNode("button", {
                        class: "btn edit",
                        onClick: ($event) => $setup.editGoods(item.id)
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "compose",
                          size: "14",
                          color: "#3a3a3a"
                        }),
                        vue.createTextVNode(" 编辑 ")
                      ], 8, ["onClick"]),
                      vue.createElementVNode("button", {
                        class: "btn collocation",
                        onClick: ($event) => $setup.openCollocation(item.id)
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "star",
                          size: "14",
                          color: "#3a3a3a"
                        }),
                        vue.createTextVNode(" 搭配参考 ")
                      ], 8, ["onClick"]),
                      vue.createElementVNode("button", {
                        class: "btn sales",
                        onClick: ($event) => $setup.openSalesPlan(item.id)
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "shop",
                          size: "14",
                          color: "#3a3a3a"
                        }),
                        vue.createTextVNode(" 贩售计划 ")
                      ], 8, ["onClick"])
                    ])
                  ],
                  2
                  /* CLASS */
                );
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]),
          vue.createElementVNode("view", { class: "load-more" }, [
            vue.createVNode(_component_uni_load_more, {
              status: $setup.loadStatus,
              contentText: {
                contentdown: "上拉加载更多",
                contentrefresh: "正在加载...",
                contentnomore: "没有更多了"
              }
            }, null, 8, ["status", "contentText"])
          ])
        ],
        32
        /* NEED_HYDRATION */
      )),
      vue.createVNode(
        _component_uni_popup,
        {
          ref: "salesPopup",
          type: "bottom",
          "background-color": "#fff"
        },
        {
          default: vue.withCtx(() => {
            var _a;
            return [
              vue.createElementVNode("view", { class: "sales-popup" }, [
                vue.createElementVNode("scroll-view", {
                  "scroll-y": "true",
                  class: "popup-scroll-view"
                }, [
                  vue.createElementVNode("view", { class: "popup-header" }, [
                    vue.createElementVNode("text", { class: "title" }, "贩售计划管理"),
                    vue.createVNode(_component_uni_icons, {
                      type: "close",
                      size: "20",
                      color: "#999",
                      onClick: $setup.closeSalesPopup
                    })
                  ]),
                  $setup.salesList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 0,
                    class: "sales-list"
                  }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList($setup.salesList, (sale) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          class: "sales-item",
                          key: sale.id
                        }, [
                          vue.createElementVNode("view", { class: "sale-info" }, [
                            vue.createElementVNode(
                              "text",
                              { class: "sale-type" },
                              vue.toDisplayString($setup.saleTypeMap[sale.sale_type] || sale.sale_type),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "sale-time" },
                              vue.toDisplayString($setup.formatDateString(sale.sub_time)) + " " + vue.toDisplayString($setup.formatHourMinute(sale.sub_time)) + " - " + vue.toDisplayString($setup.formatDateString(sale.sub_time_end)) + " " + vue.toDisplayString($setup.formatHourMinute(sale.sub_time_end)),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "sale-amount" },
                              "定金: ¥" + vue.toDisplayString(sale.sub_amount) + " 尾款: ¥" + vue.toDisplayString(sale.fin_amount),
                              1
                              /* TEXT */
                            )
                          ]),
                          vue.createElementVNode("view", { class: "sale-actions" }, [
                            vue.createVNode(_component_uni_icons, {
                              type: "compose",
                              size: "18",
                              color: "#666",
                              onClick: ($event) => $setup.editSale(sale)
                            }, null, 8, ["onClick"]),
                            vue.createVNode(_component_uni_icons, {
                              type: "trash",
                              size: "18",
                              color: "#f56c6c",
                              onClick: ($event) => $setup.deleteSale(sale.id)
                            }, null, 8, ["onClick"])
                          ])
                        ]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])) : (vue.openBlock(), vue.createElementBlock("view", {
                    key: 1,
                    class: "no-sales"
                  }, [
                    vue.createElementVNode("text", null, "暂无贩售计划")
                  ])),
                  vue.createElementVNode("button", {
                    class: "add-btn",
                    onClick: $setup.addSale
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "plus",
                      size: "16",
                      color: "#fff"
                    }),
                    vue.createTextVNode(" 添加贩售计划 ")
                  ]),
                  $setup.showSaleForm ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 2,
                    class: "sale-form"
                  }, [
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "贩售模式"),
                      vue.createElementVNode("picker", {
                        mode: "selector",
                        range: $setup.saleTypeOptions,
                        value: $setup.saleTypeIndex,
                        onChange: $setup.changeSaleType
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString($setup.saleTypeMap[$setup.currentSale.sale_type] || $setup.currentSale.sale_type || "请选择贩售模式"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["value"])
                    ]),
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "定金金额"),
                      vue.withDirectives(vue.createElementVNode(
                        "input",
                        {
                          type: "number",
                          "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.currentSale.sub_amount = $event),
                          placeholder: "请输入定金金额"
                        },
                        null,
                        512
                        /* NEED_PATCH */
                      ), [
                        [vue.vModelText, $setup.currentSale.sub_amount]
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "尾款金额"),
                      vue.withDirectives(vue.createElementVNode(
                        "input",
                        {
                          type: "number",
                          "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.currentSale.fin_amount = $event),
                          placeholder: "请输入尾款金额"
                        },
                        null,
                        512
                        /* NEED_PATCH */
                      ), [
                        [vue.vModelText, $setup.currentSale.fin_amount]
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "定金时间"),
                      vue.createElementVNode("picker", {
                        mode: "date",
                        value: $setup.subDate,
                        onChange: $setup.onSubDateChange
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString($setup.subDate || "选择日期"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["value"]),
                      vue.createElementVNode("picker", {
                        mode: "time",
                        value: $setup.subTime,
                        onChange: $setup.onSubTimeChange
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString($setup.subTime || "选择时间"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["value"])
                    ]),
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "预定/购买截止时间"),
                      vue.createElementVNode("picker", {
                        mode: "date",
                        value: $setup.endDate,
                        onChange: $setup.onEndDateChange
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString($setup.endDate || "选择日期"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["value"]),
                      vue.createElementVNode("picker", {
                        mode: "time",
                        value: $setup.endTime,
                        onChange: $setup.onEndTimeChange
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString($setup.endTime || "选择时间"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["value"])
                    ]),
                    vue.createElementVNode("view", { class: "form-item" }, [
                      vue.createElementVNode("text", { class: "label" }, "币种"),
                      vue.createElementVNode("picker", {
                        mode: "selector",
                        range: $setup.currencyOptions,
                        value: $setup.currencyIndex,
                        onChange: $setup.changeCurrency
                      }, [
                        vue.createElementVNode(
                          "view",
                          { class: "picker" },
                          vue.toDisplayString(((_a = $setup.currencies[$setup.currencyIndex]) == null ? void 0 : _a.value) || $setup.currentSale.currency || "请选择币种"),
                          1
                          /* TEXT */
                        )
                      ], 40, ["range", "value"])
                    ]),
                    vue.createElementVNode("view", { class: "form-buttons" }, [
                      vue.createElementVNode("button", {
                        class: "btn cancel",
                        onClick: _cache[3] || (_cache[3] = (...args) => _ctx.cancelForm && _ctx.cancelForm(...args))
                      }, "取消"),
                      vue.createElementVNode("button", {
                        class: "btn save",
                        onClick: $setup.saveSale
                      }, "保存")
                    ])
                  ])) : vue.createCommentVNode("v-if", true)
                ])
              ])
            ];
          }),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      ),
      vue.createElementVNode("view", {
        class: "floating-button",
        onClick: $setup.jump2postGoods
      }, [
        vue.createVNode(_component_uni_icons, {
          type: "plusempty",
          size: "30",
          color: "#fff"
        })
      ])
    ]);
  }
  const PkgCreatorCreatorBaseGoodsListGoodsList = /* @__PURE__ */ _export_sfc(_sfc_main$n, [["render", _sfc_render$m], ["__scopeId", "data-v-891cdff9"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/goods_list/goods_list.vue"]]);
  const _sfc_main$m = {
    __name: "cascade-multi-select",
    props: {
      show: {
        type: Boolean,
        default: false
      },
      sizeData: {
        type: Object,
        default: () => ({})
      },
      initialSelection: {
        type: Array,
        default: () => []
      }
    },
    emits: ["close", "confirm"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const activeCategory = vue.ref("");
      const selectedSizes = vue.ref([]);
      const mainCategories = vue.computed(() => {
        return Object.keys(props.sizeData).map((category) => ({
          label: category,
          value: category,
          count: props.sizeData[category].length
        }));
      });
      const currentSizes = vue.computed(() => {
        if (!activeCategory.value)
          return [];
        return props.sizeData[activeCategory.value] || [];
      });
      const resetSelection = () => {
        activeCategory.value = "";
        selectedSizes.value = [];
      };
      const closePopup = () => {
        emit("close");
      };
      const selectCategory = (category) => {
        activeCategory.value = category.value;
      };
      const toggleSelect = (size, category) => {
        const index = selectedSizes.value.findIndex(
          (item) => item.category === category && item.size === size
        );
        if (index >= 0) {
          selectedSizes.value.splice(index, 1);
        } else {
          selectedSizes.value.push({
            category,
            size
          });
        }
      };
      const isSelected = (size, category) => {
        return selectedSizes.value.some(
          (item) => item.category === category && item.size === size
        );
      };
      const confirmSelection = () => {
        emit("confirm", [...selectedSizes.value]);
        closePopup();
      };
      vue.watch(() => props.show, (newVal) => {
        if (newVal) {
          if (props.initialSelection && props.initialSelection.length > 0) {
            selectedSizes.value = [...props.initialSelection];
            if (props.initialSelection[0]) {
              activeCategory.value = props.initialSelection[0].category;
            }
          } else {
            resetSelection();
            if (mainCategories.value.length > 0) {
              activeCategory.value = mainCategories.value[0].value;
            }
          }
        }
      });
      const __returned__ = { props, emit, activeCategory, selectedSizes, mainCategories, currentSizes, resetSelection, closePopup, selectCategory, toggleSelect, isSelected, confirmSelection, ref: vue.ref, computed: vue.computed, watch: vue.watch };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$l(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    return vue.openBlock(), vue.createBlock(_component_bottom_popup, {
      show: $props.show,
      onClose: $setup.closePopup
    }, {
      default: vue.withCtx(() => [
        vue.createElementVNode("view", { class: "cascade-container" }, [
          vue.createElementVNode("view", { class: "header" }, [
            vue.createElementVNode("text", { class: "title" }, "选择尺寸"),
            vue.createElementVNode("view", { class: "actions" }, [
              vue.createElementVNode("text", {
                class: "action-text",
                onClick: $setup.resetSelection
              }, "重置"),
              vue.createVNode(_component_uni_icons, {
                type: "closeempty",
                size: "24",
                color: "#666",
                onClick: $setup.closePopup
              })
            ])
          ]),
          vue.createElementVNode("view", { class: "cascade-body" }, [
            vue.createElementVNode("scroll-view", {
              "scroll-y": "",
              class: "categories-container"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.mainCategories, (category, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: vue.normalizeClass(["category-item", { active: $setup.activeCategory === category.value }]),
                    onClick: ($event) => $setup.selectCategory(category)
                  }, [
                    vue.createElementVNode(
                      "text",
                      { class: "category-label" },
                      vue.toDisplayString(category.label),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "category-count" },
                      vue.toDisplayString(category.count) + "个",
                      1
                      /* TEXT */
                    )
                  ], 10, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            vue.createElementVNode("scroll-view", {
              "scroll-y": "",
              class: "sizes-container"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.currentSizes, (size, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: vue.normalizeClass(["size-item", { selected: $setup.isSelected(size, $setup.activeCategory) }]),
                    onClick: ($event) => $setup.toggleSelect(size, $setup.activeCategory)
                  }, [
                    vue.createElementVNode(
                      "text",
                      { class: "size-label" },
                      vue.toDisplayString(size),
                      1
                      /* TEXT */
                    ),
                    $setup.isSelected(size, $setup.activeCategory) ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                      key: 0,
                      type: "checkmarkempty",
                      size: "16",
                      color: "#65c6d9"
                    })) : vue.createCommentVNode("v-if", true)
                  ], 10, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ]),
          vue.createElementVNode("view", { class: "footer" }, [
            vue.createElementVNode(
              "text",
              { class: "selected-count" },
              "已选: " + vue.toDisplayString($setup.selectedSizes.length) + "个",
              1
              /* TEXT */
            ),
            vue.createElementVNode("button", {
              class: "confirm-btn",
              onClick: $setup.confirmSelection
            }, "确定")
          ])
        ])
      ]),
      _: 1
      /* STABLE */
    }, 8, ["show"]);
  }
  const __easycom_2$1 = /* @__PURE__ */ _export_sfc(_sfc_main$m, [["render", _sfc_render$l], ["__scopeId", "data-v-1f538290"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/cascade-multi-select/cascade-multi-select.vue"]]);
  const _sfc_main$l = {
    __name: "goods_editor",
    props: {
      goods_id: {
        type: String
      }
    },
    setup(__props, { expose: __expose }) {
      __expose();
      const props = __props;
      const isEditMode = vue.computed(() => {
        return props.goods_id && props.goods_id !== "0" && props.goods_id !== "new";
      });
      const goodsInfo = vue.ref({
        id: 0,
        name: "",
        brand_id: 0,
        brand_name: "",
        total_amount: 0,
        currency: "CNY",
        size: "",
        size_detail: "",
        skin: "",
        type: "",
        desc_content: "",
        doll_material: "",
        goods_images: [],
        sizes: []
        // 尺寸数组
      });
      const showDelete = vue.ref(true);
      const sizeData = vue.ref([]);
      const sizeOptions = vue.ref([]);
      const selectedSizePath = vue.ref([]);
      const showSizeSelector = vue.ref(false);
      const sizeSelector = vue.ref(null);
      const typeOptions = vue.ref([]);
      const typeIndex = vue.ref(-1);
      const currencyOptions = vue.ref([]);
      const currencyIndex = vue.ref(-1);
      const currencyMap = vue.ref({});
      const materialOptions = vue.ref([]);
      const materialIndex = vue.ref(-1);
      const skinQuickOptions = vue.ref(["白肌", "浅烧", "牡丹白", "奶油", "半白", "粉白", "普肌", "粉普", "烧肌"]);
      const showSkinQuickSelector = vue.computed(() => {
        return ["整体", "单体", "单头"].includes(goodsInfo.value.type);
      });
      const initialSizeSelection = vue.ref([]);
      const selectedSizeDisplay = vue.computed(() => {
        if (!goodsInfo.value.sizes || !Array.isArray(goodsInfo.value.sizes)) {
          return [];
        }
        return goodsInfo.value.sizes.map((item) => {
          return `${item.goods_size}/${item.size_detail}`;
        });
      });
      const openSizeSelector = () => {
        initialSizeSelection.value = (goodsInfo.value.sizes || []).map((item) => ({
          category: item.goods_size,
          size: item.size_detail
        }));
        showSizeSelector.value = true;
      };
      const handleSizeSelection = (sizes) => {
        goodsInfo.value.sizes = sizes.map((item) => ({
          goods_size: item.category,
          size_detail: item.size
        }));
      };
      const fetchGoodsInfo = async () => {
        try {
          if (!isEditMode.value) {
            const userInfo = uni.getStorageSync("userInfo");
            if (userInfo && userInfo.brand) {
              goodsInfo.value.brand_id = userInfo.brand.id;
              goodsInfo.value.brand_name = userInfo.brand.name;
            }
            return;
          }
          uni.showLoading({
            title: "加载中..."
          });
          const token = uni.getStorageSync("token");
          if (!token) {
            error.value = "未登录，请先登录";
            loading.value = false;
            loadStatus.value = "more";
            return;
          }
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-goods-info-by-id`,
            method: "GET",
            data: {
              id: props.goods_id
            },
            header: {
              Authorization: token
            }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            goodsInfo.value = {
              ...data,
              // 确保 sizes 数组存在
              sizes: data.sizes || [],
              goods_images: data.goods_images.map((img) => ({
                id: img.id,
                src: img.url,
                name: "",
                price: "",
                type: ""
              }))
            };
            typeIndex.value = typeOptions.value.indexOf(goodsInfo.value.type);
            currencyIndex.value = currencyOptions.value.findIndex(
              (item) => item === goodsInfo.value.currency
            );
            materialIndex.value = materialOptions.value.indexOf(
              goodsInfo.value.doll_material
            );
          } else {
            uni.showToast({
              title: res.data.msg,
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:305", "获取商品信息失败", error2);
          uni.showToast({
            title: "获取商品信息失败",
            icon: "none"
          });
        } finally {
          uni.hideLoading();
        }
      };
      const fetchSizes = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/sizes`,
            method: "GET"
          });
          if (res.data.status === "success") {
            sizeData.value = res.data.data;
            const formattedSizes = [];
            for (const [category, items] of Object.entries(sizeData.value)) {
              formattedSizes.push({
                text: category,
                value: category,
                children: items.map((item) => ({
                  text: item,
                  value: item
                }))
              });
            }
            sizeOptions.value = formattedSizes;
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:342", "获取尺寸数据失败:", err);
          uni.showToast({
            title: "获取尺寸数据失败",
            icon: "none"
          });
        }
      };
      const fetchTypes = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/goods-types`,
            method: "GET"
          });
          if (res.data.status === "success") {
            typeOptions.value = res.data.data;
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:362", "获取类型数据失败:", err);
          uni.showToast({
            title: "获取类型数据失败",
            icon: "none"
          });
        }
      };
      const fetchCurrencies = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/currency`,
            method: "GET"
          });
          if (res.data.status === "success") {
            currencyMap.value = res.data.data;
            currencyOptions.value = Object.keys(res.data.data);
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:383", "获取货币数据失败:", err);
          uni.showToast({
            title: "获取货币数据失败",
            icon: "none"
          });
        }
      };
      const handleItemClick = (item) => {
        formatAppLog("log", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:392", "点击了图片", item);
        const urls = goodsInfo.value.goods_images.map((img) => img.src);
        const currentIndex = goodsInfo.value.goods_images.findIndex((img) => img.id === item.id);
        uni.previewImage({
          urls,
          // 所有图片的 URL 列表
          current: currentIndex >= 0 ? currentIndex : 0,
          // 当前点击图片的索引
          indicator: "default",
          // 显示指示器
          loop: true
          // 支持循环预览
        });
        return false;
      };
      const fetchMaterials = async () => {
        try {
          let url = `${websiteUrl.value}/materials`;
          if (goodsInfo.value.type === "假发") {
            url = `${websiteUrl.value}/hair-materials`;
          }
          const res = await uni.request({
            url,
            method: "GET"
          });
          if (res.data.status === "success") {
            materialOptions.value = res.data.data;
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:428", "获取材质数据失败:", err);
          uni.showToast({
            title: "获取材质数据失败",
            icon: "none"
          });
        }
      };
      const onSizeChange = (e2) => {
        const nodes = e2.detail.value;
        if (nodes.length === 2) {
          goodsInfo.value.size = nodes[0].value;
          goodsInfo.value.size_detail = nodes[1].value;
        } else {
          goodsInfo.value.size = "";
          goodsInfo.value.size_detail = "";
        }
      };
      const updateType = (e2) => {
        const index = e2.detail.value;
        typeIndex.value = index;
        goodsInfo.value.type = typeOptions.value[index];
        fetchMaterials();
      };
      const updateCurrency = (e2) => {
        const index = e2.detail.value;
        currencyIndex.value = index;
        goodsInfo.value.currency = currencyOptions.value[index];
      };
      const updateMaterial = (e2) => {
        const index = e2.detail.value;
        materialIndex.value = index;
        goodsInfo.value.doll_material = materialOptions.value[index];
      };
      const addSkin = (skin) => {
        if (goodsInfo.value.skin) {
          goodsInfo.value.skin += ` ${skin}`;
        } else {
          goodsInfo.value.skin = skin;
        }
      };
      const handleImageSortChange = (sortedIds) => {
        const sortedImages = [];
        sortedIds.forEach((id) => {
          const image = goodsInfo.value.goods_images.find((img) => img.id === id);
          if (image)
            sortedImages.push(image);
        });
        goodsInfo.value.goods_images = sortedImages;
      };
      const saveGoods = async () => {
        try {
          uni.showLoading({
            title: "保存中..."
          });
          if (!goodsInfo.value.name) {
            uni.showToast({
              title: "请填写商品名称",
              icon: "none"
            });
            return;
          }
          if (!goodsInfo.value.type) {
            uni.showToast({
              title: "请选择商品类型",
              icon: "none"
            });
            return;
          }
          if (!goodsInfo.value.goods_images || goodsInfo.value.goods_images.length === 0) {
            uni.showToast({
              title: "请至少上传一张商品图片",
              icon: "none"
            });
            return;
          }
          const submitData = {
            ...goodsInfo.value,
            // 转换图片格式为后端需要的格式
            goods_images: goodsInfo.value.goods_images.map((img) => ({
              id: img.id,
              url: img.src
            })),
            // 尺寸数据已经是后端需要的格式
            sizes: goodsInfo.value.sizes,
            total_amount: parseInt(goodsInfo.value.total_amount)
          };
          const token = uni.getStorageSync("token");
          if (!token) {
            error.value = "未登录，请先登录";
            loading.value = false;
            loadStatus.value = "more";
            return;
          }
          const apiUrl = isEditMode.value ? `${websiteUrl.value}/brand-manager/update-goods` : `${websiteUrl.value}/brand-manager/add-goods`;
          const method = isEditMode.value ? "POST" : "POST";
          const res = await uni.request({
            url: apiUrl,
            method,
            data: submitData,
            header: {
              Authorization: token
            }
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: isEditMode.value ? "保存成功" : "添加成功"
            });
            setTimeout(() => {
              uni.navigateBack();
            }, 1500);
          } else {
            uni.showToast({
              title: res.data.msg || (isEditMode.value ? "保存失败" : "添加失败"),
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:578", "操作失败", error2);
          uni.showToast({
            title: isEditMode.value ? "保存失败" : "添加失败",
            icon: "none"
          });
        } finally {
          uni.hideLoading();
        }
      };
      const addImages = async () => {
        try {
          const remaining = 9 - goodsInfo.value.goods_images.length;
          if (remaining <= 0) {
            uni.showToast({
              title: "最多只能添加9张图片",
              icon: "none"
            });
            return;
          }
          const tempFilePaths = await chooseImageList(remaining);
          uni.showLoading({
            title: "上传中...",
            mask: true
          });
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              uni.showLoading({
                title: `上传中 (${i2 + 1}/${tempFilePaths.length})`,
                mask: true
              });
              const qiniuTokenData = await getQiniuToken();
              if (!qiniuTokenData || !qiniuTokenData.path || !qiniuTokenData.token) {
                throw new Error("获取上传凭证失败: 缺少必要字段");
              }
              const fileName = qiniuTokenData.path;
              const result = await uploadImageToQiniu(
                tempFilePaths[i2],
                qiniuTokenData.token,
                fileName
              );
              if (result && result.imageUrl) {
                goodsInfo.value.goods_images.push({
                  id: Date.now() + i2,
                  // 临时ID
                  src: result.imageUrl,
                  name: "",
                  price: "",
                  type: ""
                });
              }
            } catch (error2) {
              formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:648", `第 ${i2 + 1} 张图片上传失败:`, error2);
              uni.showToast({
                title: `第 ${i2 + 1} 张图片上传失败: ${error2.message || error2}`,
                icon: "none",
                duration: 3e3
              });
            }
          }
          uni.hideLoading();
          uni.showToast({
            title: `成功添加${tempFilePaths.length}张图片`,
            icon: "success"
          });
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/goods_editor/goods_editor.vue:663", "添加图片失败", error2);
          uni.hideLoading();
          uni.showToast({
            title: `添加图片失败: ${error2.message || error2}`,
            icon: "none",
            duration: 3e3
          });
        }
      };
      const handleDeleteImage = (imageId) => {
        uni.showModal({
          title: "删除确认",
          content: "确定要删除这张图片吗？",
          confirmText: "删除",
          confirmColor: "#ff3b30",
          success: (res) => {
            if (res.confirm) {
              goodsInfo.value.goods_images = goodsInfo.value.goods_images.filter(
                (img) => img.id !== imageId
              );
              uni.showToast({
                title: "图片已删除",
                icon: "success"
              });
            }
          }
        });
      };
      vue.onMounted(async () => {
        uni.setNavigationBarTitle({
          title: isEditMode.value ? "编辑商品信息" : "添加新商品"
        });
        await fetchSizes();
        await fetchTypes();
        await fetchCurrencies();
        await fetchGoodsInfo();
        await fetchMaterials();
      });
      const __returned__ = { props, isEditMode, goodsInfo, showDelete, sizeData, sizeOptions, selectedSizePath, showSizeSelector, sizeSelector, typeOptions, typeIndex, currencyOptions, currencyIndex, currencyMap, materialOptions, materialIndex, skinQuickOptions, showSkinQuickSelector, initialSizeSelection, selectedSizeDisplay, openSizeSelector, handleSizeSelection, fetchGoodsInfo, fetchSizes, fetchTypes, fetchCurrencies, handleItemClick, fetchMaterials, onSizeChange, updateType, updateCurrency, updateMaterial, addSkin, handleImageSortChange, saveGoods, addImages, handleDeleteImage, ref: vue.ref, onMounted: vue.onMounted, computed: vue.computed, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$k(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_cascade_multi_select = resolveEasycom(vue.resolveDynamicComponent("cascade-multi-select"), __easycom_2$1);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_shmily_drag_image = resolveEasycom(vue.resolveDynamicComponent("shmily-drag-image"), __easycom_3$6);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("scroll-view", {
        "scroll-y": "",
        class: "form-container"
      }, [
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "商品名称"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.goodsInfo.name = $event),
              placeholder: "请输入商品名称",
              class: "input"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.goodsInfo.name]
          ])
        ]),
        $setup.goodsInfo.brand_name ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "品牌"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.goodsInfo.brand_name = $event),
              disabled: "",
              class: "input disabled"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.goodsInfo.brand_name]
          ])
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "价格"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.goodsInfo.total_amount = $event),
              type: "number",
              placeholder: "请输入价格",
              class: "input"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.goodsInfo.total_amount]
          ]),
          vue.createElementVNode("picker", {
            class: "currency-picker",
            onChange: $setup.updateCurrency,
            value: $setup.currencyIndex,
            range: $setup.currencyOptions
          }, [
            vue.createElementVNode(
              "view",
              { class: "currency" },
              vue.toDisplayString($setup.goodsInfo.currency),
              1
              /* TEXT */
            )
          ], 40, ["value", "range"]),
          vue.createElementVNode("text", { class: "hint" }, "暂不支持小数点，如有加购项、不同售价，可以写在描述里")
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "尺寸"),
          vue.createElementVNode("view", {
            class: "selected-sizes",
            onClick: $setup.openSizeSelector
          }, [
            $setup.selectedSizeDisplay.length === 0 ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "选择尺寸")) : (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "size-tags"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.selectedSizeDisplay, (size, index) => {
                  return vue.openBlock(), vue.createElementBlock(
                    "view",
                    {
                      key: index,
                      class: "size-tag"
                    },
                    vue.toDisplayString(size),
                    1
                    /* TEXT */
                  );
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])),
            vue.createElementVNode("text", { class: "hint" }, "可以多选您支持的尺寸，可以选择多个分类；我们推荐不同的大分类建多个词条，并在名称后面加入（x分）的描述。")
          ]),
          vue.createVNode(_component_cascade_multi_select, {
            ref: "sizeSelector",
            show: $setup.showSizeSelector,
            sizeData: $setup.sizeData,
            initialSelection: $setup.initialSizeSelection,
            onClose: _cache[3] || (_cache[3] = ($event) => $setup.showSizeSelector = false),
            onConfirm: $setup.handleSizeSelection
          }, null, 8, ["show", "sizeData", "initialSelection"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "类型"),
          vue.createElementVNode("picker", {
            onChange: $setup.updateType,
            value: $setup.typeIndex,
            range: $setup.typeOptions
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker-content" },
              vue.toDisplayString($setup.goodsInfo.type || "请选择类型"),
              1
              /* TEXT */
            )
          ], 40, ["value", "range"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "肤色"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.goodsInfo.skin = $event),
              placeholder: "请输入肤色",
              class: "input"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.goodsInfo.skin]
          ]),
          $setup.showSkinQuickSelector ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "skin-quick-selector"
          }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.skinQuickOptions, (skin, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: "skin-tag",
                  onClick: ($event) => $setup.addSkin(skin)
                }, vue.toDisplayString(skin), 9, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])) : vue.createCommentVNode("v-if", true)
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "材质"),
          vue.createElementVNode("picker", {
            onChange: $setup.updateMaterial,
            value: $setup.materialIndex,
            range: $setup.materialOptions
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker-content" },
              vue.toDisplayString($setup.goodsInfo.doll_material || "请选择材质"),
              1
              /* TEXT */
            )
          ], 40, ["value", "range"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "描述"),
          vue.withDirectives(vue.createElementVNode(
            "textarea",
            {
              "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.goodsInfo.desc_content = $event),
              placeholder: "请输入商品描述",
              class: "textarea",
              "auto-height": ""
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.goodsInfo.desc_content]
          ]),
          vue.createElementVNode("text", { class: "hint" }, "可以填写加购项目或不同肤色的价格。")
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "商品图片"),
          vue.createElementVNode(
            "text",
            { class: "hint" },
            "长按图片可拖拽排序" + vue.toDisplayString($setup.showDelete ? "，点击图片右上角可删除。" : ""),
            1
            /* TEXT */
          ),
          vue.createElementVNode("text", { class: "hint" }, "为了保持版面美观， 请将最好看的一张图放在第一张，第一张不要用大字报。为了保证用户最佳观看效果，请不要上传过大图片和长宽比效果不佳的图片（3:4最优)。 "),
          vue.createElementVNode("text", { class: "hint" }, "同时为了保护平台数据安全，我们会在一段时间后自动添加水印，但您仍然能看到无水印原图。"),
          $setup.goodsInfo.goods_images.length < 9 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "add-image-btn",
            onClick: $setup.addImages
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "plusempty",
              size: "30",
              color: "#999"
            }),
            vue.createElementVNode("text", null, "添加图片")
          ])) : vue.createCommentVNode("v-if", true)
        ]),
        vue.createElementVNode("view", { class: "image-edit-container" }, [
          vue.createVNode(_component_shmily_drag_image, {
            modelValue: $setup.goodsInfo.goods_images,
            "onUpdate:modelValue": _cache[6] || (_cache[6] = ($event) => $setup.goodsInfo.goods_images = $event),
            cols: 3,
            showItemInfo: false,
            itemMargin: 10,
            showDelete: true,
            "border-radius": "0",
            onSortChange: $setup.handleImageSortChange,
            onItemClick: $setup.handleItemClick,
            "custom-click": true,
            onDelete: $setup.handleDeleteImage,
            keyName: "src"
          }, null, 8, ["modelValue"])
        ])
      ]),
      vue.createElementVNode("view", { class: "footer" }, [
        vue.createElementVNode(
          "button",
          {
            class: "btn save-btn",
            onClick: $setup.saveGoods
          },
          vue.toDisplayString($setup.isEditMode ? "保存修改" : "添加商品"),
          1
          /* TEXT */
        )
      ])
    ]);
  }
  const PkgCreatorCreatorBaseGoodsEditorGoodsEditor = /* @__PURE__ */ _export_sfc(_sfc_main$l, [["render", _sfc_render$k], ["__scopeId", "data-v-b1d54af3"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/goods_editor/goods_editor.vue"]]);
  const _imports_2$1 = "/static/empty.png";
  const _sfc_main$k = {
    __name: "collocation_list",
    setup(__props, { expose: __expose }) {
      __expose();
      const goodsId = vue.ref("");
      const collocationList = vue.ref([]);
      const loading2 = vue.ref(true);
      const goodsInfo = vue.ref(null);
      const fetchCollocations = async () => {
        var _a, _b;
        if (!goodsId.value) {
          uni.showToast({ title: "商品ID缺失", icon: "none" });
          return;
        }
        try {
          loading2.value = true;
          const res = await uni.request({
            url: `${websiteUrl.value}/goods-collocation`,
            method: "GET",
            data: { goods_id: goodsId.value, official: true }
          });
          if (((_a = res.data) == null ? void 0 : _a.status) === "success") {
            const data = res.data.data || {};
            const list = data.collocation_relation_list || [];
            collocationList.value = list.map((item) => ({
              ...item,
              imageList: (item.image_urls || "").split(",").map((s2) => s2.trim()).filter(Boolean)
            }));
          } else {
            throw new Error(((_b = res.data) == null ? void 0 : _b.msg) || "获取搭配数据失败");
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/collocation_list/collocation_list.vue:112", "获取搭配数据失败:", err);
          uni.showToast({ title: err.message || "请求失败", icon: "none" });
        } finally {
          loading2.value = false;
        }
      };
      const previewImage = (urls, index) => {
        uni.previewImage({ current: index, urls });
      };
      const goToPreview = (collocationId) => {
        uni.navigateTo({
          url: `/pages/collocation_share/collocation_share?collocation_id=${encodeURIComponent(collocationId)}&origin=1`
        });
      };
      const handleAddCollocation = async () => {
        var _a;
        if (!goodsInfo.value && goodsId.value) {
          try {
            const res = await uni.request({
              url: `${websiteUrl.value}/goods`,
              method: "GET",
              data: { id: goodsId.value }
            });
            if (((_a = res.data) == null ? void 0 : _a.status) === "success") {
              goodsInfo.value = res.data.data;
            }
          } catch (e2) {
            formatAppLog("error", "at pkg-creator/creator_base/collocation_list/collocation_list.vue:144", "获取商品信息失败:", e2);
          }
        }
        const queryParams = {
          goods_id: goodsId.value,
          ...goodsInfo.value ? {
            brand_id: goodsInfo.value.brand_id,
            goods_name: goodsInfo.value.name,
            brand_name: goodsInfo.value.brand_name,
            type: goodsInfo.value.type
          } : {}
        };
        const queryString = Object.entries(queryParams).map(([k, v2]) => `${k}=${v2 ?? ""}`).join("&");
        uni.navigateTo({ url: `/pages/collocation/collocation?${queryString}` });
      };
      const handleEdit = (collocationId) => {
        uni.navigateTo({
          url: `/pages/collocation/collocation?collocation_id=${encodeURIComponent(collocationId)}`
        });
      };
      const handleDelete = (item) => {
        uni.showModal({
          title: "确认删除",
          content: "确定要删除这个搭配参考吗？",
          success: async (r2) => {
            var _a, _b;
            if (!r2.confirm)
              return;
            try {
              const token = uni.getStorageSync("token");
              const res = await uni.request({
                url: `${websiteUrl.value}/with-state/delete-collocation?collocation_id=${encodeURIComponent(item.collocation_id)}`,
                method: "POST",
                header: { Authorization: token },
                data: { collocation_id: item.collocation_id }
              });
              if (((_a = res.data) == null ? void 0 : _a.status) === "success") {
                uni.showToast({ title: "删除成功" });
                fetchCollocations();
              } else {
                throw new Error(((_b = res.data) == null ? void 0 : _b.msg) || "删除失败");
              }
            } catch (e2) {
              uni.showToast({ title: e2.message || "删除失败", icon: "none" });
            }
          }
        });
      };
      const pad2 = (n2) => String(n2).padStart(2, "0");
      const formatTime = (timestamp) => {
        const d2 = new Date((Number(timestamp) || 0) * 1e3);
        const M2 = pad2(d2.getMonth() + 1);
        const D2 = pad2(d2.getDate());
        const h2 = pad2(d2.getHours());
        const m2 = pad2(d2.getMinutes());
        return `${M2}-${D2} ${h2}:${m2}`;
      };
      onLoad(async (options) => {
        uni.setNavigationBarTitle({ title: "官方搭配管理" });
        goodsId.value = (options == null ? void 0 : options.goods_id) || "";
        if (!global$1.isLogin) {
          await asyncGetUserInfo();
        }
        fetchCollocations();
      });
      const __returned__ = { goodsId, collocationList, loading: loading2, goodsInfo, fetchCollocations, previewImage, goToPreview, handleAddCollocation, handleEdit, handleDelete, pad2, formatTime, ref: vue.ref, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$j(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "top-bg" }),
      vue.createElementVNode("view", { class: "header" }, [
        vue.createElementVNode("button", {
          class: "add-btn",
          onClick: $setup.handleAddCollocation
        }, "添加您的搭配参考"),
        vue.createElementVNode("view", { class: "description" }, [
          vue.createElementVNode("text", { class: "hint" }, "搭配参考可以帮助您的购买者快速知晓体型适配问题，优秀的搭配参考图会显著提升买家购买欲望。"),
          vue.createElementVNode("text", { class: "hint" }, "同时，您在搭配参考中发布的关联，将被推送到其它娃物词条下方，他人在购买其它娃物时，也会通过了解到您发布关联的搭配参考，导航到您的词条。")
        ])
      ]),
      $setup.collocationList.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "collocation-list"
      }, [
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.collocationList, (item) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              class: "collocation-item",
              key: item.relation_id
            }, [
              vue.createElementVNode("view", { class: "item-header" }, [
                vue.createElementVNode(
                  "text",
                  { class: "title" },
                  vue.toDisplayString(item.title),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "type-tag" },
                  vue.toDisplayString($setup.formatTime(item.created_at)),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode(
                "view",
                { class: "content" },
                vue.toDisplayString(item.content),
                1
                /* TEXT */
              ),
              vue.createElementVNode("scroll-view", {
                class: "image-scroll",
                "scroll-x": "true",
                style: { "width": "100%" }
              }, [
                vue.createElementVNode("view", { class: "image-wrapper" }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList(item.imageList, (img, index) => {
                      return vue.openBlock(), vue.createElementBlock("image", {
                        key: index,
                        src: img,
                        mode: "aspectFill",
                        class: "collocation-image",
                        onClick: ($event) => $setup.previewImage(item.imageList, index),
                        style: { "width": "300rpx", "height": "300rpx", "margin-right": "20rpx", "flex-shrink": "0" }
                      }, null, 8, ["src", "onClick"]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])
              ]),
              vue.createElementVNode("view", { class: "action-buttons-single-row" }, [
                vue.createElementVNode("button", {
                  class: "btn preview",
                  onClick: ($event) => $setup.goToPreview(item.collocation_id)
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "eye",
                    size: "14",
                    color: "#3a3a3a"
                  }),
                  vue.createTextVNode(" 预览 ")
                ], 8, ["onClick"]),
                vue.createElementVNode("button", {
                  class: "btn edit",
                  onClick: ($event) => $setup.handleEdit(item.collocation_id)
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "compose",
                    size: "14",
                    color: "#3a3a3a"
                  }),
                  vue.createTextVNode(" 编辑 ")
                ], 8, ["onClick"]),
                vue.createElementVNode("button", {
                  class: "btn collocation",
                  onClick: ($event) => $setup.handleDelete(item)
                }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "closeempty",
                    size: "14",
                    color: "#3a3a3a"
                  }),
                  vue.createTextVNode(" 删除 ")
                ], 8, ["onClick"])
              ]),
              vue.createElementVNode("view", { class: "divider" })
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "empty-state"
      }, [
        vue.createElementVNode("image", {
          src: _imports_2$1,
          class: "empty-img"
        }),
        vue.createElementVNode("text", { class: "empty-text" }, "暂无搭配参考")
      ]))
    ]);
  }
  const PkgCreatorCreatorBaseCollocationListCollocationList = /* @__PURE__ */ _export_sfc(_sfc_main$k, [["render", _sfc_render$j], ["__scopeId", "data-v-bf988113"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/collocation_list/collocation_list.vue"]]);
  const _sfc_main$j = {
    __name: "artist_info",
    setup(__props, { expose: __expose }) {
      __expose();
      const activeTab = vue.ref("artist");
      const formData = vue.ref({
        artist_highlight_images: [],
        hairstylist_highlight_images: [],
        status_of_artist: 0,
        status_of_hairstylist: 0,
        base_price_of_artist: 0,
        base_price_of_hairstylist: 0,
        overload_ratio_of_artist: 1,
        overload_ratio_of_hairstylist: 1,
        rule_of_artist: "",
        rule_of_hairstylist: "",
        contact: "",
        accept_2d_faceup: 0,
        accept_3d_faceup: 0
      });
      const statusOptions = vue.ref([
        { label: "请选择状态", value: 0 },
        { label: "长期接单", value: 1 },
        { label: "限时手速投递", value: 2 },
        { label: "限时抽选投递", value: 3 },
        { label: "限时黑箱卡投递", value: 4 },
        { label: "关闭投递", value: 9 }
      ]);
      const artistUploading = vue.ref(false);
      const artistUploadProgress = vue.ref(0);
      const artistUploadStatusText = vue.ref("");
      const hairstylistUploading = vue.ref(false);
      const hairstylistUploadProgress = vue.ref(0);
      const hairstylistUploadStatusText = vue.ref("");
      const artistStatusIndex = vue.computed(
        () => Math.max(0, statusOptions.value.findIndex((item) => item.value === formData.value.status_of_artist))
      );
      const hairstylistStatusIndex = vue.computed(
        () => Math.max(0, statusOptions.value.findIndex((item) => item.value === formData.value.status_of_hairstylist))
      );
      const switchTab = (tab) => {
        activeTab.value = tab;
      };
      const previewImage = (url, urls) => {
        uni.previewImage({ current: url, urls });
      };
      const onArtistStatusChange = (e2) => {
        const index = Number(e2.detail.value);
        formData.value.status_of_artist = statusOptions.value[index].value;
      };
      const onHairstylistStatusChange = (e2) => {
        const index = Number(e2.detail.value);
        formData.value.status_of_hairstylist = statusOptions.value[index].value;
      };
      const onSwitch2D = (e2) => {
        formData.value.accept_2d_faceup = e2.detail.value ? 1 : 0;
      };
      const onSwitch3D = (e2) => {
        formData.value.accept_3d_faceup = e2.detail.value ? 1 : 0;
      };
      const chooseArtistImage = async () => {
        try {
          const maxCount = 9 - formData.value.artist_highlight_images.length;
          if (maxCount <= 0)
            return uni.showToast({ title: "最多只能上传9张图片", icon: "none" });
          const tempFilePaths = await chooseImageList(maxCount);
          artistUploading.value = true;
          artistUploadProgress.value = 0;
          artistUploadStatusText.value = "准备上传...";
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              const filePath = tempFilePaths[i2];
              artistUploadStatusText.value = `获取上传凭证 (${i2 + 1}/${tempFilePaths.length})`;
              const tk = await getQiniuToken();
              if (!(tk == null ? void 0 : tk.token))
                throw new Error("获取上传凭证失败");
              artistUploadProgress.value = Math.round(i2 / tempFilePaths.length * 100);
              artistUploadStatusText.value = `上传中 (${i2 + 1}/${tempFilePaths.length})`;
              const result = await uploadImageToQiniu(filePath, tk.token, tk.path);
              if (result == null ? void 0 : result.imageUrl)
                formData.value.artist_highlight_images.push(result.imageUrl);
            } catch (err) {
              uni.showToast({ title: `第 ${i2 + 1} 张上传失败`, icon: "none" });
            }
          }
          uni.showToast({ title: "上传成功", icon: "success" });
        } catch (error2) {
          uni.showToast({ title: "上传失败", icon: "none" });
        } finally {
          artistUploading.value = false;
          artistUploadProgress.value = 0;
        }
      };
      const removeArtistImage = (index) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除这张展示图吗？",
          success: (res) => {
            if (res.confirm) {
              formData.value.artist_highlight_images.splice(index, 1);
              uni.showToast({ title: "删除成功", icon: "success" });
            }
          }
        });
      };
      const chooseHairstylistImage = async () => {
        try {
          const maxCount = 9 - formData.value.hairstylist_highlight_images.length;
          if (maxCount <= 0)
            return uni.showToast({ title: "最多只能上传9张图片", icon: "none" });
          const tempFilePaths = await chooseImageList(maxCount);
          hairstylistUploading.value = true;
          hairstylistUploadProgress.value = 0;
          hairstylistUploadStatusText.value = "准备上传...";
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              const filePath = tempFilePaths[i2];
              hairstylistUploadStatusText.value = `获取上传凭证 (${i2 + 1}/${tempFilePaths.length})`;
              const tk = await getQiniuToken();
              if (!(tk == null ? void 0 : tk.token))
                throw new Error("获取上传凭证失败");
              hairstylistUploadProgress.value = Math.round(i2 / tempFilePaths.length * 100);
              hairstylistUploadStatusText.value = `上传中 (${i2 + 1}/${tempFilePaths.length})`;
              const result = await uploadImageToQiniu(filePath, tk.token, tk.path);
              if (result == null ? void 0 : result.imageUrl)
                formData.value.hairstylist_highlight_images.push(result.imageUrl);
            } catch (err) {
              uni.showToast({ title: `第 ${i2 + 1} 张上传失败`, icon: "none" });
            }
          }
          uni.showToast({ title: "上传成功", icon: "success" });
        } catch (error2) {
          uni.showToast({ title: "上传失败", icon: "none" });
        } finally {
          hairstylistUploading.value = false;
          hairstylistUploadProgress.value = 0;
        }
      };
      const removeHairstylistImage = (index) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除这张展示图吗？",
          success: (res) => {
            if (res.confirm) {
              formData.value.hairstylist_highlight_images.splice(index, 1);
              uni.showToast({ title: "删除成功", icon: "success" });
            }
          }
        });
      };
      const fetchArtistInfo = async () => {
        const token = uni.getStorageSync("token");
        if (!token)
          return uni.showToast({ title: "请先登录", icon: "none" });
        uni.showLoading({ title: "加载中..." });
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-artist-info`,
            method: "GET",
            header: { Authorization: token }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            formData.value = {
              artist_highlight_images: data.artist_highlight_images || [],
              hairstylist_highlight_images: data.hairstylist_highlight_images || [],
              status_of_artist: Number(data.status_of_artist) || 0,
              status_of_hairstylist: Number(data.status_of_hairstylist) || 0,
              base_price_of_artist: parseFloat(data.base_price_of_artist) || 0,
              base_price_of_hairstylist: parseFloat(data.base_price_of_hairstylist) || 0,
              overload_ratio_of_artist: parseFloat(data.overload_ratio_of_artist) || 1,
              overload_ratio_of_hairstylist: parseFloat(data.overload_ratio_of_hairstylist) || 1,
              rule_of_artist: data.rule_of_artist || "",
              rule_of_hairstylist: data.rule_of_hairstylist || "",
              contact: data.contact || "",
              // 新字段回填（后端若未返回则默认为 0）
              accept_2d_faceup: Number(data.accept_2d_faceup ?? 0),
              accept_3d_faceup: Number(data.accept_3d_faceup ?? 0)
            };
          } else {
            uni.showToast({ title: res.data.msg || "获取信息失败", icon: "none" });
          }
        } catch (error2) {
          uni.showToast({ title: "网络请求失败", icon: "none" });
        } finally {
          uni.hideLoading();
        }
      };
      const saveSettings = async () => {
        const token = uni.getStorageSync("token");
        if (!token)
          return uni.showToast({ title: "请先登录", icon: "none" });
        uni.showLoading({ title: "保存中..." });
        try {
          formData.value.base_price_of_artist = parseFloat(formData.value.base_price_of_artist);
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/update-artist-info`,
            method: "POST",
            header: { Authorization: token, "Content-Type": "application/json" },
            data: formData.value
          });
          if (res.data.status === "success") {
            uni.showToast({ title: "保存成功", icon: "success" });
            fetchArtistInfo();
          } else {
            uni.showToast({ title: res.data.msg || "保存失败", icon: "none" });
          }
        } catch (error2) {
          uni.showToast({ title: "网络请求失败", icon: "none" });
        } finally {
          uni.hideLoading();
        }
      };
      vue.onMounted(() => {
        fetchArtistInfo();
        uni.setNavigationBarTitle({ title: "妆师毛娘信息设置" });
      });
      const __returned__ = { activeTab, formData, statusOptions, artistUploading, artistUploadProgress, artistUploadStatusText, hairstylistUploading, hairstylistUploadProgress, hairstylistUploadStatusText, artistStatusIndex, hairstylistStatusIndex, switchTab, previewImage, onArtistStatusChange, onHairstylistStatusChange, onSwitch2D, onSwitch3D, chooseArtistImage, removeArtistImage, chooseHairstylistImage, removeHairstylistImage, fetchArtistInfo, saveSettings, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$i(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "tabs" }, [
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "artist" }]),
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchTab("artist"))
          },
          " 妆师 ",
          2
          /* CLASS */
        ),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "hairstylist" }]),
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchTab("hairstylist"))
          },
          " 毛娘 ",
          2
          /* CLASS */
        )
      ]),
      $setup.activeTab === "artist" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "form-container"
      }, [
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "最近的接单方式"),
          vue.createElementVNode("picker", {
            class: "picker",
            mode: "selector",
            range: $setup.statusOptions,
            "range-key": "label",
            value: $setup.artistStatusIndex,
            onChange: $setup.onArtistStatusChange
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker-text" },
              vue.toDisplayString($setup.statusOptions[$setup.artistStatusIndex].label),
              1
              /* TEXT */
            )
          ], 40, ["range", "value"])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "基础价格"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.createElementVNode("text", { class: "input-symbol" }, "¥"),
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "digit",
                "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.formData.base_price_of_artist = $event),
                placeholder: "请输入基础价格"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.base_price_of_artist]
            ])
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "钞能力系数"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "digit",
                "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.formData.overload_ratio_of_artist = $event),
                placeholder: "请输入钞能力系数"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.overload_ratio_of_artist]
            ]),
            vue.createElementVNode("text", { class: "input-symbol" }, "倍")
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "站外联系方式"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "text",
                "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.formData.contact = $event),
                placeholder: "请输入联系方式，例如【闲鱼：xxx】"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.contact]
            ])
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "可接妆面类型"),
          vue.createElementVNode("view", { class: "switch-row" }, [
            vue.createElementVNode("text", { class: "switch-label" }, "二次元妆面"),
            vue.createElementVNode("switch", {
              checked: $setup.formData.accept_2d_faceup === 1,
              onChange: $setup.onSwitch2D,
              color: "#74bfff"
            }, null, 40, ["checked"])
          ]),
          vue.createElementVNode("view", { class: "switch-row" }, [
            vue.createElementVNode("text", { class: "switch-label" }, "三次元妆面"),
            vue.createElementVNode("switch", {
              checked: $setup.formData.accept_3d_faceup === 1,
              onChange: $setup.onSwitch3D,
              color: "#74bfff"
            }, null, 40, ["checked"])
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "置顶展示图"),
          vue.createElementVNode("view", { class: "highlight-list" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.formData.artist_highlight_images, (image, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: "highlight-item"
                }, [
                  vue.createElementVNode("image", {
                    class: "highlight-img",
                    src: image,
                    mode: "aspectFill",
                    onClick: ($event) => $setup.previewImage(image, $setup.formData.artist_highlight_images)
                  }, null, 8, ["src", "onClick"]),
                  vue.createElementVNode("view", { class: "highlight-actions" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "trash",
                      size: "24",
                      color: "#ff4d6a",
                      onClick: vue.withModifiers(($event) => $setup.removeArtistImage(index), ["stop"])
                    }, null, 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.formData.artist_highlight_images.length < 9 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "upload-btn",
              onClick: $setup.chooseArtistImage
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plus",
                size: "32",
                color: "#999"
              }),
              vue.createElementVNode("text", { class: "upload-text" }, "添加图片")
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          $setup.artistUploading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "upload-progress"
          }, [
            vue.createElementVNode("progress", {
              percent: $setup.artistUploadProgress,
              "stroke-width": "4",
              activeColor: "#4a9db5"
            }, null, 8, ["percent"]),
            vue.createElementVNode(
              "text",
              null,
              vue.toDisplayString($setup.artistUploadStatusText),
              1
              /* TEXT */
            )
          ])) : vue.createCommentVNode("v-if", true)
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "细则说明"),
          vue.withDirectives(vue.createElementVNode(
            "textarea",
            {
              class: "textarea textarea-long",
              "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.formData.rule_of_artist = $event),
              placeholder: "请输入妆师细则说明"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.rule_of_artist]
          ])
        ])
      ])) : vue.createCommentVNode("v-if", true),
      $setup.activeTab === "hairstylist" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "form-container"
      }, [
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "最近的接单方式"),
          vue.createElementVNode("picker", {
            class: "picker",
            mode: "selector",
            range: $setup.statusOptions,
            "range-key": "label",
            value: $setup.hairstylistStatusIndex,
            onChange: $setup.onHairstylistStatusChange
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker-text" },
              vue.toDisplayString($setup.statusOptions[$setup.hairstylistStatusIndex].label),
              1
              /* TEXT */
            )
          ], 40, ["range", "value"])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "基础价格"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.createElementVNode("text", { class: "input-symbol" }, "¥"),
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "digit",
                "onUpdate:modelValue": _cache[6] || (_cache[6] = ($event) => $setup.formData.base_price_of_hairstylist = $event),
                placeholder: "请输入基础价格"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.base_price_of_hairstylist]
            ])
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "钞能力系数"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "digit",
                "onUpdate:modelValue": _cache[7] || (_cache[7] = ($event) => $setup.formData.overload_ratio_of_hairstylist = $event),
                placeholder: "请输入钞能力系数"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.overload_ratio_of_hairstylist]
            ]),
            vue.createElementVNode("text", { class: "input-symbol" }, "倍")
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "站外联系方式"),
          vue.createElementVNode("view", { class: "input-container" }, [
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                type: "text",
                "onUpdate:modelValue": _cache[8] || (_cache[8] = ($event) => $setup.formData.contact = $event),
                placeholder: "请输入联系方式，例如【闲鱼：xxx】"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.formData.contact]
            ])
          ])
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "置顶展示图"),
          vue.createElementVNode("view", { class: "highlight-list" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.formData.hairstylist_highlight_images, (image, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: "highlight-item"
                }, [
                  vue.createElementVNode("image", {
                    class: "highlight-img",
                    src: image,
                    mode: "aspectFill",
                    onClick: ($event) => $setup.previewImage(image, $setup.formData.hairstylist_highlight_images)
                  }, null, 8, ["src", "onClick"]),
                  vue.createElementVNode("view", { class: "highlight-actions" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "trash",
                      size: "24",
                      color: "#ff4d6a",
                      onClick: vue.withModifiers(($event) => $setup.removeHairstylistImage(index), ["stop"])
                    }, null, 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.formData.hairstylist_highlight_images.length < 9 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "upload-btn",
              onClick: $setup.chooseHairstylistImage
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plus",
                size: "32",
                color: "#999"
              }),
              vue.createElementVNode("text", { class: "upload-text" }, "添加图片")
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          $setup.hairstylistUploading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "upload-progress"
          }, [
            vue.createElementVNode("progress", {
              percent: $setup.hairstylistUploadProgress,
              "stroke-width": "4",
              activeColor: "#4a9db5"
            }, null, 8, ["percent"]),
            vue.createElementVNode(
              "text",
              null,
              vue.toDisplayString($setup.hairstylistUploadStatusText),
              1
              /* TEXT */
            )
          ])) : vue.createCommentVNode("v-if", true)
        ]),
        vue.createElementVNode("view", { class: "form-section" }, [
          vue.createElementVNode("view", { class: "section-title" }, "细则说明"),
          vue.withDirectives(vue.createElementVNode(
            "textarea",
            {
              class: "textarea textarea-long",
              "onUpdate:modelValue": _cache[9] || (_cache[9] = ($event) => $setup.formData.rule_of_hairstylist = $event),
              placeholder: "请输入毛娘细则说明"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.formData.rule_of_hairstylist]
          ])
        ])
      ])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", {
        class: "save-btn",
        onClick: $setup.saveSettings
      }, "保存设置")
    ]);
  }
  const PkgCreatorCreatorBaseArtistInfoArtistInfo = /* @__PURE__ */ _export_sfc(_sfc_main$j, [["render", _sfc_render$i], ["__scopeId", "data-v-c8a542e0"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/artist_info/artist_info.vue"]]);
  const _sfc_main$i = {
    __name: "artist_setting",
    setup(__props, { expose: __expose }) {
      __expose();
      const activeTab = vue.ref("");
      const formData = vue.ref({});
      const navigateTo = (type) => {
        let url = "";
        switch (type) {
          case "makeupGallery":
            url = "/pkg-creator/creator_base/set_showcase/set_showcase";
            break;
          case "wigGallery":
            url = "/pkg-creator/creator_base/set_hair_showcase/set_hair_showcase";
            break;
          case "profileEdit":
            url = "/pkg-creator/creator_base/artist_info/artist_info";
            break;
          case "orderPlan":
            url = "/pkg-creator/creator_base/order_plane/order_plane";
            break;
          default:
            return;
        }
        uni.navigateTo({
          url
        });
      };
      const fetchArtistInfo = async () => {
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return;
        }
        uni.showLoading({
          title: "加载中..."
        });
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-artist-info`,
            method: "GET",
            header: {
              Authorization: token
            }
          });
          if (res.data.status === "success") {
            formatAppLog("log", "at pkg-creator/creator_base/artist_setting/artist_setting.vue:114", res.data.data);
            const data = res.data.data;
            formData.value = {
              artist_highlight_images: data.artist_highlight_images || [],
              hairstylist_highlight_images: data.hairstylist_highlight_images || [],
              status_of_artist: Number(data.status_of_artist) || 0,
              status_of_hairstylist: Number(data.status_of_hairstylist) || 0,
              base_price_of_artist: parseFloat(data.base_price_of_artist) || 0,
              base_price_of_hairstylist: parseFloat(data.base_price_of_hairstylist) || 0,
              overload_ratio_of_artist: parseFloat(data.overload_ratio_of_artist) || 1,
              overload_ratio_of_hairstylist: parseFloat(data.overload_ratio_of_hairstylist) || 1,
              rule_of_artist: data.rule_of_artist || "",
              rule_of_hairstylist: data.rule_of_hairstylist || ""
            };
            isArtist.value = data.status_of_artist > 0;
            isHairstylist.value = data.status_of_hairstylist > 0;
          } else {
            uni.showToast({
              title: res.data.msg || "获取信息失败",
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/artist_setting/artist_setting.vue:138", "获取创作者信息失败:", error2);
          uni.showToast({
            title: "网络请求失败",
            icon: "none"
          });
        } finally {
          uni.hideLoading();
        }
      };
      vue.onMounted(() => {
      });
      const __returned__ = { activeTab, formData, navigateTo, fetchArtistInfo, ref: vue.ref, onMounted: vue.onMounted, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$h(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", null, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "function-section" }, [
        vue.createElementVNode("view", {
          class: "function-item",
          onClick: _cache[0] || (_cache[0] = ($event) => $setup.navigateTo("makeupGallery"))
        }, [
          vue.createElementVNode("view", { class: "function-icon" }, [
            vue.createVNode(_component_uni_icons, {
              type: "color",
              size: "24",
              color: "#3c3b48"
            })
          ]),
          vue.createElementVNode("view", { class: "function-info" }, [
            vue.createElementVNode("text", { class: "function-title" }, "设置妆图展示"),
            vue.createElementVNode("text", { class: "function-desc" }, "管理你的妆面作品展示")
          ]),
          vue.createElementVNode("view", { class: "function-arrow" }, [
            vue.createVNode(_component_uni_icons, {
              type: "arrowright",
              size: "20",
              color: "#999"
            })
          ])
        ]),
        vue.createElementVNode("view", {
          class: "function-item",
          onClick: _cache[1] || (_cache[1] = ($event) => $setup.navigateTo("wigGallery"))
        }, [
          vue.createElementVNode("view", { class: "function-icon" }, [
            vue.createVNode(_component_uni_icons, {
              type: "headphones",
              size: "24",
              color: "#3c3b48"
            })
          ]),
          vue.createElementVNode("view", { class: "function-info" }, [
            vue.createElementVNode("text", { class: "function-title" }, "设置假毛展示"),
            vue.createElementVNode("text", { class: "function-desc" }, "管理你的假发作品展示")
          ]),
          vue.createElementVNode("view", { class: "function-arrow" }, [
            vue.createVNode(_component_uni_icons, {
              type: "arrowright",
              size: "20",
              color: "#999"
            })
          ])
        ]),
        vue.createElementVNode("view", {
          class: "function-item",
          onClick: _cache[2] || (_cache[2] = ($event) => $setup.navigateTo("profileEdit"))
        }, [
          vue.createElementVNode("view", { class: "function-icon" }, [
            vue.createVNode(_component_uni_icons, {
              type: "person",
              size: "24",
              color: "#3c3b48"
            })
          ]),
          vue.createElementVNode("view", { class: "function-info" }, [
            vue.createElementVNode("text", { class: "function-title" }, "编辑个人信息"),
            vue.createElementVNode("text", { class: "function-desc" }, "修改个人资料和联系方式")
          ]),
          vue.createElementVNode("view", { class: "function-arrow" }, [
            vue.createVNode(_component_uni_icons, {
              type: "arrowright",
              size: "20",
              color: "#999"
            })
          ])
        ]),
        vue.createElementVNode("view", {
          class: "function-item",
          onClick: _cache[3] || (_cache[3] = ($event) => $setup.navigateTo("orderPlan"))
        }, [
          vue.createElementVNode("view", { class: "function-icon" }, [
            vue.createVNode(_component_uni_icons, {
              type: "calendar",
              size: "24",
              color: "#3c3b48"
            })
          ]),
          vue.createElementVNode("view", { class: "function-info" }, [
            vue.createElementVNode("text", { class: "function-title" }, "发布接单计划"),
            vue.createElementVNode("text", { class: "function-desc" }, "设置你的接单时间和规则")
          ]),
          vue.createElementVNode("view", { class: "function-arrow" }, [
            vue.createVNode(_component_uni_icons, {
              type: "arrowright",
              size: "20",
              color: "#999"
            })
          ])
        ])
      ])
    ]);
  }
  const PkgCreatorCreatorBaseArtistSettingArtistSetting = /* @__PURE__ */ _export_sfc(_sfc_main$i, [["render", _sfc_render$h], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/artist_setting/artist_setting.vue"]]);
  const _sfc_main$h = {
    __name: "set_showcase",
    setup(__props, { expose: __expose }) {
      __expose();
      const activeTab = vue.ref("faceup");
      const artistInfo = vue.ref({
        artist_id: 0,
        brand_id: 0,
        brand_name: "",
        description: "",
        logo_image: "",
        artist_highlight_images: [],
        status_of_artist: 0,
        status_of_artist_text: "",
        status_of_hairstylist: 0,
        status_of_hairstylist_text: ""
      });
      const faceupList = vue.ref([]);
      const currentPage = vue.ref(1);
      const pageSize2 = vue.ref(20);
      const hasMore = vue.ref(true);
      const brandId = vue.ref(0);
      const uploading = vue.ref(false);
      const uploadProgress = vue.ref(0);
      const uploadStatusText = vue.ref("");
      const switchTab = (tab) => {
        activeTab.value = tab;
        if (tab === "faceup" && faceupList.value.length === 0) {
          fetchFaceupList();
        }
      };
      const fetchArtistInfo = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/get-artist-info`,
            method: "GET",
            header: {
              "Authorization": `${uni.getStorageSync("token")}`
            }
          });
          if (res.data.status === "success" && res.data.data) {
            const data = res.data.data;
            artistInfo.value = {
              ...artistInfo.value,
              brand_id: data.brand_id,
              artist_highlight_images: data.artist_highlight_images || []
            };
            brandId.value = data.brand_id;
          } else {
            uni.showToast({
              title: "获取艺术家信息失败",
              icon: "none"
            });
          }
        } catch (error2) {
          uni.showToast({
            title: "网络错误",
            icon: "none"
          });
        }
      };
      const fetchFaceupList = async () => {
        if (!brandId.value)
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/bjd-faceup?brand_id=${brandId.value}&page=${currentPage.value}&size=${pageSize2.value}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            const newList = res.data.data.list.map((item) => ({
              ...item,
              image_urls: Array.isArray(item.image_urls) ? item.image_urls : (item.image_urls || "").split(",")
            }));
            if (currentPage.value === 1) {
              faceupList.value = newList;
            } else {
              faceupList.value = [...faceupList.value, ...newList];
            }
            hasMore.value = faceupList.value.length < res.data.data.total;
          } else {
            uni.showToast({
              title: "获取妆图列表失败",
              icon: "none"
            });
          }
        } catch (error2) {
          uni.showToast({
            title: "网络错误",
            icon: "none"
          });
        }
      };
      const handleUploadHighlight = async () => {
        try {
          const tempFilePaths = await chooseImageList(1);
          if (!tempFilePaths || tempFilePaths.length === 0)
            return;
          uploading.value = true;
          uploadStatusText.value = "准备上传...";
          uploadStatusText.value = "获取上传凭证...";
          const qiniuTokenData = await getQiniuToken();
          if (!qiniuTokenData || !qiniuTokenData.token) {
            throw new Error("获取上传凭证失败");
          }
          const filePath = tempFilePaths[0];
          uploadStatusText.value = "上传中...";
          uploadProgress.value = 50;
          const fileName = qiniuTokenData.path;
          const result = await uploadImageToQiniu(
            filePath,
            qiniuTokenData.token,
            fileName
          );
          if (result && result.imageUrl) {
            artistInfo.value.artist_highlight_images.push(result.imageUrl);
            uploadProgress.value = 100;
            await updateArtistHighlights();
            uni.showToast({ title: "上传成功", icon: "success" });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/set_showcase/set_showcase.vue:251", "上传图片失败:", error2);
          uni.showToast({ title: "上传图片失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadProgress.value = 0;
        }
      };
      const updateArtistHighlights = async () => {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/update-highlight-images`,
            method: "POST",
            data: {
              brand_id: artistInfo.value.brand_id,
              artist_highlight_images: artistInfo.value.artist_highlight_images.join(",")
            },
            header: {
              "Authorization": `${uni.getStorageSync("token")}`
            }
          });
          if (res.data.status !== "success") {
            formatAppLog("error", "at pkg-creator/creator_base/set_showcase/set_showcase.vue:275", "更新置顶展示图失败:", res.data.msg);
          }
          fetchArtistInfo();
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/set_showcase/set_showcase.vue:279", "更新置顶展示图失败:", error2);
        }
      };
      const previewImage = (url) => {
        uni.previewImage({
          current: url,
          urls: artistInfo.value.artist_highlight_images
        });
      };
      const deleteHighlight = async (index) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除这张置顶图吗？",
          success: async (res) => {
            if (res.confirm) {
              artistInfo.value.artist_highlight_images.splice(index, 1);
              await updateArtistHighlights();
              uni.showToast({ title: "删除成功", icon: "success" });
            }
          }
        });
      };
      const formatDate = (dateStr) => {
        const date = new Date(dateStr);
        return `${date.getFullYear()}-${(date.getMonth() + 1).toString().padStart(2, "0")}-${date.getDate().toString().padStart(2, "0")}`;
      };
      const navigateToAddFaceup = () => {
        uni.navigateTo({ url: `/pkg-creator/creator_base/faceup_editor/faceup_editor` });
      };
      const navigateToEditFaceup = (id) => {
        uni.navigateTo({ url: `/pkg-creator/creator_base/faceup_editor/faceup_editor?id=${id}` });
      };
      onShow(async () => {
        uni.setNavigationBarTitle({ title: "作品展示设置" });
        currentPage.value = 1;
        hasMore.value = true;
        await fetchArtistInfo();
        if (activeTab.value === "faceup") {
          await fetchFaceupList();
        }
      });
      const __returned__ = { activeTab, artistInfo, faceupList, currentPage, pageSize: pageSize2, hasMore, brandId, uploading, uploadProgress, uploadStatusText, switchTab, fetchArtistInfo, fetchFaceupList, handleUploadHighlight, updateArtistHighlights, previewImage, deleteHighlight, formatDate, navigateToAddFaceup, navigateToEditFaceup, ref: vue.ref, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$g(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "artist-container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "tabs" }, [
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "faceup" }]),
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchTab("faceup"))
          },
          " 妆图列表 ",
          2
          /* CLASS */
        ),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "highlight" }]),
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchTab("highlight"))
          },
          " 置顶展示图 ",
          2
          /* CLASS */
        )
      ]),
      $setup.activeTab === "faceup" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "faceup-tab"
      }, [
        vue.createElementVNode("scroll-view", {
          class: "faceup-scroll",
          "scroll-y": ""
        }, [
          vue.createElementVNode("view", { class: "faceup-list" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.faceupList, (item, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "faceup-card",
                  onClick: ($event) => $setup.navigateToEditFaceup(item.id)
                }, [
                  vue.createElementVNode("image", {
                    src: item.image_urls[0],
                    mode: "aspectFill",
                    class: "card-cover"
                  }, null, 8, ["src"]),
                  vue.createElementVNode("view", { class: "card-body" }, [
                    vue.createElementVNode(
                      "view",
                      { class: "card-title" },
                      vue.toDisplayString(item.head_name),
                      1
                      /* TEXT */
                    ),
                    item.style_tags && item.style_tags.length ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "faceup-tags"
                    }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList(item.style_tags, (tag, tagIndex) => {
                          return vue.openBlock(), vue.createElementBlock(
                            "view",
                            {
                              key: tagIndex,
                              class: "tag"
                            },
                            vue.toDisplayString(tag),
                            1
                            /* TEXT */
                          );
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("view", { class: "card-meta" }, [
                      vue.createElementVNode(
                        "text",
                        {
                          class: vue.normalizeClass(["sex", item.sex === "男" ? "male" : "female"])
                        },
                        vue.toDisplayString(item.sex),
                        3
                        /* TEXT, CLASS */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "date" },
                        vue.toDisplayString($setup.formatDate(item.created_at)),
                        1
                        /* TEXT */
                      )
                    ])
                  ]),
                  vue.createVNode(_component_uni_icons, {
                    class: "card-arrow",
                    type: "arrowright",
                    size: "20",
                    color: "#b8c1cc"
                  })
                ], 8, ["onClick"]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ]),
        vue.createElementVNode("view", {
          class: "add-btn",
          onClick: $setup.navigateToAddFaceup
        }, [
          vue.createVNode(_component_uni_icons, {
            type: "plus",
            size: "24",
            color: "#fff"
          }),
          vue.createElementVNode("text", null, "添加妆图")
        ])
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "highlight-tab"
      }, [
        vue.createElementVNode("scroll-view", {
          class: "highlight-scroll",
          "scroll-y": ""
        }, [
          vue.createElementVNode("view", { class: "highlight-list" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.artistInfo.artist_highlight_images, (img, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: "highlight-item"
                }, [
                  vue.createElementVNode("image", {
                    src: img,
                    mode: "aspectFill",
                    class: "highlight-img",
                    onClick: ($event) => $setup.previewImage(img)
                  }, null, 8, ["src", "onClick"]),
                  vue.createElementVNode("view", { class: "highlight-actions" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "trash",
                      size: "24",
                      color: "#ff4d6a",
                      onClick: ($event) => $setup.deleteHighlight(index)
                    }, null, 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ]),
        vue.createElementVNode("view", {
          class: "add-btn",
          onClick: $setup.handleUploadHighlight
        }, [
          vue.createVNode(_component_uni_icons, {
            type: "plus",
            size: "24",
            color: "#fff"
          }),
          vue.createElementVNode("text", null, "添加置顶图")
        ]),
        $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "upload-progress"
        }, [
          vue.createElementVNode("progress", {
            percent: $setup.uploadProgress,
            "stroke-width": "4",
            activeColor: "#4a9db5"
          }, null, 8, ["percent"]),
          vue.createElementVNode(
            "text",
            null,
            vue.toDisplayString($setup.uploadStatusText),
            1
            /* TEXT */
          )
        ])) : vue.createCommentVNode("v-if", true)
      ]))
    ]);
  }
  const PkgCreatorCreatorBaseSetShowcaseSetShowcase = /* @__PURE__ */ _export_sfc(_sfc_main$h, [["render", _sfc_render$g], ["__scopeId", "data-v-1f9281af"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/set_showcase/set_showcase.vue"]]);
  const _sfc_main$g = {
    __name: "faceup_editor",
    setup(__props, { expose: __expose }) {
      __expose();
      const isEdit = vue.ref(false);
      const form = vue.ref({
        id: 0,
        title: "",
        head_name: "",
        goods_id: "",
        // 可选
        sex: "男",
        styles_tags: [],
        face_up_image_urls: []
      });
      const selectedBrand = vue.ref({});
      const selectedGoods = vue.ref({});
      const styleTags = vue.ref({});
      const showTagPopup = vue.ref(false);
      const tagSizeData = vue.ref({});
      const initialTagSelection = vue.ref([]);
      const uploading = vue.ref(false);
      const uploadProgress = vue.ref(0);
      const uploadStatusText = vue.ref("");
      onLoad(async (options) => {
        await fetchStyleTags();
        if (options.id) {
          isEdit.value = true;
          await fetchFaceupDetail(options.id);
        }
        uni.setNavigationBarTitle({
          title: isEdit.value ? "编辑妆面" : "添加新妆面"
        });
      });
      function goPickHead() {
        uni.$once("associate:done", (payload) => {
          var _a, _b;
          selectedBrand.value = (payload == null ? void 0 : payload.brand) || {};
          selectedGoods.value = (payload == null ? void 0 : payload.goods) || {};
          form.value.head_name = ((_a = selectedGoods.value) == null ? void 0 : _a.name) || "";
          form.value.goods_id = ((_b = selectedGoods.value) == null ? void 0 : _b.id) || "";
        });
        uni.navigateTo({
          url: "/pkg-common/brand-pick/brand-pick",
          success(res) {
            var _a, _b;
            res.eventChannel.emit("associate:init", {
              preselect: {
                brand: ((_a = selectedBrand.value) == null ? void 0 : _a.id) ? selectedBrand.value : null,
                goods: ((_b = selectedGoods.value) == null ? void 0 : _b.id) ? selectedGoods.value : null
              }
            });
          }
        });
      }
      async function fetchStyleTags() {
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/bjd-faceup-style-tags`,
            method: "GET"
          });
          if (res.data.status === "success") {
            styleTags.value = res.data.data;
            tagSizeData.value = { "风格标签": Object.values(styleTags.value) };
          }
        } catch (e2) {
          uni.showToast({ title: "获取风格标签失败", icon: "none" });
        }
      }
      async function fetchFaceupDetail(id) {
        try {
          uni.showLoading({ title: "加载中..." });
          const res = await uni.request({
            url: `${websiteUrl.value}/faceup/detail?id=${id}`,
            method: "GET"
          });
          if (res.data.status === "success") {
            const d2 = res.data.data;
            form.value = {
              id: d2.id,
              title: d2.title,
              head_name: d2.head_name || "",
              goods_id: d2.goods_id || "",
              sex: d2.sex === 1 ? "男" : "女",
              styles_tags: d2.styles_tags ? d2.styles_tags.split(",").filter(Boolean) : [],
              face_up_image_urls: d2.images || (d2.face_up_image_urls ? d2.face_up_image_urls.split(",") : [])
            };
          } else {
            uni.showToast({ title: res.data.msg || "加载失败", icon: "none" });
          }
        } finally {
          uni.hideLoading();
        }
      }
      function handleSexChange(e2) {
        form.value.sex = e2.detail.value;
      }
      function removeTag(tagId) {
        const i2 = form.value.styles_tags.indexOf(tagId);
        if (i2 > -1)
          form.value.styles_tags.splice(i2, 1);
      }
      function handleTagConfirm(selectedItems) {
        const names = selectedItems.map((i2) => i2.size);
        const ids = [];
        for (const [id, n2] of Object.entries(styleTags.value)) {
          if (names.includes(n2))
            ids.push(id);
        }
        form.value.styles_tags = ids;
        showTagPopup.value = false;
      }
      async function handleUploadImage() {
        try {
          const remain = 9 - form.value.face_up_image_urls.length;
          if (remain <= 0) {
            uni.showToast({ title: "最多9张", icon: "none" });
            return;
          }
          const files = await chooseImageList(remain);
          if (!files || !files.length)
            return;
          uploading.value = true;
          for (let i2 = 0; i2 < files.length; i2++) {
            uploadStatusText.value = "获取上传凭证...";
            const tk = await getQiniuToken();
            if (!(tk == null ? void 0 : tk.token))
              throw new Error("获取上传凭证失败");
            const filePath = files[i2];
            uploadStatusText.value = `上传中 (${i2 + 1}/${files.length})`;
            uploadProgress.value = i2 / files.length * 100;
            const ret = await uploadImageToQiniu(filePath, tk.token, tk.path);
            if (ret == null ? void 0 : ret.imageUrl) {
              form.value.face_up_image_urls.push(ret.imageUrl);
              uploadProgress.value = (i2 + 1) / files.length * 100;
            }
          }
          uni.showToast({ title: `成功上传${files.length}张`, icon: "success" });
        } catch (e2) {
          uni.showToast({ title: (e2 == null ? void 0 : e2.message) || "上传失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadProgress.value = 0;
        }
      }
      function previewImage(url) {
        uni.previewImage({ current: url, urls: form.value.face_up_image_urls });
      }
      function removeImage(index) {
        uni.showModal({
          title: "提示",
          content: "确定要删除这张图片吗？",
          success: (res) => {
            if (res.confirm) {
              form.value.face_up_image_urls.splice(index, 1);
              uni.showToast({ title: "已删除", icon: "success" });
            }
          }
        });
      }
      async function handleDelete() {
        if (!isEdit.value)
          return;
        uni.showModal({
          title: "删除确认",
          content: "确定删除该妆面吗？删除后不可恢复",
          success: async (res) => {
            var _a, _b, _c;
            if (!res.confirm)
              return;
            const token = uni.getStorageSync("token");
            if (!token) {
              uni.showToast({ title: "未登录", icon: "none" });
              return;
            }
            try {
              uni.showLoading({ title: "删除中..." });
              const resp = await uni.request({
                // 👇 id 拼接到 URL
                url: `${websiteUrl.value}/brand-manager/faceup/delete?id=${encodeURIComponent(form.value.id)}`,
                method: "POST",
                // 若后端支持 DELETE，可改为 method: 'DELETE'
                header: { "Content-Type": "application/json", "Authorization": token }
              });
              if (resp.statusCode >= 200 && resp.statusCode < 300 && ((_a = resp.data) == null ? void 0 : _a.status) === "success") {
                uni.showToast({ title: ((_b = resp.data) == null ? void 0 : _b.msg) || "已删除", icon: "success" });
                setTimeout(() => uni.navigateBack(), 800);
              } else {
                uni.showToast({ title: ((_c = resp.data) == null ? void 0 : _c.msg) || `删除失败(${resp.statusCode})`, icon: "none" });
              }
            } catch (e2) {
              uni.showToast({ title: (e2 == null ? void 0 : e2.message) || "删除失败", icon: "none" });
            } finally {
              uni.hideLoading();
            }
          }
        });
      }
      async function handleSubmit() {
        var _a, _b, _c, _d, _e2;
        if (!((_a = form.value.title) == null ? void 0 : _a.trim())) {
          return uni.showToast({ title: "请输入妆面标题", icon: "none" });
        }
        if (!form.value.face_up_image_urls.length) {
          return uni.showToast({ title: "请至少上传一张图片", icon: "none" });
        }
        try {
          uni.showLoading({ title: "提交中..." });
          const submitData = {
            id: form.value.id,
            title: form.value.title.trim(),
            sex: form.value.sex === "男" ? 1 : 2,
            styles_tags: form.value.styles_tags.join(","),
            // 可空字符串
            face_up_image_urls: form.value.face_up_image_urls.join(",")
            // 至少1张
          };
          if ((_b = form.value.head_name) == null ? void 0 : _b.trim())
            submitData.head_name = form.value.head_name.trim();
          if (form.value.goods_id)
            submitData.goods_id = form.value.goods_id;
          const apiUrl = isEdit.value ? `${websiteUrl.value}/brand-manager/faceup/update` : `${websiteUrl.value}/brand-manager/faceup/add`;
          const token = uni.getStorageSync("token");
          if (!token) {
            uni.showToast({ title: "未登录，请先登录", icon: "none" });
            return;
          }
          const res = await uni.request({
            url: apiUrl,
            method: "POST",
            data: submitData,
            header: { "Content-Type": "application/json", "Authorization": token }
          });
          if (res.statusCode >= 200 && res.statusCode < 300 && ((_c = res.data) == null ? void 0 : _c.status) === "success") {
            uni.showToast({ title: ((_d = res.data) == null ? void 0 : _d.msg) || (isEdit.value ? "更新成功" : "添加成功"), icon: "success" });
            setTimeout(() => uni.navigateBack(), 1200);
          } else {
            uni.showToast({ title: ((_e2 = res.data) == null ? void 0 : _e2.msg) || `提交失败(${res.statusCode})`, icon: "none" });
          }
        } catch (e2) {
          uni.showToast({ title: (e2 == null ? void 0 : e2.message) || "提交失败", icon: "none" });
        } finally {
          uni.hideLoading();
        }
      }
      function handleCancel() {
        uni.navigateBack();
      }
      const __returned__ = { isEdit, form, selectedBrand, selectedGoods, styleTags, showTagPopup, tagSizeData, initialTagSelection, uploading, uploadProgress, uploadStatusText, goPickHead, fetchStyleTags, fetchFaceupDetail, handleSexChange, removeTag, handleTagConfirm, handleUploadImage, previewImage, removeImage, handleDelete, handleSubmit, handleCancel, ref: vue.ref, get onLoad() {
        return onLoad;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$f(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_cascade_multi_select = resolveEasycom(vue.resolveDynamicComponent("cascade-multi-select"), __easycom_2$1);
    return vue.openBlock(), vue.createElementBlock("view", { class: "faceup-form-container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("scroll-view", {
        class: "form-scroll",
        "scroll-y": ""
      }, [
        vue.createElementVNode("view", { class: "form-body" }, [
          vue.createElementVNode("view", { class: "form-item" }, [
            vue.createElementVNode("text", { class: "label" }, "妆面标题"),
            vue.withDirectives(vue.createElementVNode(
              "input",
              {
                class: "input",
                "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.form.title = $event),
                placeholder: "请输入妆面标题",
                maxlength: "50"
              },
              null,
              512
              /* NEED_PATCH */
            ), [
              [vue.vModelText, $setup.form.title]
            ])
          ]),
          vue.createElementVNode("view", { class: "form-item" }, [
            vue.createElementVNode("text", { class: "label" }, "头型名称（可选）"),
            vue.createElementVNode("view", {
              class: "relation-picker-trigger",
              onClick: $setup.goPickHead
            }, [
              vue.createElementVNode("view", { class: "rp-left" }, [
                $setup.selectedGoods.cover ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  src: $setup.selectedGoods.cover,
                  mode: "aspectFill",
                  class: "thumb"
                }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "rp-texts" }, [
                  $setup.form.head_name ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "selected-value"
                    },
                    vue.toDisplayString($setup.form.head_name),
                    1
                    /* TEXT */
                  )) : (vue.openBlock(), vue.createElementBlock("text", {
                    key: 1,
                    class: "placeholder"
                  }, "可选择头型（先选品牌再选商品）")),
                  $setup.selectedBrand.brand_name ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 2,
                      class: "sub"
                    },
                    vue.toDisplayString($setup.selectedBrand.brand_name),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ]),
              vue.createVNode(_component_uni_icons, {
                type: "arrowright",
                size: "16",
                color: "#999"
              })
            ])
          ]),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.form.goods_id = $event),
              type: "hidden",
              style: { "visibility": "hidden" }
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.form.goods_id]
          ]),
          vue.createElementVNode("view", { class: "form-item" }, [
            vue.createElementVNode("text", { class: "label" }, "性别"),
            vue.createElementVNode(
              "radio-group",
              {
                class: "radio-group",
                onChange: $setup.handleSexChange
              },
              [
                vue.createElementVNode("label", { class: "radio-label" }, [
                  vue.createElementVNode("radio", {
                    value: "男",
                    checked: $setup.form.sex === "男",
                    color: "#8fecff"
                  }, null, 8, ["checked"]),
                  vue.createElementVNode("text", null, "男")
                ]),
                vue.createElementVNode("label", { class: "radio-label" }, [
                  vue.createElementVNode("radio", {
                    value: "女",
                    checked: $setup.form.sex === "女",
                    color: "#ffc3c6"
                  }, null, 8, ["checked"]),
                  vue.createElementVNode("text", null, "女")
                ])
              ],
              32
              /* NEED_HYDRATION */
            )
          ]),
          vue.createElementVNode("view", { class: "form-item" }, [
            vue.createElementVNode("view", { class: "tag-header" }, [
              vue.createElementVNode("text", { class: "label" }, "风格标签"),
              vue.createElementVNode("view", {
                class: "tag-selector-btn",
                onClick: _cache[2] || (_cache[2] = ($event) => $setup.showTagPopup = true)
              }, [
                vue.createElementVNode("text", null, "管理风格标签"),
                vue.createVNode(_component_uni_icons, {
                  type: "arrowdown",
                  size: "16",
                  color: "#666"
                })
              ])
            ]),
            vue.createElementVNode("view", { class: "tags-container" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.form.styles_tags, (tagId, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: idx,
                    class: "tag-item"
                  }, [
                    vue.createElementVNode(
                      "text",
                      null,
                      vue.toDisplayString($setup.styleTags[tagId]),
                      1
                      /* TEXT */
                    ),
                    vue.createVNode(_component_uni_icons, {
                      type: "close",
                      size: "16",
                      color: "#999",
                      onClick: ($event) => $setup.removeTag(tagId)
                    }, null, 8, ["onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              )),
              $setup.form.styles_tags.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "no-tags-hint"
              }, [
                vue.createElementVNode("text", null, "未选择风格标签")
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          vue.createElementVNode("view", { class: "form-item" }, [
            vue.createElementVNode("text", { class: "label" }, "妆面图片"),
            vue.createElementVNode("text", { class: "hint" }, "请上传清晰的妆面图片，最多9张"),
            vue.createElementVNode("view", { class: "upload-container" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.form.face_up_image_urls, (img, index) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: index,
                    class: "image-preview"
                  }, [
                    vue.createElementVNode("image", {
                      src: img,
                      mode: "aspectFill",
                      class: "preview-img"
                    }, null, 8, ["src"]),
                    vue.createElementVNode("view", { class: "image-actions" }, [
                      vue.createVNode(_component_uni_icons, {
                        type: "eye",
                        size: "20",
                        color: "#fff",
                        onClick: ($event) => $setup.previewImage(img)
                      }, null, 8, ["onClick"]),
                      vue.createVNode(_component_uni_icons, {
                        type: "trash",
                        size: "20",
                        color: "#fff",
                        onClick: ($event) => $setup.removeImage(index)
                      }, null, 8, ["onClick"])
                    ])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              )),
              $setup.form.face_up_image_urls.length < 9 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "upload-btn",
                onClick: $setup.handleUploadImage
              }, [
                vue.createVNode(_component_uni_icons, {
                  type: "plus",
                  size: "32",
                  color: "#999"
                }),
                vue.createElementVNode("text", { class: "upload-text" }, "添加图片")
              ])) : vue.createCommentVNode("v-if", true)
            ]),
            $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "upload-progress"
            }, [
              vue.createElementVNode("progress", {
                percent: $setup.uploadProgress,
                "stroke-width": "4",
                activeColor: "#4a9db5"
              }, null, 8, ["percent"]),
              vue.createElementVNode(
                "text",
                null,
                vue.toDisplayString($setup.uploadStatusText),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true)
          ])
        ])
      ]),
      vue.createElementVNode("view", { class: "form-footer" }, [
        $setup.isEdit ? (vue.openBlock(), vue.createElementBlock("text", {
          key: 0,
          class: "delete-link",
          onClick: $setup.handleDelete
        }, "删除")) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("button", {
          class: "btn cancel",
          onClick: $setup.handleCancel
        }, "取消"),
        vue.createElementVNode(
          "button",
          {
            class: "btn submit",
            onClick: $setup.handleSubmit
          },
          vue.toDisplayString($setup.isEdit ? "保存修改" : "添加妆面"),
          1
          /* TEXT */
        )
      ]),
      vue.createVNode(_component_cascade_multi_select, {
        show: $setup.showTagPopup,
        sizeData: $setup.tagSizeData,
        initialSelection: $setup.initialTagSelection,
        onClose: _cache[3] || (_cache[3] = ($event) => $setup.showTagPopup = false),
        onConfirm: $setup.handleTagConfirm,
        title: "选择风格标签"
      }, null, 8, ["show", "sizeData", "initialSelection"])
    ]);
  }
  const PkgCreatorCreatorBaseFaceupEditorFaceupEditor = /* @__PURE__ */ _export_sfc(_sfc_main$g, [["render", _sfc_render$f], ["__scopeId", "data-v-ead0c999"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/faceup_editor/faceup_editor.vue"]]);
  const _sfc_main$f = {
    name: "UniNumberBox",
    emits: ["change", "input", "update:modelValue", "blur", "focus"],
    props: {
      value: {
        type: [Number, String],
        default: 1
      },
      modelValue: {
        type: [Number, String],
        default: 1
      },
      min: {
        type: Number,
        default: 0
      },
      max: {
        type: Number,
        default: 100
      },
      step: {
        type: Number,
        default: 1
      },
      background: {
        type: String,
        default: "#f5f5f5"
      },
      color: {
        type: String,
        default: "#333"
      },
      disabled: {
        type: Boolean,
        default: false
      },
      width: {
        type: Number,
        default: 40
      }
    },
    data() {
      return {
        inputValue: 0
      };
    },
    watch: {
      value(val) {
        this.inputValue = +val;
      },
      modelValue(val) {
        this.inputValue = +val;
      }
    },
    computed: {
      widthWithPx() {
        return this.width + "px";
      }
    },
    created() {
      if (this.value === 1) {
        this.inputValue = +this.modelValue;
      }
      if (this.modelValue === 1) {
        this.inputValue = +this.value;
      }
    },
    methods: {
      _calcValue(type) {
        if (this.disabled) {
          return;
        }
        const scale = this._getDecimalScale();
        let value = this.inputValue * scale;
        let step = this.step * scale;
        if (type === "minus") {
          value -= step;
          if (value < this.min * scale) {
            return;
          }
          if (value > this.max * scale) {
            value = this.max * scale;
          }
        }
        if (type === "plus") {
          value += step;
          if (value > this.max * scale) {
            return;
          }
          if (value < this.min * scale) {
            value = this.min * scale;
          }
        }
        this.inputValue = (value / scale).toFixed(String(scale).length - 1);
        this.$emit("input", +this.inputValue);
        this.$emit("update:modelValue", +this.inputValue);
        this.$emit("change", +this.inputValue);
      },
      _getDecimalScale() {
        let scale = 1;
        if (~~this.step !== this.step) {
          scale = Math.pow(10, String(this.step).split(".")[1].length);
        }
        return scale;
      },
      _onBlur(event) {
        this.$emit("blur", event);
        let value = event.detail.value;
        if (isNaN(value)) {
          this.inputValue = this.value;
          return;
        }
        value = +value;
        if (value > this.max) {
          value = this.max;
        } else if (value < this.min) {
          value = this.min;
        }
        const scale = this._getDecimalScale();
        this.inputValue = value.toFixed(String(scale).length - 1);
        this.$emit("input", +this.inputValue);
        this.$emit("update:modelValue", +this.inputValue);
        this.$emit("change", +this.inputValue);
      },
      _onFocus(event) {
        this.$emit("focus", event);
      }
    }
  };
  function _sfc_render$e(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "uni-numbox" }, [
      vue.createElementVNode(
        "view",
        {
          onClick: _cache[0] || (_cache[0] = ($event) => $options._calcValue("minus")),
          class: "uni-numbox__minus uni-numbox-btns",
          style: vue.normalizeStyle({ background: $props.background })
        },
        [
          vue.createElementVNode(
            "text",
            {
              class: vue.normalizeClass(["uni-numbox--text", { "uni-numbox--disabled": $data.inputValue <= $props.min || $props.disabled }]),
              style: vue.normalizeStyle({ color: $props.color })
            },
            "-",
            6
            /* CLASS, STYLE */
          )
        ],
        4
        /* STYLE */
      ),
      vue.withDirectives(vue.createElementVNode("input", {
        disabled: $props.disabled,
        onFocus: _cache[1] || (_cache[1] = (...args) => $options._onFocus && $options._onFocus(...args)),
        onBlur: _cache[2] || (_cache[2] = (...args) => $options._onBlur && $options._onBlur(...args)),
        class: "uni-numbox__value",
        type: $props.step < 1 ? "digit" : "number",
        "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $data.inputValue = $event),
        style: vue.normalizeStyle({ background: $props.background, color: $props.color, width: $options.widthWithPx })
      }, null, 44, ["disabled", "type"]), [
        [vue.vModelDynamic, $data.inputValue]
      ]),
      vue.createElementVNode(
        "view",
        {
          onClick: _cache[4] || (_cache[4] = ($event) => $options._calcValue("plus")),
          class: "uni-numbox__plus uni-numbox-btns",
          style: vue.normalizeStyle({ background: $props.background })
        },
        [
          vue.createElementVNode(
            "text",
            {
              class: vue.normalizeClass(["uni-numbox--text", { "uni-numbox--disabled": $data.inputValue >= $props.max || $props.disabled }]),
              style: vue.normalizeStyle({ color: $props.color })
            },
            "+",
            6
            /* CLASS, STYLE */
          )
        ],
        4
        /* STYLE */
      )
    ]);
  }
  const __easycom_0 = /* @__PURE__ */ _export_sfc(_sfc_main$f, [["render", _sfc_render$e], ["__scopeId", "data-v-7ae2ee72"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/uni_modules/uni-number-box/components/uni-number-box/uni-number-box.vue"]]);
  const _sfc_main$e = {
    __name: "order_plane",
    setup(__props, { expose: __expose }) {
      __expose();
      const tabs = [
        {
          label: "全部",
          value: "all"
        },
        {
          label: "进行中",
          value: "active"
        },
        {
          label: "未开始",
          value: "upcoming"
        },
        {
          label: "已结束",
          value: "inactive"
        }
      ];
      const activeTab = vue.ref("all");
      const planList = vue.ref([]);
      const currentPage = vue.ref(1);
      const pageSize2 = vue.ref(10);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const submitting = vue.ref(false);
      const loadingStatus = vue.ref("more");
      const scrollHeight = vue.ref(0);
      const popup = vue.ref(null);
      const isEditMode = vue.ref(false);
      const currentPlanId = vue.ref(null);
      const uploading = vue.ref(false);
      const uploadedCount = vue.ref(0);
      const totalToUpload = vue.ref(0);
      const uploadStatusText = vue.ref("");
      const tierPicker = vue.ref(null);
      const showTierPicker = vue.ref(false);
      const showAddonPicker = vue.ref(false);
      const commonTiers = vue.ref([]);
      const commonAddons = vue.ref([]);
      const tierOptions = vue.ref([{
        label: "添加空白档位",
        value: "blank"
      }]);
      const addonOptions = vue.ref([{
        label: "添加空白加购",
        value: "blank"
      }]);
      const formData = vue.ref({
        artist_name: "",
        artist_type: 1,
        // 1: 妆师, 2: 毛娘
        order_type: 1,
        // 1: 长期接单, 2: 限时手速, 3: 限时抽选, 4: 黑箱, 9: 关闭
        max_participants: 10,
        max_submissions_per_user: 1,
        open_date: "",
        open_time: "",
        close_date: "",
        close_time: "",
        images: [],
        order_config: {
          tiers: [],
          addons: []
        }
      });
      const artistTypes = [
        {
          value: 1,
          text: "妆师"
        },
        {
          value: 2,
          text: "毛娘"
        }
      ];
      const orderTypes = [
        {
          value: 1,
          text: "长期接单"
        },
        {
          value: 2,
          text: "限时手速"
        },
        {
          value: 3,
          text: "限时抽选"
        },
        {
          value: 4,
          text: "限时黑箱卡"
        },
        {
          value: 9,
          text: "关闭投递"
        }
      ];
      const imageSectionLabel = vue.computed(() => {
        return formData.value.artist_type === 2 ? "毛则详情图片或展示图" : "妆则详情图片或展示图";
      });
      const getTypeParam = (artistType) => artistType === 2 ? 1 : 0;
      vue.onMounted(() => {
        calcScrollHeight();
        fetchCommonConfigs(formData.value.artist_type);
        uni.setNavigationBarTitle({
          title: "接单计划管理"
        });
      });
      onShow(() => {
        fetchPlanList(true);
      });
      vue.watch(showTierPicker, (newVal) => {
        var _a, _b, _c, _d;
        if (newVal) {
          popup.value.close();
          (_b = (_a = tierPicker.value) == null ? void 0 : _a.open) == null ? void 0 : _b.call(_a);
        } else {
          (_d = (_c = tierPicker.value) == null ? void 0 : _c.close) == null ? void 0 : _d.call(_c);
        }
      });
      vue.watch(showAddonPicker, (newVal) => {
        var _a, _b, _c, _d;
        if (newVal) {
          popup.value.close();
          (_b = (_a = addonPicker.value) == null ? void 0 : _a.open) == null ? void 0 : _b.call(_a);
        } else {
          (_d = (_c = addonPicker.value) == null ? void 0 : _c.close) == null ? void 0 : _d.call(_c);
        }
      });
      const fetchCommonConfigs = async (artistType = formData.value.artist_type) => {
        try {
          const typeParam = getTypeParam(artistType);
          const tierRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-tiers?type=${typeParam}`,
            method: "GET",
            header: {
              Authorization: getAuthorization()
            }
          });
          if (tierRes.data.status === "success") {
            tierOptions.value = [
              {
                label: "添加空白档位",
                value: "blank"
              },
              ...tierRes.data.data.map((item) => ({
                label: item.title,
                value: item
              }))
            ];
          }
          const addonRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-addons?type=${typeParam}`,
            method: "GET",
            header: {
              Authorization: getAuthorization()
            }
          });
          if (addonRes.data.status === "success") {
            addonOptions.value = [
              {
                label: "添加空白加购",
                value: "blank"
              },
              ...addonRes.data.data.map((item) => ({
                label: item.title,
                value: item
              }))
            ];
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:467", "获取默认配置失败:", error2);
        }
      };
      const onTierPickerChange = (e2) => {
        const index = e2.detail.value;
        const option = tierOptions.value[index];
        if (option.value === "blank") {
          formData.value.order_config.tiers.push({
            title: "",
            price: 0,
            description: ""
          });
        } else {
          formData.value.order_config.tiers.push({
            ...option.value,
            price: parseFloat(option.value.price) || 0
          });
        }
      };
      const onAddonPickerChange = (e2) => {
        const index = e2.detail.value;
        const option = addonOptions.value[index];
        if (option.value === "blank") {
          formData.value.order_config.addons.push({
            title: "",
            price: 0,
            description: ""
          });
        } else {
          formData.value.order_config.addons.push({
            ...option.value,
            price: parseFloat(option.value.price) || 0
          });
        }
      };
      const calcScrollHeight = () => {
        const systemInfo2 = uni.getSystemInfoSync();
        scrollHeight.value = systemInfo2.windowHeight - 180;
      };
      const getAuthorization = () => {
        const token = uni.getStorageSync("token") || "";
        return token;
      };
      const chooseAndUploadImages = async () => {
        try {
          const remaining = 9 - formData.value.images.length;
          if (remaining <= 0)
            return;
          const tempFilePaths = await chooseImageList(remaining);
          if (!tempFilePaths || tempFilePaths.length === 0)
            return;
          uploading.value = true;
          totalToUpload.value = tempFilePaths.length;
          uploadedCount.value = 0;
          const uploadedUrls = [];
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              let qiniuData = await getQiniuToken();
              let token = qiniuData.token;
              let basePath = qiniuData.path;
              const filePath = tempFilePaths[i2];
              uploadStatusText.value = `上传中 (${i2 + 1}/${tempFilePaths.length})`;
              uploadedCount.value = i2 + 1;
              const result = await uploadImageToQiniu(filePath, token, basePath);
              if (result && result.imageUrl) {
                uploadedUrls.push(result.imageUrl);
              }
            } catch (uploadError) {
              formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:548", `第 ${i2 + 1} 张图片上传失败:`, uploadError);
              uni.showToast({
                title: `第 ${i2 + 1} 张图片上传失败`,
                icon: "none"
              });
            }
          }
          formData.value.images = [...formData.value.images, ...uploadedUrls];
          if (uploadedUrls.length > 0) {
            uni.showToast({
              title: `成功上传 ${uploadedUrls.length} 张图片`,
              icon: "success"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:563", "图片选择或上传失败:", error2);
          uni.showToast({
            title: "图片上传失败: " + (error2.message || "未知错误"),
            icon: "none"
          });
        } finally {
          uploading.value = false;
          uploadedCount.value = 0;
          totalToUpload.value = 0;
        }
      };
      const fetchPlanList = async (reset = false) => {
        if (loading2.value)
          return;
        if (reset) {
          currentPage.value = 1;
          planList.value = [];
          loadingStatus.value = "more";
        }
        loading2.value = true;
        loadingStatus.value = "loading";
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/brand-manager/order-plan/list",
            method: "GET",
            data: {
              page: currentPage.value,
              page_size: pageSize2.value,
              status: activeTab.value
            },
            header: {
              Authorization: getAuthorization()
            }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            const newList = data.list || [];
            total.value = data.total;
            const processedList = newList.map((plan) => {
              const images = typeof plan.images === "string" ? plan.images.split(",").filter((url) => url.trim() !== "") : plan.images || [];
              let orderConfig = {};
              try {
                orderConfig = plan.order_config ? JSON.parse(plan.order_config) : {};
              } catch (e2) {
                formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:611", "解析 order_config 失败:", e2);
              }
              return {
                ...plan,
                images,
                order_config: orderConfig
              };
            });
            if (reset) {
              planList.value = processedList;
            } else {
              planList.value = [...planList.value, ...processedList];
            }
            if (planList.value.length >= total.value) {
              loadingStatus.value = "noMore";
            } else {
              loadingStatus.value = "more";
              currentPage.value++;
            }
          } else {
            uni.showToast({
              title: res.data.message || "获取列表失败",
              icon: "none"
            });
          }
        } catch (error2) {
          uni.showToast({
            title: "网络错误",
            icon: "none"
          });
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (loadingStatus.value === "noMore")
          return;
        fetchPlanList();
      };
      const switchTab = (tab) => {
        activeTab.value = tab;
        fetchPlanList(true);
      };
      const showAddDialog = () => {
        var _a, _b;
        (_b = (_a = choosePopup.value) == null ? void 0 : _a.open) == null ? void 0 : _b.call(_a);
      };
      const editPlan = (plan) => {
        if (plan.artist_type == 1) {
          uni.navigateTo({
            url: `/pkg-creator/creator_base/order_plane/form-artist?id=${plan.id}`
          });
        } else {
          uni.navigateTo({
            url: `/pkg-creator/creator_base/order_plane/form-wig?id=${plan.id}`
          });
        }
      };
      const copyPlan = (plan) => {
        uni.showModal({
          title: "复制开单计划",
          content: "将该开单计划复制到新增表单。建议修改接单方式与时间后再提交。",
          success: (res) => {
            if (!res.confirm)
              return;
            uni.navigateTo({
              url: "/pkg-creator/creator_base/order_plane/form",
              success: (r2) => {
                r2.eventChannel.emit("copyFromPlan", plan);
              }
            });
            uni.showToast({
              title: "已复制到新增表单",
              icon: "success"
            });
          }
        });
      };
      const resetForm = () => {
        formData.value = {
          artist_name: "",
          artist_type: 1,
          order_type: 1,
          max_participants: 10,
          max_submissions_per_user: 1,
          open_date: "",
          open_time: "",
          close_date: "",
          close_time: "",
          images: [],
          order_config: {
            tiers: [],
            addons: []
          }
        };
        currentPlanId.value = null;
        submitting.value = false;
      };
      const onOpenDateChange = (e2) => formData.value.open_date = e2.detail.value;
      const onOpenTimeChange = (e2) => formData.value.open_time = e2.detail.value;
      const onCloseDateChange = (e2) => formData.value.close_date = e2.detail.value;
      const onCloseTimeChange = (e2) => formData.value.close_time = e2.detail.value;
      const submitPlan = async () => {
        if (!formData.value.artist_name)
          return uni.showToast({
            title: "请填写计划名称",
            icon: "none"
          });
        if (!formData.value.open_date || !formData.value.open_time || !formData.value.close_date || !formData.value.close_time) {
          return uni.showToast({
            title: "请选择开始和结束时间",
            icon: "none"
          });
        }
        const openTime = (/* @__PURE__ */ new Date(`${formData.value.open_date} ${formData.value.open_time}`)).getTime() / 1e3;
        const closeTime = (/* @__PURE__ */ new Date(`${formData.value.close_date} ${formData.value.close_time}`)).getTime() / 1e3;
        if (closeTime <= openTime)
          return uni.showToast({
            title: "结束时间必须晚于开始时间",
            icon: "none"
          });
        const params = {
          artist_name: formData.value.artist_name,
          artist_type: formData.value.artist_type,
          order_type: formData.value.order_type,
          max_participants: formData.value.max_participants,
          max_submissions_per_user: formData.value.max_submissions_per_user,
          open_time: openTime,
          close_time: closeTime,
          images: formData.value.images,
          order_config: {
            tiers: formData.value.order_config.tiers.map((tier) => ({
              ...tier,
              price: parseFloat(tier.price) || 0
            })),
            addons: formData.value.order_config.addons.map((addon) => ({
              ...addon,
              price: parseFloat(addon.price) || 0
            }))
          }
        };
        if (isEditMode.value)
          params.id = currentPlanId.value;
        submitting.value = true;
        try {
          const url = websiteUrl.value + (isEditMode.value ? "/brand-manager/order-plan/update" : "/brand-manager/order-plan/add");
          const res = await uni.request({
            url,
            method: "POST",
            data: params,
            header: {
              Authorization: getAuthorization(),
              "Content-Type": "application/json"
            }
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: isEditMode.value ? "更新成功" : "添加成功",
              icon: "success"
            });
            popup.value.close();
            fetchPlanList(true);
          } else {
            uni.showToast({
              title: res.data.message || (isEditMode.value ? "更新失败" : "添加失败"),
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:868", "提交失败:", error2);
          uni.showToast({
            title: "请求失败，请稍后重试",
            icon: "none"
          });
        } finally {
          submitting.value = false;
        }
      };
      const closeDialog = () => popup.value.close();
      const confirmDelete = (id) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除该开单计划吗？",
          success: (res) => {
            if (res.confirm)
              deletePlan(id);
          }
        });
      };
      const deletePlan = async (id) => {
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/brand-manager/order-plan/delete?id=" + id,
            method: "POST",
            header: {
              Authorization: getAuthorization(),
              "Content-Type": "application/json"
            }
          });
          if (res.data.status === "success") {
            uni.showToast({
              title: "删除成功",
              icon: "success"
            });
            fetchPlanList(true);
          } else {
            uni.showToast({
              title: res.data.message || "删除失败",
              icon: "none"
            });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/order_plane.vue:916", "删除失败:", error2);
          uni.showToast({
            title: "网络错误",
            icon: "none"
          });
        }
      };
      const removeImage = (index) => {
        formData.value.images.splice(index, 1);
      };
      const addTier = () => formData.value.order_config.tiers.push({
        title: "",
        price: 0,
        description: ""
      });
      const removeTier = (index) => {
        if (formData.value.order_config.tiers.length > 1)
          formData.value.order_config.tiers.splice(index, 1);
      };
      const addAddon = () => formData.value.order_config.addons.push({
        title: "",
        price: 0,
        description: ""
      });
      const removeAddon = (index) => {
        if (formData.value.order_config.addons.length > 1)
          formData.value.order_config.addons.splice(index, 1);
      };
      const onArtistTypeChange = async (e2) => {
        const newType = parseInt(e2.detail.value) + 1;
        if (formData.value.artist_type === newType)
          return;
        formData.value.artist_type = newType;
        formData.value.order_config.tiers = [];
        formData.value.order_config.addons = [];
        await fetchCommonConfigs(newType);
      };
      const onOrderTypeChange = (e2) => {
        const selectedIndex = parseInt(e2.detail.value);
        formData.value.order_type = orderTypes[selectedIndex].value;
      };
      const getOrderTypeIndex = (type) => orderTypes.findIndex((item) => item.value === type);
      const previewImages = (images, current) => {
        uni.previewImage({
          current,
          urls: images,
          indicator: "number"
        });
      };
      const formatDate = (timestamp, format = "yyyy-MM-dd HH:mm") => {
        if (!timestamp)
          return "";
        const date = new Date(timestamp);
        const year = date.getFullYear();
        const month = (date.getMonth() + 1).toString().padStart(2, "0");
        const day = date.getDate().toString().padStart(2, "0");
        const hours = date.getHours().toString().padStart(2, "0");
        const minutes = date.getMinutes().toString().padStart(2, "0");
        if (format === "yyyy-MM-dd")
          return `${year}-${month}-${day}`;
        if (format === "HH:mm")
          return `${hours}:${minutes}`;
        return `${year}-${month}-${day} ${hours}:${minutes}`;
      };
      const getOrderTypeText = (type) => {
        const map = {
          1: "长期接单",
          2: "限时手速",
          3: "限时抽选",
          4: "限时黑箱卡",
          9: "关闭投递"
        };
        return map[type] || "未知类型";
      };
      const getStatusText = (plan) => {
        const now = Math.floor(Date.now() / 1e3);
        if (now < plan.open_time)
          return "未开始";
        if (now > plan.close_time)
          return "已结束";
        return "进行中";
      };
      const getStatusClass = (plan) => {
        const now = Math.floor(Date.now() / 1e3);
        if (now < plan.open_time)
          return "upcoming";
        if (now > plan.close_time)
          return "inactive";
        return "active";
      };
      const choosePopup = vue.ref(null);
      const CREATE_PAGE_MAP = {
        1: "/pkg-creator/creator_base/order_plane/form-artist",
        // 妆师
        2: "/pkg-creator/creator_base/order_plane/form-wig"
        // 毛娘
      };
      const gotoCreate = (artistType) => {
        var _a, _b;
        const url = CREATE_PAGE_MAP[artistType];
        if (!url) {
          uni.showToast({
            title: "未配置的页面路径",
            icon: "none"
          });
          return;
        }
        (_b = (_a = choosePopup.value) == null ? void 0 : _a.close) == null ? void 0 : _b.call(_a);
        uni.navigateTo({
          url
        });
      };
      const __returned__ = { tabs, activeTab, planList, currentPage, pageSize: pageSize2, total, loading: loading2, submitting, loadingStatus, scrollHeight, popup, isEditMode, currentPlanId, uploading, uploadedCount, totalToUpload, uploadStatusText, tierPicker, showTierPicker, showAddonPicker, commonTiers, commonAddons, tierOptions, addonOptions, formData, artistTypes, orderTypes, imageSectionLabel, getTypeParam, fetchCommonConfigs, onTierPickerChange, onAddonPickerChange, calcScrollHeight, getAuthorization, chooseAndUploadImages, fetchPlanList, loadMore, switchTab, showAddDialog, editPlan, copyPlan, resetForm, onOpenDateChange, onOpenTimeChange, onCloseDateChange, onCloseTimeChange, submitPlan, closeDialog, confirmDelete, deletePlan, removeImage, addTier, removeTier, addAddon, removeAddon, onArtistTypeChange, onOrderTypeChange, getOrderTypeIndex, previewImages, formatDate, getOrderTypeText, getStatusText, getStatusClass, choosePopup, CREATE_PAGE_MAP, gotoCreate, ref: vue.ref, onMounted: vue.onMounted, watch: vue.watch, computed: vue.computed, get onShow() {
        return onShow;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$d(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_number_box = resolveEasycom(vue.resolveDynamicComponent("uni-number-box"), __easycom_0);
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "order-plan-page" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "filter-tabs" }, [
        (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.tabs, (tab) => {
            return vue.createElementVNode("view", {
              key: tab.value,
              class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === tab.value }]),
              onClick: ($event) => $setup.switchTab(tab.value)
            }, vue.toDisplayString(tab.label), 11, ["onClick"]);
          }),
          64
          /* STABLE_FRAGMENT */
        ))
      ]),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "plan-list",
          "scroll-y": "",
          onScrolltolower: $setup.loadMore,
          style: vue.normalizeStyle({ height: $setup.scrollHeight + "px" })
        },
        [
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "loading-container"
          }, [
            vue.createVNode(_component_uni_load_more, { status: "loading" })
          ])) : $setup.planList.length > 0 ? (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            { key: 1 },
            vue.renderList($setup.planList, (plan) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: plan.id,
                class: "plan-item"
              }, [
                vue.createElementVNode("view", { class: "plan-header" }, [
                  vue.createElementVNode("view", { class: "header-left" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "plan-title" },
                      vue.toDisplayString(plan.artist_name) + " - " + vue.toDisplayString($setup.getOrderTypeText(plan.order_type)),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "view",
                      {
                        class: vue.normalizeClass(["plan-status", $setup.getStatusClass(plan)])
                      },
                      vue.toDisplayString($setup.getStatusText(plan)),
                      3
                      /* TEXT, CLASS */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "action-btns" }, [
                    vue.createElementVNode("button", {
                      class: "edit-btn",
                      onClick: ($event) => $setup.editPlan(plan)
                    }, "编辑", 8, ["onClick"]),
                    vue.createElementVNode("button", {
                      class: "delete-btn",
                      onClick: ($event) => $setup.confirmDelete(plan.id)
                    }, "删除", 8, ["onClick"]),
                    vue.createElementVNode("button", {
                      class: "copy-btn",
                      onClick: ($event) => $setup.copyPlan(plan)
                    }, "复制", 8, ["onClick"])
                  ])
                ]),
                plan.images && plan.images.length ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "plan-images"
                }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList(plan.images, (img, index) => {
                      return vue.openBlock(), vue.createElementBlock("image", {
                        key: index,
                        src: img,
                        mode: "aspectFill",
                        class: "plan-image",
                        onClick: ($event) => $setup.previewImages(plan.images, index)
                      }, null, 8, ["src", "onClick"]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "plan-info" }, [
                  vue.createElementVNode("view", { class: "info-row" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "person",
                      size: "16",
                      color: "#666"
                    }),
                    vue.createElementVNode(
                      "text",
                      { class: "info-item" },
                      "名额: " + vue.toDisplayString(plan.max_participants) + "人/每人" + vue.toDisplayString(plan.max_submissions_per_user) + "单",
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "info-row" }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "calendar",
                      size: "16",
                      color: "#666"
                    }),
                    vue.createElementVNode(
                      "text",
                      { class: "info-item" },
                      vue.toDisplayString($setup.formatDate(plan.open_time * 1e3)) + " 至 " + vue.toDisplayString($setup.formatDate(plan.close_time * 1e3)),
                      1
                      /* TEXT */
                    )
                  ])
                ]),
                plan.order_config && plan.order_config.tiers && plan.order_config.tiers.length ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 1,
                  class: "tiers-section"
                }, [
                  vue.createElementVNode("text", { class: "section-title" }, "档位选择"),
                  vue.createElementVNode("view", { class: "tiers-container" }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList(plan.order_config.tiers, (tier, index) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          key: index,
                          class: "tier-item"
                        }, [
                          vue.createElementVNode(
                            "text",
                            { class: "tier-title" },
                            vue.toDisplayString(tier.title),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "tier-price" },
                            "¥" + vue.toDisplayString(tier.price),
                            1
                            /* TEXT */
                          ),
                          vue.createElementVNode(
                            "text",
                            { class: "tier-desc" },
                            vue.toDisplayString(tier.description),
                            1
                            /* TEXT */
                          )
                        ]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])
                ])) : vue.createCommentVNode("v-if", true)
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          )) : vue.createCommentVNode("v-if", true),
          !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "load-more"
          }, [
            $setup.loadingStatus === "loading" ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中...")) : vue.createCommentVNode("v-if", true),
            $setup.loadingStatus === "noMore" && $setup.planList.length !== 0 ? (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "— 没有更多了 —")) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          $setup.planList.length === 0 && !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 3,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              class: "empty-icon",
              src: _imports_0$4
            }),
            vue.createElementVNode("text", { class: "empty-text" }, "暂无开单计划"),
            vue.createElementVNode("text", { class: "empty-tip" }, "点击加号按钮创建您的第一个开单计划")
          ])) : vue.createCommentVNode("v-if", true)
        ],
        36
        /* STYLE, NEED_HYDRATION */
      ),
      vue.createElementVNode("button", {
        class: "add-btn",
        onClick: $setup.showAddDialog
      }, [
        vue.createVNode(_component_uni_icons, {
          type: "plusempty",
          size: "30",
          color: "#fff"
        })
      ]),
      vue.createVNode(
        _component_uni_popup,
        {
          ref: "popup",
          type: "bottom"
        },
        {
          default: vue.withCtx(() => {
            var _a, _b;
            return [
              vue.createElementVNode("view", { class: "popup-content" }, [
                vue.createElementVNode("view", { class: "popup-header" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "popup-title" },
                    vue.toDisplayString($setup.isEditMode ? "编辑开单计划" : "新增开单计划"),
                    1
                    /* TEXT */
                  ),
                  vue.createVNode(_component_uni_icons, {
                    type: "closeempty",
                    size: "24",
                    color: "#999",
                    onClick: $setup.closeDialog
                  })
                ]),
                vue.createElementVNode("scroll-view", {
                  "scroll-y": "",
                  class: "form-container"
                }, [
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "计划名称"),
                    vue.withDirectives(vue.createElementVNode(
                      "input",
                      {
                        class: "form-input",
                        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.formData.artist_name = $event),
                        placeholder: "请输入计划名称"
                      },
                      null,
                      512
                      /* NEED_PATCH */
                    ), [
                      [vue.vModelText, $setup.formData.artist_name]
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "接单类型"),
                    vue.createElementVNode("picker", {
                      onChange: $setup.onArtistTypeChange,
                      value: $setup.formData.artist_type - 1,
                      range: $setup.artistTypes,
                      "range-key": "text"
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString(((_a = $setup.artistTypes[$setup.formData.artist_type - 1]) == null ? void 0 : _a.text) || "请选择接单类型"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "接单方式"),
                    vue.createElementVNode("picker", {
                      onChange: $setup.onOrderTypeChange,
                      value: $setup.getOrderTypeIndex($setup.formData.order_type),
                      range: $setup.orderTypes,
                      "range-key": "text"
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString(((_b = $setup.orderTypes.find((t2) => t2.value === $setup.formData.order_type)) == null ? void 0 : _b.text) || "请选择接单方式"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"]),
                    vue.createElementVNode("view", { class: "helper-tip" }, " 如果本次开单包含不同接单方式如：手速×5、钞×5，需要创建两次开单计划。可以先创建一个，再点击“复制”修改接单方式后提交。 ")
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "最大参与人数"),
                    vue.createVNode(_component_uni_number_box, {
                      modelValue: $setup.formData.max_participants,
                      "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.formData.max_participants = $event),
                      min: 1,
                      max: 1e3
                    }, null, 8, ["modelValue"])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "每人最大投递数"),
                    vue.createVNode(_component_uni_number_box, {
                      modelValue: $setup.formData.max_submissions_per_user,
                      "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.formData.max_submissions_per_user = $event),
                      min: 1,
                      max: 10
                    }, null, 8, ["modelValue"])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "开始时间"),
                    vue.createElementVNode("picker", {
                      mode: "date",
                      value: $setup.formData.open_date,
                      onChange: $setup.onOpenDateChange
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString($setup.formData.open_date || "选择日期"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"]),
                    vue.createElementVNode("picker", {
                      mode: "time",
                      value: $setup.formData.open_time,
                      onChange: $setup.onOpenTimeChange
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString($setup.formData.open_time || "选择时间"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("text", { class: "form-label" }, "结束时间"),
                    vue.createElementVNode("picker", {
                      mode: "date",
                      value: $setup.formData.close_date,
                      onChange: $setup.onCloseDateChange
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString($setup.formData.close_date || "选择日期"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"]),
                    vue.createElementVNode("picker", {
                      mode: "time",
                      value: $setup.formData.close_time,
                      onChange: $setup.onCloseTimeChange
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "picker" },
                        vue.toDisplayString($setup.formData.close_time || "选择时间"),
                        1
                        /* TEXT */
                      )
                    ], 40, ["value"])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "form-label" },
                      vue.toDisplayString($setup.imageSectionLabel),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("view", { class: "image-uploader" }, [
                      $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "uploading-container"
                      }, [
                        vue.createVNode(_component_uni_load_more, { status: "loading" }),
                        vue.createElementVNode(
                          "text",
                          { class: "uploading-text" },
                          "上传中 (" + vue.toDisplayString($setup.uploadedCount) + "/" + vue.toDisplayString($setup.totalToUpload) + ")",
                          1
                          /* TEXT */
                        )
                      ])) : vue.createCommentVNode("v-if", true),
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.formData.images, (img, index) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: index,
                            class: "image-preview"
                          }, [
                            vue.createElementVNode("image", {
                              src: img,
                              mode: "aspectFill",
                              class: "preview-image"
                            }, null, 8, ["src"]),
                            vue.createVNode(_component_uni_icons, {
                              type: "close",
                              size: "18",
                              color: "#fff",
                              class: "delete-icon",
                              onClick: ($event) => $setup.removeImage(index)
                            }, null, 8, ["onClick"])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      )),
                      $setup.formData.images.length < 9 && !$setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "upload-btn",
                        onClick: $setup.chooseAndUploadImages
                      }, [
                        vue.createVNode(_component_uni_icons, {
                          type: "plusempty",
                          size: "28",
                          color: "#999"
                        }),
                        vue.createElementVNode("text", null, "添加图片")
                      ])) : vue.createCommentVNode("v-if", true)
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("view", { class: "form-label-row" }, [
                      vue.createElementVNode("text", { class: "form-label" }, "档位配置"),
                      vue.createElementVNode("view", { class: "header-buttons" }, [
                        vue.createElementVNode("picker", {
                          range: $setup.tierOptions,
                          "range-key": "label",
                          onChange: $setup.onTierPickerChange
                        }, [
                          vue.createElementVNode("button", { class: "small-btn" }, "+ 添加档位")
                        ], 40, ["range"])
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "tier-config" }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.formData.order_config.tiers, (tier, index) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: index,
                            class: "tier-item"
                          }, [
                            vue.createElementVNode("view", { class: "tier-header" }, [
                              $setup.formData.order_config.tiers.length > 1 ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                                key: 0,
                                type: "trash",
                                size: "18",
                                color: "#f56c6c",
                                onClick: ($event) => $setup.removeTier(index)
                              }, null, 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
                            ]),
                            vue.createElementVNode("view", { class: "tier-inputs" }, [
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => tier.title = $event,
                                placeholder: "档位名称"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, tier.title]
                              ]),
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => tier.price = $event,
                                placeholder: "价格",
                                type: "number"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, tier.price]
                              ]),
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => tier.description = $event,
                                placeholder: "描述"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, tier.description]
                              ])
                            ])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "form-group" }, [
                    vue.createElementVNode("view", { class: "form-label-row" }, [
                      vue.createElementVNode("text", { class: "form-label" }, "加购配置"),
                      vue.createElementVNode("view", { class: "header-buttons" }, [
                        vue.createElementVNode("picker", {
                          range: $setup.addonOptions,
                          "range-key": "label",
                          onChange: $setup.onAddonPickerChange
                        }, [
                          vue.createElementVNode("button", { class: "small-btn" }, "+ 添加加购")
                        ], 40, ["range"])
                      ])
                    ]),
                    vue.createElementVNode("view", { class: "tier-config" }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.formData.order_config.addons, (addon, index) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            key: index,
                            class: "tier-item"
                          }, [
                            vue.createElementVNode("view", { class: "tier-header" }, [
                              vue.createElementVNode(
                                "text",
                                null,
                                "加购 " + vue.toDisplayString(index + 1),
                                1
                                /* TEXT */
                              ),
                              $setup.formData.order_config.addons.length > 1 ? (vue.openBlock(), vue.createBlock(_component_uni_icons, {
                                key: 0,
                                type: "trash",
                                size: "18",
                                color: "#f56c6c",
                                onClick: ($event) => $setup.removeAddon(index)
                              }, null, 8, ["onClick"])) : vue.createCommentVNode("v-if", true)
                            ]),
                            vue.createElementVNode("view", { class: "tier-inputs" }, [
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => addon.title = $event,
                                placeholder: "加购名称"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, addon.title]
                              ]),
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => addon.price = $event,
                                placeholder: "价格",
                                type: "number"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, addon.price]
                              ]),
                              vue.withDirectives(vue.createElementVNode("input", {
                                class: "tier-input",
                                "onUpdate:modelValue": ($event) => addon.description = $event,
                                placeholder: "描述"
                              }, null, 8, ["onUpdate:modelValue"]), [
                                [vue.vModelText, addon.description]
                              ])
                            ])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])
                  ])
                ]),
                vue.createElementVNode("button", {
                  class: "submit-btn",
                  onClick: $setup.submitPlan,
                  disabled: $setup.submitting
                }, vue.toDisplayString($setup.submitting ? "提交中..." : "提交"), 9, ["disabled"])
              ])
            ];
          }),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      ),
      vue.createVNode(
        _component_uni_popup,
        {
          ref: "choosePopup",
          type: "center",
          "is-mask-click": true
        },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "choose-type-modal" }, [
              vue.createElementVNode("text", { class: "choose-title" }, "选择创建类型"),
              vue.createElementVNode("view", { class: "choose-btns" }, [
                vue.createElementVNode("button", {
                  class: "choose-btn artist",
                  onClick: _cache[3] || (_cache[3] = ($event) => $setup.gotoCreate(1))
                }, "妆师"),
                vue.createElementVNode("button", {
                  class: "choose-btn wig",
                  onClick: _cache[4] || (_cache[4] = ($event) => $setup.gotoCreate(2))
                }, "毛娘")
              ]),
              vue.createElementVNode("button", {
                class: "choose-cancel",
                onClick: _cache[5] || (_cache[5] = ($event) => $setup.choosePopup.close())
              }, "取消")
            ])
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const PkgCreatorCreatorBaseOrderPlaneOrderPlane = /* @__PURE__ */ _export_sfc(_sfc_main$e, [["render", _sfc_render$d], ["__scopeId", "data-v-33e1746f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/order_plane/order_plane.vue"]]);
  const _sfc_main$d = {
    __name: "news",
    setup(__props, { expose: __expose }) {
      __expose();
      const newsList = vue.ref([]);
      const currentPage = vue.ref(1);
      const pageSize2 = vue.ref(10);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const submitting = vue.ref(false);
      const loadingStatus = vue.ref("more");
      const scrollHeight = vue.ref(0);
      const dialogVisible = vue.ref(false);
      const isEditMode = vue.ref(false);
      const currentNewsId = vue.ref(null);
      const uploading = vue.ref(false);
      const uploadedCount = vue.ref(0);
      const totalToUpload = vue.ref(0);
      const formData = vue.ref({
        title: "",
        content: "",
        image_list: [],
        news_type: 1,
        // 默认图透
        subscribe_goods_id: 0,
        subscribe_goods_name: ""
      });
      const newsTypes = vue.ref([
        { value: 1, label: "图透" },
        { value: 2, label: "尾款" },
        { value: 3, label: "截定" },
        { value: 4, label: "公告" }
      ]);
      const currentBrandId = vue.ref(1);
      const onGoodsSelect = (goods) => {
        formData.value.subscribe_goods_id = goods.id;
        formData.value.subscribe_goods_name = goods.name;
      };
      const onNewsTypeChange = (e2) => {
        formData.value.news_type = parseInt(e2.detail.value) + 1;
      };
      vue.onMounted(() => {
        calcScrollHeight();
        uni.setNavigationBarTitle({ title: "品牌文章管理" });
      });
      onShow(() => {
        fetchNewsList(true);
      });
      const calcScrollHeight = () => {
        const systemInfo2 = uni.getSystemInfoSync();
        scrollHeight.value = systemInfo2.windowHeight - 50;
      };
      const getAuthorization = () => uni.getStorageSync("token") || "";
      const chooseAndUploadImages = async () => {
        try {
          const remaining = 9 - formData.value.image_list.length;
          if (remaining <= 0)
            return;
          const tempFilePaths = await chooseImageList(remaining);
          if (!tempFilePaths || tempFilePaths.length === 0)
            return;
          const qiniuData = await getQiniuToken();
          const token = qiniuData.token;
          const basePath = qiniuData.path;
          uploading.value = true;
          totalToUpload.value = tempFilePaths.length;
          uploadedCount.value = 0;
          const uploadedUrls = [];
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              uploadedCount.value = i2 + 1;
              const result = await uploadImageToQiniu(tempFilePaths[i2], token, basePath);
              if (result && result.imageUrl)
                uploadedUrls.push(result.imageUrl);
            } catch (e2) {
              formatAppLog("error", "at pkg-creator/creator_base/news/news.vue:262", `第 ${i2 + 1} 张图片上传失败:`, e2);
              uni.showToast({ title: `第 ${i2 + 1} 张图片上传失败`, icon: "none" });
            }
          }
          formData.value.image_list = [...formData.value.image_list, ...uploadedUrls];
          if (uploadedUrls.length > 0) {
            uni.showToast({ title: `成功上传 ${uploadedUrls.length} 张图片`, icon: "success" });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/news/news.vue:272", "图片选择或上传失败:", error2);
          uni.showToast({ title: "图片上传失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadedCount.value = 0;
          totalToUpload.value = 0;
        }
      };
      const fetchNewsList = async (reset = false) => {
        if (loading2.value)
          return;
        if (reset) {
          currentPage.value = 1;
          newsList.value = [];
          loadingStatus.value = "more";
        }
        loading2.value = true;
        loadingStatus.value = "loading";
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/brand-manager/news/list",
            method: "GET",
            data: { page_index: currentPage.value, page_size: pageSize2.value },
            header: { Authorization: getAuthorization() }
          });
          if (res.data.status === "success") {
            const data = res.data.data;
            const newList = data.news_list || [];
            total.value = data.total;
            const processedList = newList.map((news) => {
              const image_list = news.image_urls ? news.image_urls.split(",").filter((url) => url.trim() !== "") : news.image_list || [];
              return { ...news, image_list };
            });
            if (reset)
              newsList.value = processedList;
            else
              newsList.value = [...newsList.value, ...processedList];
            if (newsList.value.length >= total.value) {
              loadingStatus.value = "noMore";
            } else {
              loadingStatus.value = "more";
              currentPage.value++;
            }
          } else {
            uni.showToast({ title: res.data.message || "获取列表失败", icon: "none" });
          }
        } catch (error2) {
          uni.showToast({ title: "网络错误", icon: "none" });
        } finally {
          loading2.value = false;
        }
      };
      const loadMore = () => {
        if (loadingStatus.value === "noMore")
          return;
        fetchNewsList();
      };
      const showAddDialog = () => {
        resetForm();
        isEditMode.value = false;
        dialogVisible.value = true;
      };
      const editNews = (news) => {
        resetForm();
        isEditMode.value = true;
        currentNewsId.value = news.id;
        formData.value = {
          title: news.title,
          content: news.content,
          image_list: [...news.image_list || []],
          news_type: news.news_type || 1,
          subscribe_goods_id: news.subscribe_goods_id || 0,
          subscribe_goods_name: news.subscribe_goods_name || ""
        };
        dialogVisible.value = true;
      };
      const resetForm = () => {
        formData.value = {
          title: "",
          content: "",
          image_list: [],
          news_type: 1,
          subscribe_goods_id: 0,
          subscribe_goods_name: ""
        };
        currentNewsId.value = null;
        submitting.value = false;
      };
      const submitNews = async () => {
        if (!formData.value.title) {
          uni.showToast({ title: "请填写文章标题", icon: "none" });
          return;
        }
        if (!formData.value.content) {
          uni.showToast({ title: "请填写文章内容", icon: "none" });
          return;
        }
        const params = {
          title: formData.value.title,
          content: formData.value.content,
          image_urls: formData.value.image_list,
          // 如果后端要字符串：.join(',')
          news_type: formData.value.news_type,
          subscribe_goods_id: formData.value.subscribe_goods_id,
          subscribe_goods_name: formData.value.subscribe_goods_name
        };
        if (isEditMode.value)
          params.id = currentNewsId.value;
        submitting.value = true;
        try {
          const url = websiteUrl.value + (isEditMode.value ? "/brand-manager/news/update" : "/brand-manager/news/create");
          const res = await uni.request({
            url,
            method: "POST",
            data: params,
            header: {
              Authorization: getAuthorization(),
              "Content-Type": "application/json"
            }
          });
          if (res.data.status === "success") {
            uni.showToast({ title: isEditMode.value ? "更新成功" : "添加成功", icon: "success" });
            dialogVisible.value = false;
            fetchNewsList(true);
          } else {
            uni.showToast({ title: res.data.message || (isEditMode.value ? "更新失败" : "添加失败"), icon: "none" });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/news/news.vue:413", "提交失败:", error2);
          uni.showToast({ title: "请求失败，请稍后重试", icon: "none" });
        } finally {
          submitting.value = false;
        }
      };
      const closeDialog = () => {
        dialogVisible.value = false;
      };
      const confirmDelete = (id) => {
        uni.showModal({
          title: "提示",
          content: "确定要删除该文章吗？",
          success: (res) => {
            if (res.confirm)
              deleteNews(id);
          }
        });
      };
      const deleteNews = async (id) => {
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/brand-manager/news/delete?id=" + id,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (res.data.status === "success") {
            uni.showToast({ title: "删除成功", icon: "success" });
            fetchNewsList(true);
          } else {
            uni.showToast({ title: res.data.message || "删除失败", icon: "none" });
          }
        } catch (error2) {
          formatAppLog("error", "at pkg-creator/creator_base/news/news.vue:447", "删除失败:", error2);
          uni.showToast({ title: "网络错误", icon: "none" });
        }
      };
      const removeImage = (index) => {
        formData.value.image_list.splice(index, 1);
      };
      const previewImages = (images, current) => {
        uni.previewImage({ current, urls: images, indicator: "number" });
      };
      const formatDate = (ts2, fmt = "yyyy-MM-dd HH:mm") => {
        if (!ts2)
          return "";
        const d2 = new Date(ts2);
        const y2 = d2.getFullYear();
        const m2 = (d2.getMonth() + 1).toString().padStart(2, "0");
        const day = d2.getDate().toString().padStart(2, "0");
        const hh = d2.getHours().toString().padStart(2, "0");
        const mm = d2.getMinutes().toString().padStart(2, "0");
        if (fmt === "yyyy-MM-dd")
          return `${y2}-${m2}-${day}`;
        if (fmt === "HH:mm")
          return `${hh}:${mm}`;
        return `${y2}-${m2}-${day} ${hh}:${mm}`;
      };
      const __returned__ = { newsList, currentPage, pageSize: pageSize2, total, loading: loading2, submitting, loadingStatus, scrollHeight, dialogVisible, isEditMode, currentNewsId, uploading, uploadedCount, totalToUpload, formData, newsTypes, currentBrandId, onGoodsSelect, onNewsTypeChange, calcScrollHeight, getAuthorization, chooseAndUploadImages, fetchNewsList, loadMore, showAddDialog, editNews, resetForm, submitNews, closeDialog, confirmDelete, deleteNews, removeImage, previewImages, formatDate, ref: vue.ref, onMounted: vue.onMounted, get onShow() {
        return onShow;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, get websiteUrl() {
        return websiteUrl;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$c(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_goods_search = resolveEasycom(vue.resolveDynamicComponent("goods-search"), __easycom_3$2);
    const _component_bottom_popup = resolveEasycom(vue.resolveDynamicComponent("bottom-popup"), __easycom_4$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "brand-news-page" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode(
        "scroll-view",
        {
          class: "news-list",
          "scroll-y": "",
          style: vue.normalizeStyle({ height: $setup.scrollHeight + "px" }),
          onScrolltolower: $setup.loadMore
        },
        [
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "loading-container"
          }, [
            vue.createVNode(_component_uni_load_more, { status: "loading" })
          ])) : $setup.newsList.length > 0 ? (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            { key: 1 },
            vue.renderList($setup.newsList, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                key: item.id,
                class: "news-item"
              }, [
                vue.createElementVNode("view", { class: "news-header" }, [
                  vue.createElementVNode("view", { class: "header-left" }, [
                    vue.createElementVNode(
                      "text",
                      { class: "news-title" },
                      vue.toDisplayString(item.title),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "news-time" },
                      vue.toDisplayString($setup.formatDate(item.created_at * 1e3)),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "action-btns" }, [
                    vue.createElementVNode("button", {
                      class: "edit-btn",
                      onClick: ($event) => $setup.editNews(item)
                    }, "编辑", 8, ["onClick"]),
                    vue.createElementVNode("button", {
                      class: "delete-btn",
                      onClick: ($event) => $setup.confirmDelete(item.id)
                    }, "删除", 8, ["onClick"])
                  ])
                ]),
                item.image_list && item.image_list.length ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "news-images"
                }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList(item.image_list, (img, index) => {
                      return vue.openBlock(), vue.createElementBlock("image", {
                        key: index,
                        src: img,
                        mode: "aspectFill",
                        class: "news-image",
                        onClick: ($event) => $setup.previewImages(item.image_list, index)
                      }, null, 8, ["src", "onClick"]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode(
                  "view",
                  { class: "news-content-preview" },
                  vue.toDisplayString(item.content.length > 50 ? item.content.substring(0, 50) + "..." : item.content),
                  1
                  /* TEXT */
                )
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          )) : vue.createCommentVNode("v-if", true),
          !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 2,
            class: "load-more"
          }, [
            $setup.loadingStatus === "loading" ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中...")) : vue.createCommentVNode("v-if", true),
            $setup.loadingStatus === "noMore" && $setup.newsList.length !== 0 ? (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "— 没有更多了 —")) : vue.createCommentVNode("v-if", true)
          ])) : vue.createCommentVNode("v-if", true),
          $setup.newsList.length === 0 && !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 3,
            class: "empty"
          }, [
            vue.createElementVNode("image", {
              class: "empty-icon",
              src: _imports_0$4
            }),
            vue.createElementVNode("text", { class: "empty-text" }, "暂无品牌文章"),
            vue.createElementVNode("text", { class: "empty-tip" }, "点击加号按钮创建您的第一篇文章")
          ])) : vue.createCommentVNode("v-if", true)
        ],
        36
        /* STYLE, NEED_HYDRATION */
      ),
      vue.createElementVNode("button", {
        class: "add-btn",
        onClick: $setup.showAddDialog
      }, [
        vue.createVNode(_component_uni_icons, {
          type: "plusempty",
          size: "30",
          color: "#fff"
        })
      ]),
      vue.createVNode(_component_bottom_popup, {
        show: $setup.dialogVisible,
        onClose: $setup.closeDialog
      }, {
        default: vue.withCtx(() => {
          var _a;
          return [
            vue.createElementVNode("view", { class: "popup-content" }, [
              vue.createElementVNode("view", { class: "popup-header" }, [
                vue.createElementVNode(
                  "text",
                  { class: "popup-title" },
                  vue.toDisplayString($setup.isEditMode ? "编辑文章" : "新增文章"),
                  1
                  /* TEXT */
                ),
                vue.createVNode(_component_uni_icons, {
                  type: "closeempty",
                  size: "24",
                  color: "#999",
                  onClick: $setup.closeDialog
                })
              ]),
              vue.createElementVNode("scroll-view", {
                "scroll-y": "",
                class: "form-container"
              }, [
                vue.createElementVNode("view", { class: "form-group" }, [
                  vue.createElementVNode("text", { class: "form-label" }, "文章类型"),
                  vue.createElementVNode("picker", {
                    onChange: $setup.onNewsTypeChange,
                    value: $setup.formData.news_type,
                    range: $setup.newsTypes,
                    "range-key": "label"
                  }, [
                    vue.createElementVNode(
                      "view",
                      { class: "picker" },
                      vue.toDisplayString(((_a = $setup.newsTypes.find((t2) => t2.value === $setup.formData.news_type)) == null ? void 0 : _a.label) || "请选择文章类型"),
                      1
                      /* TEXT */
                    )
                  ], 40, ["value", "range"]),
                  vue.createElementVNode("view", { class: "goods-search-tip" }, " 只可以发布与【贩售】相关内容；半成品->图透、即将截止贩售->截定、开始贩售以及其它->公告 "),
                  vue.createElementVNode("view", { class: "goods-search-tip" }, " 我们每天上午和晚上10点左右会搬运一次小红书里发布的和贩售相关的新消息，如果有重复可以自行编辑或删除。 ")
                ]),
                vue.createElementVNode("view", { class: "form-group" }, [
                  vue.createElementVNode("text", { class: "form-label" }, "文章标题"),
                  vue.withDirectives(vue.createElementVNode(
                    "input",
                    {
                      class: "form-input",
                      "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.formData.title = $event),
                      placeholder: "请输入文章标题"
                    },
                    null,
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vModelText, $setup.formData.title]
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-group" }, [
                  vue.createElementVNode("text", { class: "form-label" }, "文章内容"),
                  vue.withDirectives(vue.createElementVNode(
                    "textarea",
                    {
                      class: "form-textarea",
                      "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.formData.content = $event),
                      placeholder: "请输入文章内容",
                      "auto-height": ""
                    },
                    null,
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vModelText, $setup.formData.content]
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-group" }, [
                  vue.createElementVNode("text", { class: "form-label" }, "关联商品"),
                  vue.createElementVNode("view", { class: "goods-search-container" }, [
                    vue.createVNode(_component_goods_search, {
                      mode: "fill",
                      modelValue: $setup.formData.subscribe_goods_name,
                      onSelect: $setup.onGoodsSelect,
                      brandId: $setup.currentBrandId
                    }, null, 8, ["modelValue", "brandId"]),
                    vue.createElementVNode("view", { class: "goods-search-tip" }, " 关联商品，用户可以订阅消息，如果该商品下次发布贩售消息，订阅用户将收到弹窗提醒 ")
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-group" }, [
                  vue.createElementVNode("text", { class: "form-label" }, "展示图片"),
                  vue.createElementVNode("view", { class: "image-uploader" }, [
                    $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "uploading-container"
                    }, [
                      vue.createVNode(_component_uni_load_more, { status: "loading" }),
                      vue.createElementVNode(
                        "text",
                        { class: "uploading-text" },
                        "上传中 (" + vue.toDisplayString($setup.uploadedCount) + "/" + vue.toDisplayString($setup.totalToUpload) + ")",
                        1
                        /* TEXT */
                      )
                    ])) : vue.createCommentVNode("v-if", true),
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList($setup.formData.image_list, (img, index) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          key: index,
                          class: "image-preview"
                        }, [
                          vue.createElementVNode("image", {
                            src: img,
                            mode: "aspectFill",
                            class: "preview-image"
                          }, null, 8, ["src"]),
                          vue.createVNode(_component_uni_icons, {
                            type: "close",
                            size: "18",
                            color: "#fff",
                            class: "delete-icon",
                            onClick: ($event) => $setup.removeImage(index)
                          }, null, 8, ["onClick"])
                        ]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    )),
                    $setup.formData.image_list.length < 9 && !$setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "upload-btn",
                      onClick: $setup.chooseAndUploadImages
                    }, [
                      vue.createVNode(_component_uni_icons, {
                        type: "plusempty",
                        size: "28",
                        color: "#999"
                      }),
                      vue.createElementVNode("text", null, "添加图片")
                    ])) : vue.createCommentVNode("v-if", true)
                  ])
                ])
              ]),
              vue.createElementVNode("view", { class: "popup-footer" }, [
                vue.createElementVNode("button", {
                  class: "submit-btn",
                  onClick: $setup.submitNews,
                  disabled: $setup.submitting
                }, vue.toDisplayString($setup.submitting ? "提交中..." : "提交"), 9, ["disabled"])
              ])
            ])
          ];
        }),
        _: 1
        /* STABLE */
      }, 8, ["show"])
    ]);
  }
  const PkgCreatorCreatorBaseNewsNews = /* @__PURE__ */ _export_sfc(_sfc_main$d, [["render", _sfc_render$c], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/news/news.vue"]]);
  const defaultCover = "https://static.fantuanpu.com/placeholder/cover.png";
  const _sfc_main$c = {
    __name: "set_hair_showcase",
    setup(__props, { expose: __expose }) {
      __expose();
      const activeTab = vue.ref("list");
      const switchTab = (t2) => {
        activeTab.value = t2;
        if (t2 === "list" && !list.value.length)
          reload();
        if (t2 === "highlight" && !artistInfo.value.brand_id)
          fetchArtistInfo();
      };
      const tokenHeader = () => ({ "Authorization": `${uni.getStorageSync("token")}` });
      const artistInfo = vue.ref({ brand_id: 0, hairstylist_highlight_images: [] });
      const uploading = vue.ref(false), uploadProgress = vue.ref(0), uploadStatusText = vue.ref("");
      const fetchArtistInfo = async () => {
        const r2 = await uni.request({ url: `${websiteUrl.value}/brand-manager/get-artist-info`, method: "GET", header: tokenHeader() });
        if (r2.data.status === "success" && r2.data.data) {
          const d2 = r2.data.data;
          artistInfo.value.brand_id = d2.brand_id;
          artistInfo.value.hairstylist_highlight_images = Array.isArray(d2.hairstylist_highlight_images) ? d2.hairstylist_highlight_images : (d2.hairstylist_highlight_images || "").split(",").filter(Boolean);
        }
      };
      const previewHairImage = (idx) => {
        uni.previewImage({ current: artistInfo.value.hairstylist_highlight_images[idx], urls: artistInfo.value.hairstylist_highlight_images });
      };
      const handleUploadHairHighlight = async () => {
        const imgs = await chooseImageList(1);
        if (!(imgs == null ? void 0 : imgs.length))
          return;
        try {
          uploading.value = true;
          uploadStatusText.value = "获取上传凭证...";
          const tk = await getQiniuToken();
          uploadStatusText.value = "上传中...";
          uploadProgress.value = 50;
          const up = await uploadImageToQiniu(imgs[0], tk.token, tk.path);
          if (up == null ? void 0 : up.imageUrl) {
            const arr = [...artistInfo.value.hairstylist_highlight_images, up.imageUrl];
            await uni.request({
              url: `${websiteUrl.value}/brand-manager/update-highlight-images`,
              method: "POST",
              header: tokenHeader(),
              data: { brand_id: artistInfo.value.brand_id, hairstylist_highlight_images: arr.join(",") }
            });
            artistInfo.value.hairstylist_highlight_images = arr;
            uni.showToast({ title: "上传成功", icon: "success" });
          }
        } catch (e2) {
          uni.showToast({ title: "上传失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadProgress.value = 0;
        }
      };
      const deleteHairHighlight = (index) => {
        uni.showModal({ title: "提示", content: "确定删除这张展示图吗？", success: async (res) => {
          if (!res.confirm)
            return;
          const arr = [...artistInfo.value.hairstylist_highlight_images];
          arr.splice(index, 1);
          await uni.request({
            url: `${websiteUrl.value}/brand-manager/update-highlight-images`,
            method: "POST",
            header: tokenHeader(),
            data: { brand_id: artistInfo.value.brand_id, hairstylist_highlight_images: arr.join(",") }
          });
          artistInfo.value.hairstylist_highlight_images = arr;
          uni.showToast({ title: "删除成功", icon: "success" });
        } });
      };
      const list = vue.ref([]), page = vue.ref(1), size = vue.ref(20), total = vue.ref(0), loading2 = vue.ref(false), noMore = vue.ref(false);
      const fetchList = async () => {
        if (loading2.value || noMore.value)
          return;
        loading2.value = true;
        try {
          const r2 = await uni.request({ url: `${websiteUrl.value}/brand-manager/custom-wig/list?page=${page.value}&size=${size.value}`, method: "GET", header: tokenHeader() });
          if (r2.data.status === "success") {
            const data = r2.data.data || {};
            const rows = (data.list || []).map((x) => ({
              id: x.id,
              wig_name: x.wig_name,
              wig_material: x.wig_material,
              wig_color: x.wig_color,
              style_tag_names: Array.isArray(x.style_tags) ? x.style_tags : [],
              cover_image: x.cover_image || ""
            }));
            list.value = list.value.concat(rows);
            total.value = data.total || 0;
            if (list.value.length >= total.value)
              noMore.value = true;
          } else {
            uni.showToast({ title: r2.data.msg || "加载失败", icon: "none" });
          }
        } catch (e2) {
          uni.showToast({ title: "网络错误", icon: "none" });
        } finally {
          loading2.value = false;
        }
      };
      const reload = () => {
        page.value = 1;
        noMore.value = false;
        list.value = [];
        fetchList();
      };
      const loadMore = () => {
        if (!loading2.value && !noMore.value) {
          page.value += 1;
          fetchList();
        }
      };
      const previewOne = (url) => {
        if (!url)
          return;
        uni.previewImage({ current: url, urls: [url] });
      };
      const doDelete = (id) => {
        uni.showModal({
          title: "删除确认",
          content: "删除后不可恢复，确定删除该假发？",
          success: async (res) => {
            if (!res.confirm)
              return;
            try {
              const r2 = await uni.request({
                url: `${websiteUrl.value}/brand-manager/custom-wig/delete?id=${id}`,
                method: "POST",
                header: tokenHeader()
              });
              if (r2.data.status === "success") {
                uni.showToast({ title: "删除成功", icon: "success" });
                reload();
              } else {
                uni.showToast({ title: r2.data.msg || "删除失败", icon: "none" });
              }
            } catch (e2) {
              uni.showToast({ title: "网络错误", icon: "none" });
            }
          }
        });
      };
      const formPopup = vue.ref(), saving = vue.ref(false);
      const form = vue.reactive({ id: 0, wig_name: "", wig_material: "", wig_color: "" });
      const formImages = vue.ref([]);
      const materials = vue.ref([]), matIndex = vue.ref(0);
      const matChange = (e2) => {
        matIndex.value = Number(e2.detail.value);
        form.wig_material = materials.value[matIndex.value] || "";
      };
      const styleMap = vue.ref({});
      const tagList = vue.computed(() => Object.keys(styleMap.value).map((k) => ({ id: Number(k), name: styleMap.value[k] })));
      const tagNameMap = vue.computed(() => Object.fromEntries(tagList.value.map((t2) => [t2.id, t2.name])));
      const tagName = (id) => tagNameMap.value[id] || id;
      const groupTitle = (id) => {
        const g2 = Math.floor(id / 100);
        return { 1: "长度与廓形", 2: "卷度与质感", 3: "刘海与分缝", 4: "造型（编/扎/盘）", 5: "氛围与主题" }[g2] || "其它";
      };
      const tagGroups = vue.computed(() => {
        const buckets = {};
        tagList.value.forEach((t2) => {
          const title = groupTitle(t2.id);
          (buckets[title] || (buckets[title] = [])).push(t2);
        });
        return Object.keys(buckets).map((title) => ({ key: title, title, items: buckets[title] }));
      });
      const selectedTagIds = vue.ref([]);
      const tempSelectedTagIds = vue.ref([]);
      const tagPopup = vue.ref();
      const openTagSelector = () => {
        tempSelectedTagIds.value = [...selectedTagIds.value];
        tagPopup.value.open();
      };
      const applyTags = () => {
        selectedTagIds.value = [...tempSelectedTagIds.value];
        tagPopup.value.close();
      };
      const clearTags = () => {
        tempSelectedTagIds.value = [];
      };
      const toggleCheck = (id) => {
        const arr = tempSelectedTagIds.value;
        const i2 = arr.indexOf(id);
        if (i2 >= 0)
          arr.splice(i2, 1);
        else
          arr.push(id);
      };
      const removeTag = (tid) => {
        selectedTagIds.value = selectedTagIds.value.filter((id) => id !== tid);
      };
      const openCreate = () => {
        resetForm();
        formPopup.value.open();
      };
      const openEdit = async (id) => {
        const r2 = await uni.request({ url: `${websiteUrl.value}/brand-manager/custom-wig/info?id=${id}`, method: "GET", header: tokenHeader() });
        if (r2.data.status !== "success" || !r2.data.data) {
          uni.showToast({ title: r2.data.msg || "获取详情失败", icon: "none" });
          return;
        }
        const d2 = r2.data.data;
        form.id = d2.id;
        form.wig_name = d2.wig_name || "";
        form.wig_material = d2.wig_material || "";
        matIndex.value = Math.max(0, materials.value.findIndex((m2) => m2 === form.wig_material));
        form.wig_color = d2.wig_color || "";
        let ids = [];
        if (Array.isArray(d2.style_tags)) {
          const name2id = Object.fromEntries(tagList.value.map((t2) => [t2.name, t2.id]));
          ids = d2.style_tags.map((n2) => name2id[n2]).filter(Boolean);
        }
        selectedTagIds.value = ids;
        if (Array.isArray(d2.custom_wig_images))
          formImages.value = d2.custom_wig_images;
        else if (typeof d2.custom_wig_image_urls === "string")
          formImages.value = d2.custom_wig_image_urls.split(",").filter(Boolean);
        else
          formImages.value = [];
        formPopup.value.open();
      };
      const closeForm = () => formPopup.value.close();
      const resetForm = () => {
        form.id = 0;
        form.wig_name = "";
        form.wig_material = materials.value[0] || "";
        matIndex.value = 0;
        form.wig_color = "";
        selectedTagIds.value = [];
        formImages.value = [];
      };
      const submitForm = async () => {
        if (!form.wig_name.trim()) {
          uni.showToast({ title: "请填写名称", icon: "none" });
          return;
        }
        const payload = {
          wig_name: form.wig_name.trim(),
          wig_material: form.wig_material || "",
          wig_color: (form.wig_color || "").trim(),
          style_tags: selectedTagIds.value.map(String),
          custom_wig_image_urls: formImages.value.join(",")
        };
        const isEdit = !!form.id;
        if (isEdit)
          payload.id = form.id;
        saving.value = true;
        try {
          const url = isEdit ? `${websiteUrl.value}/brand-manager/custom-wig/update` : `${websiteUrl.value}/brand-manager/custom-wig/add`;
          const r2 = await uni.request({ url, method: "POST", data: payload, header: tokenHeader() });
          if (r2.data.status === "success") {
            uni.showToast({ title: "已保存", icon: "success" });
            closeForm();
            reload();
          } else {
            uni.showToast({ title: r2.data.msg || "保存失败", icon: "none" });
          }
        } catch (e2) {
          uni.showToast({ title: "网络错误", icon: "none" });
        } finally {
          saving.value = false;
        }
      };
      const uploadFormImages = async () => {
        const remain = Math.max(0, 9 - formImages.value.length);
        if (!remain) {
          uni.showToast({ title: "最多上传9张", icon: "none" });
          return;
        }
        const imgs = await chooseImageList(remain);
        if (!(imgs == null ? void 0 : imgs.length))
          return;
        try {
          for (const filePath of imgs) {
            let tk = await getQiniuToken();
            const up = await uploadImageToQiniu(filePath, tk.token, tk.path);
            if (up == null ? void 0 : up.imageUrl)
              formImages.value.push(up.imageUrl);
          }
        } catch (e2) {
          uni.showToast({ title: "上传失败", icon: "none" });
        }
      };
      const removeFormImage = (idx) => {
        formImages.value.splice(idx, 1);
      };
      const previewFormImage = (idx) => {
        uni.previewImage({ current: formImages.value[idx], urls: formImages.value });
      };
      const fetchMaterials = async () => {
        const r2 = await uni.request({ url: `${websiteUrl.value}/hair-materials`, method: "GET" });
        if (r2.data.status === "success") {
          materials.value = Array.isArray(r2.data.data) ? r2.data.data : [];
          form.wig_material = materials.value[0] || "";
          matIndex.value = 0;
        }
      };
      const fetchWigStyleMap = async () => {
        const r2 = await uni.request({ url: `${websiteUrl.value}/custom-wig-style-tags`, method: "GET" });
        if (r2.data.status === "success")
          styleMap.value = r2.data.data || {};
      };
      vue.onMounted(async () => {
        uni.setNavigationBarTitle({ title: "假发管理" });
        await Promise.all([fetchMaterials(), fetchWigStyleMap(), fetchArtistInfo()]);
        reload();
      });
      onShow(() => {
        if (activeTab.value === "list") {
          reload();
        } else {
          fetchArtistInfo();
        }
      });
      const __returned__ = { activeTab, switchTab, tokenHeader, defaultCover, artistInfo, uploading, uploadProgress, uploadStatusText, fetchArtistInfo, previewHairImage, handleUploadHairHighlight, deleteHairHighlight, list, page, size, total, loading: loading2, noMore, fetchList, reload, loadMore, previewOne, doDelete, formPopup, saving, form, formImages, materials, matIndex, matChange, styleMap, tagList, tagNameMap, tagName, groupTitle, tagGroups, selectedTagIds, tempSelectedTagIds, tagPopup, openTagSelector, applyTags, clearTags, toggleCheck, removeTag, openCreate, openEdit, closeForm, resetForm, submitForm, uploadFormImages, removeFormImage, previewFormImage, fetchMaterials, fetchWigStyleMap, ref: vue.ref, reactive: vue.reactive, onMounted: vue.onMounted, computed: vue.computed, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      }, uniPopup: __easycom_0$3, uniIcons: __easycom_1$f };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$b(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock("view", { class: "artist-container" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("view", { class: "tabs" }, [
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "list" }]),
            onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchTab("list"))
          },
          "毛图列表",
          2
          /* CLASS */
        ),
        vue.createElementVNode(
          "view",
          {
            class: vue.normalizeClass(["tab-item", { active: $setup.activeTab === "highlight" }]),
            onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchTab("highlight"))
          },
          "置顶展示图",
          2
          /* CLASS */
        )
      ]),
      $setup.activeTab === "list" ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "faceup-tab"
      }, [
        vue.createElementVNode(
          "scroll-view",
          {
            class: "faceup-scroll",
            "scroll-y": "",
            onScrolltolower: $setup.loadMore,
            "lower-threshold": 60
          },
          [
            vue.createElementVNode("view", { class: "faceup-list two-col" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.list, (item) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: item.id,
                    class: "faceup-card"
                  }, [
                    vue.createElementVNode("image", {
                      src: item.cover_image || $setup.defaultCover,
                      mode: "aspectFill",
                      class: "card-cover",
                      onClick: ($event) => $setup.previewOne(item.cover_image)
                    }, null, 8, ["src", "onClick"]),
                    vue.createElementVNode("view", { class: "card-body" }, [
                      vue.createElementVNode(
                        "view",
                        { class: "card-title" },
                        vue.toDisplayString(item.wig_name || "未命名假发"),
                        1
                        /* TEXT */
                      ),
                      item.style_tag_names && item.style_tag_names.length ? (vue.openBlock(), vue.createElementBlock("view", {
                        key: 0,
                        class: "faceup-tags"
                      }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(item.style_tag_names, (t2, i2) => {
                            return vue.openBlock(), vue.createElementBlock(
                              "view",
                              {
                                class: "tag",
                                key: i2
                              },
                              vue.toDisplayString(t2),
                              1
                              /* TEXT */
                            );
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])) : vue.createCommentVNode("v-if", true),
                      vue.createElementVNode("view", { class: "kv" }, [
                        vue.createElementVNode("text", { class: "k" }, "材质"),
                        vue.createElementVNode(
                          "text",
                          { class: "v" },
                          vue.toDisplayString(item.wig_material || "—"),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "kv" }, [
                        vue.createElementVNode("text", { class: "k" }, "颜色"),
                        vue.createElementVNode(
                          "text",
                          { class: "v" },
                          vue.toDisplayString(item.wig_color || "—"),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "row-actions" }, [
                        vue.createElementVNode("button", {
                          class: "btn primary light",
                          onClick: ($event) => $setup.openEdit(item.id)
                        }, "编辑", 8, ["onClick"]),
                        vue.createElementVNode("button", {
                          class: "btn danger light",
                          onClick: ($event) => $setup.doDelete(item.id)
                        }, "删除", 8, ["onClick"])
                      ])
                    ])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ]),
            vue.createElementVNode("view", { class: "list-footer" }, [
              $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", { key: 0 }, "加载中...")) : $setup.noMore && $setup.list.length ? (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "没有更多了")) : !$setup.list.length ? (vue.openBlock(), vue.createElementBlock("text", { key: 2 }, "暂无数据")) : vue.createCommentVNode("v-if", true)
            ])
          ],
          32
          /* NEED_HYDRATION */
        ),
        vue.createElementVNode("view", {
          class: "add-btn",
          onClick: $setup.openCreate
        }, [
          vue.createVNode($setup["uniIcons"], {
            type: "plus",
            size: "24",
            color: "#fff"
          }),
          vue.createElementVNode("text", null, "新增假发")
        ])
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "highlight-tab"
      }, [
        vue.createElementVNode("scroll-view", {
          class: "highlight-scroll",
          "scroll-y": ""
        }, [
          vue.createElementVNode("view", { class: "highlight-list two-col-cards" }, [
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.artistInfo.hairstylist_highlight_images, (img, index) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: index,
                  class: "highlight-item"
                }, [
                  vue.createElementVNode("image", {
                    src: img,
                    mode: "aspectFill",
                    class: "highlight-img",
                    onClick: ($event) => $setup.previewHairImage(index)
                  }, null, 8, ["src", "onClick"]),
                  vue.createElementVNode("view", { class: "highlight-actions" }, [
                    vue.createVNode($setup["uniIcons"], {
                      type: "trash",
                      size: "22",
                      color: "#fff",
                      onClick: ($event) => $setup.deleteHairHighlight(index)
                    }, null, 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])
        ]),
        vue.createElementVNode("view", {
          class: "add-btn",
          onClick: $setup.handleUploadHairHighlight
        }, [
          vue.createVNode($setup["uniIcons"], {
            type: "plus",
            size: "24",
            color: "#fff"
          }),
          vue.createElementVNode("text", null, "添加展示图")
        ]),
        $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "upload-progress"
        }, [
          vue.createElementVNode("progress", {
            percent: $setup.uploadProgress,
            "stroke-width": "4",
            activeColor: "#4a9db5"
          }, null, 8, ["percent"]),
          vue.createElementVNode(
            "text",
            null,
            vue.toDisplayString($setup.uploadStatusText),
            1
            /* TEXT */
          )
        ])) : vue.createCommentVNode("v-if", true)
      ])),
      vue.createVNode(
        $setup["uniPopup"],
        {
          ref: "formPopup",
          type: "center",
          "mask-click": true
        },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "popup" }, [
              vue.createElementVNode(
                "view",
                { class: "popup-title" },
                vue.toDisplayString($setup.form.id ? "编辑假发" : "新增假发"),
                1
                /* TEXT */
              ),
              vue.createElementVNode("view", { class: "form" }, [
                vue.createElementVNode("view", { class: "form-item" }, [
                  vue.createElementVNode("text", { class: "label" }, "名称"),
                  vue.withDirectives(vue.createElementVNode(
                    "input",
                    {
                      class: "ipt",
                      "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.form.wig_name = $event),
                      placeholder: "必填：假发名称"
                    },
                    null,
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vModelText, $setup.form.wig_name]
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-item" }, [
                  vue.createElementVNode("text", { class: "label" }, "材质"),
                  vue.createElementVNode("picker", {
                    range: $setup.materials,
                    value: $setup.matIndex,
                    onChange: $setup.matChange
                  }, [
                    vue.createElementVNode(
                      "view",
                      { class: "picker-like" },
                      vue.toDisplayString($setup.materials[$setup.matIndex] || "请选择材质"),
                      1
                      /* TEXT */
                    )
                  ], 40, ["range", "value"])
                ]),
                vue.createElementVNode("view", { class: "form-item" }, [
                  vue.createElementVNode("text", { class: "label" }, "颜色"),
                  vue.withDirectives(vue.createElementVNode(
                    "input",
                    {
                      class: "ipt",
                      "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.form.wig_color = $event),
                      placeholder: "如：亚麻金 / 灰紫"
                    },
                    null,
                    512
                    /* NEED_PATCH */
                  ), [
                    [vue.vModelText, $setup.form.wig_color]
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-item" }, [
                  vue.createElementVNode("text", { class: "label" }, "风格标签"),
                  vue.createElementVNode("view", { class: "tag-box" }, [
                    vue.createElementVNode("button", {
                      class: "btn white big",
                      onClick: $setup.openTagSelector
                    }, "选择风格"),
                    $setup.selectedTagIds.length ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "chips"
                    }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.selectedTagIds, (tid) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            class: "chip",
                            key: tid
                          }, [
                            vue.createTextVNode(
                              vue.toDisplayString($setup.tagName(tid)),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode("text", {
                              class: "chip-x",
                              onClick: ($event) => $setup.removeTag(tid)
                            }, "×", 8, ["onClick"])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "hint"
                    }, "未选择"))
                  ])
                ]),
                vue.createElementVNode("view", { class: "form-item" }, [
                  vue.createElementVNode("text", { class: "label" }, "假发图片"),
                  vue.createElementVNode("view", { class: "img-grid" }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList($setup.formImages, (u2, idx) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          class: "img-box",
                          key: idx
                        }, [
                          vue.createElementVNode("image", {
                            src: u2,
                            mode: "aspectFill",
                            class: "img",
                            onClick: ($event) => $setup.previewFormImage(idx)
                          }, null, 8, ["src", "onClick"]),
                          vue.createElementVNode("view", {
                            class: "img-del",
                            onClick: ($event) => $setup.removeFormImage(idx)
                          }, [
                            vue.createVNode($setup["uniIcons"], {
                              type: "closeempty",
                              size: "18",
                              color: "#fff"
                            })
                          ], 8, ["onClick"])
                        ]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    )),
                    vue.createElementVNode("view", {
                      class: "img-add",
                      onClick: $setup.uploadFormImages
                    }, [
                      vue.createVNode($setup["uniIcons"], {
                        type: "plus",
                        size: "28"
                      }),
                      vue.createElementVNode("text", null, "添加")
                    ])
                  ])
                ])
              ]),
              vue.createElementVNode("view", { class: "popup-actions" }, [
                vue.createElementVNode("button", {
                  class: "btn flat cancel",
                  onClick: $setup.closeForm
                }, "取消"),
                vue.createElementVNode("button", {
                  class: "btn flat primary-strong",
                  disabled: $setup.saving,
                  onClick: $setup.submitForm
                }, vue.toDisplayString($setup.saving ? "提交中..." : "保存"), 9, ["disabled"])
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      ),
      vue.createVNode(
        $setup["uniPopup"],
        {
          ref: "tagPopup",
          type: "bottom",
          "mask-click": true
        },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode("view", { class: "tag-popup" }, [
              vue.createElementVNode("view", { class: "tag-header" }, [
                vue.createElementVNode("text", { class: "title" }, "选择风格"),
                vue.createElementVNode("view", { class: "ops" }, [
                  vue.createElementVNode("button", {
                    class: "btn flat ghost",
                    onClick: $setup.clearTags
                  }, "清空"),
                  vue.createElementVNode("button", {
                    class: "btn flat primary-strong",
                    onClick: $setup.applyTags
                  }, "完成")
                ])
              ]),
              vue.createElementVNode("scroll-view", {
                "scroll-y": "",
                class: "tag-scroll"
              }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.tagGroups, (grp) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      class: "group",
                      key: grp.key
                    }, [
                      vue.createElementVNode(
                        "view",
                        { class: "group-title" },
                        vue.toDisplayString(grp.title),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("view", { class: "tag-grid" }, [
                        (vue.openBlock(true), vue.createElementBlock(
                          vue.Fragment,
                          null,
                          vue.renderList(grp.items, (opt) => {
                            return vue.openBlock(), vue.createElementBlock("view", {
                              class: "tag-item",
                              key: opt.id,
                              onClick: ($event) => $setup.toggleCheck(opt.id)
                            }, [
                              vue.createElementVNode("checkbox", {
                                checked: $setup.tempSelectedTagIds.includes(opt.id)
                              }, null, 8, ["checked"]),
                              vue.createElementVNode(
                                "text",
                                { class: "name" },
                                vue.toDisplayString(opt.name),
                                1
                                /* TEXT */
                              )
                            ], 8, ["onClick"]);
                          }),
                          128
                          /* KEYED_FRAGMENT */
                        ))
                      ])
                    ]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const PkgCreatorCreatorBaseSetHairShowcaseSetHairShowcase = /* @__PURE__ */ _export_sfc(_sfc_main$c, [["render", _sfc_render$b], ["__scopeId", "data-v-8b92aed5"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/set_hair_showcase/set_hair_showcase.vue"]]);
  const INDICATOR_PX = 64;
  const _sfc_main$b = {
    __name: "common-time-picker",
    props: {
      show: { type: Boolean, default: false },
      // v-model:show
      modelValue: { type: [String, Number, Date], default: "" },
      // v-model => 'HH:mm'
      title: { type: String, default: "选择时间" },
      themeColor: { type: String, default: "#4df0ff" }
      // 与日期组件色系一致
    },
    emits: ["update:modelValue", "update:show", "confirm", "cancel"],
    setup(__props, { expose: __expose, emit: __emit }) {
      __expose();
      const props = __props;
      const emit = __emit;
      const popup = vue.ref(null);
      const mountedInPopup = vue.ref(false);
      const safeBottom = vue.ref(0);
      const indicatorStyle2 = `height:${INDICATOR_PX}px;`;
      const hours = vue.ref(Array.from({ length: 24 }, (_2, i2) => i2));
      const minutes = vue.ref(Array.from({ length: 60 }, (_2, i2) => i2));
      const selH = vue.ref(12);
      const selM = vue.ref(0);
      const pickerIndex = vue.computed(() => [
        Math.max(0, hours.value.indexOf(selH.value)),
        Math.max(0, minutes.value.indexOf(selM.value))
      ]);
      const two = (n2) => n2 < 10 ? "0" + n2 : "" + n2;
      function parseTime(v2) {
        if (v2 instanceof Date)
          return { h: v2.getHours(), m: v2.getMinutes() };
        if (typeof v2 === "string") {
          const m2 = v2.match(/(\d{1,2}):(\d{1,2})/);
          if (m2) {
            return { h: Math.min(23, Math.max(0, +m2[1])), m: Math.min(59, Math.max(0, +m2[2])) };
          }
        }
        if (typeof v2 === "number") {
          const d2 = new Date(v2);
          if (!isNaN(d2.getTime()))
            return { h: d2.getHours(), m: d2.getMinutes() };
        }
        return { h: 12, m: 0 };
      }
      function onPickerChange(e2) {
        const [hi, mi] = e2.detail.value;
        selH.value = hours.value[hi] ?? selH.value;
        selM.value = minutes.value[mi] ?? selM.value;
      }
      function onCancel() {
        emit("cancel");
        emit("update:show", false);
      }
      function onConfirm() {
        const out = `${two(selH.value)}:${two(selM.value)}`;
        emit("update:modelValue", out);
        emit("confirm", out);
        emit("update:show", false);
      }
      function initByModel() {
        const t2 = parseTime(props.modelValue);
        selH.value = t2.h;
        selM.value = t2.m;
      }
      function onPopupChange(e2) {
        if (e2.show) {
          vue.nextTick(() => {
            mountedInPopup.value = true;
          });
        } else {
          mountedInPopup.value = false;
          emit("update:show", false);
        }
      }
      vue.watch(() => props.show, async (val) => {
        var _a, _b, _c, _d, _e2;
        if (val) {
          try {
            const wi = uni.getWindowInfo && uni.getWindowInfo();
            safeBottom.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.bottom) || 0;
          } catch {
            safeBottom.value = 0;
          }
          initByModel();
          await vue.nextTick();
          (_c = (_b = popup.value) == null ? void 0 : _b.open) == null ? void 0 : _c.call(_b);
        } else {
          (_e2 = (_d = popup.value) == null ? void 0 : _d.close) == null ? void 0 : _e2.call(_d);
        }
      }, { immediate: true });
      const __returned__ = { props, emit, popup, mountedInPopup, safeBottom, INDICATOR_PX, indicatorStyle: indicatorStyle2, hours, minutes, selH, selM, pickerIndex, two, parseTime, onPickerChange, onCancel, onConfirm, initByModel, onPopupChange, ref: vue.ref, computed: vue.computed, watch: vue.watch, nextTick: vue.nextTick };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$a(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createBlock(
      _component_uni_popup,
      {
        ref: "popup",
        type: "bottom",
        "mask-click": true,
        onChange: $setup.onPopupChange
      },
      {
        default: vue.withCtx(() => [
          vue.createElementVNode(
            "view",
            {
              class: "sheet",
              style: vue.normalizeStyle({ paddingBottom: $setup.safeBottom + "px" })
            },
            [
              vue.createElementVNode("view", { class: "sheet-header" }, [
                vue.createElementVNode("text", {
                  class: "btn-cancel",
                  onClick: $setup.onCancel
                }, "取消"),
                vue.createElementVNode(
                  "text",
                  { class: "sheet-title" },
                  vue.toDisplayString($props.title),
                  1
                  /* TEXT */
                )
              ]),
              $setup.mountedInPopup ? (vue.openBlock(), vue.createElementBlock("picker-view", {
                key: 0,
                class: "picker",
                "indicator-style": $setup.indicatorStyle,
                value: $setup.pickerIndex,
                onChange: $setup.onPickerChange
              }, [
                vue.createElementVNode("picker-view-column", null, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.hours, (h2) => {
                      return vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: "h-" + h2,
                          class: "picker-item"
                        },
                        vue.toDisplayString($setup.two(h2)) + " 时",
                        1
                        /* TEXT */
                      );
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ]),
                vue.createElementVNode("picker-view-column", null, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.minutes, (m2) => {
                      return vue.openBlock(), vue.createElementBlock(
                        "view",
                        {
                          key: "m-" + m2,
                          class: "picker-item"
                        },
                        vue.toDisplayString($setup.two(m2)) + " 分",
                        1
                        /* TEXT */
                      );
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  ))
                ])
              ], 40, ["value"])) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("view", { class: "sheet-footer" }, [
                vue.createElementVNode("button", {
                  class: "btn-confirm",
                  onClick: $setup.onConfirm
                }, "选择")
              ])
            ],
            4
            /* STYLE */
          )
        ]),
        _: 1
        /* STABLE */
      },
      512
      /* NEED_PATCH */
    );
  }
  const __easycom_2 = /* @__PURE__ */ _export_sfc(_sfc_main$b, [["render", _sfc_render$a], ["__scopeId", "data-v-012062b6"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/components/common-time-picker/common-time-picker.vue"]]);
  const _sfc_main$a = {
    __name: "form-artist",
    setup(__props, { expose: __expose }) {
      __expose();
      const artistTypes = [
        { value: 1, text: "妆师" },
        { value: 2, text: "毛娘" }
      ];
      const orderTypes = [
        { value: 1, text: "长期接单" },
        { value: 2, text: "限时手速" },
        { value: 3, text: "限时抽选" },
        { value: 4, text: "限时黑箱卡" },
        { value: 9, text: "关闭投递" }
      ];
      const challengeTypes = [{ value: 1, text: "四则运算" }];
      const finishingMethods = [
        { value: "oil", text: "油性消光" },
        { value: "varnish", text: "罩光剂" },
        { value: "water", text: "水性消光" }
      ];
      const shippingOptions = [
        { value: "unified", text: "统一寄送" },
        { value: "separate", text: "分别寄送" }
      ];
      const allSizes = ["一分", "二分", "三分", "四分", "五分", "六分", "八分", "十二分"];
      const isEditMode = vue.ref(false);
      const currentId = vue.ref(null);
      const submitting = vue.ref(false);
      const form = vue.ref({
        id: null,
        artist_name: "",
        artist_type: 1,
        order_type: 1,
        max_participants: 0,
        max_submissions_per_user: 1,
        open_date: "",
        open_time: "",
        close_date: "",
        close_time: "",
        service_scene: 1,
        // 1=其它平台, 2=本平台
        require_platform_account: false,
        // 平台社交必填
        check_showcase: 0,
        // 抽选门槛
        queue_window_sec: 60,
        // 手速窗口
        challenge_type: 1,
        images: [],
        order_config: {
          tiers: [],
          addons: [],
          extra: {
            per_head_cycle_days: 0,
            finishing_method: "water",
            finishing_desc: "",
            shipping: { mode: "separate", unified_date: "", separate_days_before_start: 0 },
            size_surcharges: []
            // [{ size:'一分', price: 0 }]
          }
        }
      });
      const artistTypeIndex = vue.computed(() => Math.max(0, artistTypes.findIndex((x) => x.value === form.value.artist_type)));
      const orderTypeIndex = vue.computed(() => Math.max(0, orderTypes.findIndex((x) => x.value === form.value.order_type)));
      const challengeTypeIndex = vue.computed(() => Math.max(0, challengeTypes.findIndex((x) => x.value === form.value.challenge_type)));
      const finishingMethodIndex = vue.computed(() => Math.max(0, finishingMethods.findIndex((x) => x.value === form.value.order_config.extra.finishing_method)));
      const shippingIndex = vue.computed(() => Math.max(0, shippingOptions.findIndex((x) => x.value === form.value.order_config.extra.shipping.mode)));
      const showOpenDate = vue.ref(false);
      const showOpenTime = vue.ref(false);
      const showCloseDate = vue.ref(false);
      const showCloseTime = vue.ref(false);
      const showUnifiedDate = vue.ref(false);
      const dummyShow = vue.ref(false);
      const unifiedMinDate = vue.computed(() => {
        const ct2 = toUnix(form.value.close_date, form.value.close_time);
        if (!ct2)
          return "";
        const d2 = new Date((ct2 + 10 * 86400) * 1e3);
        return `${d2.getFullYear()}-${pad2(d2.getMonth() + 1)}-${pad2(d2.getDate())}`;
      });
      const tierOptions = vue.ref([{ label: "添加空白档位", value: "blank" }]);
      const addonOptions = vue.ref([{ label: "添加空白加购", value: "blank" }]);
      const uploading = vue.ref(false);
      const uploadedCount = vue.ref(0);
      const totalToUpload = vue.ref(0);
      const uploadStatusText = vue.ref("");
      onLoad((query) => {
        if (query && query.id) {
          isEditMode.value = true;
          currentId.value = Number(query.id);
          loadDetail(currentId.value);
        } else {
          isEditMode.value = false;
          currentId.value = null;
        }
      });
      vue.onMounted(() => {
        fetchCommonConfigs(form.value.artist_type);
        uni.setNavigationBarTitle({
          title: "发布BJD接妆计划"
        });
      });
      const pad2 = (n2) => n2 < 10 ? "0" + n2 : "" + n2;
      function toUnix(dateStr, timeStr) {
        if (!dateStr || !timeStr)
          return 0;
        const ts2 = (/* @__PURE__ */ new Date(`${dateStr} ${timeStr}`)).getTime();
        if (Number.isNaN(ts2))
          return 0;
        return Math.floor(ts2 / 1e3);
      }
      function displayPrice(v2) {
        const num = Number(v2);
        if (Number.isNaN(num))
          return "";
        return (Math.round(num * 100) / 100).toFixed(2);
      }
      function toFixed2(v2) {
        const num = parseFloat(String(v2).replace(/[^\d.]/g, ""));
        if (Number.isNaN(num))
          return 0;
        return Math.round(num * 100) / 100;
      }
      function getAuthorization() {
        return uni.getStorageSync("token") || "";
      }
      function onArtistTypeChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.artist_type = artistTypes[idx].value;
        form.value.order_config.tiers = [];
        form.value.order_config.addons = [];
        fetchCommonConfigs(form.value.artist_type);
      }
      function onOrderTypeChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.order_type = orderTypes[idx].value;
        if (form.value.order_type === 1) {
          form.value.order_config.extra.shipping.mode = "separate";
          form.value.order_config.extra.shipping.unified_date = "";
        }
      }
      function onChallengeTypeChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.challenge_type = challengeTypes[idx].value;
      }
      function onServiceSceneSwitch(e2) {
        form.value.service_scene = e2.detail.value ? 2 : 1;
      }
      function onFinishingChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.order_config.extra.finishing_method = finishingMethods[idx].value;
      }
      function onShippingChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.order_config.extra.shipping.mode = shippingOptions[idx].value;
        if (shippingOptions[idx].value === "unified") {
          form.value.order_config.extra.shipping.separate_days_before_start = 0;
        } else {
          form.value.order_config.extra.shipping.unified_date = "";
        }
      }
      async function fetchCommonConfigs(artistType) {
        var _a, _b, _c, _d;
        try {
          const typeParam = artistType === 2 ? 1 : 0;
          const tRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-tiers?type=${typeParam}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_a = tRes.data) == null ? void 0 : _a.status).toLowerCase() === "success") {
            tierOptions.value = [
              { label: "添加空白档位", value: "blank" },
              ...(((_b = tRes.data) == null ? void 0 : _b.data) || []).map((it2) => ({ label: it2.title, value: it2 }))
            ];
          }
          const aRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-addons?type=${typeParam}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_c = aRes.data) == null ? void 0 : _c.status).toLowerCase() === "success") {
            addonOptions.value = [
              { label: "添加空白加购", value: "blank" },
              ...(((_d = aRes.data) == null ? void 0 : _d.data) || []).map((it2) => ({ label: it2.title, value: it2 }))
            ];
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-artist.vue:477", "获取默认配置失败：", err);
        }
      }
      function onTierPickerChange(e2) {
        const idx = Number(e2.detail.value || 0);
        const opt = tierOptions.value[idx];
        if (!opt)
          return;
        if (opt.value === "blank") {
          form.value.order_config.tiers.push({ title: "", price: 0, description: "" });
        } else {
          form.value.order_config.tiers.push({
            title: opt.value.title || "",
            price: toFixed2(opt.value.price || 0),
            description: opt.value.description || ""
          });
        }
      }
      function removeTier(i2) {
        if (form.value.order_config.tiers.length > 1)
          form.value.order_config.tiers.splice(i2, 1);
      }
      function onAddonPickerChange(e2) {
        const idx = Number(e2.detail.value || 0);
        const opt = addonOptions.value[idx];
        if (!opt)
          return;
        if (opt.value === "blank") {
          form.value.order_config.addons.push({ title: "", price: 0, description: "" });
        } else {
          form.value.order_config.addons.push({
            title: opt.value.title || "",
            price: toFixed2(opt.value.price || 0),
            description: opt.value.description || ""
          });
        }
      }
      function removeAddon(i2) {
        if (form.value.order_config.addons.length > 1)
          form.value.order_config.addons.splice(i2, 1);
      }
      function isSizeSelected(size) {
        return form.value.order_config.extra.size_surcharges.some((x) => x.size === size);
      }
      function handleSizeTap(size) {
        const arr = form.value.order_config.extra.size_surcharges;
        const exists = arr.find((x) => x.size === size);
        if (exists) {
          uni.showToast({ title: "已选择该尺寸", icon: "none" });
          return;
        }
        arr.push({ size, price: 0 });
      }
      function onSizePriceBlur(idx, val) {
        form.value.order_config.extra.size_surcharges[idx].price = toFixed2(val);
      }
      function removeSize(idx) {
        form.value.order_config.extra.size_surcharges.splice(idx, 1);
      }
      async function chooseAndUpload() {
        try {
          const remaining = 9 - form.value.images.length;
          if (remaining <= 0)
            return;
          const tempFilePaths = await chooseImageList(remaining);
          if (!tempFilePaths || tempFilePaths.length === 0)
            return;
          uploading.value = true;
          totalToUpload.value = tempFilePaths.length;
          uploadedCount.value = 0;
          const uploadedUrls = [];
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              const q2 = await getQiniuToken();
              const token = q2.token;
              const basePath = q2.path;
              const filePath = tempFilePaths[i2];
              uploadStatusText.value = `上传中 (${i2 + 1}/${tempFilePaths.length})`;
              uploadedCount.value = i2 + 1;
              const ret = await uploadImageToQiniu(filePath, token, basePath);
              if (ret && ret.imageUrl)
                uploadedUrls.push(ret.imageUrl);
            } catch (err) {
              formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-artist.vue:559", `第 ${i2 + 1} 张上传失败：`, err);
              uni.showToast({ title: `第 ${i2 + 1} 张上传失败`, icon: "none" });
            }
          }
          form.value.images = [...form.value.images, ...uploadedUrls];
          if (uploadedUrls.length > 0)
            uni.showToast({ title: `成功上传 ${uploadedUrls.length} 张`, icon: "success" });
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-artist.vue:566", "选择/上传失败：", e2);
          uni.showToast({ title: "图片上传失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadedCount.value = 0;
          totalToUpload.value = 0;
          uploadStatusText.value = "";
        }
      }
      function removeImage(i2) {
        form.value.images.splice(i2, 1);
      }
      function preview(list, current) {
        uni.previewImage({ urls: list, current });
      }
      async function loadDetail(id) {
        var _a, _b;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plan/info?id=${id}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.message) || "获取详情失败", icon: "none" });
            return;
          }
          const p2 = res.data.data || res.data;
          form.value.id = p2.id;
          form.value.artist_name = p2.artist_name || "";
          form.value.artist_type = p2.artist_type || 1;
          form.value.order_type = p2.order_type || 1;
          form.value.max_participants = p2.max_participants ?? 0;
          form.value.max_submissions_per_user = p2.max_submissions_per_user ?? 1;
          form.value.service_scene = p2.service_scene || 1;
          form.value.require_platform_account = p2.require_platform_account === true || p2.require_platform_account === 1 || p2.require_platform_account === "1";
          form.value.check_showcase = p2.check_showcase || 0;
          form.value.queue_window_sec = p2.queue_window_sec || 60;
          form.value.challenge_type = p2.challenge_type || 1;
          if (p2.open_time) {
            const dt2 = new Date(p2.open_time * 1e3);
            form.value.open_date = `${dt2.getFullYear()}-${pad2(dt2.getMonth() + 1)}-${pad2(dt2.getDate())}`;
            form.value.open_time = `${pad2(dt2.getHours())}:${pad2(dt2.getMinutes())}`;
          }
          if (p2.close_time) {
            const dt2 = new Date(p2.close_time * 1e3);
            form.value.close_date = `${dt2.getFullYear()}-${pad2(dt2.getMonth() + 1)}-${pad2(dt2.getDate())}`;
            form.value.close_time = `${pad2(dt2.getHours())}:${pad2(dt2.getMinutes())}`;
          }
          if (typeof p2.images === "string") {
            form.value.images = p2.images.split(",").map((s2) => s2.trim()).filter(Boolean);
          } else if (Array.isArray(p2.images)) {
            form.value.images = p2.images.map((x) => typeof x === "string" ? x : x.url).filter(Boolean);
          } else {
            form.value.images = [];
          }
          let cfg = {};
          try {
            cfg = p2.order_config ? JSON.parse(p2.order_config) : {};
          } catch (e2) {
            cfg = {};
          }
          form.value.order_config.tiers = Array.isArray(cfg.tiers) ? cfg.tiers.map((t2) => ({
            title: t2.title || "",
            price: toFixed2(t2.price || 0),
            description: t2.description || ""
          })) : [{ title: "", price: 0, description: "" }];
          form.value.order_config.addons = Array.isArray(cfg.addons) ? cfg.addons.map((a2) => ({
            title: a2.title || "",
            price: toFixed2(a2.price || 0),
            description: a2.description || ""
          })) : [{ title: "", price: 0, description: "" }];
          form.value.order_config.extra = Object.assign({
            per_head_cycle_days: 0,
            finishing_method: "water",
            finishing_desc: "",
            shipping: { mode: "separate", unified_date: "", separate_days_before_start: 0 },
            size_surcharges: []
          }, cfg.extra || {});
          if (form.value.order_type === 1) {
            form.value.order_config.extra.shipping.mode = "separate";
            form.value.order_config.extra.shipping.unified_date = "";
          }
          fetchCommonConfigs(form.value.artist_type);
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-artist.vue:637", "加载失败：", err);
          uni.showToast({ title: "加载失败", icon: "none" });
        }
      }
      async function submitPlan() {
        var _a, _b;
        if (!form.value.artist_name)
          return uni.showToast({ title: "请填写计划名称", icon: "none" });
        const openUnix = toUnix(form.value.open_date, form.value.open_time);
        const closeUnix = toUnix(form.value.close_date, form.value.close_time);
        if (!openUnix || !closeUnix)
          return uni.showToast({ title: "请选择开始与结束时间", icon: "none" });
        if (closeUnix <= openUnix)
          return uni.showToast({ title: "结束时间必须晚于开始时间", icon: "none" });
        const payload = {
          id: form.value.id,
          artist_name: form.value.artist_name,
          artist_type: form.value.artist_type,
          order_type: form.value.order_type,
          max_participants: form.value.order_type === 1 ? 0 : form.value.max_participants,
          max_submissions_per_user: form.value.order_type === 1 ? 1 : form.value.max_submissions_per_user,
          open_time: openUnix,
          close_time: closeUnix,
          service_scene: form.value.service_scene,
          require_platform_account: !!form.value.require_platform_account,
          check_showcase: !!form.value.check_showcase,
          queue_window_sec: form.value.queue_window_sec,
          challenge_type: form.value.challenge_type,
          images: form.value.images,
          order_config: {
            tiers: (form.value.order_config.tiers || []).map((t2) => ({
              title: t2.title || "",
              price: toFixed2(t2.price || 0),
              description: t2.description || ""
            })),
            addons: (form.value.order_config.addons || []).map((a2) => ({
              title: a2.title || "",
              price: toFixed2(a2.price || 0),
              description: a2.description || ""
            })),
            extra: {
              per_head_cycle_days: Number(form.value.order_config.extra.per_head_cycle_days || 0),
              finishing_method: form.value.order_config.extra.finishing_method,
              finishing_desc: form.value.order_config.extra.finishing_desc || "",
              shipping: {
                mode: form.value.order_config.extra.shipping.mode,
                unified_date: form.value.order_config.extra.shipping.mode === "unified" ? form.value.order_config.extra.shipping.unified_date || "" : "",
                separate_days_before_start: form.value.order_config.extra.shipping.mode === "separate" ? Number(form.value.order_config.extra.shipping.separate_days_before_start || 0) : 0
              },
              size_surcharges: (form.value.order_config.extra.size_surcharges || []).map((it2) => ({
                size: it2.size,
                price: toFixed2(it2.price || 0)
              }))
            }
          }
        };
        submitting.value = true;
        try {
          const url = websiteUrl.value + (isEditMode.value ? "/brand-manager/order-plan/update" : "/brand-manager/order-plan/add");
          const res = await uni.request({
            url,
            method: "POST",
            data: payload,
            header: { Authorization: getAuthorization(), "Content-Type": "application/json" }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() === "success") {
            uni.showToast({ title: isEditMode.value ? "更新成功" : "添加成功", icon: "success" });
            setTimeout(() => uni.navigateBack(), 800);
          } else {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.message) || "提交失败", icon: "none" });
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-artist.vue:706", "提交失败：", err);
          uni.showToast({ title: "请求失败", icon: "none" });
        } finally {
          submitting.value = false;
        }
      }
      const __returned__ = { artistTypes, orderTypes, challengeTypes, finishingMethods, shippingOptions, allSizes, isEditMode, currentId, submitting, form, artistTypeIndex, orderTypeIndex, challengeTypeIndex, finishingMethodIndex, shippingIndex, showOpenDate, showOpenTime, showCloseDate, showCloseTime, showUnifiedDate, dummyShow, unifiedMinDate, tierOptions, addonOptions, uploading, uploadedCount, totalToUpload, uploadStatusText, pad2, toUnix, displayPrice, toFixed2, getAuthorization, onArtistTypeChange, onOrderTypeChange, onChallengeTypeChange, onServiceSceneSwitch, onFinishingChange, onShippingChange, fetchCommonConfigs, onTierPickerChange, removeTier, onAddonPickerChange, removeAddon, isSizeSelected, handleSizeTap, onSizePriceBlur, removeSize, chooseAndUpload, removeImage, preview, loadDetail, submitPlan, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$9(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_number_box = resolveEasycom(vue.resolveDynamicComponent("uni-number-box"), __easycom_0);
    const _component_common_date_picker = resolveEasycom(vue.resolveDynamicComponent("common-date-picker"), __easycom_1);
    const _component_common_time_picker = resolveEasycom(vue.resolveDynamicComponent("common-time-picker"), __easycom_2);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "order-plan-form" }, [
      vue.createElementVNode("view", { class: "top-panel" }, [
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "计划名称"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.form.artist_name = $event),
              placeholder: "请输入计划名称"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.form.artist_name]
          ])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "接单方式"),
          vue.createElementVNode("picker", {
            range: $setup.orderTypes,
            "range-key": "text",
            value: $setup.orderTypeIndex,
            onChange: $setup.onOrderTypeChange
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker" },
              vue.toDisplayString($setup.orderTypes[$setup.orderTypeIndex].text),
              1
              /* TEXT */
            )
          ], 40, ["value"]),
          vue.createElementVNode("view", { class: "tip" }, " 长期接单和非长期接单是互斥选项，如果存在长期接单计划，将无法创建其它计划。如果存在其它未结束的接单计划，也无法创建长期接单计划。 "),
          $setup.form.order_type === 1 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "tip"
          }, " 长期接单的选项将会常驻在日历中推送。 ")) : (vue.openBlock(), vue.createElementBlock("view", {
            key: 1,
            class: "tip"
          }, " 如需「手速×5、抽选×5」，请创建两条计划；先创建一条，再在列表中“复制”并编辑。 "))
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "接单场所"),
          vue.createElementVNode("view", { class: "inline-control" }, [
            vue.createElementVNode("switch", {
              checked: $setup.form.service_scene === 2,
              onChange: $setup.onServiceSceneSwitch
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "inline-tip" },
              vue.toDisplayString($setup.form.service_scene === 2 ? "本平台" : "其它平台"),
              1
              /* TEXT */
            )
          ])
        ]),
        $setup.form.service_scene === 2 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "是否要求填写社交账号"),
          vue.createElementVNode("view", { class: "inline-control" }, [
            vue.createElementVNode("switch", {
              checked: !!$setup.form.require_platform_account,
              onChange: _cache[1] || (_cache[1] = (e2) => $setup.form.require_platform_account = !!e2.detail.value)
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "inline-tip" },
              vue.toDisplayString($setup.form.require_platform_account ? "需要" : "不需要"),
              1
              /* TEXT */
            )
          ])
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.service_scene === 2 && $setup.form.order_type === 2 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "手速有效作答窗口(秒)"),
          vue.createVNode(_component_uni_number_box, {
            modelValue: $setup.form.queue_window_sec,
            "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.form.queue_window_sec = $event),
            min: 10,
            max: 600
          }, null, 8, ["modelValue"])
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.service_scene === 2 && $setup.form.order_type === 2 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "题目类型"),
          vue.createElementVNode("picker", {
            range: $setup.challengeTypes,
            "range-key": "text",
            value: $setup.challengeTypeIndex,
            onChange: $setup.onChallengeTypeChange
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker" },
              vue.toDisplayString($setup.challengeTypes[$setup.challengeTypeIndex].text),
              1
              /* TEXT */
            )
          ], 40, ["value"])
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.service_scene === 2 && $setup.form.order_type === 3 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "展示柜/私养做门槛"),
          vue.createElementVNode("view", { class: "inline-control" }, [
            vue.createElementVNode("switch", {
              checked: $setup.form.check_showcase === 1,
              onChange: _cache[3] || (_cache[3] = (e2) => $setup.form.check_showcase = e2.detail.value ? 1 : 0)
            }, null, 40, ["checked"]),
            vue.createElementVNode(
              "text",
              { class: "inline-tip" },
              vue.toDisplayString($setup.form.check_showcase ? "是" : "否"),
              1
              /* TEXT */
            )
          ])
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.order_type !== 1 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 4,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "最大参与人数"),
          vue.createVNode(_component_uni_number_box, {
            modelValue: $setup.form.max_participants,
            "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.form.max_participants = $event),
            min: 0,
            max: 1e5
          }, null, 8, ["modelValue"]),
          vue.createElementVNode("view", { class: "tip" }, "0 表示不限量")
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.order_type !== 1 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 5,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "每人最大投递数"),
          vue.createVNode(_component_uni_number_box, {
            modelValue: $setup.form.max_submissions_per_user,
            "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.form.max_submissions_per_user = $event),
            min: 1,
            max: 10
          }, null, 8, ["modelValue"])
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "开始时间"),
          vue.createElementVNode("view", { class: "datetime-row" }, [
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[6] || (_cache[6] = ($event) => $setup.showOpenDate = true)
              },
              vue.toDisplayString($setup.form.open_date || "选择日期"),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[7] || (_cache[7] = ($event) => $setup.showOpenTime = true)
              },
              vue.toDisplayString($setup.form.open_time || "选择时间"),
              1
              /* TEXT */
            )
          ]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showOpenDate,
            "onUpdate:show": _cache[8] || (_cache[8] = ($event) => $setup.showOpenDate = $event),
            modelValue: $setup.form.open_date,
            "onUpdate:modelValue": _cache[9] || (_cache[9] = ($event) => $setup.form.open_date = $event),
            title: "选择开始日期"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_time_picker, {
            show: $setup.showOpenTime,
            "onUpdate:show": _cache[10] || (_cache[10] = ($event) => $setup.showOpenTime = $event),
            modelValue: $setup.form.open_time,
            "onUpdate:modelValue": _cache[11] || (_cache[11] = ($event) => $setup.form.open_time = $event),
            title: "选择开始时间"
          }, null, 8, ["show", "modelValue"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "结束时间"),
          vue.createElementVNode("view", { class: "datetime-row" }, [
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[12] || (_cache[12] = ($event) => $setup.showCloseDate = true)
              },
              vue.toDisplayString($setup.form.close_date || "选择日期"),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[13] || (_cache[13] = ($event) => $setup.showCloseTime = true)
              },
              vue.toDisplayString($setup.form.close_time || "选择时间"),
              1
              /* TEXT */
            )
          ]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showCloseDate,
            "onUpdate:show": _cache[14] || (_cache[14] = ($event) => $setup.showCloseDate = $event),
            modelValue: $setup.form.close_date,
            "onUpdate:modelValue": _cache[15] || (_cache[15] = ($event) => $setup.form.close_date = $event),
            title: "选择结束日期"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_time_picker, {
            show: $setup.showCloseTime,
            "onUpdate:show": _cache[16] || (_cache[16] = ($event) => $setup.showCloseTime = $event),
            modelValue: $setup.form.close_time,
            "onUpdate:modelValue": _cache[17] || (_cache[17] = ($event) => $setup.form.close_time = $event),
            title: "选择结束时间"
          }, null, 8, ["show", "modelValue"])
        ]),
        $setup.form.service_scene === 2 ? (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 6 },
          [
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "label" }, "每颗头妆期（天）"),
              vue.createVNode(_component_uni_number_box, {
                modelValue: $setup.form.order_config.extra.per_head_cycle_days,
                "onUpdate:modelValue": _cache[18] || (_cache[18] = ($event) => $setup.form.order_config.extra.per_head_cycle_days = $event),
                min: 0,
                max: 365
              }, null, 8, ["modelValue"])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "label" }, "定妆方式"),
              vue.createElementVNode("picker", {
                range: $setup.finishingMethods,
                "range-key": "text",
                value: $setup.finishingMethodIndex,
                onChange: $setup.onFinishingChange
              }, [
                vue.createElementVNode(
                  "view",
                  { class: "picker" },
                  vue.toDisplayString($setup.finishingMethods[$setup.finishingMethodIndex].text),
                  1
                  /* TEXT */
                )
              ], 40, ["value"]),
              $setup.form.order_config.extra.finishing_method === "oil" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "tip"
              }, " 打底和定妆将会受到天气湿度的影响，因此原因产生的延期将不计入准时率统计 ")) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "label" }, "定妆方式描述"),
              vue.withDirectives(vue.createElementVNode(
                "input",
                {
                  class: "input",
                  "onUpdate:modelValue": _cache[19] || (_cache[19] = ($event) => $setup.form.order_config.extra.finishing_desc = $event),
                  placeholder: "选填：定妆的补充说明"
                },
                null,
                512
                /* NEED_PATCH */
              ), [
                [vue.vModelText, $setup.form.order_config.extra.finishing_desc]
              ])
            ]),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "label" }, "寄送约定"),
              vue.createElementVNode("picker", {
                range: $setup.shippingOptions,
                "range-key": "text",
                value: $setup.shippingIndex,
                disabled: $setup.form.order_type === 1,
                onChange: $setup.onShippingChange
              }, [
                vue.createElementVNode(
                  "view",
                  { class: "picker" },
                  vue.toDisplayString($setup.shippingOptions[$setup.shippingIndex].text + ($setup.form.order_type === 1 ? "（长期接单固定为分别寄送）" : "")),
                  1
                  /* TEXT */
                )
              ], 40, ["value", "disabled"]),
              $setup.form.order_config.extra.shipping.mode === "unified" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "tip"
              }, " 统一寄送：要求本次投递的单主统一在一个日期之前寄送到（仅可选“接单截止后 ≥10 天”的日期）。 ")) : vue.createCommentVNode("v-if", true),
              $setup.form.order_config.extra.shipping.mode === "unified" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "tip"
              }, " 若使用油性消光，建议统一寄送，便于统一打底；全部订单的时间计算以打底结束开始。 ")) : (vue.openBlock(), vue.createElementBlock("view", {
                key: 2,
                class: "tip"
              }, " 分别寄送：每个单主需在订单开始前的 N 天发出快递（注意是发出，不是寄到），请预留物流时间；未按要求寄出的订单不计入超时计算。 "))
            ]),
            $setup.form.order_config.extra.shipping.mode === "unified" ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "form-item"
            }, [
              vue.createElementVNode("text", { class: "label" }, "统一寄送截止日"),
              vue.createElementVNode(
                "view",
                {
                  class: "picker",
                  onClick: _cache[20] || (_cache[20] = ($event) => $setup.showUnifiedDate = true)
                },
                vue.toDisplayString($setup.form.order_config.extra.shipping.unified_date || "选择日期（≥ 截止后10天）"),
                1
                /* TEXT */
              ),
              vue.createVNode(_component_common_date_picker, {
                show: $setup.showUnifiedDate,
                "onUpdate:show": _cache[21] || (_cache[21] = ($event) => $setup.showUnifiedDate = $event),
                modelValue: $setup.form.order_config.extra.shipping.unified_date,
                "onUpdate:modelValue": _cache[22] || (_cache[22] = ($event) => $setup.form.order_config.extra.shipping.unified_date = $event),
                minDate: $setup.unifiedMinDate,
                title: "选择统一寄送日期"
              }, null, 8, ["show", "modelValue", "minDate"])
            ])) : vue.createCommentVNode("v-if", true),
            $setup.form.order_config.extra.shipping.mode === "separate" ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "form-item"
            }, [
              vue.createElementVNode("text", { class: "label" }, "开始前 N 天发出"),
              vue.createVNode(_component_uni_number_box, {
                modelValue: $setup.form.order_config.extra.shipping.separate_days_before_start,
                "onUpdate:modelValue": _cache[23] || (_cache[23] = ($event) => $setup.form.order_config.extra.shipping.separate_days_before_start = $event),
                min: 0,
                max: 60
              }, null, 8, ["modelValue"])
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "form-item" }, [
              vue.createElementVNode("text", { class: "label" }, "接妆尺寸"),
              vue.createElementVNode("view", { class: "size-row" }, [
                (vue.openBlock(), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.allSizes, (s2) => {
                    return vue.createElementVNode("view", {
                      key: s2,
                      class: vue.normalizeClass(["size-tag", { active: $setup.isSizeSelected(s2) }]),
                      onClick: ($event) => $setup.handleSizeTap(s2)
                    }, vue.toDisplayString(s2), 11, ["onClick"]);
                  }),
                  64
                  /* STABLE_FRAGMENT */
                ))
              ]),
              vue.createElementVNode("view", { class: "tip" }, "请选择你本次接单的尺寸范围，以及每个尺寸基于基础妆费需要加的价格。")
            ]),
            $setup.form.order_config.extra.size_surcharges.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 2,
              class: "size-price-list"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.form.order_config.extra.size_surcharges, (it2, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "size-price-item",
                    key: it2.size
                  }, [
                    vue.createElementVNode(
                      "text",
                      { class: "sp-label" },
                      vue.toDisplayString(it2.size) + " 加价 +",
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode("input", {
                      class: "sp-input",
                      type: "digit",
                      value: $setup.displayPrice(it2.price),
                      onBlur: (e2) => $setup.onSizePriceBlur(idx, e2.detail.value),
                      placeholder: "0.00"
                    }, null, 40, ["value", "onBlur"]),
                    vue.createVNode(_component_uni_icons, {
                      type: "trash",
                      color: "#f56c6c",
                      size: "20",
                      onClick: ($event) => $setup.removeSize(idx)
                    }, null, 8, ["onClick"])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : vue.createCommentVNode("v-if", true)
          ],
          64
          /* STABLE_FRAGMENT */
        )) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode(
            "text",
            { class: "label" },
            vue.toDisplayString($setup.form.artist_type === 2 ? "毛则图片" : "妆则图片"),
            1
            /* TEXT */
          ),
          vue.createElementVNode("view", { class: "images" }, [
            $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "uploading-container"
            }, [
              vue.createVNode(_component_uni_load_more, { status: "loading" }),
              vue.createElementVNode(
                "text",
                { class: "uploading-text" },
                vue.toDisplayString($setup.uploadStatusText || `上传中 (${$setup.uploadedCount}/${$setup.totalToUpload})`),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.form.images, (img, i2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: i2,
                  class: "img-wrap"
                }, [
                  vue.createElementVNode("image", {
                    src: img,
                    mode: "aspectFill",
                    class: "img",
                    onClick: ($event) => $setup.preview($setup.form.images, i2)
                  }, null, 8, ["src", "onClick"]),
                  vue.createVNode(_component_uni_icons, {
                    class: "del",
                    type: "close",
                    size: "18",
                    color: "#fff",
                    onClick: ($event) => $setup.removeImage(i2)
                  }, null, 8, ["onClick"])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.form.images.length < 9 && !$setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "img-add",
              onClick: $setup.chooseAndUpload
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plusempty",
                size: "28",
                color: "#999"
              }),
              vue.createElementVNode("text", null, "添加")
            ])) : vue.createCommentVNode("v-if", true)
          ])
        ])
      ]),
      vue.createElementVNode("view", { class: "form-item" }, [
        vue.createElementVNode("view", { class: "label-row" }, [
          vue.createElementVNode("text", { class: "label" }, "档位配置"),
          vue.createElementVNode("picker", {
            range: $setup.tierOptions,
            "range-key": "label",
            onChange: $setup.onTierPickerChange
          }, [
            vue.createElementVNode("button", { class: "btn-mini" }, "+ 添加档位")
          ], 40, ["range"])
        ]),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.form.order_config.tiers, (tier, i2) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              class: "card",
              key: i2
            }, [
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => tier.title = $event,
                  placeholder: "档位名称"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, tier.title]
                ])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.createElementVNode("input", {
                  class: "input",
                  type: "digit",
                  value: $setup.displayPrice(tier.price),
                  onBlur: (e2) => tier.price = $setup.toFixed2(e2.detail.value),
                  placeholder: "价格(0.00)"
                }, null, 40, ["value", "onBlur"])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => tier.description = $event,
                  placeholder: "描述"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, tier.description]
                ])
              ]),
              $setup.form.order_config.tiers.length > 1 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "row row-right"
              }, [
                vue.createElementVNode("button", {
                  class: "btn-danger",
                  onClick: ($event) => $setup.removeTier(i2)
                }, "删除", 8, ["onClick"])
              ])) : vue.createCommentVNode("v-if", true)
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createElementVNode("view", { class: "form-item" }, [
        vue.createElementVNode("view", { class: "label-row" }, [
          vue.createElementVNode("text", { class: "label" }, "加购配置"),
          vue.createElementVNode("picker", {
            range: $setup.addonOptions,
            "range-key": "label",
            onChange: $setup.onAddonPickerChange
          }, [
            vue.createElementVNode("button", { class: "btn-mini" }, "+ 添加加购")
          ], 40, ["range"])
        ]),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.form.order_config.addons, (addon, i2) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              class: "card",
              key: i2
            }, [
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => addon.title = $event,
                  placeholder: "加购名称"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, addon.title]
                ])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.createElementVNode("input", {
                  class: "input",
                  type: "digit",
                  value: $setup.displayPrice(addon.price),
                  onBlur: (e2) => addon.price = $setup.toFixed2(e2.detail.value),
                  placeholder: "价格(0.00)"
                }, null, 40, ["value", "onBlur"])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => addon.description = $event,
                  placeholder: "描述"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, addon.description]
                ])
              ]),
              $setup.form.order_config.addons.length > 1 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "row row-right"
              }, [
                vue.createElementVNode("button", {
                  class: "btn-danger",
                  onClick: ($event) => $setup.removeAddon(i2)
                }, "删除", 8, ["onClick"])
              ])) : vue.createCommentVNode("v-if", true)
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createElementVNode("view", { class: "submit-box" }, [
        vue.createElementVNode("button", {
          class: "btn-submit",
          disabled: $setup.submitting,
          onClick: $setup.submitPlan
        }, vue.toDisplayString($setup.submitting ? "提交中..." : $setup.isEditMode ? "保存修改" : "提交新计划"), 9, ["disabled"])
      ]),
      vue.createVNode(_component_common_date_picker, {
        show: $setup.dummyShow,
        "onUpdate:show": _cache[24] || (_cache[24] = ($event) => $setup.dummyShow = $event)
      }, null, 8, ["show"])
    ]);
  }
  const PkgCreatorCreatorBaseOrderPlaneFormArtist = /* @__PURE__ */ _export_sfc(_sfc_main$a, [["render", _sfc_render$9], ["__scopeId", "data-v-7f07b742"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/order_plane/form-artist.vue"]]);
  const _sfc_main$9 = {
    __name: "form-wig",
    setup(__props, { expose: __expose }) {
      __expose();
      const orderTypes = [
        { value: 1, text: "长期接单" },
        { value: 2, text: "限时手速" },
        { value: 3, text: "限时抽选" },
        { value: 4, text: "限时黑箱卡" },
        { value: 9, text: "关闭投递" }
      ];
      const allSizes = ["一分", "二分", "三分", "四分", "五分", "六分", "八分", "十二分"];
      const isEditMode = vue.ref(false);
      const currentId = vue.ref(null);
      const submitting = vue.ref(false);
      const sizeSurcharges = vue.ref([]);
      const form = vue.ref({
        id: null,
        artist_name: "",
        artist_type: 2,
        // 固定毛娘
        order_type: 1,
        max_participants: 0,
        max_submissions_per_user: 1,
        open_date: "",
        open_time: "",
        close_date: "",
        close_time: "",
        service_scene: 1,
        // 固定其它平台
        images: [],
        order_config: {
          tiers: [],
          addons: [],
          extra: {
            // 仅携带尺寸附加
            size_surcharges: []
            // [{size:'一分', price: 0}]
          }
        }
      });
      const orderTypeIndex = vue.computed(
        () => Math.max(0, orderTypes.findIndex((x) => x.value === form.value.order_type))
      );
      const showOpenDate = vue.ref(false);
      const showOpenTime = vue.ref(false);
      const showCloseDate = vue.ref(false);
      const showCloseTime = vue.ref(false);
      const dummyShow = vue.ref(false);
      const tierOptions = vue.ref([{ label: "添加空白档位", value: "blank" }]);
      const addonOptions = vue.ref([{ label: "添加空白加购", value: "blank" }]);
      const uploading = vue.ref(false);
      const uploadedCount = vue.ref(0);
      const totalToUpload = vue.ref(0);
      const uploadStatusText = vue.ref("");
      onLoad((query) => {
        if (query && query.id) {
          isEditMode.value = true;
          currentId.value = Number(query.id);
          loadDetail(currentId.value);
        } else {
          isEditMode.value = false;
          currentId.value = null;
        }
        const channel = getOpenerEventChannel && getOpenerEventChannel();
        if (channel) {
          channel.on && channel.on("copyFromPlan", (plan) => {
            isEditMode.value = false;
            currentId.value = null;
            form.value.id = null;
            form.value.artist_name = plan.artist_name || "";
            form.value.artist_type = 2;
            form.value.order_type = plan.order_type || 1;
            form.value.max_participants = plan.max_participants ?? 0;
            form.value.max_submissions_per_user = plan.max_submissions_per_user ?? 1;
            form.value.open_date = "";
            form.value.open_time = "";
            form.value.close_date = "";
            form.value.close_time = "";
            form.value.service_scene = 1;
            form.value.images = Array.isArray(plan.images) ? [...plan.images] : [];
            let tiers = [];
            let addons = [];
            try {
              const cfg = plan.order_config || {};
              tiers = Array.isArray(cfg.tiers) ? cfg.tiers.map((t2) => ({
                title: t2.title || "",
                price: toFixed2(t2.price || 0),
                description: t2.description || ""
              })) : [{ title: "", price: 0, description: "" }];
              addons = Array.isArray(cfg.addons) ? cfg.addons.map((a2) => ({
                title: a2.title || "",
                price: toFixed2(a2.price || 0),
                description: a2.description || ""
              })) : [{ title: "", price: 0, description: "" }];
            } catch (e2) {
            }
            form.value.order_config.tiers = tiers;
            form.value.order_config.addons = addons;
            sizeSurcharges.value = [];
            if (plan.order_config && plan.order_config.extra && Array.isArray(plan.order_config.extra.size_surcharges)) {
              sizeSurcharges.value = plan.order_config.extra.size_surcharges.map((it2) => ({
                size: it2.size,
                price: toFixed2(it2.price || 0)
              }));
            }
            uni.showToast({ title: "已复制到新增表单", icon: "success" });
          });
        }
      });
      vue.onMounted(() => {
        fetchCommonConfigs();
        uni.setNavigationBarTitle({ title: "毛娘订单管理" });
      });
      const pad2 = (n2) => n2 < 10 ? "0" + n2 : "" + n2;
      function toUnix(dateStr, timeStr) {
        if (!dateStr || !timeStr)
          return 0;
        const ts2 = (/* @__PURE__ */ new Date(`${dateStr} ${timeStr}`)).getTime();
        if (Number.isNaN(ts2))
          return 0;
        return Math.floor(ts2 / 1e3);
      }
      function displayPrice(v2) {
        const num = Number(v2);
        if (Number.isNaN(num))
          return "";
        return (Math.round(num * 100) / 100).toFixed(2);
      }
      function toFixed2(v2) {
        const num = parseFloat(String(v2).replace(/[^\d.]/g, ""));
        if (Number.isNaN(num))
          return 0;
        return Math.round(num * 100) / 100;
      }
      function getAuthorization() {
        return uni.getStorageSync("token") || "";
      }
      function onOrderTypeChange(e2) {
        const idx = Number(e2.detail.value || 0);
        form.value.order_type = orderTypes[idx].value;
      }
      async function fetchCommonConfigs() {
        var _a, _b, _c, _d;
        try {
          const typeParam = 1;
          const tRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-tiers?type=${typeParam}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_a = tRes.data) == null ? void 0 : _a.status).toLowerCase() === "success") {
            tierOptions.value = [
              { label: "添加空白档位", value: "blank" },
              ...(((_b = tRes.data) == null ? void 0 : _b.data) || []).map((it2) => ({ label: it2.title, value: it2 }))
            ];
          }
          const aRes = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plane/common-addons?type=${typeParam}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_c = aRes.data) == null ? void 0 : _c.status).toLowerCase() === "success") {
            addonOptions.value = [
              { label: "添加空白加购", value: "blank" },
              ...(((_d = aRes.data) == null ? void 0 : _d.data) || []).map((it2) => ({ label: it2.title, value: it2 }))
            ];
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-wig.vue:363", "获取默认配置失败：", err);
        }
      }
      function onTierPickerChange(e2) {
        const idx = Number(e2.detail.value || 0);
        const opt = tierOptions.value[idx];
        if (!opt)
          return;
        if (opt.value === "blank") {
          form.value.order_config.tiers.push({ title: "", price: 0, description: "" });
        } else {
          form.value.order_config.tiers.push({
            title: opt.value.title || "",
            price: toFixed2(opt.value.price || 0),
            description: opt.value.description || ""
          });
        }
      }
      function removeTier(i2) {
        if (form.value.order_config.tiers.length > 1)
          form.value.order_config.tiers.splice(i2, 1);
      }
      function onAddonPickerChange(e2) {
        const idx = Number(e2.detail.value || 0);
        const opt = addonOptions.value[idx];
        if (!opt)
          return;
        if (opt.value === "blank") {
          form.value.order_config.addons.push({ title: "", price: 0, description: "" });
        } else {
          form.value.order_config.addons.push({
            title: opt.value.title || "",
            price: toFixed2(opt.value.price || 0),
            description: opt.value.description || ""
          });
        }
      }
      function removeAddon(i2) {
        if (form.value.order_config.addons.length > 1)
          form.value.order_config.addons.splice(i2, 1);
      }
      function isSizeSelected(size) {
        return sizeSurcharges.value.some((x) => x.size === size);
      }
      function handleSizeTap(size) {
        const arr = sizeSurcharges.value;
        const exists = arr.find((x) => x.size === size);
        if (exists) {
          uni.showToast({ title: "已选择该尺寸", icon: "none" });
          return;
        }
        arr.push({ size, price: 0 });
      }
      function onSizePriceBlur(idx, val) {
        sizeSurcharges.value[idx].price = toFixed2(val);
      }
      function removeSize(idx) {
        sizeSurcharges.value.splice(idx, 1);
      }
      async function chooseAndUpload() {
        try {
          const remaining = 9 - form.value.images.length;
          if (remaining <= 0)
            return;
          const tempFilePaths = await chooseImageList(remaining);
          if (!tempFilePaths || tempFilePaths.length === 0)
            return;
          uploading.value = true;
          totalToUpload.value = tempFilePaths.length;
          uploadedCount.value = 0;
          const uploadedUrls = [];
          for (let i2 = 0; i2 < tempFilePaths.length; i2++) {
            try {
              const q2 = await getQiniuToken();
              const token = q2.token;
              const basePath = q2.path;
              const filePath = tempFilePaths[i2];
              uploadStatusText.value = `上传中 (${i2 + 1}/${tempFilePaths.length})`;
              uploadedCount.value = i2 + 1;
              const ret = await uploadImageToQiniu(filePath, token, basePath);
              if (ret && ret.imageUrl)
                uploadedUrls.push(ret.imageUrl);
            } catch (err) {
              formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-wig.vue:444", `第 ${i2 + 1} 张上传失败：`, err);
              uni.showToast({ title: `第 ${i2 + 1} 张上传失败`, icon: "none" });
            }
          }
          form.value.images = [...form.value.images, ...uploadedUrls];
          if (uploadedUrls.length > 0)
            uni.showToast({ title: `成功上传 ${uploadedUrls.length} 张`, icon: "success" });
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-wig.vue:451", "选择/上传失败：", e2);
          uni.showToast({ title: "图片上传失败", icon: "none" });
        } finally {
          uploading.value = false;
          uploadedCount.value = 0;
          totalToUpload.value = 0;
          uploadStatusText.value = "";
        }
      }
      function removeImage(i2) {
        form.value.images.splice(i2, 1);
      }
      function preview(list, current) {
        uni.previewImage({ urls: list, current });
      }
      async function loadDetail(id) {
        var _a, _b, _c;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-manager/order-plan/info?id=${id}`,
            method: "GET",
            header: { Authorization: getAuthorization() }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.message) || "获取详情失败", icon: "none" });
            return;
          }
          const p2 = res.data.data || res.data;
          form.value.id = p2.id;
          form.value.artist_name = p2.artist_name || "";
          form.value.artist_type = 2;
          form.value.order_type = p2.order_type || 1;
          form.value.max_participants = p2.max_participants ?? 0;
          form.value.max_submissions_per_user = p2.max_submissions_per_user ?? 1;
          form.value.service_scene = 1;
          if (p2.open_time) {
            const dt2 = new Date(p2.open_time * 1e3);
            form.value.open_date = `${dt2.getFullYear()}-${pad2(dt2.getMonth() + 1)}-${pad2(dt2.getDate())}`;
            form.value.open_time = `${pad2(dt2.getHours())}:${pad2(dt2.getMinutes())}`;
          }
          if (p2.close_time) {
            const dt2 = new Date(p2.close_time * 1e3);
            form.value.close_date = `${dt2.getFullYear()}-${pad2(dt2.getMonth() + 1)}-${pad2(dt2.getDate())}`;
            form.value.close_time = `${pad2(dt2.getHours())}:${pad2(dt2.getMinutes())}`;
          }
          if (typeof p2.images === "string") {
            form.value.images = p2.images.split(",").map((s2) => s2.trim()).filter(Boolean);
          } else if (Array.isArray(p2.images)) {
            form.value.images = p2.images.map((x) => typeof x === "string" ? x : x.url).filter(Boolean);
          } else {
            form.value.images = [];
          }
          let cfg = {};
          try {
            cfg = p2.order_config ? JSON.parse(p2.order_config) : {};
          } catch (e2) {
            cfg = {};
          }
          form.value.order_config.tiers = Array.isArray(cfg.tiers) ? cfg.tiers.map((t2) => ({
            title: t2.title || "",
            price: toFixed2(t2.price || 0),
            description: t2.description || ""
          })) : [{ title: "", price: 0, description: "" }];
          form.value.order_config.addons = Array.isArray(cfg.addons) ? cfg.addons.map((a2) => ({
            title: a2.title || "",
            price: toFixed2(a2.price || 0),
            description: a2.description || ""
          })) : [{ title: "", price: 0, description: "" }];
          sizeSurcharges.value = Array.isArray((_c = cfg == null ? void 0 : cfg.extra) == null ? void 0 : _c.size_surcharges) ? cfg.extra.size_surcharges.map((it2) => ({ size: it2.size, price: toFixed2(it2.price || 0) })) : [];
          fetchCommonConfigs();
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-wig.vue:515", "加载失败：", err);
          uni.showToast({ title: "加载失败", icon: "none" });
        }
      }
      async function submitPlan() {
        var _a, _b;
        if (!form.value.artist_name)
          return uni.showToast({ title: "请填写计划名称", icon: "none" });
        const openUnix = toUnix(form.value.open_date, form.value.open_time);
        const closeUnix = toUnix(form.value.close_date, form.value.close_time);
        if (!openUnix || !closeUnix)
          return uni.showToast({ title: "请选择开始与结束时间", icon: "none" });
        if (closeUnix <= openUnix)
          return uni.showToast({ title: "结束时间必须晚于开始时间", icon: "none" });
        const payload = {
          id: form.value.id,
          artist_name: form.value.artist_name,
          artist_type: 2,
          order_type: form.value.order_type,
          max_participants: form.value.order_type === 1 ? 0 : form.value.max_participants,
          max_submissions_per_user: form.value.order_type === 1 ? 1 : form.value.max_submissions_per_user,
          open_time: openUnix,
          close_time: closeUnix,
          service_scene: 1,
          // 固定其它平台
          images: form.value.images,
          order_config: {
            tiers: (form.value.order_config.tiers || []).map((t2) => ({
              title: t2.title || "",
              price: toFixed2(t2.price || 0),
              description: t2.description || ""
            })),
            addons: (form.value.order_config.addons || []).map((a2) => ({
              title: a2.title || "",
              price: toFixed2(a2.price || 0),
              description: a2.description || ""
            })),
            extra: {
              size_surcharges: sizeSurcharges.value.map((it2) => ({
                size: it2.size,
                price: toFixed2(it2.price || 0)
              }))
            }
          }
        };
        submitting.value = true;
        try {
          const url = websiteUrl.value + (isEditMode.value ? "/brand-manager/order-plan/update" : "/brand-manager/order-plan/add");
          const res = await uni.request({
            url,
            method: "POST",
            data: payload,
            header: { Authorization: getAuthorization(), "Content-Type": "application/json" }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() === "success") {
            uni.showToast({ title: isEditMode.value ? "更新成功" : "添加成功", icon: "success" });
            setTimeout(() => uni.navigateBack(), 800);
          } else {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.message) || "提交失败", icon: "none" });
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_base/order_plane/form-wig.vue:569", "提交失败：", err);
          uni.showToast({ title: "请求失败", icon: "none" });
        } finally {
          submitting.value = false;
        }
      }
      const __returned__ = { orderTypes, allSizes, isEditMode, currentId, submitting, sizeSurcharges, form, orderTypeIndex, showOpenDate, showOpenTime, showCloseDate, showCloseTime, dummyShow, tierOptions, addonOptions, uploading, uploadedCount, totalToUpload, uploadStatusText, pad2, toUnix, displayPrice, toFixed2, getAuthorization, onOrderTypeChange, fetchCommonConfigs, onTierPickerChange, removeTier, onAddonPickerChange, removeAddon, isSizeSelected, handleSizeTap, onSizePriceBlur, removeSize, chooseAndUpload, removeImage, preview, loadDetail, submitPlan, ref: vue.ref, computed: vue.computed, onMounted: vue.onMounted, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$8(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_number_box = resolveEasycom(vue.resolveDynamicComponent("uni-number-box"), __easycom_0);
    const _component_common_date_picker = resolveEasycom(vue.resolveDynamicComponent("common-date-picker"), __easycom_1);
    const _component_common_time_picker = resolveEasycom(vue.resolveDynamicComponent("common-time-picker"), __easycom_2);
    const _component_uni_load_more = resolveEasycom(vue.resolveDynamicComponent("uni-load-more"), __easycom_3);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "order-plan-form" }, [
      vue.createElementVNode("view", { class: "top-panel" }, [
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "计划名称"),
          vue.withDirectives(vue.createElementVNode(
            "input",
            {
              class: "input",
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.form.artist_name = $event),
              placeholder: "请输入计划名称"
            },
            null,
            512
            /* NEED_PATCH */
          ), [
            [vue.vModelText, $setup.form.artist_name]
          ])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "作者类型"),
          vue.createElementVNode("view", { class: "readonly-field" }, [
            vue.createElementVNode("text", { class: "readonly-text" }, "毛娘")
          ])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "接单方式"),
          vue.createElementVNode("picker", {
            range: $setup.orderTypes,
            "range-key": "text",
            value: $setup.orderTypeIndex,
            onChange: $setup.onOrderTypeChange
          }, [
            vue.createElementVNode(
              "view",
              { class: "picker" },
              vue.toDisplayString($setup.orderTypes[$setup.orderTypeIndex].text),
              1
              /* TEXT */
            )
          ], 40, ["value"]),
          vue.createElementVNode("view", { class: "tip" }, " 如果本次开单包含不同接单方式如：手速×5、抽选×5，需要创建两条计划；先创建一条，再在列表中“复制”并编辑。 ")
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "接单场所"),
          vue.createElementVNode("view", { class: "readonly-field" }, [
            vue.createElementVNode("text", { class: "readonly-text" }, "其它平台")
          ]),
          vue.createElementVNode("view", { class: "tip" }, "暂时不支持在线约毛，请等待下一版本支持")
        ]),
        $setup.form.order_type !== 1 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "最大参与人数"),
          vue.createVNode(_component_uni_number_box, {
            modelValue: $setup.form.max_participants,
            "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.form.max_participants = $event),
            min: 0,
            max: 1e5
          }, null, 8, ["modelValue"]),
          vue.createElementVNode("view", { class: "tip" }, "0 表示不限量")
        ])) : vue.createCommentVNode("v-if", true),
        $setup.form.order_type !== 1 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "form-item"
        }, [
          vue.createElementVNode("text", { class: "label" }, "每人最大投递数"),
          vue.createVNode(_component_uni_number_box, {
            modelValue: $setup.form.max_submissions_per_user,
            "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.form.max_submissions_per_user = $event),
            min: 1,
            max: 10
          }, null, 8, ["modelValue"])
        ])) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "开始时间"),
          vue.createElementVNode("view", { class: "datetime-row" }, [
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[3] || (_cache[3] = ($event) => $setup.showOpenDate = true)
              },
              vue.toDisplayString($setup.form.open_date || "选择日期"),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[4] || (_cache[4] = ($event) => $setup.showOpenTime = true)
              },
              vue.toDisplayString($setup.form.open_time || "选择时间"),
              1
              /* TEXT */
            )
          ]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showOpenDate,
            "onUpdate:show": _cache[5] || (_cache[5] = ($event) => $setup.showOpenDate = $event),
            modelValue: $setup.form.open_date,
            "onUpdate:modelValue": _cache[6] || (_cache[6] = ($event) => $setup.form.open_date = $event),
            title: "选择开始日期"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_time_picker, {
            show: $setup.showOpenTime,
            "onUpdate:show": _cache[7] || (_cache[7] = ($event) => $setup.showOpenTime = $event),
            modelValue: $setup.form.open_time,
            "onUpdate:modelValue": _cache[8] || (_cache[8] = ($event) => $setup.form.open_time = $event),
            title: "选择开始时间"
          }, null, 8, ["show", "modelValue"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "结束时间"),
          vue.createElementVNode("view", { class: "datetime-row" }, [
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[9] || (_cache[9] = ($event) => $setup.showCloseDate = true)
              },
              vue.toDisplayString($setup.form.close_date || "选择日期"),
              1
              /* TEXT */
            ),
            vue.createElementVNode(
              "view",
              {
                class: "picker-col",
                onClick: _cache[10] || (_cache[10] = ($event) => $setup.showCloseTime = true)
              },
              vue.toDisplayString($setup.form.close_time || "选择时间"),
              1
              /* TEXT */
            )
          ]),
          vue.createVNode(_component_common_date_picker, {
            show: $setup.showCloseDate,
            "onUpdate:show": _cache[11] || (_cache[11] = ($event) => $setup.showCloseDate = $event),
            modelValue: $setup.form.close_date,
            "onUpdate:modelValue": _cache[12] || (_cache[12] = ($event) => $setup.form.close_date = $event),
            title: "选择结束日期"
          }, null, 8, ["show", "modelValue"]),
          vue.createVNode(_component_common_time_picker, {
            show: $setup.showCloseTime,
            "onUpdate:show": _cache[13] || (_cache[13] = ($event) => $setup.showCloseTime = $event),
            modelValue: $setup.form.close_time,
            "onUpdate:modelValue": _cache[14] || (_cache[14] = ($event) => $setup.form.close_time = $event),
            title: "选择结束时间"
          }, null, 8, ["show", "modelValue"])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "毛则图片"),
          vue.createElementVNode("view", { class: "images" }, [
            $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "uploading-container"
            }, [
              vue.createVNode(_component_uni_load_more, { status: "loading" }),
              vue.createElementVNode(
                "text",
                { class: "uploading-text" },
                vue.toDisplayString($setup.uploadStatusText || `上传中 (${$setup.uploadedCount}/${$setup.totalToUpload})`),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.form.images, (img, i2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: i2,
                  class: "img-wrap"
                }, [
                  vue.createElementVNode("image", {
                    src: img,
                    mode: "aspectFill",
                    class: "img",
                    onClick: ($event) => $setup.preview($setup.form.images, i2)
                  }, null, 8, ["src", "onClick"]),
                  vue.createVNode(_component_uni_icons, {
                    class: "del",
                    type: "close",
                    size: "18",
                    color: "#fff",
                    onClick: ($event) => $setup.removeImage(i2)
                  }, null, 8, ["onClick"])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.form.images.length < 9 && !$setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "img-add",
              onClick: $setup.chooseAndUpload
            }, [
              vue.createVNode(_component_uni_icons, {
                type: "plusempty",
                size: "28",
                color: "#999"
              }),
              vue.createElementVNode("text", null, "添加")
            ])) : vue.createCommentVNode("v-if", true)
          ])
        ]),
        vue.createElementVNode("view", { class: "form-item" }, [
          vue.createElementVNode("text", { class: "label" }, "接妆尺寸"),
          vue.createElementVNode("view", { class: "size-row" }, [
            (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.allSizes, (s2) => {
                return vue.createElementVNode("view", {
                  key: s2,
                  class: vue.normalizeClass(["size-tag", { active: $setup.isSizeSelected(s2) }]),
                  onClick: ($event) => $setup.handleSizeTap(s2)
                }, vue.toDisplayString(s2), 11, ["onClick"]);
              }),
              64
              /* STABLE_FRAGMENT */
            ))
          ]),
          vue.createElementVNode("view", { class: "tip" }, "请选择本次接单的尺寸范围，并为所选尺寸设置加价。")
        ]),
        $setup.sizeSurcharges.length ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "size-price-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.sizeSurcharges, (it2, idx) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: "size-price-item",
                key: it2.size
              }, [
                vue.createElementVNode(
                  "text",
                  { class: "sp-label" },
                  vue.toDisplayString(it2.size) + " 加价 +",
                  1
                  /* TEXT */
                ),
                vue.createElementVNode("input", {
                  class: "sp-input",
                  type: "digit",
                  value: $setup.displayPrice(it2.price),
                  onBlur: (e2) => $setup.onSizePriceBlur(idx, e2.detail.value),
                  placeholder: "0.00"
                }, null, 40, ["value", "onBlur"]),
                vue.createVNode(_component_uni_icons, {
                  type: "trash",
                  color: "#f56c6c",
                  size: "20",
                  onClick: ($event) => $setup.removeSize(idx)
                }, null, 8, ["onClick"])
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : vue.createCommentVNode("v-if", true)
      ]),
      vue.createElementVNode("view", { class: "form-item" }, [
        vue.createElementVNode("view", { class: "label-row" }, [
          vue.createElementVNode("text", { class: "label" }, "档位配置"),
          vue.createElementVNode("picker", {
            range: $setup.tierOptions,
            "range-key": "label",
            onChange: $setup.onTierPickerChange
          }, [
            vue.createElementVNode("button", { class: "btn-mini" }, "+ 添加档位")
          ], 40, ["range"])
        ]),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.form.order_config.tiers, (tier, i2) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              class: "card",
              key: i2
            }, [
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => tier.title = $event,
                  placeholder: "档位名称"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, tier.title]
                ])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.createElementVNode("input", {
                  class: "input",
                  type: "digit",
                  value: $setup.displayPrice(tier.price),
                  onBlur: (e2) => tier.price = $setup.toFixed2(e2.detail.value),
                  placeholder: "价格(0.00)"
                }, null, 40, ["value", "onBlur"])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => tier.description = $event,
                  placeholder: "描述"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, tier.description]
                ])
              ]),
              $setup.form.order_config.tiers.length > 1 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "row row-right"
              }, [
                vue.createElementVNode("button", {
                  class: "btn-danger",
                  onClick: ($event) => $setup.removeTier(i2)
                }, "删除", 8, ["onClick"])
              ])) : vue.createCommentVNode("v-if", true)
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createElementVNode("view", { class: "form-item" }, [
        vue.createElementVNode("view", { class: "label-row" }, [
          vue.createElementVNode("text", { class: "label" }, "加购配置"),
          vue.createElementVNode("picker", {
            range: $setup.addonOptions,
            "range-key": "label",
            onChange: $setup.onAddonPickerChange
          }, [
            vue.createElementVNode("button", { class: "btn-mini" }, "+ 添加加购")
          ], 40, ["range"])
        ]),
        (vue.openBlock(true), vue.createElementBlock(
          vue.Fragment,
          null,
          vue.renderList($setup.form.order_config.addons, (addon, i2) => {
            return vue.openBlock(), vue.createElementBlock("view", {
              class: "card",
              key: i2
            }, [
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => addon.title = $event,
                  placeholder: "加购名称"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, addon.title]
                ])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.createElementVNode("input", {
                  class: "input",
                  type: "digit",
                  value: $setup.displayPrice(addon.price),
                  onBlur: (e2) => addon.price = $setup.toFixed2(e2.detail.value),
                  placeholder: "价格(0.00)"
                }, null, 40, ["value", "onBlur"])
              ]),
              vue.createElementVNode("view", { class: "row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  class: "input",
                  "onUpdate:modelValue": ($event) => addon.description = $event,
                  placeholder: "描述"
                }, null, 8, ["onUpdate:modelValue"]), [
                  [vue.vModelText, addon.description]
                ])
              ]),
              $setup.form.order_config.addons.length > 1 ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "row row-right"
              }, [
                vue.createElementVNode("button", {
                  class: "btn-danger",
                  onClick: ($event) => $setup.removeAddon(i2)
                }, "删除", 8, ["onClick"])
              ])) : vue.createCommentVNode("v-if", true)
            ]);
          }),
          128
          /* KEYED_FRAGMENT */
        ))
      ]),
      vue.createElementVNode("view", { class: "submit-box" }, [
        vue.createElementVNode("button", {
          class: "btn-submit",
          disabled: $setup.submitting,
          onClick: $setup.submitPlan
        }, vue.toDisplayString($setup.submitting ? "提交中..." : $setup.isEditMode ? "保存修改" : "提交新计划"), 9, ["disabled"])
      ]),
      vue.createVNode(_component_common_date_picker, {
        show: $setup.dummyShow,
        "onUpdate:show": _cache[15] || (_cache[15] = ($event) => $setup.dummyShow = $event)
      }, null, 8, ["show"])
    ]);
  }
  const PkgCreatorCreatorBaseOrderPlaneFormWig = /* @__PURE__ */ _export_sfc(_sfc_main$9, [["render", _sfc_render$8], ["__scopeId", "data-v-55f7510a"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_base/order_plane/form-wig.vue"]]);
  const stickyShowThreshold = 140;
  const safeArea = true;
  const _sfc_main$8 = {
    __name: "plane_detail",
    setup(__props, { expose: __expose }) {
      __expose();
      const scrollTop = vue.ref(0);
      const safeTop = vue.ref(0);
      onPageScroll((e2) => {
        scrollTop.value = (e2 == null ? void 0 : e2.scrollTop) || 0;
      });
      const showStickyTitle = vue.computed(() => scrollTop.value > stickyShowThreshold);
      const planId = vue.ref(0);
      const plan = vue.reactive({
        id: 0,
        brand_id: 0,
        artist_name: "",
        artist_type: 0,
        order_type: 1,
        max_participants: null,
        max_submissions_per_user: null,
        open_time: 0,
        close_time: 0,
        images: "",
        order_config: "",
        service_scene: 0,
        capacity: 0,
        challenge_type: 0,
        require_platform_account: false,
        check_showcase: false,
        queue_window_sec: 0,
        artist_info: null,
        brand: null,
        external_url: "",
        is_local: 1,
        tiers: [],
        addons: [],
        extra: {},
        inventory: 0
      });
      const goBack = () => uni.navigateBack({ delta: 1 });
      async function fetchPlan() {
        var _a, _b;
        const res = await uni.request({
          url: `${websiteUrl.value}/brand-artist/order-plan-info`,
          method: "GET",
          data: { id: planId.value }
        });
        if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
          uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败", icon: "none" });
          return;
        }
        const d2 = res.data.data || {};
        plan.id = d2.id || planId.value;
        plan.brand_id = d2.brand_id || 0;
        plan.artist_name = d2.artist_name || "";
        plan.artist_type = d2.artist_type || 0;
        plan.order_type = d2.order_type || 1;
        plan.max_participants = d2.max_participants ?? null;
        plan.max_submissions_per_user = d2.max_submissions_per_user ?? null;
        plan.open_time = Number(d2.open_time || 0);
        plan.close_time = Number(d2.close_time || 0);
        plan.images = d2.images || "";
        plan.order_config = d2.order_config || "";
        plan.service_scene = d2.service_scene || 0;
        plan.capacity = d2.capacity || 0;
        plan.challenge_type = d2.challenge_type || 0;
        plan.require_platform_account = !!d2.require_platform_account;
        plan.check_showcase = !!d2.check_showcase;
        plan.queue_window_sec = Number(d2.queue_window_sec || 0);
        plan.artist_info = d2.artist_info || null;
        plan.brand = d2.brand || null;
        plan.external_url = d2.external_url || "";
        plan.is_local = plan.external_url ? 0 : 1;
        plan.inventory = Number(d2.inventory || 0);
        try {
          if (plan.order_config) {
            const cfg = JSON.parse(plan.order_config);
            plan.tiers = Array.isArray(cfg == null ? void 0 : cfg.tiers) ? cfg.tiers : [];
            plan.addons = Array.isArray(cfg == null ? void 0 : cfg.addons) ? cfg.addons : [];
            plan.extra = (cfg == null ? void 0 : cfg.extra) || {};
          } else {
            plan.tiers = [];
            plan.addons = [];
            plan.extra = {};
          }
        } catch (e2) {
          plan.tiers = [];
          plan.addons = [];
          plan.extra = {};
        }
      }
      const coverUrl = vue.computed(() => {
        var _a, _b;
        const imgs = String(plan.images || "").split(",").map((s2) => s2.trim()).filter(Boolean);
        if (imgs.length)
          return imgs[0];
        if ((_a = plan.artist_info) == null ? void 0 : _a.LogoImage)
          return plan.artist_info.LogoImage;
        if ((_b = plan.brand) == null ? void 0 : _b.logo_image)
          return plan.brand.logo_image;
        return "https://images1.fantuanpu.com/brand-avatar/default";
      });
      const avatarUrl = vue.computed(() => {
        var _a, _b;
        if ((_a = plan.artist_info) == null ? void 0 : _a.LogoImage)
          return plan.artist_info.LogoImage;
        if ((_b = plan.brand) == null ? void 0 : _b.logo_image)
          return plan.brand.logo_image;
        return "https://images1.fantuanpu.com/brand-avatar/default";
      });
      const artistName = vue.computed(
        () => {
          var _a, _b;
          return ((_a = plan.artist_info) == null ? void 0 : _a.BrandName) || ((_b = plan.brand) == null ? void 0 : _b.brand_name) || plan.artist_name || "—";
        }
      );
      const highlightImages = vue.computed(() => {
        var _a, _b;
        const a2 = String(((_a = plan.artist_info) == null ? void 0 : _a.artist_highlight_images) || "").split(",").map((s2) => s2.trim()).filter(Boolean);
        const h2 = String(((_b = plan.artist_info) == null ? void 0 : _b.hairstylist_highlight_images) || "").split(",").map((s2) => s2.trim()).filter(Boolean);
        if (Number(plan.artist_type) === 2)
          return h2.length ? h2 : a2;
        return a2.length ? a2 : h2;
      });
      function previewHL(idx) {
        uni.previewImage({ urls: highlightImages.value, current: idx || 0 });
      }
      const isLocalPlan = vue.computed(() => plan.is_local ? true : false);
      const modeMap = { 1: "常驻投递", 2: "限时手速", 3: "限时抽选", 4: "限时黑箱" };
      const modeName = vue.computed(() => modeMap[Number(plan.order_type)] || "开单");
      const modeClass = vue.computed(() => {
        const m2 = Number(plan.order_type);
        return {
          "mode-direct": m2 === 1,
          "mode-speed": m2 === 2,
          "mode-lottery": m2 === 3,
          "mode-black": m2 === 4
        };
      });
      const isPermanent = vue.computed(() => Number(plan.order_type) === 1 && (!plan.open_time || !plan.close_time));
      const nowSec = () => Math.floor(Date.now() / 1e3);
      const phase = vue.computed(() => {
        if (!isLocalPlan.value)
          return "external";
        if (isPermanent.value)
          return "ongoing";
        const n2 = nowSec();
        if (n2 < plan.open_time)
          return "upcoming";
        if (n2 >= plan.open_time && n2 <= plan.close_time)
          return "ongoing";
        return "ended";
      });
      function fmtTime(ts2) {
        if (!ts2)
          return "--";
        const d2 = new Date(ts2 * 1e3);
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const dd = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0");
        const mi = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${dd} ${hh}:${mi}`;
      }
      function fmtDur(sec) {
        if (sec <= 0)
          return "0秒";
        const d2 = Math.floor(sec / 86400);
        const h2 = Math.floor(sec % 86400 / 3600);
        const m2 = Math.floor(sec % 3600 / 60);
        const s2 = sec % 60;
        const a2 = [];
        if (d2)
          a2.push(`${d2}天`);
        if (h2)
          a2.push(`${h2}小时`);
        if (m2)
          a2.push(`${m2}分`);
        if (!d2 && !h2 && !m2)
          a2.push(`${s2}秒`);
        return a2.join("");
      }
      const startsInText = vue.ref("");
      const endsInText = vue.ref("");
      let timer = null;
      function tick() {
        if (phase.value === "upcoming")
          startsInText.value = fmtDur(Math.max(0, plan.open_time - nowSec()));
        else if (phase.value === "ongoing")
          endsInText.value = fmtDur(Math.max(0, plan.close_time - nowSec()));
        else {
          startsInText.value = "";
          endsInText.value = "";
        }
      }
      function startTimer() {
        stopTimer();
        tick();
        timer = setInterval(tick, 1e3);
      }
      function stopTimer() {
        if (timer) {
          clearInterval(timer);
          timer = null;
        }
      }
      const stockText = vue.computed(() => {
        const inv = Number(plan.inventory ?? 0);
        if (Number.isNaN(inv))
          return "库存 不限";
        if (inv > 0)
          return `库存 ${inv}`;
        if (inv === 0)
          return "库存 0";
        return "库存 不限";
      });
      const perHeadCycleDays = vue.computed(() => {
        var _a;
        const n2 = Number((_a = plan.extra) == null ? void 0 : _a.per_head_cycle_days);
        return Number.isFinite(n2) ? n2 : null;
      });
      const finishingMap = { oil: "油性消光", water: "水性消光", gloss: "罩光剂" };
      const finishingMethodText = vue.computed(() => {
        var _a;
        const m2 = String(((_a = plan.extra) == null ? void 0 : _a.finishing_method) || "").trim();
        return finishingMap[m2] || (m2 ? m2 : "未设置");
      });
      const finishingDescText = vue.computed(() => {
        var _a;
        return String(((_a = plan.extra) == null ? void 0 : _a.finishing_desc) || "").trim();
      });
      const shipping = vue.computed(() => {
        var _a;
        return ((_a = plan.extra) == null ? void 0 : _a.shipping) || {};
      });
      const isUnifiedShipping = vue.computed(() => {
        var _a;
        return ((_a = shipping.value) == null ? void 0 : _a.mode) === "unified";
      });
      const shippingText = vue.computed(() => {
        const s2 = shipping.value;
        if (!s2 || !s2.mode)
          return "";
        if (s2.mode === "unified" && s2.unified_date)
          return `统一寄送：${s2.unified_date}`;
        if (s2.mode === "separate") {
          const n2 = Number(s2.separate_days_before_start || 0);
          return `分批寄送：开始前 ${n2} 天发出`;
        }
        return "";
      });
      const sizeSurcharges = vue.computed(() => {
        var _a;
        const rows = Array.isArray((_a = plan.extra) == null ? void 0 : _a.size_surcharges) ? plan.extra.size_surcharges : [];
        return rows.filter((r2) => r2 && typeof r2.price !== "undefined");
      });
      const sizePriceText = vue.computed(() => {
        if (!sizeSurcharges.value.length)
          return "";
        return sizeSurcharges.value.map((r2) => `${r2.size || "—"} +${Number(r2.price)}`).join(" · ");
      });
      const tierListText = vue.computed(() => {
        var _a;
        if (!((_a = plan.tiers) == null ? void 0 : _a.length))
          return "";
        return plan.tiers.map((t2) => `${t2.title || "—"} ¥${Number(t2.price || 0)}`).join("；");
      });
      const addonListText = vue.computed(() => {
        var _a;
        if (!((_a = plan.addons) == null ? void 0 : _a.length))
          return "";
        return plan.addons.map((a2) => `${a2.title || "—"} +¥${Number(a2.price || 0)}`).join("；");
      });
      function _min(a2) {
        return a2.length ? Math.min(...a2) : 0;
      }
      function _max(a2) {
        return a2.length ? Math.max(...a2) : 0;
      }
      const tierRangeText = vue.computed(() => {
        const tp = (plan.tiers || []).map((t2) => Number(t2.price || 0)).filter((n2) => !isNaN(n2));
        if (!tp.length)
          return "未设置档位";
        const sp = (sizeSurcharges.value || []).map((s2) => Number(s2.price || 0)).filter((n2) => !isNaN(n2));
        const min = _min(tp) + _min(sp);
        const max = _max(tp) + _max(sp);
        return min === max ? `¥${min}` : `¥${min} ~ ¥${max}`;
      });
      const tierRangePriceText = vue.computed(() => tierRangeText.value);
      const btnText = vue.computed(() => {
        if (!isLocalPlan.value)
          return "在其它平台开单";
        if (phase.value === "upcoming")
          return "即将开始";
        if (phase.value === "ongoing")
          return "立即购买";
        if (phase.value === "ended")
          return "已结束";
        return "立即购买";
      });
      const btnDisabled = vue.computed(() => {
        if (!isLocalPlan.value)
          return false;
        return phase.value === "upcoming" || phase.value === "ended";
      });
      const ctaClass = vue.computed(() => ({
        "btn-grey": phase.value === "ended" || btnDisabled.value,
        "btn-warn": phase.value === "upcoming",
        "btn-primary": phase.value === "ongoing" && !btnDisabled.value
      }));
      function goArtistHome() {
        const url = `/pages/artist_info/bjd_faceup_artist?brand_id=${plan.brand_id}`;
        uni.navigateTo({ url });
      }
      async function handleCTA() {
        var _a, _b, _c;
        if (!isLocalPlan.value) {
          if (plan.external_url) {
            uni.setClipboardData({
              data: plan.external_url,
              success: () => uni.showToast({ title: "已复制链接", icon: "none" })
            });
          }
          return;
        }
        if (btnDisabled.value)
          return;
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        try {
          uni.showLoading({ title: "投递中...", mask: true });
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/submit`,
            method: "POST",
            header: {
              Authorization: token,
              "Content-Type": "application/json"
            },
            data: {
              plan_id: plan.id,
              // 这里先用空字符串占位，后续如果有账号/备注输入，再从表单取值
              platform_account: "",
              requirement: ""
            }
          });
          uni.hideLoading();
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.msg) || "投递失败", icon: "none" });
            return;
          }
          const data = ((_c = res.data) == null ? void 0 : _c.data) || {};
          const code = data.code;
          const counter = data.brand_counter;
          let msg = "";
          if (code === 1) {
            msg = "投递成功";
          } else if (code === 0) {
            msg = "当前不在开单时间段";
          } else if (code === 2) {
            msg = "请勿重复投递";
          } else if (code === 3) {
            msg = "排队人数已满，投递未成功";
          } else {
            msg = "投递失败，请稍后重试";
          }
          if (typeof counter === "number" && counter > 0) {
            msg += `（当前品牌投递计数：${counter}）`;
          }
          uni.showToast({ title: msg, icon: "none", duration: 3e3 });
        } catch (e2) {
          uni.hideLoading();
          uni.showToast({ title: "网络异常，投递失败", icon: "none" });
        }
      }
      const draftItems = vue.ref([]);
      async function fetchDraftItems() {
        var _a, _b, _c;
        const token = uni.getStorageSync("token") || "";
        if (!token || !plan.id) {
          draftItems.value = [];
          return;
        }
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/plan-draft-items`,
            method: "GET",
            header: { Authorization: token },
            data: { plan_id: plan.id }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            return;
          }
          const list = (_c = (_b = res.data) == null ? void 0 : _b.data) == null ? void 0 : _c.items;
          draftItems.value = Array.isArray(list) ? list : [];
        } catch (e2) {
        }
      }
      function goCreateDraft() {
        if (!plan.id)
          return;
        const url = `/pkg-creator/creator_order/submission_item_form/submission_item_form?submission_id=0&plan_id=${plan.id}`;
        uni.navigateTo({ url });
      }
      function goEditDraft(item) {
        if (!item || !item.id)
          return;
        const id = item.id;
        const url = `/pkg-creator/creator_order/submission_item_form/submission_item_form?submission_item_id=${id}&plan_id=${plan.id}`;
        uni.navigateTo({ url });
      }
      function confirmDeleteDraft(item) {
        if (!item || !item.id)
          return;
        const id = item.id;
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        uni.showModal({
          title: "删除预投递",
          content: "确定要删除这条预投递吗？",
          confirmText: "删除",
          confirmColor: "#ff4d4f",
          success: async (res) => {
            var _a, _b;
            if (!res.confirm)
              return;
            try {
              uni.showLoading({ title: "删除中...", mask: true });
              const resp = await uni.request({
                url: `${websiteUrl.value}/with-state/artist-order/item/delete`,
                method: "POST",
                header: {
                  Authorization: token,
                  "Content-Type": "application/json"
                },
                data: { item_id: id }
              });
              uni.hideLoading();
              if (String((_a = resp.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
                uni.showToast({ title: ((_b = resp.data) == null ? void 0 : _b.msg) || "删除失败", icon: "none" });
                return;
              }
              uni.showToast({ title: "已删除", icon: "none" });
              fetchDraftItems();
            } catch (e2) {
              uni.hideLoading();
              uni.showToast({ title: "删除失败", icon: "none" });
            }
          }
        });
      }
      const hasLikeBrand = vue.ref(false);
      async function fetchHasLikeBrand() {
        var _a, _b;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/hasLike?id=${parseInt(plan.brand_id)}&type=2`,
            method: "POST",
            header: { Authorization: uni.getStorageSync("token") || "" }
          });
          hasLikeBrand.value = !!(((_b = (_a = res.data) == null ? void 0 : _a.data) == null ? void 0 : _b.id) > 0);
        } catch {
        }
      }
      async function toggleFollow() {
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        try {
          const url = `${websiteUrl.value}/with-state/${hasLikeBrand.value ? "unlike" : "add-like"}`;
          const res = await uni.request({
            url,
            method: "POST",
            header: { Authorization: token },
            data: { id: parseInt(plan.brand_id), type: 2 }
          });
          if (res.data.status === "success") {
            hasLikeBrand.value = !hasLikeBrand.value;
            uni.showToast({ title: hasLikeBrand.value ? "关注成功" : "已取消关注", icon: "none" });
          } else {
            uni.showToast({ title: res.data.msg || "操作失败", icon: "none" });
          }
        } catch {
          uni.showToast({ title: "操作失败", icon: "none" });
        }
      }
      const finishingVisible = vue.ref(false);
      const shippingVisible = vue.ref(false);
      const finishingSlides = [
        {
          key: "oil",
          name: "油性消光",
          img: "https://images1.fantuanpu.com/admin/sys/youxingxiaoguang.png",
          desc: "经典定妆溶剂，耐磨度较高。缺点是定妆需要在空气湿度较低的情况下操作，且未干透前有毒性。"
        },
        {
          key: "water",
          name: "水性消光",
          img: "https://images1.fantuanpu.com/admin/sys/shuixingxiaoguang.png",
          desc: "基本无毒，定妆时不需要受限于空气湿度，基本不泛白。缺点是定妆的强度相对较低，可以被酒精轻易擦掉。"
        },
        {
          key: "gloss",
          name: "罩光剂",
          img: "https://images1.fantuanpu.com/admin/sys/zhaoguangji.png",
          desc: "近几年被广泛使用的定妆方式，无毒且不受限空气湿度。缺点是定妆强度过于高，很难卸掉，并且比较沾灰。另外如果采用蘸取拍打方式打底，手感将与喷涂方式不同。"
        }
      ];
      const finishingSlideIndex = vue.ref(0);
      const currentFinishingSlide = vue.computed(
        () => finishingSlides[finishingSlideIndex.value] || finishingSlides[0]
      );
      const finishingMethodKey = vue.computed(() => {
        var _a;
        const m2 = String(((_a = plan.extra) == null ? void 0 : _a.finishing_method) || "").trim();
        if (m2 === "oil" || m2 === "water" || m2 === "gloss")
          return m2;
        return "oil";
      });
      function openFinishingModal() {
        const key = finishingMethodKey.value;
        const idx = finishingSlides.findIndex((s2) => s2.key === key);
        finishingSlideIndex.value = idx >= 0 ? idx : 0;
        finishingVisible.value = true;
      }
      function onFinishingSwiperChange(e2) {
        var _a;
        finishingSlideIndex.value = ((_a = e2.detail) == null ? void 0 : _a.current) || 0;
      }
      onLoad((q2 = {}) => {
        var _a;
        planId.value = Number((q2 == null ? void 0 : q2.id) || 0);
        try {
          const wi = uni.getWindowInfo && uni.getWindowInfo();
          safeTop.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.top) ?? (wi == null ? void 0 : wi.statusBarHeight) ?? 0;
        } catch {
          safeTop.value = 20;
        }
      });
      vue.onMounted(async () => {
        if (!planId.value) {
          uni.showToast({ title: "缺少参数：id", icon: "none" });
          return;
        }
        uni.showLoading({ title: "加载中..." });
        startTimer();
      });
      onShow(async () => {
        try {
          await fetchPlan();
          if (global$1.isLogin && plan.brand_id)
            await fetchHasLikeBrand();
          if (global$1.isLogin)
            await fetchDraftItems();
        } finally {
          uni.hideLoading();
        }
      });
      vue.onUnmounted(stopTimer);
      const descText = vue.computed(() => "");
      const __returned__ = { scrollTop, safeTop, stickyShowThreshold, showStickyTitle, planId, plan, goBack, fetchPlan, coverUrl, avatarUrl, artistName, highlightImages, previewHL, isLocalPlan, modeMap, modeName, modeClass, isPermanent, nowSec, phase, fmtTime, fmtDur, startsInText, endsInText, get timer() {
        return timer;
      }, set timer(v2) {
        timer = v2;
      }, tick, startTimer, stopTimer, stockText, perHeadCycleDays, finishingMap, finishingMethodText, finishingDescText, shipping, isUnifiedShipping, shippingText, sizeSurcharges, sizePriceText, tierListText, addonListText, _min, _max, tierRangeText, tierRangePriceText, btnText, btnDisabled, ctaClass, goArtistHome, handleCTA, draftItems, fetchDraftItems, goCreateDraft, goEditDraft, confirmDeleteDraft, hasLikeBrand, fetchHasLikeBrand, toggleFollow, finishingVisible, shippingVisible, finishingSlides, finishingSlideIndex, currentFinishingSlide, finishingMethodKey, openFinishingModal, onFinishingSwiperChange, safeArea, descText, ref: vue.ref, reactive: vue.reactive, computed: vue.computed, onMounted: vue.onMounted, onUnmounted: vue.onUnmounted, get onLoad() {
        return onLoad;
      }, get onPageScroll() {
        return onPageScroll;
      }, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$7(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_zhouWei_navBar = resolveEasycom(vue.resolveDynamicComponent("zhouWei-navBar"), __easycom_0$5);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    const _component_common_modal = resolveEasycom(vue.resolveDynamicComponent("common-modal"), __easycom_2$b);
    return vue.openBlock(), vue.createElementBlock("view", { class: "booking-page" }, [
      vue.withDirectives(vue.createElementVNode(
        "view",
        {
          class: "sticky-titlebar",
          style: vue.normalizeStyle({ paddingTop: $setup.safeTop + "px" })
        },
        [
          vue.createElementVNode("view", {
            class: "nav-left",
            onClick: $setup.goBack,
            "aria-label": "返回"
          }, [
            vue.createElementVNode("image", {
              class: "back-icon",
              src: _imports_0$3,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", {
              class: "back-text font-alimamashuhei",
              style: { "color": "#000" }
            }, "返回")
          ]),
          vue.createElementVNode("view", {
            class: "st-center",
            onClick: $setup.goArtistHome,
            "aria-label": "打开作者主页"
          }, [
            vue.createElementVNode("image", {
              class: "st-logo",
              src: $setup.avatarUrl,
              mode: "aspectFill"
            }, null, 8, ["src"]),
            vue.createElementVNode(
              "text",
              { class: "st-name" },
              vue.toDisplayString($setup.artistName),
              1
              /* TEXT */
            )
          ]),
          vue.createElementVNode("view", { class: "st-right" })
        ],
        4
        /* STYLE */
      ), [
        [vue.vShow, $setup.showStickyTitle]
      ]),
      vue.createVNode(_component_zhouWei_navBar, {
        type: "transparentFixed",
        backState: 2e3,
        homeState: 2e3,
        scrollTop: $setup.scrollTop,
        titleCenter: true,
        fontColor: "#fff",
        transparentFixedFontColor: "#fff"
      }, {
        left: vue.withCtx(() => [
          vue.createElementVNode("view", {
            class: "nav-left",
            onClick: $setup.goBack,
            "aria-label": "返回"
          }, [
            vue.createElementVNode("image", {
              class: "back-icon",
              src: _imports_0$3,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", { class: "back-text font-alimamashuhei" }, "返回")
          ])
        ]),
        transparentFixedLeft: vue.withCtx(() => [
          vue.createElementVNode("view", {
            class: "nav-left",
            onClick: $setup.goBack,
            "aria-label": "返回"
          }, [
            vue.createElementVNode("image", {
              class: "back-icon",
              src: _imports_0$3,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", { class: "back-text" }, "返回")
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["scrollTop"]),
      vue.createElementVNode("view", { class: "hero" }, [
        vue.createElementVNode("image", {
          class: "hero-img",
          src: $setup.coverUrl,
          mode: "aspectFill"
        }, null, 8, ["src"]),
        vue.createElementVNode("view", { class: "hero-mask" }),
        vue.createElementVNode("view", { class: "hero-footer" }, [
          vue.createElementVNode("image", {
            class: "avatar",
            src: $setup.avatarUrl,
            mode: "aspectFill",
            onClick: $setup.goArtistHome
          }, null, 8, ["src"]),
          vue.createElementVNode("view", { class: "hero-title" }, [
            vue.createElementVNode(
              "text",
              {
                class: "brand-name font-title",
                onClick: $setup.goArtistHome
              },
              vue.toDisplayString($setup.artistName),
              1
              /* TEXT */
            ),
            vue.createElementVNode("text", { class: "plan-title font-alimamashuhei" }, "开单")
          ])
        ])
      ]),
      $setup.highlightImages.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
        key: 0,
        class: "hl-strip",
        "scroll-x": "",
        "show-scrollbar": false
      }, [
        vue.createElementVNode("view", { class: "hl-row" }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.highlightImages, (img, i2) => {
              return vue.openBlock(), vue.createElementBlock("image", {
                key: i2,
                class: "hl-img",
                src: img,
                mode: "aspectFill",
                onClick: ($event) => $setup.previewHL(i2)
              }, null, 8, ["src", "onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])
      ])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode("view", { class: "card" }, [
        vue.createElementVNode("view", { class: "row" }, [
          vue.createElementVNode("text", { class: "label font-title" }, "投递方式"),
          vue.createElementVNode("view", { class: "val" }, [
            vue.createElementVNode(
              "text",
              {
                class: vue.normalizeClass(["mode-chip", $setup.modeClass])
              },
              vue.toDisplayString($setup.modeName),
              3
              /* TEXT, CLASS */
            ),
            !$setup.isLocalPlan ? (vue.openBlock(), vue.createElementBlock("text", {
              key: 0,
              class: "external-tip"
            }, "在其它平台开单")) : vue.createCommentVNode("v-if", true)
          ])
        ]),
        $setup.isLocalPlan ? (vue.openBlock(), vue.createElementBlock(
          vue.Fragment,
          { key: 0 },
          [
            $setup.isPermanent ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "row"
            }, [
              vue.createElementVNode("text", { class: "label font-title" }, "档期"),
              vue.createElementVNode("text", { class: "val" }, "常驻可约 · 按时间顺序排期")
            ])) : (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              { key: 1 },
              [
                vue.createElementVNode("view", { class: "row" }, [
                  vue.createElementVNode("text", { class: "label font-title" }, "开始时间"),
                  vue.createElementVNode(
                    "text",
                    { class: "val" },
                    vue.toDisplayString($setup.fmtTime($setup.plan.open_time)),
                    1
                    /* TEXT */
                  )
                ]),
                vue.createElementVNode("view", { class: "row" }, [
                  vue.createElementVNode("text", { class: "label font-title" }, "截止时间"),
                  vue.createElementVNode(
                    "text",
                    { class: "val" },
                    vue.toDisplayString($setup.fmtTime($setup.plan.close_time)),
                    1
                    /* TEXT */
                  )
                ]),
                $setup.phase === "upcoming" ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "row"
                }, [
                  vue.createElementVNode("text", { class: "label font-title" }, "距离开始"),
                  vue.createElementVNode(
                    "text",
                    { class: "val strong" },
                    vue.toDisplayString($setup.startsInText),
                    1
                    /* TEXT */
                  )
                ])) : vue.createCommentVNode("v-if", true),
                $setup.phase === "ongoing" ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 1,
                  class: "row"
                }, [
                  vue.createElementVNode("text", { class: "label font-title" }, "距离截止"),
                  vue.createElementVNode(
                    "text",
                    { class: "val strong" },
                    vue.toDisplayString($setup.endsInText),
                    1
                    /* TEXT */
                  )
                ])) : vue.createCommentVNode("v-if", true),
                $setup.phase === "ended" ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 2,
                  class: "row"
                }, [
                  vue.createElementVNode("text", { class: "label font-title" }, "状态"),
                  vue.createElementVNode("text", { class: "val strong" }, "已截止")
                ])) : vue.createCommentVNode("v-if", true)
              ],
              64
              /* STABLE_FRAGMENT */
            ))
          ],
          64
          /* STABLE_FRAGMENT */
        )) : vue.createCommentVNode("v-if", true),
        vue.createElementVNode("view", { class: "row" }, [
          vue.createElementVNode("text", { class: "label font-title" }, "定妆方式"),
          vue.createElementVNode("view", {
            class: "val finishing-val",
            onClick: $setup.openFinishingModal
          }, [
            vue.createElementVNode(
              "text",
              null,
              vue.toDisplayString($setup.finishingMethodText),
              1
              /* TEXT */
            ),
            vue.createVNode(_component_uni_icons, {
              type: "arrow-right",
              size: "18",
              color: "#fff"
            })
          ])
        ]),
        $setup.perHeadCycleDays !== null ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "row"
        }, [
          vue.createElementVNode("text", { class: "label font-title" }, "每颗头工期"),
          vue.createElementVNode(
            "text",
            { class: "val" },
            vue.toDisplayString($setup.perHeadCycleDays) + " 天",
            1
            /* TEXT */
          )
        ])) : vue.createCommentVNode("v-if", true)
      ]),
      vue.createElementVNode("view", { class: "card oc-card" }, [
        vue.createElementVNode("view", { class: "oc-grid" }, [
          vue.createElementVNode("view", { class: "oc-label" }, [
            vue.createElementVNode("text", { class: "font-title" }, "档位")
          ]),
          vue.createElementVNode("view", { class: "oc-value" }, [
            $setup.plan.tiers && $setup.plan.tiers.length ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.plan.tiers, (t2, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "oc-item",
                    key: "tier-" + idx
                  }, [
                    vue.createElementVNode("view", { class: "oc-item-hd" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "oc-badge font-title" },
                        "¥" + vue.toDisplayString(Number(t2.price || 0)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "oc-name" },
                        vue.toDisplayString(t2.title || "—"),
                        1
                        /* TEXT */
                      )
                    ]),
                    t2.description ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "oc-desc"
                      },
                      vue.toDisplayString(t2.description),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "oc-empty"
            }, "—"))
          ]),
          vue.createElementVNode("view", { class: "oc-label" }, [
            vue.createElementVNode("text", { class: "font-title" }, "加购")
          ]),
          vue.createElementVNode("view", { class: "oc-value" }, [
            $setup.plan.addons && $setup.plan.addons.length ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.plan.addons, (a2, idx) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    class: "oc-item",
                    key: "addon-" + idx
                  }, [
                    vue.createElementVNode("view", { class: "oc-item-hd" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "oc-badge add font-title" },
                        "+¥" + vue.toDisplayString(Number(a2.price || 0)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "oc-name" },
                        vue.toDisplayString(a2.title || "—"),
                        1
                        /* TEXT */
                      )
                    ]),
                    a2.description ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "oc-desc"
                      },
                      vue.toDisplayString(a2.description),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "oc-empty"
            }, "—"))
          ]),
          vue.createElementVNode("view", { class: "oc-label" }, [
            vue.createElementVNode("text", { class: "font-title" }, "不同尺寸的价格")
          ]),
          vue.createElementVNode("view", { class: "oc-value" }, [
            $setup.sizeSurcharges.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "oc-chip-row"
            }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.sizeSurcharges, (s2, i2) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: "size-" + i2,
                    class: "oc-chip"
                  }, [
                    vue.createElementVNode(
                      "text",
                      { class: "oc-chip-size" },
                      vue.toDisplayString(s2.size || "—"),
                      1
                      /* TEXT */
                    ),
                    vue.createElementVNode(
                      "text",
                      { class: "oc-chip-num font-alimamashuhei" },
                      "+ ￥" + vue.toDisplayString(Number(s2.price || 0)),
                      1
                      /* TEXT */
                    )
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "oc-empty"
            }, "—"))
          ]),
          vue.createElementVNode("view", { class: "oc-label" }, [
            vue.createElementVNode("text", { class: "font-title" }, "寄送约定")
          ]),
          vue.createElementVNode("view", { class: "oc-value oc-value-row" }, [
            $setup.shippingText ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 0,
                class: "oc-plain"
              },
              vue.toDisplayString($setup.shippingText),
              1
              /* TEXT */
            )) : (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "oc-empty"
            }, "—")),
            vue.createVNode(_component_uni_icons, {
              class: "oc-info-icon",
              type: "info",
              size: "18",
              color: "#1677ff",
              onClick: _cache[0] || (_cache[0] = ($event) => $setup.shippingVisible = true)
            })
          ]),
          vue.createElementVNode("view", { class: "oc-label" }, [
            vue.createElementVNode("text", { class: "font-title" }, "补充说明")
          ]),
          vue.createElementVNode("view", { class: "oc-value" }, [
            $setup.finishingDescText ? (vue.openBlock(), vue.createElementBlock(
              "text",
              {
                key: 0,
                class: "oc-desc"
              },
              vue.toDisplayString($setup.finishingDescText),
              1
              /* TEXT */
            )) : (vue.openBlock(), vue.createElementBlock("text", {
              key: 1,
              class: "oc-empty"
            }, "—"))
          ])
        ])
      ]),
      $setup.descText ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "desc-card"
      }, [
        vue.createElementVNode("text", { class: "desc-title font-title" }, "说明"),
        vue.createElementVNode(
          "text",
          { class: "desc" },
          vue.toDisplayString($setup.descText),
          1
          /* TEXT */
        )
      ])) : vue.createCommentVNode("v-if", true),
      $setup.isLocalPlan ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 2,
        class: "card draft-card"
      }, [
        vue.createElementVNode("view", { class: "draft-header" }, [
          vue.createElementVNode("text", { class: "draft-title font-title" }, "我的待投递"),
          vue.createElementVNode("button", {
            class: "draft-add-btn",
            onClick: $setup.goCreateDraft
          }, [
            vue.createVNode(_component_uni_icons, {
              type: "plusempty",
              size: "18",
              color: "#ffffff"
            }),
            vue.createElementVNode("text", { class: "draft-add-text" }, "新增预投递")
          ])
        ]),
        $setup.draftItems.length ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "draft-list"
        }, [
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.draftItems, (item) => {
              return vue.openBlock(), vue.createElementBlock("view", {
                class: "draft-item",
                key: item.id
              }, [
                vue.createElementVNode("view", {
                  class: "draft-main",
                  onClick: ($event) => $setup.goEditDraft(item)
                }, [
                  vue.createElementVNode(
                    "text",
                    { class: "draft-subject" },
                    vue.toDisplayString(item.work_subject || "未填写主体"),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("view", { class: "draft-meta" }, [
                    item.size ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "draft-meta-chip"
                      },
                      vue.toDisplayString(item.size),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    item.tier_title ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 1,
                        class: "draft-meta-chip"
                      },
                      vue.toDisplayString(item.tier_title),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode(
                      "text",
                      { class: "draft-price" },
                      "¥" + vue.toDisplayString(Number(item.price_total || 0)),
                      1
                      /* TEXT */
                    )
                  ])
                ], 8, ["onClick"]),
                vue.createElementVNode("view", { class: "draft-actions" }, [
                  vue.createVNode(_component_uni_icons, {
                    type: "compose",
                    size: "20",
                    color: "#999999",
                    onClick: vue.withModifiers(($event) => $setup.goEditDraft(item), ["stop"])
                  }, null, 8, ["onClick"]),
                  vue.createVNode(_component_uni_icons, {
                    type: "trash",
                    size: "20",
                    color: "#999999",
                    onClick: vue.withModifiers(($event) => $setup.confirmDeleteDraft(item), ["stop"])
                  }, null, 8, ["onClick"])
                ])
              ]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "draft-empty"
        }, [
          vue.createElementVNode("text", { class: "draft-empty-text" }, "暂无待投递内容，点击右上角按钮添加")
        ]))
      ])) : vue.createCommentVNode("v-if", true),
      vue.createElementVNode(
        "view",
        {
          class: vue.normalizeClass(["buybar", { safe: $setup.safeArea }])
        },
        [
          vue.createElementVNode("view", { class: "buy-left" }, [
            vue.createElementVNode(
              "view",
              {
                class: vue.normalizeClass(["follow", { active: $setup.hasLikeBrand }]),
                onClick: $setup.toggleFollow
              },
              [
                vue.createVNode(_component_uni_icons, {
                  type: "heart",
                  size: "22",
                  color: $setup.hasLikeBrand ? "#ff6a6c" : "#b3bcc6"
                }, null, 8, ["color"])
              ],
              2
              /* CLASS */
            )
          ]),
          vue.createElementVNode("button", {
            class: vue.normalizeClass(["buy-cta", $setup.ctaClass]),
            disabled: $setup.btnDisabled,
            onClick: $setup.handleCTA
          }, [
            vue.createElementVNode(
              "text",
              { class: "price-text" },
              vue.toDisplayString($setup.tierRangePriceText),
              1
              /* TEXT */
            ),
            vue.createElementVNode("view", { class: "sep" }),
            vue.createElementVNode(
              "text",
              { class: "stock-text" },
              vue.toDisplayString($setup.stockText),
              1
              /* TEXT */
            ),
            vue.createElementVNode("view", { class: "sep" }),
            vue.createElementVNode(
              "text",
              { class: "cta-text" },
              vue.toDisplayString($setup.btnText),
              1
              /* TEXT */
            )
          ], 10, ["disabled"])
        ],
        2
        /* CLASS */
      ),
      vue.createElementVNode("view", { style: { "height": "160rpx" } }),
      vue.createVNode(_component_common_modal, {
        visible: $setup.finishingVisible,
        "onUpdate:visible": _cache[1] || (_cache[1] = (v2) => $setup.finishingVisible = v2),
        top: "200rpx",
        width: "calc(80vw)"
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "cm-wrapper" }, [
            vue.createElementVNode("view", { class: "cm-title" }, "定妆方式说明"),
            vue.createElementVNode("view", { class: "cm-body" }, [
              vue.createElementVNode("text", { class: "cm-intro" }, " 定妆工具一般分为三种，油性消光、水性消光、罩光剂三种，它们的特点和区别如下： "),
              vue.createElementVNode("view", { class: "cm-swiper-wrapper" }, [
                vue.createElementVNode("swiper", {
                  class: "cm-swiper",
                  current: $setup.finishingSlideIndex,
                  "indicator-dots": "",
                  circular: "",
                  autoplay: true,
                  interval: 9e3,
                  "indicator-color": "rgba(0,0,0,0.16)",
                  "indicator-active-color": "#86b9fb",
                  onChange: $setup.onFinishingSwiperChange
                }, [
                  (vue.openBlock(), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.finishingSlides, (item, idx) => {
                      return vue.createElementVNode("swiper-item", {
                        key: item.key
                      }, [
                        vue.createElementVNode("view", { class: "cm-slide" }, [
                          vue.createElementVNode("image", {
                            class: "cm-img",
                            src: item.img,
                            mode: "aspectFit"
                          }, null, 8, ["src"])
                        ])
                      ]);
                    }),
                    64
                    /* STABLE_FRAGMENT */
                  ))
                ], 40, ["current"])
              ]),
              vue.createElementVNode("view", { class: "cm-desc-wrapper" }, [
                vue.createElementVNode(
                  "text",
                  { class: "cm-desc-title" },
                  vue.toDisplayString($setup.currentFinishingSlide.name),
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "cm-desc-text" },
                  vue.toDisplayString($setup.currentFinishingSlide.desc),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", null, [
                vue.createElementVNode("text", { class: "cm-desc-title" }, "为什么需要喷涂这些？"),
                vue.createElementVNode("text", { class: "cm-desc-text" }, "打底可以保护娃头不受颜料染色，同时提供一些粗糙度，以供颜料和色粉附着。定妆是为了避免颜料和色粉被磨掉。")
              ])
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["visible"]),
      vue.createVNode(_component_common_modal, {
        visible: $setup.shippingVisible,
        "onUpdate:visible": _cache[2] || (_cache[2] = (v2) => $setup.shippingVisible = v2),
        top: "200rpx",
        width: "80vw"
      }, {
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "cm-wrapper" }, [
            vue.createElementVNode("view", { class: "cm-title" }, "寄送约定"),
            vue.createElementVNode("view", { class: "cm-body" }, [
              $setup.isUnifiedShipping ? (vue.openBlock(), vue.createElementBlock("view", { key: 0 }, [
                vue.createElementVNode("text", { class: "cm-desc-text" }, " 需要在指定日期前寄送到妆师地址。 "),
                vue.createElementVNode("text", { class: "cm-desc-text" }, " 如果未按要求在指定日期前寄到，该订单将不计入超时工期。且在开始之前妆师取消订单。 ")
              ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
                vue.createElementVNode("text", { class: "cm-desc-text" }, " 会根据排单顺序约定寄送日期. "),
                vue.createElementVNode("text", { class: "cm-desc-text" }, " 如果未按要求在指定日期前寄到，该订单将不计入超时工期。且在开始之前妆师有权利取消订单。 ")
              ]))
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["visible"])
    ]);
  }
  const PkgCreatorCreatorOrderPlaneDetailPlaneDetail = /* @__PURE__ */ _export_sfc(_sfc_main$8, [["render", _sfc_render$7], ["__scopeId", "data-v-7cf7f8f2"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/plane_detail/plane_detail.vue"]]);
  const pageSize$1 = 20;
  const _sfc_main$7 = {
    __name: "my_submissions",
    setup(__props, { expose: __expose }) {
      __expose();
      const submissionList = vue.ref([]);
      const page = vue.ref(1);
      const total = vue.ref(0);
      const loading2 = vue.ref(false);
      const isLogin = vue.computed(() => !!global$1.isLogin);
      const finished = vue.computed(
        () => total.value > 0 && submissionList.value.length >= total.value
      );
      const planLimitMap = vue.ref({});
      const tabs = [
        { key: "all", label: "全部" },
        { key: "ongoing", label: "交易中" },
        { key: "finished", label: "交易结束" }
      ];
      const activeTab = vue.ref("all");
      const filteredList = vue.computed(() => {
        if (activeTab.value === "ongoing") {
          return submissionList.value.filter((row) => isOngoing(row.submission));
        }
        if (activeTab.value === "finished") {
          return submissionList.value.filter((row) => isFinished(row.submission));
        }
        return submissionList.value;
      });
      const changeTab = (key) => {
        activeTab.value = key;
      };
      const goLogin = () => {
        uni.showToast({ title: "请在「我的」里登录账号", icon: "none" });
      };
      async function ensureLogin() {
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          global$1.isLogin = false;
          return false;
        }
        if (!global$1.isLogin && typeof asyncGetUserInfo === "function") {
          await asyncGetUserInfo();
        }
        return !!global$1.isLogin;
      }
      function getPlanIdFromSubmission(sub) {
        if (!sub)
          return 0;
        return Number(
          sub.plan_id || sub.PlanId || sub.order_plan_id || sub.OrderPlanId || 0
        );
      }
      async function prefetchPlanLimits(rows) {
        const ids = Array.from(
          new Set(
            (rows || []).map((r2) => getPlanIdFromSubmission(r2.submission)).filter((id) => id && typeof planLimitMap.value[id] === "undefined")
          )
        );
        if (!ids.length)
          return;
        const tasks = ids.map((id) => fetchPlanLimit(id));
        try {
          await Promise.all(tasks);
        } catch (e2) {
          formatAppLog("warn", "at pkg-creator/creator_order/my_submissions/my_submissions.vue:220", "prefetchPlanLimits error", e2);
        }
      }
      async function fetchPlanLimit(planId) {
        if (!planId)
          return;
        if (typeof planLimitMap.value[planId] !== "undefined")
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/order-plan-info`,
            method: "GET",
            data: { id: planId }
          });
          const body = res.data || {};
          if (String(body.status).toLowerCase() !== "success") {
            planLimitMap.value = {
              ...planLimitMap.value,
              [planId]: 0
            };
            return;
          }
          const d2 = body.data || {};
          const limit = Number(d2.max_submissions_per_user || 0);
          planLimitMap.value = {
            ...planLimitMap.value,
            [planId]: limit
          };
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/my_submissions/my_submissions.vue:252", "fetchPlanLimit error", e2);
          planLimitMap.value = {
            ...planLimitMap.value,
            [planId]: 0
          };
        }
      }
      function getSubmissionStatus(sub) {
        if (!sub)
          return 0;
        return Number(sub.status ?? sub.Status ?? 0);
      }
      function isOngoing(sub) {
        const st2 = getSubmissionStatus(sub);
        return st2 === 0 || st2 === 1 || st2 === 2;
      }
      function isFinished(sub) {
        const st2 = getSubmissionStatus(sub);
        return st2 === 3 || st2 === 4 || st2 === 5;
      }
      async function fetchList(reset = false) {
        var _a, _b;
        if (loading2.value)
          return;
        const ok = await ensureLogin();
        if (!ok) {
          if (reset) {
            submissionList.value = [];
            page.value = 1;
            total.value = 0;
          }
          uni.stopPullDownRefresh();
          return;
        }
        if (reset) {
          page.value = 1;
          submissionList.value = [];
          total.value = 0;
        }
        loading2.value = true;
        try {
          const token = uni.getStorageSync("token") || "";
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/my-submissions`,
            method: "GET",
            header: {
              Authorization: token
            },
            data: {
              page: page.value,
              page_size: pageSize$1,
              size: pageSize$1
            }
          });
          if (String((_a = res.data) == null ? void 0 : _a.status).toLowerCase() !== "success") {
            uni.showToast({ title: ((_b = res.data) == null ? void 0 : _b.msg) || "加载失败", icon: "none" });
            return;
          }
          const data = res.data.data || {};
          const rows = data.list || [];
          const p2 = data.page || {};
          if (reset) {
            submissionList.value = rows;
          } else {
            submissionList.value = submissionList.value.concat(rows);
          }
          total.value = Number(p2.total || total.value || rows.length || 0);
          const currentPage = Number(p2.page || page.value);
          page.value = currentPage + 1;
          prefetchPlanLimits(rows);
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/my_submissions/my_submissions.vue:338", "加载投递列表失败", e2);
          uni.showToast({ title: "网络异常，加载失败", icon: "none" });
        } finally {
          loading2.value = false;
          uni.stopPullDownRefresh();
        }
      }
      function getBrandObj(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        return sub.brand || sub.Brand || {};
      }
      function brandName(row) {
        const b2 = getBrandObj(row);
        return b2.brand_name || b2.BrandName || "未知品牌";
      }
      function brandLogo(row) {
        const b2 = getBrandObj(row);
        return b2.logo_image || b2.LogoImage || "";
      }
      function displayPrice(it2) {
        const total2 = it2.price_total ?? it2.PriceTotal ?? it2.tier_price ?? it2.TierPrice ?? 0;
        const n2 = Number(total2) || 0;
        return n2.toFixed(2).replace(/\.00$/, "");
      }
      function firstRefImage(it2) {
        const raw = it2.ref_images || it2.RefImages || "";
        if (!raw)
          return "";
        const arr = String(raw).split(",");
        return arr[0].trim();
      }
      function formatTime(raw) {
        if (!raw)
          return "";
        let d2;
        if (typeof raw === "number") {
          if (raw > 1e12)
            d2 = new Date(raw);
          else
            d2 = new Date(raw * 1e3);
        } else if (typeof raw === "string") {
          d2 = new Date(raw.replace(" ", "T"));
        } else {
          return "";
        }
        if (Number.isNaN(d2.getTime()))
          return "";
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const day = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0");
        const mm = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${day} ${hh}:${mm}`;
      }
      function statusText(row) {
        const st2 = getSubmissionStatus(row == null ? void 0 : row.submission);
        switch (st2) {
          case 0:
            return "排队中";
          case 1:
            return "已抢到名额";
          case 2:
            return "投递成功";
          case 3:
            return "未抢到名额";
          case 4:
            return "已取消";
          case 5:
            return "支付超时";
          default:
            return "未知状态";
        }
      }
      function statusClass(row) {
        const st2 = getSubmissionStatus(row == null ? void 0 : row.submission);
        return {
          "status-running": st2 === 0 || st2 === 1,
          "status-success": st2 === 2,
          "status-failed": st2 === 3 || st2 === 4 || st2 === 5
        };
      }
      function goSubmissionDetail(row) {
        const sub = (row == null ? void 0 : row.submission) || {};
        const id = sub.id || sub.ID;
        if (!id)
          return;
        const url = `/pkg-creator/creator_order/submission_detail/submission_detail?submission_id=${id}`;
        uni.navigateTo({ url });
      }
      function editSubmission(row) {
        goSubmissionDetail(row);
      }
      function contactArtist(row) {
        uni.showToast({
          title: "联系妆师功能待接入",
          icon: "none"
        });
      }
      function goEditItem(row, it2) {
        const sub = (row == null ? void 0 : row.submission) || {};
        const submissionId = sub.id || sub.ID;
        const itemId = it2.id || it2.ID;
        if (!submissionId || !itemId)
          return;
        const url = `/pkg-creator/creator_order/submission_item_form/submission_item_form?submission_id=${submissionId}&item_id=${itemId}`;
        uni.navigateTo({ url });
      }
      onLoad(() => {
        fetchList(true);
      });
      onPullDownRefresh(() => {
        fetchList(true);
      });
      onReachBottom(() => {
        if (!finished.value) {
          fetchList(false);
        }
      });
      const __returned__ = { submissionList, page, pageSize: pageSize$1, total, loading: loading2, isLogin, finished, planLimitMap, tabs, activeTab, filteredList, changeTab, goLogin, ensureLogin, getPlanIdFromSubmission, prefetchPlanLimits, fetchPlanLimit, getSubmissionStatus, isOngoing, isFinished, fetchList, getBrandObj, brandName, brandLogo, displayPrice, firstRefImage, formatTime, statusText, statusClass, goSubmissionDetail, editSubmission, contactArtist, goEditItem, ref: vue.ref, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get onPullDownRefresh() {
        return onPullDownRefresh;
      }, get onReachBottom() {
        return onReachBottom;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get asyncGetUserInfo() {
        return asyncGetUserInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$6(_ctx, _cache, $props, $setup, $data, $options) {
    return vue.openBlock(), vue.createElementBlock("view", { class: "my-sub-page" }, [
      !$setup.isLogin && !$setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "empty-wrap"
      }, [
        vue.createElementVNode("view", { class: "empty-card" }, [
          vue.createElementVNode("text", { class: "empty-title" }, "登录后查看你的投递记录"),
          vue.createElementVNode("text", { class: "empty-desc" }, "可在这里统一管理你约妆 / 约毛的所有投递。"),
          vue.createElementVNode("button", {
            class: "login-btn",
            onClick: $setup.goLogin
          }, "去登录")
        ])
      ])) : (vue.openBlock(), vue.createElementBlock("view", {
        key: 1,
        class: "list-wrap"
      }, [
        !$setup.loading && $setup.submissionList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "empty-wrap"
        }, [
          vue.createElementVNode("view", { class: "empty-card" }, [
            vue.createElementVNode("text", { class: "empty-title" }, "还没有投递记录"),
            vue.createElementVNode("text", { class: "empty-desc" }, "去找喜欢的妆师 / 毛娘，投递你的第一个方案吧。")
          ])
        ])) : (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
          vue.createElementVNode("view", { class: "status-tabs" }, [
            (vue.openBlock(), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.tabs, (tab) => {
                return vue.createElementVNode("view", {
                  key: tab.key,
                  class: vue.normalizeClass(["status-tab", { "status-tab-active": $setup.activeTab === tab.key }]),
                  onClick: ($event) => $setup.changeTab(tab.key)
                }, [
                  vue.createElementVNode(
                    "text",
                    { class: "status-tab-text" },
                    vue.toDisplayString(tab.label),
                    1
                    /* TEXT */
                  )
                ], 10, ["onClick"]);
              }),
              64
              /* STABLE_FRAGMENT */
            ))
          ]),
          !$setup.loading && $setup.filteredList.length === 0 ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "empty-wrap status-empty"
          }, [
            vue.createElementVNode("view", { class: "empty-card" }, [
              vue.createElementVNode("text", { class: "empty-title" }, "当前状态暂无投递"),
              vue.createElementVNode("text", { class: "empty-desc" }, "换个筛选看看其他投递记录。")
            ])
          ])) : vue.createCommentVNode("v-if", true),
          (vue.openBlock(true), vue.createElementBlock(
            vue.Fragment,
            null,
            vue.renderList($setup.filteredList, (row) => {
              var _a, _b, _c, _d;
              return vue.openBlock(), vue.createElementBlock("view", {
                key: ((_a = row.submission) == null ? void 0 : _a.id) || ((_b = row.submission) == null ? void 0 : _b.ID),
                class: "sub-card",
                onClick: ($event) => $setup.goSubmissionDetail(row)
              }, [
                vue.createElementVNode("view", { class: "sub-header" }, [
                  vue.createElementVNode("view", { class: "sub-brand" }, [
                    vue.createElementVNode("view", { class: "brand-logo-box" }, [
                      $setup.brandLogo(row) ? (vue.openBlock(), vue.createElementBlock("image", {
                        key: 0,
                        class: "brand-logo",
                        src: $setup.brandLogo(row),
                        mode: "aspectFill"
                      }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("view", {
                        key: 1,
                        class: "brand-logo-placeholder"
                      }, [
                        vue.createElementVNode(
                          "text",
                          { class: "brand-logo-text" },
                          vue.toDisplayString($setup.brandName(row).slice(0, 1)),
                          1
                          /* TEXT */
                        )
                      ]))
                    ]),
                    vue.createElementVNode("view", { class: "brand-info" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "brand-name" },
                        vue.toDisplayString($setup.brandName(row)),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "text",
                        { class: "brand-time" },
                        vue.toDisplayString($setup.formatTime(((_c = row.submission) == null ? void 0 : _c.created_at) || ((_d = row.submission) == null ? void 0 : _d.CreatedAt))),
                        1
                        /* TEXT */
                      )
                    ])
                  ]),
                  vue.createElementVNode(
                    "view",
                    {
                      class: vue.normalizeClass(["sub-status-tag", $setup.statusClass(row)])
                    },
                    vue.toDisplayString($setup.statusText(row)),
                    3
                    /* TEXT, CLASS */
                  )
                ]),
                vue.createElementVNode("view", { class: "sub-body" }, [
                  (row.items || []).length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 0,
                    class: "sub-items-row"
                  }, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList((row.items || []).slice(0, 2), (it2, idx) => {
                        return vue.openBlock(), vue.createElementBlock("view", {
                          class: "sub-item-card",
                          key: it2.id || it2.ID || idx,
                          onClick: vue.withModifiers(($event) => $setup.goEditItem(row, it2), ["stop"])
                        }, [
                          vue.createElementVNode("image", {
                            class: "item-image",
                            src: $setup.firstRefImage(it2),
                            mode: "aspectFill"
                          }, null, 8, ["src"]),
                          vue.createElementVNode("view", { class: "item-info" }, [
                            vue.createElementVNode(
                              "text",
                              { class: "item-title" },
                              vue.toDisplayString(it2.work_subject || it2.WorkSubject || "作品"),
                              1
                              /* TEXT */
                            ),
                            vue.createElementVNode(
                              "text",
                              { class: "item-price" },
                              " ¥" + vue.toDisplayString($setup.displayPrice(it2)),
                              1
                              /* TEXT */
                            )
                          ])
                        ], 8, ["onClick"]);
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])) : (vue.openBlock(), vue.createElementBlock("view", {
                    key: 1,
                    class: "sub-no-items"
                  }, [
                    vue.createElementVNode("text", { class: "no-items-text" }, "暂未绑定作品，建议尽快补充作品信息。")
                  ]))
                ]),
                vue.createElementVNode("view", { class: "sub-footer-actions" }, [
                  vue.createElementVNode("text", {
                    class: "footer-link",
                    onClick: vue.withModifiers(($event) => $setup.contactArtist(row), ["stop"])
                  }, "联系妆师", 8, ["onClick"]),
                  vue.createElementVNode("text", {
                    class: "footer-link",
                    onClick: vue.withModifiers(($event) => $setup.editSubmission(row), ["stop"])
                  }, "修改投递", 8, ["onClick"])
                ])
              ], 8, ["onClick"]);
            }),
            128
            /* KEYED_FRAGMENT */
          ))
        ])),
        vue.createElementVNode("view", { class: "list-footer" }, [
          $setup.loading ? (vue.openBlock(), vue.createElementBlock("text", {
            key: 0,
            class: "footer-text"
          }, "加载中...")) : $setup.finished && $setup.submissionList.length > 0 ? (vue.openBlock(), vue.createElementBlock("text", {
            key: 1,
            class: "footer-text"
          }, "已经到底了")) : vue.createCommentVNode("v-if", true)
        ])
      ]))
    ]);
  }
  const PkgCreatorCreatorOrderMySubmissionsMySubmissions = /* @__PURE__ */ _export_sfc(_sfc_main$7, [["render", _sfc_render$6], ["__scopeId", "data-v-d9b04ccb"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/my_submissions/my_submissions.vue"]]);
  const _sfc_main$6 = {
    __name: "submission_detail",
    setup(__props, { expose: __expose }) {
      __expose();
      const submissionId = vue.ref(0);
      const loading2 = vue.ref(false);
      const hasFirstLoaded = vue.ref(false);
      const errorMsg = vue.ref("");
      const pollingTimer = vue.ref(null);
      const submission = vue.reactive({
        submission_id: 0,
        plan_id: 0,
        mode: 0,
        ahead_count: 0,
        status: 0,
        status_text: "",
        created_at: 0,
        items: []
      });
      const draftItems = vue.ref([]);
      const plan = vue.reactive({
        id: 0,
        order_type: 0,
        max_submissions_per_user: 0,
        open_time: 0,
        close_time: 0,
        artist_name: "",
        artist_info: null,
        brand: null
      });
      const hasLoadedPlan = vue.ref(false);
      const isLogin = vue.computed(() => {
        const token = uni.getStorageSync("token") || "";
        return !!token || !!global$1.isLogin;
      });
      const currentItemCount = vue.computed(
        () => submission.items && submission.items.length ? submission.items.length : 0
      );
      const maxSubmissionsPerUser = vue.computed(
        () => Number(plan.max_submissions_per_user || 0)
      );
      const canAddItem = vue.computed(() => {
        if (!isLogin.value)
          return false;
        if (!submission.submission_id)
          return false;
        const max = maxSubmissionsPerUser.value;
        if (!max || max <= 0) {
          return true;
        }
        return currentItemCount.value < max;
      });
      const planBasicText = vue.computed(() => {
        if (!plan.id)
          return "";
        const typeMap = { 1: "常驻投递", 2: "限时手速", 3: "限时抽选", 4: "限时黑箱" };
        const mode = typeMap[Number(plan.order_type) || 0] || "档期";
        const open = fmtTime(plan.open_time);
        const close = fmtTime(plan.close_time);
        if (plan.open_time && plan.close_time) {
          return `${mode} · ${open} ~ ${close}`;
        }
        return mode;
      });
      const artistLogo = vue.computed(() => {
        if (plan.artist_info) {
          return plan.artist_info.LogoImage || plan.artist_info.logo_image || "";
        }
        if (plan.brand) {
          return plan.brand.logo_image || "";
        }
        return "";
      });
      const artistBrandName = vue.computed(() => {
        if (plan.artist_info) {
          return plan.artist_info.BrandName || plan.artist_info.brand_name || "";
        }
        if (plan.brand) {
          return plan.brand.brand_name || "";
        }
        return "";
      });
      const totalPrice = vue.computed(() => {
        if (!submission.items || !submission.items.length)
          return 0;
        let sum = 0;
        submission.items.forEach((it2) => {
          const v2 = Number(it2.price_total || 0);
          if (!Number.isNaN(v2)) {
            sum += v2;
          }
        });
        return Number(sum.toFixed(2));
      });
      function fmtTime(ts2) {
        if (!ts2)
          return "--";
        const d2 = new Date(Number(ts2) * 1e3);
        if (Number.isNaN(d2.getTime()))
          return "--";
        const y2 = d2.getFullYear();
        const m2 = String(d2.getMonth() + 1).padStart(2, "0");
        const dd = String(d2.getDate()).padStart(2, "0");
        const hh = String(d2.getHours()).padStart(2, "0");
        const mi = String(d2.getMinutes()).padStart(2, "0");
        return `${y2}-${m2}-${dd} ${hh}:${mi}`;
      }
      function getFirstRefImage(refImages) {
        if (!refImages)
          return "";
        if (Array.isArray(refImages)) {
          return refImages[0] || "";
        }
        const str = String(refImages);
        if (!str)
          return "";
        const parts = str.split(",").map((s2) => s2.trim()).filter(Boolean);
        return parts[0] || "";
      }
      function fillSubmissionFromQueueInfo(raw) {
        if (!raw)
          return;
        submission.submission_id = Number(
          raw.submission_id || submission.submission_id || submissionId.value || 0
        );
        submission.plan_id = Number(raw.plan_id || submission.plan_id || 0);
        submission.mode = Number(raw.mode || 0);
        if (typeof raw.ahead_count === "number") {
          submission.ahead_count = raw.ahead_count;
        }
        if (typeof raw.status === "number") {
          submission.status = raw.status;
        }
        submission.status_text = raw.status_text || "";
        if (typeof raw.created_at === "number") {
          submission.created_at = raw.created_at;
        }
        if (Array.isArray(raw.items)) {
          submission.items = raw.items;
        } else {
          submission.items = [];
        }
        if (Array.isArray(raw.draft_items)) {
          draftItems.value = raw.draft_items;
        } else {
          draftItems.value = [];
        }
      }
      async function fetchDetail() {
        if (!isLogin.value || !submissionId.value)
          return;
        if (!hasFirstLoaded.value) {
          loading2.value = true;
          errorMsg.value = "";
        }
        try {
          const token = uni.getStorageSync("token") || "";
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/submission/queue-info`,
            method: "GET",
            header: { Authorization: token },
            data: { submission_id: submissionId.value }
          });
          const body = res.data || {};
          if (String(body.status).toLowerCase() !== "success") {
            if (!hasFirstLoaded.value) {
              throw new Error(body.msg || "加载失败");
            }
            formatAppLog("warn", "at pkg-creator/creator_order/submission_detail/submission_detail.vue:558", "轮询 submission queue-info 失败：", body.msg);
            return;
          }
          const raw = body.data || {};
          fillSubmissionFromQueueInfo(raw);
          if (!hasLoadedPlan.value && submission.plan_id) {
            fetchPlan(submission.plan_id);
          }
          hasFirstLoaded.value = true;
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_detail/submission_detail.vue:571", "加载 submission queue-info 失败", e2);
          if (!hasFirstLoaded.value) {
            errorMsg.value = (e2 == null ? void 0 : e2.message) || "加载失败，请稍后重试";
          }
        } finally {
          loading2.value = false;
        }
      }
      async function fetchPlan(planId) {
        if (!planId)
          return;
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/brand-artist/order-plan-info`,
            method: "GET",
            data: { id: planId }
          });
          const body = res.data || {};
          if (String(body.status).toLowerCase() !== "success") {
            formatAppLog("warn", "at pkg-creator/creator_order/submission_detail/submission_detail.vue:591", "加载 plan 详情失败：", body.msg);
            return;
          }
          const d2 = body.data || {};
          plan.id = d2.id || planId;
          plan.order_type = d2.order_type || 0;
          plan.max_submissions_per_user = d2.max_submissions_per_user ?? 0;
          plan.open_time = Number(d2.open_time || 0);
          plan.close_time = Number(d2.close_time || 0);
          plan.artist_name = d2.artist_name || "";
          plan.artist_info = d2.artist_info || null;
          plan.brand = d2.brand || null;
          hasLoadedPlan.value = true;
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_detail/submission_detail.vue:607", "请求 plan 详情异常", e2);
        }
      }
      function reloadDetail() {
        hasFirstLoaded.value = false;
        errorMsg.value = "";
        fetchDetail();
      }
      function goCreateItem() {
        if (!submission.submission_id || !submission.plan_id)
          return;
        const url = `/pkg-creator/creator_order/submission_item_form/submission_item_form?submission_id=${submission.submission_id}&plan_id=${submission.plan_id}`;
        uni.navigateTo({ url });
      }
      function goEditItem(item) {
        if (!item || !item.id)
          return;
        const url = `/pkg-creator/creator_order/submission_item_form/submission_item_form?submission_item_id=${item.id}`;
        uni.navigateTo({ url });
      }
      async function useDraft(draft) {
        if (!draft || !draft.id)
          return;
        if (!submission.submission_id) {
          uni.showToast({ title: "投递信息异常，无法绑定", icon: "none" });
          return;
        }
        const token = uni.getStorageSync("token") || "";
        uni.showLoading({ title: "绑定中...", mask: true });
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/item/attach`,
            method: "POST",
            header: {
              "Content-Type": "application/json",
              Authorization: token
            },
            data: {
              submission_id: submission.submission_id,
              item_ids: [draft.id]
            }
          });
          const body = res.data || {};
          if (String(body.status).toLowerCase() !== "success") {
            throw new Error(body.msg || "绑定失败");
          }
          uni.showToast({ title: "已绑定到本次投递", icon: "success" });
          fetchDetail();
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_detail/submission_detail.vue:678", "绑定草稿失败", e2);
          uni.showToast({ title: (e2 == null ? void 0 : e2.message) || "绑定失败", icon: "none" });
        } finally {
          uni.hideLoading();
        }
      }
      function handleStartChat() {
        if (!isLogin.value) {
          uni.showToast({ title: "请先登录后再发起对话", icon: "none" });
          return;
        }
        if (!plan.artist_info || !plan.artist_info.id) {
          uni.showToast({ title: "暂时无法获取作者信息", icon: "none" });
          return;
        }
        const artistId = plan.artist_info.id;
        const url = `/pkg-im/chat/chat?artist_id=${artistId}`;
        uni.navigateTo({ url });
      }
      function goPay() {
        if (!isLogin.value) {
          uni.showToast({ title: "请先登录后再付款", icon: "none" });
          return;
        }
        if (!submission.submission_id) {
          uni.showToast({ title: "投递信息异常，无法付款", icon: "none" });
          return;
        }
        const url = `/pkg-creator/creator_order/submission_pay/submission_pay?submission_id=${submission.submission_id}`;
        uni.navigateTo({ url });
      }
      function startPolling() {
        stopPolling();
        fetchDetail();
        pollingTimer.value = setInterval(fetchDetail, 5e3);
      }
      function stopPolling() {
        if (pollingTimer.value) {
          clearInterval(pollingTimer.value);
          pollingTimer.value = null;
        }
      }
      onLoad((q2 = {}) => {
        submissionId.value = Number(q2.submission_id || 0);
        uni.setNavigationBarTitle({ title: "投递详情" });
      });
      onShow(() => {
        if (!isLogin.value || !submissionId.value)
          return;
        startPolling();
      });
      onUnload(() => {
        stopPolling();
      });
      const __returned__ = { submissionId, loading: loading2, hasFirstLoaded, errorMsg, pollingTimer, submission, draftItems, plan, hasLoadedPlan, isLogin, currentItemCount, maxSubmissionsPerUser, canAddItem, planBasicText, artistLogo, artistBrandName, totalPrice, fmtTime, getFirstRefImage, fillSubmissionFromQueueInfo, fetchDetail, fetchPlan, reloadDetail, goCreateItem, goEditItem, useDraft, handleStartChat, goPay, startPolling, stopPolling, ref: vue.ref, reactive: vue.reactive, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get onUnload() {
        return onUnload;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$5(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "submission-page" }, [
      vue.createElementVNode("scroll-view", {
        class: "scroll-body",
        "scroll-y": "",
        "show-scrollbar": false
      }, [
        !$setup.isLogin ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "state-box"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "请先登录"),
          vue.createElementVNode("text", { class: "state-desc" }, "登录后可以查看本次投递详情。")
        ])) : !$setup.submissionId ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "state-box state-error"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "缺少必要参数"),
          vue.createElementVNode("text", { class: "state-desc" }, "需要提供 submission_id 才能查看详情。")
        ])) : $setup.loading && !$setup.hasFirstLoaded ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "state-box"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "加载中…"),
          vue.createElementVNode("text", { class: "state-desc" }, "正在加载投递详情")
        ])) : $setup.errorMsg ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "state-box state-error"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "加载失败"),
          vue.createElementVNode(
            "text",
            { class: "state-desc" },
            vue.toDisplayString($setup.errorMsg),
            1
            /* TEXT */
          ),
          vue.createElementVNode("button", {
            class: "primary-btn",
            type: "default",
            onClick: $setup.reloadDetail
          }, "重试")
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 4,
          class: "content"
        }, [
          vue.createElementVNode("view", { class: "card" }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              $setup.artistLogo || $setup.artistBrandName ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "card-header-artist"
              }, [
                $setup.artistLogo ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  src: $setup.artistLogo,
                  class: "artist-avatar",
                  mode: "aspectFill"
                }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "artist-meta" }, [
                  vue.createElementVNode(
                    "text",
                    { class: "artist-name" },
                    vue.toDisplayString($setup.artistBrandName || "投递作者"),
                    1
                    /* TEXT */
                  ),
                  $setup.planBasicText ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "artist-plan-text"
                    },
                    vue.toDisplayString($setup.planBasicText),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ])) : (vue.openBlock(), vue.createElementBlock(
                vue.Fragment,
                { key: 1 },
                [
                  vue.createElementVNode("text", { class: "card-title" }, "投递详情"),
                  $setup.planBasicText ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    {
                      key: 0,
                      class: "card-sub"
                    },
                    vue.toDisplayString($setup.planBasicText),
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ],
                64
                /* STABLE_FRAGMENT */
              ))
            ]),
            vue.createElementVNode("view", { class: "info-row" }, [
              vue.createElementVNode("text", { class: "info-label" }, "投递编号"),
              vue.createElementVNode(
                "text",
                { class: "info-val" },
                "#" + vue.toDisplayString($setup.submission.submission_id),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "info-row" }, [
              vue.createElementVNode("text", { class: "info-label" }, "投递状态"),
              vue.createElementVNode(
                "text",
                { class: "info-val" },
                vue.toDisplayString($setup.submission.status_text || "—"),
                1
                /* TEXT */
              )
            ]),
            $setup.plan.artist_name ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "info-row"
            }, [
              vue.createElementVNode("text", { class: "info-label" }, "本次投递开单"),
              vue.createElementVNode(
                "text",
                { class: "info-val" },
                vue.toDisplayString($setup.plan.artist_name),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "info-row" }, [
              vue.createElementVNode("text", { class: "info-label" }, "投递时间"),
              vue.createElementVNode(
                "text",
                { class: "info-val" },
                vue.toDisplayString($setup.submission.created_at ? $setup.fmtTime($setup.submission.created_at) : "--"),
                1
                /* TEXT */
              )
            ]),
            $setup.maxSubmissionsPerUser > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "info-row"
            }, [
              vue.createElementVNode("text", { class: "info-label" }, "本次可投递头数上限"),
              vue.createElementVNode("text", { class: "info-val" }, [
                vue.createTextVNode(
                  vue.toDisplayString($setup.maxSubmissionsPerUser) + " 颗 ",
                  1
                  /* TEXT */
                ),
                vue.createElementVNode(
                  "text",
                  { class: "info-extra" },
                  " （当前已填：" + vue.toDisplayString($setup.currentItemCount) + " 颗） ",
                  1
                  /* TEXT */
                )
              ])
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          vue.createElementVNode("view", { class: "card" }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              vue.createElementVNode("text", { class: "card-title" }, "排队进度")
            ]),
            vue.createElementVNode("view", { class: "queue-summary" }, [
              vue.createElementVNode("view", { class: "q-item" }, [
                vue.createElementVNode("text", { class: "q-label" }, "当前状态"),
                vue.createElementVNode(
                  "text",
                  { class: "q-value" },
                  vue.toDisplayString($setup.submission.status_text || "—"),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "q-item" }, [
                vue.createElementVNode("text", { class: "q-label" }, "前面还有"),
                vue.createElementVNode("text", { class: "q-value" }, [
                  typeof $setup.submission.ahead_count === "number" ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    { key: 0 },
                    vue.toDisplayString($setup.submission.ahead_count) + " 人 ",
                    1
                    /* TEXT */
                  )) : (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "--"))
                ])
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "card" }, [
            vue.createElementVNode("view", { class: "card-header card-header-with-action" }, [
              vue.createElementVNode("view", null, [
                vue.createElementVNode("text", { class: "card-title" }, "本次投递的娃头"),
                vue.createElementVNode("text", { class: "card-sub" }, [
                  vue.createTextVNode(
                    " 已填 " + vue.toDisplayString($setup.currentItemCount) + " 颗 ",
                    1
                    /* TEXT */
                  ),
                  $setup.maxSubmissionsPerUser > 0 ? (vue.openBlock(), vue.createElementBlock(
                    "text",
                    { key: 0 },
                    " / 最多 " + vue.toDisplayString($setup.maxSubmissionsPerUser) + " 颗 ",
                    1
                    /* TEXT */
                  )) : vue.createCommentVNode("v-if", true)
                ])
              ]),
              $setup.canAddItem ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "card-action"
              }, [
                vue.createElementVNode("button", {
                  class: "mini-add-btn",
                  type: "default",
                  onClick: $setup.goCreateItem
                }, [
                  vue.createVNode(_component_uni_icons, {
                    class: "mini-add-icon",
                    type: "plus",
                    size: "18",
                    color: "#ffffff"
                  }),
                  vue.createElementVNode("text", { class: "mini-add-text" }, "新增投递")
                ])
              ])) : vue.createCommentVNode("v-if", true)
            ]),
            !$setup.submission.items || !$setup.submission.items.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "empty-box"
            }, [
              vue.createElementVNode("text", { class: "empty-text" }, "还没有填写任何娃头信息。"),
              $setup.canAddItem ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "empty-tip"
              }, " 点右上角「新增投递」开始填写。 ")) : vue.createCommentVNode("v-if", true)
            ])) : vue.createCommentVNode("v-if", true),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.submission.items, (item) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "item-row"
                }, [
                  $setup.getFirstRefImage(item.ref_images) ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    class: "item-thumb",
                    src: $setup.getFirstRefImage(item.ref_images),
                    mode: "aspectFill"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode("view", {
                    class: "item-main",
                    onClick: ($event) => $setup.goEditItem(item)
                  }, [
                    vue.createElementVNode("view", { class: "item-title-row" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "item-title" },
                        vue.toDisplayString(item.work_subject || "未命名作品"),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode("view", { class: "item-tags" }, [
                      item.size ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 0,
                          class: "item-tag"
                        },
                        " 尺寸：" + vue.toDisplayString(item.size),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true),
                      item.tier_title ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 1,
                          class: "item-tag"
                        },
                        " 档位：" + vue.toDisplayString(item.tier_title),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true),
                      item.price_total !== void 0 && item.price_total !== null ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 2,
                          class: "item-tag"
                        },
                        " 总价：¥" + vue.toDisplayString(Number(item.price_total)),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ]),
                    item.remark ? (vue.openBlock(), vue.createElementBlock(
                      "view",
                      {
                        key: 0,
                        class: "item-remark"
                      },
                      vue.toDisplayString(item.remark),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ], 8, ["onClick"]),
                  vue.createElementVNode("view", { class: "item-right" }, [
                    vue.createElementVNode("button", {
                      class: "edit-btn",
                      type: "default",
                      size: "mini",
                      onClick: vue.withModifiers(($event) => $setup.goEditItem(item), ["stop"])
                    }, [
                      vue.createElementVNode("text", { class: "edit-text" }, "编辑")
                    ], 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            )),
            $setup.draftItems.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "draft-section"
            }, [
              vue.createElementVNode("view", { class: "draft-header" }, [
                vue.createElementVNode("text", { class: "draft-title" }, "我的草稿"),
                vue.createElementVNode("text", { class: "draft-sub" }, "可将草稿绑定到本次投递")
              ]),
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.draftItems, (d2) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: d2.id,
                    class: "draft-row"
                  }, [
                    $setup.getFirstRefImage(d2.ref_images) ? (vue.openBlock(), vue.createElementBlock("image", {
                      key: 0,
                      class: "item-thumb",
                      src: $setup.getFirstRefImage(d2.ref_images),
                      mode: "aspectFill"
                    }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("view", {
                      class: "draft-main",
                      onClick: ($event) => $setup.goEditItem(d2)
                    }, [
                      vue.createElementVNode("view", { class: "item-title-row" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "item-title" },
                          vue.toDisplayString(d2.work_subject || "未命名作品"),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "item-tags" }, [
                        d2.size ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 0,
                            class: "item-tag"
                          },
                          " 尺寸：" + vue.toDisplayString(d2.size),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true),
                        d2.tier_title ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 1,
                            class: "item-tag"
                          },
                          " 档位：" + vue.toDisplayString(d2.tier_title),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true),
                        d2.price_total !== void 0 && d2.price_total !== null ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 2,
                            class: "item-tag"
                          },
                          " 总价：¥" + vue.toDisplayString(Number(d2.price_total)),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ])
                    ], 8, ["onClick"]),
                    vue.createElementVNode("view", { class: "draft-right" }, [
                      vue.createElementVNode("button", {
                        class: "use-btn",
                        type: "default",
                        size: "mini",
                        onClick: vue.withModifiers(($event) => $setup.useDraft(d2), ["stop"])
                      }, [
                        vue.createElementVNode("text", { class: "use-text" }, "使用")
                      ], 8, ["onClick"])
                    ])
                  ]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          $setup.artistLogo ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "chat-entry"
          }, [
            vue.createElementVNode("button", {
              class: "chat-btn",
              type: "default",
              onClick: $setup.handleStartChat
            }, [
              vue.createElementVNode("image", {
                src: $setup.artistLogo,
                class: "chat-logo",
                mode: "aspectFill"
              }, null, 8, ["src"]),
              vue.createElementVNode("text", { class: "chat-text" }, "发起对话"),
              vue.createVNode(_component_uni_icons, {
                class: "chat-arrow",
                type: "arrowright",
                size: "20",
                color: "#9ca3af"
              })
            ])
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("view", { class: "hint-box" }, [
            vue.createElementVNode("text", { class: "hint-text" }, " 提示：本页展示你在当前档期下的本次投递信息和排队进度。如需修改投递内容，可点击每一条投递右侧的「编辑」按钮。 ")
          ]),
          vue.createElementVNode("view", { style: { "height": "180rpx" } })
        ]))
      ]),
      $setup.isLogin && $setup.submission.submission_id ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "bottom-bar"
      }, [
        vue.createElementVNode("button", {
          class: "pay-btn",
          type: "default",
          onClick: $setup.goPay
        }, [
          $setup.totalPrice > 0 ? (vue.openBlock(), vue.createElementBlock(
            "text",
            {
              key: 0,
              class: "pay-amount"
            },
            " 合计 ¥" + vue.toDisplayString($setup.totalPrice) + " · ",
            1
            /* TEXT */
          )) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode("text", { class: "pay-text" }, "去付款")
        ])
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PkgCreatorCreatorOrderSubmissionDetailSubmissionDetail = /* @__PURE__ */ _export_sfc(_sfc_main$6, [["render", _sfc_render$5], ["__scopeId", "data-v-5bddba3d"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/submission_detail/submission_detail.vue"]]);
  const _imports_0$1 = "/static/new-icon/20gf-pointer-copy.png";
  const _imports_1$1 = "/static/new-icon/thinking.png";
  const _imports_2 = "/static/new-icon/input.png";
  const HEAD_MODE_KEY_PREFIX = "__artist_item_head_mode_";
  const _sfc_main$5 = {
    __name: "submission_item_form",
    setup(__props, { expose: __expose }) {
      __expose();
      const submissionId = vue.ref(0);
      const itemId = vue.ref(0);
      const planId = vue.ref(0);
      const headMode = vue.ref("select");
      const hasHeadModeFromStorage = vue.ref(false);
      function loadHeadModeSnapshot() {
        if (!itemId.value)
          return;
        try {
          const key = HEAD_MODE_KEY_PREFIX + itemId.value;
          const snap = uni.getStorageSync(key);
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:356", "[item-form] loadHeadModeSnapshot key=", key, "snap=", snap);
          if (snap === "select" || snap === "cabinet" || snap === "manual") {
            headMode.value = snap;
            hasHeadModeFromStorage.value = true;
          }
        } catch (e2) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:362", "[item-form] loadHeadModeSnapshot error", e2);
          hasHeadModeFromStorage.value = false;
        }
      }
      function saveHeadModeSnapshot() {
        if (!itemId.value)
          return;
        try {
          const key = HEAD_MODE_KEY_PREFIX + itemId.value;
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:370", "[item-form] saveHeadModeSnapshot key=", key, "mode=", headMode.value);
          uni.setStorageSync(key, headMode.value);
        } catch (e2) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:373", "[item-form] saveHeadModeSnapshot error", e2);
        }
      }
      const loading2 = vue.ref(false);
      const saving = vue.ref(false);
      const errorMsg = vue.ref("");
      const hasLoadedItem = vue.ref(false);
      const form = vue.reactive({
        work_subject: "",
        // 娃头名称（手动输入 or 从商品 / 娃柜名称带出）
        size: "",
        // 尺寸
        tier_title: "",
        // 档位标题
        price_text: "",
        // 价格预估，仅前端展示
        remark: "",
        // 补充说明
        subject_goods_id: 0,
        // 选中的娃物 ID（分步选择模式下）
        ref_images: []
        // 参考图（最多 3 张）
      });
      const selectedBrand = vue.ref({});
      const selectedGoods = vue.ref({});
      const cabinetItem = vue.ref(null);
      const firstRefImage = vue.computed(() => {
        if (Array.isArray(form.ref_images) && form.ref_images.length > 0) {
          return form.ref_images[0];
        }
        return "";
      });
      const sizeOptions = vue.ref([]);
      const tierOptions = vue.ref([]);
      const addonOptions = vue.ref([]);
      const selectedSizeIndex = vue.ref(-1);
      const selectedTierIndex = vue.ref(-1);
      const selectedAddonIndexes = vue.ref([]);
      const rawAddonTitles = vue.ref([]);
      const hasPlanConfig = vue.computed(() => {
        return sizeOptions.value.length || tierOptions.value.length || addonOptions.value.length;
      });
      const isEdit = vue.computed(() => itemId.value > 0);
      const isLogin = vue.computed(() => {
        const token = uni.getStorageSync("token") || "";
        return !!token;
      });
      const uploading = vue.ref(false);
      const uploadProgress = vue.ref(0);
      const uploadStatusText = vue.ref("");
      function resetStateForNewItem() {
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:440", "[item-form] resetStateForNewItem, oldItemId=", itemId.value);
        loading2.value = false;
        saving.value = false;
        errorMsg.value = "";
        hasLoadedItem.value = false;
        form.work_subject = "";
        form.size = "";
        form.tier_title = "";
        form.price_text = "";
        form.remark = "";
        form.subject_goods_id = 0;
        form.ref_images = [];
        selectedBrand.value = {};
        selectedGoods.value = {};
        cabinetItem.value = null;
        sizeOptions.value = [];
        tierOptions.value = [];
        addonOptions.value = [];
        selectedSizeIndex.value = -1;
        selectedTierIndex.value = -1;
        selectedAddonIndexes.value = [];
        rawAddonTitles.value = [];
        uploading.value = false;
        uploadProgress.value = 0;
        uploadStatusText.value = "";
        headMode.value = "select";
        hasHeadModeFromStorage.value = false;
      }
      function syncRouteParamsFromCurrentPage() {
        let pages2 = [];
        try {
          pages2 = getCurrentPages && getCurrentPages();
        } catch (e2) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:482", "[item-form] syncRouteParams getCurrentPages error", e2);
        }
        const cur = pages2 && pages2.length ? pages2[pages2.length - 1] : null;
        const opts = cur && cur.options || {};
        const newSubmissionId = Number(opts.submission_id || 0);
        const newItemId = Number(opts.item_id || opts.submission_item_id || 0);
        const newPlanId = Number(opts.plan_id || opts.order_plan_id || 0);
        formatAppLog(
          "log",
          "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:491",
          "[item-form] syncRouteParamsFromCurrentPage opts=",
          opts,
          "prevSubmissionId=",
          submissionId.value,
          "prevItemId=",
          itemId.value,
          "prevPlanId=",
          planId.value,
          "newSubmissionId=",
          newSubmissionId,
          "newItemId=",
          newItemId,
          "newPlanId=",
          newPlanId
        );
        if (newItemId && newItemId !== itemId.value) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:502", "[item-form] detect item switch, oldItemId=", itemId.value, "newItemId=", newItemId);
          resetStateForNewItem();
          submissionId.value = newSubmissionId;
          itemId.value = newItemId;
          planId.value = newPlanId;
          if (itemId.value) {
            loadHeadModeSnapshot();
          }
          if (planId.value) {
            fetchPlanInfo();
          }
        } else {
          if (!itemId.value && newItemId) {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:517", "[item-form] syncRouteParams: init itemId from opts, newItemId=", newItemId);
            itemId.value = newItemId;
          }
          if (!planId.value && newPlanId) {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:521", "[item-form] syncRouteParams: init planId from opts, newPlanId=", newPlanId);
            planId.value = newPlanId;
          }
          if (!submissionId.value && newSubmissionId) {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:525", "[item-form] syncRouteParams: init submissionId from opts, newSubmissionId=", newSubmissionId);
            submissionId.value = newSubmissionId;
          }
        }
      }
      function switchHeadMode(mode) {
        if (headMode.value === mode)
          return;
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:534", "[item-form] switchHeadMode from", headMode.value, "to", mode, "itemId=", itemId.value);
        headMode.value = mode;
        if (mode !== "select") {
          form.subject_goods_id = 0;
          selectedGoods.value = {};
          selectedBrand.value = {};
        }
        if (mode !== "cabinet") {
          cabinetItem.value = null;
        }
        saveHeadModeSnapshot();
      }
      function goPickHead() {
        if (headMode.value !== "select") {
          headMode.value = "select";
          saveHeadModeSnapshot();
        }
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:561", "[item-form] goPickHead itemId=", itemId.value);
        uni.$once("associate:done", (payload) => {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:565", "[item-form] associate:done payload=", payload);
          const brandInfo = payload && payload.brand;
          const goodsInfo = payload && payload.goods;
          selectedBrand.value = brandInfo || {};
          selectedGoods.value = goodsInfo || {};
          if (goodsInfo && goodsInfo.id) {
            form.subject_goods_id = goodsInfo.id;
            form.work_subject = goodsInfo.name || "";
            if (goodsInfo.cover) {
              form.ref_images = [goodsInfo.cover];
            }
          }
          saveHeadModeSnapshot();
        });
        uni.navigateTo({
          url: "/pkg-common/brand-pick/brand-pick",
          success(res) {
            res.eventChannel && res.eventChannel.emit("associate:init", {
              preselect: {
                brand: selectedBrand.value && selectedBrand.value.id ? selectedBrand.value : null,
                goods: selectedGoods.value && selectedGoods.value.id ? selectedGoods.value : null
              }
            });
          }
        });
      }
      function goPickFromCabinet() {
        if (headMode.value !== "cabinet") {
          headMode.value = "cabinet";
          saveHeadModeSnapshot();
        }
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:614", "[item-form] goPickFromCabinet itemId=", itemId.value);
        try {
          uni.removeStorageSync("__pickedMyItemFromStock");
        } catch (e2) {
        }
        uni.navigateTo({
          url: "/pkg-stock/stock-myitems-search/stock-myitems-search?pickMode=1",
          events: {
            pickMyItemFromStock(payload) {
              formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:624", "[item-form] eventChannel pickMyItemFromStock payload=", payload);
              applyPickedCabinetItem(payload);
            }
          }
        });
      }
      function applyPickedCabinetItem(payload) {
        if (!payload)
          return;
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:634", "[item-form] applyPickedCabinetItem payload=", payload);
        cabinetItem.value = payload;
        const name = payload.name || payload.title || "";
        if (name) {
          form.work_subject = name;
        }
        if (payload.image) {
          form.ref_images = [payload.image];
        }
        headMode.value = "cabinet";
        saveHeadModeSnapshot();
      }
      function normalizeRefImages(val) {
        if (!val)
          return [];
        if (Array.isArray(val)) {
          return val.filter(Boolean);
        }
        if (typeof val === "string") {
          return val.split(",").map(function(s2) {
            return s2.trim();
          }).filter(Boolean);
        }
        return [];
      }
      function decodeAddonsJson(base64Str) {
        if (!base64Str)
          return [];
        try {
          if (typeof atob !== "function") {
            formatAppLog("warn", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:666", "[item-form] atob not found, skip decode addons_json");
            return [];
          }
          const text = decodeURIComponent(escape(atob(base64Str)));
          const arr = JSON.parse(text);
          return Array.isArray(arr) ? arr : [];
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:674", "[item-form] decode addons_json failed", e2, "base64=", base64Str);
          return [];
        }
      }
      function syncSelectionsFromFormAndPlan() {
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:681", "[item-form] syncSelectionsFromFormAndPlan form.size=", form.size, "form.tier_title=", form.tier_title, "rawAddonTitles=", rawAddonTitles.value);
        if (form.size && sizeOptions.value.length) {
          const idx1 = sizeOptions.value.findIndex(function(s2) {
            return String(s2.size) === String(form.size);
          });
          selectedSizeIndex.value = idx1;
        }
        if (form.tier_title && tierOptions.value.length) {
          const idx2 = tierOptions.value.findIndex(function(t2) {
            return String(t2.title) === String(form.tier_title);
          });
          selectedTierIndex.value = idx2;
        }
        if (addonOptions.value.length && rawAddonTitles.value && rawAddonTitles.value.length) {
          const selected = [];
          addonOptions.value.forEach(function(addon, idx) {
            const t2 = addon && addon.title;
            if (t2 && rawAddonTitles.value.indexOf(String(t2)) > -1) {
              selected.push(idx);
            }
          });
          selectedAddonIndexes.value = selected;
        }
        if (selectedTierIndex.value >= 0) {
          recalcPriceFromPlan();
        }
      }
      function fillFormFromRaw(raw) {
        if (!raw)
          return;
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:719", "[item-form] fillFormFromRaw id=", raw.id, "work_subject=", raw.work_subject, "size=", raw.size);
        form.work_subject = raw.work_subject || raw.title || "";
        form.size = raw.size || "";
        form.tier_title = raw.tier_title || raw.tier || "";
        if (typeof raw.price_total !== "undefined" && raw.price_total !== null) {
          const n2 = Number(raw.price_total);
          form.price_text = isNaN(n2) ? "" : String(n2);
        } else {
          form.price_text = "";
        }
        form.remark = raw.remark || raw.note || "";
        form.ref_images = normalizeRefImages(raw.ref_images || raw.images || raw.ref_images_str);
        form.subject_goods_id = Number(raw.subject_goods_id || raw.goods_id || 0);
        const goodsId = form.subject_goods_id;
        if (goodsId > 0) {
          selectedGoods.value = {
            id: goodsId,
            name: raw.subject_goods_name || raw.goods_name || raw.work_subject || form.work_subject,
            cover: form.ref_images && form.ref_images.length > 0 ? form.ref_images[0] : raw.subject_goods_cover || raw.goods_cover || ""
          };
          selectedBrand.value = {
            id: raw.subject_brand_id || raw.brand_id || 0,
            brand_name: raw.subject_brand_name || raw.brand_name || ""
          };
        } else {
          selectedGoods.value = {};
          selectedBrand.value = {};
        }
        rawAddonTitles.value = [];
        if (raw.addons_json) {
          const arr = decodeAddonsJson(raw.addons_json);
          rawAddonTitles.value = arr.map(function(a2) {
            return a2 && a2.title;
          }).filter(function(t2) {
            return !!t2;
          });
        }
        if (!hasHeadModeFromStorage.value) {
          if (goodsId > 0) {
            headMode.value = "select";
          } else if (raw.from_cabinet) {
            headMode.value = "cabinet";
          } else if (form.work_subject) {
            headMode.value = "manual";
          } else {
            headMode.value = "select";
          }
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:775", "[item-form] fillFormFromRaw guess headMode =", headMode.value);
        }
        if (sizeOptions.value.length || tierOptions.value.length || addonOptions.value.length) {
          syncSelectionsFromFormAndPlan();
        }
      }
      async function fetchItem(force) {
        const reqItemId = itemId.value;
        if (!reqItemId) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:790", "[item-form] fetchItem skip, no reqItemId");
          return;
        }
        if (!isEdit.value) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:795", "[item-form] fetchItem skip, not edit mode, itemId=", reqItemId);
          return;
        }
        if (hasLoadedItem.value && !force) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:799", "[item-form] fetchItem skip, hasLoadedItem=true & force=false, itemId=", reqItemId);
          return;
        }
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          errorMsg.value = "请先登录后再编辑投递信息";
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:806", "[item-form] fetchItem no token");
          return;
        }
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:810", "[item-form] fetchItem start, reqItemId=", reqItemId, "force=", force, "current itemId=", itemId.value);
        loading2.value = true;
        errorMsg.value = "";
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/with-state/artist-order/item/detail",
            method: "GET",
            header: {
              Authorization: token
            },
            data: {
              // 这里用的是当时记录下来的 reqItemId，而不是直接用 itemId.value
              item_id: reqItemId
            }
          });
          const body = res.data || {};
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:829", "[item-form] fetchItem response raw body=", body, "reqItemId=", reqItemId, "current itemId=", itemId.value);
          if (String(body.status).toLowerCase() !== "success") {
            throw new Error(body.msg || "接口返回失败");
          }
          const raw = body.data || {};
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:836", "[item-form] fetchItem data=", raw, "reqItemId=", reqItemId, "current itemId=", itemId.value);
          if (reqItemId !== itemId.value) {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:842", "[item-form] fetchItem stale response, ignore. reqItemId=", reqItemId, "current itemId=", itemId.value);
            return;
          }
          submissionId.value = Number(raw.submission_id || submissionId.value || 0);
          if (!planId.value) {
            planId.value = Number(raw.plan_id || raw.order_plan_id || 0);
            if (planId.value) {
              fetchPlanInfo();
            }
          }
          fillFormFromRaw(raw);
          hasLoadedItem.value = true;
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:859", "[item-form] 加载 item 详情失败", e2);
          errorMsg.value = e2 && e2.message ? e2.message : "加载失败，请稍后重试";
        } finally {
          if (reqItemId === itemId.value) {
            loading2.value = false;
          } else {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:866", "[item-form] fetchItem finally: skip loading=false 因为 itemId 已切换, reqItemId=", reqItemId, "current itemId=", itemId.value);
          }
        }
      }
      function reloadItem() {
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:872", "[item-form] reloadItem clicked itemId=", itemId.value);
        hasLoadedItem.value = false;
        fetchItem(true);
      }
      async function fetchPlanInfo() {
        if (!planId.value)
          return;
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:880", "[item-form] fetchPlanInfo planId=", planId.value);
        try {
          const res = await uni.request({
            url: websiteUrl.value + "/brand-artist/order-plan-info",
            method: "GET",
            data: { id: planId.value }
          });
          const body = res.data || {};
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:888", "[item-form] fetchPlanInfo body=", body);
          if (String(body.status).toLowerCase() !== "success") {
            return;
          }
          const data = body.data || {};
          let cfg = {};
          try {
            cfg = data.order_config ? JSON.parse(data.order_config) : {};
          } catch (e2) {
            formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:897", "[item-form] 解析 order_config 失败", e2);
            cfg = {};
          }
          sizeOptions.value = Array.isArray(cfg && cfg.extra && cfg.extra.size_surcharges) ? cfg.extra.size_surcharges : [];
          tierOptions.value = Array.isArray(cfg && cfg.tiers) ? cfg.tiers : [];
          addonOptions.value = Array.isArray(cfg && cfg.addons) ? cfg.addons : [];
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:907", "[item-form] plan parsed sizeOptions=", sizeOptions.value, "tierOptions=", tierOptions.value, "addonOptions=", addonOptions.value);
          if (form.size || form.tier_title || rawAddonTitles.value && rawAddonTitles.value.length) {
            syncSelectionsFromFormAndPlan();
          }
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:914", "[item-form] 加载 order plan 信息失败", e2);
        }
      }
      function recalcPriceFromPlan() {
        const tier = selectedTierIndex.value >= 0 ? tierOptions.value[selectedTierIndex.value] : null;
        if (!tier) {
          return;
        }
        let total = Number(tier.price) || 0;
        const size = selectedSizeIndex.value >= 0 ? sizeOptions.value[selectedSizeIndex.value] : null;
        if (size) {
          total += Number(size.price) || 0;
        }
        if (selectedAddonIndexes.value.length) {
          selectedAddonIndexes.value.forEach(function(idx) {
            const addon = addonOptions.value[idx];
            if (addon)
              total += Number(addon.price) || 0;
          });
        }
        form.price_text = String(total);
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:941", "[item-form] recalcPriceFromPlan total=", total);
      }
      function handleSelectSize(idx) {
        if (selectedSizeIndex.value === idx) {
          selectedSizeIndex.value = -1;
          form.size = "";
        } else {
          selectedSizeIndex.value = idx;
          const opt = sizeOptions.value[idx];
          form.size = opt && opt.size || "";
        }
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:954", "[item-form] handleSelectSize idx=", idx, "size=", form.size);
        if (selectedTierIndex.value >= 0) {
          recalcPriceFromPlan();
        }
      }
      function handleSelectTier(idx) {
        if (selectedTierIndex.value === idx) {
          selectedTierIndex.value = -1;
          form.tier_title = "";
        } else {
          selectedTierIndex.value = idx;
          const tier = tierOptions.value[idx];
          form.tier_title = tier && tier.title || "";
        }
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:970", "[item-form] handleSelectTier idx=", idx, "tier_title=", form.tier_title);
        recalcPriceFromPlan();
      }
      function toggleAddon(idx) {
        const list = selectedAddonIndexes.value.slice();
        const i2 = list.indexOf(idx);
        if (i2 > -1) {
          list.splice(i2, 1);
        } else {
          list.push(idx);
        }
        selectedAddonIndexes.value = list;
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:984", "[item-form] toggleAddon idx=", idx, "selectedAddonIndexes=", selectedAddonIndexes.value);
        if (selectedTierIndex.value >= 0) {
          recalcPriceFromPlan();
        }
      }
      function validateForm() {
        const subject = (form.work_subject || "").trim();
        if (headMode.value === "select") {
          if (!form.subject_goods_id) {
            uni.showToast({ title: "请选择娃头（分步选择）", icon: "none" });
            return false;
          }
        } else {
          if (!subject) {
            uni.showToast({ title: "请填写娃头名称", icon: "none" });
            return false;
          }
        }
        if (!isEdit.value && !planId.value) {
          uni.showToast({ title: "缺少 plan_id，无法保存", icon: "none" });
          return false;
        }
        return true;
      }
      async function handleUploadRefImages() {
        try {
          const currentCount = Array.isArray(form.ref_images) ? form.ref_images.length : 0;
          const remain = 3 - currentCount;
          if (remain <= 0) {
            uni.showToast({ title: "最多上传 3 张图片", icon: "none" });
            return;
          }
          const files = await chooseImageList(remain);
          if (!files || !files.length)
            return;
          uploading.value = true;
          uploadProgress.value = 0;
          for (let i2 = 0; i2 < files.length; i2++) {
            uploadStatusText.value = "获取上传凭证...";
            const tk = await getQiniuToken();
            if (!tk || !tk.token || !tk.path) {
              throw new Error("获取上传凭证失败");
            }
            const filePath = files[i2];
            uploadStatusText.value = "上传中 (" + (i2 + 1) + "/" + files.length + ")";
            uploadProgress.value = Math.round(i2 / files.length * 100);
            const ret = await uploadImageToQiniu(filePath, tk.token, tk.path);
            if (ret && ret.imageUrl) {
              if (!Array.isArray(form.ref_images)) {
                form.ref_images = [];
              }
              form.ref_images.push(ret.imageUrl);
              uploadProgress.value = Math.round((i2 + 1) / files.length * 100);
            }
          }
          uploadStatusText.value = "上传完成";
          uni.showToast({ title: "图片上传成功", icon: "success" });
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1054", "[item-form] 上传参考图片失败", e2);
          const msg = e2 && e2.message ? e2.message : "上传失败";
          uni.showToast({ title: msg, icon: "none" });
        } finally {
          uploading.value = false;
          setTimeout(function() {
            uploadProgress.value = 0;
            uploadStatusText.value = "";
          }, 500);
        }
      }
      function previewRefImage(url) {
        uni.previewImage({
          current: url,
          urls: form.ref_images
        });
      }
      function removeRefImage(index) {
        uni.showModal({
          title: "提示",
          content: "确定删除这张图片吗？",
          success(res) {
            if (res.confirm) {
              form.ref_images.splice(index, 1);
              uni.showToast({ title: "已删除", icon: "success" });
            }
          }
        });
      }
      async function handleSave() {
        if (!isLogin.value) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        if (!validateForm())
          return;
        const token = uni.getStorageSync("token") || "";
        if (!token) {
          uni.showToast({ title: "请先登录", icon: "none" });
          return;
        }
        const subjectText = (form.work_subject || "").trim();
        const subjectGoodsIdForSave = headMode.value === "select" ? form.subject_goods_id || 0 : 0;
        const addonTitles = selectedAddonIndexes.value.map(function(idx) {
          return addonOptions.value[idx] && addonOptions.value[idx].title;
        }).filter(function(t2) {
          return !!t2;
        });
        saving.value = true;
        try {
          uni.showLoading({ title: "保存中...", mask: true });
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1116", "[item-form] handleSave isEdit=", isEdit.value, "itemId=", itemId.value);
          if (isEdit.value) {
            const payload = {
              item_id: itemId.value,
              subject_goods_id: subjectGoodsIdForSave,
              work_subject: subjectText,
              size: (form.size || "").trim(),
              ref_images: Array.isArray(form.ref_images) ? form.ref_images : [],
              tier_title: (form.tier_title || "").trim(),
              addons: addonTitles,
              remark: (form.remark || "").trim()
            };
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1129", "[item-form] update payload=", payload);
            const res = await uni.request({
              url: websiteUrl.value + "/with-state/artist-order/item/update",
              method: "POST",
              header: {
                Authorization: token,
                "Content-Type": "application/json"
              },
              data: payload
            });
            const body = res.data || {};
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1142", "[item-form] update response body=", body);
            if (String(body.status).toLowerCase() !== "success") {
              uni.showToast({ title: body.msg || "保存失败", icon: "none" });
              return;
            }
            saveHeadModeSnapshot();
            uni.showToast({
              title: "保存成功",
              icon: "none"
            });
          } else {
            const createPayload = {
              plan_id: planId.value,
              subject_goods_id: subjectGoodsIdForSave,
              work_subject: subjectText,
              size: (form.size || "").trim(),
              ref_images: Array.isArray(form.ref_images) ? form.ref_images : [],
              tier_title: (form.tier_title || "").trim(),
              addons: addonTitles,
              remark: (form.remark || "").trim()
            };
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1166", "[item-form] create payload=", createPayload);
            const createRes = await uni.request({
              url: websiteUrl.value + "/with-state/artist-order/item/create",
              method: "POST",
              header: {
                Authorization: token,
                "Content-Type": "application/json"
              },
              data: createPayload
            });
            const createBody = createRes.data || {};
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1179", "[item-form] create response body=", createBody);
            if (String(createBody.status).toLowerCase() !== "success") {
              uni.showToast({ title: createBody.msg || "保存失败", icon: "none" });
              return;
            }
            const newItemId = Number(createBody.data && createBody.data.item_id || 0);
            if (!newItemId) {
              uni.showToast({ title: "返回的子项 ID 异常", icon: "none" });
              return;
            }
            itemId.value = newItemId;
            saveHeadModeSnapshot();
            if (submissionId.value > 0) {
              const attachRes = await uni.request({
                url: websiteUrl.value + "/with-state/artist-order/item/attach",
                method: "POST",
                header: {
                  Authorization: token,
                  "Content-Type": "application/json"
                },
                data: {
                  submission_id: submissionId.value,
                  item_ids: [newItemId]
                }
              });
              const attachBody = attachRes.data || {};
              formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1209", "[item-form] attach response body=", attachBody);
              if (String(attachBody.status).toLowerCase() !== "success") {
                uni.showToast({ title: attachBody.msg || "绑定失败", icon: "none" });
                return;
              }
            }
            uni.showToast({
              title: "新增成功",
              icon: "none"
            });
          }
          uni.hideLoading();
          setTimeout(function() {
            uni.navigateBack({ delta: 1 });
          }, 500);
        } catch (e2) {
          formatAppLog("error", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1228", "[item-form] 保存 submission item 失败", e2);
          uni.hideLoading();
          uni.showToast({ title: "网络异常，保存失败", icon: "none" });
        } finally {
          saving.value = false;
        }
      }
      onLoad(function(q2) {
        q2 = q2 || {};
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1239", "[item-form] onLoad q=", q2);
        uni.setNavigationBarTitle({ title: "新增投递" });
        submissionId.value = Number(q2.submission_id || 0);
        itemId.value = Number(q2.item_id || q2.submission_item_id || 0);
        planId.value = Number(q2.plan_id || q2.order_plan_id || 0);
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1246", "[item-form] onLoad parsed submissionId=", submissionId.value, "itemId=", itemId.value, "planId=", planId.value);
        if (itemId.value) {
          loadHeadModeSnapshot();
        }
        const goodsIdFromQuery = Number(q2.goods_id || 0);
        if (goodsIdFromQuery) {
          form.subject_goods_id = goodsIdFromQuery;
          if (!hasHeadModeFromStorage.value) {
            headMode.value = "select";
          }
        }
        if (planId.value) {
          fetchPlanInfo();
        }
        if (itemId.value) {
          fetchItem(true);
        }
      });
      onShow(function() {
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1270", "[item-form] onShow enter, current itemId=", itemId.value, "planId=", planId.value, "submissionId=", submissionId.value);
        syncRouteParamsFromCurrentPage();
        formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1275", "[item-form] onShow after sync, itemId=", itemId.value, "planId=", planId.value, "submissionId=", submissionId.value);
        if (isEdit.value && !hasLoadedItem.value) {
          fetchItem(true);
        }
        try {
          const cached = uni.getStorageSync("__pickedMyItemFromStock");
          if (cached && cached.id) {
            formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1288", "[item-form] onShow found cached __pickedMyItemFromStock=", cached);
            applyPickedCabinetItem(cached);
            uni.removeStorageSync("__pickedMyItemFromStock");
          }
        } catch (e2) {
          formatAppLog("log", "at pkg-creator/creator_order/submission_item_form/submission_item_form.vue:1293", "[item-form] onShow read __pickedMyItemFromStock error", e2);
        }
      });
      const __returned__ = { submissionId, itemId, planId, headMode, HEAD_MODE_KEY_PREFIX, hasHeadModeFromStorage, loadHeadModeSnapshot, saveHeadModeSnapshot, loading: loading2, saving, errorMsg, hasLoadedItem, form, selectedBrand, selectedGoods, cabinetItem, firstRefImage, sizeOptions, tierOptions, addonOptions, selectedSizeIndex, selectedTierIndex, selectedAddonIndexes, rawAddonTitles, hasPlanConfig, isEdit, isLogin, uploading, uploadProgress, uploadStatusText, resetStateForNewItem, syncRouteParamsFromCurrentPage, switchHeadMode, goPickHead, goPickFromCabinet, applyPickedCabinetItem, normalizeRefImages, decodeAddonsJson, syncSelectionsFromFormAndPlan, fillFormFromRaw, fetchItem, reloadItem, fetchPlanInfo, recalcPriceFromPlan, handleSelectSize, handleSelectTier, toggleAddon, validateForm, handleUploadRefImages, previewRefImage, removeRefImage, handleSave, ref: vue.ref, reactive: vue.reactive, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get onShow() {
        return onShow;
      }, get websiteUrl() {
        return websiteUrl;
      }, get chooseImageList() {
        return chooseImageList;
      }, get getQiniuToken() {
        return getQiniuToken;
      }, get uploadImageToQiniu() {
        return uploadImageToQiniu;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$4(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    const _component_uni_icons = resolveEasycom(vue.resolveDynamicComponent("uni-icons"), __easycom_1$f);
    return vue.openBlock(), vue.createElementBlock("view", { class: "item-form-page" }, [
      vue.createVNode(_component_view_logs),
      vue.createElementVNode("scroll-view", {
        class: "scroll-body",
        "scroll-y": "",
        "show-scrollbar": false
      }, [
        !$setup.isLogin ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "state-box"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "请先登录"),
          vue.createElementVNode("text", { class: "state-desc" }, "登录后可以编辑本次投递的娃头信息。")
        ])) : !$setup.isEdit && !$setup.planId ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 1,
          class: "state-box state-error"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "缺少必要参数"),
          vue.createElementVNode("text", { class: "state-desc" }, "新增投递时必须携带 plan_id。")
        ])) : $setup.loading && $setup.isEdit ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "state-box"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "加载中…"),
          vue.createElementVNode("text", { class: "state-desc" }, "正在加载投递详情")
        ])) : $setup.errorMsg ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 3,
          class: "state-box state-error"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "加载失败"),
          vue.createElementVNode(
            "text",
            { class: "state-desc" },
            vue.toDisplayString($setup.errorMsg),
            1
            /* TEXT */
          ),
          vue.createElementVNode("button", {
            class: "primary-btn",
            type: "default",
            onClick: $setup.reloadItem
          }, "重试")
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 4,
          class: "content"
        }, [
          vue.createElementVNode("view", { class: "card" }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              vue.createElementVNode("text", { class: "card-title" }, "基本信息")
            ]),
            vue.createElementVNode("view", { class: "field-row" }, [
              vue.createElementVNode("view", { class: "head-mode-toggle" }, [
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["head-mode-chip font-alimamashuhei", { active: $setup.headMode === "select" }]),
                    onClick: _cache[0] || (_cache[0] = ($event) => $setup.switchHeadMode("select"))
                  },
                  [
                    vue.createTextVNode(" 分步选择 "),
                    vue.createElementVNode("image", {
                      src: _imports_0$1,
                      style: { "width": "16rpx", "height": "16rpx" }
                    })
                  ],
                  2
                  /* CLASS */
                ),
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["head-mode-chip font-alimamashuhei", { active: $setup.headMode === "cabinet" }]),
                    onClick: _cache[1] || (_cache[1] = ($event) => $setup.switchHeadMode("cabinet"))
                  },
                  [
                    vue.createTextVNode(" 从娃柜中添加 "),
                    vue.createElementVNode("image", {
                      src: _imports_1$1,
                      style: { "width": "22rpx", "height": "22rpx" }
                    })
                  ],
                  2
                  /* CLASS */
                ),
                vue.createElementVNode(
                  "view",
                  {
                    class: vue.normalizeClass(["head-mode-chip font-alimamashuhei", { active: $setup.headMode === "manual" }]),
                    onClick: _cache[2] || (_cache[2] = ($event) => $setup.switchHeadMode("manual"))
                  },
                  [
                    vue.createTextVNode(" 手动输入 "),
                    vue.createElementVNode("image", {
                      src: _imports_2,
                      style: { "width": "22rpx", "height": "22rpx", "position": "relative", "top": "1px" }
                    })
                  ],
                  2
                  /* CLASS */
                )
              ]),
              $setup.headMode === "select" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "field-control relation-trigger",
                onClick: $setup.goPickHead
              }, [
                vue.createElementVNode("view", { class: "rt-left" }, [
                  $setup.selectedGoods.cover || $setup.firstRefImage ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    src: $setup.selectedGoods.cover || $setup.firstRefImage,
                    mode: "aspectFill",
                    class: "thumb"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode("view", { class: "rt-texts" }, [
                    $setup.selectedGoods.name ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "selected-text"
                      },
                      vue.toDisplayString($setup.selectedGoods.name),
                      1
                      /* TEXT */
                    )) : (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "placeholder-text"
                    }, " 可选择娃头（先选品牌再选商品） ")),
                    $setup.selectedBrand.brand_name ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 2,
                        class: "sub-text"
                      },
                      vue.toDisplayString($setup.selectedBrand.brand_name),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ])
                ]),
                vue.createVNode(_component_uni_icons, {
                  type: "arrowright",
                  size: "16",
                  color: "#999"
                })
              ])) : $setup.headMode === "cabinet" ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "field-control relation-trigger",
                onClick: $setup.goPickFromCabinet
              }, [
                vue.createElementVNode("view", { class: "rt-left" }, [
                  $setup.cabinetItem && $setup.cabinetItem.image || $setup.firstRefImage ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    src: $setup.cabinetItem && $setup.cabinetItem.image || $setup.firstRefImage,
                    mode: "aspectFill",
                    class: "thumb"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode("view", { class: "rt-texts" }, [
                    $setup.form.work_subject ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 0,
                        class: "selected-text"
                      },
                      vue.toDisplayString($setup.form.work_subject),
                      1
                      /* TEXT */
                    )) : (vue.openBlock(), vue.createElementBlock("text", {
                      key: 1,
                      class: "placeholder-text"
                    }, " 可从娃柜中选择娃头 ")),
                    $setup.cabinetItem && $setup.cabinetItem.name ? (vue.openBlock(), vue.createElementBlock(
                      "text",
                      {
                        key: 2,
                        class: "sub-text"
                      },
                      vue.toDisplayString($setup.cabinetItem.name),
                      1
                      /* TEXT */
                    )) : vue.createCommentVNode("v-if", true)
                  ])
                ]),
                vue.createVNode(_component_uni_icons, {
                  type: "arrowright",
                  size: "16",
                  color: "#999"
                })
              ])) : (vue.openBlock(), vue.createElementBlock("view", {
                key: 2,
                class: "field-control relation-trigger manual-trigger"
              }, [
                vue.createElementVNode("view", { class: "rt-left" }, [
                  $setup.firstRefImage ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    src: $setup.firstRefImage,
                    mode: "aspectFill",
                    class: "thumb"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode("view", { class: "rt-texts" }, [
                    vue.withDirectives(vue.createElementVNode(
                      "input",
                      {
                        "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.form.work_subject = $event),
                        class: "field-input manual-input",
                        type: "text",
                        placeholder: "请输入娃头名称"
                      },
                      null,
                      512
                      /* NEED_PATCH */
                    ), [
                      [
                        vue.vModelText,
                        $setup.form.work_subject,
                        void 0,
                        { trim: true }
                      ]
                    ])
                  ])
                ])
              ]))
            ]),
            vue.createElementVNode("view", { class: "field-row" }, [
              vue.createElementVNode("view", { class: "field-label" }, [
                vue.createElementVNode("text", { class: "label-text" }, "娃头图片（最多3张）")
              ]),
              vue.createElementVNode("view", { class: "field-control image-field" }, [
                vue.createElementVNode("view", { class: "upload-container" }, [
                  (vue.openBlock(true), vue.createElementBlock(
                    vue.Fragment,
                    null,
                    vue.renderList($setup.form.ref_images, (img, index) => {
                      return vue.openBlock(), vue.createElementBlock("view", {
                        key: index,
                        class: "image-preview"
                      }, [
                        vue.createElementVNode("image", {
                          src: img,
                          mode: "aspectFill",
                          class: "preview-img"
                        }, null, 8, ["src"]),
                        vue.createElementVNode("view", { class: "image-actions" }, [
                          vue.createVNode(_component_uni_icons, {
                            type: "eye",
                            size: "20",
                            color: "#fff",
                            onClick: vue.withModifiers(($event) => $setup.previewRefImage(img), ["stop"])
                          }, null, 8, ["onClick"]),
                          vue.createVNode(_component_uni_icons, {
                            type: "trash",
                            size: "20",
                            color: "#fff",
                            onClick: vue.withModifiers(($event) => $setup.removeRefImage(index), ["stop"])
                          }, null, 8, ["onClick"])
                        ])
                      ]);
                    }),
                    128
                    /* KEYED_FRAGMENT */
                  )),
                  $setup.form.ref_images.length < 3 ? (vue.openBlock(), vue.createElementBlock("view", {
                    key: 0,
                    class: "upload-btn",
                    onClick: $setup.handleUploadRefImages
                  }, [
                    vue.createVNode(_component_uni_icons, {
                      type: "plus",
                      size: "32",
                      color: "#999"
                    }),
                    vue.createElementVNode("text", { class: "upload-text" }, "添加图片")
                  ])) : vue.createCommentVNode("v-if", true)
                ]),
                $setup.uploading ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "upload-progress"
                }, [
                  vue.createElementVNode("progress", {
                    percent: $setup.uploadProgress,
                    "stroke-width": "4",
                    activeColor: "#4a9db5"
                  }, null, 8, ["percent"]),
                  vue.createElementVNode(
                    "text",
                    { class: "upload-status" },
                    vue.toDisplayString($setup.uploadStatusText || "上传中…"),
                    1
                    /* TEXT */
                  )
                ])) : vue.createCommentVNode("v-if", true)
              ])
            ]),
            $setup.sizeOptions.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "plan-row"
            }, [
              vue.createElementVNode("view", { class: "plan-row-label" }, [
                vue.createElementVNode("text", { class: "plan-row-title" }, "可投递尺寸")
              ]),
              vue.createElementVNode("view", { class: "chip-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.sizeOptions, (opt, idx) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: idx,
                      class: vue.normalizeClass(["chip", { active: $setup.selectedSizeIndex === idx }]),
                      onClick: ($event) => $setup.handleSelectSize(idx)
                    }, [
                      vue.createElementVNode(
                        "text",
                        { class: "chip-main font-alimamashuhei" },
                        vue.toDisplayString(opt.size || "未命名尺寸"),
                        1
                        /* TEXT */
                      ),
                      Number(opt.price) > 0 ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 0,
                          class: "chip-tag font-title"
                        },
                        "+" + vue.toDisplayString(Number(opt.price)) + " 元",
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])) : vue.createCommentVNode("v-if", true),
            $setup.tierOptions.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 1,
              class: "plan-row"
            }, [
              vue.createElementVNode("view", { class: "plan-row-label" }, [
                vue.createElementVNode("text", { class: "plan-row-title" }, "可选档位 / 套餐")
              ]),
              vue.createElementVNode("view", { class: "chip-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.tierOptions, (tier, idx) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: idx,
                      class: vue.normalizeClass(["chip chip-column", { active: $setup.selectedTierIndex === idx }]),
                      onClick: ($event) => $setup.handleSelectTier(idx)
                    }, [
                      vue.createElementVNode("view", { class: "chip-line" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "chip-main font-alimamashuhei" },
                          vue.toDisplayString(tier.title || "未命名档位"),
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          { class: "chip-tag font-title" },
                          vue.toDisplayString(Number(tier.price)) + " 元",
                          1
                          /* TEXT */
                        )
                      ]),
                      tier.description ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 0,
                          class: "chip-desc"
                        },
                        vue.toDisplayString(tier.description),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])) : vue.createCommentVNode("v-if", true),
            $setup.addonOptions.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 2,
              class: "plan-row"
            }, [
              vue.createElementVNode("view", { class: "plan-row-label" }, [
                vue.createElementVNode("text", { class: "plan-row-title" }, "可选加购")
              ]),
              vue.createElementVNode("view", { class: "chip-list" }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.addonOptions, (addon, idx) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      key: idx,
                      class: vue.normalizeClass(["chip chip-column", { active: $setup.selectedAddonIndexes.includes(idx) }]),
                      onClick: ($event) => $setup.toggleAddon(idx)
                    }, [
                      vue.createElementVNode("view", { class: "chip-line" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "chip-main font-alimamashuhei" },
                          vue.toDisplayString(addon.title || "未命名加购"),
                          1
                          /* TEXT */
                        ),
                        vue.createElementVNode(
                          "text",
                          { class: "chip-tag font-title" },
                          "+" + vue.toDisplayString(Number(addon.price)) + " 元",
                          1
                          /* TEXT */
                        )
                      ]),
                      addon.description ? (vue.openBlock(), vue.createElementBlock(
                        "text",
                        {
                          key: 0,
                          class: "chip-desc"
                        },
                        vue.toDisplayString(addon.description),
                        1
                        /* TEXT */
                      )) : vue.createCommentVNode("v-if", true)
                    ], 10, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "field-row" }, [
              vue.createElementVNode("view", { class: "field-label" }, [
                vue.createElementVNode("text", { class: "label-text" }, "总价预估（元）")
              ]),
              vue.createElementVNode("view", { class: "field-control price-row" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.form.price_text = $event),
                    class: "field-input font-title",
                    type: "digit",
                    disabled: ""
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.form.price_text]
                ])
              ]),
              $setup.tierOptions.length || $setup.addonOptions.length || $setup.sizeOptions.length ? (vue.openBlock(), vue.createElementBlock("view", {
                key: 0,
                class: "price-hint"
              }, [
                vue.createElementVNode("text", { class: "price-hint-text" }, " 将根据所选尺寸 / 档位 / 加购自动预估，仅供参考（最终价格以创作者确认为准）。 ")
              ])) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          vue.createElementVNode("view", { class: "card" }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              vue.createElementVNode("text", { class: "card-title" }, "补充说明"),
              vue.createElementVNode("text", { class: "card-sub" }, "如果您选择的档位可以指定要求，请在这里填写（选填）")
            ]),
            vue.createElementVNode("view", { class: "field-row field-textarea-row" }, [
              vue.createElementVNode("view", { class: "field-label" }, [
                vue.createElementVNode("text", { class: "label-text" }, "备注")
              ]),
              vue.createElementVNode("view", { class: "field-control" }, [
                vue.withDirectives(vue.createElementVNode(
                  "textarea",
                  {
                    "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.form.remark = $event),
                    class: "field-textarea",
                    maxlength: "500"
                  },
                  null,
                  512
                  /* NEED_PATCH */
                ), [
                  [
                    vue.vModelText,
                    $setup.form.remark,
                    void 0,
                    { trim: true }
                  ]
                ]),
                vue.createElementVNode("view", { class: "textarea-count" }, [
                  vue.createElementVNode(
                    "text",
                    null,
                    vue.toDisplayString($setup.form.remark.length) + "/500",
                    1
                    /* TEXT */
                  )
                ])
              ])
            ])
          ]),
          vue.createElementVNode("view", { class: "hint-box" }, [
            vue.createElementVNode("text", { class: "hint-text" }, " 提示：本页面只编辑本次投递下的单个投递信息。 ")
          ]),
          vue.createElementVNode("view", { style: { "height": "140rpx" } })
        ]))
      ]),
      $setup.isLogin && ($setup.isEdit || $setup.planId) ? (vue.openBlock(), vue.createElementBlock("view", {
        key: 0,
        class: "bottom-bar"
      }, [
        vue.createElementVNode("button", {
          class: "save-btn",
          type: "default",
          disabled: $setup.saving,
          onClick: $setup.handleSave
        }, [
          vue.createElementVNode(
            "text",
            { class: "save-text" },
            vue.toDisplayString($setup.saving ? "保存中…" : $setup.isEdit ? "保存修改" : "保存投递"),
            1
            /* TEXT */
          )
        ], 8, ["disabled"])
      ])) : vue.createCommentVNode("v-if", true)
    ]);
  }
  const PkgCreatorCreatorOrderSubmissionItemFormSubmissionItemForm = /* @__PURE__ */ _export_sfc(_sfc_main$5, [["render", _sfc_render$4], ["__scopeId", "data-v-5e9bdd7f"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/submission_item_form/submission_item_form.vue"]]);
  const _imports_0 = "/static/new-icon/loading.gif";
  const _imports_1 = "/static/new-icon/loading.png";
  const pageSize = 20;
  const indicatorStyle = "height:64px;";
  const _sfc_main$4 = {
    __name: "received-list",
    setup(__props, { expose: __expose }) {
      __expose();
      const planId = vue.ref(0);
      const paging = vue.ref(null);
      const innerList = vue.ref([]);
      const rawList = vue.ref([]);
      const totalCount = vue.ref(0);
      const statusFilterOptions = vue.ref([
        { key: "all", label: "全部", value: null },
        { key: "selected_confirm", label: "等待买家确认订单内容", value: 1 },
        { key: "buyer_confirmed", label: "买家已确认请您确认", value: 2 },
        { key: "selected_pay", label: "等待买家付款", value: 3 },
        { key: "paid", label: "已付款进行中", value: 4 },
        { key: "failed", label: "排队失败", value: 5 },
        { key: "cancelled", label: "买家取消", value: 6 },
        { key: "timeout", label: "付款超时", value: 7 }
      ]);
      const currentFilterKey = vue.ref("all");
      const currentFilterLabel = vue.computed(() => {
        const opt = statusFilterOptions.value.find((o2) => o2.key === currentFilterKey.value);
        return opt ? opt.label : "全部";
      });
      const allSubmissions = vue.computed(() => rawList.value || []);
      const filteredSubmissions = vue.computed(() => {
        const list = allSubmissions.value;
        if (currentFilterKey.value === "all")
          return list;
        const opt = statusFilterOptions.value.find((o2) => o2.key === currentFilterKey.value);
        if (!opt || opt.value === null || opt.value === void 0)
          return list;
        const targetStatus = Number(opt.value);
        return list.filter((row) => Number(getSubmissionStatus(row)) === targetStatus);
      });
      const currentSubmissionCount = vue.computed(() => filteredSubmissions.value.length);
      const currentTotalAmount = vue.computed(() => {
        return filteredSubmissions.value.reduce((sum, row) => {
          const n2 = Number(row.total_amount || 0);
          if (Number.isNaN(n2))
            return sum;
          return sum + n2;
        }, 0);
      });
      function isRefresherGif(status) {
        formatAppLog("log", "at pkg-creator/creator_order/received-list/received-list.vue:345", "[received-list] z-paging refresherStatus =", status, typeof status);
        if (status === "loading")
          return true;
        if (status === 2)
          return true;
        return false;
      }
      function getSubmission(row) {
        if (!row)
          return {};
        return row.submission || row.Submission || {};
      }
      function getSubmissionId(row) {
        const sub = getSubmission(row);
        return sub.id || sub.ID || 0;
      }
      function getSubmissionStatus(row) {
        const sub = getSubmission(row);
        const s2 = sub.status ?? sub.Status;
        return s2 === void 0 || s2 === null ? -1 : Number(s2);
      }
      function getSubmissionUserId(row) {
        const sub = getSubmission(row);
        return sub.user_id || sub.userId || 0;
      }
      function getSubmissionCreatedAt(row) {
        const sub = getSubmission(row);
        return sub.created_at || sub.createdAt || 0;
      }
      function getItems(row) {
        if (!row)
          return [];
        return row.items || row.Items || [];
      }
      function itemStatusText(status) {
        const s2 = Number(status);
        switch (s2) {
          case 0:
            return "排队中";
          case 1:
            return "等待买家确认订单内容";
          case 2:
            return "买家已确认请您确认";
          case 3:
            return "等待买家付款";
          case 4:
            return "已付款进行中";
          case 5:
            return "排队失败";
          case 6:
            return "买家取消";
          case 7:
            return "付款超时";
          default:
            return "未知状态";
        }
      }
      function buildSubmissionSubject(row) {
        const items = getItems(row);
        if (!items.length)
          return "";
        const first = items[0] || {};
        const parts = [];
        const subject = (first.work_subject || "").trim();
        const size = (first.size || "").trim();
        const tier = (first.tier_title || "").trim();
        if (subject)
          parts.push(subject);
        if (size)
          parts.push(size);
        if (tier)
          parts.push(tier);
        return parts.join(" / ");
      }
      function getFirstImage(item) {
        const src = item.ref_images || item.refImages || "";
        if (!src)
          return "";
        const arr = String(src).split(",").map((s2) => s2.trim()).filter(Boolean);
        return arr[0] || "";
      }
      function parseAddons(addonsJson) {
        if (!addonsJson)
          return [];
        try {
          const v2 = JSON.parse(addonsJson);
          if (Array.isArray(v2))
            return v2;
          return [];
        } catch (e2) {
          formatAppLog("warn", "at pkg-creator/creator_order/received-list/received-list.vue:454", "parseAddons failed", e2);
          return [];
        }
      }
      function getItemAddonsTitles(item) {
        const addonsJson = item.addons_json || item.addonsJson || "";
        const addons = parseAddons(addonsJson);
        if (!addons.length)
          return "";
        const titles = addons.map((a2) => (a2.title || a2.Title || "").trim()).filter(Boolean);
        return titles.join(" / ");
      }
      function calcItemTotal(item) {
        const base = Number(item.price_total || 0);
        const adjust = Number(item.adjust_price || 0);
        const b2 = Number.isFinite(base) ? base : 0;
        const a2 = Number.isFinite(adjust) ? adjust : 0;
        return b2 + a2;
      }
      function formatPrice(price) {
        const n2 = Number(price || 0);
        if (!Number.isFinite(n2))
          return "0.00";
        return n2.toFixed(2);
      }
      function pad2(n2) {
        return n2 < 10 ? "0" + n2 : "" + n2;
      }
      function formatTime(ts2) {
        const n2 = Number(ts2 || 0);
        if (!n2)
          return "-";
        const d2 = new Date(n2 * 1e3);
        const y2 = d2.getFullYear();
        const m2 = pad2(d2.getMonth() + 1);
        const day = pad2(d2.getDate());
        const h2 = pad2(d2.getHours());
        const mi = pad2(d2.getMinutes());
        return `${y2}-${m2}-${day} ${h2}:${mi}`;
      }
      function goSubmissionDetail(row) {
        const sub = getSubmission(row);
        const submissionId = sub.id || sub.ID;
        if (!submissionId) {
          uni.showToast({
            title: "缺少 submission_id",
            icon: "none"
          });
          return;
        }
        const items = getItems(row);
        const first = items[0] || {};
        first.id || first.ID || "";
        const query = [`submission_id=${submissionId}`];
        uni.navigateTo({
          url: `/pkg-creator/creator_order/received-detail/received-detail?${query.join("&")}`
        });
      }
      function startChat(row) {
        const uid = getSubmissionUserId(row);
        if (!uid) {
          uni.showToast({
            title: "缺少对方 UID",
            icon: "none"
          });
          return;
        }
        uni.navigateTo({
          url: `/pkg-im/chat/chat?peer_id=${uid}`
        });
      }
      function computeSubmissionTotal(items) {
        if (!items || !items.length)
          return 0;
        return items.reduce((sum, it2) => {
          const n2 = calcItemTotal(it2);
          return sum + n2;
        }, 0);
      }
      async function queryList(pageNo, pageSizeArg) {
        const size = pageSizeArg || pageSize;
        const token = uni.getStorageSync("token") || "";
        try {
          const res = await uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/received-submissions`,
            method: "GET",
            data: {
              page: pageNo,
              size,
              plan_id: planId.value || void 0
            },
            header: token ? {
              Authorization: token
            } : {}
          });
          const body = res.data || {};
          const payload = body.data || body;
          const list = payload.list || [];
          const pageInfo = payload.page || {};
          const totalSubmissions = Number(pageInfo.total || 0);
          totalCount.value = totalSubmissions;
          const normalized = list.map((row) => {
            const submission = row.submission || row.Submission || {};
            const items = row.items || row.Items || [];
            const queueStat = row.queue_stat || row.queueStat || null;
            return {
              submission,
              items,
              queue_stat: queueStat,
              total_amount: computeSubmissionTotal(items)
            };
          });
          if (pageNo === 1) {
            rawList.value = normalized;
          } else {
            rawList.value = rawList.value.concat(normalized);
          }
          if (paging.value) {
            paging.value.complete(normalized, totalSubmissions);
          } else {
            innerList.value = normalized;
          }
        } catch (err) {
          formatAppLog("error", "at pkg-creator/creator_order/received-list/received-list.vue:614", "fetch received submissions failed", err);
          uni.showToast({
            title: "加载失败，请稍后重试",
            icon: "none"
          });
          if (paging.value) {
            paging.value.complete(false);
          }
        }
      }
      const filterPopup = vue.ref(null);
      const filterMounted = vue.ref(false);
      const safeBottom = vue.ref(0);
      const currentFilterIndex = vue.computed(() => {
        const idx = statusFilterOptions.value.find((opt) => opt.key === currentFilterKey.value);
        return idx >= 0 ? idx : 0;
      });
      const tempFilterIndex = vue.ref(0);
      function openFilterSheet() {
        var _a;
        tempFilterIndex.value = currentFilterIndex.value;
        try {
          const wi = uni.getWindowInfo && uni.getWindowInfo();
          safeBottom.value = ((_a = wi == null ? void 0 : wi.safeAreaInsets) == null ? void 0 : _a.bottom) || 0;
        } catch {
          safeBottom.value = 0;
        }
        vue.nextTick(() => {
          var _a2, _b;
          filterMounted.value = true;
          (_b = (_a2 = filterPopup.value) == null ? void 0 : _a2.open) == null ? void 0 : _b.call(_a2);
        });
      }
      function closeFilterSheet() {
        var _a, _b;
        (_b = (_a = filterPopup.value) == null ? void 0 : _a.close) == null ? void 0 : _b.call(_a);
      }
      function onFilterPopupChange(e2) {
        if (!e2.show) {
          filterMounted.value = false;
        }
      }
      function onFilterPickerChange(e2) {
        const arr = e2.detail && e2.detail.value;
        if (!arr || !arr.length)
          return;
        const idx = Number(arr[0]);
        if (Number.isNaN(idx))
          return;
        tempFilterIndex.value = idx;
      }
      function confirmFilter() {
        const opt = statusFilterOptions.value[tempFilterIndex.value] || statusFilterOptions.value[0];
        currentFilterKey.value = opt.key;
        closeFilterSheet();
        if (paging.value) {
          paging.value.reload();
        }
      }
      onLoad((options) => {
        uni.setNavigationBarTitle({
          title: "投递我的"
        });
        if (options && options.plan_id) {
          const pid = Number(options.plan_id);
          if (!Number.isNaN(pid)) {
            planId.value = pid;
          }
        }
      });
      const __returned__ = { planId, paging, innerList, pageSize, rawList, totalCount, statusFilterOptions, currentFilterKey, currentFilterLabel, allSubmissions, filteredSubmissions, currentSubmissionCount, currentTotalAmount, isRefresherGif, getSubmission, getSubmissionId, getSubmissionStatus, getSubmissionUserId, getSubmissionCreatedAt, getItems, itemStatusText, buildSubmissionSubject, getFirstImage, parseAddons, getItemAddonsTitles, calcItemTotal, formatPrice, pad2, formatTime, goSubmissionDetail, startChat, computeSubmissionTotal, queryList, filterPopup, filterMounted, safeBottom, indicatorStyle, currentFilterIndex, tempFilterIndex, openFilterSheet, closeFilterSheet, onFilterPopupChange, onFilterPickerChange, confirmFilter, ref: vue.ref, computed: vue.computed, nextTick: vue.nextTick, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, LoadingJumpText };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$3(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_z_paging = resolveEasycom(vue.resolveDynamicComponent("z-paging"), __easycom_0$7);
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "booking-page received-page" }, [
      vue.createVNode(_component_z_paging, {
        ref: "paging",
        modelValue: $setup.innerList,
        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.innerList = $event),
        onQuery: $setup.queryList,
        "default-page-size": $setup.pageSize,
        "refresher-enabled": true,
        "use-custom-refresher": true,
        "show-loading-more-when-reload": "",
        auto: true,
        "loading-more-enabled": true,
        "to-bottom-loading-more-enabled": true,
        "inside-more": true,
        class: "paging-root"
      }, {
        refresher: vue.withCtx(({ refresherStatus }) => [
          vue.createElementVNode("view", { class: "custom-refresher" }, [
            $setup.isRefresherGif(refresherStatus) ? (vue.openBlock(), vue.createElementBlock("image", {
              key: 0,
              class: "custom-refresher-icon",
              src: _imports_0,
              mode: "widthFix"
            })) : (vue.openBlock(), vue.createElementBlock("image", {
              key: 1,
              class: "custom-refresher-icon",
              src: _imports_1,
              mode: "widthFix"
            }))
          ])
        ]),
        empty: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "empty-box" }, [
            vue.createElementVNode("image", {
              class: "empty-icon",
              src: _imports_2$1,
              mode: "aspectFit"
            }),
            vue.createElementVNode("text", { class: "empty-text" }, "还没有收到相关投递")
          ])
        ]),
        default: vue.withCtx(() => [
          vue.createElementVNode("view", { class: "page-header" }, [
            vue.createElementVNode("view", { class: "hero hero-small" }, [
              vue.createElementVNode("view", { class: "hero-bg" }),
              vue.createElementVNode("view", { class: "hero-footer hero-small-footer" }, [
                vue.createElementVNode("view", { class: "hero-title" }, [
                  vue.createElementVNode("text", { class: "brand-name font-title" }, "投递我的")
                ]),
                vue.createElementVNode("text", { class: "hero-desc" }, "按订单状态查看谁投递了你")
              ])
            ]),
            vue.createElementVNode("view", {
              class: "card filter-bar",
              onClick: $setup.openFilterSheet
            }, [
              vue.createElementVNode("view", { class: "filter-left" }, [
                vue.createElementVNode("text", { class: "filter-label" }, "当前筛选"),
                vue.createElementVNode(
                  "text",
                  { class: "filter-value" },
                  vue.toDisplayString($setup.currentFilterLabel),
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "filter-right" }, [
                vue.createElementVNode("text", { class: "filter-hint" }, "点击切换"),
                vue.createElementVNode("text", { class: "filter-arrow" }, "▼")
              ])
            ]),
            $setup.filteredSubmissions.length > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "card summary-card"
            }, [
              vue.createElementVNode("view", { class: "summary-row" }, [
                vue.createElementVNode("text", { class: "summary-label" }, "当前筛选"),
                vue.createElementVNode(
                  "text",
                  { class: "summary-value" },
                  vue.toDisplayString($setup.currentFilterLabel) + " · 共 " + vue.toDisplayString($setup.currentSubmissionCount) + " 笔投递 ",
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "summary-row" }, [
                vue.createElementVNode("text", { class: "summary-label" }, "总金额"),
                vue.createElementVNode(
                  "text",
                  { class: "summary-value price" },
                  " ¥ " + vue.toDisplayString($setup.formatPrice($setup.currentTotalAmount)),
                  1
                  /* TEXT */
                )
              ])
            ])) : vue.createCommentVNode("v-if", true)
          ]),
          vue.createElementVNode("view", { class: "body-container" }, [
            vue.createElementVNode("view", { class: "list-wrapper" }, [
              (vue.openBlock(true), vue.createElementBlock(
                vue.Fragment,
                null,
                vue.renderList($setup.filteredSubmissions, (row) => {
                  return vue.openBlock(), vue.createElementBlock("view", {
                    key: $setup.getSubmissionId(row),
                    class: "card item-card",
                    onClick: ($event) => $setup.goSubmissionDetail(row)
                  }, [
                    vue.createElementVNode("view", { class: "item-header" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "item-title" },
                        vue.toDisplayString($setup.buildSubmissionSubject(row) || "未填写作品标题"),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode(
                        "view",
                        {
                          class: vue.normalizeClass(["status-chip", "s-" + $setup.getSubmissionStatus(row)])
                        },
                        [
                          vue.createElementVNode(
                            "text",
                            { class: "status-text" },
                            vue.toDisplayString($setup.itemStatusText($setup.getSubmissionStatus(row))),
                            1
                            /* TEXT */
                          )
                        ],
                        2
                        /* CLASS */
                      )
                    ]),
                    vue.createElementVNode("view", { class: "item-row" }, [
                      vue.createElementVNode("text", { class: "item-label" }, "投递用户"),
                      vue.createElementVNode(
                        "text",
                        { class: "item-value" },
                        " UID " + vue.toDisplayString($setup.getSubmissionUserId(row) || "-"),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode("view", { class: "item-row" }, [
                      vue.createElementVNode("text", { class: "item-label" }, "投递时间"),
                      vue.createElementVNode(
                        "text",
                        { class: "item-value" },
                        vue.toDisplayString($setup.formatTime($setup.getSubmissionCreatedAt(row))),
                        1
                        /* TEXT */
                      )
                    ]),
                    vue.createElementVNode("view", { class: "item-row" }, [
                      vue.createElementVNode("text", { class: "item-label" }, "作品数量"),
                      vue.createElementVNode(
                        "text",
                        { class: "item-value" },
                        vue.toDisplayString($setup.getItems(row).length) + " 个作品 ",
                        1
                        /* TEXT */
                      )
                    ]),
                    Number(row.total_amount) > 0 ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "item-row"
                    }, [
                      vue.createElementVNode("text", { class: "item-label" }, "订单总金额"),
                      vue.createElementVNode(
                        "text",
                        { class: "item-value price" },
                        " ¥ " + vue.toDisplayString($setup.formatPrice(row.total_amount)),
                        1
                        /* TEXT */
                      )
                    ])) : vue.createCommentVNode("v-if", true),
                    $setup.getItems(row).length ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 1,
                      class: "items-block"
                    }, [
                      (vue.openBlock(true), vue.createElementBlock(
                        vue.Fragment,
                        null,
                        vue.renderList($setup.getItems(row), (it2) => {
                          return vue.openBlock(), vue.createElementBlock("view", {
                            class: "sub-item",
                            key: it2.id || it2.ID
                          }, [
                            vue.createElementVNode("view", { class: "sub-item-main-row" }, [
                              $setup.getFirstImage(it2) ? (vue.openBlock(), vue.createElementBlock("view", {
                                key: 0,
                                class: "sub-item-cover-wrap"
                              }, [
                                vue.createElementVNode("image", {
                                  class: "sub-item-cover",
                                  src: $setup.getFirstImage(it2),
                                  mode: "aspectFill"
                                }, null, 8, ["src"])
                              ])) : vue.createCommentVNode("v-if", true),
                              vue.createElementVNode("view", { class: "sub-item-main" }, [
                                vue.createElementVNode("view", { class: "sub-item-title-row" }, [
                                  vue.createElementVNode(
                                    "text",
                                    { class: "sub-item-title" },
                                    vue.toDisplayString((it2.work_subject || "").trim() || "未命名作品"),
                                    1
                                    /* TEXT */
                                  ),
                                  $setup.calcItemTotal(it2) > 0 ? (vue.openBlock(), vue.createElementBlock(
                                    "text",
                                    {
                                      key: 0,
                                      class: "sub-item-price"
                                    },
                                    " ¥ " + vue.toDisplayString($setup.formatPrice($setup.calcItemTotal(it2))),
                                    1
                                    /* TEXT */
                                  )) : vue.createCommentVNode("v-if", true)
                                ]),
                                vue.createElementVNode("view", { class: "sub-item-meta-row" }, [
                                  (it2.size || "").trim() ? (vue.openBlock(), vue.createElementBlock(
                                    "text",
                                    {
                                      key: 0,
                                      class: "sub-item-meta"
                                    },
                                    vue.toDisplayString((it2.size || "").trim()),
                                    1
                                    /* TEXT */
                                  )) : vue.createCommentVNode("v-if", true),
                                  (it2.tier_title || "").trim() ? (vue.openBlock(), vue.createElementBlock(
                                    "text",
                                    {
                                      key: 1,
                                      class: "sub-item-meta"
                                    },
                                    " 档位：" + vue.toDisplayString((it2.tier_title || "").trim()),
                                    1
                                    /* TEXT */
                                  )) : vue.createCommentVNode("v-if", true),
                                  $setup.getItemAddonsTitles(it2) ? (vue.openBlock(), vue.createElementBlock(
                                    "text",
                                    {
                                      key: 2,
                                      class: "sub-item-meta"
                                    },
                                    " 加购：" + vue.toDisplayString($setup.getItemAddonsTitles(it2)),
                                    1
                                    /* TEXT */
                                  )) : vue.createCommentVNode("v-if", true),
                                  Number(it2.adjust_price) !== 0 ? (vue.openBlock(), vue.createElementBlock(
                                    "text",
                                    {
                                      key: 3,
                                      class: "sub-item-meta adjust"
                                    },
                                    " 调价：" + vue.toDisplayString(it2.adjust_price > 0 ? "+" : "") + vue.toDisplayString($setup.formatPrice(it2.adjust_price)),
                                    1
                                    /* TEXT */
                                  )) : vue.createCommentVNode("v-if", true)
                                ])
                              ])
                            ])
                          ]);
                        }),
                        128
                        /* KEYED_FRAGMENT */
                      ))
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("view", { class: "item-footer" }, [
                      vue.createElementVNode("view", { class: "item-footer-left" }),
                      vue.createElementVNode("view", { class: "item-footer-right" }, [
                        vue.createElementVNode("button", {
                          class: "btn-chat",
                          onClick: vue.withModifiers(($event) => $setup.startChat(row), ["stop"])
                        }, " 发起会话 ", 8, ["onClick"])
                      ])
                    ])
                  ], 8, ["onClick"]);
                }),
                128
                /* KEYED_FRAGMENT */
              ))
            ])
          ])
        ]),
        _: 1
        /* STABLE */
      }, 8, ["modelValue"]),
      vue.createVNode(
        _component_uni_popup,
        {
          ref: "filterPopup",
          type: "bottom",
          "mask-click": true,
          onChange: $setup.onFilterPopupChange
        },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode(
              "view",
              {
                class: "sheet",
                style: vue.normalizeStyle({ paddingBottom: $setup.safeBottom + "px" })
              },
              [
                vue.createElementVNode("view", { class: "sheet-header" }, [
                  vue.createElementVNode("text", {
                    class: "btn-cancel",
                    onClick: $setup.closeFilterSheet
                  }, "取消"),
                  vue.createElementVNode("text", { class: "sheet-title" }, "筛选订单状态")
                ]),
                $setup.filterMounted ? (vue.openBlock(), vue.createElementBlock("picker-view", {
                  key: 0,
                  class: "picker",
                  "indicator-style": $setup.indicatorStyle,
                  value: [$setup.tempFilterIndex],
                  onChange: $setup.onFilterPickerChange
                }, [
                  vue.createElementVNode("picker-view-column", null, [
                    (vue.openBlock(true), vue.createElementBlock(
                      vue.Fragment,
                      null,
                      vue.renderList($setup.statusFilterOptions, (opt, idx) => {
                        return vue.openBlock(), vue.createElementBlock(
                          "view",
                          {
                            key: opt.key,
                            class: "picker-item"
                          },
                          vue.toDisplayString(opt.label),
                          1
                          /* TEXT */
                        );
                      }),
                      128
                      /* KEYED_FRAGMENT */
                    ))
                  ])
                ], 40, ["value"])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "sheet-footer" }, [
                  vue.createElementVNode("button", {
                    class: "btn-confirm",
                    onClick: $setup.confirmFilter
                  }, "确定")
                ])
              ],
              4
              /* STYLE */
            )
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const PkgCreatorCreatorOrderReceivedListReceivedList = /* @__PURE__ */ _export_sfc(_sfc_main$4, [["render", _sfc_render$3], ["__scopeId", "data-v-d6cf37ca"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/received-list/received-list.vue"]]);
  const _sfc_main$3 = {
    __name: "received-detail",
    setup(__props, { expose: __expose }) {
      __expose();
      const loading2 = vue.ref(true);
      const errorMsg = vue.ref("");
      const submissionId = vue.ref(0);
      const queueInfo = vue.ref(null);
      const items = vue.computed(() => {
        var _a;
        return ((_a = queueInfo.value) == null ? void 0 : _a.items) || [];
      });
      const draftItems = vue.computed(() => {
        var _a;
        return ((_a = queueInfo.value) == null ? void 0 : _a.draft_items) || [];
      });
      const currentUid = vue.computed(() => {
        const u2 = global$1.userInfo || {};
        return u2.id || u2.Id || 0;
      });
      const isBuyer = vue.computed(() => {
        const list = items.value;
        if (!list.length || !currentUid.value)
          return true;
        const first = list[0];
        const uid = first.user_id || first.userId;
        return Number(uid) === Number(currentUid.value);
      });
      const footerPlaceholderHeight = vue.computed(() => {
        return toPx(getFooterPlaceholderHeight());
      });
      const footerPadding = vue.computed(() => {
        return toPx(getSafeBottom());
      });
      const pricePopupRef = vue.ref(null);
      const editingItem = vue.ref(null);
      const priceInput = vue.ref("");
      const reasonInput = vue.ref("");
      function handleBack() {
        uni.navigateBack({
          delta: 1
        });
      }
      function getFirstImage(item) {
        const src = item.ref_images || item.refImages;
        if (!src)
          return "";
        const arr = String(src).split(",").map((s2) => s2.trim()).filter(Boolean);
        return arr[0] || "";
      }
      function parseAddons(addonsJson) {
        if (!addonsJson)
          return [];
        try {
          const v2 = JSON.parse(addonsJson);
          if (Array.isArray(v2))
            return v2;
          return [];
        } catch (e2) {
          formatAppLog("warn", "at pkg-creator/creator_order/received-detail/received-detail.vue:345", "[submission-queue] parseAddons failed", e2);
          return [];
        }
      }
      function getAddonsTitles(item) {
        const addonsJson = item.addons_json || item.addonsJson || "";
        const addons = parseAddons(addonsJson);
        if (!addons.length)
          return "";
        return addons.map((a2) => (a2.title || a2.Title || "").trim()).filter(Boolean).join(" / ");
      }
      function calcItemTotal(item) {
        const base = Number(item.price_total || 0);
        const adjust = Number(item.adjust_price || 0);
        Number(item.addons_price || 0);
        Number(item.surcharge_price || 0);
        const b2 = Number.isFinite(base) ? base : 0;
        const a2 = Number.isFinite(adjust) ? adjust : 0;
        return b2 + a2;
      }
      function formatPrice(v2) {
        const n2 = Number(v2 || 0);
        if (!Number.isFinite(n2))
          return "0.00";
        return n2.toFixed(2);
      }
      function formatItemStatus(st2) {
        const s2 = Number(st2);
        switch (s2) {
          case 0:
            return "待处理/排队中";
          case 1:
            return "已抢到，待确认";
          case 2:
            return "等待妆师/毛娘确认";
          case 3:
            return "待付款";
          case 4:
            return "已下单/进行中";
          case 5:
            return "未中选";
          case 6:
            return "已放弃/取消";
          case 7:
            return "保留过期";
          default:
            return "未知";
        }
      }
      async function ensureLogin() {
        await initLoginState();
        const token = uni.getStorageSync("token");
        if (!token) {
          uni.showToast({
            title: "请先登录",
            icon: "none"
          });
          return false;
        }
        return true;
      }
      async function fetchQueueInfo() {
        const ok = await ensureLogin();
        if (!ok) {
          loading2.value = false;
          errorMsg.value = "未登录";
          return;
        }
        const token = uni.getStorageSync("token") || "";
        if (!submissionId.value) {
          loading2.value = false;
          errorMsg.value = "缺少 submission_id";
          return;
        }
        loading2.value = true;
        errorMsg.value = "";
        formatAppLog(
          "log",
          "at pkg-creator/creator_order/received-detail/received-detail.vue:436",
          "[submission-queue] 请求排队信息",
          `${websiteUrl.value}/with-state/artist-order/submission/queue-info`,
          "submission_id=",
          submissionId.value
        );
        uni.request({
          url: `${websiteUrl.value}/with-state/artist-order/submission/queue-info`,
          method: "GET",
          header: {
            Authorization: token
          },
          data: {
            submission_id: submissionId.value
          },
          success: (res) => {
            formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:453", "[submission-queue] 接口返回", res.data);
            const body = res.data || {};
            if (body.status !== "success") {
              errorMsg.value = body.msg || "获取排队信息失败";
              queueInfo.value = null;
              return;
            }
            queueInfo.value = body.data || null;
          },
          fail: (err) => {
            formatAppLog("error", "at pkg-creator/creator_order/received-detail/received-detail.vue:463", "[submission-queue] 请求失败", err);
            errorMsg.value = "网络错误，请稍后重试";
            queueInfo.value = null;
          },
          complete: () => {
            loading2.value = false;
          }
        });
      }
      function reload() {
        fetchQueueInfo();
      }
      function resolvePeerId() {
        const list = items.value;
        if (!list.length) {
          uni.showToast({
            title: "暂无作品，无法发起会话",
            icon: "none"
          });
          return null;
        }
        const first = list[0];
        const buyerUid = first.user_id || first.userId || 0;
        const brandId = first.brand_id || first.brandId || 0;
        if (!isBuyer.value) {
          if (!buyerUid) {
            uni.showToast({
              title: "找不到买家ID",
              icon: "none"
            });
            return null;
          }
          return buyerUid;
        }
        if (!brandId) {
          uni.showToast({
            title: "缺少品牌信息，暂无法发起对话",
            icon: "none"
          });
          return null;
        }
        return new Promise((resolve) => {
          uni.request({
            url: `${websiteUrl.value}/brand-info`,
            method: "GET",
            data: {
              id: brandId
            },
            success: (res) => {
              formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:527", "[submission-queue] /brand-info 返回", res.data);
              const body = res.data || {};
              const data = body.data || {};
              const ownerUid = data.user_id || data.userId || 0;
              if (!ownerUid) {
                uni.showToast({
                  title: "未配置品牌账号，暂无法发起对话",
                  icon: "none"
                });
                resolve(null);
              } else {
                resolve(ownerUid);
              }
            },
            fail: (err) => {
              formatAppLog("error", "at pkg-creator/creator_order/received-detail/received-detail.vue:542", "[submission-queue] /brand-info 请求失败", err);
              uni.showToast({
                title: "获取品牌信息失败",
                icon: "none"
              });
              resolve(null);
            }
          });
        });
      }
      async function handleChatWithSeller() {
        if (!queueInfo.value) {
          uni.showToast({
            title: "还未加载完成",
            icon: "none"
          });
          return;
        }
        const ok = await ensureLogin();
        if (!ok)
          return;
        const peer = await resolvePeerId();
        if (!peer)
          return;
        formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:568", "[submission-queue] 发起会话 peer_id =", peer);
        uni.navigateTo({
          url: `/pkg-im/chat/chat?peer_id=${peer}`
        });
      }
      function openChangePricePanel(item) {
        editingItem.value = item;
        priceInput.value = String(calcItemTotal(item));
        reasonInput.value = "";
        nextTickOpenPricePopup();
      }
      function nextTickOpenPricePopup() {
        setTimeout(() => {
          var _a, _b;
          try {
            (_b = (_a = pricePopupRef.value) == null ? void 0 : _a.open) == null ? void 0 : _b.call(_a);
          } catch (e2) {
            formatAppLog("warn", "at pkg-creator/creator_order/received-detail/received-detail.vue:592", "[submission-queue] 打开 price popup 异常", e2);
          }
        }, 0);
      }
      function closePricePanel() {
        var _a, _b;
        try {
          (_b = (_a = pricePopupRef.value) == null ? void 0 : _a.close) == null ? void 0 : _b.call(_a);
        } catch (e2) {
          formatAppLog("warn", "at pkg-creator/creator_order/received-detail/received-detail.vue:601", "[submission-queue] 关闭 price popup 异常", e2);
        }
      }
      function onPricePopupChange(e2) {
        formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:606", "[submission-queue] price popup change", e2);
        if (!e2.show) {
          editingItem.value = null;
          priceInput.value = "";
          reasonInput.value = "";
        }
      }
      function submitChangePrice() {
        if (!editingItem.value) {
          uni.showToast({
            title: "未选择作品",
            icon: "none"
          });
          return;
        }
        const raw = priceInput.value.trim();
        const n2 = Number(raw);
        if (!raw || Number.isNaN(n2) || n2 <= 0) {
          uni.showToast({
            title: "请填写正确的金额",
            icon: "none"
          });
          return;
        }
        ensureLogin().then((ok) => {
          if (!ok)
            return;
          const token = uni.getStorageSync("token") || "";
          const payload = {
            item_id: editingItem.value.id || editingItem.value.ID,
            new_price: n2,
            reason: reasonInput.value.trim()
          };
          formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:643", "[submission-queue] 修改金额请求体", payload);
          uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/item/change-price`,
            method: "POST",
            header: {
              Authorization: token,
              "Content-Type": "application/json"
            },
            data: payload,
            success: (res) => {
              formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:654", "[submission-queue] 修改金额返回", res.data);
              const body = res.data || {};
              if (body.status !== "success") {
                uni.showToast({
                  title: body.msg || "修改金额失败",
                  icon: "none"
                });
                return;
              }
              uni.showToast({
                title: "修改金额成功",
                icon: "success"
              });
              closePricePanel();
              fetchQueueInfo();
            },
            fail: (err) => {
              formatAppLog("error", "at pkg-creator/creator_order/received-detail/received-detail.vue:672", "[submission-queue] 修改金额请求失败", err);
              uni.showToast({
                title: "网络错误，请稍后重试",
                icon: "none"
              });
            }
          });
        });
      }
      function handleConfirmSubmission() {
        if (!queueInfo.value)
          return;
        if (queueInfo.value.status !== 2) {
          uni.showToast({
            title: "当前状态不需要确认",
            icon: "none"
          });
          return;
        }
        uni.showModal({
          title: "确认订单",
          content: "确认后将进入待付款状态，是否继续？",
          confirmText: "确认订单",
          cancelText: "再想想",
          success: (res) => {
            if (!res.confirm)
              return;
            doConfirmSubmission();
          }
        });
      }
      function doConfirmSubmission() {
        ensureLogin().then((ok) => {
          if (!ok)
            return;
          const token = uni.getStorageSync("token") || "";
          const payload = {
            submission_id: submissionId.value
          };
          formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:716", "[submission-queue] 确认订单请求", payload);
          uni.request({
            url: `${websiteUrl.value}/with-state/artist-order/submission/confirm`,
            method: "POST",
            header: {
              Authorization: token,
              "Content-Type": "application/json"
            },
            data: payload,
            success: (res) => {
              formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:727", "[submission-queue] 确认订单返回", res.data);
              const body = res.data || {};
              if (body.status !== "success") {
                uni.showToast({
                  title: body.msg || "确认订单失败",
                  icon: "none"
                });
                return;
              }
              uni.showToast({
                title: "订单已确认",
                icon: "success"
              });
              fetchQueueInfo();
            },
            fail: (err) => {
              formatAppLog("error", "at pkg-creator/creator_order/received-detail/received-detail.vue:743", "[submission-queue] 确认订单请求失败", err);
              uni.showToast({
                title: "网络错误，请稍后重试",
                icon: "none"
              });
            }
          });
        });
      }
      function goStepEditor(item) {
        const itemId = item.id || item.ID;
        if (!itemId) {
          uni.showToast({
            title: "缺少子项ID",
            icon: "none"
          });
          return;
        }
        uni.navigateTo({
          url: `/pages/artist-order/step-editor?item_id=${itemId}`
        });
      }
      onLoad((options) => {
        formatAppLog("log", "at pkg-creator/creator_order/received-detail/received-detail.vue:774", "[submission-queue] onLoad options=", options);
        const sid = Number((options == null ? void 0 : options.submission_id) || (options == null ? void 0 : options.submissionId) || 0);
        if (!Number.isNaN(sid) && sid > 0) {
          submissionId.value = sid;
        }
        if (!submissionId.value) {
          loading2.value = false;
          errorMsg.value = "缺少 submission_id 参数";
          return;
        }
        fetchQueueInfo();
      });
      const __returned__ = { loading: loading2, errorMsg, submissionId, queueInfo, items, draftItems, currentUid, isBuyer, footerPlaceholderHeight, footerPadding, pricePopupRef, editingItem, priceInput, reasonInput, handleBack, getFirstImage, parseAddons, getAddonsTitles, calcItemTotal, formatPrice, formatItemStatus, ensureLogin, fetchQueueInfo, reload, resolvePeerId, handleChatWithSeller, openChangePricePanel, nextTickOpenPricePopup, closePricePanel, onPricePopupChange, submitChangePrice, handleConfirmSubmission, doConfirmSubmission, goStepEditor, ref: vue.ref, computed: vue.computed, get onLoad() {
        return onLoad;
      }, get websiteUrl() {
        return websiteUrl;
      }, get global() {
        return global$1;
      }, get initLoginState() {
        return initLoginState;
      }, get getFooterPlaceholderHeight() {
        return getFooterPlaceholderHeight;
      }, get getSafeBottom() {
        return getSafeBottom;
      }, get toPx() {
        return toPx;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$2(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_uni_popup = resolveEasycom(vue.resolveDynamicComponent("uni-popup"), __easycom_0$3);
    return vue.openBlock(), vue.createElementBlock("view", { class: "submission-page" }, [
      vue.createElementVNode("scroll-view", {
        class: "scroll-body",
        "scroll-y": "",
        "show-scrollbar": false
      }, [
        $setup.loading ? (vue.openBlock(), vue.createElementBlock("view", {
          key: 0,
          class: "state-box"
        }, [
          vue.createElementVNode("text", { class: "state-title" }, "加载中…"),
          vue.createElementVNode("text", { class: "state-desc" }, "正在获取投递详情")
        ])) : !$setup.queueInfo ? (vue.openBlock(), vue.createElementBlock("view", { key: 1 }, [
          vue.createElementVNode("view", { class: "state-box state-error" }, [
            vue.createElementVNode("text", { class: "state-title" }, "获取失败"),
            vue.createElementVNode(
              "text",
              { class: "state-desc" },
              vue.toDisplayString($setup.errorMsg || "暂时无法获取投递信息"),
              1
              /* TEXT */
            ),
            vue.createElementVNode("button", {
              class: "btn-retry",
              onClick: $setup.reload
            }, "重试")
          ])
        ])) : (vue.openBlock(), vue.createElementBlock("view", {
          key: 2,
          class: "content"
        }, [
          vue.createElementVNode("view", { class: "card status-card" }, [
            vue.createElementVNode("view", { class: "status-row" }, [
              vue.createElementVNode(
                "view",
                {
                  class: vue.normalizeClass(["status-chip", "s-" + $setup.queueInfo.status])
                },
                [
                  vue.createElementVNode(
                    "text",
                    { class: "status-text" },
                    vue.toDisplayString($setup.queueInfo.status_text || "未知状态"),
                    1
                    /* TEXT */
                  )
                ],
                2
                /* CLASS */
              ),
              $setup.queueInfo.mode === 1 ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 0,
                class: "status-mode"
              }, "模式：普通队列")) : $setup.queueInfo.mode === 2 ? (vue.openBlock(), vue.createElementBlock("text", {
                key: 1,
                class: "status-mode"
              }, "模式：手速队列")) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("view", { class: "status-row" }, [
              vue.createElementVNode("text", { class: "status-label" }, "排队位置"),
              vue.createElementVNode("text", { class: "status-value" }, [
                $setup.queueInfo.ahead_count > 0 ? (vue.openBlock(), vue.createElementBlock(
                  "text",
                  { key: 0 },
                  " 前面还有 " + vue.toDisplayString($setup.queueInfo.ahead_count) + " 位投递 ",
                  1
                  /* TEXT */
                )) : $setup.queueInfo.status === 0 ? (vue.openBlock(), vue.createElementBlock("text", { key: 1 }, "当前在首位 / 未统计")) : (vue.openBlock(), vue.createElementBlock("text", { key: 2 }, "当前不在排队队列"))
              ])
            ]),
            vue.createElementVNode("view", { class: "status-row" }, [
              vue.createElementVNode("text", { class: "status-label" }, "本次投递 ID"),
              vue.createElementVNode(
                "text",
                { class: "status-value" },
                "#" + vue.toDisplayString($setup.queueInfo.submission_id),
                1
                /* TEXT */
              )
            ]),
            vue.createElementVNode("view", { class: "status-row" }, [
              vue.createElementVNode("text", { class: "status-label" }, "接单计划 ID"),
              vue.createElementVNode(
                "text",
                { class: "status-value" },
                "#" + vue.toDisplayString($setup.queueInfo.plan_id),
                1
                /* TEXT */
              )
            ]),
            $setup.queueInfo.premium_queue_status ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "status-row"
            }, [
              vue.createElementVNode("text", { class: "status-label" }, "钞队列"),
              vue.createElementVNode(
                "text",
                { class: "status-value" },
                vue.toDisplayString($setup.queueInfo.premium_queue_status === 1 ? "已开启" : "未开启"),
                1
                /* TEXT */
              )
            ])) : vue.createCommentVNode("v-if", true),
            vue.createElementVNode("view", { class: "status-actions" }, [
              vue.createElementVNode(
                "button",
                {
                  class: "btn-primary",
                  onClick: $setup.handleChatWithSeller
                },
                vue.toDisplayString($setup.isBuyer ? "与卖家发起会话" : "与买家发起会话"),
                1
                /* TEXT */
              ),
              $setup.queueInfo.status === 2 ? (vue.openBlock(), vue.createElementBlock("button", {
                key: 0,
                class: "btn-secondary",
                onClick: $setup.handleConfirmSubmission
              }, " 确认订单（状态 2 → 3） ")) : vue.createCommentVNode("v-if", true)
            ])
          ]),
          vue.createElementVNode("view", { class: "card items-card" }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              vue.createElementVNode(
                "text",
                { class: "card-title" },
                "作品列表（" + vue.toDisplayString($setup.items.length) + "）",
                1
                /* TEXT */
              )
            ]),
            !$setup.items.length ? (vue.openBlock(), vue.createElementBlock("view", {
              key: 0,
              class: "state-box",
              style: { "margin-top": "0" }
            }, [
              vue.createElementVNode("text", { class: "state-title" }, "还没有作品"),
              vue.createElementVNode("text", { class: "state-desc" }, "可以稍后再为本次投递添加作品")
            ])) : vue.createCommentVNode("v-if", true),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.items, (item) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: item.id,
                  class: "item-row"
                }, [
                  vue.createElementVNode("view", { class: "item-main" }, [
                    $setup.getFirstImage(item) ? (vue.openBlock(), vue.createElementBlock("view", {
                      key: 0,
                      class: "item-cover-wrap"
                    }, [
                      vue.createElementVNode("image", {
                        class: "item-cover",
                        src: $setup.getFirstImage(item),
                        mode: "aspectFill"
                      }, null, 8, ["src"])
                    ])) : vue.createCommentVNode("v-if", true),
                    vue.createElementVNode("view", { class: "item-body" }, [
                      vue.createElementVNode("view", { class: "item-title-row" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "item-title" },
                          vue.toDisplayString((item.work_subject || "").trim() || "未命名作品"),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "item-meta-row" }, [
                        item.size ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 0,
                            class: "item-meta"
                          },
                          "尺寸：" + vue.toDisplayString(item.size),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true),
                        item.tier_title ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 1,
                            class: "item-meta"
                          },
                          "档位：" + vue.toDisplayString(item.tier_title),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true),
                        $setup.getAddonsTitles(item) ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 2,
                            class: "item-meta"
                          },
                          " 加购：" + vue.toDisplayString($setup.getAddonsTitles(item)),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ]),
                      vue.createElementVNode("view", { class: "item-meta-row" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "item-meta" },
                          " 子单状态：" + vue.toDisplayString($setup.formatItemStatus(item.status)),
                          1
                          /* TEXT */
                        )
                      ]),
                      vue.createElementVNode("view", { class: "item-meta-row" }, [
                        vue.createElementVNode(
                          "text",
                          { class: "item-meta" },
                          " 当前金额：¥ " + vue.toDisplayString($setup.formatPrice($setup.calcItemTotal(item))),
                          1
                          /* TEXT */
                        ),
                        Number(item.adjust_price) !== 0 ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 0,
                            class: "item-meta adjust"
                          },
                          " 调价差额：" + vue.toDisplayString(item.adjust_price > 0 ? "+" : "") + vue.toDisplayString($setup.formatPrice(item.adjust_price)),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ])
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "item-actions" }, [
                    vue.createElementVNode("button", {
                      class: "btn-mini",
                      onClick: ($event) => $setup.openChangePricePanel(item)
                    }, " 修改金额 ", 8, ["onClick"]),
                    vue.createElementVNode("button", {
                      class: "btn-mini outline",
                      onClick: ($event) => $setup.goStepEditor(item)
                    }, " 提交节点状态 ", 8, ["onClick"])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ]),
          $setup.draftItems.length ? (vue.openBlock(), vue.createElementBlock("view", {
            key: 0,
            class: "card draft-card"
          }, [
            vue.createElementVNode("view", { class: "card-header" }, [
              vue.createElementVNode("text", { class: "card-title" }, "草稿作品（未挂到投递）")
            ]),
            (vue.openBlock(true), vue.createElementBlock(
              vue.Fragment,
              null,
              vue.renderList($setup.draftItems, (d2) => {
                return vue.openBlock(), vue.createElementBlock("view", {
                  key: d2.id,
                  class: "item-row draft-item"
                }, [
                  vue.createElementVNode("view", { class: "item-main" }, [
                    vue.createElementVNode("view", { class: "item-body" }, [
                      vue.createElementVNode(
                        "text",
                        { class: "item-title" },
                        vue.toDisplayString((d2.work_subject || "").trim() || "未命名作品"),
                        1
                        /* TEXT */
                      ),
                      vue.createElementVNode("view", { class: "item-meta-row" }, [
                        d2.size ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 0,
                            class: "item-meta"
                          },
                          "尺寸：" + vue.toDisplayString(d2.size),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true),
                        d2.tier_title ? (vue.openBlock(), vue.createElementBlock(
                          "text",
                          {
                            key: 1,
                            class: "item-meta"
                          },
                          "档位：" + vue.toDisplayString(d2.tier_title),
                          1
                          /* TEXT */
                        )) : vue.createCommentVNode("v-if", true)
                      ])
                    ])
                  ])
                ]);
              }),
              128
              /* KEYED_FRAGMENT */
            ))
          ])) : vue.createCommentVNode("v-if", true),
          vue.createElementVNode(
            "view",
            {
              style: vue.normalizeStyle({ height: $setup.footerPlaceholderHeight })
            },
            null,
            4
            /* STYLE */
          )
        ]))
      ]),
      vue.createElementVNode(
        "view",
        {
          class: "bottom-bar",
          style: vue.normalizeStyle({ paddingBottom: $setup.footerPadding })
        },
        [
          vue.createElementVNode(
            "button",
            {
              class: "bottom-btn",
              onClick: $setup.handleChatWithSeller
            },
            vue.toDisplayString($setup.isBuyer ? "与卖家发起会话" : "与买家发起会话"),
            1
            /* TEXT */
          )
        ],
        4
        /* STYLE */
      ),
      vue.createVNode(
        _component_uni_popup,
        {
          ref: "pricePopupRef",
          type: "bottom",
          "mask-click": true,
          onChange: $setup.onPricePopupChange
        },
        {
          default: vue.withCtx(() => [
            vue.createElementVNode(
              "view",
              {
                class: "price-sheet",
                style: vue.normalizeStyle({ paddingBottom: $setup.footerPadding })
              },
              [
                vue.createElementVNode("view", { class: "price-header" }, [
                  vue.createElementVNode("text", { class: "price-title" }, "修改金额"),
                  vue.createElementVNode("text", {
                    class: "price-close",
                    onClick: $setup.closePricePanel
                  }, "关闭")
                ]),
                $setup.editingItem ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 0,
                  class: "price-body"
                }, [
                  vue.createElementVNode("view", { class: "price-row" }, [
                    vue.createElementVNode("text", { class: "price-label" }, "作品"),
                    vue.createElementVNode(
                      "text",
                      { class: "price-value" },
                      vue.toDisplayString(($setup.editingItem.work_subject || "").trim() || "未命名作品"),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "price-row" }, [
                    vue.createElementVNode("text", { class: "price-label" }, "当前金额"),
                    vue.createElementVNode(
                      "text",
                      { class: "price-value" },
                      " ¥ " + vue.toDisplayString($setup.formatPrice($setup.calcItemTotal($setup.editingItem))),
                      1
                      /* TEXT */
                    )
                  ]),
                  vue.createElementVNode("view", { class: "price-row" }, [
                    vue.createElementVNode("text", { class: "price-label" }, "新总金额"),
                    vue.withDirectives(vue.createElementVNode(
                      "input",
                      {
                        type: "digit",
                        class: "price-input",
                        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.priceInput = $event),
                        placeholder: "请输入新的总金额（整数或小数）"
                      },
                      null,
                      512
                      /* NEED_PATCH */
                    ), [
                      [vue.vModelText, $setup.priceInput]
                    ])
                  ]),
                  vue.createElementVNode("view", { class: "price-row-column" }, [
                    vue.createElementVNode("text", { class: "price-label" }, "调价说明（选填）"),
                    vue.withDirectives(vue.createElementVNode(
                      "textarea",
                      {
                        class: "price-textarea",
                        "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.reasonInput = $event),
                        placeholder: "该说明会发送给对方，建议简单说明调价原因",
                        "auto-height": "",
                        maxlength: "200"
                      },
                      null,
                      512
                      /* NEED_PATCH */
                    ), [
                      [vue.vModelText, $setup.reasonInput]
                    ])
                  ])
                ])) : vue.createCommentVNode("v-if", true),
                vue.createElementVNode("view", { class: "price-footer" }, [
                  vue.createElementVNode("button", {
                    class: "price-btn",
                    onClick: $setup.submitChangePrice
                  }, " 提交修改金额 ")
                ])
              ],
              4
              /* STYLE */
            )
          ]),
          _: 1
          /* STABLE */
        },
        512
        /* NEED_PATCH */
      )
    ]);
  }
  const PkgCreatorCreatorOrderReceivedDetailReceivedDetail = /* @__PURE__ */ _export_sfc(_sfc_main$3, [["render", _sfc_render$2], ["__scopeId", "data-v-60e1f31e"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-creator/creator_order/received-detail/received-detail.vue"]]);
  function searchBrands(q2) {
    return uni.request({
      url: `${websiteUrl.value}/search-brand?search=${encodeURIComponent(q2)}`,
      method: "GET"
    });
  }
  function getBrandInfo(id) {
    return uni.request({
      url: `${websiteUrl.value}/brand-info?id=${id}`,
      method: "GET"
    });
  }
  function searchGoods(keyword, brandId) {
    return uni.request({
      url: `${websiteUrl.value}/search-goods`,
      method: "GET",
      // 用 data 让 uni 自动拼 query，避免 [object Object]
      data: { search: keyword, brand_id: brandId }
    });
  }
  function getGoodsDetail(id) {
    return uni.request({
      url: `${websiteUrl.value}/goods?id=${id}`,
      method: "GET"
    });
  }
  const _sfc_main$2 = {
    __name: "brand-pick",
    setup(__props, { expose: __expose }) {
      var _a, _b, _c;
      __expose();
      const brand = vue.ref({
        id: 0,
        brand_name: "",
        logo_image: ""
      });
      const keyword = vue.ref("");
      const results = vue.ref([]);
      const focusing = vue.ref(false);
      const liftPx = vue.ref(0);
      const inputFocus = vue.ref(false);
      const parentEC = (_c = (_b = (_a = vue.getCurrentInstance()) == null ? void 0 : _a.proxy) == null ? void 0 : _b.getOpenerEventChannel) == null ? void 0 : _c.call(_b);
      function focusInput() {
        inputFocus.value = false;
        vue.nextTick(() => {
          inputFocus.value = true;
        });
      }
      function onFocus() {
        focusing.value = true;
        try {
          const px = typeof (uni == null ? void 0 : uni.upx2px) === "function" ? uni.upx2px(200) : 200;
          liftPx.value = px;
        } catch (e2) {
          liftPx.value = 200;
        }
      }
      function onBlur() {
        focusing.value = false;
        liftPx.value = 0;
        inputFocus.value = false;
      }
      function doSearch() {
        fetchList();
      }
      function onInput(e2) {
        var _a2;
        keyword.value = typeof e2 === "string" ? e2 : ((_a2 = e2 == null ? void 0 : e2.detail) == null ? void 0 : _a2.value) || "";
        if (!keyword.value.trim()) {
          results.value = [];
          brand.value = { id: 0, brand_name: "", logo_image: "" };
          return;
        }
        fetchList();
      }
      let timer = null;
      function fetchList() {
        clearTimeout(timer);
        timer = setTimeout(async () => {
          const { data } = await searchBrands(keyword.value.trim());
          results.value = (data == null ? void 0 : data.status) === "success" ? (data == null ? void 0 : data.data) || [] : [];
        }, 150);
      }
      vue.onUnmounted(() => clearTimeout(timer));
      async function pickBrand(item) {
        keyword.value = item.name;
        results.value = [];
        const { data } = await getBrandInfo(item.id);
        const b2 = (data == null ? void 0 : data.data) || {};
        brand.value = {
          id: b2.id,
          brand_name: b2.brand_name || item.name,
          logo_image: b2.logo_image || ""
        };
      }
      function goNext() {
        uni.navigateTo({
          url: `/pkg-common/goods-pick/goods-pick?brand_id=${brand.value.id}&brand_name=${encodeURIComponent(brand.value.brand_name)}&brand_logo=${encodeURIComponent(brand.value.logo_image || "")}`,
          "associate:done": (payload) => {
            parentEC && parentEC.emit("associate:done", payload);
            uni.navigateBack();
          }
        });
      }
      const __returned__ = { brand, keyword, results, focusing, liftPx, inputFocus, parentEC, focusInput, onFocus, onBlur, doSearch, onInput, get timer() {
        return timer;
      }, set timer(v2) {
        timer = v2;
      }, fetchList, pickBrand, goNext, ref: vue.ref, getCurrentInstance: vue.getCurrentInstance, onUnmounted: vue.onUnmounted, nextTick: vue.nextTick, get searchBrands() {
        return searchBrands;
      }, get getBrandInfo() {
        return getBrandInfo;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render$1(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode(
          "view",
          {
            class: "page",
            style: vue.normalizeStyle({ transform: `translateY(-${$setup.liftPx}px)` })
          },
          [
            vue.createElementVNode("view", {
              class: "avatar-wrap",
              onClick: $setup.focusInput
            }, [
              $setup.brand.logo_image ? (vue.openBlock(), vue.createElementBlock("image", {
                key: 0,
                src: $setup.brand.logo_image,
                class: "avatar",
                mode: "aspectFill",
                "lazy-load": true
              }, null, 8, ["src"])) : (vue.openBlock(), vue.createElementBlock("view", {
                key: 1,
                class: "avatar ph"
              }, [
                vue.createElementVNode("text", { class: "ph-text" }, "待选择")
              ]))
            ]),
            vue.createElementVNode("view", { class: "field" }, [
              vue.createElementVNode("view", { class: "input-row" }, [
                vue.withDirectives(vue.createElementVNode("input", {
                  "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.keyword = $event),
                  focus: $setup.inputFocus,
                  placeholder: "请输入品牌名称",
                  maxlength: 20,
                  "confirm-type": "search",
                  onConfirm: $setup.doSearch,
                  onInput: $setup.onInput,
                  onFocus: $setup.onFocus,
                  onBlur: $setup.onBlur,
                  "adjust-position": false,
                  "cursor-spacing": 0
                }, null, 40, ["focus"]), [
                  [vue.vModelText, $setup.keyword]
                ]),
                vue.createElementVNode(
                  "text",
                  { class: "count" },
                  vue.toDisplayString(($setup.keyword || "").length) + "/20",
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "hint" }, "（请输入品牌名称）"),
              $setup.results.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
                key: 0,
                class: "assoc",
                "scroll-y": ""
              }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.results, (it2) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      class: "item",
                      key: it2.id,
                      onClick: ($event) => $setup.pickBrand(it2)
                    }, vue.toDisplayString(it2.name), 9, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])) : vue.createCommentVNode("v-if", true)
            ]),
            vue.createElementVNode("button", {
              class: "primary",
              disabled: !$setup.brand.id,
              onClick: $setup.goNext
            }, "下一步", 8, ["disabled"])
          ],
          4
          /* STYLE */
        )
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgCommonBrandPickBrandPick = /* @__PURE__ */ _export_sfc(_sfc_main$2, [["render", _sfc_render$1], ["__scopeId", "data-v-4af2003c"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-common/brand-pick/brand-pick.vue"]]);
  const _sfc_main$1 = {
    __name: "goods-pick",
    setup(__props, { expose: __expose }) {
      __expose();
      const brand = vue.ref({ id: 0, brand_name: "", logo_image: "" });
      const goods = vue.ref({ id: 0, name: "", type: "", cover: "", price: 0, currency: "" });
      const keyword = vue.ref("");
      const results = vue.ref([]);
      const hasGoods = vue.computed(() => !!goods.value.id);
      const liftPx = vue.ref(0);
      const heroHeight = vue.computed(() => hasGoods.value ? "650rpx" : "400rpx");
      onLoad((opts) => {
        brand.value.id = Number((opts == null ? void 0 : opts.brand_id) || 0);
        brand.value.brand_name = decodeURIComponent((opts == null ? void 0 : opts.brand_name) || "");
        brand.value.logo_image = decodeURIComponent((opts == null ? void 0 : opts.brand_logo) || "");
      });
      function onFocus() {
        try {
          liftPx.value = typeof (uni == null ? void 0 : uni.upx2px) === "function" ? uni.upx2px(300) : 300;
        } catch (_2) {
          liftPx.value = 300;
        }
      }
      function onBlur() {
        liftPx.value = 0;
      }
      let timer = null;
      function doSearch() {
        fetchList();
      }
      function onInput(e2) {
        var _a;
        keyword.value = typeof e2 === "string" ? e2 : ((_a = e2 == null ? void 0 : e2.detail) == null ? void 0 : _a.value) || "";
        if (!keyword.value.trim()) {
          results.value = [];
          return;
        }
        fetchList();
      }
      function fetchList() {
        clearTimeout(timer);
        timer = setTimeout(async () => {
          if (!brand.value.id)
            return;
          const { data } = await searchGoods(keyword.value.trim(), brand.value.id);
          results.value = (data == null ? void 0 : data.status) === "success" ? (data == null ? void 0 : data.data) || [] : [];
        }, 150);
      }
      vue.onUnmounted(() => clearTimeout(timer));
      async function pickGoods(item) {
        keyword.value = item.name;
        results.value = [];
        const { data } = await getGoodsDetail(item.id);
        const g2 = (data == null ? void 0 : data.data) || {};
        const cover = g2.goods_image || (Array.isArray(g2.goods_images) && g2.goods_images.length ? g2.goods_images[0] : "");
        goods.value = {
          id: g2.id,
          name: g2.name || item.name,
          type: g2.type || "",
          cover,
          price: g2.total_amount || 0,
          currency: g2.currency || ""
        };
      }
      function goBack() {
        uni.navigateBack();
      }
      function confirm() {
        if (!goods.value.id)
          return;
        const payload = {
          brand: {
            id: brand.value.id,
            brand_name: brand.value.brand_name,
            logo_image: brand.value.logo_image
          },
          goods: {
            id: goods.value.id,
            name: goods.value.name,
            type: goods.value.type,
            cover: goods.value.cover,
            price: goods.value.price,
            currency: goods.value.currency
          }
        };
        uni.$emit("associate:done", payload);
        uni.navigateBack({ delta: 2 });
      }
      const __returned__ = { brand, goods, keyword, results, hasGoods, liftPx, heroHeight, onFocus, onBlur, get timer() {
        return timer;
      }, set timer(v2) {
        timer = v2;
      }, doSearch, onInput, fetchList, pickGoods, goBack, confirm, ref: vue.ref, computed: vue.computed, onUnmounted: vue.onUnmounted, get onLoad() {
        return onLoad;
      }, get searchGoods() {
        return searchGoods;
      }, get getGoodsDetail() {
        return getGoodsDetail;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
    const _component_view_logs = resolveEasycom(vue.resolveDynamicComponent("view-logs"), __easycom_0$9);
    return vue.openBlock(), vue.createElementBlock(
      vue.Fragment,
      null,
      [
        vue.createVNode(_component_view_logs),
        vue.createElementVNode(
          "view",
          {
            class: "page",
            style: vue.normalizeStyle({ transform: `translateY(-${$setup.liftPx}px)` })
          },
          [
            vue.createElementVNode(
              "view",
              {
                class: "hero",
                style: vue.normalizeStyle({ height: $setup.heroHeight })
              },
              [
                $setup.hasGoods ? (vue.openBlock(), vue.createElementBlock("image", {
                  key: 0,
                  class: "hero-bg",
                  src: $setup.goods.cover,
                  mode: "aspectFill"
                }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                $setup.hasGoods ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 1,
                  class: "hero-fade"
                })) : vue.createCommentVNode("v-if", true),
                !$setup.hasGoods ? (vue.openBlock(), vue.createElementBlock("view", {
                  key: 2,
                  class: "brand-center"
                }, [
                  $setup.brand.logo_image ? (vue.openBlock(), vue.createElementBlock("image", {
                    key: 0,
                    src: $setup.brand.logo_image,
                    class: "brand-logo",
                    mode: "aspectFill"
                  }, null, 8, ["src"])) : vue.createCommentVNode("v-if", true),
                  vue.createElementVNode(
                    "view",
                    { class: "brand-name" },
                    vue.toDisplayString($setup.brand.brand_name || "未选择品牌"),
                    1
                    /* TEXT */
                  ),
                  vue.createElementVNode("view", { class: "brand-sub" }, "仅在该品牌下搜索娃头")
                ])) : vue.createCommentVNode("v-if", true)
              ],
              4
              /* STYLE */
            ),
            vue.createElementVNode("view", { class: "field" }, [
              vue.createElementVNode("view", { class: "input-row" }, [
                vue.withDirectives(vue.createElementVNode(
                  "input",
                  {
                    "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.keyword = $event),
                    placeholder: "请输入娃物名称",
                    maxlength: 30,
                    "confirm-type": "search",
                    onConfirm: $setup.doSearch,
                    onInput: $setup.onInput,
                    onFocus: $setup.onFocus,
                    onBlur: $setup.onBlur,
                    "adjust-position": false,
                    "cursor-spacing": 0
                  },
                  null,
                  544
                  /* NEED_HYDRATION, NEED_PATCH */
                ), [
                  [vue.vModelText, $setup.keyword]
                ]),
                vue.createElementVNode(
                  "text",
                  { class: "count" },
                  vue.toDisplayString(($setup.keyword || "").length) + "/30",
                  1
                  /* TEXT */
                )
              ]),
              vue.createElementVNode("view", { class: "hint" }, "（仅搜索该品牌下的商品）"),
              $setup.results.length ? (vue.openBlock(), vue.createElementBlock("scroll-view", {
                key: 0,
                class: "assoc",
                "scroll-y": ""
              }, [
                (vue.openBlock(true), vue.createElementBlock(
                  vue.Fragment,
                  null,
                  vue.renderList($setup.results, (it2) => {
                    return vue.openBlock(), vue.createElementBlock("view", {
                      class: "item",
                      key: it2.id,
                      onClick: ($event) => $setup.pickGoods(it2)
                    }, [
                      vue.createElementVNode(
                        "text",
                        { class: "gname" },
                        vue.toDisplayString(it2.name),
                        1
                        /* TEXT */
                      )
                    ], 8, ["onClick"]);
                  }),
                  128
                  /* KEYED_FRAGMENT */
                ))
              ])) : vue.createCommentVNode("v-if", true),
              vue.createElementVNode("view", { class: "actions" }, [
                vue.createElementVNode("button", {
                  class: "ghost",
                  onClick: $setup.goBack
                }, "返回上一步"),
                vue.createElementVNode("button", {
                  class: "primary",
                  disabled: !$setup.goods.id,
                  onClick: $setup.confirm
                }, "确认", 8, ["disabled"])
              ])
            ])
          ],
          4
          /* STYLE */
        )
      ],
      64
      /* STABLE_FRAGMENT */
    );
  }
  const PkgCommonGoodsPickGoodsPick = /* @__PURE__ */ _export_sfc(_sfc_main$1, [["render", _sfc_render], ["__scopeId", "data-v-f003b2cf"], ["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/pkg-common/goods-pick/goods-pick.vue"]]);
  __definePage("pages/index/index", PagesIndexIndex);
  __definePage("pages/homepage/homepage", PagesHomepageHomepage);
  __definePage("pages/stock/stock", PagesStockStock);
  __definePage("pages/mine/mine", PagesMineMine);
  __definePage("pages/watch_demo/watch_demo", PagesWatchDemoWatchDemo);
  __definePage("pages/goods/goods", PagesGoodsGoods);
  __definePage("pages/brand/brand", PagesBrandBrand);
  __definePage("pages/calendar/calendar", PagesCalendarCalendar);
  __definePage("pages/summary/summary", PagesSummarySummary);
  __definePage("pages/pop_croper/pop_croper", PagesPopCroperPopCroper);
  __definePage("pages/setting/setting", PagesSettingSetting);
  __definePage("pages/setting/password/password", PagesSettingPasswordPassword);
  __definePage("pages/setting/tel_phone/tel_phone", PagesSettingTelPhoneTelPhone);
  __definePage("pages/collocation/collocation", PagesCollocationCollocation);
  __definePage("pages/collocation_share/collocation_share", PagesCollocationShareCollocationShare);
  __definePage("pages/user_page/user_page", PagesUserPageUserPage);
  __definePage("pages/message_list/message_list", PagesMessageListMessageList);
  __definePage("pages/message_info/message_info", PagesMessageInfoMessageInfo);
  __definePage("pages/register/register", PagesRegisterRegister);
  __definePage("pages/user_like/user_like", PagesUserLikeUserLike);
  __definePage("pages/collocation_square/collocation_square", PagesCollocationSquareCollocationSquare);
  __definePage("pages/sale_news/sale_news", PagesSaleNewsSaleNews);
  __definePage("pages/treehole_publish/treehole_publish", PagesTreeholePublishTreeholePublish);
  __definePage("pages/treehole_detail/treehole_detail", PagesTreeholeDetailTreeholeDetail);
  __definePage("pages/reset_password/reset_password", PagesResetPasswordResetPassword);
  __definePage("pages/setting/username/username", PagesSettingUsernameUsername);
  __definePage("pages/my_comment/my_comment", PagesMyCommentMyComment);
  __definePage("pages/my_collocation/my_collocation", PagesMyCollocationMyCollocation);
  __definePage("pages/article_detail/article_detail", PagesArticleDetailArticleDetail);
  __definePage("pages/loadding/loadding", PagesLoaddingLoadding);
  __definePage("pages/private/private", PagesPrivatePrivate);
  __definePage("pages/permission/permission", PagesPermissionPermission);
  __definePage("pages/drag-image-test/drag-image-test", PagesDragImageTestDragImageTest);
  __definePage("pages/artist_info/artist_info", PagesArtistInfoArtistInfo);
  __definePage("pages/artwork/artwork", PagesArtworkArtwork);
  __definePage("pages/recent-feed/recent-feed", PagesRecentFeedRecentFeed);
  __definePage("pages/search/search", PagesSearchSearch);
  __definePage("pages/artist_info/bjd_faceup_artist", PagesArtistInfoBjdFaceupArtist);
  __definePage("pkg-im/message_list/message_list", PkgImMessageListMessageList);
  __definePage("pkg-im/chat/chat", PkgImChatChat);
  __definePage("pkg-stock/stock/account_book_form/account_book_form", PkgStockStockAccountBookFormAccountBookForm);
  __definePage("pkg-stock/stock/showcase_form/showcase_form", PkgStockStockShowcaseFormShowcaseForm);
  __definePage("pkg-stock/stock/bill_form/bill_form", PkgStockStockBillFormBillForm);
  __definePage("pkg-stock/account_book_preview/account_book_preview", PkgStockAccountBookPreviewAccountBookPreview);
  __definePage("pkg-stock/stock-myitems-search/stock-myitems-search", PkgStockStockMyitemsSearchStockMyitemsSearch);
  __definePage("pkg-creator/creator_base/creator_base", PkgCreatorCreatorBaseCreatorBase);
  __definePage("pkg-creator/creator_base/brand_edit/brand_edit", PkgCreatorCreatorBaseBrandEditBrandEdit);
  __definePage("pkg-creator/creator_base/goods_list/goods_list", PkgCreatorCreatorBaseGoodsListGoodsList);
  __definePage("pkg-creator/creator_base/goods_editor/goods_editor", PkgCreatorCreatorBaseGoodsEditorGoodsEditor);
  __definePage("pkg-creator/creator_base/collocation_list/collocation_list", PkgCreatorCreatorBaseCollocationListCollocationList);
  __definePage("pkg-creator/creator_base/artist_info/artist_info", PkgCreatorCreatorBaseArtistInfoArtistInfo);
  __definePage("pkg-creator/creator_base/artist_setting/artist_setting", PkgCreatorCreatorBaseArtistSettingArtistSetting);
  __definePage("pkg-creator/creator_base/set_showcase/set_showcase", PkgCreatorCreatorBaseSetShowcaseSetShowcase);
  __definePage("pkg-creator/creator_base/faceup_editor/faceup_editor", PkgCreatorCreatorBaseFaceupEditorFaceupEditor);
  __definePage("pkg-creator/creator_base/order_plane/order_plane", PkgCreatorCreatorBaseOrderPlaneOrderPlane);
  __definePage("pkg-creator/creator_base/news/news", PkgCreatorCreatorBaseNewsNews);
  __definePage("pkg-creator/creator_base/set_hair_showcase/set_hair_showcase", PkgCreatorCreatorBaseSetHairShowcaseSetHairShowcase);
  __definePage("pkg-creator/creator_base/order_plane/form-artist", PkgCreatorCreatorBaseOrderPlaneFormArtist);
  __definePage("pkg-creator/creator_base/order_plane/form-wig", PkgCreatorCreatorBaseOrderPlaneFormWig);
  __definePage("pkg-creator/creator_order/plane_detail/plane_detail", PkgCreatorCreatorOrderPlaneDetailPlaneDetail);
  __definePage("pkg-creator/creator_order/my_submissions/my_submissions", PkgCreatorCreatorOrderMySubmissionsMySubmissions);
  __definePage("pkg-creator/creator_order/submission_detail/submission_detail", PkgCreatorCreatorOrderSubmissionDetailSubmissionDetail);
  __definePage("pkg-creator/creator_order/submission_item_form/submission_item_form", PkgCreatorCreatorOrderSubmissionItemFormSubmissionItemForm);
  __definePage("pkg-creator/creator_order/received-list/received-list", PkgCreatorCreatorOrderReceivedListReceivedList);
  __definePage("pkg-creator/creator_order/received-detail/received-detail", PkgCreatorCreatorOrderReceivedDetailReceivedDetail);
  __definePage("pkg-common/brand-pick/brand-pick", PkgCommonBrandPickBrandPick);
  __definePage("pkg-common/goods-pick/goods-pick", PkgCommonGoodsPickGoodsPick);
  function initPushNotification() {
    if (typeof plus !== "undefined" && plus.push) {
      plus.globalEvent.addEventListener("newPath", ({ path }) => {
        if (!path) {
          return;
        }
        const pages2 = getCurrentPages();
        const currentPage = pages2[pages2.length - 1];
        if (currentPage && currentPage.$page && currentPage.$page.fullPath === path) {
          return;
        }
        uni.navigateTo({
          url: path,
          fail(res) {
            if (res.errMsg.indexOf("tabbar") > -1) {
              uni.switchTab({
                url: path,
                fail(res2) {
                  console.error(res2.errMsg);
                }
              });
            } else {
              console.error(res.errMsg);
            }
          }
        });
      });
    }
  }
  uni.invokePushCallback({
    type: "enabled",
    offline: true
  });
  Promise.resolve().then(() => {
    initPushNotification();
    plus.push.setAutoNotification && plus.push.setAutoNotification(false);
  });
  const _sfc_main = {
    __name: "App",
    setup(__props, { expose: __expose }) {
      __expose();
      onLaunch(() => {
        formatAppLog("log", "at App.vue:8", "App Launch");
        initLoginState();
      });
      onShow(() => {
        formatAppLog("log", "at App.vue:15", "App Show");
        setTimeout(() => {
          formatAppLog("log", "at App.vue:17", "开始加载字体...");
          loadFonts();
          formatAppLog("log", "at App.vue:19", "结束加载字体");
        }, 2e3);
      });
      onHide(() => {
        formatAppLog("log", "at App.vue:24", "App Hide");
      });
      function loadFonts() {
        uni.loadFontFace({
          family: "cutefont",
          // 根据平台选择路径，或者使用同一路径
          source: 'url("https://images1.fantuanpu.com/font/ry-super-less-rokk.ttf")',
          global: true,
          success() {
            formatAppLog("log", "at App.vue:35", "字体cutefont加载成功");
          },
          fail(err) {
            formatAppLog("log", "at App.vue:38", "字体cutefont加载失败", err);
          }
        });
        uni.loadFontFace({
          family: "alimamamshuhei",
          source: 'url("https://images1.fantuanpu.com/font/AlimamaShuHeiTi-Bold.ttf")',
          global: true,
          success() {
            formatAppLog("log", "at App.vue:48", "字体alimamamshuhei加载成功");
          },
          fail(err) {
            formatAppLog("log", "at App.vue:51", "字体alimamamshuhei加载失败", err);
          }
        });
      }
      const __returned__ = { loadFonts, get onLaunch() {
        return onLaunch;
      }, get onShow() {
        return onShow;
      }, get onHide() {
        return onHide;
      }, get initLoginState() {
        return initLoginState;
      } };
      Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
      return __returned__;
    }
  };
  const App = /* @__PURE__ */ _export_sfc(_sfc_main, [["__file", "/Users/sapphirell/Documents/git/dogdogdoll-uniapp/App.vue"]]);
  function createApp() {
    const app = vue.createVueApp(App);
    return {
      app
    };
  }
  const { app: __app__, Vuex: __Vuex__, Pinia: __Pinia__ } = createApp();
  uni.Vuex = __Vuex__;
  uni.Pinia = __Pinia__;
  __app__.provide("__globalStyles", __uniConfig.styles);
  __app__._component.mpType = "app";
  __app__._component.render = () => {
  };
  __app__.mount("#app");
})(Vue);
